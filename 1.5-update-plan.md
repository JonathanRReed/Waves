# Waves 1.5 Master Update Program

**Status:** Approved for implementation  
**Plan date:** 2026-08-08  
**Release target:** Waves 1.5.0, build 13  
**Baseline:** `main` and `origin/main` at `64bad3baa901d44d02a64023a9c72acabf0557ea` (`v1.4.4`)  
**Implementation branch:** `codex/waves-1.5`  
**Release authority:** Maintainer Mac for Waves 1.5 signing, notarization, and publication  

## Review Gate

This document is the source of truth for the Waves 1.5 program. No production code, test, workflow, packaging, site, Homebrew, plugin, commit, push, tag, deployment, or release work may begin until Jonathan reviews this rewrite and changes the status above to **Approved for implementation**.

After approval, every required checkbox is release-blocking unless this document records a replacement that solves the same underlying problem more safely. A failed gate stops the tranche. No failing or skipped gate may be explained away after publication.

## Product and Release Contract

Waves 1.5 is one consolidated reliability release. There will be no interim 1.4.5 release. It carries the confirmed patch fixes, complete repository cleanup, Wave Link coexistence, Stream Deck compatibility, hardened automation, and release infrastructure into one candidate.

The release must preserve these boundaries:

- Waves remains a native SwiftPM macOS app supporting macOS 14.2 or later.
- Release binaries remain universal `arm64` and `x86_64`.
- `ControlProtocol.version` remains `1`; no existing command or response shape may break.
- Persistence remains schema 1 and additive.
- No new production dependency may be added without separate approval.
- App Intents remain outside 1.5. Any later App Intents adapter must use the same AppStore intent boundary as the UI and existing automation.
- The Stream Deck plugin remains independently versioned and released from `/Users/jonathanreed/Downloads/waves-streamdeck`.
- Real Stream Deck and Wave Link compatibility is still a hard Waves 1.5 release gate.
- Audio is never recorded, transmitted, or used for telemetry.

## Definition of Done

Waves 1.5 is complete only when all of the following are true:

- [ ] `DONE-001` Every implementation checkbox in this document is complete or has an evidence-backed safe replacement recorded in the historical ledger.
- [ ] `DONE-002` There are zero unresolved P1 or P2 defects.
- [ ] `DONE-003` There are zero unadjudicated historical commitments.
- [ ] `DONE-004` Strict formatting, build, tests, package, security, performance, platform, soak, accessibility, Wave Link, and Stream Deck gates pass.
- [ ] `DONE-005` The exact signed candidate passes Developer ID, notarization, stapling, Gatekeeper, update, upgrade, and rollback checks.
- [ ] `DONE-006` GitHub, the site, Sparkle appcast, and Homebrew serve the same verified DMG bytes.
- [ ] `DONE-007` First-party distribution evidence remains healthy for 72 hours after publication.

## Historical Reconciliation Ledger

| Prior commitment | Final 1.5 disposition | Required evidence |
|---|---|---|
| P1.1 device reattachment marks Wave Link conflicts as errors | Implement monitor-only conflict recovery | Focused device reattachment regression and physical device-switch test |
| P1.2 unmute succeeds while renderer stop fails | Implement transactional teardown with mute rollback and retained resources | Failure-injection test proves one audible path and safe ownership |
| P1.3 format or geometry mismatch silently zero-fills forever | Implement realtime-safe rebuild signaling, bounded retry, diagnostics, and health UI | Mismatch injection recovers or reaches honest actionable failure |
| P1.4 router detection can lag about eight seconds | Implement event-driven router observation with a 250 ms debounce | Release becomes eligible within one second after router release |
| P2 state and correctness findings | Implement every confirmed item | Focused regressions for hotkeys, socket replies, migration, decoding, validation, styling, router identity, and health reporting |
| P2 dead-code and regression findings | Remove or consolidate obsolete teardown, redundant render-state writes, and unused types | Strict build, tests, and zero remaining references |
| P2 process and CI findings | Implement source-change policy, handler and socket tests, rendered UI execution, release workflow repair, updater tests, and workflow limits | Local shared gate plus workflow validation |
| Seven structural refactors | Complete all seven through bounded, behavior-preserving extractions | Green gate after each extraction and final file-responsibility review |
| Entire P3 backlog | Complete all feasible cleanup items | Ledger item, implementation, and focused or full-suite proof |
| Dead Core Audio object IDs | Replace isolated pruning with a safe `matches` and `covers` lifecycle redesign | Reuse and rebuild tests with exiting and returning helpers |
| Shutdown reporting drift and blocking | Canonicalize store identifiers, deduplicate failures, and bound report completion to 250 ms | Timeout, deduplication, and clean-shutdown tests |
| Test fixtures leak background work | Give fixtures explicit shutdown and drain behavior | Tests report no leaked polling or persistence work |
| Level meter ballistics gap | Add deterministic model tests | Attack, release, floor, and inactivity cases pass |
| TCC capture preflight | Do not fabricate an unavailable public signal; solve through honest runtime failure, recovery, diagnostics, and UI | Pre-consent and denied-capture package tests |
| Zombie taps and fabricated output fallback | Improve event cleanup and surface fallback uncertainty without claiming unsupported detection | Process-exit, refresh, and diagnostics tests |
| Automatic conferencing mute persistence | Already implemented as session-only; retain and expand regressions | Relaunch and upgrade tests prove it never becomes durable user mute |
| Per-app output routing | Already shipped; remove it as a proposed 1.5 flagship and retain regression coverage | Existing and expanded device-routing tests |
| Stream Deck plugin design says not implemented | Update status to the separate implemented plugin | Plugin typecheck, tests, validation, package, live socket, and hardware evidence |
| Open PR #28 | Supersede rather than merge as written; preserve its spoofing regression inside verified-router work | Verified identity test plus actual Wave Link coexistence test |

## Task 1: Baseline, Characterization, and Formatting

**Dependencies:** Approved plan.  
**Rollback point:** Untouched `v1.4.4` source, signed app, DMG, dSYM, release metadata, state backups, and baseline measurements.  
**Exit gate:** The isolated formatting change has no behavioral diff and all baseline gates remain green.

- [x] `BASE-001` Record the exact branch, revision, origin revision, tag, dirty-tree ownership, toolchain, macOS version, architecture, current app bundle identities, installed app identity, and available audio devices.
- [x] `BASE-002` Preserve checksums and trust results for the existing 1.4.4 app, DMG, and dSYM.
- [x] `BASE-003` Back up both Waves application-support locations and the preferences plist before installed-app or migration testing.
- [x] `BASE-004` Capture 1.4.4 launch time, idle CPU, steady memory, and a deterministic active-mixing DSP baseline on the same host and replayable procedure for 1.5.
  - Safe replacement: the physical route could not be activated safely in the controlled `dist/Waves.app` attempt recorded in `.superpowers/sdd/1.5-update-plan/evidence/2026-08-08T190257Z/review-fix/active-mixing-attempt-2/transcript.txt`. The 1.4.4 source worktree at `64bad3baa901d44d02a64023a9c72acabf0557ea` instead ran `swift test --skip-build --filter 'stackedEqualizerHeadroomPreventsFullScaleCallbackClipping|float32BoostClampsToValidRange'`, which exercises the real `TapDSP.scale` and stacked per-app plus managed `EqualizerDSP.process` chain. Re-run that exact command at the 1.5 candidate revision on this host before release comparison.
  - This replacement proves deterministic real mixing/DSP-path execution only. It does not substitute for a physical managed route.
- [x] `BASE-005` Add failing characterization tests for P1.1 through P1.4 before changing production behavior.
- [x] `BASE-006` Add a checked-in `.swift-format` configuration enforcing the intended two-space project style.
- [x] `BASE-007` Normalize every Swift source and test file in one isolated mechanical change.
- [x] `BASE-008` Run strict formatting lint, `swift build`, `swift build -c release`, and the full Swift test suite.
- [x] `BASE-009` Confirm formatting changed no generated data, resources, public protocol fixtures, package metadata, or runtime behavior.

## Tranche 2: Audio Engine and Wave Link Coexistence

**Dependencies:** Tranche 1 green.  
**Rollback point:** Characterized and formatted audio baseline.  
**Exit gate:** Every route lifecycle and router arbitration regression passes before control or state work begins.

### Task 2: Audio correctness

- [x] `AUD-001` Provide a production backend decision seam that accepts only `VerifiedRouterConflictProvider` results. When Task 3 supplies verified input, device reattachment routes the conflicting app to `.monitorOnly` with a clear note instead of route error.
- [x] `AUD-002` Make muting-tap teardown transactional. Release the original hardware path only while the Waves renderer can stop safely. If renderer stop fails after release, restore tap muting and keep the renderer and callback resources alive.
- [x] `AUD-003` Stop cleanup at the first failed native dependency and retain every resource a possible late callback can access.
- [x] `AUD-004` Signal format or geometry mismatch from the callback through realtime-safe state. Perform rebuild work asynchronously outside the callback.
- [x] `AUD-005` Retry geometry recovery with bounded backoff and a finite ceiling. Publish an actionable route-health error after exhaustion.
- [x] `AUD-006` Deliver process and tap property listener events through a 250 ms debounce, surface listener-installation degradation, and use bounded fallback re-observation until listeners attach. Interpret only `VerifiedRouterConflictProvider` results.
- [x] `AUD-007` Once verified provider input changes from conflict to released, make the suspended Waves route eligible for automatic recovery within one second.

#### Task 2 tests

- [x] `AUD-T01` Device reattachment under Wave Link conflict remains monitor-only.
- [x] `AUD-T02` Unmute success plus renderer-stop failure retains exactly one intended playback path.
- [x] `AUD-T03` Stop failure plus mute-rollback failure retains resources and surfaces critical diagnostics.
- [x] `AUD-T04` Geometry mismatch schedules one coalesced recovery rather than zero-filling forever.
- [x] `AUD-T05` Recovery backoff, ceiling, success, and exhausted failure are deterministic.

### Task 3: Verified router arbitration

- [x] `AUD-008` Define verified router descriptors from current official signed artifacts. Match designated code requirements, not bundle identifiers alone.
- [x] `AUD-009` Enumerate visible Core Audio taps and read their descriptions without blocking the realtime path.
- [x] `AUD-010` Use public tap membership only when a credible public tap-to-router association exists. When Core Audio exposes no such association, treat visible system-tap membership as unattributable and make no ordinary-app Wave Link ownership claim.
- [x] `AUD-011` When verified live Wave Link owns active Core Audio output but public tap membership is unattributable, conservatively monitor active ordinary routes and explain that limitation without claiming target ownership.
- [x] `AUD-012` Never create a Waves renderer around Wave Link's mixed output.
- [x] `AUD-013` Preserve PR #28's spoofing regression so an unverified process claiming Elgato's bundle ID cannot disable unrelated Waves controls.
- [x] `AUD-014` Maintain a table-driven descriptor model for supported Wave Link variants while requiring verified identity for every entry.
- [x] `AUD-014A` Wire a real verified conflict provider, derived from Task 3's official-artifact descriptors, code identity, and tap membership evidence, into `WavesComposition`. End-user Wave Link coexistence is not complete until this wiring is exercised in the shipped composition.

#### Task 3 tests

- [x] `AUD-T06` Unattributable readable, exclusive, private, unreadable, and multiple system taps never become a Wave Link public-membership claim.
- [x] `AUD-T07` Verified-active Core Audio output fallback is conservative and explained without claiming tap or target ownership.
- [x] `AUD-T08` Spoofed bundle metadata cannot disable ordinary routes.
- [x] `AUD-T09` Wave Link mixed output is never wrapped.

### Task 4: Audio lifecycle and structure

- [x] `AUD-015` Redesign target process-object `matches` and `covers` semantics together so dead IDs retire without spurious rebuilds and returning helper processes attach correctly.
- [x] `AUD-016` Extract per-app tap lifecycle into a focused controller.
- [x] `AUD-017` Extract callback-safe render state and teardown preparation into focused types with one owner.
- [x] `AUD-018` Extract device enumeration and default-device change handling.
- [x] `AUD-019` Extract router observation and route diagnostics.
- [x] `AUD-020` Extract app discovery helpers that do not require actor state.
- [x] `AUD-021` Split competing-router policy into named mixed-output and upstream-ownership decisions.
- [x] `AUD-022` Precompute router activity once per maintenance pass and remove redundant process scans.
- [x] `AUD-023` Remove the obsolete invalidation teardown path and redundant render-state mirror writes.
- [x] `AUD-024` Prove the callback performs no locks, allocations, file access, UI work, or actor hops.

#### Task 4 tests

- [x] `AUD-T10` Exiting, returning, and expanding helper process families preserve safe controller reuse.
- [x] `AUD-T11` One hundred injected route create, update, dispose, and recovery cycles finish without leaked controllers or retained callbacks.

## Tranche 3: Control, Automation, and Stream Deck Compatibility

**Dependencies:** Tranche 2 green.  
**Rollback point:** Audio engine candidate with no control changes.  
**Exit gate:** Protocol v1, `wavesctl`, packaged Waves, and the separate plugin pass real socket integration.

### Task 5: Control server and automation

- [x] `CTL-001` Preserve `ControlProtocol.version == 1` and all command, response, and event names.
- [x] `CTL-002` Replace best-effort writes with a queued nonblocking write pump that retains unsent bytes across partial writes and `EAGAIN`.
- [x] `CTL-003` Cap queued output at 2 MiB per connection and disconnect a client that exceeds it.
- [x] `CTL-004` Require a valid protocol handshake within five seconds.
- [x] `CTL-005` Close unsubscribed connections after thirty seconds without valid activity; subscribed clients remain persistent.
- [x] `CTL-006` Preserve the eight-connection cap, same-UID verification, `0700` support directory, and `0600` socket permissions.
- [x] `CTL-007` Guard URL automation before application activation, setup presentation, or any other UI side effect.
- [x] `CTL-008` Move icon PNG encoding off the main actor and bound its result before queueing.
- [x] `CTL-009` Make `ControlError.notPermitted` reachable when external control is disabled or remove it only if a protocol-compatible response replaces it.

#### Task 5 tests

- [x] `CTL-T01` Test real Unix-socket creation, permissions, stale-file recovery, same-UID acceptance, and foreign-UID rejection where the host permits it.
- [x] `CTL-T02` Test fragmented input, multiple frames, oversized lines, EOF, reconnect, and malformed requests.
- [x] `CTL-T03` Test partial output, `EAGAIN`, slow readers, queue overflow, and complete large icon replies.
- [x] `CTL-T04` Test handshake timeout, idle timeout, subscription persistence, connection cap, and shutdown cleanup.

### Task 6: wavesctl and Stream Deck compatibility

- [x] `CTL-010` Validate `wavesctl` commands before connecting.
- [x] `CTL-011` Add `SO_NOSIGPIPE`, complete-write retry, five-second read and write deadlines, and distinct EOF, timeout, transport, framing, and decode errors to `wavesctl`.
- [x] `CTL-012` Add direct `ControlCommandHandler` tests for every command and error.
- [x] `CTL-013` Add direct updater consent, check, failure, and availability tests without contacting production feeds.
- [x] `CTL-014` Preserve pending settings operations across settings-pane navigation.

#### Task 6 tests

- [x] `CTL-T05` Run the plugin typecheck and tests with Bun.
- [x] `CTL-T06` Run the Elgato validator and build a `.streamDeckPlugin` package without changing protocol v1.
- [x] `CTL-T07` Drive the plugin's real client against a packaged Waves socket and verify list, volume, mute, subscription, reconnect, and disabled-control behavior.

## Tranche 4: State, Persistence, and AppStore Decomposition

**Dependencies:** Tranche 3 green.  
**Rollback point:** Audio and control candidate with original storage and AppStore layout.  
**Exit gate:** Schema-1 state survives clean, legacy, corrupt, upgrade, shutdown, and rollback scenarios.

### Task 7: Shared persistence engine

- [x] `STATE-001` Introduce one internal generic JSON storage engine behind the existing persistence protocols.
- [x] `STATE-002` Preserve the 10 MiB read cap, schema-1 envelope, legacy payload decoding, atomic writes, `0600` files, `0700` directories, corrupt-file preservation, coalescing, and flush semantics.
- [x] `STATE-003` Give every payload an explicit additive decoder. Unknown fields are ignored and missing fields use documented defaults.
- [x] `STATE-004` Make device volume presets retain valid entries when optional fields are missing or unknown fields are present.
- [x] `STATE-005` Replace empty-array pin migration inference with an explicit migration version.
- [x] `STATE-006` Canonicalize persistence store identifiers and deduplicate shutdown failures at the reporting boundary.
- [x] `STATE-007` Bound shutdown-report completion to 250 ms and report timeout without blocking termination indefinitely.

#### Task 7 tests

- [x] `STATE-T01` Test current, legacy unversioned, additive, missing-field, unknown-field, oversized, truncated, corrupt-envelope, and unsupported-version payloads for every store.
- [x] `STATE-T02` Test corrupt-file preservation and recovery warning consumption.
- [x] `STATE-T03` Test coalesced save, flush barriers, shutdown drain, timeout, deduplication, and later recovery after failure.
- [x] `STATE-T04` Test empty pin lists before and after explicit migration.

### Task 8: AppStore coordinators and lifecycle

- [x] `STATE-008` Extract app-intent generation, projection, cancellation, and reconciliation into an owned coordinator.
- [x] `STATE-009` Extract adaptive mixing policy, cadence, task ownership, and gain publication into an owned coordinator.
- [x] `STATE-010` Extract persistence scheduling, failure history, drain, and flush behavior into an owned coordinator.
- [x] `STATE-011` Extract URL parsing, validation, rate limiting, and command production into an automation parser while keeping mutation in the AppStore intent boundary.
- [x] `STATE-012` Keep AppStore as the main-actor observable facade and make every coordinator's input, output, cancellation, drain, and shutdown contract explicit.
- [x] `STATE-013` Clear self-initiated device-change suppression after five seconds if the expected backend event never arrives.
- [x] `STATE-014` Give all test fixtures deterministic coordinator, poller, socket, persistence, and backend shutdown.
- [x] `STATE-015` Preserve automatic conferencing mute as session-only state and never migrate it into durable user mute.

#### Task 8 tests

- [x] `STATE-T05` Test 1.4.4 state upgrade with profiles, hotkeys, EQ, output targets, device presets, pins, and preferences intact.
- [x] `STATE-T06` Test automatic conferencing mute across refresh, user override, failure, relaunch, and upgrade.
- [x] `STATE-T07` Test device-change suppression expiry and a later legitimate device toast.
- [x] `STATE-T08` Prove every fixture exits with zero tracked tasks and poll loops.

## Tranche 5: UI, Accessibility, and Repository Cleanup

**Dependencies:** Tranche 4 green.  
**Rollback point:** Behavior-complete candidate with original view layout.  
**Exit gate:** Rendered and assistive-technology QA passes with no behavior or state regression.

### Task 9: UI and accessibility

- [x] `UI-001` Split Settings into general, mixer, profiles, shortcuts and automation, and diagnostics files.
- [x] `UI-002` Replace duplicated preference binding helpers with one shared helper.
- [x] `UI-003` Replace duplicated onboarding and setup-repair rows with one readiness checklist component.
- [x] `UI-004` Preserve pending app shortcut operations across navigation.
- [x] `UI-005` Suspend and restore every registered hotkey correctly while recording a shortcut.
- [x] `UI-006` Replace stale shortcut refusal copy with the actual Carbon hotkey behavior.
- [x] `UI-007` Make profile validation return a typed result and surface accessible inline or toast feedback for every invalid save request.
- [x] `UI-008` Replace the hardcoded dark profile-name field fill with design-system tokens for light, dark, contrast, and transparency settings.
- [x] `UI-009` Gate shortcut-recorder and related animations on Reduce Motion.
- [x] `UI-010` Surface verified Wave Link ownership, private-tap fallback, geometry recovery, recovery exhaustion, and monitor-only state without implying control.
- [x] `UI-011` Add accessible labels, values, help, and non-color status for every new route-health presentation.
- [x] `UI-012` Move synchronous icon encoding off the main actor and verify interaction remains responsive.
- [x] `UI-013` Add deterministic `LevelMeterModel` attack, release, floor, and inactivity tests.
- [x] `UI-014` Replace wall-clock sleeps in shutdown and render-activity tests with bounded condition polling.

#### Task 9 rendered QA

- [x] `UI-T01` Main window and menu bar in light and dark appearance.
- [x] `UI-T02` Increase Contrast, Reduce Transparency, and Reduce Motion combinations.
- [x] `UI-T03` Keyboard-only mixer, settings, profile, EQ, output, and recovery flows.
- [x] `UI-T04` VoiceOver labels, values, actions, focus order, rotors, validation, and route-health announcements.
- [x] `UI-T05` Loading, empty, monitor-only, managed, error, recovery, Wave Link, and automation-disabled states.
- [x] `UI-T06` Rendered UI tests run locally and are not silently skipped merely because `CI` is set.

Review repair receipt, 2026-08-09: commits `e61a0f1`, `eae5434`, and `309c22e` close the independent round-one findings for `UI-010`, `UI-011`, `UI-012`, `UI-T03`, and `UI-T04`. Direct evidence includes deterministic executor traversal, truthful global recovery semantics, AppKit and SwiftUI-hosted key delivery, production-consumed accessibility actions, focus order, rotors, and announcements, 22 fresh rendered PNGs under `CI=1`, and two isolated default-scheduler full-suite passes of 469 tests each.

Round-two header-badge repair receipt, 2026-08-09: commit `8f87da9` routes contextual-error and generic-unhealthy header badges through one production-consumed global-recovery semantic boundary. Both main-window appearances visibly show `Recover All Routes`; global label, value, help, and hint assertions pass; healthy and non-error contextual badges remain status-only; 22 fresh `CI=1` PNGs, debug and release builds, and an isolated ordinary 470-test suite pass complete the direct evidence for `UI-010`, `UI-011`, and `UI-T04`.

### Task 10: Repository cleanup and documentation

- [x] `UI-015` Remove no-op visible-app invalidation and its call sites, unused merge helpers, unused control cases, unused settings dependencies, unused CLI annotations, unused system-settings cases, and empty feature directories.
- [x] `UI-016` Remove inconsistent owned-operation closure style and other remaining P3 consistency debt.
- [x] `UI-017` Bound icon caching by count and cost and evict entries for terminated apps.
- [x] `UI-018` Archive shipped historical plan files while retaining source history and update every stale status header.
- [x] `UI-019` Update the feature inventory, README, Help, Product, Design, release guide, and Stream Deck design to match the 1.5 truth.

Task 10 receipt, 2026-08-09: source commits `b9745e5`, `7dd73f2`, `f0c8df4`, `3ac8e92`, `c5d259e`, `892daa2`, `e35d170`, and `2ff0817` remove only proven-dead paths, close the remaining P3 consistency work, bound authoritative-session icon caching, archive shipped plans, and align current documentation. Required mutations failed before restoration; strict recursive format and lint, debug and release builds, 92 focused tests, 4 rendered tests with 22 fresh PNGs, and an isolated ordinary default-scheduler suite of 480 tests passed at exact source HEAD `2ff0817`. Version 1.4.4 remains the latest published release, while remote Wave Link and physical Stream Deck validation remains a hard gate for 1.5.0 build 13.

Task 10 review repair in progress, 2026-08-09: `UI-016` is reopened because the original silent-maintenance regression used empty diagnostics checks and did not expose random `DiagnosticsCheck.id` values from production-shaped report builders. UI-015, UI-018, and UI-019 remain closed.

Task 10 cache review repair in progress, 2026-08-09: `UI-017` is reopened because snapshot-wide singleton pruning lets one concurrently constructed AppStore fixture evict another store's cache entries. The repair must prune only the runtime IDs that departed the same authoritative session transition.

Task 10 review repair receipt, 2026-08-09: commits `a2d71d6` and `0f492f5` close `UI-016` and `UI-017` with production-shaped non-empty diagnostics checks, semantic ordered diagnostics comparison, transition-scoped cache eviction, independent-store coverage, and targeted mutation failures. Strict recursive format and lint, debug and release builds, a 94-test Task 10 matrix, and two isolated ordinary default-scheduler full suites of 482 tests each passed. Both isolated homes remained free of Waves Application Support state.

## Task 11: Quality and Release Infrastructure

**Dependencies:** Tranche 5 green.  
**Rollback point:** Complete product candidate before workflow and metadata consolidation.  
**Exit gate:** One shared gate produces the same outcome locally and in hosted automation.

- [x] `INFRA-001` Add a canonical release metadata file containing version `1.5.0` and build `13`.
- [x] `INFRA-002` Make the build script, CI, release workflow, changelog checks, package verification, and evidence generator consume that metadata.
- [x] `INFRA-003` Add one shared quality-gate script for strict format, build, tests, targeted sanitizers, package construction, verification, and smoke tests.
- [x] `INFRA-004` Add one shared release-gate script for revision, tag, clean tree, version, build, changelog, cask, signatures, architecture, dSYM, notarization, stapling, Gatekeeper, and evidence checks.
- [x] `INFRA-005` Make both workflows call the shared scripts instead of duplicating release logic.
- [x] `INFRA-006` Add job and step timeouts, concurrency cancellation where safe, pinned action revisions, least permissions, and artifact retention.
- [x] `INFRA-007` Add dependency update configuration for Sparkle, swift-testing, and GitHub Actions.
- [x] `INFRA-008` Reject a release tag when required local evidence is missing, incomplete, from another revision, or records skipped gates.
- [x] `INFRA-009` Reject source-changing release history that relies on `[skip ci]` without equivalent sealed local evidence.
- [x] `INFRA-010` Keep hosted automation verification-only. Local signing and notarization remain authoritative, and a future hosted signer requires a separately reviewed trusted-signing design.
- [ ] `INFRA-011` Generate a sealed evidence manifest with revision, dirty state, toolchain, test counts, performance, platforms, package identity, architecture, hashes, trust results, local QA, soak, security, and remote Elgato results.
- [x] `INFRA-012` Validate workflow syntax and shared scripts locally because Actions cost and service availability cannot be a 1.5 dependency.

Task 11 receipt, 2026-08-09: commits `87c15b2`, `6ca280a`, `ea9f333`,
`e488512`, `8b857d9`, and `99cf993` add the canonical metadata and evidence
contracts, shared bounded gates, dependency automation, hardened workflow
contracts, a read-only exact-revision hosted candidate builder, and a
macro-free focused sanitizer harness. Release-infrastructure tests pass with 22
tests and 164 assertions; process-group deadline tests pass with 2 tests and 5
assertions; repository and workflow validators pass. Debug and release builds,
the 482-test ordinary isolated suite, realtime callback audit, universal arm64
and x86_64 unsigned package construction, dSYM and DMG verification, and
packaged GUI smoke pass locally. Hosted execution is not claimed. The focused
sanitizer harness compiles and links all intended production coordination
sources, but this macOS 27.0 build `26A5388g` host's Swift 6.2.3 Command Line
Tools Thread Sanitizer runtime crashes inside `__tsan::InitializePlatform`
before `main`, including for a one-line standalone program. The gate remains
strict and `QA-004` stays open for a compatible Task 12 host. `INFRA-011` also
stays open: its deterministic generator, validator, sidecar, and annotated-tag
envelope contract exist, while the real exact-candidate performance, soak,
security, trust, platform, and remote Elgato evidence belongs to Task 12.

Task 11 review correction receipt, 2026-08-09: commit `ae0fe01` replaces raw
workflow-source string checks with structural `jobs.*.steps` validation. Shared
quality and release preflight commands must be dedicated executable steps;
14-day retention must belong to real pinned upload-artifact steps; and every
hosted candidate job must check out `${{ inputs.revision }}` through the pinned
checkout action with zero fetch depth and persisted credentials disabled. The
verify job's lowercase 40-character revision check and the sign job's release
preflight are both bound to that same input. Replayable echo, comment, wrong
retention, missing or wrong ref, and missing or enabled credential-persistence
mutations all fail. The 22-test, 164-assertion infrastructure suite,
2-test, 5-assertion process-deadline suite, repository validator, workflow
validator, and strict recursive format gate pass, restoring evidence for
`INFRA-012` without changing `INFRA-011` or `QA-004`.

Task 12A release-security correction, 2026-08-09: release builds now reject
ordinary and ignored untracked build inputs, archive the clean exact revision,
use private one-run SwiftPM scratch roots, and stamp source-archive and
build-recipe digests into the app. Canonical metadata pins the Waves bundle,
Developer ID identity, Team ID `AJ9VWBRNZN`, and designated requirement. The
release gate derives artifact identity, hardened runtime, notarized Gatekeeper,
stapling, source stamps, and hashes before comparing them with sealed evidence.
Hosted automation is verification-only and contains no credentialed signer.
Appcast generation requires exact annotated publication evidence and resolves
one verified Sparkle `sign_update` from a fresh private dependency root. These
changes address `release-provenance.untracked-build-inputs`,
`hosted-signing.attacker-selected-revision`,
`release-verification.unpinned-developer-id`,
`appcast.unauthenticated-artifact-signing`, and
`appcast.untrusted-sign-update-path`. This receipt does not close `INFRA-011`,
`QA-004`, `QA-008`, or any signed-candidate, platform, soak, hardware, or
publication gate.

Task 12B IPC and state-security correction, 2026-08-09: implementation commit
`85394ec` bounds listener admission to remaining capacity and sixteen accepts
per event, charges frame and byte work before JSON decoding, closes after three
shared malformed or over-limit strikes, and caps active plus pending request
retention at 32 frames and 256 KiB. The server verifies same-UID process
identity, permits at most four connections and two persistent subscribers per
process, and preserves protocol-v1 retry behavior after unsubscribe. Socket
startup now requires a current-UID `0700` no-follow parent and only replaces an
owned regular file or socket; shutdown leaves a replacement leaf untouched.
URL delivery shares a 30-invocation-per-minute pre-parse limiter across the
AppDelegate and SwiftUI paths while retaining the existing accepted-mutation
quota. Persistence and profile import now share a descriptor-bound,
`O_NOFOLLOW`, nonblocking, regular-file reader that checks stable identity and
reads at most the configured cap plus one byte in chunks. This resolves
`control-socket.unbounded-accept-drain`,
`control-protocol.predecode-rate-limit-bypass`,
`control-protocol.unbounded-pending-requests`,
`url-automation.unbounded-rejected-invocations`,
`control-socket.persistent-subscriber-capacity`,
`control-socket.unverified-parent-directory`, and
`file-loading.unbounded-nonregular-read` at the implementation layer.

Task 12B verification adds eleven focused security regressions, including real
Unix sockets, symlinked parents and leaves, a FIFO, and a state-file symlink
that must not be followed or chmodded. The 156-test control, persistence, URL,
and AppStore slice passes, ten repeated real-socket runs pass 80 cases, and two
fresh isolated full suites pass 492 tests each. Seven targeted source mutations
that removed the accept budget, predecode admission, pending count bound, URL
guard, process and subscriber capacity, private-parent mode, or no-follow read
failed their regressions before the intended source was restored. This receipt
does not close `QA-008`; the fresh final-revision security scan remains its
separate gate.

Task 12B independent-review correction, 2026-08-09: implementation commit
`60e735e` retains the verified socket-parent descriptor for the complete server
lifetime. Stale-leaf inspection and removal, mode changes, and shutdown cleanup
now use `fstatat`, `unlinkat`, and `fchmodat` relative to that descriptor. Darwin
does not provide `bindat`, and `/dev/fd/<fd>/<leaf>` is not traversable, so the
server derives the current parent path with `F_GETPATH`, checks both the public
and derived path limits, and revalidates the public parent identity before and
after binding without changing process-global working state. The exact created
socket device, inode, UID, and type are captured after bind and must still match
before either failure cleanup or shutdown can unlink the leaf. Deterministic
parent and leaf replacement regressions prove startup fails closed, an active
server cleans through the retained directory, real clients connect through the
unchanged public path, and a replacement Unix socket survives both start failure
and stop. Real connection tests also prove correlated malformed replies close on
the third strike and that default pending-count and retained-byte bounds close a
socket while its handler is suspended. Ten repeated 18-case socket runs pass 180
cases, the 40-test persistence slice passes, and two fresh isolated full suites
pass 499 tests each. Four production-path mutations disabling the abuse close,
enqueue failure close, count predicate, or retained-byte predicate fail their
real-socket regressions before exact source restoration. Strict recursive format,
debug and release builds, and the realtime callback audit pass. The fresh
final-revision security scan remains the separate `QA-008` gate.

Task 12B final inode-publication correction, 2026-08-09: implementation commit
`3609104` binds and listens on an unpredictable 96-bit staging name inside the
retained verified parent. Startup captures that node's identity, self-connects
through the staging path, accepts on the actual listener, requires the current
UID and `LOCAL_PEERPID == getpid()`, and rechecks the staging device, inode, UID,
and socket type before changing its mode. Descriptor-relative `linkat` publishes
the same inode atomically and without overwrite, retaining the staging link until
a second public-path self-connect and accept proves ordinary clients reach the
actual Waves listener. Immediate staging and public replacement regressions fail
closed while preserving the replacement inode and mode. Shutdown and failure
cleanup never check then unlink the public name. They atomically move it to a
random quarantine with `renameatx_np(RENAME_EXCL)`, inspect and unlink only an
exact quarantined identity, and restore mismatches with another atomic
no-overwrite rename. A Darwin/APFS probe confirmed hard-linked Unix socket names
share identity and reach the original listener. Five focused race regressions
pass, ten repeated 23-case socket runs pass 230 cases, and two fresh isolated
full suites pass 504 tests each. Mutations bypassing the staging self-proof,
public self-proof, or quarantine identity gate fail their real regressions before
source restoration. Strict recursive format, debug and release builds, realtime
callback audit, and diff checks pass. This closes the rereview implementation
finding but does not self-approve `QA-008` or any external release gate.

Task 12C audio identity and native-input security correction, 2026-08-09:
implementation commit `f37866d` replaces bundle-identifier authority with a
live runtime identity containing PID lifetime, canonical executable and outer
bundle paths, and validated Security.framework signing evidence. A bounded
256-entry lifetime-and-path-keyed cache keeps routine discovery probes cheap,
while route creation revalidates every target and helper identity plus its Core
Audio object ownership immediately before native tap creation. Termination and
controller-family coverage now reject reused process and object identifiers
without rebuilding solely because a dead helper retired. Legitimate signed and
ad hoc nested helper families remain supported, and verified Wave Link identity
and routing policy are unchanged. Native format validation now admits only the
supported 8 kHz through 384 kHz range, guards derived Double-to-Int frame
conversions, and treats stream-configuration bytes and stream-usage allocation
counts as bounded untrusted native data.

The restored 97-test audio, router, identity, and lifecycle matrix passes,
including one deterministic test that performs 100 route lifecycle cycles.
Twelve targeted source mutations covering outer-bundle authority, stale PID
termination, pre-tap revalidation, lifetime coverage, cache invalidation,
sample-rate and integer-conversion limits, property size, embedded buffer
capacity, stream-count caps, usage-count caps, and fixed-header completeness
all fail their regressions before exact source restoration. Strict recursive
format and lint, debug and release builds, the realtime callback audit, diff
checks, and two fresh isolated full suites of 518 tests each pass. This receipt
does not self-approve `QA-008`; the independent review and fresh final-revision
security scan remain separate gates.

Task 12C independent-review correction, 2026-08-09: implementation commit
`c448bd1` removes the remaining bundle-ID-or-PID teardown boundary. The backend
protocol now accepts only one exact `AppRuntimeIdentity`; AppStore resolves one
unambiguous stored row for the termination PID, distinguishes a gone process
from a still-live lifetime/path probe, requires full identity equality when a
process remains live, and otherwise leaves nil, legacy, ambiguous, reused, or
unverifiable state to non-destructive maintenance. Immediate backend teardown
and UI demotion both match the same exact stored identity. Security.framework
capture retains the original dynamic guest `SecCode`, extracts and type-checks
its designated requirement and signing fields, applies that exact requirement
back to the dynamic code, recaptures the fields, and requires stable identifier,
Team ID, designated requirement, code-directory hash, lifetime, and paths. Ad
hoc identifier prefixes no longer confer family authority; nil-Team helpers
require exact bound signing identity plus the same canonical outer bundle,
while Developer ID helper families continue to use exact Team ID and outer
bundle containment.

Six AppStore termination regressions, one direct Workspace backend regression,
three injectable Security operation-order regressions, and a real temporary
ad hoc-signed nested-helper process fixture cover the review findings. Mutations
restoring AppStore bundle-or-PID selection, backend PID selection, omission of
the final dynamic requirement check, or ad hoc identifier-prefix admission all
fail before exact source restoration. Strict format, the realtime callback
audit, debug and release builds, a 109-test focused matrix including 100 route
lifecycle cycles, diff checks, and two isolated full suites of 530 tests each
pass. This correction does not self-approve `QA-008`; independent rereview and
the fresh final-revision security scan remain separate gates.

Task 12C final delayed-notification correction, 2026-08-09: implementation
commit `1281661` recognizes that an `NSWorkspace` termination event carries
only a reusable PID and cannot be authenticated by validating the process that
currently holds that PID. AppStore now fails closed whenever any live process
probe exists for the notification PID, even when the session and backend have
already refreshed to the same fully authenticated replacement identity. Only
when the PID is confirmed absent may exactly one stored nonnil
`AppRuntimeIdentity` authorize exact backend release and matching UI demotion.
The obsolete AppStore live-identity equality dependency was removed; signed
runtime discovery and immediate route-creation revalidation remain unchanged.

The TDD regression exercises a real `WorkspaceAudioControlBackend` and live
testing controller after both session and backend refresh to the replacement.
It first failed six controller, backend-row, and UI-row survival assertions
against `353ba77`. A mutation restoring equality-based live-process acceptance
reproduced the same six failures, then the restored repair passed. Strict
format, the realtime callback audit, debug and release builds, a 110-test
focused matrix including the real Security.framework fixture and 100 route
lifecycle cycles, diff checks, and two isolated full suites of 531 tests each
pass. This receipt stops for independent rereview and does not self-approve
`QA-008` or any final QA, signed-candidate, platform, soak, remote Elgato, or
publication gate.

Task 12D final release-chain security correction, 2026-08-09: implementation
commits `ed7c203` and `6f7fce0`, plus documentation commit `380dc84`, close the
five findings in
canonical scan `c2a4e174-b699-4427-8145-2855d5a7d990` at the implementation
layer. Release-sensitive entry points reject caller-selected metadata, SDK,
compiler, and realtime-audit inputs, constrain tool resolution, and validate a
root-owned Apple developer tree. Canonical metadata now pins the inline SSH
release public key, `waves-commit-signing` principal, external receipt issuers,
Waves-specific Sparkle account, and packaged Ed25519 public key without storing
or modifying private credentials or Git signing configuration.

Publication tags require a valid SSH signature from that exact key and
principal. Sealed evidence binds the security-scan and remote-Elgato receipts,
Apple notary log, source revision, and DMG hash. Full commit bodies and the
canonical build-input list, including `PrivacyInfo.xcprivacy`, drive skip-CI
history enforcement. App, DMG, dSYM, and appcast work now occurs in fresh
mode-0700 roots with no-follow, stable-identity, and hash revalidation; only
finalized outputs reach `dist`. Sparkle publication names its account, derives
and matches its public key to packaged `SUPublicEDKey`, and verifies the exact
DMG signature. The unprovisioned account blocks appcast publication only;
candidate creation, Developer ID signing, and notarization remain independent.

The first production-shaped package run safely exposed a benign doubled-slash
spelling from macOS `TMPDIR` at the new private-root containment check.
`6f7fce0` normalizes the absolute root and destination before comparing their
parent identity while retaining leaf-only naming, symlink rejection, and
escape rejection.

Thirteen focused Task 12D regressions pass 132 assertions. The restored complete
release-infrastructure suite passes 46 tests and 364 assertions, the process
deadline suite passes 2 tests and 5 assertions, repository and workflow
contracts pass, strict recursive format passes, debug and release builds pass,
and the isolated ordinary suite passes all 531 tests. Ten security bypass
mutations failed their intended regressions before exact restoration. Exact
preflight also passes at `380dc844096658c9026b2367f8d1767d9fa67d86`.
This receipt does not create or modify credentials, sign, notarize, tag, push,
publish, close `QA-008`, or self-approve final QA, hardware, soak, signed
candidate, or publication gates.

Task 12D release-environment rereview repair, 2026-08-09: implementation commit
`f6a0166` closes the remaining interpreter-startup bypass at the source entry
boundary. `release-gate.sh`, `build_and_run.sh`, and `make_appcast.sh` now enter
through `/bin/bash -p`, resolve one canonical absolute helper path with Bash
builtins, remove all inherited exports before an external executable starts,
and restore only their validated documented inputs. The resulting environment
uses the fixed system path, a verified `C.UTF-8` locale, isolated mode-0700
`HOME` and `TMPDIR`, noninteractive Git without inherited configuration or
redirection, and `/usr/bin/ruby --disable-gems`. Tracked callers and release
documentation execute these scripts directly so the privileged shebang is not
bypassed. Candidate `SIGN_IDENTITY`, `NOTARY_PROFILE`, exact-revision, sealed
evidence, appcast, and packaged-smoke inputs retain their intended separation.

Production-shaped startup tests first reproduced `BASH_ENV` and
`RUBYOPT`/`RUBYLIB` execution. Restored focused coverage passes 84 assertions,
including adjacent Gem, Bundler, Git, shell, loader, caller-home, and temporary
directory inputs. Four independent mutations, removing `-p`, skipping the
export scrub, skipping an entry bootstrap, and invoking an entrypoint through
`bash`, each failed its intended regression before exact restoration. The
complete release-infrastructure suite passes 52 tests and 447 assertions, the
process deadline suite passes 2 tests and 5 assertions, repository and workflow
contracts pass, strict recursive format passes, debug and release builds pass,
the isolated ordinary suite passes all 531 tests, and the realtime audit
passes. A production preflight check exposed and then regression-tested the
need for `C.UTF-8` rather than US-ASCII `C` when reading the Unicode changelog.
The final clean exact-revision preflight and ad hoc package receipt remain in
the ignored Task 12D report so recording them does not change the packaged
revision. This repair does not access or change credentials, sign, notarize,
tag, push, publish, close `QA-008`, or approve remaining external gates.

Task 12D checkout-local Git-policy rereview repair, 2026-08-09:
implementation commit `1c036d2` routes every executed release Git operation in
Ruby, the three direct release entrypoints, and the hosted exact-revision check
through tracked `script/release_git` by an absolute or repository-relative
path. The `/bin/bash -p` launcher removes inherited exports using only shell
builtins before executing `/usr/bin/git`, fixes the system/global/prompt
environment, disables `core.fsmonitor`, and pins
`gpg.ssh.program=/usr/bin/ssh-keygen` at command-line configuration priority.
The build source-dirty comparison additionally uses `--no-ext-diff` and
`--no-textconv`. Checkout-local and included configuration can therefore retain
ordinary repository semantics without reactivating these executable sinks.

Production-entry regressions first reproduced local and include-imported
filesystem monitors while preflight passed, an attributes-selected textconv
during the build dirty check, and a local SSH verification program during
TagAuthority. Restored focused coverage also guards a trusted-exit external
diff and rejects any raw release Git call. Four independent mutation groups,
removing the filesystem-monitor policy, removing both diff safety flags,
removing the system SSH verifier pin, and restoring a raw workflow Git call,
failed the intended markers or caller contract before exact restoration. The
complete release-infrastructure suite now passes 58 tests and 482 assertions,
the process deadline suite passes 2 tests and 5 assertions, repository and
workflow contracts pass, strict recursive format passes, debug and release
builds pass, all 531 isolated ordinary tests pass, and the realtime audit
passes. The final clean exact-revision preflight and fresh ad hoc package/GUI
receipt remain in the ignored Task 12D report so recording them does not change
the packaged revision. This repair does not modify Git configuration or
credentials, sign, notarize, tag, push, publish, begin Task 12E, close
`QA-008`, or approve remaining external gates.

Remote Elgato handoff tooling receipt, 2026-08-09: the in-progress Task 12
release slice adds a protected `prepare-elgato-handoff.sh` entrypoint and a
canonical candidate-bound handoff format. Assembly first reruns the complete
candidate gate, privately stages and verifies the Waves release artifacts,
binds the exact DMG and Stream Deck plugin hashes and source revisions, and
emits an allowlisted kit containing checksums, ordered physical hardware tests,
bounded diagnostics, rollback instructions, mutable results, and a strict
receipt finalizer. The finalizer requires all six remote test groups to pass,
rejects unexpected or oversized diagnostics, and binds the returned receipt to
the Waves revision, DMG, plugin revision and hash, candidate evidence, and
diagnostic tree. Focused assembly, finalization, protected-entrypoint, and
release-order coverage passes 4 tests with 106 assertions, while the complete
release-infrastructure suite passes 65 tests with 599 assertions. Six
candidate-gate, artifact-binding, pass-status, diagnostic-binding, allowlist,
and size-limit mutations fail their intended regressions before exact
restoration. This is tooling evidence only. `ELG-001` through `ELG-008` remain
open until the kit is rebuilt from the final signed and notarized candidate,
delivered to the second Golden Gate machine, executed on real Wave Link and
Stream Deck hardware, and the returned evidence is reviewed.

## Task 12: Final Verification Matrix

### Automated and security

- [ ] `QA-001` `swift format lint --recursive --parallel --strict Sources Tests` passes with zero diagnostics.
- [ ] `QA-002` `swift build` and `swift build -c release` pass.
- [ ] `QA-003` The full Swift suite passes with no weakened assertion and no required skipped test.
- [ ] `QA-004` Focused Thread Sanitizer runs pass for concurrency-sensitive state, persistence, control, and route coordination.
- [ ] `QA-005` One hundred deterministic route lifecycle repetitions pass.
- [ ] `QA-006` Socket backpressure, connection, and shutdown stress passes.
- [ ] `QA-007` Plugin typecheck, tests, validation, package, and live socket integration pass using Bun.
- [ ] `QA-008` A fresh standard security scan reports no unresolved release-blocking finding.
- [ ] `QA-009` The disabled URL scheme causes no activation, setup presentation, mutation, or other side effect.

### Performance

- [ ] `PERF-001` 1.5 launch time is no more than 10 percent slower than 1.4.4 on the same host and procedure.
- [ ] `PERF-002` 1.5 idle CPU is no more than 10 percent higher.
- [ ] `PERF-003` 1.5 steady memory is no more than 10 percent higher.
- [ ] `PERF-004` 1.5 active-mixing resource use is no more than 10 percent higher.
- [ ] `PERF-005` Any approved exception records the user-visible benefit, measured magnitude, and explicit approval in this document before release.
- [ ] `PERF-006` Using a safely activated physical managed route, capture and compare 1.4.4 and the exact 1.5 candidate launch time, idle CPU, steady memory, and active-mixing resource use on the same host and procedure. The deterministic `BASE-004` replacement does not close this physical-route release gate.

### Platform and package

- [ ] `PLAT-001` The signed candidate runs natively on Golden Gate Apple Silicon.
- [ ] `PLAT-002` The x86_64 slice launches and passes packaged smoke under Rosetta.
- [ ] `PLAT-003` Install, launch, persistence, and rendered UI checks pass in Tahoe Apple Silicon.
- [ ] `PLAT-004` Install, launch, persistence, and rendered UI checks pass in Sequoia Apple Silicon.
- [ ] `PLAT-005` Mach-O metadata and package checks enforce the macOS 14.2 deployment floor.
- [ ] `PLAT-006` The evidence manifest states that a physical Sonoma host was not available rather than implying runtime proof.
- [ ] `PLAT-007` Clean consent, denied consent, existing consent, update, relaunch, login item, and rollback scenarios pass.

### Active and background soak

- [ ] `SOAK-001` Four hours of active mixed audio, app launches and exits, profiles, EQ, adaptive mixing, output changes, automation, and recovery finish without duplication, silence, stale state, controller leaks, or runaway resources.
- [ ] `SOAK-002` Twelve hours idle or backgrounded finish without route loss, runaway CPU, unbounded memory, stale control connections, or abandoned tasks.

### Remote Elgato hard gate

- [ ] `ELG-001` Deliver the exact signed candidate, checksum, version and revision display, test checklist, diagnostic collector, and rollback instructions to the other Golden Gate machine.
- [ ] `ELG-002` Verify Waves-first and Wave-Link-first launch orders.
- [ ] `ELG-003` Verify claimed and unclaimed apps, Wave Link mixed output, and private or unreadable tap fallback.
- [ ] `ELG-004` Verify a single audible path with no echo, duplication, delay, or silence.
- [ ] `ELG-005` Verify automatic yield and automatic recovery across twenty arbitration cycles.
- [ ] `ELG-006` Verify output-device changes and both-app relaunches.
- [ ] `ELG-007` Verify Stream Deck dial volume, mute, bidirectional state, app roster, disabled control, and reconnect behavior.
- [ ] `ELG-008` Import and review the remote evidence before publication. Any unexplained failure blocks release.

## Task 13: Candidate Handoff, Publication, and Rollback

**Dependencies:** Every prior checkbox required for release is complete and the evidence manifest is sealed.  
**Rollback point:** Retained 1.4.4 app, DMG, checksum, dSYM, release metadata, and matching persistence backup.

- [ ] `REL-001` Confirm `codex/waves-1.5`, the reviewed plan, tranche commits, and final diff contain no unrelated or secret material.
- [ ] `REL-002` Merge the exact verified revision to `main` without force and confirm `origin/main` equality.
- [ ] `REL-003` Build, Developer ID sign, notarize, staple, and Gatekeeper-check version 1.5.0 build 13 locally.
- [ ] `REL-004` Re-run publication and package smoke checks against the exact signed artifacts.
- [ ] `REL-005` Create tag `v1.5.0` only at the verified clean `main` revision.
- [ ] `REL-006` Publish the tested DMG, checksum, and zipped dSYM to the GitHub release.
- [ ] `REL-007` Download the public assets and prove byte identity with the local released artifacts.
- [ ] `REL-008` Generate the Sparkle appcast signature from the downloaded public DMG bytes.
- [ ] `REL-009` Update and deploy the Waves site appcast, changelog, download metadata, size, checksum-derived facts, and release copy.
- [ ] `REL-010` Update the real Homebrew tap cask with version 1.5.0 and the public DMG checksum.
- [ ] `REL-011` Verify GitHub, site download, appcast, Homebrew, and an actual Sparkle update from 1.4.4 all resolve to the same DMG.
- [ ] `REL-012` If duplication, silence, state loss, signature mismatch, updater failure, or channel disagreement appears, stop publication or roll back to retained 1.4.4 artifacts and matching state. Never reuse a tag or checksum.
- [ ] `REL-013` Observe first-party GitHub, site, appcast, and Homebrew evidence for 72 hours before marking the program complete.

## Commit and Review Policy

- The plan rewrite is not committed until Jonathan reviews it.
- After approval, use conventional commits and one independently green commit per bounded tranche or reviewable sub-tranche.
- Keep the repository-wide formatting commit mechanical and separate.
- Do not commit secrets, credentials, cookies, signing identities, notary profiles, user state, audio, diagnostics containing unreviewed identifiers, or private test evidence.
- Do not push, tag, deploy, publish, close PR #28, or mutate the plugin, site, or Homebrew repositories until the corresponding reviewed implementation and release gate authorizes it.
- Do not use force push, tag reuse, destructive reset, or unreviewed state migration.

## Current Approval Record

- Product contract: approved.
- Risk-gated architecture: approved.
- Failure and compatibility model: approved.
- Verification contract: approved.
- Rollout and rollback model: approved.
- Rewritten document: approved by Jonathan on 2026-08-08.
- Production implementation: authorized under active `/goal` `019fe27b-2609-74b2-bacd-69aea426ceb0`.
