{
  "meta": {
    "app": "Waves",
    "phase": "iter3-ui-parity",
    "totalStories": 176,
    "coverageNotes": "Reviewed the entire Sources tree (Waves + WavesAudioCore) and all root docs (README, PRODUCT, DESIGN, CHANGELOG, PRIVACY, SECURITY, CONTRIBUTING). The 171 captured stories are accurate and cover the vast majority of user-facing surface: every public AppStore func, every Settings toggle, the menu-bar panel, URL-scheme commands, global hotkeys, presets, exclusions, onboarding, diagnostics, accessibility, and persistence are represented.\n\nThe five items above are the only genuinely uncaptured user-facing behaviors I found, ordered by confidence:\n\n1. AUDIO-01 (highest-value finding): The volume-control-mode (hardware/software/automatic) capability is a documented-but-dead feature. The UI picker was removed (CHANGELOG line 39 confirms it was a no-op) yet the backend protocol method setVolumeControlMode, the VolumeControlMode enum, AudioDevice.volumeControlMode, and TWO README claims ('Smart Volume Backend' line 16; 'Choose the preferred route-control mode' line 163) all remain. No view calls setVolumeControlMode (verified by grep). This is both a missing story AND a stale-README/dead-code issue worth flagging for cleanup.\n\n2-3. DISC-07 / DISC-08: The 'Show system processes' and 'Show recent apps' toggles each drive a distinct list-population/grouping behavior (system-category inclusion; the Recent computed set used as a sidebar scope + menu-bar section). They are only loosely folded under the catch-all SET-02 'General preferences toggles', so I surfaced them as distinct discovery behaviors.\n\n4. WIN-08: Sidebar scope is persisted via @SceneStorage and restored across launches — a UX-continuity behavior not covered by the existing session/preference persistence stories.\n\n5. WIN-09 (lowest confidence; partial overlap with WIN-07/BOOST-03): in-list onKeyPress handlers include 'b' (cycle boost, wrapping 4x->1x) and 'p' (toggle pin) on the selected row. Pin-via-keyboard and the in-window boost-cycle key are not separately captured. If WIN-07's 'keyboard-driven control' is read broadly this is a duplicate, so treat it as a candidate rather than a certain gap.\n\nThings I explicitly checked and confirmed are NOT gaps (already covered): @AppStorage('showMenuBarExtra') menu-bar visibility = MENU-06; Cmd-S save-preset + sheet = PRE-01/WIN-04; Cmd-R refresh = WIN-04/MENU; preset context-menu delete in sidebar = PRE-03; versioned persistence envelope = PERSIST-06; Homebrew cask (Casks/waves.rb) and notarized release CI are distribution/build, not in-app user features. No EQ, spatial audio, recording, sleep/wake-specific, or notification features exist in the code (consistent with PRODUCT.md positioning as 'a mixer, not an EQ suite or recorder').",
    "implDist": {
      "implemented": 138,
      "partial": 35,
      "missing": 2,
      "stubbed": 1
    },
    "priorityDist": {
      "P0": 34,
      "P2": 62,
      "P1": 63,
      "P3": 12
    },
    "buildBaseline": {
      "swiftVersion": "Apple Swift 6.2.3 (swiftlang-6.2.3.3.21)",
      "swiftBuild": "pass (exit 0)",
      "swiftTest": "pass (exit 0) — 47/47 tests",
      "compileWarnings": 0,
      "compileErrors": 0,
      "note": "Clean baseline; uncommitted change present in Sources/Waves/Stores/AppStore.swift"
    },
    "phase2": {
      "confirmedIssues": 310,
      "rejectedFalsePositives": 9,
      "severityDist": {
        "medium": 107,
        "low": 157,
        "high": 43,
        "critical": 3
      },
      "storiesWithIssues": 147
    },
    "phase4": {
      "pass": 164,
      "failFix": 3,
      "regression": 9,
      "followupCount": 31
    },
    "phase4b": {
      "followupsAddressed": 31,
      "fixed": 24,
      "deferred": 1,
      "skipped": 2,
      "deferredItems": [
        "OUT-06 / ROUTE-05: \"Auto-restore device\" toggle is a no-op after the device-change refactor. Backend re-establishes routes inside its CoreAudio listener (WorkspaceAudioControlBackend.swift:1571); making the toggle functional needs the preference threaded into the backend AND a decision on off-semantics (dispose-but-not-reattach), which requires behavioral testing on real device switches."
      ],
      "note": "Login-item English-string fragility fixed by hand (LoginItemStatus.requiresApproval boolean)."
    },
    "finalRegressionFixed": "restoreDeviceVolumePresets no longer renders a false .managed when a re-tap fails (reconciles backend state post-loop)",
    "finalBuild": "swift build pass, swift test 47/47 pass, 0 warnings (Swift 6.2.3)"
  },
  "rows": [
    {
      "id": "DISC-01",
      "area": "App Discovery & List Population",
      "feature": "Running-app enumeration",
      "priority": "P0",
      "userStory": "As a Mac user, I want Waves to automatically list the apps currently running on my system, so that I can pick any of them to control its volume without manually adding anything.",
      "expectedBehavior": [
        "On start() and refresh() the backend builds a snapshot by enumerating NSWorkspace.shared.runningApplications via discoverRunningApps",
        "Waves itself is excluded by skipping the current process PID and any app whose bundleIdentifier equals currentBundleID",
        "Apps with activationPolicy == .prohibited or an empty/nil localizedName are filtered out",
        "Each discovered app becomes an AudioApp with id/logicalID, pid, bundleID, displayName, inferred category, a category icon name, and a rendered 64x64 PNG icon (iconTIFFData) drawn from the app/bundle icon",
        "The list is sorted case-insensitively by display name before being mapped",
        "sourceInventorySummary / the 'Running app inventory' count reflect visibleApps.count",
        "refresh() shows a toast '<n> app(s) detected' and start() shows 'Waves is ready'"
      ],
      "acceptanceCriteria": [
        "Given several GUI apps are running, when Waves starts, then each appears once in the Running list with its real name and icon",
        "Given Waves is running, when the list is built, then Waves does not appear in its own list",
        "Given an app with activationPolicy .prohibited or no localized name, when discovery runs, then it is not listed",
        "Given the user clicks Refresh, then the list rebuilds and a toast reports the detected app count"
      ],
      "codeRefs": [
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1134",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1135",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1180",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1204",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:988",
        "Sources/Waves/Stores/AppStore.swift:228",
        "Sources/Waves/Stores/AppStore.swift:155"
      ],
      "implStatus": "implemented",
      "implNotes": "Solid and well-guarded. Note discovery is poll/refresh-based: there is no NSWorkspace didLaunch observer, so a newly launched app only appears after a manual refresh, a device-change rebuild, or another snapshot trigger (didTerminate is observed, but launch is not). Icons are re-rendered to PNG on every snapshot build, which is mild repeated work per refresh.",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [1M 2L]",
      "errorsFound": "• [MEDIUM/inconsistency] Refresh toast counts session.apps (incl. hidden system apps), not the visible inventory (Sources/Waves/Stores/AppStore.swift:249) — refresh() reports the detected count as session.apps.count (AppStore.swift:249), but the list, sidebar Running count (MainWindowView.swift:175) and sourceInventorySummary (AppStore.swift:156) all use visibleApps.count. With default showSystemProcesses == false, system-category apps (Finder, System Settings, etc.) are present in session.apps but filtered out of visibleApps, so the toast over-counts versus every other surface. Re-verified: visibleApps filters .system unless showSystemProcesses (AppStore.swift:104-105). | Fix: Use visibleApps.count in the refresh toast detail so the number matches sourceInventorySummary and the source-list count.\n• [LOW/edge-case] Newly launched apps don't appear until a manual refresh (no didLaunch observer) (Sources/Waves/Stores/AppStore.swift:886) — Discovery runs only on start()/refresh(). The store observes didActivateApplicationNotification (AppStore.swift:889, calls only checkAutoPauseMusic) and didTerminateApplicationNotification (AppStore.swift:901), but never didLaunchApplicationNotification. A user who launches a new app after Waves is open won't see it in the mixer until Refresh or a device-change rebuild fires. The implNote acknowledges this. | Fix: Add an NSWorkspace.didLaunchApplicationNotification observer that triggers a debounced refresh() so newly launched apps appear without manual refresh.\n• [LOW/performance] Per-app icons re-rendered to 64x64 PNG on every snapshot build (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1187) — discoverRunningApps maps every representative through iconTIFFData() (WorkspaceAudioControlBackend.swift:1187 -> iconPNGData at 1221), which lockFocus/draw/encodes a fresh 64x64 PNG per app on every buildSnapshot (start/refresh/device-change). Re-verified the work runs on Task.detached (line 993), so it is not a main-thread hazard, just repeated CPU/allocation work; icons rarely change between refreshes. | Fix: Cache rendered PNG data keyed by logicalID (or bundleID + icon identity) and reuse it across snapshots, re-rendering only when missing.",
      "fixStatus": "1 fixed · 1 deferred · 1 n/a",
      "fixNotes": "tier2: FU-REFRESH-TOAST-COUNT",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "inconsistency",
          "title": "Refresh toast counts session.apps (incl. hidden system apps), not the visible inventory",
          "detail": "refresh() reports the detected count as session.apps.count (AppStore.swift:249), but the list, sidebar Running count (MainWindowView.swift:175) and sourceInventorySummary (AppStore.swift:156) all use visibleApps.count. With default showSystemProcesses == false, system-category apps (Finder, System Settings, etc.) are present in session.apps but filtered out of visibleApps, so the toast over-counts versus every other surface. Re-verified: visibleApps filters .system unless showSystemProcesses (AppStore.swift:104-105).",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:249",
          "expectedVsActual": "DISC-01/AC3 + expectedBehavior: the toast should report the detected app count consistent with the inventory (visibleApps.count). Actual: it reports session.apps.count, which includes apps hidden from the list.",
          "suggestedFix": "Use visibleApps.count in the refresh toast detail so the number matches sourceInventorySummary and the source-list count.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier2:applied:FU-REFRESH-TOAST-COUNT",
          "uid": "DISC-01#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-REFRESH-TOAST-COUNT",
          "fixTier": "tier2"
        },
        {
          "severity": "low",
          "type": "edge-case",
          "title": "Newly launched apps don't appear until a manual refresh (no didLaunch observer)",
          "detail": "Discovery runs only on start()/refresh(). The store observes didActivateApplicationNotification (AppStore.swift:889, calls only checkAutoPauseMusic) and didTerminateApplicationNotification (AppStore.swift:901), but never didLaunchApplicationNotification. A user who launches a new app after Waves is open won't see it in the mixer until Refresh or a device-change rebuild fires. The implNote acknowledges this.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:886",
          "expectedVsActual": "User story: 'automatically list the apps currently running.' Actual: the list is poll/refresh-based; new launches are not surfaced live (no AC strictly requires live launch detection).",
          "suggestedFix": "Add an NSWorkspace.didLaunchApplicationNotification observer that triggers a debounced refresh() so newly launched apps appear without manual refresh.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "DISC-01#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "low",
          "type": "performance",
          "title": "Per-app icons re-rendered to 64x64 PNG on every snapshot build",
          "detail": "discoverRunningApps maps every representative through iconTIFFData() (WorkspaceAudioControlBackend.swift:1187 -> iconPNGData at 1221), which lockFocus/draw/encodes a fresh 64x64 PNG per app on every buildSnapshot (start/refresh/device-change). Re-verified the work runs on Task.detached (line 993), so it is not a main-thread hazard, just repeated CPU/allocation work; icons rarely change between refreshes.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1187",
          "expectedVsActual": "Expected: icon data is stable and could be cached per logicalID. Actual: it is re-rendered unconditionally each snapshot.",
          "suggestedFix": "Cache rendered PNG data keyed by logicalID (or bundleID + icon identity) and reuse it across snapshots, re-rendering only when missing.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "deferred:needs-feature:A correct icon cache requires choosing a key (bundleID + icon-identity) and invalidation policy, plus plumbing shared mutable state into Self.discoverRunningApp",
          "uid": "DISC-01#3",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift"
        }
      ]
    },
    {
      "id": "DISC-02",
      "area": "App Discovery & List Population",
      "feature": "Audio-aware live vs. ready classification",
      "priority": "P0",
      "userStory": "As a user, I want apps that are actually producing sound to be marked Live and floated to the top, so that I can immediately find what I'm hearing and adjust it.",
      "expectedBehavior": [
        "getAudibleProcessPIDs() reads the Core Audio kAudioHardwarePropertyProcessObjectList and keeps PIDs whose kAudioProcessPropertyIsRunningOutput is true",
        "An app is marked isAudible (routingState .live) when its process family PIDs intersect the audible set; otherwise it is .monitorOnly ('Ready')",
        "isActive is set when the app is audible or frontmost",
        "liveApps surfaces .live apps plus managed unmuted apps with measurable level; recentApps is everything visible that is not live (only when showRecentApps is on)",
        "Activity sort ranks live (0), managed-audible (1), active (2), managed (3), others (4)",
        "The menu bar Live/Recent sections and main-window Frontmost/Recent filters are populated from these computed lists"
      ],
      "acceptanceCriteria": [
        "Given an app is emitting audio, when discovery runs, then it shows routingState .live and appears in the Live/Frontmost section",
        "Given an app is running but silent, then it shows as Ready (monitorOnly) and falls into Recent (if recent apps are enabled)",
        "Given the Core Audio process list cannot be read, then no app is falsely marked audible and discovery still succeeds (audiblePIDs empty)",
        "Given activity sort is selected, then live apps are ordered above ready/idle apps"
      ],
      "codeRefs": [
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:869",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:939",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1176",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1178",
        "Sources/Waves/Stores/AppStore.swift:117",
        "Sources/Waves/Stores/AppStore.swift:124",
        "Sources/Waves/Stores/AppStore.swift:1303"
      ],
      "implStatus": "implemented",
      "implNotes": "Live detection degrades gracefully: if getAudibleProcessPIDs returns empty (permission/OS issue) isAudible is forced false via the !audiblePIDs.isEmpty guard, so nothing is mislabeled live but a genuinely-playing app may then sit in Recent until levels arrive. isActive also folds in frontmost, so a silent foreground app counts as active — intentional but can surprise.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/dead-code] !supportsPerAppRouting branch that wipes live classification is unreachable (deployment target is 14.2) (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1037) — buildSnapshot's per-index loop (WorkspaceAudioControlBackend.swift:1037) forces routingState = .monitorOnly and notes = 'Per-app route requires macOS 14.2+' when !supportsPerAppRouting. Re-verified: supportsPerAppRouting (line 310) returns false only below macOS 14.2, and Package.swift sets the platform floor to .macOS(\"14.2\"), so #available(macOS 14.2,*) is always true at runtime and this branch never executes. The note and the monitorOnly override (which would have erased fresh .live state) are dead code. | Fix: Remove the !supportsPerAppRouting branch (and the related compatibility/notes overrides) or gate the property behind the actual minimum so the dead path is dropped.",
      "fixStatus": "1 deferred",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "dead-code",
          "title": "!supportsPerAppRouting branch that wipes live classification is unreachable (deployment target is 14.2)",
          "detail": "buildSnapshot's per-index loop (WorkspaceAudioControlBackend.swift:1037) forces routingState = .monitorOnly and notes = 'Per-app route requires macOS 14.2+' when !supportsPerAppRouting. Re-verified: supportsPerAppRouting (line 310) returns false only below macOS 14.2, and Package.swift sets the platform floor to .macOS(\"14.2\"), so #available(macOS 14.2,*) is always true at runtime and this branch never executes. The note and the monitorOnly override (which would have erased fresh .live state) are dead code.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1037",
          "expectedVsActual": "Expected: live classification (DISC-02) applies on all supported OS versions. Actual: the only path that would suppress live detection is unreachable given the 14.2 minimum — harmless but dead.",
          "suggestedFix": "Remove the !supportsPerAppRouting branch (and the related compatibility/notes overrides) or gate the property behind the actual minimum so the dead path is dropped.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "deferred:needs-user-decision:Confirmed unreachable (supportsPerAppRouting is true on all >=14.2; Package.swift floor is 14.2), but removing only this branch while leaving the 8 sibling guar",
          "uid": "DISC-02#1",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift"
        }
      ]
    },
    {
      "id": "DISC-03",
      "area": "App Discovery & List Population",
      "feature": "Process consolidation & helper filtering (logical identity)",
      "priority": "P0",
      "userStory": "As a user, I want each app to appear as a single row instead of a clutter of helper/renderer processes, so that the mixer stays clean and one slider controls the whole app.",
      "expectedBehavior": [
        "isManageableApp filters out processes whose name/bundle contains excludedProcessMarkers (daemon, updater, launcher, agent, service, crashpad, login item, xpc, helper, web content, renderer, gpu, etc.)",
        "logicalAppID returns the bundle ID for ordinary apps, a per-PID suffixed ID for companion/helper audio processes, and a name- prefixed slug when no bundle ID exists",
        "Multiple running processes that map to the same logicalID are collapsed to one representative chosen by score() (favoring .regular activation, active, real .app bundle, has icon, non-companion, penalizing daemon/agent/etc.)",
        "processFamily groups a representative with its bundle-family and same-logicalID siblings so audibility is evaluated across the whole family (bundleFamilyMatches, with sibling-helper roots for Firefox/Zen/Mozilla)",
        "resolveProcessObjectIDs later re-expands the family PIDs to translate to Core Audio process objects when a route is applied"
      ],
      "acceptanceCriteria": [
        "Given Chrome with several helper/renderer processes, when discovery runs, then only one Chrome row appears",
        "Given a process named '... Renderer'/'... Helper', then it is excluded from the manageable list",
        "Given two processes share a bundle ID, then exactly one representative row is shown and it is the higher-scored one",
        "Given a Firefox-family helper is audible, then the parent Firefox row is marked live via family matching"
      ],
      "codeRefs": [
        "Sources/Waves/Backend/AppDiscoveryPolicy.swift:75",
        "Sources/Waves/Backend/AppDiscoveryPolicy.swift:4",
        "Sources/Waves/Backend/AppDiscoveryPolicy.swift:107",
        "Sources/Waves/Backend/AppDiscoveryPolicy.swift:118",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1154",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1249",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1296"
      ],
      "implStatus": "implemented",
      "implNotes": "Logic is thorough and unit-tested. Risk: excludedProcessMarkers are broad substring matches on name+bundleID, so legitimately-named apps could be hidden — e.g. anything containing 'agent', 'service', or 'launcher' (a hypothetical 'Service Station', 'Agent' game, etc.) is silently dropped with no UI affordance to reveal it. There is no exception list. AppDiscoveryPolicy lives under Sources/WavesAudioCore/Backend/ (codeRefs use the source path).",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/edge-case] Broad substring exclusion markers can silently hide legitimately named apps with no escape hatch (Sources/WavesAudioCore/Backend/AppDiscoveryPolicy.swift:133) — isManageableApp (AppDiscoveryPolicy.swift:75-83) drops any app whose lowercased name+bundleID contains a marker from excludedProcessMarkers (line 133), including the short generic substrings 'agent', 'service', and 'launcher'. A real, user-facing app named e.g. 'Service Station', 'Agent', or one with 'launcher' in its bundle ID is filtered out in discoverRunningApps (WorkspaceAudioControlBackend.swift:1147) before it ever becomes an AudioApp. Re-verified there is no escape hatch: the 'Show system processes' toggle only un-hides the .system category in visibleApps; it does NOT re-admit apps removed by isManageableApp, and the per-app exclusions feature is the inverse (user-driven exclusion). | Fix: Tighten matching (whole-word/bundle-suffix patterns instead of naked substrings, as already done for 'arc' in inferCategory) and/or add an allowlist or a 'show hidden processes' override so a false-positive app can be recovered.",
      "fixStatus": "1 deferred",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "edge-case",
          "title": "Broad substring exclusion markers can silently hide legitimately named apps with no escape hatch",
          "detail": "isManageableApp (AppDiscoveryPolicy.swift:75-83) drops any app whose lowercased name+bundleID contains a marker from excludedProcessMarkers (line 133), including the short generic substrings 'agent', 'service', and 'launcher'. A real, user-facing app named e.g. 'Service Station', 'Agent', or one with 'launcher' in its bundle ID is filtered out in discoverRunningApps (WorkspaceAudioControlBackend.swift:1147) before it ever becomes an AudioApp. Re-verified there is no escape hatch: the 'Show system processes' toggle only un-hides the .system category in visibleApps; it does NOT re-admit apps removed by isManageableApp, and the per-app exclusions feature is the inverse (user-driven exclusion).",
          "codeRef": "Sources/WavesAudioCore/Backend/AppDiscoveryPolicy.swift:133",
          "expectedVsActual": "Expected (DISC-03): helper/renderer/daemon processes are collapsed/hidden while genuine apps stay listed. Actual: substring matching can also remove genuine apps that merely contain a marker word, with no way to un-hide them.",
          "suggestedFix": "Tighten matching (whole-word/bundle-suffix patterns instead of naked substrings, as already done for 'arc' in inferCategory) and/or add an allowlist or a 'show hidden processes' override so a false-positive app can be recovered.",
          "confidence": "confirmed",
          "primaryFile": "Sources/WavesAudioCore/Backend/AppDiscoveryPolicy.swift",
          "disposition": "deferred:needs-user-decision:Behavior-changing heuristic plus new override UX; no single obvious-correct answer.",
          "uid": "DISC-03#1",
          "canonicalFile": "Sources/WavesAudioCore/Backend/AppDiscoveryPolicy.swift"
        }
      ]
    },
    {
      "id": "DISC-04",
      "area": "App Discovery & List Population",
      "feature": "Category inference & per-app icon",
      "priority": "P2",
      "userStory": "As a user, I want each discovered app tagged with a category and a recognizable icon, so that I can scan, sort, and group the list quickly.",
      "expectedBehavior": [
        "inferCategory maps tokens to browser/conferencing/media/communication/system/unknown (Safari/Chrome/Arc-as-word/browser; zoom/meet/teams/webex/facetime; spotify/music/vlc/podcast/tv; discord/slack/messages/telegram; com.apple.* => system)",
        "'arc' is matched only as a whole word so Archive Utility / Monarch are not misclassified as browsers",
        "iconName(for:) maps each category to an SF Symbol used as a fallback glyph",
        "The app's real icon is captured as a 64x64 PNG (iconTIFFData) from app.icon, bundleURL, or urlForApplication",
        "Category drives Category sort order and the conferencing detection used by auto-pause"
      ],
      "acceptanceCriteria": [
        "Given Safari, Zoom, Spotify, Discord and Finder, when discovery runs, then they are categorized browser/conferencing/media/communication/system respectively",
        "Given 'Archive Utility', then it is NOT categorized as a browser",
        "Given an app with no embedded icon, then a category SF-Symbol fallback name is still provided",
        "Given Category sort, then rows group by category then name"
      ],
      "codeRefs": [
        "Sources/Waves/Backend/AppDiscoveryPolicy.swift:27",
        "Sources/Waves/Backend/AppDiscoveryPolicy.swift:30",
        "Sources/Waves/Backend/AppDiscoveryPolicy.swift:90",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1204",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1221",
        "Sources/Waves/Stores/AppStore.swift:1291"
      ],
      "implStatus": "implemented",
      "implNotes": "Heuristics are keyword-based and intentionally rough. The media token 'music' matches any app whose name/bundle contains 'music' (Apple Music, but also unrelated apps), 'tv' and 'meet' are short substrings that can false-positive, and there is no user override to recategorize an app. Acceptable for an inference layer but worth noting as a UX edge.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/edge-case] Category inference uses short ambiguous substrings ('tv', 'meet', 'music') with no user override (Sources/WavesAudioCore/Backend/AppDiscoveryPolicy.swift:38) — inferCategory (AppDiscoveryPolicy.swift:27-61) matches media via token.contains(\"tv\") and contains(\"music\") (lines 44-45) and conferencing via contains(\"meet\") (line 38). These are short, unanchored substrings that false-positive on unrelated names/bundle IDs, and there is no way to recategorize a mis-tagged app. Because category drives Category sort (AppStore.swift:1292) and conferencing-based auto-pause (checkAutoPauseMusic at AppStore.swift:950-953), a misclassification can mis-sort or wrongly auto-pause an app. The named ACs (Safari/Zoom/Spotify/Discord/Finder, Archive Utility) all pass; 'arc' is correctly whole-word anchored. | Fix: Anchor the risky tokens as whole words via \\b...\\b regex (as done for 'arc'), and consider a per-app category override stored in preferences.",
      "fixStatus": "1 deferred",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "edge-case",
          "title": "Category inference uses short ambiguous substrings ('tv', 'meet', 'music') with no user override",
          "detail": "inferCategory (AppDiscoveryPolicy.swift:27-61) matches media via token.contains(\"tv\") and contains(\"music\") (lines 44-45) and conferencing via contains(\"meet\") (line 38). These are short, unanchored substrings that false-positive on unrelated names/bundle IDs, and there is no way to recategorize a mis-tagged app. Because category drives Category sort (AppStore.swift:1292) and conferencing-based auto-pause (checkAutoPauseMusic at AppStore.swift:950-953), a misclassification can mis-sort or wrongly auto-pause an app. The named ACs (Safari/Zoom/Spotify/Discord/Finder, Archive Utility) all pass; 'arc' is correctly whole-word anchored.",
          "codeRef": "Sources/WavesAudioCore/Backend/AppDiscoveryPolicy.swift:38",
          "expectedVsActual": "Expected: each app tagged with a recognizable category (rough inference is acceptable). Actual: ambiguous tokens can mis-tag apps and the result feeds auto-pause/sorting with no manual override.",
          "suggestedFix": "Anchor the risky tokens as whole words via \\b...\\b regex (as done for 'arc'), and consider a per-app category override stored in preferences.",
          "confidence": "confirmed",
          "primaryFile": "Sources/WavesAudioCore/Backend/AppDiscoveryPolicy.swift",
          "disposition": "deferred:needs-user-decision:Heuristic redesign affecting auto-pause/sorting plus a new override feature.",
          "uid": "DISC-04#1",
          "canonicalFile": "Sources/WavesAudioCore/Backend/AppDiscoveryPolicy.swift"
        }
      ]
    },
    {
      "id": "DISC-05",
      "area": "App Discovery & List Population",
      "feature": "Snapshot merge, persistence & retention of quit apps",
      "priority": "P1",
      "userStory": "As a returning user, I want my per-app volume/mute/boost/output choices to stick to the right app across refreshes and relaunches even if it briefly isn't running, so that I don't have to reconfigure every session.",
      "expectedBehavior": [
        "buildSnapshot merges each freshly-discovered app with the previous snapshot by logicalID, carrying over desiredVolume, appliedVolume, isMuted, isPinned, compatibility, volumeBoost, muteSource and targetDeviceUID",
        "Previously-known apps not in the current scan are retained (as monitorOnly or managed if a controller is still active) only while isStillRunning confirms they're alive; otherwise they drop off",
        "On launch the AppStore loads a cached session from disk and re-merges it with the freshly built live session (mergedSession) so saved settings reattach to rediscovered apps",
        "cleanupStaleEntries prunes pending volume targets and paused-music IDs for apps no longer present",
        "handleAppTermination + releaseControllers immediately demote a quit app to monitorOnly and tear down its route without waiting for a refresh",
        "Each snapshot build is persisted via sessionStore"
      ],
      "acceptanceCriteria": [
        "Given an app had a custom volume, when the list refreshes, then the same app keeps that volume (matched by logicalID, not pid)",
        "Given the app quits, when termination fires, then its row goes inactive/monitorOnly and its controller is released promptly",
        "Given a previously-seen app is no longer running at all, then it is removed from the list",
        "Given Waves relaunches, then cached per-app settings reattach to the rediscovered apps"
      ],
      "codeRefs": [
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:996",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1017",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1274",
        "Sources/Waves/Stores/AppStore.swift:1383",
        "Sources/Waves/Stores/AppStore.swift:472",
        "Sources/Waves/Stores/AppStore.swift:915"
      ],
      "implStatus": "implemented",
      "implNotes": "Merge keys on logicalID, which is the bundle ID for ordinary apps, so identity is stable. For bundle-less apps logicalID is a name slug, so a renamed/localized app could lose its saved settings — a narrow edge. Retention correctly hinges on isStillRunning so the list doesn't accumulate dead apps.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "DISC-06",
      "area": "App Discovery & List Population",
      "feature": "Empty, loading & filtered-list states",
      "priority": "P1",
      "userStory": "As a user, I want clear feedback when the list is loading or when no apps match the current view or search, so that an empty mixer never looks broken.",
      "expectedBehavior": [
        "The live backend seeds from .empty (no fabricated apps/volumes/error) so nothing fake appears before the first real scan",
        "While loading, both the main window and menu bar show a 'Refreshing' progress indicator (isLoading true while session.apps is empty)",
        "The main window shows a ContentUnavailableView with per-filter empty titles/messages (No Running Apps / No Pinned Apps / No Live Apps / No Recent Apps), and a search-specific 'Try a different search term.' message",
        "The menu bar shows a 'No audio apps detected' empty card with Refresh and Settings buttons when pinned, live and recent are all empty",
        "visibleApps respects showSystemProcesses (default off => system-category apps hidden) and recentApps respects showRecentApps",
        "Search filters across all visible apps (not just the active scope) by name or category"
      ],
      "acceptanceCriteria": [
        "Given no cached session, when Waves first launches, then the list is empty with a loading indicator and no fake rows",
        "Given no apps are detected, when the menu bar opens, then a 'No audio apps detected' card with Refresh/Settings is shown",
        "Given a filter/search yields no rows, then a descriptive empty state (not a blank list) is shown",
        "Given showSystemProcesses is off, then com.apple.* system apps are hidden from the list"
      ],
      "codeRefs": [
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:12",
        "Sources/Waves/Stores/AppStore.swift:103",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:336",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:227",
        "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:52",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:64"
      ],
      "implStatus": "implemented",
      "implNotes": "Empty/loading/search states are all real and wired. Minor: isLoading is only true when session.apps is empty, so a refresh that already has a populated list shows no spinner in the main window (the menu bar guards the same way) — acceptable but means subsequent refreshes feel instantaneous with no progress cue.",
      "source": "phase1-story",
      "testStatus": "4 issue(s) [2M 2L]",
      "errorsFound": "• [MEDIUM/ux] Loading indicator and empty-state card render simultaneously on first launch (Sources/Waves/Features/Mixer/MenuBarMixerView.swift:52) — On first launch with no cached session, session.apps is empty so isLoading is true (AppStore.swift:194), while pinnedApps/liveApps/recentApps are all empty so the empty-state UI also renders. Re-verified in the menu bar: the 'Refreshing audio sessions' spinner (MenuBarMixerView.swift:30) and the 'No audio apps detected' card (line 52) are gated independently and both show. In the main window the 'Refreshing' pill (MainWindowView.swift:64) overlays the SourceListView 'No Running Apps' ContentUnavailableView (line 336, gated only by apps.isEmpty). The user is simultaneously told the app is loading and that nothing was found. | Fix: Suppress the empty-state card/ContentUnavailableView while store.isLoading is true (show only the spinner until the first scan completes), or merge them into a single 'Loading…' state.\n• [MEDIUM/inconsistency] Menu bar shows 'No audio apps detected' and hides running apps when 'Show recent apps' is off (Sources/Waves/Features/Mixer/MenuBarMixerView.swift:52) — The menu bar renders only Pinned, Live and Recent sections — there is no 'Running' section (MenuBarMixerView.swift:42-50). recentApps returns [] whenever preferences.showRecentApps is false (AppStore.swift:125). The empty-card condition is pinnedApps.isEmpty && liveApps.isEmpty && recentApps.isEmpty (line 52). So with the user-exposed 'Show recent apps' toggle off, any time nothing is pinned and nothing is live, the menu bar shows 'No audio apps detected' even though silent running apps exist — and those apps are unreachable from the menu bar entirely (the main window's Running scope still lists them, so the two surfaces disagree). | Fix: Base the empty card on store.visibleApps.isEmpty rather than the Live/Recent/Pinned sections, and/or keep a minimal running list visible in the menu bar when Recent is disabled.\n• [LOW/ux] Subsequent refreshes show no progress cue once the list is already populated (Sources/Waves/Stores/AppStore.swift:234) — isLoading is computed as session.apps.isEmpty in both start() (AppStore.swift:194) and refresh() (line 234), so once at least one app is present a Refresh never sets isLoading true. The main-window 'Refreshing' pill (MainWindowView.swift:64) and the menu-bar spinner (MenuBarMixerView.swift:30) are both gated on isLoading, so a refresh over an already-populated list shows no progress indication while the async snapshot rebuild runs. isRefreshing is tracked (line 233) but never surfaced in the UI. | Fix: Drive the progress indicators from isRefreshing (or an explicit isBusy = isLoading || isRefreshing) so a refresh over a populated list still shows the spinner/pill.\n• [LOW/accessibility] Loading ProgressViews have no accessibility label, announcing bare 'in progress' (Sources/Waves/Features/Mixer/MainWindowView.swift:65) — Both loading spinners are bare ProgressView() with the descriptive text as a sibling Text node, not combined: menu bar (MenuBarMixerView.swift:31-37) and main window (MainWindowView.swift:65-71). VoiceOver announces the ProgressView as an unlabeled 'in progress' indicator separate from the 'Refreshing'/'Refreshing audio sessions' caption, so the loading state is not conveyed as a single coherent element. Lower-impact than the visual contradictions above but a real a11y gap in the same loading UI. | Fix: Wrap each spinner+caption in an .accessibilityElement(children: .combine) container or add an .accessibilityLabel to the ProgressView so the loading state reads as one element.",
      "fixStatus": "2 fixed · 1 deferred · 1 n/a",
      "fixNotes": "tier2: FU-MENU-LOADING-EMPTYSTATE-GATING | tier3: FU-LOADING-SPINNER-A11Y-LABEL",
      "retestStatus": "PASS (remediated & verified)",
      "issues": [
        {
          "severity": "medium",
          "type": "ux",
          "title": "Loading indicator and empty-state card render simultaneously on first launch",
          "detail": "On first launch with no cached session, session.apps is empty so isLoading is true (AppStore.swift:194), while pinnedApps/liveApps/recentApps are all empty so the empty-state UI also renders. Re-verified in the menu bar: the 'Refreshing audio sessions' spinner (MenuBarMixerView.swift:30) and the 'No audio apps detected' card (line 52) are gated independently and both show. In the main window the 'Refreshing' pill (MainWindowView.swift:64) overlays the SourceListView 'No Running Apps' ContentUnavailableView (line 336, gated only by apps.isEmpty). The user is simultaneously told the app is loading and that nothing was found.",
          "codeRef": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:52",
          "expectedVsActual": "DISC-06/AC0: first launch should show an empty list with a loading indicator (and no fake rows). Actual: a definitive 'no apps detected'/'No Running Apps' empty state shows alongside the loading indicator while the first scan is still running.",
          "suggestedFix": "Suppress the empty-state card/ContentUnavailableView while store.isLoading is true (show only the spinner until the first scan completes), or merge them into a single 'Loading…' state.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift",
          "disposition": "tier2:applied:FU-MENU-LOADING-EMPTYSTATE-GATING",
          "uid": "DISC-06#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-MENU-LOADING-EMPTYSTATE-GATING",
          "fixTier": "tier2"
        },
        {
          "severity": "medium",
          "type": "inconsistency",
          "title": "Menu bar shows 'No audio apps detected' and hides running apps when 'Show recent apps' is off",
          "detail": "The menu bar renders only Pinned, Live and Recent sections — there is no 'Running' section (MenuBarMixerView.swift:42-50). recentApps returns [] whenever preferences.showRecentApps is false (AppStore.swift:125). The empty-card condition is pinnedApps.isEmpty && liveApps.isEmpty && recentApps.isEmpty (line 52). So with the user-exposed 'Show recent apps' toggle off, any time nothing is pinned and nothing is live, the menu bar shows 'No audio apps detected' even though silent running apps exist — and those apps are unreachable from the menu bar entirely (the main window's Running scope still lists them, so the two surfaces disagree).",
          "codeRef": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:52",
          "expectedVsActual": "DISC-06/AC1: the 'No audio apps detected' card should appear only when no apps are detected. Actual: it appears whenever there are running-but-silent apps and Recent is disabled, falsely implying nothing is running and giving no menu-bar path to those apps.",
          "suggestedFix": "Base the empty card on store.visibleApps.isEmpty rather than the Live/Recent/Pinned sections, and/or keep a minimal running list visible in the menu bar when Recent is disabled.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift",
          "disposition": "deferred:needs-user-decision:Fix is a product fork: gating the empty card on visibleApps.isEmpty would show the card correctly but still render zero app rows (apps exist",
          "uid": "DISC-06#2",
          "canonicalFile": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift"
        },
        {
          "severity": "low",
          "type": "ux",
          "title": "Subsequent refreshes show no progress cue once the list is already populated",
          "detail": "isLoading is computed as session.apps.isEmpty in both start() (AppStore.swift:194) and refresh() (line 234), so once at least one app is present a Refresh never sets isLoading true. The main-window 'Refreshing' pill (MainWindowView.swift:64) and the menu-bar spinner (MenuBarMixerView.swift:30) are both gated on isLoading, so a refresh over an already-populated list shows no progress indication while the async snapshot rebuild runs. isRefreshing is tracked (line 233) but never surfaced in the UI.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:234",
          "expectedVsActual": "DISC-06 expectedBehavior: 'While loading, both the main window and menu bar show a Refreshing progress indicator.' Actual: the indicator only appears when the list is empty; refreshes over a non-empty list give no in-progress feedback.",
          "suggestedFix": "Drive the progress indicators from isRefreshing (or an explicit isBusy = isLoading || isRefreshing) so a refresh over a populated list still shows the spinner/pill.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "DISC-06#3",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "low",
          "type": "accessibility",
          "title": "Loading ProgressViews have no accessibility label, announcing bare 'in progress'",
          "detail": "Both loading spinners are bare ProgressView() with the descriptive text as a sibling Text node, not combined: menu bar (MenuBarMixerView.swift:31-37) and main window (MainWindowView.swift:65-71). VoiceOver announces the ProgressView as an unlabeled 'in progress' indicator separate from the 'Refreshing'/'Refreshing audio sessions' caption, so the loading state is not conveyed as a single coherent element. Lower-impact than the visual contradictions above but a real a11y gap in the same loading UI.",
          "codeRef": "Sources/Waves/Features/Mixer/MainWindowView.swift:65",
          "expectedVsActual": "Expected: the loading state is announced as one labeled element (e.g. 'Refreshing app list, in progress'). Actual: the spinner and its caption are independent nodes; the spinner itself is unlabeled.",
          "suggestedFix": "Wrap each spinner+caption in an .accessibilityElement(children: .combine) container or add an .accessibilityLabel to the ProgressView so the loading state reads as one element.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "disposition": "tier3:applied:FU-LOADING-SPINNER-A11Y-LABEL",
          "uid": "DISC-06#4",
          "canonicalFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-LOADING-SPINNER-A11Y-LABEL",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "VOL-01",
      "area": "Per-App Volume Control",
      "feature": "Per-app volume slider (0-100%)",
      "priority": "P0",
      "userStory": "As a macOS user running several audio apps, I want a per-app volume slider in the mixer, so that I can set each app's loudness independently without touching the system volume.",
      "expectedBehavior": [
        "Each mixer row shows a Slider bound to app.desiredVolume over the range 0...1, plus a monospaced '<n>%' label (Int(desiredVolume*100)) (MixerRowView.swift:57-91, 283-304)",
        "Dragging the slider calls store.setDesiredVolume on every tick, which clamps to [0,1], updates session.apps[index].desiredVolume and appliedVolume (0 if muted), records a pendingVolumeTarget, and invalidates the visible-apps cache (AppStore.swift:356-382)",
        "On drag end (onEditingChanged false) store.commitDesiredVolume schedules an immediate backend apply; mid-drag ticks are debounced via scheduleVolumeApply with a 50ms delay (MixerRowView.swift:65-69; AppStore.swift:384-393, 395-462, 51)",
        "Backend setDesiredVolume locates the app by logicalID/id, clamps to [0,1], applies the route, and sets appliedVolume = muted ? 0 : target, routingState=.managed, clears notes/lastError (WorkspaceAudioControlBackend.swift:53-83)",
        "The realtime IO proc scales the tapped buffer in place by effectiveVolume = volume*volumeBoost; volume==0 zeroes the output buffer entirely (WorkspaceAudioControlBackend.swift:1700-1713, 1879-1938; TapDSP.swift:21-51)",
        "Adjusting the slider on a monitor-only app enrolls it into a managed route (creates tap + aggregate device) on first apply (WorkspaceAudioControlBackend.swift:360-387; help text 'Adjust to enroll ... in managed routing' MixerRowView.swift:211-215)",
        "Successful apply shows a 'Managed route active — <App> set to <n>%' success toast; failure shows a 'Volume change failed' error toast and refreshes the session (AppStore.swift:432-460)"
      ],
      "acceptanceCriteria": [
        "Setting the slider to 50% results in app.desiredVolume==0.5 and the label reading '50%'",
        "Values passed outside 0...1 (including via URL scheme) are clamped to [0,1] before apply",
        "A non-finite volume (NaN/Inf) is rejected by the backend and the app's desiredVolume defaults to 1.0 without throwing (WorkspaceAudioControlBackend.swift:59-63)",
        "Setting volume to 0% produces silence for that app (IO proc zeroes output) while leaving other apps unaffected",
        "After a successful change, routingState becomes .managed and a success toast appears; on backend failure routingState becomes .error and an error toast appears",
        "Rapid slider dragging coalesces into debounced applies (50ms) rather than one backend call per pixel"
      ],
      "codeRefs": [
        "MixerRowView.swift:57-91",
        "MixerRowView.swift:283-304",
        "AppStore.swift:356-382",
        "AppStore.swift:384-462",
        "WorkspaceAudioControlBackend.swift:53-83",
        "WorkspaceAudioControlBackend.swift:1879-1938",
        "TapDSP.swift:21-51"
      ],
      "implStatus": "implemented",
      "implNotes": "Core path is complete and wired end-to-end with real Core Audio taps, debouncing, clamping, NaN-guarding, and error surfacing. Concern: NaN handling in the backend (line 59-63) returns early WITHOUT applying any route or clearing a prior error — desiredVolume is set to 1.0 in the snapshot but appliedVolume and routingState are left stale, so UI and audio can diverge for that one call. Minor UX: every committed change emits a transient toast, which can be noisy for keyboard/slider nudges.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1M 1L]",
      "errorsFound": "• [MEDIUM/dead-code] Slider-drag debounce (50ms) is dead code; backend apply only fires on drag end (AppStore.swift:395-420) — Verified in code. The SwiftUI Slider's set closure (MixerRowView.swift:60-62 main row; 286-288 compact row) calls only store.setDesiredVolume, which updates session/pendingVolumeTargets and (optionally) the in-memory device preset, but never schedules an apply. The sole call to scheduleVolumeApply is from commitDesiredVolume with immediate:true (AppStore.swift:392), invoked only on drag end (onEditingChanged==false, MixerRowView.swift:66-68). Confirmed by grep: scheduleVolumeApply is called exactly once, always immediate:true, so the `if !immediate { try await Task.sleep(for: volumeApplyDelay) }` branch (AppStore.swift:418-420) and volumeApplyDelay=50ms (AppStore.swift:51) are never exercised. The VoiceOver adjust path also commits immediately. Backend gain therefore changes only on slider release. | Fix: Either (a) call scheduleVolumeApply(forAppID:appKey, immediate:false) from setDesiredVolume so mid-drag ticks coalesce into 50ms-debounced live applies, or (b) delete the immediate parameter, the sleep branch, and volumeApplyDelay, and correct the spec to state audio changes only on drag end.\n• [LOW/error-handling] Backend NaN guard leaves appliedVolume/routingState/lastError stale and is unreachable in practice (WorkspaceAudioControlBackend.swift:59-63) — Verified. WorkspaceAudioControlBackend.setDesiredVolume rejects non-finite input by setting desiredVolume=1.0 and returning early WITHOUT calling applyRoute, updating appliedVolume, setting routingState, or clearing backendStatus.lastError (lines 59-63). A prior error chip/note would persist while desiredVolume silently jumps to 1.0. I independently confirmed the guard is effectively unreachable: in Swift max(0,min(1,NaN))==1.0 and max(0,min(1,+Inf))==1.0 (verified by running it), so the store pre-clamps NaN/Inf before the backend ever sees it (AppStore.swift:365); the URL path rejects NaN because `NaN >= 0` is false (AppStore.swift:297-298); and persisted presets cannot encode NaN through JSON Codable, so the restore loops can't deliver it either. Downgraded from medium to low: the inconsistent-state branch is real but cannot be reached by any live caller. | Fix: In the NaN branch, treat 1.0 as the target (set appliedVolume = isMuted ? 0 : 1.0, set routingState consistently, clear notes/lastError, apply the route) — or remove the redundant guard and document that upstream clamping makes NaN impossible.",
      "fixStatus": "1 fixed · 1 deferred",
      "fixNotes": "tier3: FU-NAN-VOLUME-EARLY-RETURN",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "dead-code",
          "title": "Slider-drag debounce (50ms) is dead code; backend apply only fires on drag end",
          "detail": "Verified in code. The SwiftUI Slider's set closure (MixerRowView.swift:60-62 main row; 286-288 compact row) calls only store.setDesiredVolume, which updates session/pendingVolumeTargets and (optionally) the in-memory device preset, but never schedules an apply. The sole call to scheduleVolumeApply is from commitDesiredVolume with immediate:true (AppStore.swift:392), invoked only on drag end (onEditingChanged==false, MixerRowView.swift:66-68). Confirmed by grep: scheduleVolumeApply is called exactly once, always immediate:true, so the `if !immediate { try await Task.sleep(for: volumeApplyDelay) }` branch (AppStore.swift:418-420) and volumeApplyDelay=50ms (AppStore.swift:51) are never exercised. The VoiceOver adjust path also commits immediately. Backend gain therefore changes only on slider release.",
          "codeRef": "AppStore.swift:395-420",
          "expectedVsActual": "Expected (acceptance criterion 'Rapid slider dragging coalesces into debounced applies (50ms)'): mid-drag ticks produce 50ms-debounced live backend applies so audio tracks the slider. Actual: no backend apply occurs during the drag; gain changes once, on release. The debounce mechanism never runs.",
          "suggestedFix": "Either (a) call scheduleVolumeApply(forAppID:appKey, immediate:false) from setDesiredVolume so mid-drag ticks coalesce into 50ms-debounced live applies, or (b) delete the immediate parameter, the sleep branch, and volumeApplyDelay, and correct the spec to state audio changes only on drag end.",
          "confidence": "confirmed",
          "primaryFile": "AppStore.swift",
          "disposition": "deferred:needs-user-decision:Implementing live debounced applies vs deleting the mechanism is a product decision affecting backend load and the spec.",
          "uid": "VOL-01#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "low",
          "type": "error-handling",
          "title": "Backend NaN guard leaves appliedVolume/routingState/lastError stale and is unreachable in practice",
          "detail": "Verified. WorkspaceAudioControlBackend.setDesiredVolume rejects non-finite input by setting desiredVolume=1.0 and returning early WITHOUT calling applyRoute, updating appliedVolume, setting routingState, or clearing backendStatus.lastError (lines 59-63). A prior error chip/note would persist while desiredVolume silently jumps to 1.0. I independently confirmed the guard is effectively unreachable: in Swift max(0,min(1,NaN))==1.0 and max(0,min(1,+Inf))==1.0 (verified by running it), so the store pre-clamps NaN/Inf before the backend ever sees it (AppStore.swift:365); the URL path rejects NaN because `NaN >= 0` is false (AppStore.swift:297-298); and persisted presets cannot encode NaN through JSON Codable, so the restore loops can't deliver it either. Downgraded from medium to low: the inconsistent-state branch is real but cannot be reached by any live caller.",
          "codeRef": "WorkspaceAudioControlBackend.swift:59-63",
          "expectedVsActual": "Expected (acceptance criterion): a non-finite volume is rejected and desiredVolume defaults to 1.0 without throwing, leaving consistent state. Actual: desiredVolume is set to 1.0 but appliedVolume, routingState, and lastError are left stale; and because all callers pre-clamp/reject NaN, the guard never actually runs.",
          "suggestedFix": "In the NaN branch, treat 1.0 as the target (set appliedVolume = isMuted ? 0 : 1.0, set routingState consistently, clear notes/lastError, apply the route) — or remove the redundant guard and document that upstream clamping makes NaN impossible.",
          "confidence": "confirmed",
          "primaryFile": "WorkspaceAudioControlBackend.swift",
          "disposition": "tier3:applied:FU-NAN-VOLUME-EARLY-RETURN",
          "uid": "VOL-01#2",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-NAN-VOLUME-EARLY-RETURN",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "VOL-02",
      "area": "Per-App Volume Control",
      "feature": "Per-app mute / unmute",
      "priority": "P0",
      "userStory": "As a user, I want a one-click mute toggle on each app row, so that I can instantly silence or restore a specific app without losing its volume setting.",
      "expectedBehavior": [
        "Each row has a borderless speaker button toggling app.isMuted; icon switches between speaker.wave.2.fill and speaker.slash.fill with a bounce symbol effect (suppressed under Reduce Motion) and selection sensory feedback (MixerRowView.swift:96-107, 313-324)",
        "store.setMuted optimistically sets session isMuted, sets appliedVolume to 0 when muted (else desiredVolume), stamps muteSource=.user, removes the app from pausedMusicApps on unmute, then calls backend.setMuted (AppStore.swift:491-542)",
        "Backend setMuted applies the route with the new muted flag; on mute it forces peakLevel/rmsLevel and appliedVolume to 0, sets routingState=.managed and clears errors (WorkspaceAudioControlBackend.swift:85-111)",
        "In the realtime IO proc, isMuted!=0 zeroes the output buffer so the app is fully silenced while still tapped (WorkspaceAudioControlBackend.swift:1695-1698)",
        "The level meter overlay is hidden while muted (showsLevelMeter requires !app.isMuted) (MixerRowView.swift:173-175)",
        "Toggling mute on a monitor-only app enrolls it into managed routing (same applyRoute path); help text says 'Mute to enroll ...' (MixerRowView.swift:217-221)",
        "Success shows 'App muted'/'App unmuted' toast; failure re-merges backend state and shows 'Mute toggle failed' (AppStore.swift:521-541)"
      ],
      "acceptanceCriteria": [
        "Clicking mute silences the app's audio (output zeroed) and the icon shows speaker.slash.fill",
        "Unmuting restores audio at the previously set desiredVolume (appliedVolume returns to desiredVolume)",
        "Muting sets appliedVolume to 0 and zeroes the displayed level meter/peak/rms",
        "A user mute sets muteSource=.user so auto-resume logic won't override it",
        "Mute/unmute on a not-yet-managed app establishes a managed route",
        "Backend failure leaves UI consistent with the backend snapshot and surfaces an error toast"
      ],
      "codeRefs": [
        "MixerRowView.swift:96-107",
        "MixerRowView.swift:313-324",
        "AppStore.swift:491-542",
        "WorkspaceAudioControlBackend.swift:85-111",
        "WorkspaceAudioControlBackend.swift:1695-1698"
      ],
      "implStatus": "implemented",
      "implNotes": "Fully wired with optimistic UI, mute-source tracking, level zeroing, and error recovery. The tap uses CATapMuteBehavior.mutedWhenTapped so the original system stream is muted and Waves re-renders the scaled/muted copy — correct. Minor: optimistic UI updates session before the async backend call resolves, so a failed mute briefly shows the wrong icon until the catch-branch re-merge runs.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/inconsistency] On backend mute failure the UI keeps the muted icon while audio is not actually silenced (WorkspaceAudioControlBackend.swift:101-110) — Verified. Backend setMuted sets snapshot.apps[index].isMuted = isMuted (line 90) BEFORE applyRoute and does not revert it in the catch (lines 101-110); the catch only sets routingState=.error, peak/rms=0, appliedVolume=0. The store's catch then calls mergeAppState (AppStore.swift:535-536), which copies the backend app verbatim (mergeAppState line 1425), so isMuted stays true. The full row renders speaker.slash.fill (MixerRowView.swift:100) plus the red error chip and inline note (line 111-116), even though the tap/route failed and the app's audio is in fact NOT muted. The level meter is also hidden (showsLevelMeter requires !isMuted), reinforcing the false 'muted' impression. | Fix: In the backend setMuted catch branch, revert snapshot.apps[index].isMuted to its pre-call value so a failed mute does not present as applied, or have the store reconcile the icon to the actual route outcome rather than the requested flag.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier2: FU-MUTE-FAILURE-STALE-ICON",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "On backend mute failure the UI keeps the muted icon while audio is not actually silenced",
          "detail": "Verified. Backend setMuted sets snapshot.apps[index].isMuted = isMuted (line 90) BEFORE applyRoute and does not revert it in the catch (lines 101-110); the catch only sets routingState=.error, peak/rms=0, appliedVolume=0. The store's catch then calls mergeAppState (AppStore.swift:535-536), which copies the backend app verbatim (mergeAppState line 1425), so isMuted stays true. The full row renders speaker.slash.fill (MixerRowView.swift:100) plus the red error chip and inline note (line 111-116), even though the tap/route failed and the app's audio is in fact NOT muted. The level meter is also hidden (showsLevelMeter requires !isMuted), reinforcing the false 'muted' impression.",
          "codeRef": "WorkspaceAudioControlBackend.swift:101-110",
          "expectedVsActual": "Expected (acceptance criterion): backend failure leaves UI consistent with reality and surfaces an error. Actual: the icon claims muted while audio plays; only the error chip/note hints something went wrong.",
          "suggestedFix": "In the backend setMuted catch branch, revert snapshot.apps[index].isMuted to its pre-call value so a failed mute does not present as applied, or have the store reconcile the icon to the actual route outcome rather than the requested flag.",
          "confidence": "confirmed",
          "primaryFile": "WorkspaceAudioControlBackend.swift",
          "disposition": "tier2:applied:FU-MUTE-FAILURE-STALE-ICON",
          "uid": "VOL-02#1",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-MUTE-FAILURE-STALE-ICON",
          "fixTier": "tier2"
        }
      ]
    },
    {
      "id": "VOL-03",
      "area": "Per-App Volume Control",
      "feature": "Per-app volume boost (1x-4x gain above 100%)",
      "priority": "P1",
      "userStory": "As a user with an app that is too quiet even at 100%, I want to apply a 2x/3x/4x boost, so that I can amplify just that app beyond its normal maximum.",
      "expectedBehavior": [
        "Each row has a BoostMenu offering 1x/2x/3x/4x; the current multiplier is shown as '<n>x' (MixerRowView.swift:93-94, 310-311, 361-388)",
        "store.setVolumeBoost clamps to [1.0,4.0], updates session volumeBoost optimistically, and calls backend.setVolumeBoost; success/failure toasts shown (AppStore.swift:544-588)",
        "Backend setVolumeBoost clamps to [1,4], updates the live controller via controller.setVolumeBoost, then re-applies the route (WorkspaceAudioControlBackend.swift:113-140)",
        "The IO proc multiplies volume by boost: effectiveVolume = volume*volumeBoost, applied per-sample by TapDSP.scale (WorkspaceAudioControlBackend.swift:1899, 1934)",
        "TapDSP.scale clamps every scaled sample to the format's valid range (±1.0 float, Int16/Int32 min/max) so boost cannot emit out-of-range samples / hard wrap-around clipping (TapDSP.swift:21-51, 105-117)",
        "Boost persists across snapshot rebuilds (merged from previous app) and is saved into per-device volume presets when that preference is on (WorkspaceAudioControlBackend.swift:1010; AppStore.swift:555-565)"
      ],
      "acceptanceCriteria": [
        "Selecting 2x makes the app's effective gain 2.0 (volume*2), audibly louder than 100%",
        "Boost values are clamped to the 1x-4x range at both store and backend layers",
        "Boosted samples are clamped to the format range (no integer wrap-around producing harsh artifacts)",
        "Changing boost on a managed app updates the live controller without tearing down the tap (controller.setVolumeBoost on existing controller)",
        "The selected boost multiplier is reflected in the row label and survives a session refresh"
      ],
      "codeRefs": [
        "MixerRowView.swift:361-388",
        "AppStore.swift:544-588",
        "WorkspaceAudioControlBackend.swift:113-140",
        "WorkspaceAudioControlBackend.swift:1899-1937",
        "TapDSP.swift:21-51"
      ],
      "implStatus": "implemented",
      "implNotes": "Complete and a genuine differentiator (true >100% gain via DSP). Correctness note: clamping each sample to ±1.0 means boost on already-loud content effectively flat-tops (soft-ish) at full scale — louder but with clipping distortion on peaks, which is inherent to naive gain and not a soft limiter. setVolumeBoost(backend) updates the controller AND re-applies the route, which for a live controller calls apply() a second time with the same values — harmless but slightly redundant.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "VOL-04",
      "area": "Per-App Volume Control",
      "feature": "Global keyboard shortcuts for frontmost-app volume/mute",
      "priority": "P2",
      "userStory": "As a user, I want global hotkeys (⌘⌥↑/↓ for volume, ⌘⌥M for mute) that act on the frontmost app, so that I can adjust the app I'm using without opening the Waves menu.",
      "expectedBehavior": [
        "increase/decreaseVolumeForFrontmostApp pick activeApps.first (fallback visibleApps.first), clamp the step to [0.01,0.5], nudge desiredVolume by ±step within [0,1], then setDesiredVolume + commitDesiredVolume and show an info toast (AppStore.swift:1210-1246)",
        "toggleMuteForFrontmostApp flips isMuted on the frontmost app and shows a Muted/Unmuted info toast (AppStore.swift:1248-1262)",
        "All three early-return unless preferences.enableKeyboardShortcuts is true (AppStore.swift:1211,1230,1249)",
        "The VoiceOver accessibilityAdjustableAction on each slider nudges by a fixed 0.05 step and commits immediately (MixerRowView.swift:78-80, 223-238, 305-307, 343-358)",
        "Default keystrokes documented as ⌘⌥↑ / ⌘⌥↓ / ⌘⌥M (README.md:122-124)"
      ],
      "acceptanceCriteria": [
        "With shortcuts enabled, ⌘⌥↑ raises the frontmost app's volume by one step (default 0.1) up to 100%",
        "⌘⌥↓ lowers it by one step down to 0%; ⌘⌥M toggles mute",
        "With shortcuts disabled, the hotkey handlers do nothing",
        "When no app is active/visible, the handlers no-op safely (guard let app)",
        "VoiceOver increment/decrement on a slider changes volume by 0.05 and applies it",
        "Each shortcut action surfaces a brief confirmation toast"
      ],
      "codeRefs": [
        "AppStore.swift:1210-1246",
        "AppStore.swift:1248-1262",
        "MixerRowView.swift:78-80",
        "MixerRowView.swift:223-238"
      ],
      "implStatus": "implemented",
      "implNotes": "Handlers, clamping, and the enable-flag gate are all present and reuse the same setDesiredVolume/setMuted path, so correctness mirrors VOL-01/02. I verified the store methods and the gating flag but did not locate the actual hotkey registration (NSEvent/global monitor) in the read files — registration is assumed to live elsewhere and should be confirmed for full end-to-end coverage. The slider's VoiceOver step (0.05) differs from the hotkey default (0.1); intentional but worth noting for consistency.",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [1H 1M 1L]",
      "errorsFound": "• [HIGH/correctness] Hotkeys target activeApps.first (sort-order dependent), not the actual frontmost app (AppStore.swift:1212) — Verified end-to-end. increase/decrease/toggleMute resolve the target as `activeApps.first ?? visibleApps.first` (AppStore.swift:1212,1231,1250). activeApps = visibleApps.filter(\\.isActive) (AppStore.swift:113-114), and isActive is set to `isAudible || isFrontmost` during discovery (WorkspaceAudioControlBackend.swift:1189). The list is ordered by sortMode (sortedApps, AppStore.swift:1278): with .name it is alphabetical; with .activity an audible app ranks 0 while a merely-frontmost app ranks 2 (activityRank, AppStore.swift:1303-1320). So whenever any background app is playing audio, activeApps.first is that audible app, not the app the user is in. The global NSEvent monitor (WavesApp.swift:152,179-185) fires while a different app is frontmost but passes no identifier; the store never consults NSWorkspace.shared.frontmostApplication to pick the hotkey target (it only reads frontmostApplication in checkAutoPauseMusic, AppStore.swift:945, not in the hotkey handlers). | Fix: Resolve the target by matching NSWorkspace.shared.frontmostApplication.bundleIdentifier (or pid) against session.apps, falling back to activeApps.first only when no match exists. The frontmost app is known where the event is handled (WavesApp), so pass its identifier into the store methods.\n• [MEDIUM/ux] Hotkey shows a success toast even when the resolved app is excluded (no-op) (AppStore.swift:1210-1262) — Verified. increase/decreaseVolumeForFrontmostApp and toggleMuteForFrontmostApp call setDesiredVolume/setMuted, which early-return when isExcluded(app) is true (AppStore.swift:357,492), but the handlers have no exclusion guard and unconditionally show a 'Volume increased/decreased' or 'Muted/Unmuted' info toast (AppStore.swift:1221-1226,1240-1245,1256-1261). For the volume handlers the toast even reports the would-be new percentage computed locally, while app.desiredVolume never changed. If the frontmost-resolved app is excluded, nothing changes yet the user sees a confirmation toast implying it did. | Fix: Add `guard !isExcluded(app) else { return }` (or show an explanatory toast) at the top of each frontmost-app handler before mutating and before showing the success toast.\n• [LOW/ux] A single volume hotkey press emits two stacked toasts (also affects mute) (AppStore.swift:1221) — Verified. increaseVolumeForFrontmostApp shows a 'Volume increased' info toast (AppStore.swift:1221) AND calls commitDesiredVolume -> scheduleVolumeApply(immediate:true), whose success path shows a second 'Managed route active — <app> set to <n>%' toast (AppStore.swift:433-438). With maxToasts=3 both appear; same for decrease. I also confirmed the same double-toast for mute: toggleMuteForFrontmostApp shows 'Muted/Unmuted' (AppStore.swift:1256) while setMuted's success path shows 'App muted/App unmuted' (AppStore.swift:528-533). So every Cmd-Opt-Up/Down/M press stacks two near-identical confirmations (and on failure, the handler's success-style toast appears alongside the error toast). | Fix: Suppress the per-apply/per-mute toast when the change originates from a keyboard shortcut (pass a silent/source flag through commitDesiredVolume/setMuted/scheduleVolumeApply), or drop the handler's own toast and rely on the apply toast.",
      "fixStatus": "3 fixed",
      "fixNotes": "tier1: FU-FRONTMOST-RESOLUTION | tier2: FU-EXCLUDED-FRONTMOST-TOASTS | tier3: FU-HOTKEY-DOUBLE-TOAST",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "correctness",
          "title": "Hotkeys target activeApps.first (sort-order dependent), not the actual frontmost app",
          "detail": "Verified end-to-end. increase/decrease/toggleMute resolve the target as `activeApps.first ?? visibleApps.first` (AppStore.swift:1212,1231,1250). activeApps = visibleApps.filter(\\.isActive) (AppStore.swift:113-114), and isActive is set to `isAudible || isFrontmost` during discovery (WorkspaceAudioControlBackend.swift:1189). The list is ordered by sortMode (sortedApps, AppStore.swift:1278): with .name it is alphabetical; with .activity an audible app ranks 0 while a merely-frontmost app ranks 2 (activityRank, AppStore.swift:1303-1320). So whenever any background app is playing audio, activeApps.first is that audible app, not the app the user is in. The global NSEvent monitor (WavesApp.swift:152,179-185) fires while a different app is frontmost but passes no identifier; the store never consults NSWorkspace.shared.frontmostApplication to pick the hotkey target (it only reads frontmostApplication in checkAutoPauseMusic, AppStore.swift:945, not in the hotkey handlers).",
          "codeRef": "AppStore.swift:1212",
          "expectedVsActual": "Expected (user story / README): Cmd-Opt-Up/Down/M adjust the frontmost app the user is using. Actual: they adjust the first active app in the current sort order, frequently a background audible app (e.g. Spotify), so the wrong app's volume/mute changes.",
          "suggestedFix": "Resolve the target by matching NSWorkspace.shared.frontmostApplication.bundleIdentifier (or pid) against session.apps, falling back to activeApps.first only when no match exists. The frontmost app is known where the event is handled (WavesApp), so pass its identifier into the store methods.",
          "confidence": "confirmed",
          "primaryFile": "AppStore.swift",
          "disposition": "tier1:applied:FU-FRONTMOST-RESOLUTION",
          "uid": "VOL-04#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-FRONTMOST-RESOLUTION",
          "fixTier": "tier1"
        },
        {
          "severity": "medium",
          "type": "ux",
          "title": "Hotkey shows a success toast even when the resolved app is excluded (no-op)",
          "detail": "Verified. increase/decreaseVolumeForFrontmostApp and toggleMuteForFrontmostApp call setDesiredVolume/setMuted, which early-return when isExcluded(app) is true (AppStore.swift:357,492), but the handlers have no exclusion guard and unconditionally show a 'Volume increased/decreased' or 'Muted/Unmuted' info toast (AppStore.swift:1221-1226,1240-1245,1256-1261). For the volume handlers the toast even reports the would-be new percentage computed locally, while app.desiredVolume never changed. If the frontmost-resolved app is excluded, nothing changes yet the user sees a confirmation toast implying it did.",
          "codeRef": "AppStore.swift:1210-1262",
          "expectedVsActual": "Expected: a hotkey on an excluded app is a safe no-op with no misleading feedback. Actual: the underlying mutation is skipped, but a success toast (with a fabricated new %) is still shown.",
          "suggestedFix": "Add `guard !isExcluded(app) else { return }` (or show an explanatory toast) at the top of each frontmost-app handler before mutating and before showing the success toast.",
          "confidence": "confirmed",
          "primaryFile": "AppStore.swift",
          "disposition": "tier2:partial:FU-EXCLUDED-FRONTMOST-TOASTS",
          "uid": "VOL-04#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "partial",
          "fixedBy": "FU-EXCLUDED-FRONTMOST-TOASTS",
          "fixTier": "tier2"
        },
        {
          "severity": "low",
          "type": "ux",
          "title": "A single volume hotkey press emits two stacked toasts (also affects mute)",
          "detail": "Verified. increaseVolumeForFrontmostApp shows a 'Volume increased' info toast (AppStore.swift:1221) AND calls commitDesiredVolume -> scheduleVolumeApply(immediate:true), whose success path shows a second 'Managed route active — <app> set to <n>%' toast (AppStore.swift:433-438). With maxToasts=3 both appear; same for decrease. I also confirmed the same double-toast for mute: toggleMuteForFrontmostApp shows 'Muted/Unmuted' (AppStore.swift:1256) while setMuted's success path shows 'App muted/App unmuted' (AppStore.swift:528-533). So every Cmd-Opt-Up/Down/M press stacks two near-identical confirmations (and on failure, the handler's success-style toast appears alongside the error toast).",
          "codeRef": "AppStore.swift:1221",
          "expectedVsActual": "Expected: each shortcut surfaces one brief confirmation toast. Actual: two toasts per keypress (the handler's own toast plus the async apply/mute toast); on failure a misleading success-style toast appears next to the error toast.",
          "suggestedFix": "Suppress the per-apply/per-mute toast when the change originates from a keyboard shortcut (pass a silent/source flag through commitDesiredVolume/setMuted/scheduleVolumeApply), or drop the handler's own toast and rely on the apply toast.",
          "confidence": "confirmed",
          "primaryFile": "AppStore.swift",
          "disposition": "tier3:applied:FU-HOTKEY-DOUBLE-TOAST",
          "uid": "VOL-04#3",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-HOTKEY-DOUBLE-TOAST",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "VOL-05",
      "area": "Per-App Volume Control",
      "feature": "Exclude-from-Waves disables all per-app controls",
      "priority": "P2",
      "userStory": "As a user, I want to exclude certain apps from Waves, so that their audio is left untouched and I can't accidentally change their volume, mute, or boost.",
      "expectedBehavior": [
        "The row context menu and accessibility action offer 'Exclude from Waves' / 'Manage with Waves' toggling store.setExcluded (MixerRowView.swift:159-168)",
        "Excluded rows dim to 0.55 opacity, show an 'Excluded' badge, hide the route-state chip, and disable the slider, boost menu, and mute button (MixerRowView.swift:29-37, 81, 94, 107, 109, 273-279, 308-311, 324, 326)",
        "store.setDesiredVolume, setMuted, setVolumeBoost, and setOutputDevice all early-return when isExcluded(app) is true (AppStore.swift:357, 492, 545, 650)",
        "On exclusion the store cancels any pending volume-apply task and clears the pending volume target for that app (AppStore.swift:709-710)",
        "Excluded apps are skipped by bulk/auto operations (e.g. boost-all and auto-mute restore loops) (AppStore.swift:826, 959, 973)"
      ],
      "acceptanceCriteria": [
        "Excluding an app dims its row, shows the Excluded badge, and disables its slider/boost/mute controls",
        "Calls to setDesiredVolume/setMuted/setVolumeBoost/setOutputDevice for an excluded app are no-ops",
        "Excluding an app cancels its in-flight debounced volume apply and clears its pending target",
        "Re-selecting 'Manage with Waves' restores the controls",
        "Excluded apps are not affected by bulk boost or auto-mute/auto-resume routines"
      ],
      "codeRefs": [
        "MixerRowView.swift:159-168",
        "MixerRowView.swift:273-279",
        "AppStore.swift:357",
        "AppStore.swift:492",
        "AppStore.swift:545",
        "AppStore.swift:686-710"
      ],
      "implStatus": "implemented",
      "implNotes": "Exclusion is consistently enforced at both the UI (disabled controls) and store (early-return) layers, plus pending-task cleanup, so it can't be bypassed via the slider. It does not by itself tear down an already-active managed controller in this guard path — exclusion relies on the controls being disabled; verify whether a previously-managed app keeps its tap alive after exclusion (the setExcluded path at 686-710 cancels pending applies but the read excerpt does not show explicit controller disposal/route restoration to original audio).",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1M]",
      "errorsFound": "• [MEDIUM/logic] Per-device volume preset restore re-applies routes to excluded apps (AppStore.swift:848-862) — Verified. setDesiredVolume/setMuted/setVolumeBoost/setOutputDevice and the auto-mute/auto-resume loops all skip excluded apps (AppStore.swift:357,492,545,650,959,973), and reapplyRestoredAudioState guards with `!isExcluded` (line 826). However restoreDeviceVolumePresets iterates every app and calls backend.setDesiredVolume/setMuted/setVolumeBoost with no isExcluded check (AppStore.swift:848-862). When the output device changes (handleDeviceChange -> restoreDeviceVolumePresets, AppStore.swift:802-803) and a stored preset exists for an excluded app, Waves re-taps and re-applies volume/mute/boost to an app the user explicitly excluded — establishing a managed route on it. Note a preset is recorded for any app the user touched before excluding it, so this is reachable. Raised severity to medium: it violates the core exclusion guarantee ('excluded apps are never tapped') by silently establishing a tap, not merely a cosmetic glitch. | Fix: Add `guard !isExcluded(app) else { continue }` inside the restoreDeviceVolumePresets loop before applying any backend changes, mirroring reapplyRestoredAudioState.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier2: FU-EXCLUSION-PRESET-RESTORE",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "logic",
          "title": "Per-device volume preset restore re-applies routes to excluded apps",
          "detail": "Verified. setDesiredVolume/setMuted/setVolumeBoost/setOutputDevice and the auto-mute/auto-resume loops all skip excluded apps (AppStore.swift:357,492,545,650,959,973), and reapplyRestoredAudioState guards with `!isExcluded` (line 826). However restoreDeviceVolumePresets iterates every app and calls backend.setDesiredVolume/setMuted/setVolumeBoost with no isExcluded check (AppStore.swift:848-862). When the output device changes (handleDeviceChange -> restoreDeviceVolumePresets, AppStore.swift:802-803) and a stored preset exists for an excluded app, Waves re-taps and re-applies volume/mute/boost to an app the user explicitly excluded — establishing a managed route on it. Note a preset is recorded for any app the user touched before excluding it, so this is reachable. Raised severity to medium: it violates the core exclusion guarantee ('excluded apps are never tapped') by silently establishing a tap, not merely a cosmetic glitch.",
          "codeRef": "AppStore.swift:848-862",
          "expectedVsActual": "Expected (story): excluded apps are never tapped and are skipped by automatic/bulk routines. Actual: the device-preset restore loop ignores exclusion and can establish a managed route on an excluded app on device switch.",
          "suggestedFix": "Add `guard !isExcluded(app) else { continue }` inside the restoreDeviceVolumePresets loop before applying any backend changes, mirroring reapplyRestoredAudioState.",
          "confidence": "confirmed",
          "primaryFile": "AppStore.swift",
          "disposition": "tier2:partial:FU-EXCLUSION-PRESET-RESTORE",
          "uid": "VOL-05#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "partial",
          "fixedBy": "FU-EXCLUSION-PRESET-RESTORE",
          "fixTier": "tier2"
        }
      ]
    },
    {
      "id": "VOL-06",
      "area": "Per-App Volume Control",
      "feature": "Monitor-only vs managed route state (honest control gating)",
      "priority": "P1",
      "userStory": "As a user, I want the row to clearly tell me whether Waves is merely watching an app or actually controlling its audio, so that I understand when a volume change will or won't take effect.",
      "expectedBehavior": [
        "Apps start as .live (currently audible) or .monitorOnly (visible but not managed); only after a volume/mute/boost change does applyRoute create a tap and promote the app to .managed (WorkspaceAudioControlBackend.swift:1178, 1036-1055, 360-387)",
        "RoutingStateIndicator/Dot render per-state icon, color, and help text: managed=green check 'Managed route is active', live=accent waveform, monitorOnly hidden chip with hint 'Move the slider or mute the app to start per-app control', error=red triangle 'Route setup failed' (MixerRowView.swift:390-511)",
        "On an unsupported OS (<macOS 14.2) all apps are forced to .monitorOnly with note 'Per-app route requires macOS 14.2+' and compatibility .planned (WorkspaceAudioControlBackend.swift:1037-1042, 310-316)",
        "If a managed route's controller is no longer active the app is demoted back to .monitorOnly on the next snapshot (WorkspaceAudioControlBackend.swift:1044-1054, 1104-1124)",
        "When routingState==.error, the row displays the failure note text inline beneath the controls (MixerRowView.swift:111-116)",
        "slider/mute help text changes based on canControlAudio (routingState==.managed) to explain that adjusting will enroll the app (MixerRowView.swift:207-221)"
      ],
      "acceptanceCriteria": [
        "A freshly listed audible app shows a 'live' chip and a not-yet-managed app shows no chip with a hint to start control",
        "After the first volume/mute/boost change, the app shows the green 'managed' chip",
        "On macOS older than 14.2, every app stays monitor-only with the explanatory note and no route is attempted",
        "A managed app whose controller dies reverts to monitor-only on the next refresh",
        "A route setup failure shows the red error chip plus the inline error note"
      ],
      "codeRefs": [
        "WorkspaceAudioControlBackend.swift:1036-1055",
        "WorkspaceAudioControlBackend.swift:1178",
        "WorkspaceAudioControlBackend.swift:360-387",
        "MixerRowView.swift:390-511",
        "MixerRowView.swift:111-116",
        "MixerRowView.swift:207-221"
      ],
      "implStatus": "implemented",
      "implNotes": "This is the 'honest routing' promise from PRODUCT.md and is genuinely implemented: control is lazily enrolled on first interaction and state is recomputed each snapshot. Edge case worth a test: an app that is denied audio-capture (TCC) will repeatedly fail applyRoute (3 retries with backoff) and land in .error rather than monitor-only, so the user sees a red chip + 'couldn't take over audio' message rather than a permission-specific prompt — capture permission is surfaced separately in diagnostics, not on the row itself (WorkspaceAudioControlBackend.swift:389-427, 288-299).",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1M]",
      "errorsFound": "• [MEDIUM/edge-case] On macOS <14.2, moving the slider attempts a route and flashes a red error chip + generic failure toast instead of staying monitor-only (WorkspaceAudioControlBackend.swift:53-83) — Verified. The store layer has no OS gate (grep confirms no supportsPerAppRouting/@available/14.2 check in AppStore before invoking the backend; backendStatus.isAudioComponentInstalled is only read for onboarding/diagnostics). On an unsupported OS setDesiredVolume/setMuted/setVolumeBoost still call the backend; applyRoute throws BackendError.unsupportedOperation immediately (WorkspaceAudioControlBackend.swift:361-363); the backend setter catch sets routingState=.error with the note and rethrows (e.g. lines 76-81). The store then shows a 'Volume change failed' error toast (AppStore.swift:447) and only the subsequent refresh() rebuilds the snapshot, forcing the app back to .monitorOnly with the 'Per-app route requires macOS 14.2+' note (WorkspaceAudioControlBackend.swift:1037-1041). So a route IS attempted and the user transiently sees a red error chip plus a generic failure toast rather than the calm monitor-only explanation. Upgraded confidence from likely to confirmed: every step is directly verifiable in code. | Fix: Gate control entry on OS support before invoking the backend (expose supportsPerAppRouting or check backendStatus.isAudioComponentInstalled in the store), or special-case BackendError.unsupportedOperation to set .monitorOnly with the 14.2 note and suppress the error toast, so unsupported OSes never flash an error state.",
      "fixStatus": "1 fixed",
      "fixNotes": "",
      "retestStatus": "PASS (remediated & verified)",
      "issues": [
        {
          "severity": "medium",
          "type": "edge-case",
          "title": "On macOS <14.2, moving the slider attempts a route and flashes a red error chip + generic failure toast instead of staying monitor-only",
          "detail": "Verified. The store layer has no OS gate (grep confirms no supportsPerAppRouting/@available/14.2 check in AppStore before invoking the backend; backendStatus.isAudioComponentInstalled is only read for onboarding/diagnostics). On an unsupported OS setDesiredVolume/setMuted/setVolumeBoost still call the backend; applyRoute throws BackendError.unsupportedOperation immediately (WorkspaceAudioControlBackend.swift:361-363); the backend setter catch sets routingState=.error with the note and rethrows (e.g. lines 76-81). The store then shows a 'Volume change failed' error toast (AppStore.swift:447) and only the subsequent refresh() rebuilds the snapshot, forcing the app back to .monitorOnly with the 'Per-app route requires macOS 14.2+' note (WorkspaceAudioControlBackend.swift:1037-1041). So a route IS attempted and the user transiently sees a red error chip plus a generic failure toast rather than the calm monitor-only explanation. Upgraded confidence from likely to confirmed: every step is directly verifiable in code.",
          "codeRef": "WorkspaceAudioControlBackend.swift:53-83",
          "expectedVsActual": "Expected (acceptance criterion): on macOS older than 14.2 every app stays monitor-only with the explanatory note and no route is attempted. Actual: an apply is attempted, throws, transiently sets .error, and raises a generic 'Volume change failed' toast; only the next snapshot rebuild restores monitor-only.",
          "suggestedFix": "Gate control entry on OS support before invoking the backend (expose supportsPerAppRouting or check backendStatus.isAudioComponentInstalled in the store), or special-case BackendError.unsupportedOperation to set .monitorOnly with the 14.2 note and suppress the error toast, so unsupported OSes never flash an error state.",
          "confidence": "confirmed",
          "primaryFile": "WorkspaceAudioControlBackend.swift",
          "disposition": "gapfill:fixed",
          "uid": "VOL-06#1",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "fixStatus": "applied",
          "fixedBy": "gapfill",
          "fixTier": "gapfill"
        }
      ]
    },
    {
      "id": "MUTE-01",
      "area": "Per-App Mute/Unmute & Mute Provenance",
      "feature": "Manual per-app mute/unmute toggle",
      "priority": "P0",
      "userStory": "As a macOS user running several audio apps, I want to mute or unmute a specific app from its mixer row, so that I can silence one app instantly without quitting it or touching system volume.",
      "expectedBehavior": [
        "Each mixer row (full and compact) shows a speaker button: filled speaker.slash.fill icon when muted, speaker.wave.2.fill when not (MixerRowView.swift:100, :317).",
        "Tapping the button calls store.setMuted(!app.isMuted, for: app) (MixerRowView.swift:97, :314).",
        "setMuted updates session.apps[index].isMuted and sets appliedVolume to 0 when muted, else desiredVolume (AppStore.swift:497-498).",
        "The backend silences the app: the IO render callback zeroes the output buffer whenever isMuted != 0 (WorkspaceAudioControlBackend.swift:1695-1698), and the process tap is created with muteBehavior=.mutedWhenTapped so the app's normal path is muted too (line 450).",
        "A success toast 'App muted'/'App unmuted' with the app name is shown on success (AppStore.swift:528-533); a 'Mute toggle failed' error toast on backend failure (line 539).",
        "The change is persisted to the session store after applying (AppStore.swift:527).",
        "Muting bounces the speaker symbol and emits selection sensory feedback unless Reduce Motion is on (MixerRowView.swift:98, :101, :106).",
        "Accessibility label flips between 'Mute <app>' and 'Unmute <app>' (MixerRowView.swift:105, :322)."
      ],
      "acceptanceCriteria": [
        "Clicking the mute button on an unmuted managed app silences its audio and shows the filled slash icon.",
        "Clicking again restores audio at the app's desiredVolume and shows the wave icon.",
        "After a mute, the app's appliedVolume reads 0; after unmute it equals desiredVolume.",
        "On a backend error the row state is re-synced from the backend snapshot and an error toast appears.",
        "VoiceOver announces 'Mute <app>' / 'Unmute <app>' matching the current state."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:491-542",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:96-107",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:313-324",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:85-111",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1695-1698"
      ],
      "implStatus": "implemented",
      "implNotes": "Fully wired end-to-end: UI -> store -> actor backend -> DSP zeroing, with optimistic UI update, persistence, error rollback (re-merge from backend snapshot), and accessibility. Mute is enforced both by zeroing the re-rendered tap output and by the tap's mutedWhenTapped behavior, so audio truly stops. No correctness concerns found in the manual path itself.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1M]",
      "errorsFound": "• [MEDIUM/error-handling] On backend applyRoute failure the row shows the muted icon while audio is NOT silenced (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:90) — WorkspaceAudioControlBackend.setMuted assigns snapshot.apps[index].isMuted = isMuted at line 90 BEFORE the do/catch and never reverts it in the catch (lines 101-109). On a failed mute (tap creation fails: permission revoked mid-session, device unplugged, app quit) the backend keeps isMuted == true with routingState == .error. mergeAppState copies that whole app back into the store (AppStore.swift:1425). The mixer row icon is driven purely by app.isMuted (MixerRowView.swift:100, :317), so it renders speaker.slash.fill (muted) even though no tap was established and the app is still playing at full volume. There is an error notes line (MixerRowView.swift:111) and an error toast, but the mute glyph itself contradicts the real audio state. Verified: the catch block leaves isMuted untouched and only flips routingState/notes/levels. | Fix: In the setMuted catch block, capture the prior isMuted before line 90 and restore snapshot.apps[index].isMuted to it on failure, or introduce a distinct 'mute could not be applied' presentation so the icon does not assert a silence that did not happen.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier2: FU-MUTE-FAILURE-STALE-ICON",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "error-handling",
          "title": "On backend applyRoute failure the row shows the muted icon while audio is NOT silenced",
          "detail": "WorkspaceAudioControlBackend.setMuted assigns snapshot.apps[index].isMuted = isMuted at line 90 BEFORE the do/catch and never reverts it in the catch (lines 101-109). On a failed mute (tap creation fails: permission revoked mid-session, device unplugged, app quit) the backend keeps isMuted == true with routingState == .error. mergeAppState copies that whole app back into the store (AppStore.swift:1425). The mixer row icon is driven purely by app.isMuted (MixerRowView.swift:100, :317), so it renders speaker.slash.fill (muted) even though no tap was established and the app is still playing at full volume. There is an error notes line (MixerRowView.swift:111) and an error toast, but the mute glyph itself contradicts the real audio state. Verified: the catch block leaves isMuted untouched and only flips routingState/notes/levels.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:90",
          "expectedVsActual": "Expected (MUTE-01: 'On a backend error the row state is re-synced from the backend snapshot'): a failed mute should not leave the row claiming the app is silenced. Actual: the backend snapshot itself keeps isMuted = requested value on the error path, so the re-synced row shows the slashed-speaker icon while audio keeps playing.",
          "suggestedFix": "In the setMuted catch block, capture the prior isMuted before line 90 and restore snapshot.apps[index].isMuted to it on failure, or introduce a distinct 'mute could not be applied' presentation so the icon does not assert a silence that did not happen.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "tier2:applied:FU-MUTE-FAILURE-STALE-ICON",
          "uid": "MUTE-01#1",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-MUTE-FAILURE-STALE-ICON",
          "fixTier": "tier2"
        }
      ]
    },
    {
      "id": "MUTE-02",
      "area": "Per-App Mute/Unmute & Mute Provenance",
      "feature": "Mute provenance — never auto-resume a user-muted app",
      "priority": "P0",
      "userStory": "As a user who deliberately muted an app, I want Waves to remember that I muted it myself, so that automatic features (like call auto-pause) never silently un-mute it behind my back.",
      "expectedBehavior": [
        "AudioApp carries a muteSource: MuteSource field, either .user or .autoConferencing (AudioApp.swift:24-27, :193-198).",
        "Any direct user mute/unmute via setMuted always stamps muteSource = .user (AppStore.swift:500-501).",
        "Auto-pause stamps muteSource = .autoConferencing only when Waves itself mutes a media app for a call (AppStore.swift:964).",
        "Auto-resume only un-mutes apps whose muteSource is still .autoConferencing, explicitly excluding user-muted apps (AppStore.swift:972-973).",
        "muteSource is Codable and persisted, and is carried across relaunch merges (AppStore.swift:1403) and live backend snapshot merges (WorkspaceAudioControlBackend.swift:1011); legacy session files without the field decode as .user (AudioApp.swift:139).",
        "Toggling a preset/URL/keyboard mute also flows through setMuted, so it is treated as user-sourced."
      ],
      "acceptanceCriteria": [
        "If the user mutes a media app, then a conferencing app becomes frontmost and later un-frontmost, the user-muted app stays muted (it is not in the resumable set).",
        "An app auto-paused by Waves has muteSource == .autoConferencing until the user touches it.",
        "A session file written before muteSource existed decodes with muteSource == .user (covered by AppDiscoveryPolicyTests.swift:62-67).",
        "muteSource round-trips through encode/decode (AppDiscoveryPolicyTests.swift:69-72)."
      ],
      "codeRefs": [
        "Sources/WavesAudioCore/Models/AudioApp.swift:24-27",
        "Sources/WavesAudioCore/Models/AudioApp.swift:193-198",
        "Sources/Waves/Stores/AppStore.swift:500-501",
        "Sources/Waves/Stores/AppStore.swift:972-983",
        "Sources/Waves/Stores/AppStore.swift:1403",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1011",
        "Tests/WavesTests/AppDiscoveryPolicyTests.swift:62-72"
      ],
      "implStatus": "implemented",
      "implNotes": "The core provenance guarantee is correctly implemented: auto-resume strictly filters on muteSource == .autoConferencing, so a user-muted app is never auto-resumed, and the field survives relaunch and legacy decode. Minor wrinkle: on auto-resume the code writes the unmuted app's muteSource back to .user (AppStore.swift:977, :992) rather than a neutral value; harmless because the app is being unmuted, but it means an unmuted app's provenance is always .user (the default), which is fine. Covered by unit tests for decode default and round-trip.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1H]",
      "errorsFound": "• [HIGH/correctness] Any whole-session backend re-sync clobbers .autoConferencing provenance with backend's stale .user, breaking auto-resume (Sources/Waves/Stores/AppStore.swift:1425) — Auto-pause records provenance ONLY in the store session (AppStore.swift:992 sets muteSource = .autoConferencing). It is never written into the backend snapshot: the backend's setMuted/applyPreset never assign .autoConferencing, and the only backend handling is app.muteSource = previous.muteSource (WorkspaceAudioControlBackend.swift:1011), which for an auto-paused app always resolves to the .user default. The store and backend therefore diverge (store=.autoConferencing, backend=.user). Numerous common operations replace the whole store session from the backend and thus overwrite .autoConferencing with .user: mergeAppState after a volume edit (AppStore.swift:429) or boost edit (:571), and full replacements at refresh() (:242), togglePinned (:596), device change (:799), applyPreset (:1010), and recoverRoutes (:1184). After any of these while the conferencing app is still frontmost, muteSource(app) == .user, so the auto-resume filter `$0.muteSource == .autoConferencing` (AppStore.swift:973) no longer matches and the music stays muted with no indication Waves muted it. (Note: the post-refresh checkAutoPauseMusic at :248 cannot re-tag because previousFrontmostApp is unchanged and the guard at :947 short-circuits.) | Fix: Propagate the mute source into the backend (have backend.setMuted accept/store a source and have auto-pause push .autoConferencing into the backend snapshot), OR preserve the store's muteSource across every backend re-merge (e.g. carry session muteSource forward in mergeAppState and in the cached/live merge instead of taking the backend's value).",
      "fixStatus": "1 deferred",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "correctness",
          "title": "Any whole-session backend re-sync clobbers .autoConferencing provenance with backend's stale .user, breaking auto-resume",
          "detail": "Auto-pause records provenance ONLY in the store session (AppStore.swift:992 sets muteSource = .autoConferencing). It is never written into the backend snapshot: the backend's setMuted/applyPreset never assign .autoConferencing, and the only backend handling is app.muteSource = previous.muteSource (WorkspaceAudioControlBackend.swift:1011), which for an auto-paused app always resolves to the .user default. The store and backend therefore diverge (store=.autoConferencing, backend=.user). Numerous common operations replace the whole store session from the backend and thus overwrite .autoConferencing with .user: mergeAppState after a volume edit (AppStore.swift:429) or boost edit (:571), and full replacements at refresh() (:242), togglePinned (:596), device change (:799), applyPreset (:1010), and recoverRoutes (:1184). After any of these while the conferencing app is still frontmost, muteSource(app) == .user, so the auto-resume filter `$0.muteSource == .autoConferencing` (AppStore.swift:973) no longer matches and the music stays muted with no indication Waves muted it. (Note: the post-refresh checkAutoPauseMusic at :248 cannot re-tag because previousFrontmostApp is unchanged and the guard at :947 short-circuits.)",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1425",
          "expectedVsActual": "Expected (MUTE-02): an app auto-paused by Waves keeps muteSource == .autoConferencing until the user explicitly mutes/unmutes it, so auto-resume can restore it. Actual: a plain refresh, a pin, a device change, a preset apply, or any volume/boost change on the auto-paused app re-pulls the backend snapshot and silently rewrites muteSource to .user, permanently breaking auto-resume for that app.",
          "suggestedFix": "Propagate the mute source into the backend (have backend.setMuted accept/store a source and have auto-pause push .autoConferencing into the backend snapshot), OR preserve the store's muteSource across every backend re-merge (e.g. carry session muteSource forward in mergeAppState and in the cached/live merge instead of taking the backend's value).",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-user-decision:Provenance ownership (store vs backend) is a design decision spanning the backend file and all re-merge paths; out of allowed single-file scope and risks regres",
          "uid": "MUTE-02#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "MUTE-03",
      "area": "Per-App Mute/Unmute & Mute Provenance",
      "feature": "Auto-pause media apps during conference calls",
      "priority": "P1",
      "userStory": "As someone who joins video calls with music playing, I want Waves to automatically mute my media apps when a conferencing app comes to the front and restore them afterward, so that I don't blast music into a meeting or have to scramble for the mute button.",
      "expectedBehavior": [
        "Behavior is gated by preferences.autoPauseMusicForConferencing (default true), toggleable in Settings (AppStore.swift:941; SettingsView.swift:91-93; UserPreferences.swift:10).",
        "On every frontmost-app change (and at startup/refresh/preset apply) checkAutoPauseMusic runs; it only acts when the frontmost bundle actually changed (AppStore.swift:946-948).",
        "When the new frontmost app is categorized as conferencing, all visible media-category apps that are currently unmuted and not excluded are muted via the backend and tagged .autoConferencing, and tracked in pausedMusicApps (AppStore.swift:957-969).",
        "When the frontmost app is not conferencing, apps Waves auto-paused (muteSource == .autoConferencing, not excluded) are un-muted and pausedMusicApps is cleared (AppStore.swift:970-984).",
        "Only the affected apps' mute state is mutated in place (not a whole-session replace), to avoid wiping launch-restored state (AppStore.swift:986-997).",
        "Frontmost detection uses the live NSWorkspace.frontmostApplication rather than the periodically-refreshed snapshot (AppStore.swift:945)."
      ],
      "acceptanceCriteria": [
        "With the preference on, switching focus to a conferencing app mutes a playing, unmuted media app and tags it .autoConferencing.",
        "Switching focus away from conferencing (to a non-conferencing app) un-mutes only those Waves-auto-paused apps.",
        "A media app the user had already muted is not touched by auto-resume (overlaps MUTE-02).",
        "Excluded apps are never auto-paused (filter excludes isExcluded).",
        "With the preference off, no automatic mute/unmute occurs."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:940-999",
        "Sources/Waves/Stores/AppStore.swift:886-897",
        "Sources/Waves/Settings/SettingsView.swift:91-93",
        "Sources/Waves/Stores/UserPreferences.swift:10"
      ],
      "implStatus": "implemented",
      "implNotes": "Works and is observer-driven off NSWorkspace.didActivateApplication. Edge-case gaps: (1) auto-pause only fires on a frontmost-app CHANGE — enabling the preference (or launching) while a conferencing app is already frontmost will not pause music until focus changes, because the previousFrontmostApp guard short-circuits (line 947). (2) 'conferencing' is inferred purely from the frontmost app's category, not from whether a call/mic is actually active, so simply focusing (e.g.) a Zoom window with no call mutes media. (3) Resume happens on focusing ANY non-conferencing app, so quickly tabbing away mid-call resumes music. These are product-behavior nuances rather than crashes; the core mute/restore plumbing is sound.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1M 1L]",
      "errorsFound": "• [MEDIUM/concurrency] Rapid frontmost changes interleave the pause and resume tasks, leaving music stuck muted (Sources/Waves/Stores/AppStore.swift:955) — checkAutoPauseMusic updates previousFrontmostApp synchronously then spawns a Task that performs per-app `await backend.setMuted` and only writes the resulting mute state / muteSource into session AFTER all awaits (AppStore.swift:989-995). AppStore is @MainActor and backend is an actor, so each `await backend.setMuted` genuinely suspends the task on the main actor. Two quick frontmost changes (conferencing -> non-conferencing) enqueue Task A (pause) and Task B (resume). While Task A is suspended on its await — before it has written .autoConferencing into session (line 992) — Task B runs, computes resumable from the still-unpaused session (sees nothing), calls pausedMusicApps.removeAll() (line 983), and returns. Task A then resumes, finishes, and writes the app as muted/.autoConferencing while the user is already on a non-conferencing app, with no resume pending. The interleave is structurally guaranteed by the deferred session write, not merely possible. | Fix: Serialize auto-pause handling: cancel the prior in-flight checkAutoPauseMusic task before starting a new one, or funnel pause/resume through a single async sequence, and apply session/pausedMusicApps mutations atomically relative to the backend calls (e.g. mark intent in session before awaiting).\n• [LOW/edge-case] Enabling auto-pause while a conferencing app is already frontmost does not pause music until focus changes (Sources/Waves/Stores/AppStore.swift:947) — checkAutoPauseMusic short-circuits when currentFrontmostApp == previousFrontmostApp (AppStore.swift:947). The Settings 'Auto-pause music during calls' toggle setter only writes the preference and persists (SettingsView.swift:92-95); it never calls checkAutoPauseMusic and never resets previousFrontmostApp. So if a conferencing app was already frontmost when the user turns the preference on, no auto-pause happens until the user switches focus away and back. Verified the toggle setter has no side-effect call into the store's auto-pause routine. | Fix: From the SettingsView auto-pause toggle setter, when turning the preference on, reset previousFrontmostApp to nil and call store.checkAutoPauseMusic() so the current frontmost app is evaluated immediately.",
      "fixStatus": "1 fixed · 1 deferred",
      "fixNotes": "tier3: FU-AUTOPAUSE-ENABLE-NOW",
      "retestStatus": "PASS (remediated & verified)",
      "issues": [
        {
          "severity": "medium",
          "type": "concurrency",
          "title": "Rapid frontmost changes interleave the pause and resume tasks, leaving music stuck muted",
          "detail": "checkAutoPauseMusic updates previousFrontmostApp synchronously then spawns a Task that performs per-app `await backend.setMuted` and only writes the resulting mute state / muteSource into session AFTER all awaits (AppStore.swift:989-995). AppStore is @MainActor and backend is an actor, so each `await backend.setMuted` genuinely suspends the task on the main actor. Two quick frontmost changes (conferencing -> non-conferencing) enqueue Task A (pause) and Task B (resume). While Task A is suspended on its await — before it has written .autoConferencing into session (line 992) — Task B runs, computes resumable from the still-unpaused session (sees nothing), calls pausedMusicApps.removeAll() (line 983), and returns. Task A then resumes, finishes, and writes the app as muted/.autoConferencing while the user is already on a non-conferencing app, with no resume pending. The interleave is structurally guaranteed by the deferred session write, not merely possible.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:955",
          "expectedVsActual": "Expected (MUTE-03): switching focus away from conferencing un-mutes the apps Waves auto-paused. Actual: under rapid focus toggling the pause and resume tasks interleave at their await points, so an app can remain auto-muted after focus has already moved to a non-conferencing app.",
          "suggestedFix": "Serialize auto-pause handling: cancel the prior in-flight checkAutoPauseMusic task before starting a new one, or funnel pause/resume through a single async sequence, and apply session/pausedMusicApps mutations atomically relative to the backend calls (e.g. mark intent in session before awaiting).",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-hardware:Reordering pause/resume around backend calls changes auto-pause audio behavior that only validates on real hardware.",
          "uid": "MUTE-03#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "low",
          "type": "edge-case",
          "title": "Enabling auto-pause while a conferencing app is already frontmost does not pause music until focus changes",
          "detail": "checkAutoPauseMusic short-circuits when currentFrontmostApp == previousFrontmostApp (AppStore.swift:947). The Settings 'Auto-pause music during calls' toggle setter only writes the preference and persists (SettingsView.swift:92-95); it never calls checkAutoPauseMusic and never resets previousFrontmostApp. So if a conferencing app was already frontmost when the user turns the preference on, no auto-pause happens until the user switches focus away and back. Verified the toggle setter has no side-effect call into the store's auto-pause routine.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:947",
          "expectedVsActual": "Expected (MUTE-03): with the preference on, focusing a conferencing app mutes playing media. Actual: if the conferencing app was already frontmost when the preference is enabled, no auto-pause occurs until the next frontmost change.",
          "suggestedFix": "From the SettingsView auto-pause toggle setter, when turning the preference on, reset previousFrontmostApp to nil and call store.checkAutoPauseMusic() so the current frontmost app is evaluated immediately.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier3:applied:FU-AUTOPAUSE-ENABLE-NOW",
          "uid": "MUTE-03#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-AUTOPAUSE-ENABLE-NOW",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "MUTE-04",
      "area": "Per-App Mute/Unmute & Mute Provenance",
      "feature": "Mute state persists and is re-applied across relaunch",
      "priority": "P1",
      "userStory": "As a user who muted certain apps, I want those mutes to still be in effect after I quit and reopen Waves (or the app), so that my routing setup is durable and I don't have to redo it every launch.",
      "expectedBehavior": [
        "The session snapshot (including isMuted, appliedVolume, muteSource) is saved on every mute change (AppStore.swift:527, persistSessionSnapshot -> sessionStore.save).",
        "On launch the cached session is loaded and merged with the freshly-built live snapshot, preserving isMuted, appliedVolume, and muteSource per logical app (AppStore.swift:1383-1405).",
        "After merge, reapplyRestoredAudioState re-applies saved mute/volume/boost to the backend for any 'customized' app (isMuted true, boost>1, non-default volume, or pinned device) so audible output matches the restored UI (AppStore.swift:820-845, esp. :829, :834).",
        "Apps not running this session are skipped gracefully (caught error, saved state left intact) (AppStore.swift:840-843).",
        "The backend's own buildSnapshot merge also carries isMuted and muteSource forward from the previous snapshot (WorkspaceAudioControlBackend.swift:1007, :1011)."
      ],
      "acceptanceCriteria": [
        "Muting app A, quitting Waves, and relaunching shows app A still muted in the mixer.",
        "After relaunch, app A's audio is actually silenced (backend route re-applied), not just visually muted.",
        "A user-muted app restored from disk retains muteSource == .user so auto-resume won't unmute it.",
        "If app A is not running on relaunch, its saved muted state is preserved without error."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:820-845",
        "Sources/Waves/Stores/AppStore.swift:1383-1405",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:999-1014",
        "Sources/Waves/Services/Persistence/SessionStore.swift:78"
      ],
      "implStatus": "implemented",
      "implNotes": "Persistence and restore are wired and re-apply audio (not just UI) on launch. One subtlety: reapplyRestoredAudioState calls backend.setVolumeBoost/setMuted/setDesiredVolume which each create or reuse a managed route — for a muted app this re-establishes a tap purely to keep it silenced, which is correct but means a previously-untapped, user-muted app becomes 'managed' on relaunch. Restore is best-effort and silently skips apps that aren't running. No data-loss issues observed.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "MUTE-05",
      "area": "Per-App Mute/Unmute & Mute Provenance",
      "feature": "Mute via global keyboard shortcut and URL scheme automation",
      "priority": "P2",
      "userStory": "As a power user, I want to toggle mute for the frontmost app with ⌘⌥M or via a waves:// URL, so that I can control audio hands-free or from scripts and automations.",
      "expectedBehavior": [
        "⌘⌥M toggles mute for the frontmost (or first visible) app when keyboard shortcuts are enabled (WavesApp.swift:174-185; AppStore.swift:1248-1262).",
        "The global key monitor ignores the shortcut while typing in a text field and requires both Command and Option (WavesApp.swift:168-177).",
        "The shortcut is no-op when preferences.enableKeyboardShortcuts is false (AppStore.swift:1249); the monitor is installed/removed based on that preference (AppStore.swift:1272-1276).",
        "Space and 'm' also toggle mute for the focused row inside the main window list (MainWindowView.swift:381-382).",
        "waves://mute?app=ID&muted=true|false validates inputs (app id <=256 chars, muted parses as Bool), looks up the app, and calls setMuted; invalid input or unknown app shows a warning toast (AppStore.swift:312-328).",
        "URL-scheme commands are gated by preferences.enableURLScheme and rate-limited to 10 well-formed commands per 60s (AppStore.swift:256-273, :345-354).",
        "All three paths flow through setMuted, so muteSource becomes .user (overlaps MUTE-02)."
      ],
      "acceptanceCriteria": [
        "Pressing ⌘⌥M with shortcuts enabled toggles mute on the frontmost app and shows a 'Muted'/'Unmuted' toast.",
        "Pressing ⌘⌥M while focused in a text field does nothing.",
        "With keyboard shortcuts disabled, ⌘⌥M does nothing.",
        "A valid waves://mute?app=...&muted=true URL mutes the named app; a malformed one shows 'URL command blocked'.",
        "URL mute is rejected when URL schemes are disabled or the rate limit is exceeded."
      ],
      "codeRefs": [
        "Sources/Waves/App/WavesApp.swift:164-189",
        "Sources/Waves/Stores/AppStore.swift:1248-1262",
        "Sources/Waves/Stores/AppStore.swift:312-328",
        "Sources/Waves/Stores/AppStore.swift:256-273",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:381-382"
      ],
      "implStatus": "implemented",
      "implNotes": "All three alternate mute entry points are wired and converge on setMuted with input validation and rate-limiting on the URL path. The frontmost-app target for the keyboard shortcut is 'activeApps.first ?? visibleApps.first', which is a heuristic — if no app is flagged active it falls back to the alphabetically-first visible app, which may not be the app the user is actually looking at. Minor UX risk, not a defect in the mute mechanism itself.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1M 1L]",
      "errorsFound": "• [MEDIUM/ux] Global ⌘⌥M shows a misleading 'Muted'/'Unmuted' toast even when the target app is excluded (no-op) (Sources/Waves/Stores/AppStore.swift:1256) — toggleMuteForFrontmostApp picks activeApps.first ?? visibleApps.first (AppStore.swift:1250). visibleApps only filters by category/system, not exclusion (AppStore.swift:103-107), so the resolved frontmost app can be excluded. setMuted is a guarded no-op for excluded apps (AppStore.swift:492, returns before touching state or backend), but toggleMuteForFrontmostApp unconditionally shows a 'Muted'/'Unmuted' toast afterward (AppStore.swift:1256-1261). The user gets positive feedback that the app was muted when nothing happened. | Fix: Resolve the target app, then `guard !isExcluded(app) else { showToast('Cannot mute excluded app'...) ; return }` before mutating or showing the success toast.\n• [LOW/inconsistency] ⌘⌥M emits two overlapping toasts for one action (Sources/Waves/Stores/AppStore.swift:1256) — toggleMuteForFrontmostApp shows an immediate 'Muted'/'Unmuted' info toast (AppStore.swift:1256-1261), then setMuted asynchronously shows its own 'App muted'/'App unmuted' success toast (AppStore.swift:528-533). A single ⌘⌥M press therefore produces two near-simultaneous toasts with different wording, unlike the in-window and URL paths which produce one. Confirmed both toast call sites fire for a non-excluded app. | Fix: Remove the toast from toggleMuteForFrontmostApp and rely on setMuted's toast, or add a suppressToast flag to setMuted so the caller owns the single message.",
      "fixStatus": "2 fixed",
      "fixNotes": "tier2: FU-EXCLUDED-FRONTMOST-TOASTS | tier3: FU-HOTKEY-DOUBLE-TOAST",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "ux",
          "title": "Global ⌘⌥M shows a misleading 'Muted'/'Unmuted' toast even when the target app is excluded (no-op)",
          "detail": "toggleMuteForFrontmostApp picks activeApps.first ?? visibleApps.first (AppStore.swift:1250). visibleApps only filters by category/system, not exclusion (AppStore.swift:103-107), so the resolved frontmost app can be excluded. setMuted is a guarded no-op for excluded apps (AppStore.swift:492, returns before touching state or backend), but toggleMuteForFrontmostApp unconditionally shows a 'Muted'/'Unmuted' toast afterward (AppStore.swift:1256-1261). The user gets positive feedback that the app was muted when nothing happened.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1256",
          "expectedVsActual": "Expected (MUTE-06: setMuted on an excluded app does not change its state; MUTE-05: ⌘⌥M toggles mute and shows a toast). Actual: for an excluded frontmost app the mute is correctly skipped but a false 'Muted'/'Unmuted' success toast is still shown.",
          "suggestedFix": "Resolve the target app, then `guard !isExcluded(app) else { showToast('Cannot mute excluded app'...) ; return }` before mutating or showing the success toast.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier2:partial:FU-EXCLUDED-FRONTMOST-TOASTS",
          "uid": "MUTE-05#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "partial",
          "fixedBy": "FU-EXCLUDED-FRONTMOST-TOASTS",
          "fixTier": "tier2"
        },
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "⌘⌥M emits two overlapping toasts for one action",
          "detail": "toggleMuteForFrontmostApp shows an immediate 'Muted'/'Unmuted' info toast (AppStore.swift:1256-1261), then setMuted asynchronously shows its own 'App muted'/'App unmuted' success toast (AppStore.swift:528-533). A single ⌘⌥M press therefore produces two near-simultaneous toasts with different wording, unlike the in-window and URL paths which produce one. Confirmed both toast call sites fire for a non-excluded app.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1256",
          "expectedVsActual": "Expected (MUTE-05): one 'Muted'/'Unmuted' toast per ⌘⌥M press. Actual: two toasts ('Muted' from the shortcut handler and 'App muted' from setMuted) appear for one press.",
          "suggestedFix": "Remove the toast from toggleMuteForFrontmostApp and rely on setMuted's toast, or add a suppressToast flag to setMuted so the caller owns the single message.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier3:applied:FU-HOTKEY-DOUBLE-TOAST",
          "uid": "MUTE-05#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-HOTKEY-DOUBLE-TOAST",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "MUTE-06",
      "area": "Per-App Mute/Unmute & Mute Provenance",
      "feature": "Mute interaction with excluded apps and menu-bar mute indicator",
      "priority": "P2",
      "userStory": "As a user who excluded a sensitive app (DAW, conferencing tool) from Waves, I want mute controls disabled for it and its mute state cleared, and I want the menu-bar icon to reflect when anything is muted, so that Waves never tampers with excluded apps and I can see mute state at a glance.",
      "expectedBehavior": [
        "setMuted is a guarded no-op for excluded apps (AppStore.swift:492); the mute button and slider are disabled and the row is dimmed for excluded apps (MixerRowView.swift:107, :109, :324).",
        "Excluding an app tears down its route, sets isMuted = false and muteSource = .user, and stops showing Waves-applied mute state (AppStore.swift:696-712).",
        "Presets strip excluded apps before apply/save so a preset can never re-mute/re-tap an excluded app (AppStore.swift:1001-1007, :1037-1039).",
        "The menu-bar icon becomes speaker.slash.fill whenever any visible app is muted, else reflects live/idle state (AppStore.swift:143-153).",
        "An empty mixer shows a speaker.slash placeholder with Refresh/Settings (MenuBarMixerView.swift:52-72) — this is an empty-state glyph, not a mute control."
      ],
      "acceptanceCriteria": [
        "The mute button is disabled (and row dimmed) for an excluded app.",
        "Calling setMuted on an excluded app does not change its state or call the backend.",
        "Excluding a currently-muted app clears its muted state and releases its route.",
        "The menu-bar icon shows the slashed-speaker symbol whenever at least one visible app is muted.",
        "Applying a preset never mutes an app that is in the exclusion list."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:491-492",
        "Sources/Waves/Stores/AppStore.swift:686-712",
        "Sources/Waves/Stores/AppStore.swift:143-153",
        "Sources/Waves/Stores/AppStore.swift:1001-1007",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:107-109"
      ],
      "implStatus": "implemented",
      "implNotes": "Exclusion correctly blocks and clears mute, presets are sanitized against the exclusion list, and the menu-bar icon honestly reflects aggregate mute state. The menu-bar slash speaker at MenuBarMixerView.swift:54 is purely the 'no apps detected' empty-state icon and is not interactive — worth noting so it isn't mistaken for a global mute toggle. No correctness issues found.",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [1M 2L]",
      "errorsFound": "• [MEDIUM/correctness] Excluding a muted app clears mute only in the store, not in the backend snapshot, so a later re-sync resurrects the stale mute (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1117) — setExcluded clears the mute in the STORE session (AppStore.swift:706-707: isMuted = false, muteSource = .user) and calls backend.releaseControllers (line 702). But releaseControllers (WorkspaceAudioControlBackend.swift:1117-1123) only resets routingState/isActive/appliedVolume/peak/rms — it does NOT reset isMuted or muteSource in the backend snapshot. So the backend still holds isMuted == true for that app. Any subsequent whole-session pull from the backend re-imports that stale value: e.g. un-excluding then togglePinned does `session = await backend.currentSnapshot()` (AppStore.swift:596), and refresh()/applyPreset/device-change similarly replace the session. Because backend.buildSnapshot carries previous.isMuted forward (WorkspaceAudioControlBackend.swift:1007), the row reappears as muted even though the user cleared it by excluding. State desync between the store-local mutation and the backend snapshot. | Fix: In releaseControllers (or in a backend call invoked from setExcluded) also reset snapshot.apps[index].isMuted = false and muteSource = .user, so the backend snapshot agrees with the store after exclusion.\n• [LOW/inconsistency] A muted, non-live app is unreachable from the menu bar when 'Show recent apps' is off (Sources/Waves/Features/Mixer/MenuBarMixerView.swift:46) — The menu-bar panel renders only Pinned, Live (store.liveApps), and Recent (only when showRecentApps is on) sections (MenuBarMixerView.swift:42-50). liveApps excludes muted apps (`!app.isMuted`, AppStore.swift:120), and recentApps returns [] when showRecentApps is off (AppStore.swift:125). So an app the user muted from the main window — if it isn't pinned and isn't producing level — disappears entirely from the menu bar when recent apps are hidden. The menu-bar icon correctly turns to speaker.slash.fill (AppStore.swift:146-147) signaling something is muted, but there is no row to unmute it from the menu, creating a menu-bar vs. main-window state mismatch. | Fix: Include muted managed apps in a menu-bar section regardless of showRecentApps (e.g. surface muted apps under Live or a dedicated 'Muted' group) so they can be unmuted from the menu.\n• [LOW/accessibility] Menu-bar icon's mute state is not exposed to VoiceOver (Sources/Waves/App/WavesApp.swift:57) — The MenuBarExtra uses a static title 'Waves' with systemImage bound to store.menuBarIconName (WavesApp.swift:57-60). The mute/live/idle distinction is carried solely by the SF Symbol variant (speaker.slash.fill vs wave.3 vs wave.2). There is no accessibility label conveying 'muted'/'playing', so a VoiceOver user always hears 'Waves' regardless of state and cannot perceive the at-a-glance mute indicator the story promises. | Fix: Provide a dynamic accessibility label for the MenuBarExtra reflecting the current state (e.g. 'Waves — muted', 'Waves — playing', 'Waves — idle') derived from the same logic as menuBarIconName.",
      "fixStatus": "2 fixed · 1 deferred",
      "fixNotes": "tier2: FU-EXCLUDE-STALE-MUTE | tier3: FU-MENUBAR-A11Y-LABEL",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "correctness",
          "title": "Excluding a muted app clears mute only in the store, not in the backend snapshot, so a later re-sync resurrects the stale mute",
          "detail": "setExcluded clears the mute in the STORE session (AppStore.swift:706-707: isMuted = false, muteSource = .user) and calls backend.releaseControllers (line 702). But releaseControllers (WorkspaceAudioControlBackend.swift:1117-1123) only resets routingState/isActive/appliedVolume/peak/rms — it does NOT reset isMuted or muteSource in the backend snapshot. So the backend still holds isMuted == true for that app. Any subsequent whole-session pull from the backend re-imports that stale value: e.g. un-excluding then togglePinned does `session = await backend.currentSnapshot()` (AppStore.swift:596), and refresh()/applyPreset/device-change similarly replace the session. Because backend.buildSnapshot carries previous.isMuted forward (WorkspaceAudioControlBackend.swift:1007), the row reappears as muted even though the user cleared it by excluding. State desync between the store-local mutation and the backend snapshot.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1117",
          "expectedVsActual": "Expected (MUTE-06: excluding a currently-muted app clears its muted state and releases its route, and the row no longer shows Waves-applied mute state). Actual: the store clears the mute but the backend snapshot keeps isMuted == true, so a later pin/refresh/preset/device-change re-pulls the muted state and the app shows muted again.",
          "suggestedFix": "In releaseControllers (or in a backend call invoked from setExcluded) also reset snapshot.apps[index].isMuted = false and muteSource = .user, so the backend snapshot agrees with the store after exclusion.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "tier2:applied:FU-EXCLUDE-STALE-MUTE",
          "uid": "MUTE-06#1",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-EXCLUDE-STALE-MUTE",
          "fixTier": "tier2"
        },
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "A muted, non-live app is unreachable from the menu bar when 'Show recent apps' is off",
          "detail": "The menu-bar panel renders only Pinned, Live (store.liveApps), and Recent (only when showRecentApps is on) sections (MenuBarMixerView.swift:42-50). liveApps excludes muted apps (`!app.isMuted`, AppStore.swift:120), and recentApps returns [] when showRecentApps is off (AppStore.swift:125). So an app the user muted from the main window — if it isn't pinned and isn't producing level — disappears entirely from the menu bar when recent apps are hidden. The menu-bar icon correctly turns to speaker.slash.fill (AppStore.swift:146-147) signaling something is muted, but there is no row to unmute it from the menu, creating a menu-bar vs. main-window state mismatch.",
          "codeRef": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:46",
          "expectedVsActual": "Expected (MUTE-06: see mute state at a glance and manage from the menu bar): a muted app should be reachable/unmutable from the menu bar that signals it is muted. Actual: with recent apps off, a muted non-live unpinned app is absent from the menu entirely; only the aggregate icon hints at it.",
          "suggestedFix": "Include muted managed apps in a menu-bar section regardless of showRecentApps (e.g. surface muted apps under Live or a dedicated 'Muted' group) so they can be unmuted from the menu.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift",
          "disposition": "deferred:needs-user-decision:Surfacing it needs a new section/heading (where and under what label muted apps appear) — a design choice, not reuse of an existing list (li",
          "uid": "MUTE-06#2",
          "canonicalFile": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift"
        },
        {
          "severity": "low",
          "type": "accessibility",
          "title": "Menu-bar icon's mute state is not exposed to VoiceOver",
          "detail": "The MenuBarExtra uses a static title 'Waves' with systemImage bound to store.menuBarIconName (WavesApp.swift:57-60). The mute/live/idle distinction is carried solely by the SF Symbol variant (speaker.slash.fill vs wave.3 vs wave.2). There is no accessibility label conveying 'muted'/'playing', so a VoiceOver user always hears 'Waves' regardless of state and cannot perceive the at-a-glance mute indicator the story promises.",
          "codeRef": "Sources/Waves/App/WavesApp.swift:57",
          "expectedVsActual": "Expected (MUTE-06: the menu-bar icon reflects when anything is muted so the user can see mute state at a glance): the state should also be perceivable non-visually. Actual: the mute signal is purely the icon glyph; VoiceOver gets only the static 'Waves' label.",
          "suggestedFix": "Provide a dynamic accessibility label for the MenuBarExtra reflecting the current state (e.g. 'Waves — muted', 'Waves — playing', 'Waves — idle') derived from the same logic as menuBarIconName.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/App/WavesApp.swift",
          "disposition": "tier3:applied:FU-MENUBAR-A11Y-LABEL",
          "uid": "MUTE-06#3",
          "canonicalFile": "Sources/Waves/App/WavesApp.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-MENUBAR-A11Y-LABEL",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "BOOST-01",
      "area": "Volume Boost (2x/3x/4x)",
      "feature": "Per-app boost selection (1x/2x/3x/4x)",
      "priority": "P0",
      "userStory": "As a Waves user with a too-quiet app, I want to pick a 2x, 3x, or 4x boost for that specific app from its mixer row, so that I can make it louder than the system maximum without touching other apps.",
      "expectedBehavior": [
        "Each mixer row (full and compact) shows a BoostMenu with options 1x, 2x, 3x, 4x (MixerRowView.swift:366,370-374).",
        "The menu label shows the app's current boost as an integer 'Nx' using monospaced digits (MixerRowView.swift:376).",
        "Selecting an option calls store.setVolumeBoost(boost, for: app) (MixerRowView.swift:371-372).",
        "store.setVolumeBoost optimistically updates session.apps[index].volumeBoost, clamps to [1.0, 4.0], then calls backend.setVolumeBoost and merges the returned snapshot (AppStore.swift:548-571).",
        "On success a 'Boost updated' toast shows the app name and 'Nx' for ~1.1s; on failure a 'Boost update failed' error toast shows the error (AppStore.swift:574-585).",
        "The backend marks the app routingState .managed on success and .error with notes on failure (WorkspaceAudioControlBackend.swift:129-138).",
        "Boost controls are disabled for excluded apps (Waves itself), both in UI (.disabled(isExcluded), MixerRowView.swift:94,311) and in the store guard (AppStore.swift:545)."
      ],
      "acceptanceCriteria": [
        "Opening the boost menu lists exactly 1x, 2x, 3x, 4x.",
        "Choosing 3x updates the row label to '3x' and shows a 'Boost updated: <app>: 3x' toast.",
        "A boost value passed outside [1.0,4.0] is clamped to that range before reaching the backend.",
        "For an excluded app the boost control is disabled and setVolumeBoost is a no-op.",
        "On backend failure the app shows an error state and an error toast appears."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MixerRowView.swift:361",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:366",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:370",
        "Sources/Waves/Stores/AppStore.swift:544",
        "Sources/Waves/Stores/AppStore.swift:548",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:113"
      ],
      "implStatus": "implemented",
      "implNotes": "Fully wired UI->store->backend with optimistic update, clamping, toasts, and error handling. UX concern: the Menu does not mark the currently-selected option (no checkmark/state), so the dropdown gives no visual cue of which boost is active; the current value only appears on the closed label (MixerRowView.swift:369-376). Minor: the success toast fires from store-side clampedBoost even though the authoritative value comes from the backend snapshot merge.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/accessibility] Boost menu gives no indication of the currently-selected option inside the dropdown (Sources/Waves/Features/Mixer/MixerRowView.swift:369) — BoostMenu (MixerRowView.swift:368-387) builds the menu as four plain Buttons ('1x'..'4x') with no checkmark, .pickerStyle, or selected-state marker. When the menu is open, neither a sighted user nor VoiceOver can tell which boost is currently active from within the list — every option looks identical. The active value is only exposed on the closed label (Text + accessibilityValue at MixerRowView.swift:376,386), so the in-menu experience is ambiguous. This is a minor accessibility/UX gap, not a functional defect; the first tester noted it as a BOOST-01 implNote but did not file it. | Fix: Render the active option with a checkmark (e.g. Button with a leading Image(systemName: boost == app.volumeBoost ? \"checkmark\" : \"\") or use a Picker bound to the boost) so the open menu reflects the current selection for both sighted and VoiceOver users.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier3: FU-BOOST-MENU-SELECTION",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "accessibility",
          "title": "Boost menu gives no indication of the currently-selected option inside the dropdown",
          "detail": "BoostMenu (MixerRowView.swift:368-387) builds the menu as four plain Buttons ('1x'..'4x') with no checkmark, .pickerStyle, or selected-state marker. When the menu is open, neither a sighted user nor VoiceOver can tell which boost is currently active from within the list — every option looks identical. The active value is only exposed on the closed label (Text + accessibilityValue at MixerRowView.swift:376,386), so the in-menu experience is ambiguous. This is a minor accessibility/UX gap, not a functional defect; the first tester noted it as a BOOST-01 implNote but did not file it.",
          "codeRef": "Sources/Waves/Features/Mixer/MixerRowView.swift:369",
          "expectedVsActual": "Expected: opening the boost menu makes the current boost visually obvious (e.g. a checkmark on the active option, as macOS menus conventionally do). Actual: all four options render identically with no selected-state cue.",
          "suggestedFix": "Render the active option with a checkmark (e.g. Button with a leading Image(systemName: boost == app.volumeBoost ? \"checkmark\" : \"\") or use a Picker bound to the boost) so the open menu reflects the current selection for both sighted and VoiceOver users.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "disposition": "tier3:applied:FU-BOOST-MENU-SELECTION",
          "uid": "BOOST-01#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-BOOST-MENU-SELECTION",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "BOOST-02",
      "area": "Volume Boost (2x/3x/4x)",
      "feature": "Realtime gain application with clip protection",
      "priority": "P0",
      "userStory": "As a user who has set a boost, I want the boosted gain applied to the app's live audio without harsh clipping, so that the app is audibly louder but does not distort into out-of-range garbage.",
      "expectedBehavior": [
        "The tap render path computes effectiveVolume = volume * volumeBoost and passes it as the gain to TapDSP.scale (WorkspaceAudioControlBackend.swift:1899,1934).",
        "TapDSP.scale multiplies each sample in place and clamps: float32 to [-1.0, 1.0], int16/int32 saturate to their type min/max instead of overflowing (TapDSP.swift:33-47,105-117).",
        "A gain of exactly 1.0 is a no-op early return, so 1x boost at full volume leaves samples untouched (TapDSP.swift:27).",
        "Peak/RMS levels are computed from the original (pre-scale) input, so meters reflect source level not boosted level (WorkspaceAudioControlBackend.swift:1924-1932).",
        "controller.apply(volume:volumeBoost:muted:) and setVolumeBoost write the boost into a lock-free TapRenderState read on the audio thread (WorkspaceAudioControlBackend.swift:1651-1670).",
        "Boost has no effect when muted or volume==0 because the render path zeroes output before scaling (WorkspaceAudioControlBackend.swift:1695-1705)."
      ],
      "acceptanceCriteria": [
        "A float32 sample of 0.5 at 4x gain emits 1.0 (saturated), and -0.5 emits -1.0 (verified by TapDSPTests.float32BoostClampsToValidRange).",
        "An int16 sample of 20000 at 4x saturates to Int16.max rather than wrapping (TapDSPTests.int16ScalingSaturatesInsteadOfOverflowing).",
        "Gain of 1.0 leaves the buffer byte-identical (TapDSPTests.gainOfOneIsNoOp).",
        "Level meters do not jump when boost increases, since levels are read pre-scale.",
        "An unknown sample format results in no scaling (samples unchanged)."
      ],
      "codeRefs": [
        "Sources/WavesAudioCore/Audio/TapDSP.swift:21",
        "Sources/WavesAudioCore/Audio/TapDSP.swift:33",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1899",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1934",
        "Tests/WavesTests/TapDSPTests.swift:8"
      ],
      "implStatus": "implemented",
      "implNotes": "Core DSP is allocation-free, clamps correctly, and is unit-tested across float32/int16/int32. Correctness concern: boost is applied by hard clamping (limiting) rather than any soft limiter/headroom, so loud source material at 3x/4x will flat-top at full scale and distort audibly — this is the expected behavior of the code, and the README/Settings copy acknowledges it by advising users to reserve 2x-4x for quiet apps (SettingsView.swift:202). TapSampleFormat.unknown silently skips scaling, so a tap whose format isn't recognized plays at unity regardless of boost (TapDSP.swift:48-50).",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/concurrency] setVolumeBoost reads volume/mute off-queue, risking a stale read-modify-write of the render state (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1660) — PerAppTapController.setVolumeBoost (WorkspaceAudioControlBackend.swift:1660-1670) reads currentState = stateBox.read() on the CALLING thread, then enqueues an async block on callbackQueue that calls writeVolumeAndMute(volume: currentState.volume, volumeBoost: clampedBoost, muted: currentState.isMuted != 0). If a concurrent apply(volume:volumeBoost:muted:) (line 1651) enqueues a newer volume/mute between the read and the async write, setVolumeBoost's block runs afterward and reverts volume/mute to the captured (stale) values. TapRenderStateBox has no boost-only writer (only write/writeVolumeAndMute/writeLevels at lines 1550/1567/1578), so the boost write must re-supply volume+mute. The window is narrow and self-correcting because backend.setVolumeBoost immediately follows with applyRoute -> controller.apply, which re-applies the correct volume/mute; audible impact is minimal. | Fix: Add a writeBoost(_:) to TapRenderStateBox that takes the lock and mutates only state.pointee.volumeBoost (and the shadow stateBox.volumeBoost), and have setVolumeBoost enqueue that instead of re-reading and re-writing volume/mute. This removes the off-queue read entirely.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier3: FU-VOLUMEBOOST-OFFQUEUE-WRITE",
      "retestStatus": "PASS (remediated & verified)",
      "issues": [
        {
          "severity": "low",
          "type": "concurrency",
          "title": "setVolumeBoost reads volume/mute off-queue, risking a stale read-modify-write of the render state",
          "detail": "PerAppTapController.setVolumeBoost (WorkspaceAudioControlBackend.swift:1660-1670) reads currentState = stateBox.read() on the CALLING thread, then enqueues an async block on callbackQueue that calls writeVolumeAndMute(volume: currentState.volume, volumeBoost: clampedBoost, muted: currentState.isMuted != 0). If a concurrent apply(volume:volumeBoost:muted:) (line 1651) enqueues a newer volume/mute between the read and the async write, setVolumeBoost's block runs afterward and reverts volume/mute to the captured (stale) values. TapRenderStateBox has no boost-only writer (only write/writeVolumeAndMute/writeLevels at lines 1550/1567/1578), so the boost write must re-supply volume+mute. The window is narrow and self-correcting because backend.setVolumeBoost immediately follows with applyRoute -> controller.apply, which re-applies the correct volume/mute; audible impact is minimal.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1660",
          "expectedVsActual": "Expected: writing the boost into TapRenderState updates only the boost field without disturbing the latest volume/mute. Actual: it re-writes volume and mute from a snapshot taken before enqueue, which can clobber a concurrently-applied newer volume/mute.",
          "suggestedFix": "Add a writeBoost(_:) to TapRenderStateBox that takes the lock and mutates only state.pointee.volumeBoost (and the shadow stateBox.volumeBoost), and have setVolumeBoost enqueue that instead of re-reading and re-writing volume/mute. This removes the off-queue read entirely.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "tier3:applied:FU-VOLUMEBOOST-OFFQUEUE-WRITE",
          "uid": "BOOST-02#1",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-VOLUMEBOOST-OFFQUEUE-WRITE",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "BOOST-03",
      "area": "Volume Boost (2x/3x/4x)",
      "feature": "Keyboard boost cycling",
      "priority": "P2",
      "userStory": "As a keyboard-driven user, I want to press 'b' on the selected mixer row to cycle the boost, so that I can adjust loudness without reaching for the mouse menu.",
      "expectedBehavior": [
        "Pressing 'b' while a row is selected calls cycleBoost on that app (MainWindowView.swift:385).",
        "cycleBoost advances 1->2->3->4 and wraps 4->1 via app.volumeBoost >= 4 ? 1 : (app.volumeBoost + 1).rounded() (MainWindowView.swift:422-424).",
        "The cycle routes through store.setVolumeBoost, so it shares the same clamping, persistence, toast, and backend-apply path as the menu (MainWindowView.swift:424).",
        "handleKey is a no-op (.ignored) when no row is selected (MainWindowView.swift:408-413)."
      ],
      "acceptanceCriteria": [
        "With a row at 1x selected, pressing 'b' four times produces 2x, 3x, 4x, then back to 1x.",
        "Pressing 'b' with no selection does nothing and does not error.",
        "Each 'b' press produces the same 'Boost updated' toast as the menu."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MainWindowView.swift:385",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:422",
        "Sources/Waves/Stores/AppStore.swift:544"
      ],
      "implStatus": "implemented",
      "implNotes": "Cleanly reuses the store path. Note the .rounded() guards against any fractional boost coming from per-device presets so the cycle stays on integer steps; an excluded selected app silently does nothing because store.setVolumeBoost guards on isExcluded.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "BOOST-04",
      "area": "Volume Boost (2x/3x/4x)",
      "feature": "Boost persistence and restore across relaunch / per-device presets",
      "priority": "P1",
      "userStory": "As a returning user, I want each app's boost to be remembered and re-applied when I relaunch Waves or switch output devices, so that I don't have to re-set boosts every session.",
      "expectedBehavior": [
        "volumeBoost is encoded/decoded on AudioApp, PresetEntry, and AppVolumeSettings, defaulting to 1.0 when absent (AudioApp.swift:138, Preset.swift:56, UserPreferences.swift:85).",
        "setVolumeBoost is persisted via persistSessionSnapshot and, when enablePerDeviceVolumePresets is on, saved per (appKey, deviceID) (AppStore.swift:555-565,573).",
        "On relaunch, reapplyRestoredAudioState treats volumeBoost > 1.0 as a 'customized' app and re-applies it to the backend (AppStore.swift:829-833).",
        "Switching device restores saved per-device boost via restoreDeviceVolumePresets, calling backend.setVolumeBoost (AppStore.swift:854,859).",
        "applyPreset writes entry.volumeBoost and re-routes each app (WorkspaceAudioControlBackend.swift:164-171).",
        "Cached/merged app state preserves volumeBoost when reconciling backend snapshots (AppStore.swift:1402)."
      ],
      "acceptanceCriteria": [
        "Setting 3x, quitting, and relaunching shows the app at 3x and re-applies it audibly once the app is running.",
        "An app not running at relaunch keeps its saved boost without error (restore is skipped per-app on failure).",
        "With per-device presets enabled, the same app can hold different boosts on different output devices, restored on device switch.",
        "Loading a preset created without a volumeBoost field defaults that entry to 1x.",
        "A persisted boost is clamped to [1.0,4.0] on decode (PresetEntry/AppVolumeSettings)."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:829",
        "Sources/Waves/Stores/AppStore.swift:848",
        "Sources/WavesAudioCore/Models/Preset.swift:40",
        "Sources/Waves/Stores/UserPreferences.swift:72",
        "Sources/WavesAudioCore/Models/AudioApp.swift:138"
      ],
      "implStatus": "implemented",
      "implNotes": "Persistence is consistently plumbed through model, presets, and per-device settings with safe defaults. Concern: clamp ranges are inconsistent — AudioApp clamps volumeBoost to [0.0, 10.0] (AudioApp.swift:87-88) while every user-facing path (store, backend, controller, Preset, UserPreferences) clamps to [1.0, 4.0]. A decoded AudioApp with a boost of 0.0 or e.g. 7.0 would survive in the model and, until a setVolumeBoost call re-clamps it, the restore path in reapplyRestoredAudioState passes app.volumeBoost into backend.setVolumeBoost which then re-clamps to [1.0,4.0] — so audible output is safe, but the in-model value can momentarily be out of the user-facing range. A boost of 0.0<boost<1.0 would also fail the 'volumeBoost > 1.0' customized check and silently not be restored.",
      "source": "phase1-story",
      "testStatus": "4 issue(s) [2H 1M 1L]",
      "errorsFound": "• [HIGH/correctness] mergedSession drops cached-only apps at relaunch, so an app not running at relaunch loses its saved boost on the first launch (then it is persisted to disk) (Sources/Waves/Stores/AppStore.swift:1390) — This is the root-cause version of the first tester's refresh() finding, and it triggers with no user action. At launch session is loaded from disk WITH the app's saved boost (AppStore.swift:83), but start() then does session = mergedSession(with: built, cached: warmSnapshot) (AppStore.swift:211). mergedSession (AppStore.swift:1383-1415) builds mergedApps = liveSession.apps and only copies cached values onto apps that ALSO appear in the live (backend) snapshot; it never appends cached-only apps. The backend's built snapshot at fresh launch contains only currently-running apps (the backend starts from .empty, not from disk), so an app X that is not running at relaunch is absent from liveSession and is therefore DROPPED from session.apps by mergedSession. start() then calls persistSessionSnapshot() unconditionally (AppStore.swift:217), overwriting the on-disk session with one that no longer contains X — permanently losing X's saved boost/volume/mute/targetDeviceUID. reapplyRestoredAudioState (AppStore.swift:820-846) cannot help because X is already gone from session.apps. This directly violates BOOST-04 acceptance #2 ('An app not running at relaunch keeps its saved boost'). | Fix: In mergedSession, after merging live apps, append any cached apps whose logicalID is not present in liveSession (carrying them as monitorOnly/retained entries) so saved settings survive until the app is rediscovered. Mirror the backend's isStillRunning-style retention, or keep a separate 'remembered customizations' store keyed by logicalID that is reattached on rediscovery rather than living only in the volatile session snapshot.\n• [HIGH/correctness] refresh() assigns the backend snapshot directly with no cache reconciliation, dropping cached-only per-app boost/state (Sources/Waves/Stores/AppStore.swift:242) — refresh() does session = try await backend.refresh() (AppStore.swift:242) and assigns the backend snapshot DIRECTLY, unlike start() which routes through mergedSession. backend.refresh -> buildSnapshot(merging: previousSnapshot) (WorkspaceAudioControlBackend.swift:48-51, 999-1014) only preserves boost the backend already held in its own previous snapshot. Any per-app state that exists in AppStore.session but not in the backend snapshot (e.g. an app whose boost was reinjected into the AppStore session but never accepted by the backend because backend.setVolumeBoost threw appNotFound for a not-yet-running app in reapplyRestoredAudioState, AppStore.swift:833) is lost when refresh overwrites session, and persistSessionSnapshot() (AppStore.swift:245) then writes the loss to disk. Refresh is a prominent button (MainWindowView, MenuBarMixerView) and is also reachable via the waves://refresh URL command (AppStore.swift:284-285). togglePinned (AppStore.swift:596) and handleDeviceChange (AppStore.swift:799) similarly do session = await backend.currentSnapshot() with no reconciliation, so the same class of loss occurs on pin/unpin and device change. | Fix: In refresh(), reconcile like bootstrap: session = mergedSession(with: try await backend.refresh(), cached: session) (after fixing mergedSession to retain cached-only apps), and re-run reapplyRestoredAudioState() for newly-appearing customized apps before persistSessionSnapshot(). Apply the same reconciliation in togglePinned and handleDeviceChange.\n• [MEDIUM/edge-case] Per-device boost is not restored on device switch when auto-restore-device is disabled (Sources/Waves/Stores/AppStore.swift:793) — restoreDeviceVolumePresets (AppStore.swift:848) — the only place a saved per-device boost is re-applied on a device switch — is called exclusively from handleDeviceChange (AppStore.swift:803). handleDeviceChange begins with guard preferences.autoRestoreDevice else { return } (AppStore.swift:793), so if autoRestoreDevice is OFF while enablePerDeviceVolumePresets is ON, a device change never reaches the per-device restore branch (AppStore.swift:802-804). These are two independent Settings toggles, both defaulting true (UserPreferences.swift:9,12), so the OFF/ON combination is a reachable, legitimate configuration. selectOutputDevice (AppStore.swift:759) relies on the device-change event funneling through handleDeviceChange, so manual switches are affected too. This violates BOOST-04 acceptance #3. | Fix: Decouple the per-device-preset restore from the autoRestoreDevice guard. Split handleDeviceChange into a route-restore part gated by autoRestoreDevice and a preset-restore part gated only by enablePerDeviceVolumePresets, so the enablePerDeviceVolumePresets block runs on device change regardless of autoRestoreDevice.\n• [LOW/inconsistency] AudioApp clamps volumeBoost to [0.0,10.0] instead of the documented user-facing range [1.0,4.0] (Sources/WavesAudioCore/Models/AudioApp.swift:88) — AudioApp's memberwise init clamps volumeBoost with max(0.0, min(10.0, volumeBoost)) (AudioApp.swift:88). init(from:) (AudioApp.swift:118-165) decodes volumeBoost defaulting to 1.0 when absent and then routes through the memberwise init, so a persisted value is clamped to [0.0,10.0], NOT to the user-facing [1.0,4.0]. Every code path that SETS a boost clamps to [1.0,4.0] (AppStore.setVolumeBoost:548, backend.setVolumeBoost:118, controller.apply:1653 / setVolumeBoost:1661), and PresetEntry/AppVolumeSettings clamp to [1.0,4.0] on decode. But a tampered/corrupted session file (loaded at AppStore.swift:83) can carry a per-app boost in [0.0,1.0) or (4.0,10.0] straight into session.apps, which BoostMenu renders verbatim as Text(\"\\(Int(app.volumeBoost))x\") (MixerRowView.swift:376) — showing non-selectable labels like '0x', '7x', or '10x'. The backend re-clamps to [1.0,4.0] on the next apply and cycleBoost self-heals, so audio is safe, but the stale UI label persists until the boost is re-set. | Fix: Clamp volumeBoost to [1.0,4.0] in AudioApp's memberwise init (change max(0.0, min(10.0, volumeBoost)) to max(1.0, min(4.0, volumeBoost))), matching PresetEntry and AppVolumeSettings; since init(from:) routes through the memberwise init, this also clamps on decode.",
      "fixStatus": "2 fixed · 2 deferred",
      "fixNotes": "tier1: FU-DEVICECHANGE-GATING | tier3: FU-AUDIOAPP-BOOST-CLAMP",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "correctness",
          "title": "mergedSession drops cached-only apps at relaunch, so an app not running at relaunch loses its saved boost on the first launch (then it is persisted to disk)",
          "detail": "This is the root-cause version of the first tester's refresh() finding, and it triggers with no user action. At launch session is loaded from disk WITH the app's saved boost (AppStore.swift:83), but start() then does session = mergedSession(with: built, cached: warmSnapshot) (AppStore.swift:211). mergedSession (AppStore.swift:1383-1415) builds mergedApps = liveSession.apps and only copies cached values onto apps that ALSO appear in the live (backend) snapshot; it never appends cached-only apps. The backend's built snapshot at fresh launch contains only currently-running apps (the backend starts from .empty, not from disk), so an app X that is not running at relaunch is absent from liveSession and is therefore DROPPED from session.apps by mergedSession. start() then calls persistSessionSnapshot() unconditionally (AppStore.swift:217), overwriting the on-disk session with one that no longer contains X — permanently losing X's saved boost/volume/mute/targetDeviceUID. reapplyRestoredAudioState (AppStore.swift:820-846) cannot help because X is already gone from session.apps. This directly violates BOOST-04 acceptance #2 ('An app not running at relaunch keeps its saved boost').",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1390",
          "expectedVsActual": "Expected (acceptance #2): an app not running at relaunch retains its saved boost (and other per-app settings) so that when it next runs the boost re-applies. Actual: mergedSession silently discards the cached-only app, and the subsequent persistSessionSnapshot() erases its saved boost from disk on the very first launch where the app isn't running.",
          "suggestedFix": "In mergedSession, after merging live apps, append any cached apps whose logicalID is not present in liveSession (carrying them as monitorOnly/retained entries) so saved settings survive until the app is rediscovered. Mirror the backend's isStillRunning-style retention, or keep a separate 'remembered customizations' store keyed by logicalID that is reattached on rediscovery rather than living only in the volatile session snapshot.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-user-decision:Retention/expiry policy for not-running apps is a product decision affecting mixer contents, sorting, and persistence — not a behavior-preserving edit.",
          "uid": "BOOST-04#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "high",
          "type": "correctness",
          "title": "refresh() assigns the backend snapshot directly with no cache reconciliation, dropping cached-only per-app boost/state",
          "detail": "refresh() does session = try await backend.refresh() (AppStore.swift:242) and assigns the backend snapshot DIRECTLY, unlike start() which routes through mergedSession. backend.refresh -> buildSnapshot(merging: previousSnapshot) (WorkspaceAudioControlBackend.swift:48-51, 999-1014) only preserves boost the backend already held in its own previous snapshot. Any per-app state that exists in AppStore.session but not in the backend snapshot (e.g. an app whose boost was reinjected into the AppStore session but never accepted by the backend because backend.setVolumeBoost threw appNotFound for a not-yet-running app in reapplyRestoredAudioState, AppStore.swift:833) is lost when refresh overwrites session, and persistSessionSnapshot() (AppStore.swift:245) then writes the loss to disk. Refresh is a prominent button (MainWindowView, MenuBarMixerView) and is also reachable via the waves://refresh URL command (AppStore.swift:284-285). togglePinned (AppStore.swift:596) and handleDeviceChange (AppStore.swift:799) similarly do session = await backend.currentSnapshot() with no reconciliation, so the same class of loss occurs on pin/unpin and device change.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:242",
          "expectedVsActual": "Expected: refresh reconciles the backend snapshot with cached per-app settings (boost/volume/mute/device) like bootstrap does, so a Refresh never silently resets a customized app. Actual: refresh (and togglePinned/handleDeviceChange) replace session with the backend snapshot wholesale, dropping any cached-only per-app state and then persisting the reset.",
          "suggestedFix": "In refresh(), reconcile like bootstrap: session = mergedSession(with: try await backend.refresh(), cached: session) (after fixing mergedSession to retain cached-only apps), and re-run reapplyRestoredAudioState() for newly-appearing customized apps before persistSessionSnapshot(). Apply the same reconciliation in togglePinned and handleDeviceChange.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-user-decision:Depends on the unresolved cached-app retention policy (BOOST-04#1); reconciling here changes persisted/visible state and needs the same product decision.",
          "uid": "BOOST-04#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "medium",
          "type": "edge-case",
          "title": "Per-device boost is not restored on device switch when auto-restore-device is disabled",
          "detail": "restoreDeviceVolumePresets (AppStore.swift:848) — the only place a saved per-device boost is re-applied on a device switch — is called exclusively from handleDeviceChange (AppStore.swift:803). handleDeviceChange begins with guard preferences.autoRestoreDevice else { return } (AppStore.swift:793), so if autoRestoreDevice is OFF while enablePerDeviceVolumePresets is ON, a device change never reaches the per-device restore branch (AppStore.swift:802-804). These are two independent Settings toggles, both defaulting true (UserPreferences.swift:9,12), so the OFF/ON combination is a reachable, legitimate configuration. selectOutputDevice (AppStore.swift:759) relies on the device-change event funneling through handleDeviceChange, so manual switches are affected too. This violates BOOST-04 acceptance #3.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:793",
          "expectedVsActual": "Expected (acceptance #3): with per-device presets enabled, the same app holds different boosts on different output devices, restored on device switch. Actual: when autoRestoreDevice is disabled, switching devices skips restoreDeviceVolumePresets entirely, so the per-device boost (and per-device volume/mute) is never restored.",
          "suggestedFix": "Decouple the per-device-preset restore from the autoRestoreDevice guard. Split handleDeviceChange into a route-restore part gated by autoRestoreDevice and a preset-restore part gated only by enablePerDeviceVolumePresets, so the enablePerDeviceVolumePresets block runs on device change regardless of autoRestoreDevice.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:FU-DEVICECHANGE-GATING",
          "uid": "BOOST-04#3",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-DEVICECHANGE-GATING",
          "fixTier": "tier1"
        },
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "AudioApp clamps volumeBoost to [0.0,10.0] instead of the documented user-facing range [1.0,4.0]",
          "detail": "AudioApp's memberwise init clamps volumeBoost with max(0.0, min(10.0, volumeBoost)) (AudioApp.swift:88). init(from:) (AudioApp.swift:118-165) decodes volumeBoost defaulting to 1.0 when absent and then routes through the memberwise init, so a persisted value is clamped to [0.0,10.0], NOT to the user-facing [1.0,4.0]. Every code path that SETS a boost clamps to [1.0,4.0] (AppStore.setVolumeBoost:548, backend.setVolumeBoost:118, controller.apply:1653 / setVolumeBoost:1661), and PresetEntry/AppVolumeSettings clamp to [1.0,4.0] on decode. But a tampered/corrupted session file (loaded at AppStore.swift:83) can carry a per-app boost in [0.0,1.0) or (4.0,10.0] straight into session.apps, which BoostMenu renders verbatim as Text(\"\\(Int(app.volumeBoost))x\") (MixerRowView.swift:376) — showing non-selectable labels like '0x', '7x', or '10x'. The backend re-clamps to [1.0,4.0] on the next apply and cycleBoost self-heals, so audio is safe, but the stale UI label persists until the boost is re-set.",
          "codeRef": "Sources/WavesAudioCore/Models/AudioApp.swift:88",
          "expectedVsActual": "Expected: a persisted boost is constrained to the documented [1.0,4.0] user-facing range everywhere it can surface. Actual: AudioApp permits [0.0,10.0] on construction/decode, so the model and its menu label can hold/display values outside [1.0,4.0].",
          "suggestedFix": "Clamp volumeBoost to [1.0,4.0] in AudioApp's memberwise init (change max(0.0, min(10.0, volumeBoost)) to max(1.0, min(4.0, volumeBoost))), matching PresetEntry and AppVolumeSettings; since init(from:) routes through the memberwise init, this also clamps on decode.",
          "confidence": "confirmed",
          "primaryFile": "Sources/WavesAudioCore/Models/AudioApp.swift",
          "disposition": "tier3:applied:FU-AUDIOAPP-BOOST-CLAMP",
          "uid": "BOOST-04#4",
          "canonicalFile": "Sources/WavesAudioCore/Models/AudioApp.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-AUDIOAPP-BOOST-CLAMP",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "TAP-01",
      "area": "Audio Process Taps & DSP Backend",
      "feature": "Per-app managed routing via Core Audio process taps",
      "priority": "P0",
      "userStory": "As a macOS user, I want Waves to take over a specific app's audio with a Core Audio process tap, so that I can control that one app's playback independently from system volume without installing a kernel/audio driver.",
      "expectedBehavior": [
        "applyRoute resolves the app's audible Core Audio process objects from its bundle family PIDs and/or its own PID via kAudioHardwarePropertyTranslatePIDToProcessObject (resolveProcessObjectIDs).",
        "createController builds a private CATapDescription (stereoMixdownOfProcesses) with mutedWhenTapped behavior, creates a process tap (AudioHardwareCreateProcessTap), reads its UID, then creates a private aggregate device whose main/clock/sub-device is the chosen output device and whose tap list includes the tap UID.",
        "A PerAppTapController starts an IO proc (AudioDeviceCreateIOProcIDWithBlock) on the aggregate device, configures input+output stream usage, and starts the device; success marks the app routingState=.managed.",
        "The aggregate routes to app.targetDeviceUID when set (per-app output device) or to the current system default output otherwise (currentDefaultOutputDeviceUID).",
        "If the app already has an active controller matching the same process object IDs, the existing controller is reused and just re-applied instead of rebuilt (applyRoute fast path).",
        "Waves' own private aggregate devices (UID prefix com.waves.aggregate.) and aggregates named 'waves' are filtered out of user-selectable output devices.",
        "When the chosen targetDeviceUID is no longer available, createController throws managedRouteUnavailable with a message telling the user to pick another output device instead of silently falling back."
      ],
      "acceptanceCriteria": [
        "Given a 14.2+ system with capture granted and a running audible app, when the user adjusts that app, then a process tap + private aggregate device + running IO proc are created and the app shows routingState=.managed.",
        "Given an app with no resolvable Core Audio process objects, when applyRoute runs, then it throws managedRouteUnavailable and the app is marked .error with a note (not silently 'managed').",
        "Given an app pinned to a targetDeviceUID that has been unplugged, when a route is (re)built, then it throws managedRouteUnavailable referencing the Output Device menu rather than routing to the default device.",
        "Waves' own aggregate devices never appear in availableOutputDevices()."
      ],
      "codeRefs": [
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:360",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:429",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:447",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:471",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:530",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1676",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:661"
      ],
      "implStatus": "implemented",
      "implNotes": "Full real Core Audio tap+aggregate+IOProc pipeline, not a stub. Honest failure handling: partial-create leaks are cleaned up (tap destroyed if UID read or aggregate creation fails; controller disposed if start() fails). One latent concern: resolveProcessObjectIDs unions the WHOLE bundle family's process objects into a single stereoMixdown tap, so a multi-process app (e.g. browser helpers) is tapped as a group — correct for grouping but means the tap set can drift as helper PIDs come/go, forcing rebuilds. translateProcessID throws on any non-noErr status, so one bad PID in the set aborts resolution rather than skipping that PID (the compactMap can still throw out of the closure).",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1M]",
      "errorsFound": "• [MEDIUM/edge-case] One untranslatable sibling PID aborts whole process-object resolution for multi-process apps (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:546) — resolveProcessObjectIDs maps the bundle family's candidatePIDs with `try candidatePIDs.compactMap { ... try translateProcessID(forPID:) ... }` (line 546-552). translateProcessID (line 843-867) routes every OSStatus through withStatusCheck (line 1335-1338), which throws BackendError.managedRouteUnavailable on ANY non-noErr status (e.g. kAudioHardwareBadObjectError for a PID that has no Core Audio process object yet — common for transient helper/renderer processes in a browser family). Because the compactMap closure is `throws`, that single failure propagates out of the whole resolution, so the route is aborted even though other PIDs in the family resolve cleanly. The .pid-only fallback at line 559 is never reached because the throw escapes before line 555. | Fix: Make per-PID translation non-throwing inside the map (wrap translateProcessID in `try?`, or a do/catch that logs and returns nil) so a failing PID is skipped. Only throw managedRouteUnavailable when the resolved set is empty (the existing empty-check at 555/563 already covers that).",
      "fixStatus": "1 fixed",
      "fixNotes": "tier2: FU-MULTIPROCESS-PID-RESOLUTION",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "edge-case",
          "title": "One untranslatable sibling PID aborts whole process-object resolution for multi-process apps",
          "detail": "resolveProcessObjectIDs maps the bundle family's candidatePIDs with `try candidatePIDs.compactMap { ... try translateProcessID(forPID:) ... }` (line 546-552). translateProcessID (line 843-867) routes every OSStatus through withStatusCheck (line 1335-1338), which throws BackendError.managedRouteUnavailable on ANY non-noErr status (e.g. kAudioHardwareBadObjectError for a PID that has no Core Audio process object yet — common for transient helper/renderer processes in a browser family). Because the compactMap closure is `throws`, that single failure propagates out of the whole resolution, so the route is aborted even though other PIDs in the family resolve cleanly. The .pid-only fallback at line 559 is never reached because the throw escapes before line 555.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:546",
          "expectedVsActual": "Expected (TAP-01: 'resolves the app's audible Core Audio process objects from its bundle family PIDs'): unresolvable PIDs are skipped and the route is built from the resolvable subset. Actual: any one PID whose translation returns non-noErr throws out of compactMap and aborts resolution for the whole family, marking the app .error/managedRouteUnavailable despite valid audible siblings.",
          "suggestedFix": "Make per-PID translation non-throwing inside the map (wrap translateProcessID in `try?`, or a do/catch that logs and returns nil) so a failing PID is skipped. Only throw managedRouteUnavailable when the resolved set is empty (the existing empty-check at 555/563 already covers that).",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "tier2:applied:FU-MULTIPROCESS-PID-RESOLUTION",
          "uid": "TAP-01#1",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-MULTIPROCESS-PID-RESOLUTION",
          "fixTier": "tier2"
        }
      ]
    },
    {
      "id": "TAP-02",
      "area": "Audio Process Taps & DSP Backend",
      "feature": "Real-time per-app volume and mute on tapped audio",
      "priority": "P0",
      "userStory": "As a user, I want to set a managed app's volume and mute it independently, so that I can quiet or silence one app while everything else keeps playing.",
      "expectedBehavior": [
        "setDesiredVolume clamps to [0,1] (NaN/inf falls back to 1.0), stores desiredVolume, re-applies the route, and sets appliedVolume to 0 when muted else the target.",
        "setMuted stores isMuted, re-applies, and zeroes peak/rms + appliedVolume when muted.",
        "The IO render proc reads a lock-protected TapRenderState; if inactive, muted, or volume==0 it writes silence (zeroOutput) for that cycle.",
        "Otherwise it memcpy's tapped input to output and scales output in place via TapDSP.scale by effectiveVolume = volume * volumeBoost, with TapDSP clamping float to [-1,1] and saturating int16/int32 instead of overflowing.",
        "Volume/mute updates are pushed to the realtime thread asynchronously on the controller's callback queue (apply -> writeVolumeAndMute) so the caller never blocks the audio thread; values are clamped (volume [0,1], boost [1,4]) before write.",
        "On any apply error the app is marked routingState=.error, notes/backendStatus.lastError carry error.localizedDescription, and isRouteRecoveryHealthy=false."
      ],
      "acceptanceCriteria": [
        "Setting volume 0.5 on a managed app halves its output samples (TapDSP.scale gain 0.5 -> linear), verified by TapDSPTests float32AttenuationScalesLinearly.",
        "Muting a managed app produces silence on its aggregate output and zeroes its meters.",
        "Passing NaN/inf volume does not crash and results in desiredVolume defaulting to 1.0.",
        "A failed re-apply leaves the app in .error with a user-visible note and clears isRouteRecoveryHealthy."
      ],
      "codeRefs": [
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:53",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:85",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1684",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1879",
        "Sources/WavesAudioCore/Audio/TapDSP.swift:21",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1651"
      ],
      "implStatus": "implemented",
      "implNotes": "Core mixer behavior is fully wired and unit-tested at the DSP layer. Note: setMuted on success path does NOT recompute appliedVolume from the re-applied state in the same way as desiredVolume edits, but it does set appliedVolume = isMuted ? 0 : desiredVolume, which is consistent. Minor UX gap: setDesiredVolume's success path sets appliedVolume but does not reset peak/rms; muting resets them, unmuting relies on the next level poll to repopulate.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [2L]",
      "errorsFound": "• [LOW/correctness] NaN/inf volume sets desiredVolume=1.0 but skips re-apply and leaves appliedVolume/routingState stale (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:59) — In setDesiredVolume the `guard volume.isFinite else { ...; snapshot.apps[index].desiredVolume = 1.0; return }` branch (lines 59-63) returns immediately: it does NOT call applyRoute and does NOT update appliedVolume or routingState. If the app is currently managed at e.g. 0.3, the live tap keeps scaling by 0.3 while the model now reports desiredVolume=1.0 and appliedVolume=0.3. This is a real divergence from the story's stated behavior, but downgraded from medium to low because the only way a non-finite value reaches this method is a programmatic/edge caller (the UI slider is bounded 0...1), so it has near-zero practical reach and the AC 'does not crash and defaults to 1.0' is still met. | Fix: Treat NaN/inf as target = 1.0 and fall through to the normal apply path instead of returning early: `let target: Float = volume.isFinite ? max(0,min(1,volume)) : 1.0`, store it, then run the same applyRoute + appliedVolume/routingState update block.\n• [LOW/inconsistency] Muting leaves the snapshot's peak/rms stale after the next poll (model-level only; no UI effect) (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1393) — setMuted zeroes snapshot.apps[i].peakLevel/rmsLevel on success (lines 95-96), but the render proc short-circuits to zeroOutput when isMuted!=0 (line 1695-1698) and never calls writeLevels, so the controller's stateBox keeps the last pre-mute peak/rms. updateAudioLevels() (line 1393-1402) then copies controller.getCurrentLevels() back into the snapshot with no isMuted guard, repopulating the muted app's peak/rms ~0.25s later, and audioLevels() (line 1098) re-exposes them. HOWEVER, the first tester's 'meters are re-shown to the UI' claim is a MISREAD: every meter/indicator surface gates on !app.isMuted — showsLevelMeter (MixerRowView.swift:174), the 'Playing audio' subtitle (MixerRowView.swift:187), liveApps (AppStore.swift:120), and activityRank (AppStore.swift:1308). So a muted app never renders a meter regardless of the stale value. This is a latent model/data inconsistency with no user-visible effect, hence low (not medium). The genuinely user-visible variant is the volume-0 case captured under TAP-04. | Fix: In updateAudioLevels, skip/zero muted apps (`if snapshot.apps[index].isMuted { peak = 0; rms = 0 }`), or have the controller zero its stateBox levels when it becomes muted (writeLevels(0,0) in the muted branch / when muted transitions true).",
      "fixStatus": "2 fixed",
      "fixNotes": "tier1: FU-STALE-METERS-ON-SILENCE | tier3: FU-NAN-VOLUME-EARLY-RETURN",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "correctness",
          "title": "NaN/inf volume sets desiredVolume=1.0 but skips re-apply and leaves appliedVolume/routingState stale",
          "detail": "In setDesiredVolume the `guard volume.isFinite else { ...; snapshot.apps[index].desiredVolume = 1.0; return }` branch (lines 59-63) returns immediately: it does NOT call applyRoute and does NOT update appliedVolume or routingState. If the app is currently managed at e.g. 0.3, the live tap keeps scaling by 0.3 while the model now reports desiredVolume=1.0 and appliedVolume=0.3. This is a real divergence from the story's stated behavior, but downgraded from medium to low because the only way a non-finite value reaches this method is a programmatic/edge caller (the UI slider is bounded 0...1), so it has near-zero practical reach and the AC 'does not crash and defaults to 1.0' is still met.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:59",
          "expectedVsActual": "Expected (TAP-02 EXPECTED BEHAVIOR: 'setDesiredVolume clamps to [0,1] (NaN/inf falls back to 1.0), stores desiredVolume, re-applies the route, and sets appliedVolume to 0 when muted else the target'): after NaN/inf the route is re-applied at 1.0 and appliedVolume becomes 1.0 (or 0 if muted). Actual: route is not re-applied and appliedVolume/routingState are left at their old values.",
          "suggestedFix": "Treat NaN/inf as target = 1.0 and fall through to the normal apply path instead of returning early: `let target: Float = volume.isFinite ? max(0,min(1,volume)) : 1.0`, store it, then run the same applyRoute + appliedVolume/routingState update block.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "tier3:applied:FU-NAN-VOLUME-EARLY-RETURN",
          "uid": "TAP-02#1",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-NAN-VOLUME-EARLY-RETURN",
          "fixTier": "tier3"
        },
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "Muting leaves the snapshot's peak/rms stale after the next poll (model-level only; no UI effect)",
          "detail": "setMuted zeroes snapshot.apps[i].peakLevel/rmsLevel on success (lines 95-96), but the render proc short-circuits to zeroOutput when isMuted!=0 (line 1695-1698) and never calls writeLevels, so the controller's stateBox keeps the last pre-mute peak/rms. updateAudioLevels() (line 1393-1402) then copies controller.getCurrentLevels() back into the snapshot with no isMuted guard, repopulating the muted app's peak/rms ~0.25s later, and audioLevels() (line 1098) re-exposes them. HOWEVER, the first tester's 'meters are re-shown to the UI' claim is a MISREAD: every meter/indicator surface gates on !app.isMuted — showsLevelMeter (MixerRowView.swift:174), the 'Playing audio' subtitle (MixerRowView.swift:187), liveApps (AppStore.swift:120), and activityRank (AppStore.swift:1308). So a muted app never renders a meter regardless of the stale value. This is a latent model/data inconsistency with no user-visible effect, hence low (not medium). The genuinely user-visible variant is the volume-0 case captured under TAP-04.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1393",
          "expectedVsActual": "Expected (AC: 'Muting a managed app ... zeroes its meters'): muted app meters stay 0 in the model. Actual: model peak/rms zero on mute, then the next 0.25s poll restores the stale pre-mute value; but because all UI surfaces gate on !isMuted, nothing is shown to the user.",
          "suggestedFix": "In updateAudioLevels, skip/zero muted apps (`if snapshot.apps[index].isMuted { peak = 0; rms = 0 }`), or have the controller zero its stateBox levels when it becomes muted (writeLevels(0,0) in the muted branch / when muted transitions true).",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "tier1:applied:FU-STALE-METERS-ON-SILENCE",
          "uid": "TAP-02#2",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-STALE-METERS-ON-SILENCE",
          "fixTier": "tier1"
        }
      ]
    },
    {
      "id": "TAP-03",
      "area": "Audio Process Taps & DSP Backend",
      "feature": "Per-app volume boost (1x-4x) with clip protection",
      "priority": "P1",
      "userStory": "As a user with a too-quiet app, I want to boost its volume up to 4x above unity, so that I can hear it without raising every other app or my system volume.",
      "expectedBehavior": [
        "setVolumeBoost clamps the boost to [1.0, 4.0], stores volumeBoost, updates the live controller (setVolumeBoost), and re-applies the route.",
        "In the render proc, effectiveVolume = volume * volumeBoost is applied via TapDSP.scale, so boost multiplies the gain on the tapped buffer.",
        "TapDSP.scale clamps boosted float samples to [-1,1] and saturates int16/int32 at their min/max so boost cannot emit out-of-range / overflowed samples that clip harshly.",
        "Presets persist and re-apply volumeBoost per app (applyPreset / saveCurrentPreset include volumeBoost).",
        "AudioApp clamps stored volumeBoost to [0,10] at the model layer, while the backend/controller enforce the user-facing [1,4] range."
      ],
      "acceptanceCriteria": [
        "Boost 4x on a 0.5-volume app saturates to full-scale rather than wrapping (TapDSPTests float32BoostClampsToValidRange, int16/int32 saturation tests).",
        "Requesting boost 6x is clamped to 4x; requesting 0.5x is clamped to 1x.",
        "A saved preset restores the same boost value when re-applied."
      ],
      "codeRefs": [
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:113",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1660",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1899",
        "Sources/WavesAudioCore/Audio/TapDSP.swift:27",
        "Sources/WavesAudioCore/Audio/TapDSP.swift:105"
      ],
      "implStatus": "implemented",
      "implNotes": "Boost is real (applied in the render path) and clip-protected, matching the README's '2x/3x/4x boost' claim. Redundancy concern: setVolumeBoost both calls controller.setVolumeBoost(clampedBoost) AND re-runs applyRoute (which calls controller.apply with app.volumeBoost) — two writes to the realtime state for one user action. setVolumeBoost reads currentState synchronously then writes asynchronously, so a concurrent volume change between read and the queued write could momentarily clobber volume/mute with a slightly stale value (cosmetic, self-corrects on next apply). int16/int32 boost rounds via .rounded() which is fine; unknown sample format silently no-ops scaling (boost has no effect, audio passes through at unity).",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/logic] setVolumeBoost double-writes realtime state (controller.setVolumeBoost + applyRoute) for one action (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:122) — setVolumeBoost calls controller.setVolumeBoost(clampedBoost) (line 123) — which reads currentState synchronously and queues an async writeVolumeAndMute — then immediately calls applyRoute (line 128), which on the fast path (line 367-369) calls controller.apply(volume:, volumeBoost: app.volumeBoost, muted:) queuing a SECOND async write with the same boost (snapshot.apps[index].volumeBoost was already set to clampedBoost at line 119, so apply carries the correct value). Both target the same realtime state for one user action; the first write is fully redundant and, because setVolumeBoost snapshots volume/muted at read time and writes them back later, an interleaved volume/mute change between its read and its queued write can momentarily clobber the newer value (self-correcting on the subsequent apply). Confirmed; cosmetic, so low. | Fix: Drop the explicit controller.setVolumeBoost(clampedBoost) call and rely solely on applyRoute (which already passes the updated app.volumeBoost into controller.apply).",
      "fixStatus": "1 fixed",
      "fixNotes": "tier3: FU-VOLUMEBOOST-OFFQUEUE-WRITE",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "logic",
          "title": "setVolumeBoost double-writes realtime state (controller.setVolumeBoost + applyRoute) for one action",
          "detail": "setVolumeBoost calls controller.setVolumeBoost(clampedBoost) (line 123) — which reads currentState synchronously and queues an async writeVolumeAndMute — then immediately calls applyRoute (line 128), which on the fast path (line 367-369) calls controller.apply(volume:, volumeBoost: app.volumeBoost, muted:) queuing a SECOND async write with the same boost (snapshot.apps[index].volumeBoost was already set to clampedBoost at line 119, so apply carries the correct value). Both target the same realtime state for one user action; the first write is fully redundant and, because setVolumeBoost snapshots volume/muted at read time and writes them back later, an interleaved volume/mute change between its read and its queued write can momentarily clobber the newer value (self-correcting on the subsequent apply). Confirmed; cosmetic, so low.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:122",
          "expectedVsActual": "Expected: one write of the new boost to the realtime state per boost change. Actual: two writes (controller.setVolumeBoost then applyRoute->apply), the first redundant and carrying a stale read of volume/muted.",
          "suggestedFix": "Drop the explicit controller.setVolumeBoost(clampedBoost) call and rely solely on applyRoute (which already passes the updated app.volumeBoost into controller.apply).",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "tier3:applied:FU-VOLUMEBOOST-OFFQUEUE-WRITE",
          "uid": "TAP-03#1",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-VOLUMEBOOST-OFFQUEUE-WRITE",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "TAP-04",
      "area": "Audio Process Taps & DSP Backend",
      "feature": "Live per-app peak/RMS level metering from the tap render path",
      "priority": "P1",
      "userStory": "As a user, I want to see live audio levels for each managed app, so that I can tell which app is actually making sound and confirm my routing is working.",
      "expectedBehavior": [
        "The render proc computes peak + sum-of-squares over the original (pre-scale) input via TapDSP.levels, normalizes int formats to [0,1], and writes peak/RMS into the shared state using a non-blocking try-lock (writeLevels skips the update if contended so the realtime thread never blocks).",
        "A backend level-update task polls controllers every 0.25s and copies each active controller's getCurrentLevels() into the matching app's peakLevel/rmsLevel.",
        "audioLevels() returns levels keyed by logicalID only for apps in .managed or .live state, intended for visibility-gated UI polling.",
        "RMS is sqrt(sum/sampleCount) and is defined as 0 (not NaN) when sampleCount is 0.",
        "Muting an app or disposing its controller zeroes its meters."
      ],
      "acceptanceCriteria": [
        "A known float buffer [1,-1,1,-1] yields peak 1.0 and RMS 1.0 (TapDSPTests levelsComputesPeakAndRMSForKnownFloatInput).",
        "Silence yields peak 0 and RMS 0; zero samples yield RMS 0 not NaN (levelsOfSilence / rmsOfZeroSamplesIsZeroNotNaN).",
        "int16 full-scale samples normalize to ~1.0 peak (int16LevelsNormalizeToUnitRange).",
        "audioLevels() omits apps that are not managed/live."
      ],
      "codeRefs": [
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1924",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1578",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1371",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1096",
        "Sources/WavesAudioCore/Audio/TapDSP.swift:56",
        "Sources/WavesAudioCore/Audio/TapDSP.swift:101"
      ],
      "implStatus": "implemented",
      "implNotes": "Meters are derived from real tapped audio and the realtime path is correctly non-blocking (try-lock on writeLevels, sync read on the poll side). Subtlety: levels are computed from the INPUT buffer before volume/boost scaling, so meters reflect source loudness, not post-fader output — reasonable but worth noting for UX expectations. The 0.25s poll cadence runs whenever any controller exists (it is not itself visibility-gated inside the backend; the comment says callers should gate). audioLevels() includes .live apps, but .live apps have no controller, so their peak/rms stay 0 until they become managed.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1M 1L]",
      "errorsFound": "• [MEDIUM/correctness] A managed app set to 0% volume (unmuted) keeps a frozen non-zero meter and false 'Playing audio' indicator (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1396) — When a managed app's volume is set to 0 (not muted), the render proc short-circuits at `if volume == 0.0 { zeroOutput; return }` (line 1702-1704) and never reaches writeLevels (line 1937), so the controller's stateBox keeps the last non-zero peak/rms. setDesiredVolume's success path (lines 65-74) does NOT zero peak/rms (only setMuted does). updateAudioLevels (line 1393-1402) then copies the stale levels back and audioLevels()/store.liveLevels keep returning them. Crucially, unlike the muted case, NO UI surface gates on volume==0: showsLevelMeter (MixerRowView.swift:174) and the 'Playing audio' subtitle (MixerRowView.swift:187) gate only on !isMuted, and liveApps (AppStore.swift:120) / activityRank (AppStore.swift:1308) require only `!app.isMuted && max(peakLevel,rmsLevel) > 0.001`. So a silent 0%-volume managed app renders a frozen cyan level meter, shows 'Playing audio', and stays sorted in the Live section indefinitely. This is the genuinely user-visible form of the stale-meter bug the first tester reported only for the (fully UI-gated, inert) muted case. | Fix: In updateAudioLevels, zero peak/rms when the app is effectively silent (snapshot.apps[index].isMuted || snapshot.apps[index].appliedVolume == 0), or have the render proc write zero levels (writeLevels(0,0)) in the muted and volume==0 short-circuit branches so getCurrentLevels() refreshes to 0.\n• [LOW/inconsistency] Level poller does not refresh meters for muted apps (model-level stale, no UI effect) (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1396) — Same root cause as TAP-02's second issue: updateAudioLevels (line 1393-1402) copies controller.getCurrentLevels() with no isMuted guard, and the muted render path (line 1695-1698) never updates the stateBox, so a muted app's snapshot peak/rms is repopulated with the frozen pre-mute value and audioLevels() re-exposes it. As established, all meter/indicator surfaces gate on !app.isMuted (MixerRowView.swift:174/187, AppStore.swift:120/1308), so the muted case is not actually shown to the user — hence low (not medium as first reported). The user-visible defect is the volume-0 case above. | Fix: Zero peak/rms for muted apps in updateAudioLevels, or zero the controller's stateBox levels when it becomes muted.",
      "fixStatus": "2 fixed",
      "fixNotes": "tier1: FU-STALE-METERS-ON-SILENCE",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "correctness",
          "title": "A managed app set to 0% volume (unmuted) keeps a frozen non-zero meter and false 'Playing audio' indicator",
          "detail": "When a managed app's volume is set to 0 (not muted), the render proc short-circuits at `if volume == 0.0 { zeroOutput; return }` (line 1702-1704) and never reaches writeLevels (line 1937), so the controller's stateBox keeps the last non-zero peak/rms. setDesiredVolume's success path (lines 65-74) does NOT zero peak/rms (only setMuted does). updateAudioLevels (line 1393-1402) then copies the stale levels back and audioLevels()/store.liveLevels keep returning them. Crucially, unlike the muted case, NO UI surface gates on volume==0: showsLevelMeter (MixerRowView.swift:174) and the 'Playing audio' subtitle (MixerRowView.swift:187) gate only on !isMuted, and liveApps (AppStore.swift:120) / activityRank (AppStore.swift:1308) require only `!app.isMuted && max(peakLevel,rmsLevel) > 0.001`. So a silent 0%-volume managed app renders a frozen cyan level meter, shows 'Playing audio', and stays sorted in the Live section indefinitely. This is the genuinely user-visible form of the stale-meter bug the first tester reported only for the (fully UI-gated, inert) muted case.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1396",
          "expectedVsActual": "Expected (TAP-04: meters reflect actual tapped audio; a silenced app reads 0): a 0%-volume app, which emits silence, shows an empty/zero meter and is not flagged as playing. Actual: the meter freezes at the last pre-zero level and the app keeps a 'Playing audio'/Live classification until volume is raised or the controller is disposed.",
          "suggestedFix": "In updateAudioLevels, zero peak/rms when the app is effectively silent (snapshot.apps[index].isMuted || snapshot.apps[index].appliedVolume == 0), or have the render proc write zero levels (writeLevels(0,0)) in the muted and volume==0 short-circuit branches so getCurrentLevels() refreshes to 0.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "tier1:applied:FU-STALE-METERS-ON-SILENCE",
          "uid": "TAP-04#1",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-STALE-METERS-ON-SILENCE",
          "fixTier": "tier1"
        },
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "Level poller does not refresh meters for muted apps (model-level stale, no UI effect)",
          "detail": "Same root cause as TAP-02's second issue: updateAudioLevels (line 1393-1402) copies controller.getCurrentLevels() with no isMuted guard, and the muted render path (line 1695-1698) never updates the stateBox, so a muted app's snapshot peak/rms is repopulated with the frozen pre-mute value and audioLevels() re-exposes it. As established, all meter/indicator surfaces gate on !app.isMuted (MixerRowView.swift:174/187, AppStore.swift:120/1308), so the muted case is not actually shown to the user — hence low (not medium as first reported). The user-visible defect is the volume-0 case above.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1396",
          "expectedVsActual": "Expected (TAP-04: 'Muting an app or disposing its controller zeroes its meters'): muted app meters report 0 in the model. Actual: dispose zeroes meters; mute zeroes them momentarily but the next poll restores the frozen pre-mute peak/rms in the model (UI is unaffected because it gates on mute).",
          "suggestedFix": "Zero peak/rms for muted apps in updateAudioLevels, or zero the controller's stateBox levels when it becomes muted.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "tier1:applied:FU-STALE-METERS-ON-SILENCE",
          "uid": "TAP-04#2",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-STALE-METERS-ON-SILENCE",
          "fixTier": "tier1"
        }
      ]
    },
    {
      "id": "TAP-05",
      "area": "Audio Process Taps & DSP Backend",
      "feature": "Capability and capture-permission gating with diagnostics",
      "priority": "P0",
      "userStory": "As a user on an unsupported OS or without audio-capture permission, I want Waves to honestly tell me per-app control can't take effect and how to fix it, so that I'm not misled into thinking routing works when it silently does nothing.",
      "expectedBehavior": [
        "supportsPerAppRouting requires macOS 14.2+; on older OS applyRoute/createController throw unsupportedOperation and every app is forced to monitorOnly with note 'Per-app route requires macOS 14.2+' and compatibility .planned.",
        "refreshCaptureAuthorization probes real TCC capture by creating and immediately destroying a private global process tap (no IO proc, no audio captured): noErr => .authorized, any failure => .notGranted, pre-14.2 => .unsupported; the result caches on captureAuthorization.",
        "buildSnapshot re-probes capture authorization every refresh so backendStatus.hasRequiredPermissions reflects actual capture grant, and isRouteRecoveryHealthy requires support AND authorized AND no route errors.",
        "diagnosticsReport surfaces checks for audio component (OS support), capture permission (passed/failed/warning with System Settings guidance), accessibility permission, route recovery health, and support-matrix coverage.",
        "When capture is not granted, the diagnostics detail directs the user to System Settings > Privacy & Security > Microphone."
      ],
      "acceptanceCriteria": [
        "On macOS < 14.2, no taps are created; all apps are monitorOnly/planned and diagnostics report per-app routing unsupported.",
        "With capture denied, refreshCaptureAuthorization returns .notGranted and the capture diagnostics check is .failed with actionable text.",
        "backendStatus.hasRequiredPermissions is true only when captureAuthorization == .authorized.",
        "The capability probe creates no lasting tap (it destroys the probe tap immediately)."
      ],
      "codeRefs": [
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:310",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:332",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:988",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1086",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:241",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:280"
      ],
      "implStatus": "implemented",
      "implNotes": "The capture probe is a genuinely clever and honest TCC check (create+destroy a global tap) rather than guessing from UI state, and it is re-run on every snapshot build so stale 'authorized' can't persist. Limitation: Core Audio exposes no dedicated 'denied' code, so undetermined vs. denied are both collapsed to .notGranted after a probe — a fresh, never-prompted state can show .undetermined only until the first probe runs, after which a not-yet-granted user reads as .notGranted/.failed. The probe creating a real (if instantly destroyed) tap is what can trigger the system permission prompt, which is intended but means merely opening diagnostics may prompt the user.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "TAP-06",
      "area": "Audio Process Taps & DSP Backend",
      "feature": "Route resilience: retry/backoff, recovery, device-change auto-restore, and lifecycle cleanup",
      "priority": "P0",
      "userStory": "As a user, I want my per-app routes to survive transient failures, output-device changes, and app quits, so that my volume/mute settings keep working without me manually re-establishing them and without orphaned audio devices piling up.",
      "expectedBehavior": [
        "createControllerWithRetry retries tap/aggregate creation up to 3 times with exponential backoff (100/400/1600ms), re-resolving process object IDs between attempts; after exhausting retries it throws managedRouteUnavailable with plain-language guidance.",
        "A reentrancy guard disposes any controller installed for the same app during an await suspension so live taps/aggregates are not orphaned (no leak / double audio).",
        "recoverRoutes() tears down all controllers, rebuilds the snapshot, clears lastError, and reattaches routes for previously managed/active logical IDs; reattachRoutes sets isRouteRecoveryHealthy based on whether any route recovered.",
        "A default-output-device change listener fires handleDeviceChange -> autoRestoreDevice(), which disposes controllers, rebuilds the snapshot on the new device, reattaches managed routes, and yields a deviceChangeEvents signal so the UI/store can refresh and restore per-device presets even if restoration failed.",
        "releaseControllers(forBundleID:pid:) promptly disposes a quit app's controller (destroying its IO proc, aggregate device, and process tap) and resets that app to monitorOnly with zeroed levels, instead of leaking until the next manual refresh.",
        "disposeControllers(keeping:) garbage-collects controllers for apps no longer present in the snapshot; dispose() is idempotent (didDispose guard) and fully tears down IO proc + aggregate + tap, draining the callback queue.",
        "stop() removes the device-change listener, cancels the level task, disposes all controllers, and finishes the deviceChangeEvents stream; the listener registration is guarded against double-install."
      ],
      "acceptanceCriteria": [
        "A transient tap-creation failure that succeeds on attempt 2 yields a working managed route (logged 'on attempt 2').",
        "After 3 failed attempts the app is marked .error with a message pointing at Privacy & Security, and no partial tap/aggregate is left running.",
        "Switching the default output device disposes old aggregates, rebuilds routes on the new device, and emits exactly one deviceChangeEvents value.",
        "Quitting a managed app releases its tap/aggregate (no lingering 'Waves-<app>' aggregate device) and resets it to monitorOnly.",
        "recoverRoutes on a healthy session re-establishes all previously managed routes and sets isRouteRecoveryHealthy=true."
      ],
      "codeRefs": [
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:389",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:204",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:223",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1462",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1104",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1851",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1405"
      ],
      "implStatus": "implemented",
      "implNotes": "This is unusually thorough lifecycle handling: explicit leak avoidance on the reentrancy race (line 382), idempotent dispose, retry with PID re-resolution, and device-change auto-restore with an event stream. Concerns: (1) reattachRoutes sets isRouteRecoveryHealthy=true if ANY route recovers, so a session where some apps fail can still report 'healthy' while individual apps are .error. (2) autoRestoreDevice/recoverRoutes dispose then rebuild ALL controllers on any default-device change, causing a brief audio gap for every managed app even if only one app cared about the device. (3) The device-change listener only watches kAudioHardwarePropertyDefaultOutputDevice, so hot-unplugging a per-app pinned (targetDeviceUID) device that is NOT the default won't auto-trigger recovery — that route stays .managed in state until the next manual refresh/apply, where createController then fails with 'device unavailable'. (4) deviceChangeListenerToken is a simple 0/1 flag, fine here but not a real token. These are edge-case correctness/UX gaps, not blockers, so the feature is marked implemented.",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [1M 2L]",
      "errorsFound": "• [MEDIUM/edge-case] Hot-unplugging a per-app pinned (non-default) output device does not trigger auto-restore (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1410) — addDeviceChangeListener registers exactly one listener, on kAudioHardwarePropertyDefaultOutputDevice (line 1410-1427; verified this is the only AudioObjectAddPropertyListenerBlock call in the file). If an app is pinned to a specific targetDeviceUID that is NOT the system default and that device is unplugged, no listener fires, handleDeviceChange/autoRestoreDevice are never invoked, and the controller keeps an aggregate whose main/sub device just vanished. The app stays routingState=.managed in the snapshot (and in buildSnapshot's merge at line 1044-1047 it is re-marked .managed as long as controller.isActive reports true) even though its aggregate is broken, until a manual refresh/re-apply — at which point createController throws 'The chosen output device ... is unavailable.' (line 438). Meters freeze/go silent with no error surfaced in the meantime. | Fix: Also register a listener on kAudioHardwarePropertyDevices and, on change, check each managed app whose targetDeviceUID is no longer in availableOutputDevices(): dispose its controller and mark it .error (or attempt reattach on the default) so the UI reflects the broken route immediately.\n• [LOW/inconsistency] isRouteRecoveryHealthy reports healthy if ANY route recovers, masking partially-failed sessions (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1366) — reattachRoutes sets snapshot.backendStatus.isRouteRecoveryHealthy = recoveredAnyRoute (line 1366), where recoveredAnyRoute (line 1358) becomes true as soon as a single app reattaches. In a recover/auto-restore where some apps reattach and others throw and are marked .error (line 1360-1362), the session-level health still reads true and the diagnostics 'Route recovery' check (diagnosticsReport line 264-270) shows .passed even though individual apps are in .error and lastError is set. Confirmed; low. | Fix: Set isRouteRecoveryHealthy = recoveredAnyRoute && (lastError == nil) — require that no app in the reattach set ended in .error — or compute it as 'all attempted routes recovered'.\n• [LOW/error-handling] Mid-retry re-resolve can escape createControllerWithRetry, bypassing the friendly exhausted message (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:412) — Inside the retry loop's catch block, the between-attempt re-resolution `let refreshedProcessObjectIDs = try resolveProcessObjectIDs(for: app)` (line 412) is not wrapped in its own do/catch. If the app quits or loses all audible process objects between attempts, resolveProcessObjectIDs throws (line 563) and that error propagates straight out of createControllerWithRetry, skipping the remaining attempts and the final managedRouteUnavailable 'check that audio capture is allowed in System Settings › Privacy & Security' message (line 424-426). Confirmed; low. | Fix: Wrap the re-resolve in `if let refreshed = try? resolveProcessObjectIDs(for: app) { currentProcessObjectIDs = refreshed }` so a transient resolution failure between attempts is tolerated and the loop continues to the final friendly error.",
      "fixStatus": "2 fixed · 1 deferred",
      "fixNotes": "tier1: FU-ROUTE-HEALTH-ANY-SUCCESS | tier3: FU-RETRY-RERESOLVE-ESCAPE",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "edge-case",
          "title": "Hot-unplugging a per-app pinned (non-default) output device does not trigger auto-restore",
          "detail": "addDeviceChangeListener registers exactly one listener, on kAudioHardwarePropertyDefaultOutputDevice (line 1410-1427; verified this is the only AudioObjectAddPropertyListenerBlock call in the file). If an app is pinned to a specific targetDeviceUID that is NOT the system default and that device is unplugged, no listener fires, handleDeviceChange/autoRestoreDevice are never invoked, and the controller keeps an aggregate whose main/sub device just vanished. The app stays routingState=.managed in the snapshot (and in buildSnapshot's merge at line 1044-1047 it is re-marked .managed as long as controller.isActive reports true) even though its aggregate is broken, until a manual refresh/re-apply — at which point createController throws 'The chosen output device ... is unavailable.' (line 438). Meters freeze/go silent with no error surfaced in the meantime.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1410",
          "expectedVsActual": "Expected (TAP-06 user story + AC: per-app routes survive output-device changes / an unplugged pinned device is detected and recovered or honestly marked .error automatically): unplugging a pinned non-default device is detected and the route is recovered or marked .error. Actual: only default-output-device changes are observed; a pinned non-default device disappearing leaves a stale .managed route with a dead aggregate until a manual refresh.",
          "suggestedFix": "Also register a listener on kAudioHardwarePropertyDevices and, on change, check each managed app whose targetDeviceUID is no longer in availableOutputDevices(): dispose its controller and mark it .error (or attempt reattach on the default) so the UI reflects the broken route immediately.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "deferred:needs-hardware:Adds a new AudioObjectAddPropertyListenerBlock and a device-vanished reconciliation policy that changes audio-routing behavior, interacts with device-change ree",
          "uid": "TAP-06#1",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift"
        },
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "isRouteRecoveryHealthy reports healthy if ANY route recovers, masking partially-failed sessions",
          "detail": "reattachRoutes sets snapshot.backendStatus.isRouteRecoveryHealthy = recoveredAnyRoute (line 1366), where recoveredAnyRoute (line 1358) becomes true as soon as a single app reattaches. In a recover/auto-restore where some apps reattach and others throw and are marked .error (line 1360-1362), the session-level health still reads true and the diagnostics 'Route recovery' check (diagnosticsReport line 264-270) shows .passed even though individual apps are in .error and lastError is set. Confirmed; low.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1366",
          "expectedVsActual": "Expected: a session with any failed route should not advertise itself as fully recovery-healthy/.passed. Actual: isRouteRecoveryHealthy=true whenever at least one route recovered, regardless of how many ended in .error.",
          "suggestedFix": "Set isRouteRecoveryHealthy = recoveredAnyRoute && (lastError == nil) — require that no app in the reattach set ended in .error — or compute it as 'all attempted routes recovered'.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "tier1:applied:FU-ROUTE-HEALTH-ANY-SUCCESS",
          "uid": "TAP-06#2",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-ROUTE-HEALTH-ANY-SUCCESS",
          "fixTier": "tier1"
        },
        {
          "severity": "low",
          "type": "error-handling",
          "title": "Mid-retry re-resolve can escape createControllerWithRetry, bypassing the friendly exhausted message",
          "detail": "Inside the retry loop's catch block, the between-attempt re-resolution `let refreshedProcessObjectIDs = try resolveProcessObjectIDs(for: app)` (line 412) is not wrapped in its own do/catch. If the app quits or loses all audible process objects between attempts, resolveProcessObjectIDs throws (line 563) and that error propagates straight out of createControllerWithRetry, skipping the remaining attempts and the final managedRouteUnavailable 'check that audio capture is allowed in System Settings › Privacy & Security' message (line 424-426). Confirmed; low.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:412",
          "expectedVsActual": "Expected (TAP-06: 'after exhausting retries it throws managedRouteUnavailable with plain-language guidance'): retries proceed and the user sees the Privacy & Security guidance. Actual: a throw from the mid-retry re-resolve short-circuits the loop and surfaces the raw 'Unable to resolve active Core Audio process objects' error instead.",
          "suggestedFix": "Wrap the re-resolve in `if let refreshed = try? resolveProcessObjectIDs(for: app) { currentProcessObjectIDs = refreshed }` so a transient resolution failure between attempts is tolerated and the loop continues to the final friendly error.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "tier3:applied:FU-RETRY-RERESOLVE-ESCAPE",
          "uid": "TAP-06#3",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-RETRY-RERESOLVE-ESCAPE",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "ROUTE-01",
      "area": "Route Health & Status (visible/monitored/managed/errored)",
      "feature": "Per-app route-state chip",
      "priority": "P0",
      "userStory": "As a user managing per-app audio, I want each app row to clearly label its routing state (Live, Recent, Managed, Ready/monitored, or Error), so that I can tell at a glance whether Waves is actually controlling that app's audio or just observing it.",
      "expectedBehavior": [
        "RoutingState has five cases: live, recent, managed, monitorOnly (displayName \"Ready\"), error (AudioApp.swift:200-221).",
        "In a full mixer row, RoutingStateIndicator renders an SF Symbol + text label + tinted capsule: managed=green checkmark.circle.fill \"Managed\", live=accent waveform \"Live\", recent=secondary clock.fill \"Recent\", error=red exclamationmark.triangle.fill \"Error\" (MixerRowView.swift:407-481).",
        "The monitorOnly (\"Ready\") chip is intentionally NOT drawn in the full row — RoutingStateIndicator returns nothing when state == .monitorOnly (MixerRowView.swift:413).",
        "Compact rows (menu bar) render RoutingStateDot, an icon-only indicator that DOES include monitorOnly (open checkmark.circle, secondary color) (MixerRowView.swift:278,483-511).",
        "Each chip/dot carries a help tooltip and a VoiceOver accessibilityLabel describing the state, so route state is conveyed without relying on color alone (MixerRowView.swift:435-436,467-480,491-492).",
        "Under Increase Contrast the chip gains a solid stroked outline and higher background opacity (MixerRowView.swift:430-434,442-443).",
        "The chip/dot is hidden entirely for excluded apps, since Waves isn't controlling them (MixerRowView.swift:46-50,273-279).",
        "routingState is assigned by the backend: managed when a tap controller is active, live when the app is currently audible, monitorOnly otherwise (WorkspaceAudioControlBackend.swift:1036-1055,1178)."
      ],
      "acceptanceCriteria": [
        "A managed app shows a green \"Managed\" chip with a checkmark icon in the main window row.",
        "A currently-audible unmanaged app shows an accent \"Live\" chip with a waveform icon.",
        "A monitor-only app shows NO chip in the main window row, but shows an open-circle dot in the compact menu-bar row.",
        "Each indicator exposes a non-color VoiceOver label (e.g. \"Managed route is active.\") and a matching tooltip.",
        "Excluded apps show no route-state chip/dot at all.",
        "With Increase Contrast enabled, chips render with a solid outline."
      ],
      "codeRefs": [
        "Sources/WavesAudioCore/Models/AudioApp.swift:200-221",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:48-50",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:390-511",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1036-1055",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1178"
      ],
      "implStatus": "implemented",
      "implNotes": "Wired end-to-end and rendered consistently. Two UX inconsistencies worth noting: (1) the \"Ready\"/monitorOnly state has a real displayName, help text, and an icon defined, but the full-row RoutingStateIndicator suppresses it (line 413), so in the primary window a monitored app is visually indistinguishable from a recent one (both show no chip) — the honest visible/monitored/managed distinction the docs promise is only partially expressed in the main window. (2) The compact dot does render monitorOnly, so the two surfaces disagree on whether \"Ready\" is shown. The README/DESIGN explicitly promise distinguishable visible/monitored/managed states; this is a minor gap against that intent.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [2L]",
      "errorsFound": "• [LOW/dead-code] The \"Recent\" (clock.fill) chip is unreachable in normal operation (Sources/Waves/Features/Mixer/MixerRowView.swift:399-405) — Re-verified. discoverRunningApps assigns only .live or .monitorOnly (WorkspaceAudioControlBackend.swift:1178). The buildSnapshot per-index loop (1036-1054) only ever produces .managed, .monitorOnly, or leaves .live; the retention loop (1017-1034) sets .managed or .monitorOnly. setDesiredVolume/setMuted/etc. produce .managed or .error. The ONLY source of .recent is the AudioApp init/decoder default (AudioApp.swift:48,135), which can only surface a persisted snapshot transiently before backend.start() rebuilds. So a running/discovered app is never assigned .recent in steady state, making the Recent chip effectively dead UI. Confirmed. | Fix: Either assign .recent in discovery for apps that were recently audible (e.g. audible within the last N seconds via a remembered audible-PID window), or remove the .recent case and its UI branches if the state is not intended to be produced at runtime.\n• [LOW/inconsistency] Main window and menu bar disagree on whether monitor-only (\"Ready\") is shown (Sources/Waves/Features/Mixer/MixerRowView.swift:413) — Re-verified. RoutingStateIndicator (full row) returns nothing when state == .monitorOnly (MixerRowView.swift:413), so a monitored app shows no chip in the main window and is visually identical to a (never-reached) recent app. RoutingStateDot (compact menu-bar row, used at MixerRowView.swift:278) DOES render .monitorOnly as an open checkmark.circle (503-504). The two surfaces express the visible/monitored/managed distinction differently for the same app. Confirmed — though this matches ROUTE-01's own documented expectedBehavior (the doc says the suppression and the dot are intentional), so it is a UX inconsistency against README/DESIGN intent rather than a code-vs-doc mismatch. | Fix: Decide on one policy: either render a subtle \"Ready\" chip in the full row too (it already has displayName/icon/help defined), or drop .monitorOnly from RoutingStateDot for parity and document that monitorOnly is intentionally chip-less in both surfaces.",
      "fixStatus": "2 n/a",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "dead-code",
          "title": "The \"Recent\" (clock.fill) chip is unreachable in normal operation",
          "detail": "Re-verified. discoverRunningApps assigns only .live or .monitorOnly (WorkspaceAudioControlBackend.swift:1178). The buildSnapshot per-index loop (1036-1054) only ever produces .managed, .monitorOnly, or leaves .live; the retention loop (1017-1034) sets .managed or .monitorOnly. setDesiredVolume/setMuted/etc. produce .managed or .error. The ONLY source of .recent is the AudioApp init/decoder default (AudioApp.swift:48,135), which can only surface a persisted snapshot transiently before backend.start() rebuilds. So a running/discovered app is never assigned .recent in steady state, making the Recent chip effectively dead UI. Confirmed.",
          "codeRef": "Sources/Waves/Features/Mixer/MixerRowView.swift:399-405",
          "expectedVsActual": "Expected: five distinguishable route states including a reachable \"Recent\" chip (ROUTE-01 expectedBehavior). Actual: a running/discovered app is never assigned .recent, so the Recent chip never renders in steady state; the documented five-state distinction collapses to four reachable states.",
          "suggestedFix": "Either assign .recent in discovery for apps that were recently audible (e.g. audible within the last N seconds via a remembered audible-PID window), or remove the .recent case and its UI branches if the state is not intended to be produced at runtime.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "ROUTE-01#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MixerRowView.swift"
        },
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "Main window and menu bar disagree on whether monitor-only (\"Ready\") is shown",
          "detail": "Re-verified. RoutingStateIndicator (full row) returns nothing when state == .monitorOnly (MixerRowView.swift:413), so a monitored app shows no chip in the main window and is visually identical to a (never-reached) recent app. RoutingStateDot (compact menu-bar row, used at MixerRowView.swift:278) DOES render .monitorOnly as an open checkmark.circle (503-504). The two surfaces express the visible/monitored/managed distinction differently for the same app. Confirmed — though this matches ROUTE-01's own documented expectedBehavior (the doc says the suppression and the dot are intentional), so it is a UX inconsistency against README/DESIGN intent rather than a code-vs-doc mismatch.",
          "codeRef": "Sources/Waves/Features/Mixer/MixerRowView.swift:413",
          "expectedVsActual": "Expected (per README/DESIGN intent): distinguishable visible/monitored/managed states. Actual: monitorOnly is suppressed in the main window but shown in the menu bar, so the honest \"monitored\" distinction is only partially expressed and inconsistent across surfaces.",
          "suggestedFix": "Decide on one policy: either render a subtle \"Ready\" chip in the full row too (it already has displayName/icon/help defined), or drop .monitorOnly from RoutingStateDot for parity and document that monitorOnly is intentionally chip-less in both surfaces.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "ROUTE-01#2",
          "canonicalFile": "Sources/Waves/Features/Mixer/MixerRowView.swift"
        }
      ]
    },
    {
      "id": "ROUTE-02",
      "area": "Route Health & Status (visible/monitored/managed/errored)",
      "feature": "Errored route surfacing with inline failure reason",
      "priority": "P0",
      "userStory": "As a user whose per-app route failed to set up, I want the row to show an Error state with the specific failure reason, so that I understand why volume/mute didn't take effect and what to do next.",
      "expectedBehavior": [
        "Every route-applying backend action (setDesiredVolume, setMuted, setVolumeBoost, applyPreset, setOutputDevice) sets routingState=.error and stores error.localizedDescription into app.notes when applyRoute throws (WorkspaceAudioControlBackend.swift:76-77,102-103,134-135,176-177,736-737,1360-1361).",
        "In the full mixer row, when routingState==.error and notes are present, the failure text is rendered beneath the row in caption/secondary styling, indented under the slider (MixerRowView.swift:111-116).",
        "The error chip itself shows a red exclamation-triangle with \"Error\" and tooltip \"Route setup failed.\" (MixerRowView.swift:402-403,462-463,476-477).",
        "Errored apps are reachable via the \"Needs attention\" VoiceOver rotor, which filters apps where routingState==.error (MainWindowView.swift:393-397).",
        "An app's error note is cleared (notes=nil) and state returns to .managed on the next successful apply or recovery (WorkspaceAudioControlBackend.swift:71-72,729-733).",
        "Successful applies also clear the global backendStatus.lastError and mark route recovery healthy again (WorkspaceAudioControlBackend.swift:73-74,99-100)."
      ],
      "acceptanceCriteria": [
        "When a route apply fails, the affected row shows a red \"Error\" chip and the failure's localized description below it.",
        "The VoiceOver \"Needs attention\" rotor lists exactly the apps in error state.",
        "After a subsequent successful volume/mute change, the error chip and note disappear and the chip returns to \"Managed\".",
        "A failed action also surfaces a toast (e.g. \"Mute toggle failed\") via the store catch blocks."
      ],
      "codeRefs": [
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:76-77",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:102-103",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:729-739",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:111-116",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:393-397"
      ],
      "implStatus": "implemented",
      "implNotes": "Solid and honest: the raw localizedDescription is exposed verbatim, which is great for diagnostics but can be terse/technical for non-technical users (e.g. \"managedRouteUnavailable(...)\" wording from BackendError). The inline note is only shown in the full MixerRowView, not the compact menu-bar row, so a menu-bar-only user sees the red error dot but no reason text. Error apps also are not pulled into a dedicated visible \"needs attention\" section in the on-screen list (only the accessibility rotor groups them); a sighted user must scan rows to find the red chip.",
      "source": "phase1-story",
      "testStatus": "4 issue(s) [2H 1M 1L]",
      "errorsFound": "• [HIGH/correctness] A failed volume change does not leave a persistent Error chip/note (unlike mute/boost) (Sources/Waves/Stores/AppStore.swift:443-460) — Re-verified. On a failed volume apply the backend's setDesiredVolume sets the app to .error with notes (WorkspaceAudioControlBackend.swift:76-77) before throwing. The store's scheduleVolumeApply catch (AppStore.swift:443-460) shows the \"Volume change failed\" toast, then calls self.session = try await self.backend.refresh() (453). refresh() rebuilds via buildSnapshot whose merge (999-1014) does NOT carry over previous.routingState/notes, and whose per-index loop resets a no-controller, non-.live app back to .monitorOnly with notes=nil (1051-1054), discarding the .error the backend just set. By contrast setMuted/setVolumeBoost catch blocks use backend.currentSnapshot() + mergeAppState (AppStore.swift:535-536,581-582); mergeAppState (1417-1436) copies the single backend app verbatim, preserving .error + notes. Confirmed and the inconsistency with mute/boost is real. | Fix: In the volume-failure path, mirror setMuted/setVolumeBoost: replace backend.refresh() with backend.currentSnapshot() + mergeAppState(from:appID:) so the .error state and notes set by the backend are preserved and rendered.\n• [HIGH/correctness] Any plain refresh silently clears Error chips/notes without a successful re-apply (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1048-1054) — Re-verified. buildSnapshot's merge (WorkspaceAudioControlBackend.swift:999-1014) carries over volume/mute/pin/boost/targetDevice but NOT routingState or notes, and the per-index loop (1051-1054) resets any non-controller, non-.live app to .monitorOnly with notes=nil. buildSnapshot runs on every backend.refresh() (store.refresh() AppStore.swift:242, the Cmd-R toolbar at MainWindowView.swift:39) and on every autoRestoreDevice() (device-change). So after a real route failure has correctly shown an Error chip, the next refresh (which can be user- or system-triggered with no successful apply) clears the chip and inline reason. Note buildSnapshot also recomputes isRouteRecoveryHealthy = !hasRouteErrors (1089), so the global RouteHealthBadge also flips back to green after the same refresh. Confirmed. | Fix: Preserve previous.routingState==.error and previous.notes across buildSnapshot for apps rediscovered with no active controller, OR re-probe/re-apply the route during rebuild so the error clears only when the route actually recovers.\n• [MEDIUM/accessibility] \"Needs attention\" rotor under-reports errored apps in non-Running filters (Sources/Waves/Features/Mixer/MainWindowView.swift:393-397) — Re-verified. The rotor filters the view-local `apps` array for routingState==.error (MainWindowView.swift:393-397). `apps` is bound to filteredApps (line 20), which equals scopedApps when not searching; scopedApps is store.visibleApps only for .running, but store.liveApps for .frontmost, store.pinnedApps for .pinned, and store.recentApps for .recent (MainWindowView.swift:102-113). An errored app is not .live, may not be pinned, and recentApps is empty when showRecentApps is off. So on any tab other than Running the rotor omits errored apps outside that scoped subset, defeating the accessibility affordance for VoiceOver users. Confirmed. | Fix: Build the rotor from store.visibleApps.filter { $0.routingState == .error } (all visible apps) rather than the view-scoped `apps`, so it always enumerates every errored app regardless of the active filter. (Severity raised to medium: this is the only programmatic affordance that groups errored apps, so the gap fully blocks VoiceOver users from reaching them in 3 of 4 tabs.)\n• [LOW/ux] Errored apps have no on-screen failure reason in the menu bar surface (Sources/Waves/Features/Mixer/MixerRowView.swift:483-511) — Re-verified. The inline failure note renders only in the full MixerRowView when routingState==.error (MixerRowView.swift:111-116). CompactMixerRow (menu bar) shows only RoutingStateDot, a red exclamation-triangle for .error with no notes text (483-511). Additionally the menu bar lists only Pinned/Live/Recent CompactSections (MenuBarMixerView.swift:42-50); a non-live errored app falls into recentApps only if showRecentApps is on (recentApps = visibleApps minus live), so with showRecentApps off an errored, unpinned, non-audible app does not appear at all — a menu-bar-only user gets no indication of the failure. Confirmed. | Fix: Surface the error reason in the compact row (tooltip/secondary line on the dot) and/or always include errored apps in a menu-bar \"Needs attention\" section independent of showRecentApps.",
      "fixStatus": "4 fixed",
      "fixNotes": "tier1: FU-REFRESH-CLEARS-ERRORS; FU-VOLUME-ERROR-CHIP | tier2: FU-NEEDS-ATTENTION-ROTOR-SCOPE",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "correctness",
          "title": "A failed volume change does not leave a persistent Error chip/note (unlike mute/boost)",
          "detail": "Re-verified. On a failed volume apply the backend's setDesiredVolume sets the app to .error with notes (WorkspaceAudioControlBackend.swift:76-77) before throwing. The store's scheduleVolumeApply catch (AppStore.swift:443-460) shows the \"Volume change failed\" toast, then calls self.session = try await self.backend.refresh() (453). refresh() rebuilds via buildSnapshot whose merge (999-1014) does NOT carry over previous.routingState/notes, and whose per-index loop resets a no-controller, non-.live app back to .monitorOnly with notes=nil (1051-1054), discarding the .error the backend just set. By contrast setMuted/setVolumeBoost catch blocks use backend.currentSnapshot() + mergeAppState (AppStore.swift:535-536,581-582); mergeAppState (1417-1436) copies the single backend app verbatim, preserving .error + notes. Confirmed and the inconsistency with mute/boost is real.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:443-460",
          "expectedVsActual": "Expected (ROUTE-02): a failed apply shows a red Error chip and the failure's localized description below the row. Actual: for volume changes the error chip/note is wiped within the same catch block by refresh(), so the user sees only a transient toast and no persistent Error state — inconsistent with mute/boost which preserve it.",
          "suggestedFix": "In the volume-failure path, mirror setMuted/setVolumeBoost: replace backend.refresh() with backend.currentSnapshot() + mergeAppState(from:appID:) so the .error state and notes set by the backend are preserved and rendered.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:FU-VOLUME-ERROR-CHIP",
          "uid": "ROUTE-02#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-VOLUME-ERROR-CHIP",
          "fixTier": "tier1"
        },
        {
          "severity": "high",
          "type": "correctness",
          "title": "Any plain refresh silently clears Error chips/notes without a successful re-apply",
          "detail": "Re-verified. buildSnapshot's merge (WorkspaceAudioControlBackend.swift:999-1014) carries over volume/mute/pin/boost/targetDevice but NOT routingState or notes, and the per-index loop (1051-1054) resets any non-controller, non-.live app to .monitorOnly with notes=nil. buildSnapshot runs on every backend.refresh() (store.refresh() AppStore.swift:242, the Cmd-R toolbar at MainWindowView.swift:39) and on every autoRestoreDevice() (device-change). So after a real route failure has correctly shown an Error chip, the next refresh (which can be user- or system-triggered with no successful apply) clears the chip and inline reason. Note buildSnapshot also recomputes isRouteRecoveryHealthy = !hasRouteErrors (1089), so the global RouteHealthBadge also flips back to green after the same refresh. Confirmed.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1048-1054",
          "expectedVsActual": "Expected (ROUTE-02): the error note is cleared and state returns to .managed only on the next successful apply or recovery. Actual: a mere refresh with no successful apply clears the Error chip, the inline reason, and the global health badge, falsely implying the route recovered.",
          "suggestedFix": "Preserve previous.routingState==.error and previous.notes across buildSnapshot for apps rediscovered with no active controller, OR re-probe/re-apply the route during rebuild so the error clears only when the route actually recovers.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "tier1:applied:FU-REFRESH-CLEARS-ERRORS",
          "uid": "ROUTE-02#2",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-REFRESH-CLEARS-ERRORS",
          "fixTier": "tier1"
        },
        {
          "severity": "medium",
          "type": "accessibility",
          "title": "\"Needs attention\" rotor under-reports errored apps in non-Running filters",
          "detail": "Re-verified. The rotor filters the view-local `apps` array for routingState==.error (MainWindowView.swift:393-397). `apps` is bound to filteredApps (line 20), which equals scopedApps when not searching; scopedApps is store.visibleApps only for .running, but store.liveApps for .frontmost, store.pinnedApps for .pinned, and store.recentApps for .recent (MainWindowView.swift:102-113). An errored app is not .live, may not be pinned, and recentApps is empty when showRecentApps is off. So on any tab other than Running the rotor omits errored apps outside that scoped subset, defeating the accessibility affordance for VoiceOver users. Confirmed.",
          "codeRef": "Sources/Waves/Features/Mixer/MainWindowView.swift:393-397",
          "expectedVsActual": "Expected (ROUTE-02): the VoiceOver \"Needs attention\" rotor lists exactly the apps in error state. Actual: it lists only errored apps in the current filter's scoped subset, so in Pinned/Live/Recent tabs errored monitor-only apps are unreachable via the rotor.",
          "suggestedFix": "Build the rotor from store.visibleApps.filter { $0.routingState == .error } (all visible apps) rather than the view-scoped `apps`, so it always enumerates every errored app regardless of the active filter. (Severity raised to medium: this is the only programmatic affordance that groups errored apps, so the gap fully blocks VoiceOver users from reaching them in 3 of 4 tabs.)",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "disposition": "tier2:applied:FU-NEEDS-ATTENTION-ROTOR-SCOPE",
          "uid": "ROUTE-02#3",
          "canonicalFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-NEEDS-ATTENTION-ROTOR-SCOPE",
          "fixTier": "tier2"
        },
        {
          "severity": "low",
          "type": "ux",
          "title": "Errored apps have no on-screen failure reason in the menu bar surface",
          "detail": "Re-verified. The inline failure note renders only in the full MixerRowView when routingState==.error (MixerRowView.swift:111-116). CompactMixerRow (menu bar) shows only RoutingStateDot, a red exclamation-triangle for .error with no notes text (483-511). Additionally the menu bar lists only Pinned/Live/Recent CompactSections (MenuBarMixerView.swift:42-50); a non-live errored app falls into recentApps only if showRecentApps is on (recentApps = visibleApps minus live), so with showRecentApps off an errored, unpinned, non-audible app does not appear at all — a menu-bar-only user gets no indication of the failure. Confirmed.",
          "codeRef": "Sources/Waves/Features/Mixer/MixerRowView.swift:483-511",
          "expectedVsActual": "Expected (ROUTE-02 intent): the user understands why volume/mute didn't take effect. Actual: in the menu bar there is no reason text, and with showRecentApps off an errored monitor-only app may not be listed at all.",
          "suggestedFix": "Surface the error reason in the compact row (tooltip/secondary line on the dot) and/or always include errored apps in a menu-bar \"Needs attention\" section independent of showRecentApps.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "disposition": "iter3:implemented",
          "uid": "ROUTE-02#4",
          "canonicalFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "fixStatus": "applied",
          "fixedBy": "iter3-ui-parity",
          "fixTier": "iter3"
        }
      ]
    },
    {
      "id": "ROUTE-03",
      "area": "Route Health & Status (visible/monitored/managed/errored)",
      "feature": "Global route-health badge, status pill, and manual recovery",
      "priority": "P1",
      "userStory": "As a user, I want a single overall routing-health indicator and a one-click way to recover routes, so that I can confirm Waves is healthy and fix transient failures without restarting the app.",
      "expectedBehavior": [
        "RouteHealthBadge in the output summary shows one of three states: \"Ready\" (green checkmark) when backendStatus.isRouteRecoveryHealthy, \"Needs attention\" (red triangle) when not healthy and lastError != nil, else \"Limited\" (secondary) (MainWindowView.swift:479-520).",
        "isRouteRecoveryHealthy is computed as supportsPerAppRouting && capture authorized && no app is in .error state (WorkspaceAudioControlBackend.swift:1061,1089).",
        "A toolbar button \"Recover managed routes\" calls store.recoverRoutes(), which asks the backend to reattach routes, refreshes diagnostics, persists, and shows a success/failure toast (MainWindowView.swift:48-55; AppStore.swift:1181-1202).",
        "On recovery failure the store falls back to the current snapshot and surfaces a \"Recovery failed\" error toast (AppStore.swift:1194-1200).",
        "The badge text/color/icon never relies on color alone — it pairs an SF Symbol and text label, plus a help/accessibility label \"Routing status: <title>\" (MainWindowView.swift:483-490,517-519)."
      ],
      "acceptanceCriteria": [
        "With the audio component installed, permission granted, and no errored apps, the badge reads \"Ready\" in green.",
        "If any app is in error state (or lastError is set), the badge reads \"Needs attention\" in red.",
        "If per-app routing is unsupported/permission missing with no specific error, the badge reads \"Limited\".",
        "Clicking \"Recover managed routes\" reattaches routes and shows a \"Routes recovered\" toast on success or \"Recovery failed\" on error.",
        "The badge conveys its state via icon+text, not color alone."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MainWindowView.swift:479-520",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:48-55",
        "Sources/Waves/Stores/AppStore.swift:1181-1202",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1061",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1086-1091"
      ],
      "implStatus": "implemented",
      "implNotes": "Works and is honest. Edge case: the \"Limited\" branch only renders when isRouteRecoveryHealthy is false AND lastError is nil — but when per-app routing is unsupported (pre-14.2), no per-app apply runs so lastError typically stays nil, correctly yielding \"Limited\". However on a healthy supported system the badge says \"Ready\" even if zero apps are managed, which slightly overstates that routing is doing anything. The badge reflects backend recovery health, not whether any specific route is active. Recovery success toast claims routes were reattached even if there were none to reattach.",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [1H 1M 1L]",
      "errorsFound": "• [HIGH/correctness] Partial recovery/restore reports route health as \"Ready\" despite apps left in Error (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1366) — Re-verified. reattachRoutes sets snapshot.backendStatus.isRouteRecoveryHealthy = recoveredAnyRoute (WorkspaceAudioControlBackend.swift:1366), and recoveredAnyRoute is true if at least one route succeeds (1358), even when other apps fail and are set to .error (1360-1362). reattachRoutes runs AFTER buildSnapshot in both recoverRoutes (214-217) and autoRestoreDevice (231-235), overwriting the correct health (supportsPerAppRouting && authorized && !hasRouteErrors, line 1089). RouteHealthBadge reads isRouteRecoveryHealthy directly (MainWindowView.swift:494), so with one success and one failure the badge shows \"Ready\" green while an app remains errored. Confirmed. | Fix: Compute health as no-errors, not any-success: isRouteRecoveryHealthy = !snapshot.apps.contains { $0.routingState == .error } (still gated on supportsPerAppRouting && authorized), rather than = recoveredAnyRoute.\n• [MEDIUM/correctness] A single successful per-app apply flips the GLOBAL route-health badge to Ready while another app stays errored (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:73-74) — NEW (missed by first tester). On any successful single-app apply, the backend unconditionally sets the GLOBAL snapshot.backendStatus.isRouteRecoveryHealthy = true and lastError = nil: setDesiredVolume (WorkspaceAudioControlBackend.swift:73-74), setMuted (99-100), setVolumeBoost (131-132); setOutputDevice clears lastError on success (734). These never consider whether OTHER apps are in .error. The store merges the new backendStatus via mergeAppState (AppStore.swift:429,525,571) — mergeAppState copies session.backendStatus = backendSession.backendStatus (1429). RouteHealthBadge reads isRouteRecoveryHealthy/lastError directly (MainWindowView.swift:494,497). So if app A is in .error and the user merely nudges app B's volume successfully, the global badge flips to \"Ready\" green and the diagnostics export flag flips to healthy, even though app A is still errored. Distinct from the reattach bug above (different code paths, normal everyday apply path). | Fix: Do not set isRouteRecoveryHealthy=true / lastError=nil unconditionally on per-app success. Recompute global health from the whole snapshot (e.g. isRouteRecoveryHealthy = supportsPerAppRouting && authorized && !snapshot.apps.contains { $0.routingState == .error }) at the end of each apply, and only clear lastError when no app remains errored.\n• [LOW/ux] Recovery success toast claims routes were reattached even when there were none (Sources/Waves/Stores/AppStore.swift:1189-1193) — Re-verified. recoverRoutes calls reattachRoutes only when managedLogicalIDs is non-empty (WorkspaceAudioControlBackend.swift:216), but the store unconditionally shows \"Routes recovered — Managed routing paths were reattached.\" on success (AppStore.swift:1189-1193). With zero managed apps nothing is reattached, yet the toast asserts it was. Confirmed. | Fix: Return a count of reattached routes from recoverRoutes() and tailor the toast (e.g. \"No managed routes to recover\" vs \"Reattached N routes\").",
      "fixStatus": "2 fixed · 1 n/a",
      "fixNotes": "tier1: FU-ROUTE-HEALTH-ANY-SUCCESS",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "correctness",
          "title": "Partial recovery/restore reports route health as \"Ready\" despite apps left in Error",
          "detail": "Re-verified. reattachRoutes sets snapshot.backendStatus.isRouteRecoveryHealthy = recoveredAnyRoute (WorkspaceAudioControlBackend.swift:1366), and recoveredAnyRoute is true if at least one route succeeds (1358), even when other apps fail and are set to .error (1360-1362). reattachRoutes runs AFTER buildSnapshot in both recoverRoutes (214-217) and autoRestoreDevice (231-235), overwriting the correct health (supportsPerAppRouting && authorized && !hasRouteErrors, line 1089). RouteHealthBadge reads isRouteRecoveryHealthy directly (MainWindowView.swift:494), so with one success and one failure the badge shows \"Ready\" green while an app remains errored. Confirmed.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1366",
          "expectedVsActual": "Expected (ROUTE-03): if any app is in error state the badge reads \"Needs attention\" in red. Actual: after a partial recovery/device-change reattach, isRouteRecoveryHealthy is true because at least one route succeeded, so the badge incorrectly reads \"Ready\".",
          "suggestedFix": "Compute health as no-errors, not any-success: isRouteRecoveryHealthy = !snapshot.apps.contains { $0.routingState == .error } (still gated on supportsPerAppRouting && authorized), rather than = recoveredAnyRoute.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "tier1:applied:FU-ROUTE-HEALTH-ANY-SUCCESS",
          "uid": "ROUTE-03#1",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-ROUTE-HEALTH-ANY-SUCCESS",
          "fixTier": "tier1"
        },
        {
          "severity": "medium",
          "type": "correctness",
          "title": "A single successful per-app apply flips the GLOBAL route-health badge to Ready while another app stays errored",
          "detail": "NEW (missed by first tester). On any successful single-app apply, the backend unconditionally sets the GLOBAL snapshot.backendStatus.isRouteRecoveryHealthy = true and lastError = nil: setDesiredVolume (WorkspaceAudioControlBackend.swift:73-74), setMuted (99-100), setVolumeBoost (131-132); setOutputDevice clears lastError on success (734). These never consider whether OTHER apps are in .error. The store merges the new backendStatus via mergeAppState (AppStore.swift:429,525,571) — mergeAppState copies session.backendStatus = backendSession.backendStatus (1429). RouteHealthBadge reads isRouteRecoveryHealthy/lastError directly (MainWindowView.swift:494,497). So if app A is in .error and the user merely nudges app B's volume successfully, the global badge flips to \"Ready\" green and the diagnostics export flag flips to healthy, even though app A is still errored. Distinct from the reattach bug above (different code paths, normal everyday apply path).",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:73-74",
          "expectedVsActual": "Expected (ROUTE-03): the badge reads \"Needs attention\" whenever ANY app is in error state. Actual: a successful apply on one app sets global health true and clears lastError regardless of other apps still in .error, so the badge falsely reads \"Ready\".",
          "suggestedFix": "Do not set isRouteRecoveryHealthy=true / lastError=nil unconditionally on per-app success. Recompute global health from the whole snapshot (e.g. isRouteRecoveryHealthy = supportsPerAppRouting && authorized && !snapshot.apps.contains { $0.routingState == .error }) at the end of each apply, and only clear lastError when no app remains errored.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "tier1:applied:FU-ROUTE-HEALTH-ANY-SUCCESS",
          "uid": "ROUTE-03#2",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-ROUTE-HEALTH-ANY-SUCCESS",
          "fixTier": "tier1"
        },
        {
          "severity": "low",
          "type": "ux",
          "title": "Recovery success toast claims routes were reattached even when there were none",
          "detail": "Re-verified. recoverRoutes calls reattachRoutes only when managedLogicalIDs is non-empty (WorkspaceAudioControlBackend.swift:216), but the store unconditionally shows \"Routes recovered — Managed routing paths were reattached.\" on success (AppStore.swift:1189-1193). With zero managed apps nothing is reattached, yet the toast asserts it was. Confirmed.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1189-1193",
          "expectedVsActual": "Expected: feedback that accurately reflects what happened. Actual: the success toast always claims routes were reattached, overstating the action when there were no managed routes.",
          "suggestedFix": "Return a count of reattached routes from recoverRoutes() and tailor the toast (e.g. \"No managed routes to recover\" vs \"Reattached N routes\").",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "ROUTE-03#3",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "ROUTE-04",
      "area": "Route Health & Status (visible/monitored/managed/errored)",
      "feature": "Live-audio detection: subtitle, level meter, and live grouping",
      "priority": "P1",
      "userStory": "As a user, I want apps that are actively playing audio to be visibly marked as live (label, animated level meter, and grouping), so that I can quickly find and adjust whatever is making sound right now.",
      "expectedBehavior": [
        "The backend marks an app .live when its process family is in the set of currently-audible PIDs (WorkspaceAudioControlBackend.swift:1176-1178).",
        "The row subtitle reads \"Playing audio\" when the app is .live, or when it's .managed, unmuted, and has nonzero peak/RMS level; otherwise \"Frontmost app\" or \"Running app\" (MixerRowView.swift:182-205).",
        "A quiet cyan level-meter capsule is overlaid on managed/live, unmuted, non-excluded rows, width scaled by the live RMS/peak from the visibility-gated poll (MixerRowView.swift:122-134,173-180).",
        "Live levels are polled every 300ms only while a mixer surface is visible (reference-counted begin/endLiveLevels); the poll pulls levels for managed/live apps only (AppStore.swift:616-638; WorkspaceAudioControlBackend.swift:1096-1102).",
        "store.liveApps groups apps that are .live OR (.managed && unmuted && audible), feeding the Live menu-bar section, the \"Frontmost/Live\" filter, the output-summary \"…playing\" summary, and the \"Playing apps\" rotor (AppStore.swift:117-122; MainWindowView.swift:109,389-391,469-476; MenuBarMixerView.swift:46).",
        "The menu-bar icon switches to speaker.wave.3.fill when any app is live (AppStore.swift:149-152)."
      ],
      "acceptanceCriteria": [
        "An app that is currently producing audio shows the \"Playing audio\" subtitle and a moving cyan level meter.",
        "Muted or excluded apps never show the level meter even if managed.",
        "The Live section / Frontmost filter lists exactly the apps detected as live or audibly managed.",
        "Level polling stops (CPU idle) when no mixer surface is on screen and resumes when one opens.",
        "The menu-bar icon reflects a live state with the 3-wave speaker glyph."
      ],
      "codeRefs": [
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1176-1178",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1096-1102",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:122-134",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:173-205",
        "Sources/Waves/Stores/AppStore.swift:117-122",
        "Sources/Waves/Stores/AppStore.swift:616-638"
      ],
      "implStatus": "implemented",
      "implNotes": "Well-built and performance-conscious (visibility-gated polling, no-op redraw guards). One correctness nuance: the \"Playing audio\" subtitle for a managed app depends on peakLevel/rmsLevel stored in the snapshot, while the overlay meter reads store.liveLevels[logicalID]; these can briefly disagree (subtitle says playing while meter is at 0, or vice versa) between the 300ms poll and a snapshot refresh. For a purely .live (unmanaged) app, audioLevels() does include it but the snapshot peak/rms are 0 (set at discovery, line 1190-1191), so a live-but-unmanaged app may show \"Playing audio\" yet a zero-width meter until tapped.",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [2M 1L]",
      "errorsFound": "• [MEDIUM/logic] Menu-bar icon never shows the live 3-wave glyph when any app is muted (Sources/Waves/Stores/AppStore.swift:146-152) — Re-verified. menuBarIconName returns speaker.slash.fill whenever ANY visible app isMuted, checked BEFORE the live check (AppStore.swift:146-152). Muting one background app permanently overrides the live indicator, so even with audio actively playing in another app the icon shows the mute glyph instead of speaker.wave.3.fill. Confirmed. | Fix: Reorder so live takes precedence, or distinguish \"all muted\" from \"some muted\" (e.g. show speaker.wave.3.fill when liveApps is non-empty; fall back to slash only when nothing is live or all live apps are themselves muted).\n• [MEDIUM/edge-case] Live-but-unmanaged apps show \"Playing audio\" with a permanently zero-width level meter (Sources/Waves/Features/Mixer/MixerRowView.swift:177-180) — Re-verified. showsLevelMeter is true for .live apps (MixerRowView.swift:174) and the subtitle reads \"Playing audio\" for .live apps (185-186). But a purely .live (untapped) app has peakLevel/rmsLevel == 0 at discovery (WorkspaceAudioControlBackend.swift:1190-1191), updateAudioLevels only writes levels for apps with an active controller (1393-1394), and audioLevels() returns the stored 0/0 for live apps (1098-1099). So store.liveLevels yields meterLevel 0 (MixerRowView.swift:177-180) and the capsule renders at width 0 and never moves until the app is actually tapped/managed. Confirmed. | Fix: Either suppress the meter for live-unmanaged apps (show only when real levels exist), or source a lightweight level for live-unmanaged apps so the meter reflects actual audio, keeping subtitle and meter consistent.\n• [LOW/ux] Subtitle \"Playing audio\" and the overlay meter can disagree due to two independent level sources (Sources/Waves/Features/Mixer/MixerRowView.swift:182-205) — Re-verified as plausible. For a managed app the \"Playing audio\" subtitle derives from app.peakLevel/app.rmsLevel in the snapshot (MixerRowView.swift:187), updated by the backend's 250ms task (WorkspaceAudioControlBackend.swift:1375) and merged on snapshot reads. The overlay meter reads store.liveLevels[logicalID], updated by the store's separate 300ms poll (AppStore.swift:621). The two update on different cadences and merge points, so transiently the subtitle can say \"Playing audio\" while the meter sits at 0 (or vice versa). This is a brief transient rather than a steady-state defect, hence likely. | Fix: Drive both the subtitle and the meter from a single level source (e.g. compute the \"Playing audio\" predicate from store.liveLevels too), so they cannot diverge.",
      "fixStatus": "3 deferred",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "logic",
          "title": "Menu-bar icon never shows the live 3-wave glyph when any app is muted",
          "detail": "Re-verified. menuBarIconName returns speaker.slash.fill whenever ANY visible app isMuted, checked BEFORE the live check (AppStore.swift:146-152). Muting one background app permanently overrides the live indicator, so even with audio actively playing in another app the icon shows the mute glyph instead of speaker.wave.3.fill. Confirmed.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:146-152",
          "expectedVsActual": "Expected (ROUTE-04): the menu-bar icon reflects a live state with the 3-wave glyph when any app is live. Actual: if any single app is muted, the icon shows the mute glyph and never the live glyph, regardless of other apps playing audio.",
          "suggestedFix": "Reorder so live takes precedence, or distinguish \"all muted\" from \"some muted\" (e.g. show speaker.wave.3.fill when liveApps is non-empty; fall back to slash only when nothing is live or all live apps are themselves muted).",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-user-decision:Conflicting acceptance criteria require a product decision on icon precedence.",
          "uid": "ROUTE-04#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "medium",
          "type": "edge-case",
          "title": "Live-but-unmanaged apps show \"Playing audio\" with a permanently zero-width level meter",
          "detail": "Re-verified. showsLevelMeter is true for .live apps (MixerRowView.swift:174) and the subtitle reads \"Playing audio\" for .live apps (185-186). But a purely .live (untapped) app has peakLevel/rmsLevel == 0 at discovery (WorkspaceAudioControlBackend.swift:1190-1191), updateAudioLevels only writes levels for apps with an active controller (1393-1394), and audioLevels() returns the stored 0/0 for live apps (1098-1099). So store.liveLevels yields meterLevel 0 (MixerRowView.swift:177-180) and the capsule renders at width 0 and never moves until the app is actually tapped/managed. Confirmed.",
          "codeRef": "Sources/Waves/Features/Mixer/MixerRowView.swift:177-180",
          "expectedVsActual": "Expected (ROUTE-04 acceptance): an app producing audio shows the \"Playing audio\" subtitle AND a moving cyan level meter. Actual: a live-but-unmanaged app shows the subtitle but a flat zero-width meter, so the promised \"moving\" meter is absent until the app is managed.",
          "suggestedFix": "Either suppress the meter for live-unmanaged apps (show only when real levels exist), or source a lightweight level for live-unmanaged apps so the meter reflects actual audio, keeping subtitle and meter consistent.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "disposition": "deferred:needs-user-decision:Suppress-meter vs source-a-level is an explicit product fork; the new compact meter simply mirrors the main window's existing behavior, intr",
          "uid": "ROUTE-04#2",
          "canonicalFile": "Sources/Waves/Features/Mixer/MixerRowView.swift"
        },
        {
          "severity": "low",
          "type": "ux",
          "title": "Subtitle \"Playing audio\" and the overlay meter can disagree due to two independent level sources",
          "detail": "Re-verified as plausible. For a managed app the \"Playing audio\" subtitle derives from app.peakLevel/app.rmsLevel in the snapshot (MixerRowView.swift:187), updated by the backend's 250ms task (WorkspaceAudioControlBackend.swift:1375) and merged on snapshot reads. The overlay meter reads store.liveLevels[logicalID], updated by the store's separate 300ms poll (AppStore.swift:621). The two update on different cadences and merge points, so transiently the subtitle can say \"Playing audio\" while the meter sits at 0 (or vice versa). This is a brief transient rather than a steady-state defect, hence likely.",
          "codeRef": "Sources/Waves/Features/Mixer/MixerRowView.swift:182-205",
          "expectedVsActual": "Expected: subtitle and meter consistently reflect the same live state. Actual: brief disagreement between subtitle (snapshot peak/rms) and meter (store.liveLevels) between poll/refresh cycles.",
          "suggestedFix": "Drive both the subtitle and the meter from a single level source (e.g. compute the \"Playing audio\" predicate from store.liveLevels too), so they cannot diverge.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "disposition": "deferred:needs-user-decision:Requires threshold/source choice plus AppStore poll-reorder coordination across files (outside the allowed file) with regression risk.",
          "uid": "ROUTE-04#3",
          "canonicalFile": "Sources/Waves/Features/Mixer/MixerRowView.swift"
        }
      ]
    },
    {
      "id": "ROUTE-05",
      "area": "Route Health & Status (visible/monitored/managed/errored)",
      "feature": "Route-state lifecycle transitions on exclusion, termination, device change, and OS support",
      "priority": "P1",
      "userStory": "As a user, I want route states to update correctly when I exclude an app, when an app quits, when I switch output devices, or when my macOS is too old for per-app routing, so that the displayed status always matches reality and never falsely claims control.",
      "expectedBehavior": [
        "Excluding an app tears down its controllers and resets it to monitorOnly with appliedVolume nil, unmuted, in both store and backend; re-including restores normal discovery (AppStore.swift:686-720; WorkspaceAudioControlBackend.swift:1104-1124).",
        "When an app terminates, its tap is released and managed/live/active rows are immediately demoted to monitorOnly with levels zeroed (AppStore.swift:899-938; WorkspaceAudioControlBackend.swift:1117-1123).",
        "On output-device change with auto-restore on, the backend re-establishes managed routes before emitting the event and the store reads the fresh snapshot (no double-restore) (AppStore.swift:792-818,820-846).",
        "If a route is rebuilt for a managed app on device change/output retarget and fails, it goes to .error with notes; if it never had a managed route, only the preference is recorded (WorkspaceAudioControlBackend.swift:710-741).",
        "When per-app routing is unsupported (pre-macOS 14.2), every app is forced to monitorOnly with note \"Per-app route requires macOS 14.2+\" and compatibility .planned (WorkspaceAudioControlBackend.swift:1036-1042).",
        "Retained-but-not-rediscovered apps that still have an active controller keep .managed; otherwise drop to monitorOnly (WorkspaceAudioControlBackend.swift:1016-1034)."
      ],
      "acceptanceCriteria": [
        "Excluding a managed app immediately changes its chip away from Managed and stops tapping its audio.",
        "Quitting a managed app demotes its row to monitored and zeroes its meter without waiting for a full refresh.",
        "Switching output devices with auto-restore keeps previously-managed apps managed (or marks them Error if reattach fails) rather than silently dropping control.",
        "On macOS older than 14.2, all apps show monitor-only with a \"requires macOS 14.2+\" note and never claim Managed.",
        "Re-including a previously excluded app makes it eligible for management again."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:686-720",
        "Sources/Waves/Stores/AppStore.swift:899-938",
        "Sources/Waves/Stores/AppStore.swift:792-846",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1016-1042",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:710-741"
      ],
      "implStatus": "implemented",
      "implNotes": "Transitions are carefully handled across all four lifecycle paths and consistently zero out levels/appliedVolume to avoid stale 'managed' claims. Note: the pre-14.2 note \"Per-app route requires macOS 14.2+\" is set on the app but is only rendered in the row when routingState==.error (MixerRowView.swift:111) — since these apps are monitorOnly, that explanatory note is never shown to the user in the row (it does reach diagnostics export). That's a small UX gap against DESIGN.md's promise that disabled/limited controls explain why control is limited. The slider/mute help text does generically say 'Move the slider… to start per-app control', which doesn't mention the OS requirement.",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [1H 2L]",
      "errorsFound": "• [HIGH/correctness] Backend auto-restores routes on device change even when the user disabled auto-restore, and the UI then goes stale (Sources/Waves/Stores/AppStore.swift:792-818) — Re-verified. The backend's handleDeviceChange unconditionally calls autoRestoreDevice() before yielding the event (WorkspaceAudioControlBackend.swift:1462-1473) — it consults no user preference; autoRestoreDevice disposes all controllers and rebuilds/reattaches managed routes (223-238). The store's handleDeviceChange is gated by guard preferences.autoRestoreDevice else { return } (AppStore.swift:792-793). So with auto-restore OFF: (1) the backend still tears down and rebuilds managed routes against the user's intent, and (2) the store ignores the event and never reads the new snapshot, leaving session.currentDevice and all per-app states stale (wrong device name, stale managed/error chips) until some other refresh. Confirmed. | Fix: Pass the autoRestoreDevice preference into the backend so it skips reattach when disabled; at minimum always refresh the store's session/device on the device-change event and only skip the route-reattach behavior — never leave the UI stale when auto-restore is off.\n• [LOW/inconsistency] Choosing an output device for a live-but-unmanaged app forces it into managed routing (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:725-732) — Re-verified. setOutputDevice treats wasManaged = (routingState == .managed || routingState == .live) (WorkspaceAudioControlBackend.swift:726) and, when true, calls applyRoute which creates a tap/controller and sets the app to .managed (729-733). A .live app has never had a managed route (no controller exists at discovery), yet picking a device for it silently enrolls it in managed routing rather than merely recording the preference. Note the doc's own ROUTE-05 expectedBehavior says \"if it never had a managed route, only the preference is recorded\" — so this is a genuine code-vs-intent mismatch. Confirmed. | Fix: Define wasManaged strictly by an existing managed route (controllers[id]?.isActive == true || routingState == .managed); for purely .live apps just record targetDeviceUID and defer route creation until the user actually manages the app.\n• [LOW/ux] Pre-14.2 \"requires macOS 14.2+\" note is never shown in the row (Sources/Waves/Features/Mixer/MixerRowView.swift:111-116) — Re-verified. When per-app routing is unsupported, buildSnapshot sets routingState=.monitorOnly with notes=\"Per-app route requires macOS 14.2+\" and compatibility=.planned (WorkspaceAudioControlBackend.swift:1037-1041). But the row renders notes only when routingState==.error (MixerRowView.swift:111). Since these apps are .monitorOnly, the explanatory note never appears in the UI (it only reaches diagnostics export). The slider/mute help text generically says \"Move the slider… to start per-app control\" (MixerRowView.swift:474) / \"Adjust to enroll…\" (211-215) and never mentions the OS requirement. Confirmed. | Fix: Render app.notes for monitorOnly apps too (or specifically when compatibility==.planned), or show the OS-requirement message in the slider/mute help text when supportsPerAppRouting is false.",
      "fixStatus": "2 deferred · 1 n/a",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "correctness",
          "title": "Backend auto-restores routes on device change even when the user disabled auto-restore, and the UI then goes stale",
          "detail": "Re-verified. The backend's handleDeviceChange unconditionally calls autoRestoreDevice() before yielding the event (WorkspaceAudioControlBackend.swift:1462-1473) — it consults no user preference; autoRestoreDevice disposes all controllers and rebuilds/reattaches managed routes (223-238). The store's handleDeviceChange is gated by guard preferences.autoRestoreDevice else { return } (AppStore.swift:792-793). So with auto-restore OFF: (1) the backend still tears down and rebuilds managed routes against the user's intent, and (2) the store ignores the event and never reads the new snapshot, leaving session.currentDevice and all per-app states stale (wrong device name, stale managed/error chips) until some other refresh. Confirmed.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:792-818",
          "expectedVsActual": "Expected (ROUTE-05): on output-device change WITH auto-restore on, the backend re-establishes routes and the store reads the fresh snapshot. Actual: the backend re-establishes routes regardless of the preference, and when the preference is off the store skips the refresh entirely, so displayed device and route states no longer match reality.",
          "suggestedFix": "Pass the autoRestoreDevice preference into the backend so it skips reattach when disabled; at minimum always refresh the store's session/device on the device-change event and only skip the route-reattach behavior — never leave the UI stale when auto-restore is off.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-hardware:Requires plumbing the preference into the backend actor (cross-file protocol change) and validating real device-change route reattachment on hardware.",
          "uid": "ROUTE-05#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "Choosing an output device for a live-but-unmanaged app forces it into managed routing",
          "detail": "Re-verified. setOutputDevice treats wasManaged = (routingState == .managed || routingState == .live) (WorkspaceAudioControlBackend.swift:726) and, when true, calls applyRoute which creates a tap/controller and sets the app to .managed (729-733). A .live app has never had a managed route (no controller exists at discovery), yet picking a device for it silently enrolls it in managed routing rather than merely recording the preference. Note the doc's own ROUTE-05 expectedBehavior says \"if it never had a managed route, only the preference is recorded\" — so this is a genuine code-vs-intent mismatch. Confirmed.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:725-732",
          "expectedVsActual": "Expected (ROUTE-05): if an app never had a managed route, only the preference is recorded on output retarget. Actual: a .live (unmanaged, untapped) app is treated as managed and gets a new tap created, transitioning live->managed as a side effect of choosing a device.",
          "suggestedFix": "Define wasManaged strictly by an existing managed route (controllers[id]?.isActive == true || routingState == .managed); for purely .live apps just record targetDeviceUID and defer route creation until the user actually manages the app.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "deferred:needs-user-decision:Narrowing wasManaged to exclude .live changes user-visible routing semantics (live->managed side effect) and the documented behavior; flipping it is a product c",
          "uid": "ROUTE-05#2",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift"
        },
        {
          "severity": "low",
          "type": "ux",
          "title": "Pre-14.2 \"requires macOS 14.2+\" note is never shown in the row",
          "detail": "Re-verified. When per-app routing is unsupported, buildSnapshot sets routingState=.monitorOnly with notes=\"Per-app route requires macOS 14.2+\" and compatibility=.planned (WorkspaceAudioControlBackend.swift:1037-1041). But the row renders notes only when routingState==.error (MixerRowView.swift:111). Since these apps are .monitorOnly, the explanatory note never appears in the UI (it only reaches diagnostics export). The slider/mute help text generically says \"Move the slider… to start per-app control\" (MixerRowView.swift:474) / \"Adjust to enroll…\" (211-215) and never mentions the OS requirement. Confirmed.",
          "codeRef": "Sources/Waves/Features/Mixer/MixerRowView.swift:111-116",
          "expectedVsActual": "Expected (DESIGN intent): when control is limited, the UI explains why. Actual: on pre-14.2 the row shows a chip-less monitor-only app with no visible reason; the \"requires macOS 14.2+\" note is suppressed because notes render only for .error.",
          "suggestedFix": "Render app.notes for monitorOnly apps too (or specifically when compatibility==.planned), or show the OS-requirement message in the slider/mute help text when supportsPerAppRouting is false.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "ROUTE-05#3",
          "canonicalFile": "Sources/Waves/Features/Mixer/MixerRowView.swift"
        }
      ]
    },
    {
      "id": "ROUTE-06",
      "area": "Route Health & Status (visible/monitored/managed/errored)",
      "feature": "Diagnostics export and system-status panel",
      "priority": "P2",
      "userStory": "As a user filing a bug report or verifying setup, I want an in-app diagnostics view and a copyable text export of route health and per-app state, so that I can confirm what's wrong and share it without exposing audio content.",
      "expectedBehavior": [
        "diagnosticsExportText builds a plain-text report: macOS version, output device, audio-component-installed, capture-permission, route-recovery-healthy, last error, then every visible app's name, route-state displayName, volume %, mute, and boost, plus diagnostic checks (AppStore.swift:725-751).",
        "copyDiagnosticsToPasteboard writes that text to the general pasteboard and toasts \"Diagnostics copied\" (AppStore.swift:774-779).",
        "The DiagnosticsPanel disclosure group shows the diagnostics summary and each check with a colored status dot (passed=green, warning=orange, failed=red, info=secondary) plus a non-color VoiceOver label like \"Failed: <title>\" (MainWindowView.swift:522-589).",
        "Diagnostics are refreshed after start, device change, recovery, and on demand (AppStore.swift:215,807,1186,1204-1208).",
        "BackendStatus carries isAudioComponentInstalled, hasRequiredPermissions, isRouteRecoveryHealthy, lastError, all surfaced in the export (AudioSessionSnapshot.swift:45-63)."
      ],
      "acceptanceCriteria": [
        "Copying diagnostics places a human-readable report containing OS, device, permission/component/route-health flags, and per-app route states on the clipboard.",
        "The diagnostics panel lists each check with a status dot and an accessible status label that doesn't depend on color.",
        "The export contains no audio data — only app names already visible in the mixer.",
        "Diagnostics refresh after a recovery or device change so the report reflects current health."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:722-779",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:522-589",
        "Sources/WavesAudioCore/Models/AudioSessionSnapshot.swift:45-63",
        "Sources/Waves/Stores/AppStore.swift:1204-1208"
      ],
      "implStatus": "implemented",
      "implNotes": "Complete and matches the README's 'in-app diagnostics export' claim. The export honestly reports component/permission/route health and per-app route states. Minor: the DiagnosticsPanel relies on store.diagnostics being non-nil; if a backend diagnosticsReport() ever fails to populate, the disclosure body renders empty with no fallback message. The copy/export entry point's discoverability (which toolbar/menu invokes copyDiagnosticsToPasteboard) lives outside the assigned files; the function itself is wired correctly.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [2L]",
      "errorsFound": "• [LOW/ux] Diagnostics disclosure renders an empty body when diagnostics are nil (Sources/Waves/Features/Mixer/MainWindowView.swift:530-557) — Re-verified. DiagnosticsPanel's disclosure content is wrapped in if let diagnostics = store.diagnostics with no else branch (MainWindowView.swift:531-557). If diagnostics has not populated yet (e.g. expanded before start() completes, or if diagnosticsReport ever fails to set it), expanding the disclosure shows a blank body with no loading/unavailable message, and nothing triggers a refresh on expand. Confirmed. | Fix: Add an else branch rendering a \"Diagnostics not available yet\" placeholder, and/or trigger refreshDiagnostics() on expand, so the panel is never blank.\n• [LOW/correctness] Diagnostics export reflects stale route health after a partial recovery or a single successful apply (Sources/Waves/Stores/AppStore.swift:734) — Re-verified. diagnosticsExportText reports \"Route recovery healthy: <status.isRouteRecoveryHealthy>\" (AppStore.swift:734) and then lists each app's routingState.displayName (741). Because (a) reattachRoutes sets isRouteRecoveryHealthy = recoveredAnyRoute (WorkspaceAudioControlBackend.swift:1366, ROUTE-03 issue 1) and (b) every single-app success path sets it = true unconditionally (73-74/99-100/131-132, ROUTE-03 NEW issue), a snapshot with some apps still in .error can export \"Route recovery healthy: true\" while listing those apps as \"Error\" in the per-app section. The exported report is internally contradictory. Confirmed. | Fix: Fix the isRouteRecoveryHealthy computation (see ROUTE-03 issues) so the exported flag is derived from the absence of any .error app, keeping it consistent with the per-app states.",
      "fixStatus": "1 fixed · 1 n/a",
      "fixNotes": "tier3: FU-DIAGNOSTICS-EMPTY-FALLBACK",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "ux",
          "title": "Diagnostics disclosure renders an empty body when diagnostics are nil",
          "detail": "Re-verified. DiagnosticsPanel's disclosure content is wrapped in if let diagnostics = store.diagnostics with no else branch (MainWindowView.swift:531-557). If diagnostics has not populated yet (e.g. expanded before start() completes, or if diagnosticsReport ever fails to set it), expanding the disclosure shows a blank body with no loading/unavailable message, and nothing triggers a refresh on expand. Confirmed.",
          "codeRef": "Sources/Waves/Features/Mixer/MainWindowView.swift:530-557",
          "expectedVsActual": "Expected: the diagnostics panel always lists checks with status dots/labels. Actual: when store.diagnostics is nil the expanded disclosure is empty with no fallback text.",
          "suggestedFix": "Add an else branch rendering a \"Diagnostics not available yet\" placeholder, and/or trigger refreshDiagnostics() on expand, so the panel is never blank.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "disposition": "tier3:applied:FU-DIAGNOSTICS-EMPTY-FALLBACK",
          "uid": "ROUTE-06#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-DIAGNOSTICS-EMPTY-FALLBACK",
          "fixTier": "tier3"
        },
        {
          "severity": "low",
          "type": "correctness",
          "title": "Diagnostics export reflects stale route health after a partial recovery or a single successful apply",
          "detail": "Re-verified. diagnosticsExportText reports \"Route recovery healthy: <status.isRouteRecoveryHealthy>\" (AppStore.swift:734) and then lists each app's routingState.displayName (741). Because (a) reattachRoutes sets isRouteRecoveryHealthy = recoveredAnyRoute (WorkspaceAudioControlBackend.swift:1366, ROUTE-03 issue 1) and (b) every single-app success path sets it = true unconditionally (73-74/99-100/131-132, ROUTE-03 NEW issue), a snapshot with some apps still in .error can export \"Route recovery healthy: true\" while listing those apps as \"Error\" in the per-app section. The exported report is internally contradictory. Confirmed.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:734",
          "expectedVsActual": "Expected: the exported route-health flag matches the per-app states. Actual: after a partial reattach or a one-app success the export can say route recovery is healthy while listing apps in Error.",
          "suggestedFix": "Fix the isRouteRecoveryHealthy computation (see ROUTE-03 issues) so the exported flag is derived from the absence of any .error app, keeping it consistent with the per-app states.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "ROUTE-06#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "OUT-01",
      "area": "Per-App Output Device Routing",
      "feature": "Pin an app to a specific output device",
      "priority": "P0",
      "userStory": "As a Mac user running several audio apps, I want to send one app's sound to a specific output device (e.g. my headphones) while everything else stays on the system default, so that I can hear different apps on different speakers without changing my global output.",
      "expectedBehavior": [
        "Each managed (non-excluded) mixer row has a context-menu submenu 'Output Device' listing 'System Default' plus every available output device (MixerRowView.swift:140-156).",
        "The currently chosen target shows a checkmark: 'System Default' is checked when app.targetDeviceUID == nil, otherwise the matching device row is checked (MixerRowView.swift:142-154).",
        "Selecting a device calls store.setOutputDevice(device, for: app), which sets session.apps[index].targetDeviceUID = device.id, invalidates the visible-apps cache, then calls backend.setOutputDevice(uid:forAppID:) (AppStore.swift:649-661).",
        "In the backend, the app's targetDeviceUID is recorded and any existing tap controller is torn down so the route can be rebuilt on the new device; if the app was already managed/live the route is re-applied via applyRoute, otherwise only the preference is stored for next time (WorkspaceAudioControlBackend.swift:710-741).",
        "When a managed route is (re)built, the per-app aggregate device is created with the chosen device UID as its main sub-device and clock device, so the tapped audio physically plays out the selected device (WorkspaceAudioControlBackend.swift:436-491).",
        "On success a toast 'Output set — AppName → DeviceName' is shown; on failure a toast 'Couldn't route AppName' with the error is shown and the app's state is re-merged from the backend (AppStore.swift:662-672).",
        "The choice is persisted via persistSessionSnapshot() so it survives relaunch (AppStore.swift:661)."
      ],
      "acceptanceCriteria": [
        "Opening the context menu on a managed app shows 'System Default' plus all enumerated output devices, with exactly one checkmark on the active choice.",
        "Picking a non-default device on a currently-managed app moves its audible output to that device and the route stays in the managed state.",
        "app.targetDeviceUID equals the selected device's UID after selection and nil after choosing 'System Default'.",
        "A success toast naming the app and device appears within ~1.5s on success; an error toast appears on failure.",
        "After quitting and relaunching, the app is still pinned to the previously chosen device."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MixerRowView.swift:139-157",
        "Sources/Waves/Stores/AppStore.swift:649-674",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:710-741",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:431-491",
        "Sources/WavesAudioCore/Models/AudioApp.swift:30"
      ],
      "implStatus": "partial",
      "implNotes": "The core path is fully wired and physically routes via the aggregate device's main sub-device. Gap: when the app is NOT yet managed (routingState == .monitorOnly), backend.setOutputDevice returns early after only recording the preference (no route is built, WorkspaceAudioControlBackend.swift:723-727), yet AppStore still shows a success toast 'Output set — App → Device' (AppStore.swift:662-667). The user is told the output was set even though nothing audibly changed until they later move the slider/mute to enroll the app — misleading UX. Also setOutputDevice does not call refreshOutputDevices() first, so the menu relies on a possibly-stale availableDevices list captured at start()/onAppear.",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [1H 2M]",
      "errorsFound": "• [HIGH/ux] Success toast 'Output set' shown for monitor-only apps even though no route is built (Sources/Waves/Stores/AppStore.swift:662-667) — For any app whose routingState is .monitorOnly (the common case for a running-but-not-yet-enrolled app), backend.setOutputDevice records snapshot.apps[index].targetDeviceUID then returns early at `guard wasManaged else { return }` (WorkspaceAudioControlBackend.swift:725-727) without building a route or throwing. AppStore's do-block therefore succeeds and unconditionally shows 'Output set — AppName → DeviceName' (AppStore.swift:662-667). Re-verified: wasManaged is only .managed/.live; a Ready app is .monitorOnly so the guard returns. Audio keeps playing to the system default until the user later enrolls the app via slider/mute. | Fix: Have backend.setOutputDevice return whether a route was actually (re)built, or have AppStore inspect the merged app.routingState after the call; when still .monitorOnly, show an informational 'Output saved — applies when AppName is adjusted/starts playing' instead of a success 'Output set'.\n• [MEDIUM/edge-case] Stale device list: per-app submenu never refreshes and there is no device add/remove listener (Sources/Waves/Features/Mixer/MixerRowView.swift:140-156) — availableDevices is populated only on backend start (AppStore.swift:216), the menu-bar picker's onAppear (MenuBarMixerView.swift:156), and on a default-device change. The Core Audio listener is registered only on kAudioHardwarePropertyDefaultOutputDevice (WorkspaceAudioControlBackend.swift:1410-1414); there is no listener on kAudioHardwarePropertyDevices, and the per-app Output Device contextMenu in MixerRowView (lines 140-156) has no .onAppear/.task calling store.refreshOutputDevices(). Plugging in a device after launch without making it the default leaves it absent from the per-app submenu until a default change or relaunch. | Fix: Add a kAudioHardwarePropertyDevices listener that yields a refresh, and/or call store.refreshOutputDevices() when the per-app Output Device menu appears, mirroring the menu-bar picker's onAppear.\n• [MEDIUM/ux] No checkmark shown when the pinned device is missing from availableDevices (Sources/Waves/Features/Mixer/MixerRowView.swift:141-155) — The checkmark logic checks app.targetDeviceUID == nil for 'System Default' (MixerRowView.swift:144) and app.targetDeviceUID == device.id per row (line 152). When targetDeviceUID != nil but that device is absent from store.availableDevices (unplugged, or list stale before refresh), neither 'System Default' nor any device row is checked, so zero checkmarks appear. | Fix: When targetDeviceUID is non-nil but absent from availableDevices, surface the saved device as a disabled/checked '<name> (unavailable)' row using store.targetDevice(for:) fallback, so the active-but-missing choice is still indicated.",
      "fixStatus": "1 fixed · 2 deferred",
      "fixNotes": "tier1: FU-OUTPUT-SET-TOAST",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "ux",
          "title": "Success toast 'Output set' shown for monitor-only apps even though no route is built",
          "detail": "For any app whose routingState is .monitorOnly (the common case for a running-but-not-yet-enrolled app), backend.setOutputDevice records snapshot.apps[index].targetDeviceUID then returns early at `guard wasManaged else { return }` (WorkspaceAudioControlBackend.swift:725-727) without building a route or throwing. AppStore's do-block therefore succeeds and unconditionally shows 'Output set — AppName → DeviceName' (AppStore.swift:662-667). Re-verified: wasManaged is only .managed/.live; a Ready app is .monitorOnly so the guard returns. Audio keeps playing to the system default until the user later enrolls the app via slider/mute.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:662-667",
          "expectedVsActual": "Expected: the toast tells the truth — either route now or tell the user the choice will apply when the app is enrolled. Actual: 'Output set' is shown even though nothing audibly changed.",
          "suggestedFix": "Have backend.setOutputDevice return whether a route was actually (re)built, or have AppStore inspect the merged app.routingState after the call; when still .monitorOnly, show an informational 'Output saved — applies when AppName is adjusted/starts playing' instead of a success 'Output set'.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:FU-OUTPUT-SET-TOAST",
          "uid": "OUT-01#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-OUTPUT-SET-TOAST",
          "fixTier": "tier1"
        },
        {
          "severity": "medium",
          "type": "edge-case",
          "title": "Stale device list: per-app submenu never refreshes and there is no device add/remove listener",
          "detail": "availableDevices is populated only on backend start (AppStore.swift:216), the menu-bar picker's onAppear (MenuBarMixerView.swift:156), and on a default-device change. The Core Audio listener is registered only on kAudioHardwarePropertyDefaultOutputDevice (WorkspaceAudioControlBackend.swift:1410-1414); there is no listener on kAudioHardwarePropertyDevices, and the per-app Output Device contextMenu in MixerRowView (lines 140-156) has no .onAppear/.task calling store.refreshOutputDevices(). Plugging in a device after launch without making it the default leaves it absent from the per-app submenu until a default change or relaunch.",
          "codeRef": "Sources/Waves/Features/Mixer/MixerRowView.swift:140-156",
          "expectedVsActual": "Expected: the routing menu offers the real set of currently-attached output devices. Actual: a device attached after launch (without a default change) never appears until a default change or relaunch.",
          "suggestedFix": "Add a kAudioHardwarePropertyDevices listener that yields a refresh, and/or call store.refreshOutputDevices() when the per-app Output Device menu appears, mirroring the menu-bar picker's onAppear.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "disposition": "deferred:needs-hardware:Needs a new kAudioHardwarePropertyDevices listener and on-device hardware validation; cross-file backend change.",
          "uid": "OUT-01#2",
          "canonicalFile": "Sources/Waves/Features/Mixer/MixerRowView.swift"
        },
        {
          "severity": "medium",
          "type": "ux",
          "title": "No checkmark shown when the pinned device is missing from availableDevices",
          "detail": "The checkmark logic checks app.targetDeviceUID == nil for 'System Default' (MixerRowView.swift:144) and app.targetDeviceUID == device.id per row (line 152). When targetDeviceUID != nil but that device is absent from store.availableDevices (unplugged, or list stale before refresh), neither 'System Default' nor any device row is checked, so zero checkmarks appear.",
          "codeRef": "Sources/Waves/Features/Mixer/MixerRowView.swift:141-155",
          "expectedVsActual": "Expected (acceptance criterion): exactly one checkmark on the active choice. Actual: zero checkmarks, so the user cannot tell what the app is routed to.",
          "suggestedFix": "When targetDeviceUID is non-nil but absent from availableDevices, surface the saved device as a disabled/checked '<name> (unavailable)' row using store.targetDevice(for:) fallback, so the active-but-missing choice is still indicated.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "disposition": "deferred:needs-feature:targetDevice(for:) returns nil when the device is absent, so the saved device name is not cheaply derivable without new persisted device-nam",
          "uid": "OUT-01#3",
          "canonicalFile": "Sources/Waves/Features/Mixer/MixerRowView.swift"
        }
      ]
    },
    {
      "id": "OUT-02",
      "area": "Per-App Output Device Routing",
      "feature": "Reset an app back to the system default output",
      "priority": "P1",
      "userStory": "As a user who previously pinned an app to a specific device, I want to send it back to following the system default output, so that it tracks whatever device I'm using without me having to re-pick it.",
      "expectedBehavior": [
        "The 'Output Device' submenu always offers a 'System Default' entry at the top (MixerRowView.swift:141-145).",
        "Choosing 'System Default' calls store.setOutputDevice(nil, for: app), clearing targetDeviceUID to nil (MixerRowView.swift:142, AppStore.swift:653).",
        "With targetDeviceUID == nil, a (re)built route uses currentDefaultOutputDeviceUID() as the aggregate's output, so the app follows the live system default (WorkspaceAudioControlBackend.swift:443-444).",
        "The row subtitle stops showing the '→ Device' suffix once targetDeviceUID is nil (MixerRowView.swift:200-202).",
        "A success toast 'Output set — AppName → System default' is shown (AppStore.swift:663-664)."
      ],
      "acceptanceCriteria": [
        "Selecting 'System Default' sets app.targetDeviceUID to nil and the checkmark moves to 'System Default'.",
        "A previously-pinned app then plays out whatever the current system default device is.",
        "The '→ DeviceName' subtitle suffix disappears after reset.",
        "Success toast reads '… → System default'."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MixerRowView.swift:141-145",
        "Sources/Waves/Stores/AppStore.swift:649-664",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:443-444",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:200-202"
      ],
      "implStatus": "implemented",
      "implNotes": "Clean and correct. Same monitor-only caveat as OUT-01 applies (toast can claim success when no live route exists yet), but for the reset-to-default case that is low-impact.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "OUT-03",
      "area": "Per-App Output Device Routing",
      "feature": "Enumerate selectable output devices (empty/error states)",
      "priority": "P1",
      "userStory": "As a user, I want Waves to show me the real set of output devices currently attached to my Mac, so that the routing menus only offer destinations that actually exist.",
      "expectedBehavior": [
        "backend.availableOutputDevices() walks all Core Audio devices, keeps only those with output streams, reads each UID/name, classifies a DeviceKind, and returns them sorted case-insensitively by name (WorkspaceAudioControlBackend.swift:661-682, 766-775, 823-841).",
        "Waves' own private aggregate devices are filtered out so they never appear as user-selectable outputs: UIDs prefixed 'com.waves.aggregate.' are skipped, and any aggregate named containing 'waves' is skipped (WorkspaceAudioControlBackend.swift:667-672).",
        "The current default device is flagged via isCurrent (uid == currentUID) (WorkspaceAudioControlBackend.swift:663,677).",
        "On macOS older than 14.2 the function returns an empty list because supportsPerAppRouting is false (WorkspaceAudioControlBackend.swift:662, 310-316).",
        "availableDevices is populated on backend start (AppStore.swift:216), refreshed by refreshOutputDevices() on the menu-bar picker's onAppear (AppStore.swift:753-756, MenuBarMixerView.swift:156), and after a device change (AppStore.swift:808).",
        "The per-app routing menu shows a Divider only when availableDevices is non-empty so an empty list collapses to just 'System Default' (MixerRowView.swift:147); the menu-bar system picker shows 'No output devices found' when the list is empty (MenuBarMixerView.swift:136-138)."
      ],
      "acceptanceCriteria": [
        "The device list contains exactly the attached output-capable devices, sorted by name.",
        "No device whose UID begins with 'com.waves.aggregate.' and no Waves-named aggregate ever appears in either menu.",
        "On macOS < 14.2 both routing menus show no real devices (per-app menu shows only 'System Default'; system picker shows the empty-state text).",
        "The device matching the current system default is reported with isCurrent == true."
      ],
      "codeRefs": [
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:661-682",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:823-841",
        "Sources/Waves/Stores/AppStore.swift:216",
        "Sources/Waves/Stores/AppStore.swift:753-757",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:147-148",
        "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:136-138"
      ],
      "implStatus": "implemented",
      "implNotes": "Enumeration, filtering of Waves' own aggregates, sorting, and empty/old-OS states are all handled. Minor concern: the per-app submenu in MixerRowView has no explicit 'No output devices found' message (it just shows 'System Default' with no Divider), whereas the menu-bar picker does — slightly inconsistent empty-state messaging. availableDevices can be momentarily stale because the per-app menu does not trigger a refresh on open the way the menu-bar picker does.",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [1M 2L]",
      "errorsFound": "• [MEDIUM/edge-case] availableOutputDevices can be stale because no device-arrival listener exists (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1405-1435) — availableOutputDevices() correctly enumerates, filters, sorts and flags isCurrent (WorkspaceAudioControlBackend.swift:661-682), but results are only re-read on backend start, default-device change, and the menu-bar picker onAppear. There is no Core Audio listener on kAudioHardwarePropertyDevices (only kAudioHardwarePropertyDefaultOutputDevice at line 1410-1414), so hot-plug/unplug that does not change the default never refreshes availableDevices, and MixerRowView never calls refreshOutputDevices() on open. | Fix: Register an AudioObjectAddPropertyListenerBlock on kAudioHardwarePropertyDevices that yields a deviceChangeEvents tick (or refreshes), so availableDevices tracks hot-plug events.\n• [LOW/dead-code] Secondary 'aggregate named waves' filter is unreachable for Waves' own aggregates (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:672) — availableOutputDevices() skips Waves aggregates by UID prefix (line 669) then, as a second guard, skips devices where kind == .aggregate && name contains 'waves' (line 672). But deviceKind() checks token.contains(\"aggregate\")/\"multi-output\" (line 831) BEFORE token.contains(\"waves\") (line 834); a 'Waves-AppName' device's token has 'waves' but not 'aggregate', so it is classified .virtual, never .aggregate. The line-672 guard is therefore ineffective; only the line-669 UID-prefix check actually filters Waves aggregates. Harmless (UID-prefix check already covers them), hence low. | Fix: Make the name guard kind-independent (skip when name.lowercased().contains(\"waves\")) or reorder deviceKind so the aggregate check precedes the virtual/'waves' check.\n• [LOW/inconsistency] Per-app submenu has no empty-state text, unlike the menu-bar picker (Sources/Waves/Features/Mixer/MixerRowView.swift:147-155) — The menu-bar OutputDevicePicker shows 'No output devices found' when availableDevices is empty (MenuBarMixerView.swift:136-138). The per-app Output Device submenu only renders 'System Default' (no Divider, no message) when the list is empty (MixerRowView.swift:147-155), so on macOS < 14.2 or before the first refresh the user gets no indication real devices are unavailable. | Fix: Add a disabled 'No output devices found' (or 'Requires macOS 14.2+') row in the per-app submenu when store.availableDevices.isEmpty, mirroring the menu-bar picker.",
      "fixStatus": "2 fixed · 1 deferred",
      "fixNotes": "tier3: FU-DEAD-WAVES-AGGREGATE-NAME-GUARD",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "edge-case",
          "title": "availableOutputDevices can be stale because no device-arrival listener exists",
          "detail": "availableOutputDevices() correctly enumerates, filters, sorts and flags isCurrent (WorkspaceAudioControlBackend.swift:661-682), but results are only re-read on backend start, default-device change, and the menu-bar picker onAppear. There is no Core Audio listener on kAudioHardwarePropertyDevices (only kAudioHardwarePropertyDefaultOutputDevice at line 1410-1414), so hot-plug/unplug that does not change the default never refreshes availableDevices, and MixerRowView never calls refreshOutputDevices() on open.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1405-1435",
          "expectedVsActual": "Expected (acceptance criterion): 'The device list contains exactly the attached output-capable devices.' Actual: the cached list can omit a just-attached device or still list a just-removed one until some other event refreshes it.",
          "suggestedFix": "Register an AudioObjectAddPropertyListenerBlock on kAudioHardwarePropertyDevices that yields a deviceChangeEvents tick (or refreshes), so availableDevices tracks hot-plug events.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "deferred:needs-hardware:Registering a new Core Audio device listener broadens the device-change surface and must be validated against real hot-plug/unplug events; not a localized stati",
          "uid": "OUT-03#1",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift"
        },
        {
          "severity": "low",
          "type": "dead-code",
          "title": "Secondary 'aggregate named waves' filter is unreachable for Waves' own aggregates",
          "detail": "availableOutputDevices() skips Waves aggregates by UID prefix (line 669) then, as a second guard, skips devices where kind == .aggregate && name contains 'waves' (line 672). But deviceKind() checks token.contains(\"aggregate\")/\"multi-output\" (line 831) BEFORE token.contains(\"waves\") (line 834); a 'Waves-AppName' device's token has 'waves' but not 'aggregate', so it is classified .virtual, never .aggregate. The line-672 guard is therefore ineffective; only the line-669 UID-prefix check actually filters Waves aggregates. Harmless (UID-prefix check already covers them), hence low.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:672",
          "expectedVsActual": "Expected: a Waves aggregate lacking the com.waves.aggregate. UID prefix would still be filtered by the name check. Actual: it is classified .virtual and slips past the kind==.aggregate guard.",
          "suggestedFix": "Make the name guard kind-independent (skip when name.lowercased().contains(\"waves\")) or reorder deviceKind so the aggregate check precedes the virtual/'waves' check.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "tier3:applied:FU-DEAD-WAVES-AGGREGATE-NAME-GUARD",
          "uid": "OUT-03#2",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-DEAD-WAVES-AGGREGATE-NAME-GUARD",
          "fixTier": "tier3"
        },
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "Per-app submenu has no empty-state text, unlike the menu-bar picker",
          "detail": "The menu-bar OutputDevicePicker shows 'No output devices found' when availableDevices is empty (MenuBarMixerView.swift:136-138). The per-app Output Device submenu only renders 'System Default' (no Divider, no message) when the list is empty (MixerRowView.swift:147-155), so on macOS < 14.2 or before the first refresh the user gets no indication real devices are unavailable.",
          "codeRef": "Sources/Waves/Features/Mixer/MixerRowView.swift:147-155",
          "expectedVsActual": "Expected: consistent empty-state messaging across both routing menus. Actual: the per-app menu silently collapses to just 'System Default'.",
          "suggestedFix": "Add a disabled 'No output devices found' (or 'Requires macOS 14.2+') row in the per-app submenu when store.availableDevices.isEmpty, mirroring the menu-bar picker.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "disposition": "iter2:fixed",
          "uid": "OUT-03#3",
          "canonicalFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "fixStatus": "applied",
          "fixedBy": "iter2-deferred-reduction",
          "fixTier": "iter2"
        }
      ]
    },
    {
      "id": "OUT-04",
      "area": "Per-App Output Device Routing",
      "feature": "Honest failure when a pinned device disappears",
      "priority": "P1",
      "userStory": "As a user who pinned an app to a device that later gets unplugged, I want Waves to tell me the route failed instead of silently moving the audio somewhere else, so that I'm never surprised by sound coming out of the wrong speaker.",
      "expectedBehavior": [
        "When building a managed route for an app with a targetDeviceUID, the backend checks isDeviceAvailable(uid:) and, if the device is gone, throws managedRouteUnavailable with the message 'The chosen output device for AppName is unavailable. Pick another in the app's Output Device menu.' rather than falling back to the default (WorkspaceAudioControlBackend.swift:436-441).",
        "isDeviceAvailable scans all current device IDs for a matching UID (WorkspaceAudioControlBackend.swift:743-745).",
        "On that throw, setOutputDevice marks the app routingState = .error, stores the error text in notes and backendStatus.lastError, and rethrows (WorkspaceAudioControlBackend.swift:735-740).",
        "The mixer row renders the error: the RoutingStateIndicator chip turns red with an exclamation symbol and 'Route setup failed.' help text, and the app.notes string is shown beneath the row (MixerRowView.swift:111-116, 452-480).",
        "In the AppStore the error surfaces as a 'Couldn't route AppName' toast (AppStore.swift:668-671)."
      ],
      "acceptanceCriteria": [
        "Pinning to a device and then removing that device causes the app's route to enter the .error state, not a silent fallback to the default device.",
        "The error message names the app and tells the user to pick another device.",
        "The row shows a red route-state chip and the failure note text.",
        "No audio is rerouted to an unexpected device on failure."
      ],
      "codeRefs": [
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:431-445",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:735-745",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:111-116",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:452-480",
        "Sources/Waves/Stores/AppStore.swift:668-671"
      ],
      "implStatus": "implemented",
      "implNotes": "Deliberately fails honestly with a clear, actionable message and a visible error state — matches PRODUCT.md's 'never overpromise' principle. Note: this honest-failure check only fires when a route is actively (re)built; a pinned-but-monitor-only app whose device is gone shows no error until enrolled. The recovery/auto-restore path (reattachRoutes) also depends on this same throw to flag the failure.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1M]",
      "errorsFound": "• [MEDIUM/error-handling] Pinned-but-monitor-only app whose device is gone reports false success instead of an error (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:723-727) — The honest-failure check (isDeviceAvailable -> managedRouteUnavailable) only fires inside createController when a route is actively (re)built (WorkspaceAudioControlBackend.swift:436-441). In setOutputDevice a .monitorOnly app returns early at line 727 before any route build, so pinning to a currently-unavailable device produces no throw and AppStore shows the 'Output set' success toast (AppStore.swift:662-667). The .error state and the actionable 'pick another device' message only appear later when the app is enrolled. Re-verified against the early-return at line 727 and the error path at lines 436-441. | Fix: In setOutputDevice, when uid is non-nil, run isDeviceAvailable(uid:) on the monitor-only early-return path and throw managedRouteUnavailable (or return a 'saved but unavailable' status) so the UX is honest before enrollment.",
      "fixStatus": "1 deferred",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "error-handling",
          "title": "Pinned-but-monitor-only app whose device is gone reports false success instead of an error",
          "detail": "The honest-failure check (isDeviceAvailable -> managedRouteUnavailable) only fires inside createController when a route is actively (re)built (WorkspaceAudioControlBackend.swift:436-441). In setOutputDevice a .monitorOnly app returns early at line 727 before any route build, so pinning to a currently-unavailable device produces no throw and AppStore shows the 'Output set' success toast (AppStore.swift:662-667). The .error state and the actionable 'pick another device' message only appear later when the app is enrolled. Re-verified against the early-return at line 727 and the error path at lines 436-441.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:723-727",
          "expectedVsActual": "Expected (user story): the user is told the route failed rather than being surprised. Actual: for a not-yet-managed app, selecting an unavailable device yields a success toast and no error chip until later enrollment.",
          "suggestedFix": "In setOutputDevice, when uid is non-nil, run isDeviceAvailable(uid:) on the monitor-only early-return path and throw managedRouteUnavailable (or return a 'saved but unavailable' status) so the UX is honest before enrollment.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "deferred:needs-user-decision:Throwing on the monitor-only path changes the documented 'just record the preference' contract and the store's success-toast handling; the right behavior is a p",
          "uid": "OUT-04#1",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift"
        }
      ]
    },
    {
      "id": "OUT-05",
      "area": "Per-App Output Device Routing",
      "feature": "Show which device each app is routed to",
      "priority": "P2",
      "userStory": "As a user scanning the mixer, I want each pinned app's row to display its destination device, so that I can tell at a glance where its audio is going without opening a menu.",
      "expectedBehavior": [
        "When app.targetDeviceUID is non-nil, the mixer row subtitle appends '→ DeviceName' (MixerRowView.swift:200-202).",
        "The device name comes from store.targetDevice(for: app), which looks up the device in availableDevices by UID; if not found it falls back to the literal 'Custom output' (AppStore.swift:642-645, MixerRowView.swift:201).",
        "The suffix is joined with other subtitle parts (playing/frontmost/category) by commas (MixerRowView.swift:182-204)."
      ],
      "acceptanceCriteria": [
        "A pinned app's row shows '→ <device name>' in its subtitle.",
        "If the pinned device's name can't be resolved from the current device list, the row shows '→ Custom output' rather than an empty arrow.",
        "An unpinned app's row shows no '→' suffix."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MixerRowView.swift:182-204",
        "Sources/Waves/Stores/AppStore.swift:642-645"
      ],
      "implStatus": "implemented",
      "implNotes": "Works and degrades gracefully to 'Custom output'. Because targetDevice(for:) resolves against availableDevices, a stale or empty device list (e.g. before refresh, or a disconnected device) will display 'Custom output' for a real pinned device — acceptable fallback but it hides the actual device name in that window.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "OUT-06",
      "area": "Per-App Output Device Routing",
      "feature": "Pinned routes survive device changes and relaunch",
      "priority": "P2",
      "userStory": "As a user, I want my per-app output pins to be re-established automatically after I switch the system output device or restart Waves, so that my carefully arranged routing isn't lost.",
      "expectedBehavior": [
        "On a default-device change the backend re-establishes managed routes before emitting deviceChangeEvents, and the store reads the fresh snapshot rather than restoring again (AppStore.swift:792-808, WorkspaceAudioControlBackend.swift:1462-1473).",
        "On relaunch, reapplyRestoredAudioState treats a non-nil targetDeviceUID as 'customized' and re-establishes the app's route so it plays to the chosen device immediately, even at default volume (AppStore.swift:827-839).",
        "targetDeviceUID is preserved across snapshot merges (WorkspaceAudioControlBackend.swift:1012, AppStore.swift:1404) and is Codable so it round-trips through the persisted session (AudioApp.swift:115,140,163,189).",
        "Auto-restore is gated by the autoRestoreDevice preference; if disabled, handleDeviceChange returns early (AppStore.swift:792-793)."
      ],
      "acceptanceCriteria": [
        "Switching the system default output device while an app is pinned to a different device leaves that app on its pinned device (its route is rebuilt, not dropped).",
        "Relaunching Waves re-applies a pinned app's route to its saved device without the user re-selecting it.",
        "targetDeviceUID is present in the persisted/decoded session and survives snapshot merges.",
        "With Auto-restore disabled, no automatic re-establishment toast/flow runs on device change."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:792-818",
        "Sources/Waves/Stores/AppStore.swift:820-846",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1462-1473",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1012",
        "Sources/WavesAudioCore/Models/AudioApp.swift:115-189"
      ],
      "implStatus": "partial",
      "implNotes": "Persistence and relaunch re-application of pinned routes are solidly wired. Concern: when the system DEFAULT device changes, the backend's autoRestoreDevice/reattachRoutes path rebuilds routes for managed apps, but a pin to a still-present non-default device should remain unchanged — that is correct. However, if a pinned app's specific device is the one that just disappeared, recovery will throw managedRouteUnavailable (OUT-04) and the app lands in .error with no automatic re-pin prompt; the user must manually re-pick. Also reapplyRestoredAudioState's re-establishment is skipped for excluded apps and silently swallows per-app errors (AppStore.swift:840-843), so a relaunch failure to re-pin is only visible via route state, not a toast.",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [1C 2M]",
      "errorsFound": "• [CRITICAL/correctness] After relaunch a pinned app is NOT routed to its saved device — it silently plays to the system default while the UI claims it is pinned/managed (Sources/Waves/Stores/AppStore.swift:832-839) — Re-verified the full path. On relaunch the persisted targetDeviceUID is merged only into the STORE session via mergedSession (AppStore.swift:1404). The injected backend's own snapshot is never seeded with the persisted session: backend.start() calls buildSnapshot(merging: snapshot) where the backend's prior snapshot is empty, and buildSnapshot only carries targetDeviceUID from previousByLogicalID (WorkspaceAudioControlBackend.swift:1012) — i.e. from the backend's own (empty) snapshot, so every backend app has targetDeviceUID == nil. reapplyRestoredAudioState then re-establishes routes via backend.setVolumeBoost/setMuted/setDesiredVolume (AppStore.swift:833-835) — never backend.setOutputDevice. Those methods call applyRoute(for: snapshot.apps[index]) on the BACKEND entry (targetDeviceUID == nil) -> createController routes to currentDefaultOutputDeviceUID() (WorkspaceAudioControlBackend.swift:443-444). Meanwhile AppStore sets routingState = .managed (AppStore.swift:838) and the row subtitle shows '→ DeviceName' from the store's retained targetDeviceUID (MixerRowView.swift:200-201). Trigger: pin an app to a non-default device, quit, relaunch with that app running — its audio comes out of the system default while the UI claims it is pinned and Managed. | Fix: In reapplyRestoredAudioState, when app.targetDeviceUID != nil, call backend.setOutputDevice(uid: app.targetDeviceUID, forAppID:) (after enrolling), or seed the backend snapshot's targetDeviceUID before start/reapply so applyRoute honors the saved device.\n• [MEDIUM/error-handling] Relaunch re-pin failures are swallowed — only logged at debug, never surfaced (Sources/Waves/Stores/AppStore.swift:840-843) — reapplyRestoredAudioState wraps each app's re-establishment in do/catch that only logs at debug and leaves saved state untouched (AppStore.swift:840-843). If re-establishing a pinned route fails (saved device gone, or createController throws managedRouteUnavailable), there is no toast, and because the catch is taken before line 838 the store's routingState is not set to .error for that app — the failure is effectively silent. Note: the backend DID set its own snapshot entry to .error inside setDesiredVolume/setMuted, but the store never merges that backend state on the catch path, so the store keeps the stale .recent/.monitorOnly state. | Fix: On catch, merge the backend's resulting state (which carries .error + notes) into the store for that app and surface at least one aggregated toast like 'Some pinned routes could not be restored' so the failure is discoverable.\n• [MEDIUM/inconsistency] Auto-restore disabled still rebuilds backend routes and leaves the store snapshot/device name stale (state desync) (Sources/Waves/Stores/AppStore.swift:792-799) — MISSED BY FIRST TESTER. The autoRestoreDevice user preference is checked ONLY in AppStore.handleDeviceChange (AppStore.swift:793 `guard preferences.autoRestoreDevice else { return }`). The backend's own handleDeviceChange (WorkspaceAudioControlBackend.swift:1462-1474) unconditionally calls autoRestoreDevice(), which disposeControllers(keeping: []) and reattachRoutes(...) for every managed app (lines 230-235) regardless of the user's preference. Consequences when auto-restore is OFF: (a) acceptance criterion 4 ('no automatic re-establishment runs on device change') is violated — the backend tears down and rebuilds all managed routes anyway; (b) because the store returns at line 793 before `session = await backend.currentSnapshot()` (line 799) and before refreshing availableDevices/currentDevice, the store keeps the PRE-change snapshot — so the main-window/menu-bar output-device name (currentDeviceName) and per-app routing/error/level state remain stale and desynced from the backend until some other event refreshes them. | Fix: Plumb the autoRestoreDevice preference into the backend so it can skip the dispose/reattach when disabled, and/or always update the store's currentDevice + availableDevices on a device-change event (refresh the lightweight snapshot) even when full auto-restore is off, so the UI never shows a stale output device.",
      "fixStatus": "3 fixed",
      "fixNotes": "tier1: FU-DEVICECHANGE-GATING; FU-RELAUNCH-PINNED-ROUTE",
      "retestStatus": "PASS (remediated & verified)",
      "issues": [
        {
          "severity": "critical",
          "type": "correctness",
          "title": "After relaunch a pinned app is NOT routed to its saved device — it silently plays to the system default while the UI claims it is pinned/managed",
          "detail": "Re-verified the full path. On relaunch the persisted targetDeviceUID is merged only into the STORE session via mergedSession (AppStore.swift:1404). The injected backend's own snapshot is never seeded with the persisted session: backend.start() calls buildSnapshot(merging: snapshot) where the backend's prior snapshot is empty, and buildSnapshot only carries targetDeviceUID from previousByLogicalID (WorkspaceAudioControlBackend.swift:1012) — i.e. from the backend's own (empty) snapshot, so every backend app has targetDeviceUID == nil. reapplyRestoredAudioState then re-establishes routes via backend.setVolumeBoost/setMuted/setDesiredVolume (AppStore.swift:833-835) — never backend.setOutputDevice. Those methods call applyRoute(for: snapshot.apps[index]) on the BACKEND entry (targetDeviceUID == nil) -> createController routes to currentDefaultOutputDeviceUID() (WorkspaceAudioControlBackend.swift:443-444). Meanwhile AppStore sets routingState = .managed (AppStore.swift:838) and the row subtitle shows '→ DeviceName' from the store's retained targetDeviceUID (MixerRowView.swift:200-201). Trigger: pin an app to a non-default device, quit, relaunch with that app running — its audio comes out of the system default while the UI claims it is pinned and Managed.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:832-839",
          "expectedVsActual": "Expected (acceptance criterion 2): 'Relaunching Waves re-applies a pinned app's route to its saved device without the user re-selecting it.' Actual: the route is rebuilt to the system default; the UI falsely shows the app pinned/Managed. Also violates OUT-04's promise of never silently routing to an unexpected device.",
          "suggestedFix": "In reapplyRestoredAudioState, when app.targetDeviceUID != nil, call backend.setOutputDevice(uid: app.targetDeviceUID, forAppID:) (after enrolling), or seed the backend snapshot's targetDeviceUID before start/reapply so applyRoute honors the saved device.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:FU-RELAUNCH-PINNED-ROUTE",
          "uid": "OUT-06#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-RELAUNCH-PINNED-ROUTE",
          "fixTier": "tier1"
        },
        {
          "severity": "medium",
          "type": "error-handling",
          "title": "Relaunch re-pin failures are swallowed — only logged at debug, never surfaced",
          "detail": "reapplyRestoredAudioState wraps each app's re-establishment in do/catch that only logs at debug and leaves saved state untouched (AppStore.swift:840-843). If re-establishing a pinned route fails (saved device gone, or createController throws managedRouteUnavailable), there is no toast, and because the catch is taken before line 838 the store's routingState is not set to .error for that app — the failure is effectively silent. Note: the backend DID set its own snapshot entry to .error inside setDesiredVolume/setMuted, but the store never merges that backend state on the catch path, so the store keeps the stale .recent/.monitorOnly state.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:840-843",
          "expectedVsActual": "Expected: a relaunch failure to re-pin is communicated (toast and/or a visible .error chip with the actionable message). Actual: it is only logged at debug; the user gets no notification and no error chip.",
          "suggestedFix": "On catch, merge the backend's resulting state (which carries .error + notes) into the store for that app and surface at least one aggregated toast like 'Some pinned routes could not be restored' so the failure is discoverable.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "gapfill:fixed",
          "uid": "OUT-06#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "gapfill",
          "fixTier": "gapfill"
        },
        {
          "severity": "medium",
          "type": "inconsistency",
          "title": "Auto-restore disabled still rebuilds backend routes and leaves the store snapshot/device name stale (state desync)",
          "detail": "MISSED BY FIRST TESTER. The autoRestoreDevice user preference is checked ONLY in AppStore.handleDeviceChange (AppStore.swift:793 `guard preferences.autoRestoreDevice else { return }`). The backend's own handleDeviceChange (WorkspaceAudioControlBackend.swift:1462-1474) unconditionally calls autoRestoreDevice(), which disposeControllers(keeping: []) and reattachRoutes(...) for every managed app (lines 230-235) regardless of the user's preference. Consequences when auto-restore is OFF: (a) acceptance criterion 4 ('no automatic re-establishment runs on device change') is violated — the backend tears down and rebuilds all managed routes anyway; (b) because the store returns at line 793 before `session = await backend.currentSnapshot()` (line 799) and before refreshing availableDevices/currentDevice, the store keeps the PRE-change snapshot — so the main-window/menu-bar output-device name (currentDeviceName) and per-app routing/error/level state remain stale and desynced from the backend until some other event refreshes them.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:792-799",
          "expectedVsActual": "Expected (acceptance criterion 4): with Auto-restore disabled, no automatic re-establishment flow runs and the UI is consistent. Actual: the backend still rebuilds every route, and the store's device name and per-app state go stale because the change event is ignored.",
          "suggestedFix": "Plumb the autoRestoreDevice preference into the backend so it can skip the dispose/reattach when disabled, and/or always update the store's currentDevice + availableDevices on a device-change event (refresh the lightweight snapshot) even when full auto-restore is off, so the UI never shows a stale output device.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:FU-DEVICECHANGE-GATING",
          "uid": "OUT-06#3",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-DEVICECHANGE-GATING",
          "fixTier": "tier1"
        }
      ]
    },
    {
      "id": "DEV-01",
      "area": "Output Device Management & Auto-Restore",
      "feature": "System output device switching",
      "priority": "P1",
      "userStory": "As a menu-bar user, I want to switch the macOS system output device from inside Waves, so that I can move all audio to headphones/speakers without opening System Settings or Control Center.",
      "expectedBehavior": [
        "The menu-bar panel shows an output-device picker (speaker icon + current device name) that lists every available output device",
        "Selecting a device that is not already current calls backend.setDefaultOutputDevice(uid:) which sets kAudioHardwarePropertyDefaultOutputDevice on the system object",
        "Selecting the already-current device is a no-op (guard device.id != currentDeviceID)",
        "On success a 'Output switched' toast is shown and the available-device list is refreshed so the checkmark moves to the new device",
        "The current device is marked with a checkmark; currentDeviceName falls back to 'No output device' when session.currentDevice is nil",
        "If the chosen device disappeared between enumeration and selection, setDefaultOutputDevice throws managedRouteUnavailable('That output device is no longer available.') and a 'Couldn't switch output' error toast is shown"
      ],
      "acceptanceCriteria": [
        "Picking a different device in the menu-bar picker changes the macOS default output device system-wide",
        "Picking the current device performs no Core Audio write",
        "A success toast appears with the device name on a successful switch and the checkmark updates immediately",
        "An error toast appears when the device is no longer available and no crash occurs",
        "currentDeviceName shows 'No output device' when there is no current device"
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:759",
        "Sources/Waves/Stores/AppStore.swift:130",
        "Sources/Waves/Stores/AppStore.swift:139",
        "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:120",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:684"
      ],
      "implStatus": "implemented",
      "implNotes": "Fully wired through real Core Audio. Minor UX gap: selectOutputDevice does not optimistically update currentDeviceID; the checkmark only moves after the device-change listener fires and the snapshot refreshes, so there can be a brief lag. The main-window header (MainWindowView.swift:442) shows currentDeviceName read-only and offers no switcher, so switching is only available from the menu bar.",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [1H 2L]",
      "errorsFound": "• [HIGH/logic] Checkmark never moves after a manual output switch when 'Auto-restore device' is off (Sources/Waves/Stores/AppStore.swift:759) — Verified: selectOutputDevice (AppStore.swift:759-772) calls backend.setDefaultOutputDevice then refreshes only availableDevices; it never writes session.currentDevice. The picker's checkmark is bound to device.id == store.currentDeviceID (MenuBarMixerView.swift:129), and currentDeviceID derives from session.currentDevice?.id (AppStore.swift:130-131). The only post-switch update to session.currentDevice is the store's handleDeviceChange, which early-returns when preferences.autoRestoreDevice is false (AppStore.swift:793). Note the refreshed availableDevices list DOES carry correct isCurrent flags (backend sets isCurrent: uid==currentUID at WorkspaceAudioControlBackend.swift:677), but the picker ignores isCurrent and uses currentDeviceID, so the checkmark stays on the old device until an unrelated action (a volume change → mergeAppState, or refresh) updates session.currentDevice. | Fix: Either bind the picker checkmark to device.isCurrent (already populated on the freshly fetched availableDevices) instead of currentDeviceID, or optimistically set session.currentDevice in selectOutputDevice after a successful setDefaultOutputDevice. Decouple session.currentDevice refresh from the autoRestoreDevice gate.\n• [LOW/ux] Manual switch produces two overlapping toasts (Sources/Waves/Stores/AppStore.swift:767) — Verified: selectOutputDevice shows an 'Output switched' success toast (AppStore.swift:767). The Core Audio default-device listener then fires the backend handleDeviceChange → deviceChangeEvents → store handleDeviceChange, which (when auto-restore is on) shows a second 'Output device changed' info toast (AppStore.swift:811-816) for the same user action. A user who switches output with auto-restore on sees two toasts in quick succession. | Fix: Suppress the handleDeviceChange info toast when the change was self-initiated (set a short-lived flag in selectOutputDevice that handleDeviceChange checks), or skip the info toast when the new device equals the one just selected.\n• [LOW/ux] Brief checkmark/name lag on switch even when auto-restore is on (Sources/Waves/Stores/AppStore.swift:760) — Verified: selectOutputDevice does not optimistically update session.currentDevice; with auto-restore on, the checkmark and currentDeviceName only move once the async device-change listener fires and handleDeviceChange replaces session via currentSnapshot(). This is a visible lag between click and the UI updating even on the normal path. (This is the implNotes-acknowledged minor gap for DEV-01.) | Fix: Optimistically update session.currentDevice in selectOutputDevice to the selected device immediately after the backend write succeeds.",
      "fixStatus": "1 fixed · 2 n/a",
      "fixNotes": "",
      "retestStatus": "PASS (remediated & verified)",
      "issues": [
        {
          "severity": "high",
          "type": "logic",
          "title": "Checkmark never moves after a manual output switch when 'Auto-restore device' is off",
          "detail": "Verified: selectOutputDevice (AppStore.swift:759-772) calls backend.setDefaultOutputDevice then refreshes only availableDevices; it never writes session.currentDevice. The picker's checkmark is bound to device.id == store.currentDeviceID (MenuBarMixerView.swift:129), and currentDeviceID derives from session.currentDevice?.id (AppStore.swift:130-131). The only post-switch update to session.currentDevice is the store's handleDeviceChange, which early-returns when preferences.autoRestoreDevice is false (AppStore.swift:793). Note the refreshed availableDevices list DOES carry correct isCurrent flags (backend sets isCurrent: uid==currentUID at WorkspaceAudioControlBackend.swift:677), but the picker ignores isCurrent and uses currentDeviceID, so the checkmark stays on the old device until an unrelated action (a volume change → mergeAppState, or refresh) updates session.currentDevice.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:759",
          "expectedVsActual": "Expected (acceptance): after a successful switch the checkmark updates immediately. Actual: with auto-restore off, session.currentDevice is never refreshed by the switch, so currentDeviceID/currentDeviceName stay stale and the checkmark stays on the previous device.",
          "suggestedFix": "Either bind the picker checkmark to device.isCurrent (already populated on the freshly fetched availableDevices) instead of currentDeviceID, or optimistically set session.currentDevice in selectOutputDevice after a successful setDefaultOutputDevice. Decouple session.currentDevice refresh from the autoRestoreDevice gate.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:verified-in-code",
          "uid": "DEV-01#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-DEVICECHANGE-GATING/FU-MENU-SECTION-DEDUP",
          "fixTier": "tier1"
        },
        {
          "severity": "low",
          "type": "ux",
          "title": "Manual switch produces two overlapping toasts",
          "detail": "Verified: selectOutputDevice shows an 'Output switched' success toast (AppStore.swift:767). The Core Audio default-device listener then fires the backend handleDeviceChange → deviceChangeEvents → store handleDeviceChange, which (when auto-restore is on) shows a second 'Output device changed' info toast (AppStore.swift:811-816) for the same user action. A user who switches output with auto-restore on sees two toasts in quick succession.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:767",
          "expectedVsActual": "Expected: one feedback toast per user-initiated switch. Actual: 'Output switched' immediately followed by 'Output device changed' for the same action (auto-restore on).",
          "suggestedFix": "Suppress the handleDeviceChange info toast when the change was self-initiated (set a short-lived flag in selectOutputDevice that handleDeviceChange checks), or skip the info toast when the new device equals the one just selected.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "DEV-01#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "low",
          "type": "ux",
          "title": "Brief checkmark/name lag on switch even when auto-restore is on",
          "detail": "Verified: selectOutputDevice does not optimistically update session.currentDevice; with auto-restore on, the checkmark and currentDeviceName only move once the async device-change listener fires and handleDeviceChange replaces session via currentSnapshot(). This is a visible lag between click and the UI updating even on the normal path. (This is the implNotes-acknowledged minor gap for DEV-01.)",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:760",
          "expectedVsActual": "Expected: checkmark updates immediately on selection. Actual: it updates only after the async listener round-trip completes.",
          "suggestedFix": "Optimistically update session.currentDevice in selectOutputDevice to the selected device immediately after the backend write succeeds.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "DEV-01#3",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "DEV-02",
      "area": "Output Device Management & Auto-Restore",
      "feature": "Available output device enumeration",
      "priority": "P1",
      "userStory": "As a user, I want Waves to show the real set of output devices on my Mac, so that the picker only offers devices I can actually route audio to.",
      "expectedBehavior": [
        "availableOutputDevices enumerates all Core Audio devices and keeps only those with output streams (hasOutputStreams)",
        "Waves' own private aggregate devices are excluded: any UID prefixed 'com.waves.aggregate.' and any aggregate whose name contains 'waves' is skipped",
        "Each device is classified into a DeviceKind (builtInOutput/bluetooth/display/virtual/aggregate/unknown) via keyword matching on UID+name",
        "Devices are returned sorted case-insensitively by name and the current device is flagged isCurrent",
        "On macOS older than 14.2 (no per-app routing support) the list is returned empty",
        "The list is refreshed on menu-bar appearance (onAppear), at startup, after switching device, and on device-change events",
        "When availableDevices is empty the picker shows 'No output devices found'"
      ],
      "acceptanceCriteria": [
        "Only devices that have output streams appear in the picker",
        "Waves-created aggregate devices never appear as selectable outputs",
        "The list is alphabetized and the current device is marked",
        "On an unsupported OS version the picker is empty and shows the empty-state text",
        "Refreshing returns an up-to-date device list after hardware is plugged/unplugged"
      ],
      "codeRefs": [
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:661",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:766",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:823",
        "Sources/Waves/Stores/AppStore.swift:753",
        "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:136"
      ],
      "implStatus": "implemented",
      "implNotes": "Solid. deviceKind classification is heuristic string-matching (e.g. 'usb-c' is mapped to .display, 'bluetooth/airpods/beats' to .bluetooth) so kind labels can be wrong for some devices, but kind only affects an icon/label, not routing correctness. recentDeviceIDs is computed and persisted (buildSnapshot:1075) via Array(Set(...)).sorted(), which discards recency order, and it is never surfaced in any UI — effectively dead data.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1M 1L]",
      "errorsFound": "• [MEDIUM/inconsistency] Device list is not refreshed on device-change events when 'Auto-restore device' is off (Sources/Waves/Stores/AppStore.swift:808) — Verified: the only device-change-triggered refresh of availableDevices is inside the store's handleDeviceChange (AppStore.swift:808), which early-returns at line 793 when preferences.autoRestoreDevice is false. With auto-restore disabled, plugging/unplugging hardware does not refresh availableDevices until the menu-bar picker's onAppear re-runs refreshOutputDevices (MenuBarMixerView.swift:156) or another action triggers a refresh. The main-window header (MainWindowView.swift:442) offers no picker and never triggers a device refresh. The expectedBehavior explicitly lists 'on device-change events' as a refresh trigger. | Fix: Move the availableDevices (and session.currentDevice) refresh out of the autoRestoreDevice-gated branch so the device list always tracks hardware changes regardless of the auto-restore preference.\n• [LOW/dead-code] Name-based Waves-aggregate exclusion guard is unreachable (and doubly moot) (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:672) — Verified: the name guard at WorkspaceAudioControlBackend.swift:672 only fires when kind == .aggregate. Waves names its aggregates 'Waves-<app>' (line 472) with UID 'com.waves.aggregate.<uuid>' (line 473) and no 'aggregate'/'multi-output' token, so deviceKind classifies them as .virtual (the 'waves' token at line 834 is reached before any aggregate match), never .aggregate — so the kind==.aggregate name guard cannot match a real Waves aggregate. Severity is low/cosmetic, not just because the UID-prefix guard at line 669 already excludes them, but ALSO because these aggregates are created with kAudioAggregateDeviceIsPrivateKey: true (line 476), so they are not even enumerated by allDeviceIDs() in the first place. The guard is dead but causes no functional gap. | Fix: Drop the kind==.aggregate condition and skip any device whose name contains 'waves', or remove the misleading name guard entirely and rely on the UID prefix, so the documented behavior matches the code.",
      "fixStatus": "2 fixed",
      "fixNotes": "tier1: FU-DEVICECHANGE-GATING | tier3: FU-DEAD-WAVES-AGGREGATE-NAME-GUARD",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "inconsistency",
          "title": "Device list is not refreshed on device-change events when 'Auto-restore device' is off",
          "detail": "Verified: the only device-change-triggered refresh of availableDevices is inside the store's handleDeviceChange (AppStore.swift:808), which early-returns at line 793 when preferences.autoRestoreDevice is false. With auto-restore disabled, plugging/unplugging hardware does not refresh availableDevices until the menu-bar picker's onAppear re-runs refreshOutputDevices (MenuBarMixerView.swift:156) or another action triggers a refresh. The main-window header (MainWindowView.swift:442) offers no picker and never triggers a device refresh. The expectedBehavior explicitly lists 'on device-change events' as a refresh trigger.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:808",
          "expectedVsActual": "Expected: the device list updates after hardware plug/unplug via device-change events. Actual: with auto-restore off, the store ignores device-change events, so cached availableDevices can be stale until the menu is reopened.",
          "suggestedFix": "Move the availableDevices (and session.currentDevice) refresh out of the autoRestoreDevice-gated branch so the device list always tracks hardware changes regardless of the auto-restore preference.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:FU-DEVICECHANGE-GATING",
          "uid": "DEV-02#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-DEVICECHANGE-GATING",
          "fixTier": "tier1"
        },
        {
          "severity": "low",
          "type": "dead-code",
          "title": "Name-based Waves-aggregate exclusion guard is unreachable (and doubly moot)",
          "detail": "Verified: the name guard at WorkspaceAudioControlBackend.swift:672 only fires when kind == .aggregate. Waves names its aggregates 'Waves-<app>' (line 472) with UID 'com.waves.aggregate.<uuid>' (line 473) and no 'aggregate'/'multi-output' token, so deviceKind classifies them as .virtual (the 'waves' token at line 834 is reached before any aggregate match), never .aggregate — so the kind==.aggregate name guard cannot match a real Waves aggregate. Severity is low/cosmetic, not just because the UID-prefix guard at line 669 already excludes them, but ALSO because these aggregates are created with kAudioAggregateDeviceIsPrivateKey: true (line 476), so they are not even enumerated by allDeviceIDs() in the first place. The guard is dead but causes no functional gap.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:672",
          "expectedVsActual": "Expected (per story): any aggregate whose name contains 'waves' is skipped. Actual: Waves aggregates classify as .virtual not .aggregate, so the guard never matches; exclusion is fully covered by the UID prefix and the private-device flag.",
          "suggestedFix": "Drop the kind==.aggregate condition and skip any device whose name contains 'waves', or remove the misleading name guard entirely and rely on the UID prefix, so the documented behavior matches the code.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "tier3:applied:FU-DEAD-WAVES-AGGREGATE-NAME-GUARD",
          "uid": "DEV-02#2",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-DEAD-WAVES-AGGREGATE-NAME-GUARD",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "DEV-03",
      "area": "Output Device Management & Auto-Restore",
      "feature": "Per-app output device routing (pinning)",
      "priority": "P2",
      "userStory": "As a power user, I want to pin an individual app to a specific output device, so that, for example, music plays to my speakers while a call stays on my headset.",
      "expectedBehavior": [
        "A per-app 'Output Device' submenu in the mixer row lists 'System Default' plus every available device, with a checkmark on the current target",
        "Choosing a device sets app.targetDeviceUID and, if the app is already managed/live, tears down the existing controller and rebuilds the route against the pinned device's aggregate",
        "Choosing 'System Default' clears targetDeviceUID so the app follows the system default output again",
        "If the app is not currently managed, the device preference is recorded for next time it is managed (wasManaged guard) without forcing a route",
        "The pinned device choice is persisted in the session snapshot and reflected in the row subtitle ('→ <device name>')",
        "Excluded apps cannot have an output device set (guard !isExcluded) and the submenu is hidden for excluded apps",
        "On success an 'Output set' toast shows 'app → device/System default'; on failure a 'Couldn't route' error toast is shown",
        "If the pinned device is unavailable when building the route, createController throws managedRouteUnavailable('The chosen output device for <app> is unavailable...') and the row goes to .error state rather than silently falling back to default"
      ],
      "acceptanceCriteria": [
        "Pinning a running, managed app to a device moves only that app's audio to the chosen device",
        "Selecting 'System Default' returns the app to following the system output",
        "Pinning a non-managed app records the preference without error and applies it when the app later becomes managed",
        "The pinned device persists across the session and shows in the row subtitle",
        "Excluded apps offer no output-device menu",
        "A pinned device that becomes unavailable puts the app into an error state with an actionable message, not a silent default fallback"
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:649",
        "Sources/Waves/Stores/AppStore.swift:642",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:710",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:435",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:140",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:200"
      ],
      "implStatus": "implemented",
      "implNotes": "Routing logic is complete and honest about unavailable devices. Edge case: a pinned-device route is built from the app's own aggregate whose main sub-device is the pinned UID, so it does NOT follow the system default — which is the intent. However, when a pinned device is unplugged there is no automatic re-pin or fallback; the route just errors until the user re-picks (createController:437). A pinned app's preference survives in-session but is keyed by logicalID in the snapshot, not in the per-device presets store, so it relies on session persistence to outlive a relaunch.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "DEV-04",
      "area": "Output Device Management & Auto-Restore",
      "feature": "Auto-restore managed routes on output-device change",
      "priority": "P0",
      "userStory": "As a user with managed app volumes, I want Waves to re-establish my per-app routes automatically when I switch output devices, so that my volume/mute/boost settings keep working without manual recovery after plugging in headphones or switching to AirPods.",
      "expectedBehavior": [
        "A Core Audio listener on kAudioHardwarePropertyDefaultOutputDevice fires handleDeviceChange whenever the default output device changes",
        "handleDeviceChange (backend) calls autoRestoreDevice(), which records the set of currently-managed logical IDs, disposes all existing controllers, rebuilds the snapshot, then reattaches routes for the previously-managed apps via reattachRoutes",
        "Each reattached app is set to .managed on success or .error on failure; backendStatus.isRouteRecoveryHealthy reflects whether any route recovered and lastError captures the latest failure",
        "After restoration the backend emits a deviceChangeEvents signal so the store refreshes UI, diagnostics, and the device list and shows an 'Output device changed' toast",
        "The store-level handleDeviceChange is gated by the 'Auto-restore device' preference (defaults on); when off, the store skips its refresh/preset work",
        "A manual 'Recover routes' action (recoverRoutes) provides the same reattach behavior on demand",
        "The listener is registered once (idempotent guard) and torn down on stop, with the change continuation finished"
      ],
      "acceptanceCriteria": [
        "Switching the system output device while apps are managed results in those apps being re-tapped and their volume/mute/boost reapplied to the new device",
        "After a device switch the route-recovery health reflects success/failure and lastError is populated on failure",
        "An 'Output device changed' toast appears with the new device name when the store handles the event",
        "Disabling 'Auto-restore device' stops the store from acting on the change event",
        "No duplicate listeners are registered if start() runs twice; stopping removes the listener"
      ],
      "codeRefs": [
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1405",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1462",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:223",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1341",
        "Sources/Waves/Stores/AppStore.swift:781",
        "Sources/Waves/Stores/AppStore.swift:792"
      ],
      "implStatus": "partial",
      "implNotes": "Correctness gap around the preference: the backend's handleDeviceChange ALWAYS calls autoRestoreDevice() and reattaches routes on every device change (WorkspaceAudioControlBackend.swift:1464), regardless of the 'Auto-restore device' toggle. The preferences.autoRestoreDevice flag only gates the STORE's reaction (AppStore.swift:793) — UI refresh, preset restore, and the toast. So a user who turns auto-restore OFF still gets routes torn down and reattached at the backend level; they just don't see the toast or get presets restored. The preference name implies it disables auto-restore, but the actual route restoration cannot be disabled. Otherwise the restore flow itself is robust (controllers disposed before rebuild, race-safe reattach).",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [2H 1M]",
      "errorsFound": "• [HIGH/inconsistency] 'Auto-restore device' toggle does not actually disable route restoration (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1464) — Verified: the backend's handleDeviceChange unconditionally calls autoRestoreDevice() on every default-output change (WorkspaceAudioControlBackend.swift:1463-1464), disposing all controllers (disposeControllers(keeping: []) line 230) and rebuilding/reattaching routes regardless of preferences.autoRestoreDevice. The preference only gates the STORE's reaction (AppStore.swift:793) — UI refresh, preset restore, and the toast. The backend has no knowledge of the preference (no setter; grep confirms autoRestoreDevice preference lives only in AppStore/UserPreferences/SettingsView). So with the toggle OFF, every managed app's tap/aggregate is still torn down and reattached on each device switch; only the visible feedback is suppressed. The toggle's label implies it disables auto-restore. | Fix: Thread the autoRestoreDevice preference into the backend (a setter the store keeps in sync, or have the store own the reattach decision) so that when auto-restore is off, the backend's handleDeviceChange only yields the deviceChangeEvents signal and skips autoRestoreDevice(). Alternatively relabel the toggle to reflect that core restoration always happens.\n• [HIGH/ux] With auto-restore off, the UI shows a stale output device after a switch (Sources/Waves/Stores/AppStore.swift:793) — Verified: when preferences.autoRestoreDevice is false, the store's handleDeviceChange early-returns (AppStore.swift:793) and never replaces session via currentSnapshot(). The backend has already changed its snapshot's currentDevice and rebuilt routes, but the store's session.currentDevice is not updated, so currentDeviceName (main-window header MainWindowView.swift:442, Audio settings SettingsView.swift:188) and the menu-bar picker keep displaying the previous device. It only self-corrects when an unrelated action runs mergeAppState (which copies backendSession.currentDevice, AppStore.swift:1426) or a refresh occurs — not from the device change itself. | Fix: Always refresh session.currentDevice and availableDevices on a device-change event; gate only the preset-restore and the 'Output device changed' toast behind the autoRestoreDevice preference.\n• [MEDIUM/concurrency] Overlapping device changes can drop route reattachment via actor reentrancy (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:223) — Verified by tracing the actor: autoRestoreDevice() captures managedLogicalIDs from apps whose routingState==.managed or with an active controller (lines 224-228), then disposeControllers(keeping: []) (synchronous, line 230), then awaits buildSnapshot (231) and reattachRoutes (235). disposeControllers empties controllers, and buildSnapshot then sets every non-.live app to .monitorOnly (lines 1044-1054 — the controller check at 1044 sees an empty controllers dict). Because the method suspends at await points, a second handleDeviceChange (rapid plug/unplug or AirPods reconnect storm) can interleave on the actor: it recomputes managedLogicalIDs from a snapshot whose routingState is now .monitorOnly and whose controllers are gone, yielding an EMPTY set, so it skips reattachRoutes (line 234 guard). Worse, if the second invocation's disposeControllers(keeping: []) runs while the first call's reattachRoutes is mid-flight (between its own applyRoute awaits), it disposes controllers the first call just rebuilt, and the second call won't rebuild them (empty managed set) — potentially leaving previously-managed apps un-reattached after a burst settles. | Fix: Serialize device-change handling (single-flight guard or debounce/coalesce so only the latest change runs to completion), or snapshot the managed-logical-ID set once at the top and protect it so a concurrent invocation cannot observe an empty set or dispose in-flight rebuilds.",
      "fixStatus": "1 fixed · 2 deferred",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "inconsistency",
          "title": "'Auto-restore device' toggle does not actually disable route restoration",
          "detail": "Verified: the backend's handleDeviceChange unconditionally calls autoRestoreDevice() on every default-output change (WorkspaceAudioControlBackend.swift:1463-1464), disposing all controllers (disposeControllers(keeping: []) line 230) and rebuilding/reattaching routes regardless of preferences.autoRestoreDevice. The preference only gates the STORE's reaction (AppStore.swift:793) — UI refresh, preset restore, and the toast. The backend has no knowledge of the preference (no setter; grep confirms autoRestoreDevice preference lives only in AppStore/UserPreferences/SettingsView). So with the toggle OFF, every managed app's tap/aggregate is still torn down and reattached on each device switch; only the visible feedback is suppressed. The toggle's label implies it disables auto-restore.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1464",
          "expectedVsActual": "Expected: disabling 'Auto-restore device' stops Waves from re-establishing routes on a device change. Actual: routes are always disposed and reattached at the backend; the preference only suppresses UI/preset/toast in the store, making the toggle misleading and partly a dead control.",
          "suggestedFix": "Thread the autoRestoreDevice preference into the backend (a setter the store keeps in sync, or have the store own the reattach decision) so that when auto-restore is off, the backend's handleDeviceChange only yields the deviceChangeEvents signal and skips autoRestoreDevice(). Alternatively relabel the toggle to reflect that core restoration always happens.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "deferred:needs-user-decision:Requires a product decision (leave routes torn down vs preserve when off) and a cross-file contract: the store-owned preference must be pushed into the actor an",
          "uid": "DEV-04#1",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift"
        },
        {
          "severity": "high",
          "type": "ux",
          "title": "With auto-restore off, the UI shows a stale output device after a switch",
          "detail": "Verified: when preferences.autoRestoreDevice is false, the store's handleDeviceChange early-returns (AppStore.swift:793) and never replaces session via currentSnapshot(). The backend has already changed its snapshot's currentDevice and rebuilt routes, but the store's session.currentDevice is not updated, so currentDeviceName (main-window header MainWindowView.swift:442, Audio settings SettingsView.swift:188) and the menu-bar picker keep displaying the previous device. It only self-corrects when an unrelated action runs mergeAppState (which copies backendSession.currentDevice, AppStore.swift:1426) or a refresh occurs — not from the device change itself.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:793",
          "expectedVsActual": "Expected: the displayed current output device tracks the system default. Actual: with auto-restore off, the header/settings/picker show the old device name until an unrelated app action or manual refresh updates session.currentDevice.",
          "suggestedFix": "Always refresh session.currentDevice and availableDevices on a device-change event; gate only the preset-restore and the 'Output device changed' toast behind the autoRestoreDevice preference.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:verified-in-code",
          "uid": "DEV-04#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-DEVICECHANGE-GATING/FU-MENU-SECTION-DEDUP",
          "fixTier": "tier1"
        },
        {
          "severity": "medium",
          "type": "concurrency",
          "title": "Overlapping device changes can drop route reattachment via actor reentrancy",
          "detail": "Verified by tracing the actor: autoRestoreDevice() captures managedLogicalIDs from apps whose routingState==.managed or with an active controller (lines 224-228), then disposeControllers(keeping: []) (synchronous, line 230), then awaits buildSnapshot (231) and reattachRoutes (235). disposeControllers empties controllers, and buildSnapshot then sets every non-.live app to .monitorOnly (lines 1044-1054 — the controller check at 1044 sees an empty controllers dict). Because the method suspends at await points, a second handleDeviceChange (rapid plug/unplug or AirPods reconnect storm) can interleave on the actor: it recomputes managedLogicalIDs from a snapshot whose routingState is now .monitorOnly and whose controllers are gone, yielding an EMPTY set, so it skips reattachRoutes (line 234 guard). Worse, if the second invocation's disposeControllers(keeping: []) runs while the first call's reattachRoutes is mid-flight (between its own applyRoute awaits), it disposes controllers the first call just rebuilt, and the second call won't rebuild them (empty managed set) — potentially leaving previously-managed apps un-reattached after a burst settles.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:223",
          "expectedVsActual": "Expected: after a burst of device changes, previously-managed apps end up re-tapped. Actual: reentrant interleaving at the await points can have the second pass observe zero managed apps (and even dispose freshly-rebuilt controllers), losing routes until the user manually recovers.",
          "suggestedFix": "Serialize device-change handling (single-flight guard or debounce/coalesce so only the latest change runs to completion), or snapshot the managed-logical-ID set once at the top and protect it so a concurrent invocation cannot observe an empty set or dispose in-flight rebuilds.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "deferred:needs-hardware:A single-flight/coalesce redesign across autoRestoreDevice/buildSnapshot/reattachRoutes await points is easy to get subtly wrong, overlaps the hotplug-listener ",
          "uid": "DEV-04#3",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift"
        }
      ]
    },
    {
      "id": "DEV-05",
      "area": "Output Device Management & Auto-Restore",
      "feature": "Per-device volume presets (remember and restore volumes per output device)",
      "priority": "P1",
      "userStory": "As a user, I want Waves to remember each app's volume, mute, and boost separately per output device, so that switching from speakers to headphones automatically restores the levels I prefer on each.",
      "expectedBehavior": [
        "When 'Per-device volume presets' is enabled, every volume/mute/boost change is captured into deviceVolumePresets keyed by [deviceID][appLogicalID] as AppVolumeSettings (desiredVolume, isMuted, volumeBoost)",
        "Volume changes update the in-memory preset on each tick but defer the disk write to commitDesiredVolume to avoid rewriting the file during a slider drag; mute and boost changes persist asynchronously immediately",
        "On a device change to a NEW device, restoreDeviceVolumePresets applies any saved per-app settings for that device to the live snapshot and the backend (desiredVolume, isMuted, volumeBoost), then re-persists the session",
        "Presets are stored on disk as deviceVolumePresets.json in Application Support with an atomic write, a 10MB size guard, and corrupt/oversized files backed up to a .corrupt sibling rather than wiped",
        "Loading a missing file returns empty defaults without writing; the toggle is exposed in Settings and defaults on"
      ],
      "acceptanceCriteria": [
        "With the toggle on, adjusting an app's volume/mute/boost on device A and switching to device B then back to A restores A's saved levels for that app",
        "Settings persist across relaunch and are stored under Application Support/Waves/deviceVolumePresets.json",
        "A corrupt or oversized presets file is preserved as .corrupt and the app falls back to empty defaults without crashing",
        "With the toggle off, no per-device preset is captured or restored",
        "A slider drag does not rewrite the presets file on every tick (write deferred to commit)"
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:371",
        "Sources/Waves/Stores/AppStore.swift:508",
        "Sources/Waves/Stores/AppStore.swift:555",
        "Sources/Waves/Stores/AppStore.swift:848",
        "Sources/Waves/Stores/UserPreferences.swift:97",
        "Sources/Waves/Services/Persistence/DeviceVolumePresetsStore.swift:29"
      ],
      "implStatus": "partial",
      "implNotes": "Capture and persistence are well implemented. The restore path is the weak link: restoreDeviceVolumePresets is only ever called from the store's handleDeviceChange (AppStore.swift:803), which early-returns when preferences.autoRestoreDevice is OFF (AppStore.swift:793). So a user who disables 'Auto-restore device' but keeps 'Per-device volume presets' ON will silently never have presets restored on a device switch — the two toggles are coupled in a way the UI does not communicate. Also, presets are only restored when previousDeviceID != newDeviceID, so a restore is skipped if the snapshot's currentDevice hasn't updated yet when the event is handled. Presets are keyed by deviceID (the device UID); there is no UI to view or clear stored presets (removeDevice exists but is never called), so stale entries for removed devices accumulate.",
      "source": "phase1-story",
      "testStatus": "4 issue(s) [1H 2M 1L]",
      "errorsFound": "• [HIGH/logic] Per-device presets silently never restore when 'Auto-restore device' is off (Sources/Waves/Stores/AppStore.swift:803) — Verified: restoreDeviceVolumePresets is only called from the store's handleDeviceChange (AppStore.swift:803), which early-returns when preferences.autoRestoreDevice is false (AppStore.swift:793). So a user who keeps 'Per-device volume presets' ON but turns 'Auto-restore device' OFF will have presets captured on every change (capture paths at AppStore.swift:371, 508, 555 gate only on enablePerDeviceVolumePresets) but never restored on a device switch. The two Settings toggles are presented as fully independent (SettingsView.swift:78-86 and 105-113, no help text/coupling note) yet are silently coupled. | Fix: Decouple the two preferences: run the per-device preset restore on device-change whenever enablePerDeviceVolumePresets is true, independent of autoRestoreDevice. Move the restore (and the minimal session/currentDevice refresh it needs) outside the autoRestoreDevice gate.\n• [MEDIUM/edge-case] With auto-restore off, the first per-device capture after a switch is keyed to the stale (old) device (Sources/Waves/Stores/AppStore.swift:371) — New defect (missed by first tester). Per-device capture in setDesiredVolume/setMuted/setVolumeBoost keys on currentDeviceID = session.currentDevice?.id (AppStore.swift:371, 508, 555, 130-131). When auto-restore is OFF, a device switch does NOT update session.currentDevice (handleDeviceChange early-returns at line 793, and selectOutputDevice never sets it). session.currentDevice only catches up later via mergeAppState (line 1426) AFTER the async backend apply completes. But the capture at the top of setDesiredVolume runs synchronously BEFORE that apply. So the first volume/mute/boost change made on the new device after a switch is written into deviceVolumePresets under the OLD device's UID, corrupting per-device data. Subsequent changes use the corrected device once mergeAppState has run. | Fix: Ensure session.currentDevice is refreshed on every device-change event regardless of autoRestoreDevice (same root fix as DEV-04), so currentDeviceID reflects the live device before any capture occurs.\n• [MEDIUM/edge-case] Saved presets for the current device are not applied on relaunch (Sources/Waves/Stores/AppStore.swift:214) — Verified: restoreDeviceVolumePresets is only invoked from handleDeviceChange (a device switch). On startup, start() calls reapplyRestoredAudioState (AppStore.swift:214) which reapplies only the single set of volume/mute/boost values stored in the session snapshot (lines 824-844); it never consults deviceVolumePresets for the current device. If the default output device changed while Waves was closed, the app applies the last-session values rather than that device's saved preset, so per-device levels are not honored across relaunch in that case. | Fix: During start(), after building the snapshot and resolving currentDeviceID, call restoreDeviceVolumePresets(for: currentDeviceID) when enablePerDeviceVolumePresets is true, so the live device's saved preset is applied at launch.\n• [LOW/dead-code] removeDevice is dead code; no UI to view or clear stored per-device presets (Sources/Waves/Stores/UserPreferences.swift:111) — Verified: DeviceVolumePresets.removeDevice(_:) (UserPreferences.swift:111) is never called anywhere (grep confirms the only removeDevice* hits are the unrelated removeDeviceChangeListener). There is no UI surface to inspect or clear deviceVolumePresets.json. Presets are keyed by device UID, so entries for removed/forgotten devices accumulate indefinitely with no pruning or user control. | Fix: Either wire removeDevice into a 'Clear stored device presets' action in Settings (and/or prune entries for devices not seen recently), or remove the dead removeDevice method if pruning is intentionally out of scope.",
      "fixStatus": "4 fixed",
      "fixNotes": "tier1: FU-DEVICECHANGE-GATING | tier2: FU-LAUNCH-DEVICE-PRESETS",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "logic",
          "title": "Per-device presets silently never restore when 'Auto-restore device' is off",
          "detail": "Verified: restoreDeviceVolumePresets is only called from the store's handleDeviceChange (AppStore.swift:803), which early-returns when preferences.autoRestoreDevice is false (AppStore.swift:793). So a user who keeps 'Per-device volume presets' ON but turns 'Auto-restore device' OFF will have presets captured on every change (capture paths at AppStore.swift:371, 508, 555 gate only on enablePerDeviceVolumePresets) but never restored on a device switch. The two Settings toggles are presented as fully independent (SettingsView.swift:78-86 and 105-113, no help text/coupling note) yet are silently coupled.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:803",
          "expectedVsActual": "Expected (acceptance): with the per-device toggle on, switching devices restores each device's saved per-app levels. Actual: if auto-restore is off, restoreDeviceVolumePresets is never reached, so nothing is ever restored despite the toggle being on, with no UI indication.",
          "suggestedFix": "Decouple the two preferences: run the per-device preset restore on device-change whenever enablePerDeviceVolumePresets is true, independent of autoRestoreDevice. Move the restore (and the minimal session/currentDevice refresh it needs) outside the autoRestoreDevice gate.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:verified-in-code",
          "uid": "DEV-05#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-DEVICECHANGE-GATING/FU-MENU-SECTION-DEDUP",
          "fixTier": "tier1"
        },
        {
          "severity": "medium",
          "type": "edge-case",
          "title": "With auto-restore off, the first per-device capture after a switch is keyed to the stale (old) device",
          "detail": "New defect (missed by first tester). Per-device capture in setDesiredVolume/setMuted/setVolumeBoost keys on currentDeviceID = session.currentDevice?.id (AppStore.swift:371, 508, 555, 130-131). When auto-restore is OFF, a device switch does NOT update session.currentDevice (handleDeviceChange early-returns at line 793, and selectOutputDevice never sets it). session.currentDevice only catches up later via mergeAppState (line 1426) AFTER the async backend apply completes. But the capture at the top of setDesiredVolume runs synchronously BEFORE that apply. So the first volume/mute/boost change made on the new device after a switch is written into deviceVolumePresets under the OLD device's UID, corrupting per-device data. Subsequent changes use the corrected device once mergeAppState has run.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:371",
          "expectedVsActual": "Expected: a level change on device B is stored under device B's preset key. Actual: with auto-restore off, immediately after switching to B the change is stored under device A's key because session.currentDevice is still stale at capture time.",
          "suggestedFix": "Ensure session.currentDevice is refreshed on every device-change event regardless of autoRestoreDevice (same root fix as DEV-04), so currentDeviceID reflects the live device before any capture occurs.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:FU-DEVICECHANGE-GATING",
          "uid": "DEV-05#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-DEVICECHANGE-GATING",
          "fixTier": "tier1"
        },
        {
          "severity": "medium",
          "type": "edge-case",
          "title": "Saved presets for the current device are not applied on relaunch",
          "detail": "Verified: restoreDeviceVolumePresets is only invoked from handleDeviceChange (a device switch). On startup, start() calls reapplyRestoredAudioState (AppStore.swift:214) which reapplies only the single set of volume/mute/boost values stored in the session snapshot (lines 824-844); it never consults deviceVolumePresets for the current device. If the default output device changed while Waves was closed, the app applies the last-session values rather than that device's saved preset, so per-device levels are not honored across relaunch in that case.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:214",
          "expectedVsActual": "Expected (acceptance): settings persist across relaunch so each device's saved per-app levels are restored. Actual: on launch only the session's last values are reapplied; the current device's stored deviceVolumePresets entry is ignored, so a device that changed while closed gets the wrong levels.",
          "suggestedFix": "During start(), after building the snapshot and resolving currentDeviceID, call restoreDeviceVolumePresets(for: currentDeviceID) when enablePerDeviceVolumePresets is true, so the live device's saved preset is applied at launch.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier2:applied:FU-LAUNCH-DEVICE-PRESETS",
          "uid": "DEV-05#3",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-LAUNCH-DEVICE-PRESETS",
          "fixTier": "tier2"
        },
        {
          "severity": "low",
          "type": "dead-code",
          "title": "removeDevice is dead code; no UI to view or clear stored per-device presets",
          "detail": "Verified: DeviceVolumePresets.removeDevice(_:) (UserPreferences.swift:111) is never called anywhere (grep confirms the only removeDevice* hits are the unrelated removeDeviceChangeListener). There is no UI surface to inspect or clear deviceVolumePresets.json. Presets are keyed by device UID, so entries for removed/forgotten devices accumulate indefinitely with no pruning or user control.",
          "codeRef": "Sources/Waves/Stores/UserPreferences.swift:111",
          "expectedVsActual": "Expected: stored presets are manageable/clearable and removeDevice is used. Actual: removeDevice is dead code and stale per-device entries grow unbounded with no UI to view or reset them.",
          "suggestedFix": "Either wire removeDevice into a 'Clear stored device presets' action in Settings (and/or prune entries for devices not seen recently), or remove the dead removeDevice method if pruning is intentionally out of scope.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/UserPreferences.swift",
          "disposition": "iter2:fixed",
          "uid": "DEV-05#4",
          "canonicalFile": "Sources/Waves/Stores/UserPreferences.swift",
          "fixStatus": "applied",
          "fixedBy": "iter2-deferred-reduction",
          "fixTier": "iter2"
        }
      ]
    },
    {
      "id": "DEV-06",
      "area": "Output Device Management & Auto-Restore",
      "feature": "Output-device visibility in onboarding/setup status",
      "priority": "P3",
      "userStory": "As a new user, I want the setup checklist to confirm Waves can see my output device, so that I know audio routing has a valid target before I start adjusting volumes.",
      "expectedBehavior": [
        "Onboarding/setup tracks outputDeviceVisible, set true when the session snapshot has a non-nil currentDevice (syncOnboarding)",
        "buildSnapshot always produces a currentDevice, falling back to the previous snapshot's device or a synthetic 'System Output' placeholder when Core Audio cannot report one, so the setup step does not falsely fail",
        "The setup completion/route-health step depends on output device visibility (onboarding step gating)",
        "The current output device name is shown in Settings and the main window header as read-only status"
      ],
      "acceptanceCriteria": [
        "The setup checklist marks the output-device step complete when a current device is present",
        "If Core Audio cannot report a device, a 'System Output' placeholder keeps the snapshot's currentDevice non-nil instead of crashing or blanking",
        "Settings and the main window display the current output device name"
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:1360",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1062",
        "Sources/Waves/Features/Onboarding/OnboardingView.swift:33",
        "Sources/Waves/Settings/SettingsView.swift:188"
      ],
      "implStatus": "implemented",
      "implNotes": "Straightforward and defensive: the synthetic 'System Output' fallback (id 'system-output') guarantees currentDevice is non-nil, so outputDeviceVisible is effectively always true on a supported OS — meaning this setup step rarely, if ever, signals a real problem. It is more of a status indicator than a gating check.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "DVP-01",
      "area": "Per-Device Volume Presets",
      "feature": "Capture per-app volume/mute/boost per output device",
      "priority": "P1",
      "userStory": "As a user who moves between speakers, headphones, and external interfaces, I want Waves to silently remember each app's volume, mute, and boost separately for the device I'm using, so that I don't have to re-tune levels every time I plug in a different output.",
      "expectedBehavior": [
        "When the per-device feature is enabled and an output device is active, changing an app's volume (setDesiredVolume) records an AppVolumeSettings (desiredVolume, isMuted, volumeBoost) keyed by [deviceID][appLogicalID] in the in-memory deviceVolumePresets",
        "Muting/unmuting (setMuted) and changing boost (setVolumeBoost) likewise write an updated AppVolumeSettings for the current device",
        "The settings captured reflect the app's other current attributes (e.g. a volume change captures the app's current isMuted and volumeBoost, not just the changed field)",
        "deviceVolumes is a nested dictionary [deviceID: [appID: AppVolumeSettings]] so each device has an independent set of per-app settings",
        "Keys are the app's logicalID (stable across PID changes), and deviceID is session.currentDevice?.id",
        "If the feature toggle is off, or there is no current output device (currentDeviceID == nil), no preset is recorded"
      ],
      "acceptanceCriteria": [
        "With the toggle on and a device active, setting app A to 40% then reading deviceVolumePresets.getVolumeSettings(for: A.logicalID, deviceID:) returns desiredVolume == 0.4",
        "Muting app A updates the same entry's isMuted to true while preserving its desiredVolume and volumeBoost",
        "Switching the active output device and setting app A to a different volume leaves the first device's stored value unchanged",
        "With the toggle off, changing a volume does not add or modify any entry in deviceVolumes",
        "With no current output device, changing a volume does not crash and records nothing"
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:356",
        "Sources/Waves/Stores/AppStore.swift:371",
        "Sources/Waves/Stores/AppStore.swift:491",
        "Sources/Waves/Stores/AppStore.swift:508",
        "Sources/Waves/Stores/AppStore.swift:544",
        "Sources/Waves/Stores/AppStore.swift:555",
        "Sources/Waves/Stores/UserPreferences.swift:97",
        "Sources/Waves/Stores/UserPreferences.swift:100",
        "Sources/Waves/Stores/UserPreferences.swift:64"
      ],
      "implStatus": "implemented",
      "implNotes": "Capture path is fully wired across volume, mute, and boost mutations and keyed correctly by logicalID + deviceID. Minor staleness concern: setMuted (line 508-514) and setVolumeBoost (line 555-561) build AppVolumeSettings from the passed-in `app` parameter (a snapshot taken at call time) rather than the freshly-mutated session.apps[index], so if the caller's `app` is stale the stored desiredVolume/boost could lag by one edit. In practice each handler only changes one field so this is low-risk, but it is an inconsistency versus setDesiredVolume which reads from the live session row.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/inconsistency] setMuted/setVolumeBoost capture the unchanged preset fields from the stale passed-in `app` snapshot, not the live session row (Sources/Waves/Stores/AppStore.swift:510) — setDesiredVolume (line 372) reads `let currentApp = session.apps[index]` and builds the preset from the freshly mutated live row. But setMuted (lines 509-513) builds AppVolumeSettings from `app.desiredVolume`/`app.volumeBoost` and setVolumeBoost (lines 556-560) from `app.desiredVolume`/`app.isMuted` — both read the `app` value parameter the caller passed, not session.apps[index]. If the caller's `app` is one edit behind the live session (a SwiftUI row bound to a value captured before a near-simultaneous change merged in), the persisted preset records a stale desiredVolume/boost. Confirmed: the asymmetry between the three capture sites is real. Severity remains low because in practice the SwiftUI binding for a given row is usually current and only the changed field is authoritative; the window for divergence is narrow. | Fix: After locating `index`, read `let currentApp = session.apps[index]` in setMuted and setVolumeBoost and build AppVolumeSettings from currentApp (using the freshly mutated field for the changed value), mirroring setDesiredVolume.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier3: FU-PERDEVICE-CAPTURE-STALE-PARAM",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "setMuted/setVolumeBoost capture the unchanged preset fields from the stale passed-in `app` snapshot, not the live session row",
          "detail": "setDesiredVolume (line 372) reads `let currentApp = session.apps[index]` and builds the preset from the freshly mutated live row. But setMuted (lines 509-513) builds AppVolumeSettings from `app.desiredVolume`/`app.volumeBoost` and setVolumeBoost (lines 556-560) from `app.desiredVolume`/`app.isMuted` — both read the `app` value parameter the caller passed, not session.apps[index]. If the caller's `app` is one edit behind the live session (a SwiftUI row bound to a value captured before a near-simultaneous change merged in), the persisted preset records a stale desiredVolume/boost. Confirmed: the asymmetry between the three capture sites is real. Severity remains low because in practice the SwiftUI binding for a given row is usually current and only the changed field is authoritative; the window for divergence is narrow.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:510",
          "expectedVsActual": "Story DVP-01: a change should capture the app's current other fields from live state. Actual: mute/boost capture reads the possibly-stale `app` parameter rather than session.apps[index].",
          "suggestedFix": "After locating `index`, read `let currentApp = session.apps[index]` in setMuted and setVolumeBoost and build AppVolumeSettings from currentApp (using the freshly mutated field for the changed value), mirroring setDesiredVolume.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier3:applied:FU-PERDEVICE-CAPTURE-STALE-PARAM",
          "uid": "DVP-01#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-PERDEVICE-CAPTURE-STALE-PARAM",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "DVP-02",
      "area": "Per-Device Volume Presets",
      "feature": "Restore saved per-device settings when the output device changes",
      "priority": "P0",
      "userStory": "As a user who switches output devices while Waves is running, I want my previously-saved volumes, mutes, and boosts for that device to be reapplied automatically, so that each device sounds the way I last left it without manual re-adjustment.",
      "expectedBehavior": [
        "On a device-change event from the backend, handleDeviceChange captures the previous deviceID, refreshes the session snapshot, and detects the new deviceID",
        "If the feature is enabled and the device actually changed (previousDeviceID != newDeviceID), restoreDeviceVolumePresets runs for the new device",
        "For each running app that has a saved AppVolumeSettings for the new device, the stored desiredVolume, isMuted, and volumeBoost are written back into session.apps and pushed to the backend (setDesiredVolume, setMuted, setVolumeBoost)",
        "Apps with no saved settings for that device are left untouched",
        "After restoration the session snapshot is persisted and the visible-apps cache invalidated; a 'Output device changed' toast is shown",
        "Restoration only happens when preferences.autoRestoreDevice is also true, because handleDeviceChange early-returns if it is off"
      ],
      "acceptanceCriteria": [
        "With saved settings for device B and a switch from A to B, each app with a B entry has its session volume/mute/boost updated to the saved values and the same values are sent to the backend",
        "An app with no entry for device B retains its current volume after the switch",
        "If previousDeviceID == newDeviceID (spurious event), restoreDeviceVolumePresets is not invoked",
        "If autoRestoreDevice is off, no preset restoration occurs even when enablePerDeviceVolumePresets is on",
        "A backend failure while applying one app's settings is logged and does not abort restoration of the remaining apps"
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:792",
        "Sources/Waves/Stores/AppStore.swift:798",
        "Sources/Waves/Stores/AppStore.swift:802",
        "Sources/Waves/Stores/AppStore.swift:848",
        "Sources/Waves/Stores/UserPreferences.swift:107",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1462"
      ],
      "implStatus": "partial",
      "implNotes": "Restoration logic is correct and per-app failures are isolated (logged, loop continues). Two real gaps: (1) restoration is gated behind preferences.autoRestoreDevice (handleDeviceChange returns early at line 793), so a user who wants per-device presets but disables auto-restore-device silently gets no preset restoration — the two settings are coupled in a way the UI does not communicate. (2) restoreDeviceVolumePresets only iterates apps currently present in session.apps, so an app that isn't running at switch time never has its saved settings reapplied even if it launches later on that device.",
      "source": "phase1-story",
      "testStatus": "5 issue(s) [1H 2M 2L]",
      "errorsFound": "• [HIGH/logic] Per-device preset restoration is silently gated behind the unrelated autoRestoreDevice toggle (Sources/Waves/Stores/AppStore.swift:793) — handleDeviceChange() returns at line 793 when preferences.autoRestoreDevice is false; the per-device restore (restoreDeviceVolumePresets) is reached only after that guard, at line 803. 'Auto-restore device' and 'Per-device volume presets' are two independent toggles in SettingsView (lines 78-86 and 105-113) with no UI coupling. A user who turns OFF auto-restore-device but leaves per-device presets ON gets no restoration on a device switch, with no UI indication the two are coupled. Corroborated by feature-status.json implNotes for DVP-02 (line 1791) and the named feature (line 1680). | Fix: Run the preset-restore block whenever enablePerDeviceVolumePresets is true regardless of autoRestoreDevice (move the early-return so it only guards the route/snapshot refresh), or surface the dependency in Settings (disable/explain the per-device toggle when auto-restore is off).\n• [MEDIUM/edge-case] Saved presets are never reapplied to an app that launches after the device switch (Sources/Waves/Stores/AppStore.swift:849) — restoreDeviceVolumePresets (line 849) iterates only `session.apps.indices` — apps present at the moment of the device-change event. An app that launches later, while that device is still active, never has its saved AppVolumeSettings applied because nothing re-runs device-preset restoration on app appearance; the merge path restores the persisted session values, not the device-specific preset. Confirmed; corroborated by feature-status.json (lines 1680, 1791). | Fix: On new-app appearance (mergedSession / app-discovery path), if per-device presets are enabled look up deviceVolumePresets.getVolumeSettings(for: app.logicalID, deviceID: currentDeviceID) and apply it for newly added apps.\n• [MEDIUM/correctness] Restore updates desiredVolume/isMuted/volumeBoost but leaves appliedVolume and routingState stale (Sources/Waves/Stores/AppStore.swift:852) — restoreDeviceVolumePresets writes settings.desiredVolume/isMuted/volumeBoost into session.apps[index] (lines 852-854) and pushes them to the backend, but never updates session.apps[index].appliedVolume or routingState. Contrast reapplyRestoredAudioState (lines 837-838) which sets appliedVolume = isMuted ? 0 : desiredVolume and routingState = .managed. After restore the row's appliedVolume retains the prior snapshot value, so applied/mute visuals and any appliedVolume-derived logic disagree with the restored desiredVolume until the next backend merge. Confirmed by direct comparison of the two functions. | Fix: After writing the three fields, set session.apps[index].appliedVolume = settings.isMuted ? 0 : settings.desiredVolume and routingState = .managed (matching reapplyRestoredAudioState).\n• [LOW/concurrency] Index-based restore loop with awaits can write to the wrong app if session.apps is replaced concurrently (Sources/Waves/Stores/AppStore.swift:850) — restoreDeviceVolumePresets loops over session.apps.indices and, across three `await backend.set...` suspension points per iteration, subscripts session.apps[index]. AppStore is @MainActor (line 24), so a true data race / out-of-bounds crash cannot happen mid-statement, but another @MainActor task CAN run during an await: mergeAppState assigns `session = backendSession` when an app isn't found (lines 1419/1433), and scheduleVolumeApply's completion (line 429) calls mergeAppState. If that replaces session.apps between this loop's awaits, a later subscript at the captured `index` can write the next app's restored fields onto a different positional row (mergeAppState normally preserves order, so a full-replace mismatch is the realistic case, not a crash). Severity downgraded from medium to low because MainActor serialization rules out the OOB-crash framing the first tester gave and order is usually preserved; the wrong-app write is a narrow window. | Fix: Snapshot (logicalID, settings) pairs up front, then after each await re-resolve via session.apps.firstIndex(matchingAppKey:) before mutating, skipping apps that disappeared.\n• [LOW/concurrency] In-flight slider commit can overwrite a just-restored value because device change does not cancel pending volume-apply tasks (Sources/Waves/Stores/AppStore.swift:802) — handleDeviceChange/restoreDeviceVolumePresets never cancel pendingVolumeApplyTasks or clear pendingVolumeTargets. If a slider commit for app A is still in flight (sleeping on volumeApplyDelay at line 419, or awaiting backend.setDesiredVolume at line 427) when a device change restores A's preset, the pending task completes afterward and re-pushes pendingVolumeTargets[A] (the prior device's drag target) to the backend, clobbering the restored level. setExcluded shows the correct pattern (cancel + remove at lines 709-710). Confirmed reachable. | Fix: Before restoring, cancel pendingVolumeApplyTasks and remove pendingVolumeTargets for apps being restored, or clear the pending target for each restored appID so restore takes precedence.",
      "fixStatus": "3 fixed · 2 n/a",
      "fixNotes": "tier2: FU-EXCLUSION-PRESET-RESTORE",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "logic",
          "title": "Per-device preset restoration is silently gated behind the unrelated autoRestoreDevice toggle",
          "detail": "handleDeviceChange() returns at line 793 when preferences.autoRestoreDevice is false; the per-device restore (restoreDeviceVolumePresets) is reached only after that guard, at line 803. 'Auto-restore device' and 'Per-device volume presets' are two independent toggles in SettingsView (lines 78-86 and 105-113) with no UI coupling. A user who turns OFF auto-restore-device but leaves per-device presets ON gets no restoration on a device switch, with no UI indication the two are coupled. Corroborated by feature-status.json implNotes for DVP-02 (line 1791) and the named feature (line 1680).",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:793",
          "expectedVsActual": "Per-device presets should restore whenever enablePerDeviceVolumePresets is on. Actual: restoration is silently coupled to autoRestoreDevice; the Settings UI presents the two as independent.",
          "suggestedFix": "Run the preset-restore block whenever enablePerDeviceVolumePresets is true regardless of autoRestoreDevice (move the early-return so it only guards the route/snapshot refresh), or surface the dependency in Settings (disable/explain the per-device toggle when auto-restore is off).",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:verified-in-code",
          "uid": "DVP-02#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-DEVICECHANGE-GATING/FU-MENU-SECTION-DEDUP",
          "fixTier": "tier1"
        },
        {
          "severity": "medium",
          "type": "edge-case",
          "title": "Saved presets are never reapplied to an app that launches after the device switch",
          "detail": "restoreDeviceVolumePresets (line 849) iterates only `session.apps.indices` — apps present at the moment of the device-change event. An app that launches later, while that device is still active, never has its saved AppVolumeSettings applied because nothing re-runs device-preset restoration on app appearance; the merge path restores the persisted session values, not the device-specific preset. Confirmed; corroborated by feature-status.json (lines 1680, 1791).",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:849",
          "expectedVsActual": "An app with a saved entry for device B should get those levels on B. Actual: only apps already running at switch time are restored; late-launching apps are missed.",
          "suggestedFix": "On new-app appearance (mergedSession / app-discovery path), if per-device presets are enabled look up deviceVolumePresets.getVolumeSettings(for: app.logicalID, deviceID: currentDeviceID) and apply it for newly added apps.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "gapfill:fixed",
          "uid": "DVP-02#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "gapfill",
          "fixTier": "gapfill"
        },
        {
          "severity": "medium",
          "type": "correctness",
          "title": "Restore updates desiredVolume/isMuted/volumeBoost but leaves appliedVolume and routingState stale",
          "detail": "restoreDeviceVolumePresets writes settings.desiredVolume/isMuted/volumeBoost into session.apps[index] (lines 852-854) and pushes them to the backend, but never updates session.apps[index].appliedVolume or routingState. Contrast reapplyRestoredAudioState (lines 837-838) which sets appliedVolume = isMuted ? 0 : desiredVolume and routingState = .managed. After restore the row's appliedVolume retains the prior snapshot value, so applied/mute visuals and any appliedVolume-derived logic disagree with the restored desiredVolume until the next backend merge. Confirmed by direct comparison of the two functions.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:852",
          "expectedVsActual": "After restoration session state should be internally consistent (applied reflects restored desired/mute). Actual: appliedVolume/routingState are not updated, producing a transient inconsistent UI state and a stale persisted snapshot (persistSessionSnapshot runs at line 866).",
          "suggestedFix": "After writing the three fields, set session.apps[index].appliedVolume = settings.isMuted ? 0 : settings.desiredVolume and routingState = .managed (matching reapplyRestoredAudioState).",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier2:partial:FU-EXCLUSION-PRESET-RESTORE",
          "uid": "DVP-02#3",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "partial",
          "fixedBy": "FU-EXCLUSION-PRESET-RESTORE",
          "fixTier": "tier2"
        },
        {
          "severity": "low",
          "type": "concurrency",
          "title": "Index-based restore loop with awaits can write to the wrong app if session.apps is replaced concurrently",
          "detail": "restoreDeviceVolumePresets loops over session.apps.indices and, across three `await backend.set...` suspension points per iteration, subscripts session.apps[index]. AppStore is @MainActor (line 24), so a true data race / out-of-bounds crash cannot happen mid-statement, but another @MainActor task CAN run during an await: mergeAppState assigns `session = backendSession` when an app isn't found (lines 1419/1433), and scheduleVolumeApply's completion (line 429) calls mergeAppState. If that replaces session.apps between this loop's awaits, a later subscript at the captured `index` can write the next app's restored fields onto a different positional row (mergeAppState normally preserves order, so a full-replace mismatch is the realistic case, not a crash). Severity downgraded from medium to low because MainActor serialization rules out the OOB-crash framing the first tester gave and order is usually preserved; the wrong-app write is a narrow window.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:850",
          "expectedVsActual": "Restoration should be robust against the session being refreshed mid-loop. Actual: it indexes by position across awaits, assuming session.apps order/length is stable.",
          "suggestedFix": "Snapshot (logicalID, settings) pairs up front, then after each await re-resolve via session.apps.firstIndex(matchingAppKey:) before mutating, skipping apps that disappeared.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "DVP-02#4",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "low",
          "type": "concurrency",
          "title": "In-flight slider commit can overwrite a just-restored value because device change does not cancel pending volume-apply tasks",
          "detail": "handleDeviceChange/restoreDeviceVolumePresets never cancel pendingVolumeApplyTasks or clear pendingVolumeTargets. If a slider commit for app A is still in flight (sleeping on volumeApplyDelay at line 419, or awaiting backend.setDesiredVolume at line 427) when a device change restores A's preset, the pending task completes afterward and re-pushes pendingVolumeTargets[A] (the prior device's drag target) to the backend, clobbering the restored level. setExcluded shows the correct pattern (cancel + remove at lines 709-710). Confirmed reachable.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:802",
          "expectedVsActual": "After restoration the restored backend value should stick. Actual: a concurrently-pending pre-switch commit can overwrite it.",
          "suggestedFix": "Before restoring, cancel pendingVolumeApplyTasks and remove pendingVolumeTargets for apps being restored, or clear the pending target for each restored appID so restore takes precedence.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "DVP-02#5",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "DVP-03",
      "area": "Per-Device Volume Presets",
      "feature": "Persist per-device presets to disk and reload across launches",
      "priority": "P1",
      "userStory": "As a user, I want my per-device volume memory to survive quitting and relaunching Waves, so that my carefully tuned per-device levels are not lost between sessions.",
      "expectedBehavior": [
        "DeviceVolumePresetsStore writes deviceVolumePresets as pretty-printed JSON to Application Support/Waves/deviceVolumePresets.json using an atomic write on a background queue",
        "commitDesiredVolume flushes the in-memory presets to disk after a slider interaction (per-tick writes during a drag are deferred to avoid rewriting the file on every change)",
        "setMuted and setVolumeBoost each persist asynchronously via a Task after updating the in-memory presets",
        "On init, AppStore loads the persisted presets via deviceVolumePresetsStore.load(); a missing file is treated as the normal first-launch case and yields empty defaults without writing",
        "Saving/loading is decoded through PersistedSchema (schema-versioned) and tolerant of older AppVolumeSettings missing volumeBoost (defaults to 1.0)"
      ],
      "acceptanceCriteria": [
        "After changing a volume and committing, deviceVolumePresets.json exists and contains the app/device entry",
        "Relaunching the app loads the same deviceVolumePresets into AppStore.deviceVolumePresets",
        "First launch with no file present returns empty presets and does not create the file until a save occurs",
        "An AppVolumeSettings JSON object lacking volumeBoost decodes with volumeBoost == 1.0",
        "Writes occur off the main thread and use atomic replacement"
      ],
      "codeRefs": [
        "Sources/Waves/Services/Persistence/DeviceVolumePresetsStore.swift:29",
        "Sources/Waves/Services/Persistence/DeviceVolumePresetsStore.swift:69",
        "Sources/Waves/Stores/AppStore.swift:84",
        "Sources/Waves/Stores/AppStore.swift:389",
        "Sources/Waves/Stores/AppStore.swift:516",
        "Sources/Waves/Stores/AppStore.swift:562",
        "Sources/Waves/Stores/UserPreferences.swift:81"
      ],
      "implStatus": "implemented",
      "implNotes": "Persistence is solid: atomic background writes, schema-versioned decode, and graceful first-launch. One UX gap versus the README claim ('Remember volume settings for each app across different audio devices'): saved presets are NOT reapplied on app launch — start() calls reapplyRestoredAudioState() (which replays the persisted session snapshot, DVP-04) but never calls restoreDeviceVolumePresets for the current device. So at launch the device-specific presets only matter once the user switches devices; if the last persisted session was tuned on a different device than the one active at launch, the wrong device's levels apply until a switch occurs.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1M]",
      "errorsFound": "• [MEDIUM/inconsistency] Device-specific presets are not applied at launch for the currently-active device (only on a later device switch) (Sources/Waves/Stores/AppStore.swift:214) — start() calls reapplyRestoredAudioState() (line 214), which replays the persisted session snapshot's volumes/mutes/boosts, but never calls restoreDeviceVolumePresets(for: currentDeviceID). Device-preset restoration only runs inside handleDeviceChange when previousDeviceID != newDeviceID. So if the last persisted session was tuned while a different device was active than the one at launch, the wrong device's levels apply at startup and the active device's saved presets are ignored until the user switches devices. Confirmed; corroborated by feature-status.json DVP-03 implNotes (line 1829). | Fix: In start(), after reapplyRestoredAudioState() and once currentDeviceID is known, if preferences.enablePerDeviceVolumePresets call restoreDeviceVolumePresets(for: currentDeviceID).",
      "fixStatus": "1 fixed",
      "fixNotes": "tier2: FU-LAUNCH-DEVICE-PRESETS",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "inconsistency",
          "title": "Device-specific presets are not applied at launch for the currently-active device (only on a later device switch)",
          "detail": "start() calls reapplyRestoredAudioState() (line 214), which replays the persisted session snapshot's volumes/mutes/boosts, but never calls restoreDeviceVolumePresets(for: currentDeviceID). Device-preset restoration only runs inside handleDeviceChange when previousDeviceID != newDeviceID. So if the last persisted session was tuned while a different device was active than the one at launch, the wrong device's levels apply at startup and the active device's saved presets are ignored until the user switches devices. Confirmed; corroborated by feature-status.json DVP-03 implNotes (line 1829).",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:214",
          "expectedVsActual": "README ('Remember volume settings for each app across different audio devices') implies the active device's saved levels apply on launch. Actual: at launch the generic session snapshot wins; device-specific presets only take effect on a subsequent device change.",
          "suggestedFix": "In start(), after reapplyRestoredAudioState() and once currentDeviceID is known, if preferences.enablePerDeviceVolumePresets call restoreDeviceVolumePresets(for: currentDeviceID).",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier2:applied:FU-LAUNCH-DEVICE-PRESETS",
          "uid": "DVP-03#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-LAUNCH-DEVICE-PRESETS",
          "fixTier": "tier2"
        }
      ]
    },
    {
      "id": "DVP-04",
      "area": "Per-Device Volume Presets",
      "feature": "Resilient load on corrupt or oversized preset files",
      "priority": "P2",
      "userStory": "As a user, I don't want a damaged or tampered preset file to crash Waves or silently erase all my saved per-device levels, so that I can recover or at least keep using the app with sane defaults.",
      "expectedBehavior": [
        "On load, if the file size exceeds the 10MB cap, the file is backed up to deviceVolumePresets.json.corrupt and empty defaults are returned",
        "If decoding throws, the unreadable file is preserved by moving it to a .corrupt sidecar (logged) and empty defaults are returned instead of wiping data",
        "backupCorruptFile removes any prior .corrupt backup first, then moves the bad file aside",
        "If the Application Support directory cannot be obtained, the store falls back to ~/.Waves/deviceVolumePresets.json",
        "All load/save work runs on a dedicated serial queue (load synchronously, save asynchronously)"
      ],
      "acceptanceCriteria": [
        "A malformed JSON preset file causes load() to return empty DeviceVolumePresets and leaves a .corrupt backup on disk",
        "A file larger than 10MB is rejected, backed up, and defaults returned",
        "Calling load() twice on a corrupt file does not throw and overwrites the single .corrupt backup rather than accumulating many",
        "With Application Support unavailable, presets are read/written under ~/.Waves without crashing"
      ],
      "codeRefs": [
        "Sources/Waves/Services/Persistence/DeviceVolumePresetsStore.swift:33",
        "Sources/Waves/Services/Persistence/DeviceVolumePresetsStore.swift:39",
        "Sources/Waves/Services/Persistence/DeviceVolumePresetsStore.swift:48",
        "Sources/Waves/Services/Persistence/DeviceVolumePresetsStore.swift:58",
        "Sources/Waves/Services/Persistence/DeviceVolumePresetsStore.swift:12"
      ],
      "implStatus": "implemented",
      "implNotes": "Error and edge-case handling is thorough and clearly intentional (preserve-not-destroy on corruption, size cap to bound memory, directory fallback). No user-facing notification is shown when a corrupt file is detected — recovery is silent (log only) — which is acceptable but means a user who loses presets to corruption gets no explanation. AppVolumeSettings/DeviceVolumePresets have no clamping on decode of desiredVolume beyond AppVolumeSettings' boost clamp, so an out-of-range desiredVolume from a hand-edited file would pass through; downstream setDesiredVolume clamps to [0,1] so audible effect is bounded.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [2L]",
      "errorsFound": "• [LOW/ux] Corruption recovery is entirely silent — user is never told their per-device presets were reset (Sources/Waves/Services/Persistence/DeviceVolumePresetsStore.swift:51) — On an oversized (>10MB) or undecodable preset file, load() backs the file up to .corrupt and returns empty DeviceVolumePresets, logging only (DeviceVolumePresetsStore.swift lines 40-42 and 51-53). No toast or user-facing signal; load() returns no recoverable flag the caller could surface. A user whose presets vanish after corruption gets no explanation and no pointer to the .corrupt backup. Confirmed. | Fix: Have load() return/expose a recoverable flag (or post a notification) when backupCorruptFile() runs, and show a one-time warning toast from AppStore.\n• [LOW/edge-case] desiredVolume is not range-validated on decode of a hand-edited/tampered preset file (Sources/Waves/Stores/UserPreferences.swift:83) — AppVolumeSettings clamps volumeBoost to [1,4] in init (UserPreferences.swift:72), but desiredVolume is stored as-is (line 70/83-86); a tampered deviceVolumePresets.json with desiredVolume = 9.0 or -2.0 decodes unclamped and is stored back. Audible effect is bounded because setDesiredVolume clamps to [0,1] downstream, but restoreDeviceVolumePresets assigns session.apps[index].desiredVolume = settings.desiredVolume (AppStore.swift:852) before any clamp, carrying the out-of-range value into the slider model. Confirmed. | Fix: Clamp desiredVolume to [0,1] in AppVolumeSettings.init alongside the existing boost clamp.",
      "fixStatus": "1 fixed · 1 deferred",
      "fixNotes": "tier3: FU-APPVOLSETTINGS-VOLUME-CLAMP",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "ux",
          "title": "Corruption recovery is entirely silent — user is never told their per-device presets were reset",
          "detail": "On an oversized (>10MB) or undecodable preset file, load() backs the file up to .corrupt and returns empty DeviceVolumePresets, logging only (DeviceVolumePresetsStore.swift lines 40-42 and 51-53). No toast or user-facing signal; load() returns no recoverable flag the caller could surface. A user whose presets vanish after corruption gets no explanation and no pointer to the .corrupt backup. Confirmed.",
          "codeRef": "Sources/Waves/Services/Persistence/DeviceVolumePresetsStore.swift:51",
          "expectedVsActual": "Keeping running with defaults is satisfied, but silent loss gives no recovery chance. Expected: a one-time notification that presets were reset and a backup was kept.",
          "suggestedFix": "Have load() return/expose a recoverable flag (or post a notification) when backupCorruptFile() runs, and show a one-time warning toast from AppStore.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Persistence/DeviceVolumePresetsStore.swift",
          "disposition": "deferred:needs-user-decision:Changes load() contract and adds cross-file toast UX with new copy.",
          "uid": "DVP-04#1",
          "canonicalFile": "Sources/Waves/Services/Persistence/DeviceVolumePresetsStore.swift"
        },
        {
          "severity": "low",
          "type": "edge-case",
          "title": "desiredVolume is not range-validated on decode of a hand-edited/tampered preset file",
          "detail": "AppVolumeSettings clamps volumeBoost to [1,4] in init (UserPreferences.swift:72), but desiredVolume is stored as-is (line 70/83-86); a tampered deviceVolumePresets.json with desiredVolume = 9.0 or -2.0 decodes unclamped and is stored back. Audible effect is bounded because setDesiredVolume clamps to [0,1] downstream, but restoreDeviceVolumePresets assigns session.apps[index].desiredVolume = settings.desiredVolume (AppStore.swift:852) before any clamp, carrying the out-of-range value into the slider model. Confirmed.",
          "codeRef": "Sources/Waves/Stores/UserPreferences.swift:83",
          "expectedVsActual": "Expected sane clamped values from a tampered file. Actual: desiredVolume passes through unclamped at the model layer; only the audio path clamps it.",
          "suggestedFix": "Clamp desiredVolume to [0,1] in AppVolumeSettings.init alongside the existing boost clamp.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/UserPreferences.swift",
          "disposition": "tier3:applied:FU-APPVOLSETTINGS-VOLUME-CLAMP",
          "uid": "DVP-04#2",
          "canonicalFile": "Sources/Waves/Stores/UserPreferences.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-APPVOLSETTINGS-VOLUME-CLAMP",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "DVP-05",
      "area": "Per-Device Volume Presets",
      "feature": "Enable/disable per-device presets via Settings toggle",
      "priority": "P2",
      "userStory": "As a user who prefers one global volume per app regardless of device, I want a settings toggle to turn per-device volume presets off, so that Waves stops recording and restoring device-specific levels.",
      "expectedBehavior": [
        "Settings shows a 'Per-device volume presets' toggle bound to preferences.enablePerDeviceVolumePresets",
        "Toggling it updates the preference and immediately persists preferences (persistPreferences)",
        "The preference defaults to true (on) for new installs",
        "When off, all capture sites (setDesiredVolume/setMuted/setVolumeBoost) skip writing presets and handleDeviceChange skips restoreDeviceVolumePresets",
        "The preference decodes resiliently: an older preferences file missing the key loads with the default (true) rather than failing"
      ],
      "acceptanceCriteria": [
        "Toggling the setting off then on round-trips through preferences and persists across relaunch",
        "With the setting off, a volume change makes no change to deviceVolumePresets and a device switch does not reapply saved levels",
        "A new install starts with the feature enabled",
        "A preferences JSON predating enablePerDeviceVolumePresets decodes with the field == true and other settings intact"
      ],
      "codeRefs": [
        "Sources/Waves/Settings/SettingsView.swift:105",
        "Sources/Waves/Stores/UserPreferences.swift:12",
        "Sources/Waves/Stores/UserPreferences.swift:57",
        "Sources/Waves/Stores/AppStore.swift:371",
        "Sources/Waves/Stores/AppStore.swift:802",
        "Tests/WavesTests/UserPreferencesTests.swift:19"
      ],
      "implStatus": "implemented",
      "implNotes": "Toggle is fully wired and persisted, default-on, with backward-compatible decoding covered by a test. UX nuance: disabling the toggle stops capture and restore but does NOT clear any presets already saved on disk, and there is no UI to view, edit, or delete stored per-device settings (see DVP-06). The toggle's effect is also entangled with autoRestoreDevice for the restore half (DVP-02).",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1M]",
      "errorsFound": "• [MEDIUM/ux] Disabling the per-device toggle neither clears stored presets nor explains its dependence on Auto-restore device (Sources/Waves/Settings/SettingsView.swift:105) — The toggle at SettingsView.swift:105-113 flips enablePerDeviceVolumePresets and persists correctly. But (1) turning it off leaves every saved entry on disk with no clear/reset UI (see DVP-06 — removeDevice has no callers), and (2) the restore half is gated behind autoRestoreDevice (DVP-02), yet the toggle has no .help() text or visual coupling (verified: only the URL-scheme toggle has .help() at line 125; the per-device toggle has none). Plain Toggle gives no feedback and no scope description. Confirmed; corroborated by feature-status.json (line 1900). | Fix: Add a .help() describing that restoration also requires 'Auto-restore device', and provide a 'Clear stored per-device settings' action wiring removeDevice / a clear-all.",
      "fixStatus": "1 fixed",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "ux",
          "title": "Disabling the per-device toggle neither clears stored presets nor explains its dependence on Auto-restore device",
          "detail": "The toggle at SettingsView.swift:105-113 flips enablePerDeviceVolumePresets and persists correctly. But (1) turning it off leaves every saved entry on disk with no clear/reset UI (see DVP-06 — removeDevice has no callers), and (2) the restore half is gated behind autoRestoreDevice (DVP-02), yet the toggle has no .help() text or visual coupling (verified: only the URL-scheme toggle has .help() at line 125; the per-device toggle has none). Plain Toggle gives no feedback and no scope description. Confirmed; corroborated by feature-status.json (line 1900).",
          "codeRef": "Sources/Waves/Settings/SettingsView.swift:105",
          "expectedVsActual": "DVP-05 ('turn per-device presets off so Waves stops recording and restoring'): capture/restore stop, but the user is left with orphaned stored data and an undocumented dependency on a separate toggle.",
          "suggestedFix": "Add a .help() describing that restoration also requires 'Auto-restore device', and provide a 'Clear stored per-device settings' action wiring removeDevice / a clear-all.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Settings/SettingsView.swift",
          "disposition": "iter2:fixed",
          "uid": "DVP-05#1",
          "canonicalFile": "Sources/Waves/Settings/SettingsView.swift",
          "fixStatus": "applied",
          "fixedBy": "iter2-deferred-reduction",
          "fixTier": "iter2"
        }
      ]
    },
    {
      "id": "DVP-06",
      "area": "Per-Device Volume Presets",
      "feature": "Pruning/management of stale device entries (gap)",
      "priority": "P3",
      "userStory": "As a user who connects many short-lived or one-off audio devices, I want stale per-device preset entries to be cleaned up or manageable, so that the preset file doesn't grow unbounded with devices I'll never use again and I can reset a device's settings.",
      "expectedBehavior": [
        "DeviceVolumePresets exposes removeDevice(deviceID:) to drop all stored settings for a device",
        "A 10MB file-size cap exists as a last-resort guard against unbounded growth (oversized files are discarded on load)"
      ],
      "acceptanceCriteria": [
        "There exists a code path or UI that removes presets for devices that are no longer present / that the user wants to reset",
        "removeDevice is invoked somewhere in the app flow (device removal, settings 'clear', etc.)",
        "The preset store cannot grow without bound across many transient devices"
      ],
      "codeRefs": [
        "Sources/Waves/Stores/UserPreferences.swift:111",
        "Sources/Waves/Services/Persistence/DeviceVolumePresetsStore.swift:39"
      ],
      "implStatus": "missing",
      "implNotes": "DeviceVolumePresets.removeDevice(_:) is defined but has ZERO callers anywhere in Sources — confirmed via grep. There is no pruning when a device is unplugged/forgotten and no UI to view, edit, reset, or delete per-device presets. Entries for every device ever used accumulate indefinitely (keyed by Core Audio device UID/ID), bounded only by the coarse 10MB load-time cap which discards the entire file rather than pruning. Disabling the feature toggle also does not clear stored data. This is a real management/UX gap rather than a correctness crash, hence P3.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1M]",
      "errorsFound": "• [MEDIUM/dead-code] DeviceVolumePresets.removeDevice(_:) has zero callers — pruning/management is entirely unimplemented (Sources/Waves/Stores/UserPreferences.swift:111) — removeDevice(_:) is defined at UserPreferences.swift:111 but a grep across Sources/ returns only the definition (other 'removeDevice...' hits like removeDeviceChangeListener are unrelated and live in the backend). No code path removes presets when a device is unplugged/forgotten, and there is no UI to view, edit, reset, or delete per-device presets — only the on/off toggle. Entries accumulate for every Core Audio device ever used, bounded only by the destructive 10MB load-time cap (DeviceVolumePresetsStore.swift:39-43) which discards the whole file rather than pruning; disabling the toggle does not clear data. Confirmed via grep; corroborated by feature-status.json (lines 1928, 5490). | Fix: Wire removeDevice into a device-removal handler (prune entries for devices absent from availableOutputDevices) and/or add a Settings affordance to view/delete/reset a device's presets, plus a clear-all.",
      "fixStatus": "1 fixed",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "dead-code",
          "title": "DeviceVolumePresets.removeDevice(_:) has zero callers — pruning/management is entirely unimplemented",
          "detail": "removeDevice(_:) is defined at UserPreferences.swift:111 but a grep across Sources/ returns only the definition (other 'removeDevice...' hits like removeDeviceChangeListener are unrelated and live in the backend). No code path removes presets when a device is unplugged/forgotten, and there is no UI to view, edit, reset, or delete per-device presets — only the on/off toggle. Entries accumulate for every Core Audio device ever used, bounded only by the destructive 10MB load-time cap (DeviceVolumePresetsStore.swift:39-43) which discards the whole file rather than pruning; disabling the toggle does not clear data. Confirmed via grep; corroborated by feature-status.json (lines 1928, 5490).",
          "codeRef": "Sources/Waves/Stores/UserPreferences.swift:111",
          "expectedVsActual": "DVP-06: 'removeDevice is invoked somewhere' and 'a code path or UI removes presets for devices no longer present / that the user wants to reset'. Actual: removeDevice is dead code; no pruning on device removal; no management UI; growth bounded only by the destructive 10MB cap.",
          "suggestedFix": "Wire removeDevice into a device-removal handler (prune entries for devices absent from availableOutputDevices) and/or add a Settings affordance to view/delete/reset a device's presets, plus a clear-all.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/UserPreferences.swift",
          "disposition": "iter2:fixed",
          "uid": "DVP-06#1",
          "canonicalFile": "Sources/Waves/Stores/UserPreferences.swift",
          "fixStatus": "applied",
          "fixedBy": "iter2-deferred-reduction",
          "fixTier": "iter2"
        }
      ]
    },
    {
      "id": "PRE-01",
      "area": "Volume Presets (Save/Restore)",
      "feature": "Save current mix as a named preset",
      "priority": "P1",
      "userStory": "As a macOS user who has dialed in per-app volumes, I want to save the current mix as a named preset, so that I can recall that exact configuration later without re-adjusting every slider.",
      "expectedBehavior": [
        "A '+' toolbar button (help/label 'Save preset') in the main window opens a 'Save Preset' sheet with a text field and Cancel/Save buttons",
        "Save is disabled while the trimmed name is empty; pressing Save (default action / Return) commits",
        "The name is trimmed of whitespace/newlines and rejected client-side if empty or longer than 100 characters",
        "On save, the backend captures every app in the current snapshot into PresetEntry values (appID, desiredVolume, isMuted, volumeBoost)",
        "Apps in the user's excluded list are filtered out of the saved entries so re-applying won't re-tap them",
        "If a preset already exists with the same name (case-insensitive), it is overwritten in place — keeping the original id/name/createdAt and bumping updatedAt — instead of creating a duplicate",
        "Otherwise the new preset is appended to the library and the whole list is persisted via PresetStore",
        "A success toast 'Preset saved' with the name is shown; backend failures show a 'Save failed' toast"
      ],
      "acceptanceCriteria": [
        "Saving with a non-empty name creates a preset whose entries reflect current app volumes/mute/boost",
        "Saving with a name matching an existing preset (any case) replaces it rather than duplicating, preserving the original id and createdAt",
        "Excluded app IDs never appear in the saved preset's entries",
        "A name >100 chars or empty/whitespace-only does not create a preset",
        "After save, the preset appears in the Settings Presets list and the sidebar Presets section",
        "Saved presets survive an app relaunch (written to presets.json)"
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MainWindowView.swift:26",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:137",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:592",
        "Sources/Waves/Stores/AppStore.swift:1031",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:188",
        "Sources/WavesAudioCore/Models/Preset.swift:25"
      ],
      "implStatus": "implemented",
      "implNotes": "Fully wired end-to-end. Two UX concerns: (1) saveCurrentPreset captures ALL apps in the snapshot (not just managed/active ones), so a saved preset can include dozens of momentary apps at their default volume=1.0/unmuted, which then re-asserts those volumes on every apply. (2) Name collision is resolved silently with no overwrite confirmation, so a user can clobber an existing preset by reusing a name. The 100-char limit is enforced only in the view's savePreset(), not in AppStore.savePreset, but the backend Preset has no length cap of its own.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1M 1L]",
      "errorsFound": "• [MEDIUM/ux] Save button silently no-ops for names over 100 characters (dead control, no feedback) (Sources/Waves/Features/Mixer/MainWindowView.swift:143) — In SavePresetSheet the Save button is disabled only when the trimmed name is empty (MainWindowView.swift:612). For a name >100 chars the button stays enabled, but savePreset() hits `guard trimmed.count <= maxLength else { return }` (line 143) and returns BEFORE store.savePreset or dismissPresetSheet(). No toast is shown and the sheet stays open, so Save/Return appears to do nothing. There is no length cap on the TextField, so a >100-char name is easy to produce via paste. | Fix: Disable Save when trimmed.count > 100 (mirror the empty-name disable at line 612) with inline helper text, or in savePreset() show a 'Name too long (max 100 characters)' warning toast in the >100 branch instead of a bare return.\n• [LOW/dead-code] Backend saveCurrentPreset appends to a write-only presets array that is never read (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:200) — WorkspaceAudioControlBackend.saveCurrentPreset does `presets.append(preset)` (line 200) on the backend's private `presets` property (declared line 13, seeded line 25). I grepped every use of `presets` in the file: it is written at init and at line 200 but never read anywhere. The authoritative library lives in AppStore.presets and is persisted via presetStore.save; the backend copy silently accumulates every saved preset for the process lifetime and is dead state. | Fix: Remove the `presets` stored property and the append; have saveCurrentPreset return the constructed Preset without mutating backend state.",
      "fixStatus": "2 fixed",
      "fixNotes": "tier2: FU-PRESET-NAME-LENGTH-DISABLE | tier3: FU-DEAD-PRESETS-ARRAY",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "ux",
          "title": "Save button silently no-ops for names over 100 characters (dead control, no feedback)",
          "detail": "In SavePresetSheet the Save button is disabled only when the trimmed name is empty (MainWindowView.swift:612). For a name >100 chars the button stays enabled, but savePreset() hits `guard trimmed.count <= maxLength else { return }` (line 143) and returns BEFORE store.savePreset or dismissPresetSheet(). No toast is shown and the sheet stays open, so Save/Return appears to do nothing. There is no length cap on the TextField, so a >100-char name is easy to produce via paste.",
          "codeRef": "Sources/Waves/Features/Mixer/MainWindowView.swift:143",
          "expectedVsActual": "Story: a name >100 chars 'does not create a preset' — acceptable, but a usable control should communicate why. Actual: the Save control silently does nothing with zero feedback and the sheet does not dismiss, reading as a broken button.",
          "suggestedFix": "Disable Save when trimmed.count > 100 (mirror the empty-name disable at line 612) with inline helper text, or in savePreset() show a 'Name too long (max 100 characters)' warning toast in the >100 branch instead of a bare return.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "disposition": "tier2:applied:FU-PRESET-NAME-LENGTH-DISABLE",
          "uid": "PRE-01#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-PRESET-NAME-LENGTH-DISABLE",
          "fixTier": "tier2"
        },
        {
          "severity": "low",
          "type": "dead-code",
          "title": "Backend saveCurrentPreset appends to a write-only presets array that is never read",
          "detail": "WorkspaceAudioControlBackend.saveCurrentPreset does `presets.append(preset)` (line 200) on the backend's private `presets` property (declared line 13, seeded line 25). I grepped every use of `presets` in the file: it is written at init and at line 200 but never read anywhere. The authoritative library lives in AppStore.presets and is persisted via presetStore.save; the backend copy silently accumulates every saved preset for the process lifetime and is dead state.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:200",
          "expectedVsActual": "Story: saved preset is appended to the AppStore library and persisted via PresetStore. Actual: that happens in AppStore, but the backend also keeps a parallel, never-read, never-persisted copy.",
          "suggestedFix": "Remove the `presets` stored property and the append; have saveCurrentPreset return the constructed Preset without mutating backend state.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "tier3:applied:FU-DEAD-PRESETS-ARRAY",
          "uid": "PRE-01#2",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-DEAD-PRESETS-ARRAY",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "PRE-02",
      "area": "Volume Presets (Save/Restore)",
      "feature": "Apply a saved preset to restore a mix",
      "priority": "P0",
      "userStory": "As a user switching contexts (focus, meeting, gaming), I want to apply a saved preset, so that all my app volumes, mutes, and boosts snap back to that known configuration in one click.",
      "expectedBehavior": [
        "Saved presets are listed in the main-window sidebar 'Presets' section and in the menu-bar 'Presets' quick picker; clicking one calls applyPreset",
        "applyPreset first strips entries for currently-excluded apps so they are never re-tapped",
        "The backend walks each preset entry, finds the matching app by logicalID, and sets desiredVolume, isMuted, and volumeBoost, then applies the route",
        "appliedVolume is set to 0 when muted or to desiredVolume otherwise; successful apps move to routingState .managed",
        "On success the session snapshot is refreshed, diagnostics updated, auto-pause-music re-checked, the snapshot persisted, and a 'Preset applied' toast shown",
        "On a thrown error the store falls back to the backend's current snapshot, refreshes diagnostics, persists, and shows a 'Preset apply failed' error toast"
      ],
      "acceptanceCriteria": [
        "Applying a preset sets each matching running app's volume/mute/boost to the stored values",
        "Apps in the preset that are not currently present in the snapshot are skipped without aborting the rest",
        "Excluded apps are not affected by an applied preset even if the preset contains them",
        "A successful apply shows the 'Preset applied' toast with the preset name; a backend failure shows an error toast and leaves the session in a consistent state",
        "Per-app route failures during apply mark that app's routingState as .error and surface lastError without failing the whole apply"
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MainWindowView.swift:263",
        "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:160",
        "Sources/Waves/Stores/AppStore.swift:1001",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:156"
      ],
      "implStatus": "implemented",
      "implNotes": "Core restore path is solid and resilient (per-entry errors don't abort the whole apply). Gap: entries whose appID isn't currently in the snapshot are silently skipped (continue) with no user feedback, so applying a 'Focus' preset when Spotify isn't running quietly does nothing for Spotify and the toast still says 'Preset applied' — the user can't tell which apps were actually restored. Per-app route errors are aggregated into a single backendStatus.lastError and a generic success toast, so partial failures may not be obvious.",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [1M 2L]",
      "errorsFound": "• [MEDIUM/inconsistency] Applying a named preset does not update per-device presets, so a later device switch silently reverts it (Sources/Waves/Stores/AppStore.swift:1001) — applyPreset (AppStore.swift:1001) sets each app's desiredVolume/isMuted/volumeBoost via the backend but never writes the new values into deviceVolumePresets. I grepped every deviceVolumePresets.saveVolumeSettings call — only setDesiredVolume (380), setMuted (514), setVolumeBoost (561) update it, never applyPreset. With per-device presets ON (default), the per-device store keeps the pre-preset volume for the current device; on the next output-device switch handleDeviceChange -> restoreDeviceVolumePresets (848) re-applies those stale stored settings, silently undoing the preset the user just applied for any app that has a prior per-device entry. | Fix: After a successful applyPreset, when enablePerDeviceVolumePresets and a currentDeviceID exist, write each applied app's settings into deviceVolumePresets (and persist), so the named-preset result and the per-device store agree.\n• [LOW/dead-code] Store's 'Preset apply failed' fallback is unreachable with the shipped backend (Sources/Waves/Stores/AppStore.swift:1021) — WorkspaceAudioControlBackend.applyPreset (line 156) is `async throws` but its body never propagates a throw: the only throwing call (applyRoute) is in a per-entry do/catch that swallows the error and marks that app .error (lines 166-181); firstIndex/array access cannot throw. So the catch in AppStore.applyPreset (line 1021) that resets `session = await backend.currentSnapshot()` and shows the 'Preset apply failed' toast can never fire on the production path. The protocol declares throws, so the catch is defensible for other conformers but is dead for the shipped backend. | Fix: Either keep/document the catch as defensive for non-shipping backends, or have the backend throw when zero entries could be applied / a fatal failure occurs so the user-facing 'Preset apply failed' path is reachable.\n• [LOW/ux] Menu-bar Presets quick picker shows an empty, dead menu when no presets exist (Sources/Waves/Features/Mixer/MenuBarMixerView.swift:164) — PresetQuickPicker (MenuBarMixerView.swift:160) is rendered unconditionally (line 40) and ForEach's over store.presets with no empty-state row. With zero presets (e.g. after deleting both seeded defaults) the menu opens to nothing — unlike the main-window sidebar which hides the Presets section entirely when presets.isEmpty (MainWindowView.swift:261). A state-desync between the menu-bar and main-window surfaces. | Fix: When store.presets.isEmpty, hide the picker or show a disabled 'No presets yet' item, matching the sidebar's conditional rendering.",
      "fixStatus": "2 fixed · 1 n/a",
      "fixNotes": "tier2: FU-PRESET-DEVICEPRESET-SYNC | tier3: FU-MENU-PRESET-PICKER-EMPTY",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "inconsistency",
          "title": "Applying a named preset does not update per-device presets, so a later device switch silently reverts it",
          "detail": "applyPreset (AppStore.swift:1001) sets each app's desiredVolume/isMuted/volumeBoost via the backend but never writes the new values into deviceVolumePresets. I grepped every deviceVolumePresets.saveVolumeSettings call — only setDesiredVolume (380), setMuted (514), setVolumeBoost (561) update it, never applyPreset. With per-device presets ON (default), the per-device store keeps the pre-preset volume for the current device; on the next output-device switch handleDeviceChange -> restoreDeviceVolumePresets (848) re-applies those stale stored settings, silently undoing the preset the user just applied for any app that has a prior per-device entry.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1001",
          "expectedVsActual": "Story: applying a preset sets each matching app's volume/mute/boost and leaves the session in a consistent state. Actual: with the (default-on) per-device feature, the applied values are not mirrored into the per-device store, so they can be reverted on the next device switch — a silent state-desync between the two preset subsystems.",
          "suggestedFix": "After a successful applyPreset, when enablePerDeviceVolumePresets and a currentDeviceID exist, write each applied app's settings into deviceVolumePresets (and persist), so the named-preset result and the per-device store agree.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier2:applied:FU-PRESET-DEVICEPRESET-SYNC",
          "uid": "PRE-02#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-PRESET-DEVICEPRESET-SYNC",
          "fixTier": "tier2"
        },
        {
          "severity": "low",
          "type": "dead-code",
          "title": "Store's 'Preset apply failed' fallback is unreachable with the shipped backend",
          "detail": "WorkspaceAudioControlBackend.applyPreset (line 156) is `async throws` but its body never propagates a throw: the only throwing call (applyRoute) is in a per-entry do/catch that swallows the error and marks that app .error (lines 166-181); firstIndex/array access cannot throw. So the catch in AppStore.applyPreset (line 1021) that resets `session = await backend.currentSnapshot()` and shows the 'Preset apply failed' toast can never fire on the production path. The protocol declares throws, so the catch is defensible for other conformers but is dead for the shipped backend.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1021",
          "expectedVsActual": "Story: 'On a thrown error the store falls back to the backend's current snapshot ... and shows a Preset apply failed error toast.' Actual: the shipped backend never throws from applyPreset, so that error toast/fallback is never exercised; per-app route failures instead surface only as .error routingState + backendStatus.lastError.",
          "suggestedFix": "Either keep/document the catch as defensive for non-shipping backends, or have the backend throw when zero entries could be applied / a fatal failure occurs so the user-facing 'Preset apply failed' path is reachable.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "PRE-02#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "low",
          "type": "ux",
          "title": "Menu-bar Presets quick picker shows an empty, dead menu when no presets exist",
          "detail": "PresetQuickPicker (MenuBarMixerView.swift:160) is rendered unconditionally (line 40) and ForEach's over store.presets with no empty-state row. With zero presets (e.g. after deleting both seeded defaults) the menu opens to nothing — unlike the main-window sidebar which hides the Presets section entirely when presets.isEmpty (MainWindowView.swift:261). A state-desync between the menu-bar and main-window surfaces.",
          "codeRef": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:164",
          "expectedVsActual": "Story: 'Saved presets are listed in ... the menu-bar Presets quick picker.' Actual: when there are no presets the picker still appears but opens to an empty menu, a dead control with no guidance.",
          "suggestedFix": "When store.presets.isEmpty, hide the picker or show a disabled 'No presets yet' item, matching the sidebar's conditional rendering.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift",
          "disposition": "tier3:applied:FU-MENU-PRESET-PICKER-EMPTY",
          "uid": "PRE-02#3",
          "canonicalFile": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-MENU-PRESET-PICKER-EMPTY",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "PRE-03",
      "area": "Volume Presets (Save/Restore)",
      "feature": "Manage the preset library (list, empty state, delete)",
      "priority": "P2",
      "userStory": "As a user accumulating presets, I want to see and delete my saved presets in Settings, so that I can keep the library tidy and remove ones I no longer use.",
      "expectedBehavior": [
        "Settings has a 'Presets' tab showing each preset's name and an entry count ('N app'/'N apps')",
        "When no presets exist, an empty-state line is shown: 'No presets yet. Save a mix from the main window, or import one.'",
        "Each row has a per-row 'Delete' (destructive) button with accessibility label 'Delete preset <name>', and the List also supports swipe/onDelete",
        "deletePresets removes the preset(s) at the given offsets, persists the updated list, and shows a 'Preset removed' info toast",
        "First launch with no presets.json seeds two default presets ('Focus', 'Meeting') onto disk and into the library"
      ],
      "acceptanceCriteria": [
        "The Presets settings tab lists all saved presets with correct singular/plural app counts",
        "Deleting a preset removes it from the list and from presets.json and shows a 'Preset removed' toast",
        "With zero presets the empty-state message is displayed",
        "On a clean install the 'Focus' and 'Meeting' default presets appear without user action"
      ],
      "codeRefs": [
        "Sources/Waves/Settings/SettingsView.swift:210",
        "Sources/Waves/Stores/AppStore.swift:1066",
        "Sources/Waves/Services/Persistence/PresetStore.swift:30",
        "Sources/WavesAudioCore/Backend/AudioSessionSnapshot+Preview.swift:121"
      ],
      "implStatus": "implemented",
      "implNotes": "List/delete/empty-state are complete. There is no rename or reorder affordance — to change a preset's name the user must delete and re-save, or import a renamed JSON. The seeded default presets ('Focus'/'Meeting') hardcode legacy entries without volumeBoost (defaulting to 1.0); they reference fixed bundle IDs (Safari, Zoom, Spotify, Discord) that most users won't all have, so applying a default preset often touches only a subset of apps. Delete is immediate with no confirmation (only a toast), though it is reversible only by re-saving.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "PRE-04",
      "area": "Volume Presets (Save/Restore)",
      "feature": "Export and import presets as JSON",
      "priority": "P2",
      "userStory": "As a user who wants to back up or share my configurations, I want to export a preset to a JSON file and import presets from JSON, so that I can move them between machines or share them with others.",
      "expectedBehavior": [
        "Each preset row in Settings has an 'Export' button that opens an NSSavePanel pre-filled with '<name>.json' (JSON content type) and writes the pretty-printed encoded preset atomically",
        "An 'Import' button opens an NSOpenPanel limited to single JSON files",
        "Import rejects files larger than 10MB, presets with an empty name, and presets with more than 1000 entries, each with a specific error toast",
        "On import, a name collision (case-insensitive) overwrites the existing preset in place preserving its id/name/createdAt; otherwise the imported preset is assigned a fresh UUID and createdAt before being appended",
        "After a successful import the library is persisted and a 'Preset imported' success toast is shown; export shows 'Preset exported'",
        "If no main window is available, export/import show a failure toast instead of crashing"
      ],
      "acceptanceCriteria": [
        "Exporting writes a valid, re-importable pretty-printed JSON file named after the preset",
        "Importing a well-formed preset adds it to the library and persists it",
        "Importing a file >10MB, with an empty name, or >1000 entries is rejected with the corresponding error toast and no library change",
        "Importing a preset whose name matches an existing one replaces that preset rather than duplicating it",
        "An imported non-colliding preset receives a new UUID so it never collides with an existing SwiftUI list identity"
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:1079",
        "Sources/Waves/Stores/AppStore.swift:1112",
        "Sources/Waves/Settings/SettingsView.swift:221",
        "Sources/Waves/Settings/SettingsView.swift:246"
      ],
      "implStatus": "implemented",
      "implNotes": "Robust import validation (size, name, entry count) and atomic export. Minor concern: imported PresetEntry values are sanitized by PresetEntry.init (appID truncated to 256, volume clamped 0–1, boost clamped 1–4), but there is no validation of the appID format itself, and import name-collision overwrites silently with no confirmation. Export uses a fresh JSONEncoder rather than PersistedSchema, so exported files lack the schema-version wrapper used internally — round-tripping works because import uses a plain JSONDecoder, but the on-disk export format diverges from the internal persisted format.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "PRE-05",
      "area": "Volume Presets (Save/Restore)",
      "feature": "Apply a preset via waves:// URL automation",
      "priority": "P3",
      "userStory": "As a power user automating my setup, I want to trigger a named preset from a waves://apply-preset URL, so that scripts, Shortcuts, or links can switch my mix hands-free.",
      "expectedBehavior": [
        "A waves://apply-preset?name=<name> URL is routed to handleURLApplyPreset",
        "The name query item is required and must be <=256 characters, otherwise a 'URL command blocked' warning toast is shown",
        "The preset is matched case-insensitively by name; if no match, a 'Preset not found: <name>' warning toast is shown (name truncated to 64 chars in the message)",
        "On a match, the normal applyPreset path runs (including excluded-app stripping and toasts)",
        "URL command handling is gated behind a user preference (URL automation is off by default)"
      ],
      "acceptanceCriteria": [
        "waves://apply-preset?name=Focus applies the 'Focus' preset when URL automation is enabled",
        "A missing/oversized name parameter produces a 'URL command blocked' toast and applies nothing",
        "An unknown preset name produces a 'Preset not found' toast and applies nothing",
        "Name matching is case-insensitive"
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:282",
        "Sources/Waves/Stores/AppStore.swift:330",
        "Sources/Waves/Settings/SettingsView.swift:125",
        "README.md:132"
      ],
      "implStatus": "implemented",
      "implNotes": "Clean, validated, and correctly reuses applyPreset so all the excluded-app and toast logic is consistent. Inherits PRE-02's limitation that apps not currently running are silently skipped. The 256-char name cap here is looser than the 100-char cap enforced when saving via the UI, so a URL could reference a name that the in-app save flow would never produce.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/inconsistency] Unknown-preset URL uses toast title 'URL command blocked' instead of 'Preset not found' (Sources/Waves/Stores/AppStore.swift:338) — handleURLApplyPreset, when no preset matches, calls showToast(title: \"URL command blocked\", detail: \"Preset not found: <name>\", ...) at AppStore.swift:338. The story's acceptance criteria describe a 'Preset not found' toast; the descriptive text lives only in the detail line while the prominent title says 'URL command blocked' — the same title used for a malformed/oversized name parameter (line 333), conflating two distinct failure modes. | Fix: Set the title to 'Preset not found' for the no-match case so it is distinguishable from the invalid-parameter 'URL command blocked' case and matches the story copy.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier3: FU-URL-FEEDBACK-WORDING",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "Unknown-preset URL uses toast title 'URL command blocked' instead of 'Preset not found'",
          "detail": "handleURLApplyPreset, when no preset matches, calls showToast(title: \"URL command blocked\", detail: \"Preset not found: <name>\", ...) at AppStore.swift:338. The story's acceptance criteria describe a 'Preset not found' toast; the descriptive text lives only in the detail line while the prominent title says 'URL command blocked' — the same title used for a malformed/oversized name parameter (line 333), conflating two distinct failure modes.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:338",
          "expectedVsActual": "Story: an unknown preset name 'produces a Preset not found toast'. Actual: title is 'URL command blocked' (identical to the invalid-parameter case); only the detail carries 'Preset not found:'.",
          "suggestedFix": "Set the title to 'Preset not found' for the no-match case so it is distinguishable from the invalid-parameter 'URL command blocked' case and matches the story copy.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier3:applied:FU-URL-FEEDBACK-WORDING",
          "uid": "PRE-05#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-URL-FEEDBACK-WORDING",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "PRE-06",
      "area": "Volume Presets (Save/Restore)",
      "feature": "Per-device automatic volume presets (save/restore per output device)",
      "priority": "P1",
      "userStory": "As a user who switches between speakers, headphones, and a dock, I want each app's volume/mute/boost remembered separately per output device and auto-restored when I switch devices, so that my headphone mix doesn't follow me onto loud desk speakers.",
      "expectedBehavior": [
        "When the 'Per-device volume presets' preference is enabled (on by default), changing an app's volume/mute/boost stores an AppVolumeSettings keyed by deviceID and app logicalID in DeviceVolumePresets",
        "In-memory presets update on every change but disk writes are deferred/coalesced to avoid thrashing DeviceVolumePresetsStore",
        "On detecting an output-device change (previousDeviceID != newDeviceID), restoreDeviceVolumePresets re-applies each app's stored settings for the new device via the backend and re-persists the session",
        "Stored settings clamp volumeBoost to [1.0,4.0]; decoding tolerates missing fields with defaults (volume 1.0, unmuted, boost 1.0)",
        "Persistence uses a separate deviceVolumePresets.json file with 10MB size guard and corrupt-file backup, defaulting to empty presets on first launch or unreadable data"
      ],
      "acceptanceCriteria": [
        "With the feature on, adjusting an app's volume while on device A and switching to device B restores B's previously-saved volume for that app",
        "Per-device data is persisted to deviceVolumePresets.json and survives relaunch",
        "With the feature off, device switches do not rewrite app volumes from stored per-device data",
        "A corrupt or oversized deviceVolumePresets.json is backed up to .corrupt and the app falls back to empty presets without losing the bad file",
        "First launch with no file returns empty presets and writes nothing until a change occurs"
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:371",
        "Sources/Waves/Stores/AppStore.swift:802",
        "Sources/Waves/Stores/AppStore.swift:848",
        "Sources/Waves/Stores/UserPreferences.swift:97",
        "Sources/Waves/Services/Persistence/DeviceVolumePresetsStore.swift:29"
      ],
      "implStatus": "implemented",
      "implNotes": "This is a separate auto-preset subsystem from the named-preset library and is the README's 'Per-Device Volume Presets' feature. It is fully wired but has edge-case gaps: restoreDeviceVolumePresets logs per-app backend failures but surfaces nothing to the user, so a failed restore after a device swap is silent. There is no UI to inspect, edit, or clear per-device stored volumes (only removeDevice exists on the model and isn't exposed); stale entries accumulate per deviceID indefinitely. Restore depends on a correct currentDeviceID at switch time — if device identity changes (e.g. a renamed/aggregate device), stored settings won't match and won't restore.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1H 1L]",
      "errorsFound": "• [HIGH/logic] Per-device volume restore is gated behind autoRestoreDevice, not just enablePerDeviceVolumePresets (Sources/Waves/Stores/AppStore.swift:793) — handleDeviceChange begins with `guard preferences.autoRestoreDevice else { return }` (AppStore.swift:793); the per-device restore call restoreDeviceVolumePresets (line 803) sits inside that guard, as does the entire snapshot refresh / availableDevices refresh / toast. autoRestoreDevice and enablePerDeviceVolumePresets are two independent Settings toggles (SettingsView.swift:79 and :106) with no documented dependency; both default true so the default config works. But if a user turns OFF 'Auto-restore device' while leaving 'Per-device volume presets' ON, the whole device-change handler returns immediately — per-device volumes are still saved on change but never re-applied on a switch. selectOutputDevice (line 759) routes manual switches through the same event, so even an explicit device switch won't restore. | Fix: Decouple the features: in handleDeviceChange refresh the snapshot and call restoreDeviceVolumePresets when enablePerDeviceVolumePresets is on, regardless of autoRestoreDevice; let autoRestoreDevice gate only the managed-route re-establishment. Move the restoreDeviceVolumePresets branch out from under the autoRestoreDevice guard.\n• [LOW/inconsistency] Disk-write coalescing only applies to volume; mute and boost write the file on every change (Sources/Waves/Stores/AppStore.swift:517) — For volume, the in-memory preset updates per tick and the disk write defers to commitDesiredVolume (AppStore.swift:389-391), matching the story's coalescing claim. But setMuted (lines 516-518) and setVolumeBoost (lines 562-564) each call deviceVolumePresetsStore.save(deviceVolumePresets) inside a Task on EVERY change with no coalescing. Boost can be stepped rapidly, so the full deviceVolumePresets.json is re-encoded and rewritten per change. | Fix: Route mute/boost per-device persistence through the same deferred/coalesced commit used for volume (or a debounced save), so rapid boost/mute changes do not rewrite the file each time.",
      "fixStatus": "1 fixed · 1 deferred",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "logic",
          "title": "Per-device volume restore is gated behind autoRestoreDevice, not just enablePerDeviceVolumePresets",
          "detail": "handleDeviceChange begins with `guard preferences.autoRestoreDevice else { return }` (AppStore.swift:793); the per-device restore call restoreDeviceVolumePresets (line 803) sits inside that guard, as does the entire snapshot refresh / availableDevices refresh / toast. autoRestoreDevice and enablePerDeviceVolumePresets are two independent Settings toggles (SettingsView.swift:79 and :106) with no documented dependency; both default true so the default config works. But if a user turns OFF 'Auto-restore device' while leaving 'Per-device volume presets' ON, the whole device-change handler returns immediately — per-device volumes are still saved on change but never re-applied on a switch. selectOutputDevice (line 759) routes manual switches through the same event, so even an explicit device switch won't restore.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:793",
          "expectedVsActual": "Story: restore runs 'On detecting an output-device change (previousDeviceID != newDeviceID)' gated by enablePerDeviceVolumePresets (on by default). Actual: restore is additionally and silently coupled to autoRestoreDevice; disabling that one independent setting disables per-device restore (and the device-change snapshot refresh) entirely even with the per-device feature on.",
          "suggestedFix": "Decouple the features: in handleDeviceChange refresh the snapshot and call restoreDeviceVolumePresets when enablePerDeviceVolumePresets is on, regardless of autoRestoreDevice; let autoRestoreDevice gate only the managed-route re-establishment. Move the restoreDeviceVolumePresets branch out from under the autoRestoreDevice guard.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:verified-in-code",
          "uid": "PRE-06#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-DEVICECHANGE-GATING/FU-MENU-SECTION-DEDUP",
          "fixTier": "tier1"
        },
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "Disk-write coalescing only applies to volume; mute and boost write the file on every change",
          "detail": "For volume, the in-memory preset updates per tick and the disk write defers to commitDesiredVolume (AppStore.swift:389-391), matching the story's coalescing claim. But setMuted (lines 516-518) and setVolumeBoost (lines 562-564) each call deviceVolumePresetsStore.save(deviceVolumePresets) inside a Task on EVERY change with no coalescing. Boost can be stepped rapidly, so the full deviceVolumePresets.json is re-encoded and rewritten per change.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:517",
          "expectedVsActual": "Story PRE-06: 'In-memory presets update on every change but disk writes are deferred/coalesced to avoid thrashing DeviceVolumePresetsStore.' Actual: only the volume path defers writes; mute and boost write to disk immediately on every change.",
          "suggestedFix": "Route mute/boost per-device persistence through the same deferred/coalesced commit used for volume (or a debounced save), so rapid boost/mute changes do not rewrite the file each time.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-user-decision:setMuted (AppStore.swift:602-610) and setVolumeBoost still save the full deviceVolumePresets file on every change with no coalescing, unlike the volume path. Ro",
          "uid": "PRE-06#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "PRE-07",
      "area": "Volume Presets (Save/Restore)",
      "feature": "Preset persistence integrity and corruption recovery",
      "priority": "P1",
      "userStory": "As a user who relies on my saved presets, I want them stored durably and protected when the file is damaged, so that a corrupt file never silently destroys my whole preset library.",
      "expectedBehavior": [
        "Presets persist to Application Support/Waves/presets.json, with a ~/.Waves fallback directory if Application Support is unavailable",
        "Loading checks file size against a 10MB cap and, if exceeded, backs up the file (.corrupt) and returns defaults",
        "If decoding fails, the unreadable file is preserved by moving it to presets.json.corrupt and defaults are returned (the user's data is not destroyed in place)",
        "Saves are serialized on a dedicated queue and written atomically; load uses the PersistedSchema versioned codec",
        "A missing presets.json on first launch is treated as normal and seeded with default presets"
      ],
      "acceptanceCriteria": [
        "A valid presets.json round-trips through save/load without data loss",
        "A corrupt or oversized presets.json is moved to a .corrupt backup and the app continues with defaults instead of crashing or wiping data",
        "Concurrent saves do not interleave or produce partially-written files (atomic write on a serial queue)",
        "First launch seeds and persists the default presets"
      ],
      "codeRefs": [
        "Sources/Waves/Services/Persistence/PresetStore.swift:13",
        "Sources/Waves/Services/Persistence/PresetStore.swift:30",
        "Sources/Waves/Services/Persistence/PresetStore.swift:59",
        "Sources/Waves/Services/Persistence/PresetStore.swift:70"
      ],
      "implStatus": "implemented",
      "implNotes": "Solid defensive persistence: corrupt files are preserved rather than overwritten, size is bounded, writes are atomic and queue-serialized. One subtlety: on a corrupt file the loader returns Preset.defaults, which the caller then holds in memory; the next save will overwrite the (now backed-up) path with defaults, so without manual recovery from the .corrupt file the user effectively reverts to the two seeded presets. The backup uses a single fixed .corrupt name, so a second corruption overwrites the first backup.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "SHARE-01",
      "area": "Preset Sharing (Export/Import JSON)",
      "feature": "Export a preset to a JSON file",
      "priority": "P2",
      "userStory": "As a Waves user, I want to export a saved preset to a JSON file, so that I can back it up or send my mix to someone else.",
      "expectedBehavior": [
        "Each preset row in Settings > Presets shows an 'Export' button (SettingsView.swift:246-250).",
        "Tapping Export encodes the chosen Preset with a pretty-printed JSONEncoder (AppStore.swift:1082-1084).",
        "An NSSavePanel opens as a sheet on the main window, restricted to the .json content type, with the default filename pre-filled as '<preset name>.json' and directory creation allowed (AppStore.swift:1086-1096).",
        "If the user confirms (response == .OK) the JSON is written to the chosen URL atomically and a success toast 'Preset exported / Saved to <filename>' is shown for ~2s (AppStore.swift:1097-1105).",
        "If no main window is available, a 'Export failed / No main window available' error toast is shown and nothing is written (AppStore.swift:1091-1094).",
        "Any thrown error (encode or write failure) is surfaced as an 'Export failed' toast with the error's localizedDescription (AppStore.swift:1106-1108)."
      ],
      "acceptanceCriteria": [
        "Given a saved preset, when I press its Export button and pick a location, then a .json file containing that preset's name, entries (appID, desiredVolume, isMuted, volumeBoost), id and timestamps is written to disk.",
        "The save panel's default filename equals the preset name with a .json extension.",
        "On a successful save a success toast naming the file appears; cancelling the panel writes nothing and shows no toast.",
        "If the write fails or no main window exists, an error toast appears and no partial file replaces an existing one (write is atomic)."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:1079",
        "Sources/Waves/Stores/AppStore.swift:1082",
        "Sources/Waves/Stores/AppStore.swift:1086",
        "Sources/Waves/Stores/AppStore.swift:1096",
        "Sources/Waves/Stores/AppStore.swift:1098",
        "Sources/Waves/Settings/SettingsView.swift:246"
      ],
      "implStatus": "implemented",
      "implNotes": "Fully wired and functional. Correctness concern: export uses a raw JSONEncoder on a bare Preset, so the exported file has NO schemaVersion envelope, whereas the on-disk presets.json is wrapped by PersistedSchema (schemaVersion:1). The two formats are therefore not interchangeable and exported files carry no version for forward-compat. Minor leak of internal state: the exported JSON includes the preset's id, createdAt and updatedAt, which are meaningless/ignored on the recipient's machine (import reassigns them) — harmless but noisy for a 'share' format. No filename sanitization, but NSSavePanel handles that.",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [1H 1M 1L]",
      "errorsFound": "• [HIGH/ux] Export save panel attaches to NSApp.mainWindow, not the Settings window hosting the button (Sources/Waves/Stores/AppStore.swift:1091) — exportPreset() guards on NSApp.mainWindow and presents the NSSavePanel via beginSheetModal(for:) on it. The Export button lives in PresetSettingsView inside the Settings scene, which can be opened directly from the menu bar (MenuBarMixerView.swift:65,84) without the mixer Window ever being created, and the mixer Window can be closed independently. When only Settings is open, NSApp.mainWindow may be nil -> guard fires and shows 'Export failed / No main window available', making Export unusable. When the mixer window is open behind Settings, the panel attaches to the mixer window the user isn't looking at. | Fix: Target the window hosting the control: prefer NSApp.keyWindow ?? NSApp.mainWindow, or pass the hosting window down from SettingsView. Only show the error path when no usable window exists at all.\n• [MEDIUM/inconsistency] Exported file is a bare unversioned Preset, diverging from the versioned presets.json envelope (Sources/Waves/Stores/AppStore.swift:1082) — exportPreset() encodes a bare Preset with a plain JSONEncoder (1082-1084). PresetStore persists via PersistedSchema.encode, which wraps an ARRAY of presets in a VersionedPayload envelope ({schemaVersion, payload}) (PresetStore.swift:74, PersistedSchema.swift:17-18). The exported share file therefore carries no schemaVersion (no forward-compat marker) and is a single bare object rather than the versioned-array shape of presets.json, so the two formats are not interchangeable. | Fix: Wrap the export in a versioned envelope (VersionedPayload or a dedicated single-preset share envelope carrying schemaVersion) and teach the importer to accept it.\n• [LOW/accessibility] Per-row Export button has no accessibility label identifying its preset (Sources/Waves/Settings/SettingsView.swift:246) — In PresetSettingsView the Delete button gets .accessibilityLabel(\"Delete preset \\(preset.name)\") (SettingsView.swift:259) but the sibling Export button (246-250) has none. VoiceOver announces every row's Export button simply as 'Export', so a user cannot tell which preset will be exported. | Fix: Add .accessibilityLabel(\"Export preset \\(preset.name)\") to the Export button.",
      "fixStatus": "3 fixed",
      "fixNotes": "tier1: FU-PANEL-WINDOW-ANCHOR | tier3: FU-EXPORT-A11Y-LABEL",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "ux",
          "title": "Export save panel attaches to NSApp.mainWindow, not the Settings window hosting the button",
          "detail": "exportPreset() guards on NSApp.mainWindow and presents the NSSavePanel via beginSheetModal(for:) on it. The Export button lives in PresetSettingsView inside the Settings scene, which can be opened directly from the menu bar (MenuBarMixerView.swift:65,84) without the mixer Window ever being created, and the mixer Window can be closed independently. When only Settings is open, NSApp.mainWindow may be nil -> guard fires and shows 'Export failed / No main window available', making Export unusable. When the mixer window is open behind Settings, the panel attaches to the mixer window the user isn't looking at.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1091",
          "expectedVsActual": "Expected: pressing Export in Settings always presents a save panel on the window hosting the control. Actual: it targets NSApp.mainWindow (the mixer window); if that is nil the action aborts with an error toast, and if it is open the sheet attaches to the wrong window.",
          "suggestedFix": "Target the window hosting the control: prefer NSApp.keyWindow ?? NSApp.mainWindow, or pass the hosting window down from SettingsView. Only show the error path when no usable window exists at all.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:FU-PANEL-WINDOW-ANCHOR",
          "uid": "SHARE-01#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-PANEL-WINDOW-ANCHOR",
          "fixTier": "tier1"
        },
        {
          "severity": "medium",
          "type": "inconsistency",
          "title": "Exported file is a bare unversioned Preset, diverging from the versioned presets.json envelope",
          "detail": "exportPreset() encodes a bare Preset with a plain JSONEncoder (1082-1084). PresetStore persists via PersistedSchema.encode, which wraps an ARRAY of presets in a VersionedPayload envelope ({schemaVersion, payload}) (PresetStore.swift:74, PersistedSchema.swift:17-18). The exported share file therefore carries no schemaVersion (no forward-compat marker) and is a single bare object rather than the versioned-array shape of presets.json, so the two formats are not interchangeable.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1082",
          "expectedVsActual": "Expected: a self-describing, version-tagged share format consistent with how the app stores presets. Actual: a bare, unversioned Preset that diverges from the persisted envelope format.",
          "suggestedFix": "Wrap the export in a versioned envelope (VersionedPayload or a dedicated single-preset share envelope carrying schemaVersion) and teach the importer to accept it.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "gapfill:fixed",
          "uid": "SHARE-01#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "gapfill",
          "fixTier": "gapfill"
        },
        {
          "severity": "low",
          "type": "accessibility",
          "title": "Per-row Export button has no accessibility label identifying its preset",
          "detail": "In PresetSettingsView the Delete button gets .accessibilityLabel(\"Delete preset \\(preset.name)\") (SettingsView.swift:259) but the sibling Export button (246-250) has none. VoiceOver announces every row's Export button simply as 'Export', so a user cannot tell which preset will be exported.",
          "codeRef": "Sources/Waves/Settings/SettingsView.swift:246",
          "expectedVsActual": "Expected: each Export control identifies its preset to assistive tech, matching the Delete control. Actual: all Export buttons read identically as 'Export'.",
          "suggestedFix": "Add .accessibilityLabel(\"Export preset \\(preset.name)\") to the Export button.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Settings/SettingsView.swift",
          "disposition": "tier3:applied:FU-EXPORT-A11Y-LABEL",
          "uid": "SHARE-01#3",
          "canonicalFile": "Sources/Waves/Settings/SettingsView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-EXPORT-A11Y-LABEL",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "SHARE-02",
      "area": "Preset Sharing (Export/Import JSON)",
      "feature": "Import a preset from a JSON file",
      "priority": "P2",
      "userStory": "As a Waves user, I want to import a preset from a JSON file, so that I can use a mix someone shared with me or restore one I backed up.",
      "expectedBehavior": [
        "An 'Import' button sits in the Presets settings header (SettingsView.swift:221-224).",
        "Tapping Import opens an NSOpenPanel sheet limited to .json files, single selection, files only (no directories) (AppStore.swift:1114-1118).",
        "If no main window is available, an 'Import failed / No main window available' toast is shown (AppStore.swift:1120-1123).",
        "On confirm, the file is decoded into a Preset with a plain JSONDecoder (AppStore.swift:1135-1137).",
        "If the file's name matches an existing preset case-insensitively, the existing preset is overwritten in place (keeping the existing id, name, createdAt; updatedAt set to now) (AppStore.swift:1150-1156).",
        "Otherwise the imported preset is appended with a fresh UUID and createdAt/updatedAt set to now, so it never collides with an existing preset's identity (AppStore.swift:1157-1165).",
        "The updated presets array is persisted via presetStore.save and a 'Preset imported / <name>' success toast is shown for ~2s (AppStore.swift:1167-1173).",
        "Imported presets immediately appear in the library and become applicable from the menu bar and main window preset lists (MenuBarMixerView.swift:165-167, MainWindowView.swift:263-265)."
      ],
      "acceptanceCriteria": [
        "Given a valid preset JSON whose name does not exist in my library, when I import it, then a new preset is added with a fresh id and now timestamps and appears in the list.",
        "Given a valid preset JSON whose name matches (case-insensitively) an existing preset, when I import it, then the existing preset is replaced in place (same id/name/createdAt, entries updated, updatedAt bumped) rather than duplicated.",
        "After a successful import, applying the imported preset from the menu bar or main window applies its entries.",
        "Cancelling the open panel or having no main window leaves the library unchanged (the latter shows an error toast)."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:1112",
        "Sources/Waves/Stores/AppStore.swift:1135",
        "Sources/Waves/Stores/AppStore.swift:1150",
        "Sources/Waves/Stores/AppStore.swift:1157",
        "Sources/Waves/Stores/AppStore.swift:1167",
        "Sources/Waves/Settings/SettingsView.swift:221"
      ],
      "implStatus": "implemented",
      "implNotes": "Wired end-to-end and imported presets are usable. Format-mismatch concern: import decodes a bare Preset with a raw JSONDecoder (no PersistedSchema). It will accept exactly what SHARE-01 exports, but it will NOT accept a presets.json copied from the app-support directory because that file is a VersionedPayload envelope wrapping an ARRAY of presets — so 'restore from a backup of presets.json' silently fails decoding (DecodingError → generic 'Import failed' toast). Also, because de-dup is by name only, importing a preset whose name collides overwrites the user's existing preset's entries without confirmation — potential surprise data loss; a renamed copy would be safer.",
      "source": "phase1-story",
      "testStatus": "4 issue(s) [2H 1M 1L]",
      "errorsFound": "• [HIGH/ux] Import open panel attaches to NSApp.mainWindow, so Import fails/mis-attaches when the mixer window is closed (Sources/Waves/Stores/AppStore.swift:1120) — importPreset() guards on NSApp.mainWindow and presents the NSOpenPanel via beginSheetModal(for:) on it (1120-1125). The Import button lives in the Settings header (SettingsView.swift:221), and Settings can be opened from the menu bar with the mixer Window never created or closed. When only Settings is open, NSApp.mainWindow may be nil -> Import aborts with 'Import failed / No main window available'; when the mixer window is open behind Settings, the open panel attaches to the wrong window. Same defect as SHARE-01. | Fix: Target the Settings/key window (NSApp.keyWindow ?? NSApp.mainWindow) for beginSheetModal, falling back to the error path only when no window exists.\n• [HIGH/error-handling] Importing a presets.json backup silently fails with a cryptic generic decode error (Sources/Waves/Stores/AppStore.swift:1135) — The user story explicitly covers 'restore one I backed up.' importPreset() decodes a bare Preset with a plain JSONDecoder (1135-1137). The app's own presets.json backup is a VersionedPayload<[Preset]> envelope (PresetStore.swift:74) — an object with schemaVersion/payload wrapping an array, not a single Preset. Decoding it throws a DecodingError caught at 1174-1175 and surfaced as error.localizedDescription, i.e. the cryptic 'The data couldn't be read because it isn't in the correct format.' The user gets no hint this file type is unsupported. | Fix: Make the importer accept multiple shapes: try VersionedPayload<[Preset]>/[Preset] (importing each with name de-dup) and fall back to a single bare Preset; show a clearer message when none match.\n• [MEDIUM/inconsistency] Import name handling is not trimmed unlike savePreset, defeating de-dup and allowing blank-looking names (Sources/Waves/Stores/AppStore.swift:1150) — importPreset() compares and stores preset.name raw (empty check at 1140, dedup caseInsensitiveCompare at 1150). savePreset() trims with trimmingCharacters(in: .whitespacesAndNewlines) before comparison and storage (1041-1042). Consequences: (1) a whitespace-only name ('   ') passes preset.name.isEmpty and creates a visibly blank preset; (2) importing 'Work ' (trailing space) does NOT match existing 'Work' (caseInsensitiveCompare differs by length), so it is appended as a near-duplicate instead of overwriting in place as the story requires. | Fix: Trim preset.name before the empty check and before the caseInsensitiveCompare dedup, and store the trimmed name, mirroring savePreset.\n• [LOW/inconsistency] Import success toast announces the imported file's name, not the name actually stored on overwrite (Sources/Waves/Stores/AppStore.swift:1170) — On the overwrite path the stored name is kept as the existing preset's name (imported.name = presets[existingIndex].name, line 1153), but the success toast reports detail: preset.name (the imported file's name) at 1170. If the imported file's name differs only by case or whitespace from the existing preset (e.g. existing 'work', imported 'WORK'), the toast says 'WORK' while the library still shows 'work', mismatching what the user sees in the list. | Fix: Use the stored name (the value placed in presets[existingIndex] / the appended preset) in the success toast detail.",
      "fixStatus": "4 fixed",
      "fixNotes": "tier1: FU-IMPORT-DECODE-SHAPE; FU-PANEL-WINDOW-ANCHOR | tier3: FU-IMPORT-PRESET-VALIDATION",
      "retestStatus": "PASS (remediated & verified)",
      "issues": [
        {
          "severity": "high",
          "type": "ux",
          "title": "Import open panel attaches to NSApp.mainWindow, so Import fails/mis-attaches when the mixer window is closed",
          "detail": "importPreset() guards on NSApp.mainWindow and presents the NSOpenPanel via beginSheetModal(for:) on it (1120-1125). The Import button lives in the Settings header (SettingsView.swift:221), and Settings can be opened from the menu bar with the mixer Window never created or closed. When only Settings is open, NSApp.mainWindow may be nil -> Import aborts with 'Import failed / No main window available'; when the mixer window is open behind Settings, the open panel attaches to the wrong window. Same defect as SHARE-01.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1120",
          "expectedVsActual": "Expected: Import in Settings always opens an open panel on the hosting window. Actual: it targets NSApp.mainWindow and fails or mis-attaches depending on whether the mixer window exists.",
          "suggestedFix": "Target the Settings/key window (NSApp.keyWindow ?? NSApp.mainWindow) for beginSheetModal, falling back to the error path only when no window exists.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:FU-PANEL-WINDOW-ANCHOR",
          "uid": "SHARE-02#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-PANEL-WINDOW-ANCHOR",
          "fixTier": "tier1"
        },
        {
          "severity": "high",
          "type": "error-handling",
          "title": "Importing a presets.json backup silently fails with a cryptic generic decode error",
          "detail": "The user story explicitly covers 'restore one I backed up.' importPreset() decodes a bare Preset with a plain JSONDecoder (1135-1137). The app's own presets.json backup is a VersionedPayload<[Preset]> envelope (PresetStore.swift:74) — an object with schemaVersion/payload wrapping an array, not a single Preset. Decoding it throws a DecodingError caught at 1174-1175 and surfaced as error.localizedDescription, i.e. the cryptic 'The data couldn't be read because it isn't in the correct format.' The user gets no hint this file type is unsupported.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1135",
          "expectedVsActual": "Expected per the user story: importing a backed-up presets.json restores those presets, or at least gives an actionable message. Actual: it fails to decode and shows a generic, cryptic 'Import failed' toast.",
          "suggestedFix": "Make the importer accept multiple shapes: try VersionedPayload<[Preset]>/[Preset] (importing each with name de-dup) and fall back to a single bare Preset; show a clearer message when none match.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:FU-IMPORT-DECODE-SHAPE",
          "uid": "SHARE-02#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-IMPORT-DECODE-SHAPE",
          "fixTier": "tier1"
        },
        {
          "severity": "medium",
          "type": "inconsistency",
          "title": "Import name handling is not trimmed unlike savePreset, defeating de-dup and allowing blank-looking names",
          "detail": "importPreset() compares and stores preset.name raw (empty check at 1140, dedup caseInsensitiveCompare at 1150). savePreset() trims with trimmingCharacters(in: .whitespacesAndNewlines) before comparison and storage (1041-1042). Consequences: (1) a whitespace-only name ('   ') passes preset.name.isEmpty and creates a visibly blank preset; (2) importing 'Work ' (trailing space) does NOT match existing 'Work' (caseInsensitiveCompare differs by length), so it is appended as a near-duplicate instead of overwriting in place as the story requires.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1150",
          "expectedVsActual": "Expected: case-insensitive name matching that overwrites the existing preset, consistent with how presets are saved/named. Actual: whitespace differences defeat de-dup and whitespace-only names are accepted.",
          "suggestedFix": "Trim preset.name before the empty check and before the caseInsensitiveCompare dedup, and store the trimmed name, mirroring savePreset.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "gapfill:already-resolved",
          "uid": "SHARE-02#3",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "earlier-tier"
        },
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "Import success toast announces the imported file's name, not the name actually stored on overwrite",
          "detail": "On the overwrite path the stored name is kept as the existing preset's name (imported.name = presets[existingIndex].name, line 1153), but the success toast reports detail: preset.name (the imported file's name) at 1170. If the imported file's name differs only by case or whitespace from the existing preset (e.g. existing 'work', imported 'WORK'), the toast says 'WORK' while the library still shows 'work', mismatching what the user sees in the list.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1170",
          "expectedVsActual": "Expected: the toast names the preset as it now appears in the library. Actual: it echoes the imported file's name, which can differ from the stored name on overwrite.",
          "suggestedFix": "Use the stored name (the value placed in presets[existingIndex] / the appended preset) in the success toast detail.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier3:applied:FU-IMPORT-PRESET-VALIDATION",
          "uid": "SHARE-02#4",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-IMPORT-PRESET-VALIDATION",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "SHARE-03",
      "area": "Preset Sharing (Export/Import JSON)",
      "feature": "Validation and error handling on import",
      "priority": "P2",
      "userStory": "As a Waves user, I want imports of malformed, oversized, or hostile JSON files to be rejected with a clear message, so that a bad share file can't corrupt my preset library or hurt the app.",
      "expectedBehavior": [
        "Before decoding, the file's size is read and any file larger than 10MB is rejected with an 'Import failed / File exceeds 10MB limit' toast (AppStore.swift:1128-1133).",
        "After decoding, a preset whose name is empty is rejected with 'Import failed / Preset name cannot be empty' (AppStore.swift:1140-1143).",
        "A preset with more than 1000 entries is rejected with 'Import failed / Preset has too many entries (max 1000)' (AppStore.swift:1145-1148).",
        "Any decode/read error throws into the catch block and shows 'Import failed' with the error's localizedDescription (AppStore.swift:1174-1176).",
        "Per-entry values are sanitized at the model layer regardless of file contents: appID is truncated to 256 chars, desiredVolume is clamped to [0,1], volumeBoost is clamped to [1,4], and a missing volumeBoost defaults to 1.0 (Preset.swift:32-57)."
      ],
      "acceptanceCriteria": [
        "Importing a >10MB file shows the size-limit error and changes nothing.",
        "Importing JSON that decodes to a preset with an empty name shows the empty-name error and changes nothing.",
        "Importing a preset with >1000 entries shows the too-many-entries error and changes nothing.",
        "Importing non-Preset or truncated JSON shows a generic 'Import failed' error and changes nothing.",
        "Importing a preset with out-of-range desiredVolume/volumeBoost or an over-long appID results in clamped/truncated values being stored, not the raw file values."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:1128",
        "Sources/Waves/Stores/AppStore.swift:1140",
        "Sources/Waves/Stores/AppStore.swift:1145",
        "Sources/Waves/Stores/AppStore.swift:1174",
        "Sources/WavesAudioCore/Models/Preset.swift:32",
        "Sources/WavesAudioCore/Models/Preset.swift:51"
      ],
      "implStatus": "implemented",
      "implNotes": "Reasonable defense-in-depth: size cap, name/entries-count checks, and model-level clamping of every numeric field via PresetEntry.init (which the custom decoder routes through). Gaps: (1) the size limit is read from file attributes before reading bytes, but a symlink/race is not considered (minor, local-only). (2) Decode errors surface the raw localizedDescription, which for JSON is cryptic ('The data couldn't be read because it isn't in the correct format'), giving the user little guidance — notably this is exactly what they'll hit if they try to import a presets.json backup (see SHARE-02). (3) There is no de-duplication or cap on the NUMBER of presets, only entries per preset.",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [1M 2L]",
      "errorsFound": "• [MEDIUM/error-handling] Decode failures surface a cryptic Foundation message instead of guidance (Sources/Waves/Stores/AppStore.swift:1174) — On any decode/read failure the catch at 1174-1175 shows error.localizedDescription. For JSON decode errors this is the unhelpful 'The data couldn't be read because it isn't in the correct format.' Users importing a wrong-but-plausible file (e.g. a presets.json backup per SHARE-02) get no actionable explanation of why it failed. | Fix: Map DecodingError to a fixed friendly detail string (e.g. 'This file isn't a valid Waves preset.') rather than passing through localizedDescription.\n• [LOW/edge-case] Empty-name validation accepts whitespace-only names (Sources/Waves/Stores/AppStore.swift:1140) — The post-decode validation rejects only preset.name.isEmpty (1140). A name of spaces/tabs/newlines ('   ') is not empty, so it passes and an effectively-nameless preset is stored, contradicting the spirit of acceptance criterion 2 ('empty name shows the empty-name error and changes nothing'). | Fix: Validate preset.name.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty.\n• [LOW/edge-case] File-size check stats a possibly-symlinked path separately from the byte read (minor TOCTOU) (Sources/Waves/Stores/AppStore.swift:1129) — The size guard uses FileManager.attributesOfItem(atPath:) (1129-1130), which resolves symlinks and reads metadata, then Data(contentsOf:) reads the bytes separately (1135). A symlink can point the size check at a small file while the read target differs, or the file can change between the two calls. Low impact because import is a local, user-initiated file pick, but the 10MB guard is not airtight. | Fix: Read the bytes once and enforce the cap on data.count, or stat the open file descriptor, rather than statting a path that may be a symlink.",
      "fixStatus": "3 fixed",
      "fixNotes": "tier3: FU-IMPORT-PRESET-VALIDATION",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "error-handling",
          "title": "Decode failures surface a cryptic Foundation message instead of guidance",
          "detail": "On any decode/read failure the catch at 1174-1175 shows error.localizedDescription. For JSON decode errors this is the unhelpful 'The data couldn't be read because it isn't in the correct format.' Users importing a wrong-but-plausible file (e.g. a presets.json backup per SHARE-02) get no actionable explanation of why it failed.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1174",
          "expectedVsActual": "Expected: a clear, user-facing reason ('This file isn't a Waves preset.'). Actual: the raw Foundation DecodingError description, which is opaque.",
          "suggestedFix": "Map DecodingError to a fixed friendly detail string (e.g. 'This file isn't a valid Waves preset.') rather than passing through localizedDescription.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "gapfill:already-resolved",
          "uid": "SHARE-03#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "earlier-tier"
        },
        {
          "severity": "low",
          "type": "edge-case",
          "title": "Empty-name validation accepts whitespace-only names",
          "detail": "The post-decode validation rejects only preset.name.isEmpty (1140). A name of spaces/tabs/newlines ('   ') is not empty, so it passes and an effectively-nameless preset is stored, contradicting the spirit of acceptance criterion 2 ('empty name shows the empty-name error and changes nothing').",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1140",
          "expectedVsActual": "Expected: blank/whitespace names are rejected as empty. Actual: only a zero-length string is rejected; whitespace-only passes.",
          "suggestedFix": "Validate preset.name.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier3:applied:FU-IMPORT-PRESET-VALIDATION",
          "uid": "SHARE-03#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-IMPORT-PRESET-VALIDATION",
          "fixTier": "tier3"
        },
        {
          "severity": "low",
          "type": "edge-case",
          "title": "File-size check stats a possibly-symlinked path separately from the byte read (minor TOCTOU)",
          "detail": "The size guard uses FileManager.attributesOfItem(atPath:) (1129-1130), which resolves symlinks and reads metadata, then Data(contentsOf:) reads the bytes separately (1135). A symlink can point the size check at a small file while the read target differs, or the file can change between the two calls. Low impact because import is a local, user-initiated file pick, but the 10MB guard is not airtight.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1129",
          "expectedVsActual": "Expected: the size cap reflects exactly the bytes that will be decoded. Actual: size is measured on a possibly-symlinked path in a separate syscall from the read.",
          "suggestedFix": "Read the bytes once and enforce the cap on data.count, or stat the open file descriptor, rather than statting a path that may be a symlink.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier3:applied:FU-IMPORT-PRESET-VALIDATION",
          "uid": "SHARE-03#3",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-IMPORT-PRESET-VALIDATION",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "SHARE-04",
      "area": "Preset Sharing (Export/Import JSON)",
      "feature": "Empty-state guidance for presets/import",
      "priority": "P3",
      "userStory": "As a new Waves user with no presets, I want the Presets settings pane to tell me how to get one, so that I understand I can save a mix or import a shared file.",
      "expectedBehavior": [
        "When store.presets is empty, the Presets pane shows the message 'No presets yet. Save a mix from the main window, or import one.' instead of a populated list (SettingsView.swift:227-232).",
        "The Import button remains available in the header even when the library is empty, so import is reachable from the empty state (SettingsView.swift:221-224)."
      ],
      "acceptanceCriteria": [
        "With zero saved presets, the Presets settings pane displays the 'No presets yet…' guidance text.",
        "The Import button is visible and functional while the list is empty.",
        "Once at least one preset exists (saved or imported), the guidance text disappears and the list of presets with per-row Export/Delete buttons is shown."
      ],
      "codeRefs": [
        "Sources/Waves/Settings/SettingsView.swift:227",
        "Sources/Waves/Settings/SettingsView.swift:228",
        "Sources/Waves/Settings/SettingsView.swift:234"
      ],
      "implStatus": "implemented",
      "implNotes": "Simple and correct empty-state copy that points users toward both creation paths (save and import). The empty List is still rendered below the message, which on macOS shows an empty bordered list area — slightly redundant alongside the explicit empty-state text, but not a functional issue.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/ux] Empty state shows the guidance text AND an empty bordered List, contradicting 'instead of a populated list' (Sources/Waves/Settings/SettingsView.swift:234) — The story expects the 'No presets yet…' message 'instead of a populated list.' In PresetSettingsView the empty-state Text is rendered (227-232) but the List (234-263) is still always rendered below it. When store.presets is empty, macOS draws an empty bordered list area beneath the guidance text, which is visually redundant and not what 'instead of a list' implies. | Fix: Wrap the List in `if !store.presets.isEmpty { ... }` (or use an else branch) so the empty bordered list is not drawn in the empty state.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier3: FU-PRESET-EMPTY-STATE-LIST",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "ux",
          "title": "Empty state shows the guidance text AND an empty bordered List, contradicting 'instead of a populated list'",
          "detail": "The story expects the 'No presets yet…' message 'instead of a populated list.' In PresetSettingsView the empty-state Text is rendered (227-232) but the List (234-263) is still always rendered below it. When store.presets is empty, macOS draws an empty bordered list area beneath the guidance text, which is visually redundant and not what 'instead of a list' implies.",
          "codeRef": "Sources/Waves/Settings/SettingsView.swift:234",
          "expectedVsActual": "Expected: when there are no presets, show only the guidance message (no list). Actual: the guidance message plus an empty bordered List are both shown.",
          "suggestedFix": "Wrap the List in `if !store.presets.isEmpty { ... }` (or use an else branch) so the empty bordered list is not drawn in the empty state.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Settings/SettingsView.swift",
          "disposition": "tier3:applied:FU-PRESET-EMPTY-STATE-LIST",
          "uid": "SHARE-04#1",
          "canonicalFile": "Sources/Waves/Settings/SettingsView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-PRESET-EMPTY-STATE-LIST",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "PIN-01",
      "area": "App Pinning",
      "feature": "Pin / unpin an app",
      "priority": "P2",
      "userStory": "As a Waves user, I want to pin (and unpin) an important app, so that I can mark it as a favorite I keep an eye on across all my running apps.",
      "expectedBehavior": [
        "Right-clicking a mixer row shows a context-menu item labeled \"Pin\" when the app is not pinned and \"Unpin\" when it is (MixerRowView.swift:136).",
        "Selecting the item calls store.togglePinned(app), which computes willPin = !app.isPinned and calls backend.pinApp(willPin, appID:) then re-reads the snapshot (AppStore.swift:590-609).",
        "The real backend flips snapshot.apps[index].isPinned for the matching logicalID/id, throwing BackendError.appNotFound if no match (WorkspaceAudioControlBackend.swift:148-154).",
        "On success an info toast \"Pinned\"/\"Unpinned\" with the app name shows for ~1.2s; on failure an error toast \"Pinning failed\" with the error description shows (AppStore.swift:599-607).",
        "The same toggle is reachable via a VoiceOver accessibility action named \"Pin\"/\"Unpin\" (MixerRowView.swift:163-165) and via the \"p\" key on the selected row in the main window list (MainWindowView.swift:386, handleKey at 408-414).",
        "Pinning is independent of exclusion and audio control: togglePinned has no isExcluded guard, so even excluded apps can be pinned."
      ],
      "acceptanceCriteria": [
        "Right-clicking an unpinned app shows \"Pin\"; after invoking it the app's isPinned becomes true and the menu now shows \"Unpin\".",
        "Invoking the toggle on an app the backend cannot find surfaces a \"Pinning failed\" error toast and leaves isPinned unchanged.",
        "Pressing \"p\" with a row selected in the main window toggles that row's pinned state.",
        "A successful pin shows an info toast containing the app's display name.",
        "The VoiceOver action label matches the current state (\"Pin\" when unpinned, \"Unpin\" when pinned)."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:590",
        "Sources/WavesAudioCore/Backend/AudioControlBackend.swift:12",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:148",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:136",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:163",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:386"
      ],
      "implStatus": "implemented",
      "implNotes": "Toggle is fully wired through store -> backend -> snapshot with toasts and three entry points (menu, a11y action, keyboard). Minor concern: togglePinned replaces the whole session with backend.currentSnapshot() (AppStore.swift:596) rather than merging a single app like setMuted/setVolumeBoost do; if an in-flight volume/mute apply hasn't merged yet this could momentarily clobber locally-set state, though pinning itself stays correct. Unlike volume/mute/boost/output, pinning has no isExcluded() guard, so an excluded app can still be pinned — harmless (pinning doesn't tap audio) but slightly inconsistent.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1M]",
      "errorsFound": "• [MEDIUM/concurrency] togglePinned replaces the whole session, transiently clobbering in-flight optimistic volume/mute state (Sources/Waves/Stores/AppStore.swift:596) — togglePinned does `session = await backend.currentSnapshot()` (AppStore.swift:596), replacing the entire local session, unlike setMuted/setVolumeBoost/setDesiredVolume/setOutputDevice which optimistically mutate only session.apps[index] then call mergeAppState(from:appID:) (AppStore.swift:1417) to merge a SINGLE app. Trigger: the user is dragging app A's slider (setDesiredVolume writes session.apps[A].desiredVolume locally + sets pendingVolumeTargets[A] at AppStore.swift:366-368; the backend apply is deferred until commitDesiredVolume->scheduleVolumeApply), OR app A's mute is mid-flight (setMuted set session.apps[A].isMuted locally at :497 but backend.setMuted at :523 has not returned). Before that backend write lands, the user pins ANY app via 'p' (MainWindowView.swift:386), the context menu, or the a11y action. The await in togglePinned reads a backend snapshot that does NOT yet contain A's new value, so A's optimistic volume/mute reverts in the UI. The durable state self-heals once the pending apply task runs mergeAppState (AppStore.swift:429), but the user sees a transient revert/flicker of A's slider or mute glyph. Verified: mergeAppState only overwrites the one matching app, whereas togglePinned overwrites the whole session. | Fix: Mirror the other mutators: after backend.pinApp succeeds, call mergeAppState(from: await backend.currentSnapshot(), appID: app.logicalID) instead of overwriting `session` wholesale; or set session.apps[index].isPinned optimistically before the await and merge only that app on completion.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier2: FU-PIN-SESSION-CLOBBER",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "concurrency",
          "title": "togglePinned replaces the whole session, transiently clobbering in-flight optimistic volume/mute state",
          "detail": "togglePinned does `session = await backend.currentSnapshot()` (AppStore.swift:596), replacing the entire local session, unlike setMuted/setVolumeBoost/setDesiredVolume/setOutputDevice which optimistically mutate only session.apps[index] then call mergeAppState(from:appID:) (AppStore.swift:1417) to merge a SINGLE app. Trigger: the user is dragging app A's slider (setDesiredVolume writes session.apps[A].desiredVolume locally + sets pendingVolumeTargets[A] at AppStore.swift:366-368; the backend apply is deferred until commitDesiredVolume->scheduleVolumeApply), OR app A's mute is mid-flight (setMuted set session.apps[A].isMuted locally at :497 but backend.setMuted at :523 has not returned). Before that backend write lands, the user pins ANY app via 'p' (MainWindowView.swift:386), the context menu, or the a11y action. The await in togglePinned reads a backend snapshot that does NOT yet contain A's new value, so A's optimistic volume/mute reverts in the UI. The durable state self-heals once the pending apply task runs mergeAppState (AppStore.swift:429), but the user sees a transient revert/flicker of A's slider or mute glyph. Verified: mergeAppState only overwrites the one matching app, whereas togglePinned overwrites the whole session.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:596",
          "expectedVsActual": "Expected: pinning one app leaves every other app's just-set (and not-yet-persisted) volume/mute untouched, matching how the other mutators surgically merge a single app. Actual: pinning replaces the entire session from the backend snapshot, momentarily reverting any optimistic change the backend hasn't applied yet.",
          "suggestedFix": "Mirror the other mutators: after backend.pinApp succeeds, call mergeAppState(from: await backend.currentSnapshot(), appID: app.logicalID) instead of overwriting `session` wholesale; or set session.apps[index].isPinned optimistically before the await and merge only that app on completion.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier2:applied:FU-PIN-SESSION-CLOBBER",
          "uid": "PIN-01#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-PIN-SESSION-CLOBBER",
          "fixTier": "tier2"
        }
      ]
    },
    {
      "id": "PIN-02",
      "area": "App Pinning",
      "feature": "Pinned indicator on the mixer row",
      "priority": "P3",
      "userStory": "As a user, I want a clear visual marker on pinned apps, so that I can tell at a glance which apps I have flagged as important.",
      "expectedBehavior": [
        "A pinned app's row shows a small filled pin glyph (\"pin.fill\") next to its name in the full mixer row (MixerRowView.swift:22-27).",
        "The glyph carries an accessibility label \"Pinned\" so VoiceOver announces the pinned state (MixerRowView.swift:26).",
        "The pin glyph is not rendered when isPinned is false (the if app.isPinned block is skipped)."
      ],
      "acceptanceCriteria": [
        "A row whose app.isPinned is true displays the pin.fill symbol beside the app name; an unpinned row does not.",
        "VoiceOver reports \"Pinned\" for the indicator on a pinned row.",
        "Toggling pin state via PIN-01 shows/hides the glyph accordingly."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MixerRowView.swift:22"
      ],
      "implStatus": "implemented",
      "implNotes": "Correct and accessible in the full MixerRowView. Note the indicator is absent from CompactMixerRow (MixerRowView.swift:259-327), which is used by the menu-bar sections — but pinned apps there already live under an explicit \"Pinned\" header (PIN-04), so the missing glyph is reasonable rather than a bug.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "PIN-03",
      "area": "App Pinning",
      "feature": "Pinned filter scope in the main window",
      "priority": "P2",
      "userStory": "As a user, I want a dedicated \"Pinned\" section in the main window sidebar, so that I can focus on just my favorite apps and quickly find them.",
      "expectedBehavior": [
        "The sidebar lists a \"Pinned\" source filter with a pin.fill icon and a live count subtitle like \"2 pinned apps\" (MainWindowView.swift:150-212, count via store.pinnedApps.count at 177).",
        "Selecting the Pinned filter scopes the list to store.pinnedApps, which is visibleApps.filter(\\.isPinned) (MainWindowView.swift:106-107; AppStore.swift:109-111).",
        "store.pinnedApps respects the showSystemProcesses preference because it derives from visibleApps (AppStore.swift:103-111).",
        "When no apps are pinned, the empty state shows title \"No Pinned Apps\" and message \"Pin apps from the source list to keep them here.\" (MainWindowView.swift:219, 236).",
        "A search query while in the Pinned scope searches across ALL visible apps, not just pinned ones, so a match in another scope is not hidden (MainWindowView.swift:115-129)."
      ],
      "acceptanceCriteria": [
        "The sidebar always shows a Pinned row whose count equals the number of pinned visible apps.",
        "Selecting Pinned with at least one pinned app lists exactly the pinned apps.",
        "Selecting Pinned with zero pinned apps shows the \"No Pinned Apps\" empty state and its guidance message.",
        "Pinning a system process does not appear in the Pinned list unless showSystemProcesses is enabled.",
        "Typing a search term while in Pinned scope can surface non-pinned apps that match."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MainWindowView.swift:106",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:150",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:219",
        "Sources/Waves/Stores/AppStore.swift:109"
      ],
      "implStatus": "implemented",
      "implNotes": "Filter, count, icon, and empty state are all present and correct. UX nuance: while in the Pinned scope, an active search silently widens results to all visible apps (MainWindowView.swift:126), so the list can show unpinned apps even though the sidebar selection reads \"Pinned\" — intentional per the comment but potentially surprising.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/ux] Active search in the Pinned scope shows unpinned apps while the sidebar still reads \"Pinned\" with a pinned-only count (Sources/Waves/Features/Mixer/MainWindowView.swift:177) — filteredApps (MainWindowView.swift:115-130) searches store.visibleApps regardless of the selected scope, so when the Pinned filter is selected and the user types a query, the list can show apps that are not pinned. The sidebar selection still highlights 'Pinned' and its subtitle still reads e.g. '2 pinned apps' because the sidebar count comes from filter.count(in:) -> store.pinnedApps.count (MainWindowView.swift:177), which ignores the search query. (Note: the in-pane OutputSummaryView header at MainWindowView.swift:334/447 DOES reflect the widened filtered count, so the contradiction is specifically between the sidebar label/count and the list, not the pane header — the first tester slightly conflated the two counts.) This is intentional per the inline comment and is encoded as PIN-03 acceptance criterion 5, but there is no on-screen cue that search has widened the scope beyond pinned apps. | Fix: When a search query is active and the result set exceeds the selected scope, show a subtle hint (e.g. set the OutputSummaryView subtitle to 'Searching all apps', or update the sidebar count to reflect the matched set) so the widened scope is discoverable.",
      "fixStatus": "1 fixed",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "ux",
          "title": "Active search in the Pinned scope shows unpinned apps while the sidebar still reads \"Pinned\" with a pinned-only count",
          "detail": "filteredApps (MainWindowView.swift:115-130) searches store.visibleApps regardless of the selected scope, so when the Pinned filter is selected and the user types a query, the list can show apps that are not pinned. The sidebar selection still highlights 'Pinned' and its subtitle still reads e.g. '2 pinned apps' because the sidebar count comes from filter.count(in:) -> store.pinnedApps.count (MainWindowView.swift:177), which ignores the search query. (Note: the in-pane OutputSummaryView header at MainWindowView.swift:334/447 DOES reflect the widened filtered count, so the contradiction is specifically between the sidebar label/count and the list, not the pane header — the first tester slightly conflated the two counts.) This is intentional per the inline comment and is encoded as PIN-03 acceptance criterion 5, but there is no on-screen cue that search has widened the scope beyond pinned apps.",
          "codeRef": "Sources/Waves/Features/Mixer/MainWindowView.swift:177",
          "expectedVsActual": "Expected (per criterion 5): searching in Pinned scope can surface non-pinned matches — satisfied. UX gap: the sidebar still says 'Pinned' with a pinned-only count, giving no indication the result set now spans all visible apps.",
          "suggestedFix": "When a search query is active and the result set exceeds the selected scope, show a subtle hint (e.g. set the OutputSummaryView subtitle to 'Searching all apps', or update the sidebar count to reflect the matched set) so the widened scope is discoverable.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "disposition": "iter3:implemented",
          "uid": "PIN-03#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "fixStatus": "applied",
          "fixedBy": "iter3-ui-parity",
          "fixTier": "iter3"
        }
      ]
    },
    {
      "id": "PIN-04",
      "area": "App Pinning",
      "feature": "Pinned section in the menu-bar panel",
      "priority": "P2",
      "userStory": "As a menu-bar user, I want my pinned apps grouped at the top of the quick panel, so that the apps I care about are immediately reachable without opening the main window.",
      "expectedBehavior": [
        "The menu-bar panel renders a \"Pinned\" CompactSection above Live and Recent whenever store.pinnedApps is non-empty (MenuBarMixerView.swift:42-44).",
        "The Pinned section is hidden entirely when no apps are pinned (the if !store.pinnedApps.isEmpty guard).",
        "A global \"No audio apps detected\" empty state shows only when pinned, live, and recent are all empty (MenuBarMixerView.swift:52)."
      ],
      "acceptanceCriteria": [
        "With at least one pinned app, the menu-bar panel shows a \"Pinned\" header listing those apps above the Live section.",
        "With no pinned apps, no \"Pinned\" header is rendered.",
        "The combined empty placeholder appears only when there are no pinned, live, or recent apps.",
        "Each pinned row in the panel exposes its volume slider, boost, and mute controls (CompactMixerRow)."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:42",
        "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:52"
      ],
      "implStatus": "implemented",
      "implNotes": "Section is correctly gated and ordered first. Concern: pinnedApps, liveApps, and recentApps are not mutually exclusive — liveApps/recentApps filter only on routing/level state and never exclude pinned apps (AppStore.swift:117-128). So a pinned app that is currently playing appears in BOTH the Pinned and Live sections of the menu-bar panel, duplicating the row.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1M 1L]",
      "errorsFound": "• [MEDIUM/inconsistency] Pinned apps are duplicated in the menu-bar panel — they also render under Live and/or Recent (Sources/Waves/Features/Mixer/MenuBarMixerView.swift:46) — MenuBarMixerView renders Pinned (MenuBarMixerView.swift:42-44), Live (46), and Recent (48-50) from store.pinnedApps, store.liveApps, and store.recentApps with no cross-section de-duplication. These collections are NOT mutually exclusive: liveApps (AppStore.swift:117-122) filters only on routingState/level and never excludes pinned apps; recentApps = visibleApps minus liveApps (AppStore.swift:124-128), again ignoring isPinned. So a pinned app that is playing renders in BOTH Pinned and Live; a pinned app that is not playing renders in BOTH Pinned and Recent (showRecentApps defaults to true, UserPreferences.swift:5). With default settings every pinned app appears twice, duplicating its CompactMixerRow controls. Confirmed against the source. | Fix: Exclude pinned logicalIDs from liveApps and recentApps when building the menu-bar sections (compute a partition: pinned first, then live-not-pinned, then recent-not-pinned-not-live), e.g. add a pinnedIDs Set and filter both.\n• [LOW/accessibility] No way to pin or unpin an app from the menu-bar panel (CompactMixerRow has no pin affordance) (Sources/Waves/Features/Mixer/MixerRowView.swift:259) — The pin/unpin entry points all live in the full MixerRowView: the context-menu Button (MixerRowView.swift:135-138), the VoiceOver accessibilityAction (MixerRowView.swift:163-165), and the 'p' key handler in the main-window list (MainWindowView.swift:386). CompactMixerRow (MixerRowView.swift:259-327), which is the row used by every menu-bar section including the Pinned section, has NO context menu, NO accessibility action, and NO pin glyph (verified: grep shows contextMenu/accessibilityAction/togglePinned only in the MixerRowView range). A user who works primarily from the menu-bar panel therefore cannot pin a new favorite or unpin an existing one without opening the main window, and a VoiceOver user navigating the menu-bar panel has no Pin/Unpin action and no 'Pinned' indicator there. Arguably a deliberate scoping decision (main window is the management surface), hence low severity, but it is an undocumented discoverability/accessibility gap for the menu-bar-first user the PIN-04 story targets. | Fix: Add a context-menu Pin/Unpin item and a matching accessibilityAction to CompactMixerRow (reusing store.togglePinned), so pins can be created/removed and announced from the menu-bar panel.",
      "fixStatus": "2 fixed",
      "fixNotes": "tier1: FU-MENU-SECTION-DEDUP | tier2: FU-MENU-SECTION-DEDUP",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "inconsistency",
          "title": "Pinned apps are duplicated in the menu-bar panel — they also render under Live and/or Recent",
          "detail": "MenuBarMixerView renders Pinned (MenuBarMixerView.swift:42-44), Live (46), and Recent (48-50) from store.pinnedApps, store.liveApps, and store.recentApps with no cross-section de-duplication. These collections are NOT mutually exclusive: liveApps (AppStore.swift:117-122) filters only on routingState/level and never excludes pinned apps; recentApps = visibleApps minus liveApps (AppStore.swift:124-128), again ignoring isPinned. So a pinned app that is playing renders in BOTH Pinned and Live; a pinned app that is not playing renders in BOTH Pinned and Recent (showRecentApps defaults to true, UserPreferences.swift:5). With default settings every pinned app appears twice, duplicating its CompactMixerRow controls. Confirmed against the source.",
          "codeRef": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:46",
          "expectedVsActual": "Expected: the Pinned section groups favorites at the top while Live/Recent list the remaining apps, so each app appears once. Actual: pinned apps remain in liveApps/recentApps, so they render in two sections simultaneously, producing duplicated rows.",
          "suggestedFix": "Exclude pinned logicalIDs from liveApps and recentApps when building the menu-bar sections (compute a partition: pinned first, then live-not-pinned, then recent-not-pinned-not-live), e.g. add a pinnedIDs Set and filter both.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift",
          "disposition": "tier2:applied:FU-MENU-SECTION-DEDUP",
          "uid": "PIN-04#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-MENU-SECTION-DEDUP",
          "fixTier": "tier2"
        },
        {
          "severity": "low",
          "type": "accessibility",
          "title": "No way to pin or unpin an app from the menu-bar panel (CompactMixerRow has no pin affordance)",
          "detail": "The pin/unpin entry points all live in the full MixerRowView: the context-menu Button (MixerRowView.swift:135-138), the VoiceOver accessibilityAction (MixerRowView.swift:163-165), and the 'p' key handler in the main-window list (MainWindowView.swift:386). CompactMixerRow (MixerRowView.swift:259-327), which is the row used by every menu-bar section including the Pinned section, has NO context menu, NO accessibility action, and NO pin glyph (verified: grep shows contextMenu/accessibilityAction/togglePinned only in the MixerRowView range). A user who works primarily from the menu-bar panel therefore cannot pin a new favorite or unpin an existing one without opening the main window, and a VoiceOver user navigating the menu-bar panel has no Pin/Unpin action and no 'Pinned' indicator there. Arguably a deliberate scoping decision (main window is the management surface), hence low severity, but it is an undocumented discoverability/accessibility gap for the menu-bar-first user the PIN-04 story targets.",
          "codeRef": "Sources/Waves/Features/Mixer/MixerRowView.swift:259",
          "expectedVsActual": "Expected (PIN-04 user story: 'menu-bar user ... apps I care about immediately reachable without opening the main window'): a menu-bar user can manage their pins from the panel. Actual: the panel can only group already-pinned apps; pinning/unpinning requires the main window, and the compact rows expose no Pin action to VoiceOver.",
          "suggestedFix": "Add a context-menu Pin/Unpin item and a matching accessibilityAction to CompactMixerRow (reusing store.togglePinned), so pins can be created/removed and announced from the menu-bar panel.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "disposition": "iter3:implemented",
          "uid": "PIN-04#2",
          "canonicalFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "fixStatus": "applied",
          "fixedBy": "iter3-ui-parity",
          "fixTier": "iter3"
        }
      ]
    },
    {
      "id": "PIN-05",
      "area": "App Pinning",
      "feature": "Pin state persists across refresh, device changes, and relaunch",
      "priority": "P2",
      "userStory": "As a user, I want my pins to stick, so that I don't have to re-pin my favorite apps every time the app list refreshes or I restart Waves.",
      "expectedBehavior": [
        "isPinned is a Codable field on AudioApp that round-trips through encode/decode (defaulting to false for older data) (AudioApp.swift:19, 134, 183).",
        "The session snapshot persists isPinned to disk via SessionStore.save (SessionStore.swift:73) and is reloaded on launch (AppStore.swift:83).",
        "On every backend snapshot rebuild, a rediscovered app inherits previous.isPinned from the prior snapshot keyed by logicalID (WorkspaceAudioControlBackend.swift:1008).",
        "AppStore.mergedSession also carries cachedApp.isPinned forward when reconciling the warm cached session with a freshly built one at startup (AppStore.swift:1400).",
        "Pin state is keyed by logicalID, so it tracks an app across PID changes between launches."
      ],
      "acceptanceCriteria": [
        "Pinning an app, then refreshing the library, keeps the app pinned and still listed in the Pinned scope.",
        "Quitting and relaunching Waves restores previously pinned apps as pinned.",
        "A pinned app that briefly stops and restarts (same logicalID) remains pinned after the next snapshot.",
        "Loading a session file written by an older build without an isPinned key does not crash and treats apps as unpinned."
      ],
      "codeRefs": [
        "Sources/WavesAudioCore/Models/AudioApp.swift:134",
        "Sources/Waves/Services/Persistence/SessionStore.swift:73",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1008",
        "Sources/Waves/Stores/AppStore.swift:1400"
      ],
      "implStatus": "implemented",
      "implNotes": "Persistence is consistently threaded through the model, disk store, backend merge, and store merge — pins survive refresh and relaunch. One edge case: pin state lives only in the session snapshot, not in UserPreferences; if the cached session file is absent/corrupt and load() returns nil (AppStore.swift:83 falls back to emptySession), all pins are lost on that launch. Acceptable but worth noting as the single point of truth for pins.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/edge-case] Pins are a single point of truth in session.json; a corrupt/missing session file silently drops all pins on launch (Sources/Waves/Stores/AppStore.swift:83) — Pin state is persisted only inside the session snapshot (SessionStore.save explicitly re-maps isPinned at SessionStore.swift:73) — it is not duplicated into UserPreferences. On launch the store does `self.session = sessionStore.load() ?? Self.emptySession` (AppStore.swift:83). SessionStore.load() (SessionStore.swift:30-48) returns nil on ANY failure — file absent, over the 10MB cap, unreadable, or a decode error — logging only a warning. When that happens the session falls back to emptySession, so every pin (and every other persisted per-app setting) is silently lost for that launch with no user-facing toast or recovery. The first tester did not list this; the feature-status implNotes call it out as 'acceptable' but it is a real persistence edge case worth flagging given pins are explicitly promised to 'stick' across relaunch (PIN-05 user story). Backward-compat for the missing-key case is fine (decodeIfPresent defaults isPinned to false, AudioApp.swift:134) — the risk is whole-file load failure, not field migration. | Fix: Either mirror pinned logicalIDs into UserPreferences (a tiny, more robust store) as a secondary source of truth, or surface a non-fatal toast when load() returns nil for a file that exists, so the user knows their saved state was dropped rather than silently losing pins.",
      "fixStatus": "1 n/a",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "edge-case",
          "title": "Pins are a single point of truth in session.json; a corrupt/missing session file silently drops all pins on launch",
          "detail": "Pin state is persisted only inside the session snapshot (SessionStore.save explicitly re-maps isPinned at SessionStore.swift:73) — it is not duplicated into UserPreferences. On launch the store does `self.session = sessionStore.load() ?? Self.emptySession` (AppStore.swift:83). SessionStore.load() (SessionStore.swift:30-48) returns nil on ANY failure — file absent, over the 10MB cap, unreadable, or a decode error — logging only a warning. When that happens the session falls back to emptySession, so every pin (and every other persisted per-app setting) is silently lost for that launch with no user-facing toast or recovery. The first tester did not list this; the feature-status implNotes call it out as 'acceptable' but it is a real persistence edge case worth flagging given pins are explicitly promised to 'stick' across relaunch (PIN-05 user story). Backward-compat for the missing-key case is fine (decodeIfPresent defaults isPinned to false, AudioApp.swift:134) — the risk is whole-file load failure, not field migration.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:83",
          "expectedVsActual": "Expected (PIN-05 user story): pins stick across relaunch so the user does not have to re-pin favorites. Actual: if the single session.json fails to load (corruption, truncation, size cap, schema break), load() returns nil and all pins vanish silently with only a log warning.",
          "suggestedFix": "Either mirror pinned logicalIDs into UserPreferences (a tiny, more robust store) as a secondary source of truth, or surface a non-fatal toast when load() returns nil for a file that exists, so the user knows their saved state was dropped rather than silently losing pins.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "PIN-05#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "PIN-06",
      "area": "App Pinning",
      "feature": "Pinned apps sorted to the top of the mixer (intended)",
      "priority": "P3",
      "userStory": "As a user, I want pinned apps floated to the top of the mixer list, so that my favorites stay at the top regardless of the current sort mode.",
      "expectedBehavior": [
        "DESIGN.md and the archived design doc describe pinning as floating \"favorite apps to the top of the mixer\" (designdoc.md:38, 78, 251).",
        "In the running/main list, ordering is produced solely by sortedApps, which switches on preferences.sortMode (name/activity/category/manual) and never references isPinned (AppStore.swift:1278-1337).",
        "Therefore pinning has no effect on the position of an app within the Running list or within any section; pinned apps are not floated to the top."
      ],
      "acceptanceCriteria": [
        "With sortMode = name, pinning an app whose name sorts last does NOT move it to the top of the Running list (documents current behavior — pinning is sort-neutral).",
        "No code path in sortedApps or its comparators inspects app.isPinned.",
        "The only positional effect of pinning is membership in the separate Pinned scope/section, not reordering within other lists."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:1278",
        "Sources/Waves/Stores/AppStore.swift:1303",
        "Sources/Waves/Stores/AppStore.swift:1327",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:102"
      ],
      "implStatus": "missing",
      "implNotes": "Intent gap: the design docs repeatedly state pins float favorites to the TOP of the mixer, but the implemented behavior only adds a dedicated Pinned filter/section and a glyph — sortedApps ignores isPinned, so pinned apps keep their normal sorted position in Running/Live/Recent. README's weaker phrasing (\"keep them visible\") is satisfied, but the \"to the top\" behavior from DESIGN.md/designdoc.md is not implemented. Either the docs overpromise or a pin-first tiebreak in sortedApps is missing.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/inconsistency] Archived design doc promises pinned apps 'float to the top of the mixer'; sortedApps ignores isPinned, so they don't (Sources/Waves/Stores/AppStore.swift:1278) — The archived design doc states pinning floats favorites to the top of the mixer ('pin favorite apps to the top of the mixer', designdoc.md:38; 'Pin favorite apps', :78), and PIN-06's user story repeats this want. But sortedApps (AppStore.swift:1278-1301) switches only on preferences.sortMode, and none of its comparators inspect isPinned — displayNameComparator (:1323), activityRank (:1303), and manualOrderComparator (:1327) all ignore it. So pinning has zero positional effect inside Running/Live/Recent; the only positional effect is membership in the separate Pinned scope/section plus the glyph. Confirmed by reading every comparator. Severity is low (not medium) because: (a) PIN-06 is explicitly marked implStatus 'missing' and its acceptanceCriteria are deliberately written to assert the CURRENT sort-neutral behavior, which the code satisfies; (b) the user-facing README and current DESIGN.md use the accurate weaker phrasing ('keep them visible' / lists pin as a component) — only the archived designdoc.md overpromises. So this is a docs-vs-code mismatch confined to an archived doc, with minimal user impact. | Fix: Pick one and make docs and code agree: either add a pin-first tiebreak in sortedApps (sort isPinned descending before applying the active comparator) to honor the design promise, or update the archived designdoc.md to match the shipped 'dedicated Pinned section, sort-neutral' behavior (the current DESIGN.md/README already do).",
      "fixStatus": "1 n/a",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "Archived design doc promises pinned apps 'float to the top of the mixer'; sortedApps ignores isPinned, so they don't",
          "detail": "The archived design doc states pinning floats favorites to the top of the mixer ('pin favorite apps to the top of the mixer', designdoc.md:38; 'Pin favorite apps', :78), and PIN-06's user story repeats this want. But sortedApps (AppStore.swift:1278-1301) switches only on preferences.sortMode, and none of its comparators inspect isPinned — displayNameComparator (:1323), activityRank (:1303), and manualOrderComparator (:1327) all ignore it. So pinning has zero positional effect inside Running/Live/Recent; the only positional effect is membership in the separate Pinned scope/section plus the glyph. Confirmed by reading every comparator. Severity is low (not medium) because: (a) PIN-06 is explicitly marked implStatus 'missing' and its acceptanceCriteria are deliberately written to assert the CURRENT sort-neutral behavior, which the code satisfies; (b) the user-facing README and current DESIGN.md use the accurate weaker phrasing ('keep them visible' / lists pin as a component) — only the archived designdoc.md overpromises. So this is a docs-vs-code mismatch confined to an archived doc, with minimal user impact.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1278",
          "expectedVsActual": "Expected (archived designdoc.md + PIN-06 user story): pinned apps float to the top of the mixer regardless of sort mode. Actual: sortedApps ignores isPinned; pinned apps keep their normal sorted position, and pinning only adds the dedicated Pinned scope/section and a glyph.",
          "suggestedFix": "Pick one and make docs and code agree: either add a pin-first tiebreak in sortedApps (sort isPinned descending before applying the active comparator) to honor the design promise, or update the archived designdoc.md to match the shipped 'dedicated Pinned section, sort-neutral' behavior (the current DESIGN.md/README already do).",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "PIN-06#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "ORDER-01",
      "area": "Drag-to-Reorder App List",
      "feature": "Drag-to-reorder rows in Running list",
      "priority": "P2",
      "userStory": "As a Waves user, I want to drag app rows up and down in the Running list, so that the apps I care about sit where I expect them in the mixer.",
      "expectedBehavior": [
        "The detail List in the Running scope supports SwiftUI drag-to-reorder via an .onMove handler on the ForEach of MixerRowView rows (MainWindowView.swift:362-376).",
        "On a successful move, store.reorderApps(from:to:) is called with the source IndexSet and destination index (MainWindowView.swift:369-374).",
        "reorderApps maps the current visibleApps to their logicalIDs, applies Array.move(fromOffsets:toOffset:) (the standard collection move so downward drags land on the drop target, not one row below), and stores the result as preferences.customAppOrder (AppStore.swift:1345-1351).",
        "Because the move uses Array.move, a downward drag lands on the intended drop target rather than off-by-one (AppStore.swift:1346-1349).",
        "The new order is persisted immediately via persistPreferences() so it survives relaunch (AppStore.swift:1352)."
      ],
      "acceptanceCriteria": [
        "In the Running scope with Manual sort and no active search, dragging a row to a new position leaves it at exactly the drop location.",
        "After dragging and relaunching the app, the customized order is preserved.",
        "Dragging a row downward past N rows lands it directly below the Nth row, not one position further.",
        "reorderApps writes the full set of visible logicalIDs into customAppOrder in the new order."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MainWindowView.swift:362-376",
        "Sources/Waves/Stores/AppStore.swift:1339-1354"
      ],
      "implStatus": "implemented",
      "implNotes": "Core drag path is correctly wired and persists. The off-by-one comment shows a known bug was already fixed by using Array.move. One latent concern: reorderApps rebuilds customAppOrder from visibleApps only (the currently visible, filtered set), so any apps hidden by showSystemProcesses==false are dropped from the saved order on every drag; they will re-collide at Int.max when later shown (see ORDER-04).",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1H 1M]",
      "errorsFound": "• [HIGH/concurrency] reorderApps re-derives order from a freshly-read visibleApps instead of the snapshot the drop indices were computed against (stale-index trap) (Sources/Waves/Stores/AppStore.swift:1345-1349) — The .onMove closure (MainWindowView.swift:369) receives source/destination indices that SwiftUI computed against the rendered `apps` array (filteredApps, which for filter==.running with no search equals store.visibleApps at render time). reorderApps ignores those rows and re-reads `visibleApps` freshly (AppStore.swift:1345), then calls Array.move(fromOffsets:toOffset:). In the common case the handler runs synchronously on the main actor immediately after the drop with no intervening await, so the rendered snapshot and the re-read snapshot match and there is no problem. The latent risk: session.apps is mutated by async work (refresh() AppStore.swift:242, backend snapshot merges, termination/frontmost observers). If SwiftUI's displayed list lags a session mutation that already shrank visibleApps, a source/destination offset can exceed the new array's bounds and Array.move traps (crash); if the set merely reordered, the wrong rows move. Confirmed as a real robustness gap, though the trigger window is narrow. | Fix: Pass the exact ordered identifier list the indices were computed against into reorderApps (e.g. reorderApps(visibleIDs: apps.map(\\.logicalID), from:to:)) and apply move() to that array, or clamp/guard source and destination against visibleApps.count and bail if they no longer fit. Do not re-read visibleApps inside reorderApps.\n• [MEDIUM/edge-case] Hidden system processes are stripped from customAppOrder on every drag (Sources/Waves/Stores/AppStore.swift:1345) — visibleApps filters out system-category apps when preferences.showSystemProcesses == false (AppStore.swift:104-105) before sortedApps. reorderApps overwrites customAppOrder with `visibleApps.map { $0.logicalID }` (AppStore.swift:1345,1351), so any system process hidden at drag time is silently removed from the saved order. When the user later re-enables Show System Processes, those apps are absent from customAppOrder, get Int.max in manualOrderComparator, and collapse to the bottom, losing their previously-customized position. Verified against the filter at lines 104-105 and the overwrite at 1351. (Same root cause as ORDER-04 issue 2.) | Fix: Build the new order by merging the moved visible IDs back into the existing preferences.customAppOrder, preserving entries for currently-hidden apps in their relative positions, rather than overwriting customAppOrder with only the visible subset.",
      "fixStatus": "2 fixed",
      "fixNotes": "tier1: FU-REORDER-STALE-SNAPSHOT | tier2: FU-MANUAL-ORDER-HIDDEN-STRIP",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "concurrency",
          "title": "reorderApps re-derives order from a freshly-read visibleApps instead of the snapshot the drop indices were computed against (stale-index trap)",
          "detail": "The .onMove closure (MainWindowView.swift:369) receives source/destination indices that SwiftUI computed against the rendered `apps` array (filteredApps, which for filter==.running with no search equals store.visibleApps at render time). reorderApps ignores those rows and re-reads `visibleApps` freshly (AppStore.swift:1345), then calls Array.move(fromOffsets:toOffset:). In the common case the handler runs synchronously on the main actor immediately after the drop with no intervening await, so the rendered snapshot and the re-read snapshot match and there is no problem. The latent risk: session.apps is mutated by async work (refresh() AppStore.swift:242, backend snapshot merges, termination/frontmost observers). If SwiftUI's displayed list lags a session mutation that already shrank visibleApps, a source/destination offset can exceed the new array's bounds and Array.move traps (crash); if the set merely reordered, the wrong rows move. Confirmed as a real robustness gap, though the trigger window is narrow.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1345-1349",
          "expectedVsActual": "Expected: the move is applied to exactly the list the user dragged against, so the dropped row lands at the drop location (AC0/AC2). Actual: the move is applied to a possibly-different live snapshot; under an interleaved session mutation the indices may no longer correspond, risking an out-of-bounds trap or moving the wrong elements.",
          "suggestedFix": "Pass the exact ordered identifier list the indices were computed against into reorderApps (e.g. reorderApps(visibleIDs: apps.map(\\.logicalID), from:to:)) and apply move() to that array, or clamp/guard source and destination against visibleApps.count and bail if they no longer fit. Do not re-read visibleApps inside reorderApps.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:FU-REORDER-STALE-SNAPSHOT",
          "uid": "ORDER-01#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-REORDER-STALE-SNAPSHOT",
          "fixTier": "tier1"
        },
        {
          "severity": "medium",
          "type": "edge-case",
          "title": "Hidden system processes are stripped from customAppOrder on every drag",
          "detail": "visibleApps filters out system-category apps when preferences.showSystemProcesses == false (AppStore.swift:104-105) before sortedApps. reorderApps overwrites customAppOrder with `visibleApps.map { $0.logicalID }` (AppStore.swift:1345,1351), so any system process hidden at drag time is silently removed from the saved order. When the user later re-enables Show System Processes, those apps are absent from customAppOrder, get Int.max in manualOrderComparator, and collapse to the bottom, losing their previously-customized position. Verified against the filter at lines 104-105 and the overwrite at 1351. (Same root cause as ORDER-04 issue 2.)",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1345",
          "expectedVsActual": "Expected (AC3): reorderApps writes the full set of the user's intended ordering. Actual: it writes only the currently-visible (filtered) subset, discarding ordering for any app hidden by the showSystemProcesses filter.",
          "suggestedFix": "Build the new order by merging the moved visible IDs back into the existing preferences.customAppOrder, preserving entries for currently-hidden apps in their relative positions, rather than overwriting customAppOrder with only the visible subset.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "gapfill:already-resolved",
          "uid": "ORDER-01#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "earlier-tier",
          "fixTier": "tier2"
        }
      ]
    },
    {
      "id": "ORDER-02",
      "area": "Drag-to-Reorder App List",
      "feature": "Manual sort mode gating of reorder",
      "priority": "P2",
      "userStory": "As a Waves user, I want my drag-to-reorder to take effect only when I have chosen Manual ordering, so that a manual drag does not silently override an automatic sort I selected.",
      "expectedBehavior": [
        "The .onMove handler only forwards the move to the store when filter == .running AND preferences.sortMode == .manual AND searchText is empty (MainWindowView.swift:372-374).",
        "If sort mode is Name, Activity, or Category, the drag is accepted by the List UI but the handler does nothing, so the list re-renders back to its sorted order (MainWindowView.swift:372-375).",
        "Manual sort mode is selectable from Settings > General via the 'Sort apps by' Picker, which writes preferences.sortMode and persists it (SettingsView.swift:162-175).",
        "reorderApps itself defensively switches sortMode to .manual and persists if it is ever called in a non-manual mode (AppStore.swift:1340-1343)."
      ],
      "acceptanceCriteria": [
        "With sort mode set to Name/Activity/Category, dragging a row produces no lasting reorder (the row snaps back).",
        "With sort mode set to Manual, dragging a row reorders and persists.",
        "Selecting Manual in the Settings picker is reflected immediately and persisted.",
        "Reorder never fires from any scope other than Running."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MainWindowView.swift:369-375",
        "Sources/Waves/Settings/SettingsView.swift:162-175",
        "Sources/Waves/Stores/AppStore.swift:1340-1343"
      ],
      "implStatus": "partial",
      "implNotes": "Functionally correct but a real UX trap: the README advertises 'Drag-to-Reorder: Customize the order of your app list' with no mention that you must first switch Settings to Manual. In Name/Activity/Category mode the row is draggable (the system shows the drag affordance) but snaps back with no feedback, which reads as a broken feature. The defensive auto-switch-to-manual branch inside reorderApps (AppStore.swift:1340-1343) is effectively dead code because the only caller already requires manual mode before calling. There is also no in-window control to enter Manual mode or to start reordering — it is buried in Settings.",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [1M 2L]",
      "errorsFound": "• [MEDIUM/ux] Rows are draggable in Name/Activity/Category modes but silently snap back with no feedback; feature reads as broken (Sources/Waves/Features/Mixer/MainWindowView.swift:369-375) — The List always attaches .onMove (MainWindowView.swift:369), so macOS presents the drag affordance and accepts the gesture in every sort mode. When sortMode != .manual the handler's guard (line 372) is false and the closure does nothing, so the view re-renders back to the sorted order and the row snaps back with zero explanation. README.md:27 advertises 'Drag-to-Reorder: Customize the order of your app list' with no mention that the user must first switch Settings > General > Sort apps by to Manual. Confirmed: guard at line 372 requires sortMode == .manual; nothing suppresses the affordance otherwise. | Fix: Either (a) only attach .onMove when sortMode == .manual && filter == .running && the trimmed search is empty so the OS presents no reorder affordance otherwise, or (b) when a drag no-ops because the mode isn't Manual, show a toast/inline hint ('Switch Sort to Manual to reorder') and offer to auto-switch. Update README to state the Manual prerequisite.\n• [LOW/dead-code] Defensive sortMode-switch branch in reorderApps is unreachable (Sources/Waves/Stores/AppStore.swift:1340-1343) — reorderApps begins with `if preferences.sortMode != .manual { preferences.sortMode = .manual; persistPreferences() }` (AppStore.swift:1340-1343). The only caller is the .onMove closure (MainWindowView.swift:373), which is itself gated by `store.preferences.sortMode == .manual` (line 372). grep confirms no other production or test code calls reorderApps. Therefore the branch can never execute. | Fix: Remove the branch, or repurpose it: drop the sortMode==.manual condition from the .onMove guard and let reorderApps own the switch-to-manual, which would let a drag from a currently no-op non-manual mode intentionally engage Manual mode.\n• [LOW/ux] No in-window affordance to enter Manual mode or start reordering (Sources/Waves/Settings/SettingsView.swift:162-175) — Manual sort is only selectable from Settings > General > 'Sort apps by' (SettingsView.swift:162-175). The main mixer window has no control to switch to Manual or any cue that the list is reorderable, so discovering drag-to-reorder requires opening Settings and knowing to choose Manual first. Confirmed: the only sortMode write site is the Settings picker; the toolbar/list header has no sort control. | Fix: Add a sort-mode control (menu/segmented control) to the main window toolbar or list header, with Manual as an option, and a subtle 'drag to reorder' hint when Manual is active.",
      "fixStatus": "1 fixed · 2 n/a",
      "fixNotes": "tier2: FU-ONMOVE-AFFORDANCE-GATING",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "ux",
          "title": "Rows are draggable in Name/Activity/Category modes but silently snap back with no feedback; feature reads as broken",
          "detail": "The List always attaches .onMove (MainWindowView.swift:369), so macOS presents the drag affordance and accepts the gesture in every sort mode. When sortMode != .manual the handler's guard (line 372) is false and the closure does nothing, so the view re-renders back to the sorted order and the row snaps back with zero explanation. README.md:27 advertises 'Drag-to-Reorder: Customize the order of your app list' with no mention that the user must first switch Settings > General > Sort apps by to Manual. Confirmed: guard at line 372 requires sortMode == .manual; nothing suppresses the affordance otherwise.",
          "codeRef": "Sources/Waves/Features/Mixer/MainWindowView.swift:369-375",
          "expectedVsActual": "Expected: dragging in a non-manual mode gives a clear reason it had no effect, or the affordance is suppressed. Actual: the drag is fully presented, no-ops, and snaps back silently; documentation implies reorder works without any mode prerequisite.",
          "suggestedFix": "Either (a) only attach .onMove when sortMode == .manual && filter == .running && the trimmed search is empty so the OS presents no reorder affordance otherwise, or (b) when a drag no-ops because the mode isn't Manual, show a toast/inline hint ('Switch Sort to Manual to reorder') and offer to auto-switch. Update README to state the Manual prerequisite.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "disposition": "tier2:applied:FU-ONMOVE-AFFORDANCE-GATING",
          "uid": "ORDER-02#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-ONMOVE-AFFORDANCE-GATING",
          "fixTier": "tier2"
        },
        {
          "severity": "low",
          "type": "dead-code",
          "title": "Defensive sortMode-switch branch in reorderApps is unreachable",
          "detail": "reorderApps begins with `if preferences.sortMode != .manual { preferences.sortMode = .manual; persistPreferences() }` (AppStore.swift:1340-1343). The only caller is the .onMove closure (MainWindowView.swift:373), which is itself gated by `store.preferences.sortMode == .manual` (line 372). grep confirms no other production or test code calls reorderApps. Therefore the branch can never execute.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1340-1343",
          "expectedVsActual": "Expected: defensive code either guards a real call path or is removed. Actual: it guards a path the sole caller already excludes, so it is dead code that gives a false impression reorderApps is safe to call from non-manual contexts.",
          "suggestedFix": "Remove the branch, or repurpose it: drop the sortMode==.manual condition from the .onMove guard and let reorderApps own the switch-to-manual, which would let a drag from a currently no-op non-manual mode intentionally engage Manual mode.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "ORDER-02#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "low",
          "type": "ux",
          "title": "No in-window affordance to enter Manual mode or start reordering",
          "detail": "Manual sort is only selectable from Settings > General > 'Sort apps by' (SettingsView.swift:162-175). The main mixer window has no control to switch to Manual or any cue that the list is reorderable, so discovering drag-to-reorder requires opening Settings and knowing to choose Manual first. Confirmed: the only sortMode write site is the Settings picker; the toolbar/list header has no sort control.",
          "codeRef": "Sources/Waves/Settings/SettingsView.swift:162-175",
          "expectedVsActual": "Expected: a discoverable path to the documented reorder feature from the mixer window. Actual: the prerequisite (Manual mode) is buried in a separate Settings window with no in-window cue.",
          "suggestedFix": "Add a sort-mode control (menu/segmented control) to the main window toolbar or list header, with Manual as an option, and a subtle 'drag to reorder' hint when Manual is active.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Settings/SettingsView.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "ORDER-02#3",
          "canonicalFile": "Sources/Waves/Settings/SettingsView.swift"
        }
      ]
    },
    {
      "id": "ORDER-03",
      "area": "Drag-to-Reorder App List",
      "feature": "Reorder disabled while searching or in non-Running scopes",
      "priority": "P2",
      "userStory": "As a Waves user, I want reordering to be disabled while I am filtering the list or viewing a non-Running scope, so that a partial/subset view can never scramble my real saved order.",
      "expectedBehavior": [
        "When searchText is non-empty, the .onMove handler refuses to reorder, because a search shows a subset whose indices would not map back to the full list correctly (MainWindowView.swift:370-374).",
        "Reorder is only honored in the Running scope; Pinned, Live, and Recent scopes (which are filtered derivations of visibleApps) provide no working reorder because the guard requires filter == .running (MainWindowView.swift:372).",
        "The menu-bar mixer (MenuBarMixerView) renders rows with ForEach but defines no .onMove, so it offers no drag-reorder at all (MenuBarMixerView.swift:190; no onMove present)."
      ],
      "acceptanceCriteria": [
        "With a non-empty search query active, dragging a filtered row does not change customAppOrder.",
        "Dragging within Pinned, Live, or Recent scopes does not persist a reorder.",
        "The menu-bar popover offers no drag-to-reorder."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MainWindowView.swift:369-375",
        "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:185-195"
      ],
      "implStatus": "implemented",
      "implNotes": "The guard correctly protects against the subset-index-mapping corruption that a filtered drag would cause, which is the right call. Edge case worth noting: the List still presents a drag handle / accepts the gesture in these disabled situations and silently no-ops, so the user gets no explanation for why the row snapped back. Pinned/Recent/Live rows do reflect the manually-saved order (because they sort through visibleApps) but cannot themselves be reordered.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [2L]",
      "errorsFound": "• [LOW/inconsistency] Whitespace-only search query blocks reorder even though the list shows the full unfiltered set (Sources/Waves/Features/Mixer/MainWindowView.swift:372) — The .onMove guard checks the raw, un-trimmed `searchText.isEmpty` (MainWindowView.swift:372). But filteredApps trims the query and treats a whitespace-only query as no filter, returning the full scopedApps (MainWindowView.swift:116-117). So if searchText is e.g. two spaces, the list shows the complete unfiltered Running set, yet the guard's `searchText.isEmpty` is false and reorder is refused; the row snaps back with no explanation despite no actual filtering. Confirmed against the trim at line 116 vs the raw check at line 372. | Fix: Guard on the same trimmed value filteredApps uses: `searchText.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty`, or pass a precomputed isFiltering flag down so the guard and the filter share one source of truth.\n• [LOW/ux] Dragging in a disabled state (search active, or Pinned/Live/Recent scope) silently no-ops with no explanation (Sources/Waves/Features/Mixer/MainWindowView.swift:369-375) — In Pinned/Live(frontmost)/Recent scopes and while a search is active, the List still attaches .onMove (MainWindowView.swift:369) and presents the drag affordance, but the guard (line 372 requires filter == .running and empty search) makes the handler do nothing. The row snaps back and the user gets no indication of why. Confirmed: scopedApps returns the subset for non-running scopes (MainWindowView.swift:102-113) and the guard blocks them; no corruption occurs because the move is never forwarded. | Fix: Suppress the .onMove affordance entirely when not in the reorderable state (running + manual + no trimmed search) so these scopes/searches present no drag handle.",
      "fixStatus": "2 fixed",
      "fixNotes": "tier2: FU-ONMOVE-AFFORDANCE-GATING",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "Whitespace-only search query blocks reorder even though the list shows the full unfiltered set",
          "detail": "The .onMove guard checks the raw, un-trimmed `searchText.isEmpty` (MainWindowView.swift:372). But filteredApps trims the query and treats a whitespace-only query as no filter, returning the full scopedApps (MainWindowView.swift:116-117). So if searchText is e.g. two spaces, the list shows the complete unfiltered Running set, yet the guard's `searchText.isEmpty` is false and reorder is refused; the row snaps back with no explanation despite no actual filtering. Confirmed against the trim at line 116 vs the raw check at line 372.",
          "codeRef": "Sources/Waves/Features/Mixer/MainWindowView.swift:372",
          "expectedVsActual": "Expected: reorder is allowed exactly when the list is unfiltered (AC0). Actual: a whitespace-only query that filters nothing still disables reorder. (Fails safe — no corruption — but is inconsistent.)",
          "suggestedFix": "Guard on the same trimmed value filteredApps uses: `searchText.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty`, or pass a precomputed isFiltering flag down so the guard and the filter share one source of truth.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "disposition": "tier2:applied:FU-ONMOVE-AFFORDANCE-GATING",
          "uid": "ORDER-03#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-ONMOVE-AFFORDANCE-GATING",
          "fixTier": "tier2"
        },
        {
          "severity": "low",
          "type": "ux",
          "title": "Dragging in a disabled state (search active, or Pinned/Live/Recent scope) silently no-ops with no explanation",
          "detail": "In Pinned/Live(frontmost)/Recent scopes and while a search is active, the List still attaches .onMove (MainWindowView.swift:369) and presents the drag affordance, but the guard (line 372 requires filter == .running and empty search) makes the handler do nothing. The row snaps back and the user gets no indication of why. Confirmed: scopedApps returns the subset for non-running scopes (MainWindowView.swift:102-113) and the guard blocks them; no corruption occurs because the move is never forwarded.",
          "codeRef": "Sources/Waves/Features/Mixer/MainWindowView.swift:369-375",
          "expectedVsActual": "Expected: reorder is correctly disabled in subset/filtered views AND the user understands why. Actual: it is correctly disabled (no corruption), but offers a misleading drag affordance and silent snap-back.",
          "suggestedFix": "Suppress the .onMove affordance entirely when not in the reorderable state (running + manual + no trimmed search) so these scopes/searches present no drag handle.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "disposition": "tier2:applied:FU-ONMOVE-AFFORDANCE-GATING",
          "uid": "ORDER-03#2",
          "canonicalFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-ONMOVE-AFFORDANCE-GATING",
          "fixTier": "tier2"
        }
      ]
    },
    {
      "id": "ORDER-04",
      "area": "Drag-to-Reorder App List",
      "feature": "Manual order persistence, comparator fallback, and new/stale apps",
      "priority": "P3",
      "userStory": "As a Waves user, I want my manual order to be remembered across launches and to behave sensibly when apps appear, quit, or are unknown to the saved order, so that customizing the list is durable and predictable.",
      "expectedBehavior": [
        "sortedApps applies manualOrderComparator when sortMode == .manual, ordering each app by its index in preferences.customAppOrder (AppStore.swift:1298-1300, 1327-1337).",
        "Apps whose logicalID is absent from customAppOrder are assigned Int.max and therefore sink to the bottom of the manual list (AppStore.swift:1330-1332).",
        "Apps that tie (e.g. multiple unknown apps all at Int.max) are broken by raw displayName comparison app1.displayName < app2.displayName (AppStore.swift:1335).",
        "On startup, if sortMode was persisted as .manual but customAppOrder is empty, the store falls back to .name so the user is not left with an empty/degenerate manual order (AppStore.swift:88-90).",
        "customAppOrder and sortMode are decoded field-by-field with defaults so an older preferences file missing these keys still loads cleanly (UserPreferences.swift:52-53, 40-61)."
      ],
      "acceptanceCriteria": [
        "A manually ordered list reproduces the same order after relaunch.",
        "A newly launched app not present in customAppOrder appears at the bottom of the manual list rather than at the top or in a random spot.",
        "Two apps both missing from customAppOrder are ordered alphabetically relative to each other.",
        "Persisted sortMode==.manual with an empty customAppOrder resolves to Name sort on next launch (no blank/degenerate ordering)."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:88-90",
        "Sources/Waves/Stores/AppStore.swift:1298-1300",
        "Sources/Waves/Stores/AppStore.swift:1327-1337",
        "Sources/Waves/Stores/UserPreferences.swift:52-53"
      ],
      "implStatus": "partial",
      "implNotes": "Persistence and the empty-order fallback are solid. Gaps: (1) customAppOrder is never pruned — quit apps' logicalIDs accumulate in the array indefinitely (unbounded slow growth, and a quit-then-relaunched app keeps its old slot, which may surprise the user). (2) reorderApps rebuilds customAppOrder from visibleApps only, so any system processes hidden at drag time are stripped from the saved order and then collapse to Int.max next time they are shown. (3) The tie-break at AppStore.swift:1335 uses raw '<' on displayName instead of the localizedCaseInsensitiveCompare used everywhere else (displayNameComparator), so unknown/new apps sort case-sensitively and inconsistently with the rest of the app. (4) No UI affordance resets manual order back to a clean state; the only escape is switching sort mode away from Manual.",
      "source": "phase1-story",
      "testStatus": "6 issue(s) [2M 4L]",
      "errorsFound": "• [MEDIUM/inconsistency] Manual-order tie-break uses case-sensitive raw '<' on displayName instead of localizedCaseInsensitiveCompare (Sources/Waves/Stores/AppStore.swift:1335) — manualOrderComparator breaks ties (apps both absent from customAppOrder, both Int.max) with `app1.displayName < app2.displayName` (AppStore.swift:1335), a raw lexicographic, case-sensitive, non-locale-aware comparison. Every other sort path uses displayNameComparator (localizedCaseInsensitiveCompare, AppStore.swift:1323-1324). New/unknown apps in Manual mode therefore sort case-sensitively (e.g. 'Zoom' before 'audacity' because 'Z'(0x5A) < 'a'(0x61)), inconsistent with Name mode. Visible immediately when Manual is selected with an empty customAppOrder, since every app is then a tie. Confirmed by direct comparison of lines 1335 vs 1323-1324. | Fix: Replace `return app1.displayName < app2.displayName` with `return displayNameComparator(app1, app2)`.\n• [MEDIUM/edge-case] System processes hidden at drag time are stripped from the persisted manual order (Sources/Waves/Stores/AppStore.swift:1345) — reorderApps rebuilds customAppOrder solely from visibleApps, which excludes system apps when showSystemProcesses==false (AppStore.swift:104-105, 1345, 1351). Reordering while system processes are hidden discards their saved positions. On re-enabling Show System Processes those apps are no longer in customAppOrder, get Int.max in manualOrderComparator (AppStore.swift:1330-1331), and sink to the bottom. Confirmed (same root cause as ORDER-01 issue 2). | Fix: Merge the moved visible IDs back into the existing customAppOrder, preserving entries for apps not currently in visibleApps, instead of overwriting with the filtered subset.\n• [LOW/ux] No way to reset/clear a manual order (Sources/Waves/Stores/AppStore.swift:1339-1354) — customAppOrder is only ever written by reorderApps (AppStore.swift:1351) or flipped to .name on startup when empty (AppStore.swift:88-90). There is no UI action to clear customAppOrder or reset Manual ordering. The only escape is switching Sort to a non-manual mode, which leaves the stale customAppOrder in place to reappear if Manual is chosen again. Confirmed: no reset/clear write site exists. | Fix: Add a 'Reset manual order' action (sets preferences.customAppOrder = [] and persists; per the startup rule it then falls back to Name) near the sort-mode picker or in the mixer window.\n• [LOW/edge-case] customAppOrder is never pruned; quit apps retain slots and a relaunched app re-seizes its old position (Sources/Waves/Stores/AppStore.swift:1327-1337) — customAppOrder is rebuilt only on a drag (AppStore.swift:1351); there is no prune on session refresh. Between drags, a logicalID of a quit app remains in customAppOrder and grows unbounded over time. If that app relaunches before the next reorder, manualOrderComparator (AppStore.swift:1330-1334) finds its old index and restores it to its former slot rather than treating it as new. Confirmed: no pruning call site against the live app set exists. | Fix: Prune customAppOrder against the current known app set when the session refreshes (or deliberately document the keep-slot policy). At minimum drop logicalIDs not seen for some retention window so the array does not grow indefinitely.\n• [LOW/accessibility] No keyboard or VoiceOver path to reorder, contradicting the list's 'fully drivable without a mouse' claim (Sources/Waves/Features/Mixer/MainWindowView.swift:369-386) — The list's keyboard handlers cover mute/volume/boost/pin (MainWindowView.swift:381-386) and a comment asserts the mixer is 'fully drivable without a mouse' (MainWindowView.swift:378-380). However reorder is exposed only via SwiftUI .onMove drag (line 369); on macOS this provides no built-in keyboard or VoiceOver reordering action, and there is no key handler to move the selected row up/down, nor an .accessibilityAction. Confirmed: only the six onKeyPress handlers at 381-386 exist; none move a row. | Fix: Add keyboard handlers (e.g. Option+Up/Down on the selected row) that call store.reorderApps with the selected row's index, gated on Manual+Running+no-search, and expose an .accessibilityAction so VoiceOver users can move a row.\n• [LOW/edge-case] Selecting Manual in Settings without ever dragging silently reverts to Name on next launch, defeating the chosen ordering (Sources/Waves/Stores/AppStore.swift:88-90) — A user can pick Manual in Settings > General (SettingsView.swift:162-175), which persists sortMode==.manual but leaves customAppOrder empty. On the next launch, init() downgrades sortMode to .name because customAppOrder is empty (AppStore.swift:88-90). So a user who selects Manual (intending a stable name-based baseline to then reorder) but closes/relaunches before dragging finds the mode silently reset to Name with no notice. This is a persistence edge case specific to the manual-order feature that the first tester did not capture in this area. | Fix: Either keep Manual selected and render a stable fallback ordering (e.g. name order) when customAppOrder is empty instead of downgrading the mode, or surface a hint in Settings that Manual takes effect only after the first reorder.",
      "fixStatus": "2 fixed · 3 deferred · 1 n/a",
      "fixNotes": "tier2: FU-MANUAL-ORDER-HIDDEN-STRIP; FU-MANUAL-ORDER-TIEBREAK",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "inconsistency",
          "title": "Manual-order tie-break uses case-sensitive raw '<' on displayName instead of localizedCaseInsensitiveCompare",
          "detail": "manualOrderComparator breaks ties (apps both absent from customAppOrder, both Int.max) with `app1.displayName < app2.displayName` (AppStore.swift:1335), a raw lexicographic, case-sensitive, non-locale-aware comparison. Every other sort path uses displayNameComparator (localizedCaseInsensitiveCompare, AppStore.swift:1323-1324). New/unknown apps in Manual mode therefore sort case-sensitively (e.g. 'Zoom' before 'audacity' because 'Z'(0x5A) < 'a'(0x61)), inconsistent with Name mode. Visible immediately when Manual is selected with an empty customAppOrder, since every app is then a tie. Confirmed by direct comparison of lines 1335 vs 1323-1324.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1335",
          "expectedVsActual": "Expected (AC2): two apps both missing from customAppOrder are ordered alphabetically, consistent with the rest of the app. Actual: they are ordered by case-sensitive ASCII codepoint, differing from Name mode.",
          "suggestedFix": "Replace `return app1.displayName < app2.displayName` with `return displayNameComparator(app1, app2)`.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier2:applied:FU-MANUAL-ORDER-TIEBREAK",
          "uid": "ORDER-04#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-MANUAL-ORDER-TIEBREAK",
          "fixTier": "tier2"
        },
        {
          "severity": "medium",
          "type": "edge-case",
          "title": "System processes hidden at drag time are stripped from the persisted manual order",
          "detail": "reorderApps rebuilds customAppOrder solely from visibleApps, which excludes system apps when showSystemProcesses==false (AppStore.swift:104-105, 1345, 1351). Reordering while system processes are hidden discards their saved positions. On re-enabling Show System Processes those apps are no longer in customAppOrder, get Int.max in manualOrderComparator (AppStore.swift:1330-1331), and sink to the bottom. Confirmed (same root cause as ORDER-01 issue 2).",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1345",
          "expectedVsActual": "Expected: a manual order is durable for all apps the user has positioned, regardless of the transient showSystemProcesses filter. Actual: each drag while system apps are hidden permanently drops their order entries.",
          "suggestedFix": "Merge the moved visible IDs back into the existing customAppOrder, preserving entries for apps not currently in visibleApps, instead of overwriting with the filtered subset.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "gapfill:already-resolved",
          "uid": "ORDER-04#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "earlier-tier",
          "fixTier": "tier2"
        },
        {
          "severity": "low",
          "type": "ux",
          "title": "No way to reset/clear a manual order",
          "detail": "customAppOrder is only ever written by reorderApps (AppStore.swift:1351) or flipped to .name on startup when empty (AppStore.swift:88-90). There is no UI action to clear customAppOrder or reset Manual ordering. The only escape is switching Sort to a non-manual mode, which leaves the stale customAppOrder in place to reappear if Manual is chosen again. Confirmed: no reset/clear write site exists.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1339-1354",
          "expectedVsActual": "Expected: the user can undo/reset a manual customization to a known-good baseline. Actual: a manual order, once created, can only be hidden (by changing sort mode), never reset; re-selecting Manual restores the old custom order including stale slots.",
          "suggestedFix": "Add a 'Reset manual order' action (sets preferences.customAppOrder = [] and persists; per the startup rule it then falls back to Name) near the sort-mode picker or in the mixer window.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "ORDER-04#3",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "low",
          "type": "edge-case",
          "title": "customAppOrder is never pruned; quit apps retain slots and a relaunched app re-seizes its old position",
          "detail": "customAppOrder is rebuilt only on a drag (AppStore.swift:1351); there is no prune on session refresh. Between drags, a logicalID of a quit app remains in customAppOrder and grows unbounded over time. If that app relaunches before the next reorder, manualOrderComparator (AppStore.swift:1330-1334) finds its old index and restores it to its former slot rather than treating it as new. Confirmed: no pruning call site against the live app set exists.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1327-1337",
          "expectedVsActual": "Expected (AC1 spirit): newly (re)appearing apps land predictably; stale entries don't silently steer ordering. Actual: a relaunched app keeps its prior slot via a stale entry, and stale IDs accumulate, only pruned opportunistically on the next drag.",
          "suggestedFix": "Prune customAppOrder against the current known app set when the session refreshes (or deliberately document the keep-slot policy). At minimum drop logicalIDs not seen for some retention window so the array does not grow indefinitely.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-user-decision:customAppOrder is still never pruned: the merge path (AppStore.swift:1850-1858) deliberately keeps non-visible IDs, so a relaunched app re-seizes its old slot v",
          "uid": "ORDER-04#4",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "low",
          "type": "accessibility",
          "title": "No keyboard or VoiceOver path to reorder, contradicting the list's 'fully drivable without a mouse' claim",
          "detail": "The list's keyboard handlers cover mute/volume/boost/pin (MainWindowView.swift:381-386) and a comment asserts the mixer is 'fully drivable without a mouse' (MainWindowView.swift:378-380). However reorder is exposed only via SwiftUI .onMove drag (line 369); on macOS this provides no built-in keyboard or VoiceOver reordering action, and there is no key handler to move the selected row up/down, nor an .accessibilityAction. Confirmed: only the six onKeyPress handlers at 381-386 exist; none move a row.",
          "codeRef": "Sources/Waves/Features/Mixer/MainWindowView.swift:369-386",
          "expectedVsActual": "Expected: a mixer claiming to be fully keyboard-drivable lets users reorder rows without a mouse. Actual: reorder is achievable only by mouse drag.",
          "suggestedFix": "Add keyboard handlers (e.g. Option+Up/Down on the selected row) that call store.reorderApps with the selected row's index, gated on Manual+Running+no-search, and expose an .accessibilityAction so VoiceOver users can move a row.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "disposition": "deferred:needs-feature:New feature with keybinding-design choices, not an additive low-risk change reusing existing patterns; explicitly flagged to defer.",
          "uid": "ORDER-04#5",
          "canonicalFile": "Sources/Waves/Features/Mixer/MainWindowView.swift"
        },
        {
          "severity": "low",
          "type": "edge-case",
          "title": "Selecting Manual in Settings without ever dragging silently reverts to Name on next launch, defeating the chosen ordering",
          "detail": "A user can pick Manual in Settings > General (SettingsView.swift:162-175), which persists sortMode==.manual but leaves customAppOrder empty. On the next launch, init() downgrades sortMode to .name because customAppOrder is empty (AppStore.swift:88-90). So a user who selects Manual (intending a stable name-based baseline to then reorder) but closes/relaunches before dragging finds the mode silently reset to Name with no notice. This is a persistence edge case specific to the manual-order feature that the first tester did not capture in this area.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:88-90",
          "expectedVsActual": "Expected: the user's explicitly chosen sort mode survives a relaunch, or they are told Manual needs a saved order. Actual: Manual silently downgrades to Name across launches whenever no drag has occurred, with no UI indication.",
          "suggestedFix": "Either keep Manual selected and render a stable fallback ordering (e.g. name order) when customAppOrder is empty instead of downgrading the mode, or surface a hint in Settings that Manual takes effect only after the first reorder.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-user-decision:init still downgrades sortMode .manual->.name when customAppOrder is empty (AppStore.swift:107-109), per the intentional comment. Whether Manual-with-no-order s",
          "uid": "ORDER-04#6",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "SORT-01",
      "area": "Smart Sorting (activity/name/category/manual)",
      "feature": "Choose sort mode",
      "priority": "P1",
      "userStory": "As a Waves user with many audio apps, I want to choose how the app list is ordered (Activity, Name, Category, or Manual), so that I can find and control the app I care about quickly.",
      "expectedBehavior": [
        "Settings > General exposes a 'Sort apps by' Picker listing all four SortMode cases via SortMode.allCases, labeled by displayName: Activity, Name, Category, Manual (SettingsView.swift:162-175, UserPreferences.swift:116-136).",
        "Selecting a mode writes store.preferences.sortMode and immediately calls store.persistPreferences() so the choice survives relaunch (SettingsView.swift:166-169).",
        "visibleApps applies the chosen mode through sortedApps(_:) every time the list is read, so the mixer list, pinned/live/recent derived lists, and menu-bar sections all reflect the active sort (AppStore.swift:103-128, 1278-1301).",
        "Default sort mode is .name for a fresh install (UserPreferences.swift:7).",
        "On launch, if sortMode is .manual but customAppOrder is empty, the mode is downgraded to .name so manual sort never produces an undefined order (AppStore.swift:85-90)."
      ],
      "acceptanceCriteria": [
        "The Settings picker shows exactly four options (Activity, Name, Category, Manual).",
        "Changing the picker reorders the mixer list without a relaunch.",
        "Relaunching the app preserves the previously selected sort mode (except the manual-with-empty-order fallback to Name).",
        "A fresh install defaults to Name sort.",
        "Launching with sortMode=manual and an empty customAppOrder results in Name sort."
      ],
      "codeRefs": [
        "Sources/Waves/Settings/SettingsView.swift:162-175",
        "Sources/Waves/Stores/UserPreferences.swift:7",
        "Sources/Waves/Stores/UserPreferences.swift:116-136",
        "Sources/Waves/Stores/AppStore.swift:85-90",
        "Sources/Waves/Stores/AppStore.swift:103-107",
        "Sources/Waves/Stores/AppStore.swift:1278-1301"
      ],
      "implStatus": "implemented",
      "implNotes": "Fully wired and persisted. UX gap: the sort-mode control lives only in Settings — there is no in-window/menu-bar affordance to change or even see the current sort, so users may not discover it. The picker write happens outside any AppStore mutation method (direct preferences write + manual persistPreferences call), which works but bypasses any validation/side-effects a setter could centralize.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/ux] Sort mode is only changeable/visible inside Settings — no in-window or menu-bar affordance or current-mode indicator (Sources/Waves/Settings/SettingsView.swift:162-175) — Re-verified: the only sort control is the 'Sort apps by' Picker in Settings > General (SettingsView.swift:162-175). The main mixer window (MainWindowView.swift) and the menu-bar panel (MenuBarMixerView.swift) neither expose the control nor display which sort is active, and there is no VoiceOver announcement of sort state. A user whose list silently flips to Manual on a first drag (reorderApps auto-switch, AppStore.swift:1340-1343) has no on-list indication of why ordering changed and no quick way to switch back. feature-status.json implNotes explicitly call this a 'UX gap'. Acceptance criteria do not mandate an in-window control, so this is a discoverability/UX observation, not a hard failure. | Fix: Add a small sort menu/segmented control to the mixer toolbar (and optionally the menu-bar header) bound to store.preferences.sortMode, showing the active mode, so users can see and change sort without opening Settings.",
      "fixStatus": "1 n/a",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "ux",
          "title": "Sort mode is only changeable/visible inside Settings — no in-window or menu-bar affordance or current-mode indicator",
          "detail": "Re-verified: the only sort control is the 'Sort apps by' Picker in Settings > General (SettingsView.swift:162-175). The main mixer window (MainWindowView.swift) and the menu-bar panel (MenuBarMixerView.swift) neither expose the control nor display which sort is active, and there is no VoiceOver announcement of sort state. A user whose list silently flips to Manual on a first drag (reorderApps auto-switch, AppStore.swift:1340-1343) has no on-list indication of why ordering changed and no quick way to switch back. feature-status.json implNotes explicitly call this a 'UX gap'. Acceptance criteria do not mandate an in-window control, so this is a discoverability/UX observation, not a hard failure.",
          "codeRef": "Sources/Waves/Settings/SettingsView.swift:162-175",
          "expectedVsActual": "Story userStory wants users to 'find and control the app I care about quickly' by choosing a sort; the only sort affordance is buried in a separate Settings window with no current-mode indicator or in-context switch.",
          "suggestedFix": "Add a small sort menu/segmented control to the mixer toolbar (and optionally the menu-bar header) bound to store.preferences.sortMode, showing the active mode, so users can see and change sort without opening Settings.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Settings/SettingsView.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "SORT-01#1",
          "canonicalFile": "Sources/Waves/Settings/SettingsView.swift"
        }
      ]
    },
    {
      "id": "SORT-02",
      "area": "Smart Sorting (activity/name/category/manual)",
      "feature": "Activity sort (most-active apps first)",
      "priority": "P0",
      "userStory": "As a user actively mixing audio, I want apps that are currently making sound to rise to the top, so that the apps I am most likely to adjust are always within reach.",
      "expectedBehavior": [
        "Activity sort ranks each app into five tiers via activityRank: live route (0), managed+unmuted+audible peak/rms>0.001 (1), frontmost/isActive (2), managed but not audible (3), everything else (4) (AppStore.swift:1303-1321).",
        "Apps within the same activity rank are tie-broken alphabetically by displayName using localizedCaseInsensitiveCompare (AppStore.swift:1283-1290, 1323-1325).",
        "Activity sort needs no stored order and is preserved across launches unlike manual (AppStore.swift:85-90 comment).",
        "Ranking reacts to live level metering — peakLevel/rmsLevel updates from the level poller can change an app's tier while sound plays (AppStore.swift:1308, level metering at 611-619)."
      ],
      "acceptanceCriteria": [
        "An app with routingState .live appears above an idle app of any name.",
        "Two apps in the same tier appear in case-insensitive alphabetical order.",
        "A managed app that is muted or silent ranks below an audible managed app.",
        "Switching to Activity sort requires no saved custom order and works on first use."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:1282-1290",
        "Sources/Waves/Stores/AppStore.swift:1303-1321",
        "Sources/Waves/Stores/AppStore.swift:1323-1325"
      ],
      "implStatus": "implemented",
      "implNotes": "Logic is coherent and mirrors the liveApps/activityRank definitions. Correctness nuance: ordering depends on peakLevel/rmsLevel in the session snapshot, which is only refreshed while a mixer surface is visible and the level poller runs; when polling is stopped the audible tier (rank 1) may go stale and apps can appear mis-ranked until the next refresh. Re-sorting on every level tick could also cause rows to visibly jump during playback (potential UX churn), though SwiftUI list diffing softens this.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1H 1L]",
      "errorsFound": "• [HIGH/correctness] Activity sort does not react to live metering — audible (rank 1) and frontmost (rank 2) tiers are stale between manual refreshes (Sources/Waves/Stores/AppStore.swift:1303-1321) — Re-verified in code. activityRank reads app.peakLevel/app.rmsLevel and app.isActive from session.apps (AppStore.swift:1303-1321). Those fields are only written by a full backend.currentSnapshot()/refresh() (computed at WorkspaceAudioControlBackend.swift:1177-1189 for isActive), which runs only on explicit user actions (⌘R, Refresh, onboarding) — there is NO periodic session-refresh timer anywhere in AppStore (the only repeating loop is the level poller at AppStore.swift:619-628). The poller writes the SEPARATE store.liveLevels dictionary via backend.audioLevels(); it never updates session.apps peak/rms. The frontmost observer (AppStore.swift:886-897) only calls checkAutoPauseMusic() and never sets session.apps[].isActive. Therefore an app that starts/stops producing sound, or comes to the foreground, does NOT change its activity tier until the next manual refresh, directly contradicting an explicit expectedBehavior bullet. | Fix: Either (a) merge poller results into session.apps[].peakLevel/rmsLevel and refresh isActive on NSWorkspace.didActivateApplicationNotification, or (b) have activityRank consult store.liveLevels for the audible-tier test (e.g. max(liveLevels[id].peak, liveLevels[id].rms) > 0.001).\n• [LOW/inconsistency] liveApps/recentApps audibility test shares the same stale-level flaw (Sources/Waves/Stores/AppStore.swift:117-122) — Re-verified. liveApps (AppStore.swift:117-122) classifies a managed app as live when max(app.peakLevel, app.rmsLevel) > 0.001 using the static session levels, and recentApps is the complement (AppStore.swift:124-128). Because session peak/rms are not refreshed by the poller, the Live and Recent scope tabs and the menu-bar Live/Recent sections only reflect audibility captured at the last full refresh, so an app that goes silent stays in 'Live' until a manual refresh. Severity low because it is the same root cause as the rank-1 staleness above and is cosmetic for the derived lists. | Fix: Use the same live-levels source for the liveApps/recentApps audibility predicate as proposed for activityRank, so all activity-derived lists agree with the meters.",
      "fixStatus": "1 deferred · 1 n/a",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "correctness",
          "title": "Activity sort does not react to live metering — audible (rank 1) and frontmost (rank 2) tiers are stale between manual refreshes",
          "detail": "Re-verified in code. activityRank reads app.peakLevel/app.rmsLevel and app.isActive from session.apps (AppStore.swift:1303-1321). Those fields are only written by a full backend.currentSnapshot()/refresh() (computed at WorkspaceAudioControlBackend.swift:1177-1189 for isActive), which runs only on explicit user actions (⌘R, Refresh, onboarding) — there is NO periodic session-refresh timer anywhere in AppStore (the only repeating loop is the level poller at AppStore.swift:619-628). The poller writes the SEPARATE store.liveLevels dictionary via backend.audioLevels(); it never updates session.apps peak/rms. The frontmost observer (AppStore.swift:886-897) only calls checkAutoPauseMusic() and never sets session.apps[].isActive. Therefore an app that starts/stops producing sound, or comes to the foreground, does NOT change its activity tier until the next manual refresh, directly contradicting an explicit expectedBehavior bullet.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1303-1321",
          "expectedVsActual": "Story expectedBehavior: 'Ranking reacts to live level metering — peakLevel/rmsLevel updates from the level poller can change an app's tier while sound plays.' Actual: the poller updates only liveLevels (used by row meters at MixerRowView.swift:177-180), while activityRank consults the static session.apps peak/rms and isActive, so the audible/frontmost tiers never move live.",
          "suggestedFix": "Either (a) merge poller results into session.apps[].peakLevel/rmsLevel and refresh isActive on NSWorkspace.didActivateApplicationNotification, or (b) have activityRank consult store.liveLevels for the audible-tier test (e.g. max(liveLevels[id].peak, liveLevels[id].rms) > 0.001).",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-user-decision:Choosing the liveness source of truth and accepting per-tick re-sort cost is a product/perf decision reconciling the story acceptance, not a safe localized edit",
          "uid": "SORT-02#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "liveApps/recentApps audibility test shares the same stale-level flaw",
          "detail": "Re-verified. liveApps (AppStore.swift:117-122) classifies a managed app as live when max(app.peakLevel, app.rmsLevel) > 0.001 using the static session levels, and recentApps is the complement (AppStore.swift:124-128). Because session peak/rms are not refreshed by the poller, the Live and Recent scope tabs and the menu-bar Live/Recent sections only reflect audibility captured at the last full refresh, so an app that goes silent stays in 'Live' until a manual refresh. Severity low because it is the same root cause as the rank-1 staleness above and is cosmetic for the derived lists.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:117-122",
          "expectedVsActual": "Story implies activity-derived live ordering tracks playback in real time; in practice Live/Recent membership only updates on full refresh.",
          "suggestedFix": "Use the same live-levels source for the liveApps/recentApps audibility predicate as proposed for activityRank, so all activity-derived lists agree with the meters.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "SORT-02#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "SORT-03",
      "area": "Smart Sorting (activity/name/category/manual)",
      "feature": "Name and Category sort",
      "priority": "P1",
      "userStory": "As a user scanning a long app list, I want to sort alphabetically by name or grouped by category, so that I can locate a specific app predictably.",
      "expectedBehavior": [
        "Name sort orders all visible apps by displayName using localizedCaseInsensitiveCompare ascending (AppStore.swift:1280-1281, 1323-1325).",
        "Category sort groups apps by AppCategory.rawValue ascending, then tie-breaks within a category by displayName (AppStore.swift:1291-1297).",
        "Categories available: browser, conferencing, media, communication, system, unknown (AudioApp.swift:223-247).",
        "Both sorts are stable across launches and require no stored order."
      ],
      "acceptanceCriteria": [
        "Name sort lists apps A→Z ignoring case.",
        "Category sort keeps apps of the same category contiguous, alphabetized within the group.",
        "Apps with category .unknown sort together as a group.",
        "Neither mode depends on customAppOrder."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:1280-1281",
        "Sources/Waves/Stores/AppStore.swift:1291-1297",
        "Sources/Waves/Stores/AppStore.swift:1323-1325",
        "Sources/WavesAudioCore/Models/AudioApp.swift:223-247"
      ],
      "implStatus": "implemented",
      "implNotes": "Works, but category ordering is by enum rawValue string, not by the human-facing displayName. The resulting group order is browser, communication, conferencing, media, system, unknown (alphabetical by raw value), which is NOT the same as alphabetical by the displayed labels (e.g. 'Communication' vs 'Conferencing' happen to align, but the coupling to rawValue is fragile and there are no visible section headers delineating the groups in the main List, so the grouping reads as a plain sorted list to the user).",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/inconsistency] Stated category group order in the story does not match the code's rawValue ordering (Communication/Conferencing swapped) (Sources/Waves/Stores/AppStore.swift:1291-1297) — Re-verified. Category sort orders by AppCategory.rawValue string ascending (AppStore.swift:1291-1297). Enum cases are declared browser, conferencing, media, communication, system, unknown (AudioApp.swift:222-229), but sorting by the STRING rawValue yields alphabetical order: browser, communication, conferencing, media, system, unknown. The story's expectedBehavior asserts 'browser, conferencing, media, communication, system, unknown', which the code does not produce — communication and conferencing are swapped. All acceptance criteria (contiguity, within-group alpha, unknown grouped, no customAppOrder dependency) are still satisfied, so this is a doc/code prose mismatch, not a functional break. | Fix: Decide the intended group order and make it explicit (sort by a defined category ordinal or by category.displayName, ideally with visible section headers), then update the story to match. Without headers, Category sort reads as a plain sorted list to the user.",
      "fixStatus": "1 n/a",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "Stated category group order in the story does not match the code's rawValue ordering (Communication/Conferencing swapped)",
          "detail": "Re-verified. Category sort orders by AppCategory.rawValue string ascending (AppStore.swift:1291-1297). Enum cases are declared browser, conferencing, media, communication, system, unknown (AudioApp.swift:222-229), but sorting by the STRING rawValue yields alphabetical order: browser, communication, conferencing, media, system, unknown. The story's expectedBehavior asserts 'browser, conferencing, media, communication, system, unknown', which the code does not produce — communication and conferencing are swapped. All acceptance criteria (contiguity, within-group alpha, unknown grouped, no customAppOrder dependency) are still satisfied, so this is a doc/code prose mismatch, not a functional break.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1291-1297",
          "expectedVsActual": "Story prose order browser→conferencing→media→communication→… vs actual alphabetical-by-rawValue browser→communication→conferencing→media→system→unknown.",
          "suggestedFix": "Decide the intended group order and make it explicit (sort by a defined category ordinal or by category.displayName, ideally with visible section headers), then update the story to match. Without headers, Category sort reads as a plain sorted list to the user.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "SORT-03#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "SORT-04",
      "area": "Smart Sorting (activity/name/category/manual)",
      "feature": "Manual drag-to-reorder",
      "priority": "P1",
      "userStory": "As a power user, I want to drag apps into a custom order and have it stick, so that my most-used apps stay exactly where I put them.",
      "expectedBehavior": [
        "Dragging a row triggers List.onMove, which calls store.reorderApps(from:to:) only when the scope filter is .running, sortMode is .manual, and the search field is empty (MainWindowView.swift:369-375).",
        "reorderApps auto-switches sortMode to .manual (and persists) if it was not already, so a first drag converts the list to manual ordering (AppStore.swift:1339-1343).",
        "The new order is computed by Array.move(fromOffsets:toOffset:) over visibleApps logicalIDs, fixing the off-by-one that a naive remove/insert would cause on downward drags (AppStore.swift:1345-1349).",
        "customAppOrder (an array of logicalIDs) is saved and persisted after each move (AppStore.swift:1351-1352).",
        "Manual sort orders apps by their index in customAppOrder; apps not present in the saved order sink to the bottom (firstIndex ?? Int.max) and tie-break by raw displayName comparison (AppStore.swift:1327-1337).",
        "Drag-to-reorder is documented as a feature in README (README.md:27)."
      ],
      "acceptanceCriteria": [
        "Dragging a row while in Running scope with no search reorders the list and the order persists across relaunch.",
        "Performing a drag while in Name/Activity/Category mode switches the app into Manual mode.",
        "A downward drag drops the row on the target position, not one below it.",
        "Reordering is a no-op while a search query is active or while in a non-Running scope.",
        "A newly launched app not in customAppOrder appears at the end of the manual list."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MainWindowView.swift:369-375",
        "Sources/Waves/Stores/AppStore.swift:1327-1337",
        "Sources/Waves/Stores/AppStore.swift:1339-1354",
        "README.md:27"
      ],
      "implStatus": "partial",
      "implNotes": "Core reorder works and the off-by-one is handled. Two real gaps: (1) reorderApps rebuilds customAppOrder from visibleApps only — when showSystemProcesses is off, system apps are excluded from visibleApps, so every manual reorder silently drops their logicalIDs from the saved order; if the user later enables system processes those apps fall to the bottom regardless of any prior manual placement. (2) Manual mode's final tie-break uses raw `app1.displayName < app2.displayName` (case-sensitive, non-localized), inconsistent with the localizedCaseInsensitiveCompare used everywhere else, so unsaved/new apps with mixed-case names can order unexpectedly. Also, onMove can only be invoked in Manual mode + Running scope + empty search, so there is no way to drag-reorder while filtered/searching — acceptable but worth noting.",
      "source": "phase1-story",
      "testStatus": "4 issue(s) [2H 2L]",
      "errorsFound": "• [HIGH/edge-case] Manual reorder silently drops system-app IDs from customAppOrder when system processes are hidden (Sources/Waves/Stores/AppStore.swift:1345-1352) — Re-verified. reorderApps rebuilds customAppOrder from visibleApps only (AppStore.swift:1345: 'var apps = visibleApps.map { $0.logicalID }'). visibleApps excludes system-category apps whenever preferences.showSystemProcesses is false (AppStore.swift:104-106), which is the default (UserPreferences.swift:6). So any single drag while system apps are hidden overwrites the persisted customAppOrder with a list that omits every system app's logicalID. If the user later enables 'Show system processes', those apps are no longer in customAppOrder, so manualOrderComparator gives them firstIndex == Int.max (AppStore.swift:1330-1334) and they all sink to the bottom, permanently losing any earlier manual placement. This is a real persistence/data-loss bug. | Fix: Merge rather than replace: splice the reordered visible IDs back into the full saved customAppOrder, preserving entries for IDs not currently in visibleApps (hidden/system) at their relative positions.\n• [HIGH/correctness] First drag in a non-Manual mode moves the wrong row — reorderApps re-sorts visibleApps to the manual fallback BEFORE applying the move (Sources/Waves/Stores/AppStore.swift:1339-1352) — MISSED BY FIRST TESTER. reorderApps sets preferences.sortMode = .manual at line 1340-1343, and only THEN reads visibleApps at line 1345. Because visibleApps is a live computed property with no cache (invalidateVisibleAppsCache is a no-op, AppStore.swift:134-137), it immediately re-sorts using manualOrderComparator. On a first drag customAppOrder is empty, so every app ties at Int.max and the fallback order is raw case-sensitive displayName ascending (AppStore.swift:1335) — which generally differs from the Activity/Category/Name order the user was actually looking at on screen. The source/destination IndexSet came from the displayed (old-sort) list, so move(fromOffsets:toOffset:) is applied to a DIFFERENTLY ordered array and relocates the wrong app. The off-by-one fix is irrelevant here; the indices themselves point at the wrong elements. | Fix: Capture the displayed order first: snapshot let ids = visibleApps.map(\\.logicalID) BEFORE changing sortMode, apply move() to that snapshot, then set sortMode=.manual and customAppOrder=ids and persist once.\n• [LOW/inconsistency] Manual-mode tie-break uses case-sensitive non-localized comparison, unlike every other sort (Sources/Waves/Stores/AppStore.swift:1335) — Re-verified. manualOrderComparator's final tie-break is 'return app1.displayName < app2.displayName' (AppStore.swift:1335), a raw String comparison that is case-sensitive and non-localized. Every other sort path (name, activity, category) tie-breaks via displayNameComparator with localizedCaseInsensitiveCompare (AppStore.swift:1281, 1289, 1296, 1323-1325). Apps absent from customAppOrder (newly launched) all tie at Int.max and are ordered by this inconsistent comparator, so e.g. 'Zoom' sorts before 'discord' (uppercase < lowercase in ASCII). | Fix: Replace the tie-break with 'return displayNameComparator(app1, app2)' to match the other sort modes.\n• [LOW/ux] onMove is attached unconditionally, so rows show a drag affordance and snap back outside Manual/Running/empty-search (Sources/Waves/Features/Mixer/MainWindowView.swift:369-375) — Re-verified. The List installs .onMove on the ForEach unconditionally (MainWindowView.swift:369-375) with no .moveDisabled guard; only the closure body calls store.reorderApps, and only when filter == .running && sortMode == .manual && searchText.isEmpty. In Name/Activity/Category mode, in a non-Running scope, or while searching, the OS still presents the row as draggable, animates the drag, then snaps it back with no explanation — a dead/broken-feeling control. | Fix: Gate the affordance, not just the action: attach .onMove (or .moveDisabled(false)) only when filter == .running && sortMode == .manual && searchText.isEmpty; otherwise suppress the drag, or surface a hint that dragging requires Manual sort in the Running scope.",
      "fixStatus": "4 fixed",
      "fixNotes": "tier1: FU-REORDER-STALE-SNAPSHOT | tier2: FU-ONMOVE-AFFORDANCE-GATING | tier3: FU-MANUAL-TIEBREAK-LOCALIZED",
      "retestStatus": "PASS (remediated & verified)",
      "issues": [
        {
          "severity": "high",
          "type": "edge-case",
          "title": "Manual reorder silently drops system-app IDs from customAppOrder when system processes are hidden",
          "detail": "Re-verified. reorderApps rebuilds customAppOrder from visibleApps only (AppStore.swift:1345: 'var apps = visibleApps.map { $0.logicalID }'). visibleApps excludes system-category apps whenever preferences.showSystemProcesses is false (AppStore.swift:104-106), which is the default (UserPreferences.swift:6). So any single drag while system apps are hidden overwrites the persisted customAppOrder with a list that omits every system app's logicalID. If the user later enables 'Show system processes', those apps are no longer in customAppOrder, so manualOrderComparator gives them firstIndex == Int.max (AppStore.swift:1330-1334) and they all sink to the bottom, permanently losing any earlier manual placement. This is a real persistence/data-loss bug.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1345-1352",
          "expectedVsActual": "Story: 'my most-used apps stay exactly where I put them' and manual order persists. Actual: reordering with system apps hidden destroys system apps' saved positions, and they fall to the end if shown later.",
          "suggestedFix": "Merge rather than replace: splice the reordered visible IDs back into the full saved customAppOrder, preserving entries for IDs not currently in visibleApps (hidden/system) at their relative positions.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:FU-REORDER-STALE-SNAPSHOT",
          "uid": "SORT-04#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-REORDER-STALE-SNAPSHOT",
          "fixTier": "tier1"
        },
        {
          "severity": "high",
          "type": "correctness",
          "title": "First drag in a non-Manual mode moves the wrong row — reorderApps re-sorts visibleApps to the manual fallback BEFORE applying the move",
          "detail": "MISSED BY FIRST TESTER. reorderApps sets preferences.sortMode = .manual at line 1340-1343, and only THEN reads visibleApps at line 1345. Because visibleApps is a live computed property with no cache (invalidateVisibleAppsCache is a no-op, AppStore.swift:134-137), it immediately re-sorts using manualOrderComparator. On a first drag customAppOrder is empty, so every app ties at Int.max and the fallback order is raw case-sensitive displayName ascending (AppStore.swift:1335) — which generally differs from the Activity/Category/Name order the user was actually looking at on screen. The source/destination IndexSet came from the displayed (old-sort) list, so move(fromOffsets:toOffset:) is applied to a DIFFERENTLY ordered array and relocates the wrong app. The off-by-one fix is irrelevant here; the indices themselves point at the wrong elements.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1339-1352",
          "expectedVsActual": "Acceptance: 'Performing a drag while in Name/Activity/Category mode switches the app into Manual mode' and the dragged row lands where dropped. Actual: switching the mode first re-sorts the underlying array, so the first drag in Activity/Category (and case-mismatched Name) order moves an unintended row.",
          "suggestedFix": "Capture the displayed order first: snapshot let ids = visibleApps.map(\\.logicalID) BEFORE changing sortMode, apply move() to that snapshot, then set sortMode=.manual and customAppOrder=ids and persist once.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:FU-REORDER-STALE-SNAPSHOT",
          "uid": "SORT-04#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-REORDER-STALE-SNAPSHOT",
          "fixTier": "tier1"
        },
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "Manual-mode tie-break uses case-sensitive non-localized comparison, unlike every other sort",
          "detail": "Re-verified. manualOrderComparator's final tie-break is 'return app1.displayName < app2.displayName' (AppStore.swift:1335), a raw String comparison that is case-sensitive and non-localized. Every other sort path (name, activity, category) tie-breaks via displayNameComparator with localizedCaseInsensitiveCompare (AppStore.swift:1281, 1289, 1296, 1323-1325). Apps absent from customAppOrder (newly launched) all tie at Int.max and are ordered by this inconsistent comparator, so e.g. 'Zoom' sorts before 'discord' (uppercase < lowercase in ASCII).",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1335",
          "expectedVsActual": "Expected: apps with equal manual index ordered consistently (case-insensitive, localized) like the rest of the app. Actual: ASCII case-sensitive ordering for new apps at the bottom of the manual list.",
          "suggestedFix": "Replace the tie-break with 'return displayNameComparator(app1, app2)' to match the other sort modes.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "gapfill:already-resolved",
          "uid": "SORT-04#3",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "earlier-tier",
          "fixTier": "tier3"
        },
        {
          "severity": "low",
          "type": "ux",
          "title": "onMove is attached unconditionally, so rows show a drag affordance and snap back outside Manual/Running/empty-search",
          "detail": "Re-verified. The List installs .onMove on the ForEach unconditionally (MainWindowView.swift:369-375) with no .moveDisabled guard; only the closure body calls store.reorderApps, and only when filter == .running && sortMode == .manual && searchText.isEmpty. In Name/Activity/Category mode, in a non-Running scope, or while searching, the OS still presents the row as draggable, animates the drag, then snaps it back with no explanation — a dead/broken-feeling control.",
          "codeRef": "Sources/Waves/Features/Mixer/MainWindowView.swift:369-375",
          "expectedVsActual": "Acceptance: 'Reordering is a no-op while a search query is active or while in a non-Running scope.' Functionally a no-op, but the UI still affords dragging and gives no signal.",
          "suggestedFix": "Gate the affordance, not just the action: attach .onMove (or .moveDisabled(false)) only when filter == .running && sortMode == .manual && searchText.isEmpty; otherwise suppress the drag, or surface a hint that dragging requires Manual sort in the Running scope.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "disposition": "tier2:applied:FU-ONMOVE-AFFORDANCE-GATING",
          "uid": "SORT-04#4",
          "canonicalFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-ONMOVE-AFFORDANCE-GATING",
          "fixTier": "tier2"
        }
      ]
    },
    {
      "id": "SORT-05",
      "area": "Smart Sorting (activity/name/category/manual)",
      "feature": "Sort interaction with pinned apps and scoped/searched lists",
      "priority": "P2",
      "userStory": "As a user who pins important apps and filters by scope, I want sorting to behave consistently across pinned, live, recent, and search results, so that ordering never feels arbitrary.",
      "expectedBehavior": [
        "pinnedApps, activeApps, liveApps, and recentApps are all derived by filtering the already-sorted visibleApps, so every scope inherits the active sort order (AppStore.swift:109-128).",
        "Search filters across all visibleApps (not just the current scope) and preserves the active sort order, matching on displayName or category displayName (MainWindowView.swift:115-130).",
        "The menu-bar panel renders Pinned, Live, and Recent sections, each drawing from the sorted derived lists (MenuBarMixerView.swift:42-52)."
      ],
      "acceptanceCriteria": [
        "Within the Pinned scope, apps follow the same sort mode as the Running scope.",
        "A search result list stays ordered per the active sort mode.",
        "Pinning/unpinning an app does not change the sort order of the surrounding list (pin only changes scope membership)."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:109-128",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:102-130",
        "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:42-52"
      ],
      "implStatus": "partial",
      "implNotes": "Sorting is consistently inherited by all scopes/search, which is good. However, pinned apps do NOT float to the top of the main Running list — sortedApps never consults isPinned, so a pinned app sits wherever its name/activity/category/manual rank places it. Users may expect pins to surface to the top in the unified list (they only get their own section in the menu bar and the Pinned scope tab). This is a defensible design choice but is an easy point of confusion and is not surfaced anywhere in the UI.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/ux] Menu-bar sections can show the same app twice (Pinned + Live, or Pinned + Recent) (Sources/Waves/Features/Mixer/MenuBarMixerView.swift:42-50) — Re-verified. MenuBarMixerView renders Pinned (store.pinnedApps), Live (store.liveApps), and Recent (store.recentApps) as independent CompactSections (MenuBarMixerView.swift:42-50), and CompactSection (line 177-204) just renders whatever apps it is handed with no cross-section dedup. pinnedApps is visibleApps filtered by isPinned (AppStore.swift:109-111) with no exclusion of live/recent; recentApps is visibleApps minus liveIDs (AppStore.swift:124-128) with no exclusion of pinned. So a pinned audible app appears in both Pinned and Live; a pinned idle app appears in both Pinned and Recent — two separate row instances controlling the same app. | Fix: De-duplicate across sections (e.g. exclude pinned apps from Live/Recent, or exclude live apps from Recent and pinned-but-live from Pinned), or label that sections may overlap.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier1: FU-MENU-SECTION-DEDUP | tier2: FU-MENU-SECTION-DEDUP",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "ux",
          "title": "Menu-bar sections can show the same app twice (Pinned + Live, or Pinned + Recent)",
          "detail": "Re-verified. MenuBarMixerView renders Pinned (store.pinnedApps), Live (store.liveApps), and Recent (store.recentApps) as independent CompactSections (MenuBarMixerView.swift:42-50), and CompactSection (line 177-204) just renders whatever apps it is handed with no cross-section dedup. pinnedApps is visibleApps filtered by isPinned (AppStore.swift:109-111) with no exclusion of live/recent; recentApps is visibleApps minus liveIDs (AppStore.swift:124-128) with no exclusion of pinned. So a pinned audible app appears in both Pinned and Live; a pinned idle app appears in both Pinned and Recent — two separate row instances controlling the same app.",
          "codeRef": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:42-50",
          "expectedVsActual": "Story wants ordering across pinned/live/recent to never 'feel arbitrary'; showing the identical app in two sections is confusing and makes a single change appear in two places.",
          "suggestedFix": "De-duplicate across sections (e.g. exclude pinned apps from Live/Recent, or exclude live apps from Recent and pinned-but-live from Pinned), or label that sections may overlap.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift",
          "disposition": "tier2:applied:FU-MENU-SECTION-DEDUP",
          "uid": "SORT-05#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-MENU-SECTION-DEDUP",
          "fixTier": "tier2"
        }
      ]
    },
    {
      "id": "LVL-01",
      "area": "Live Level Meters",
      "feature": "Real-time per-app level metering on the row",
      "priority": "P1",
      "userStory": "As someone running several audio apps, I want each managed app's row to show a live cyan meter that moves with its actual output, so that I can tell at a glance which app is making sound and how loud it is.",
      "expectedBehavior": [
        "A cyan capsule meter is overlaid at the bottom-leading edge of each MixerRowView, sized to proxy.size.width * meterLevel at a fixed 2pt height, drawn as a non-layout-shifting overlay (MixerRowView.swift:122-134).",
        "meterLevel reads store.liveLevels[app.logicalID] and returns min(1, max(rms, peak*0.7)); apps with no entry render 0 (MixerRowView.swift:177-180).",
        "Levels originate on the realtime IO render thread: renderTappedAudio computes peak/RMS from the original (pre-scale) input buffer via TapDSP.levels and stores them with writeLevels (WorkspaceAudioControlBackend.swift:1924-1937).",
        "TapDSP.levels normalizes float32/int16/int32 samples to [0,1] returning (peak, sum, sampleCount); RMS is sqrt(sum/sampleCount) (TapDSP.swift:56-103).",
        "The backend's levelUpdateTask copies each active controller's getCurrentLevels() into the snapshot every 250ms (WorkspaceAudioControlBackend.swift:1371-1403).",
        "audioLevels() returns AudioLevels(peak,rms) keyed by logicalID for apps in .managed or .live routing state (WorkspaceAudioControlBackend.swift:1096-1102).",
        "The meter animates with a 0.18s linear animation, disabled under Reduce Motion (MixerRowView.swift:129)."
      ],
      "acceptanceCriteria": [
        "When a managed app is actively playing audio, its row shows a non-zero cyan meter whose width tracks the audio level within ~0.3-0.5s.",
        "A silent managed app shows a zero-width (invisible) meter.",
        "TapDSP.levels of pure silence yields peak 0 and rms 0; full-scale [1,-1,1,-1] float input yields peak 1.0 and rms 1.0 (covered by TapDSPTests).",
        "The meter overlay never changes the row's layout/height regardless of level.",
        "With Reduce Motion enabled, the meter updates without animation."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MixerRowView.swift:122-134",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:177-180",
        "Sources/WavesAudioCore/Audio/TapDSP.swift:56-103",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1924-1937",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1096-1102",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1371-1403"
      ],
      "implStatus": "implemented",
      "implNotes": "End-to-end path is real and wired: realtime DSP -> per-controller box -> 250ms snapshot copy -> 300ms store poll -> SwiftUI overlay. DSP math is unit-tested. Effective UI refresh latency is the sum of two un-phase-aligned timers (250ms backend + 300ms store), so worst-case meter lag is ~550ms and the visual cadence is coarse (~3 fps) for a 'real-time' meter — fine for an activity indicator, not a true VU meter. meterLevel blends rms with peak*0.7 (a smoothing choice, not standard peak metering); there is no peak-hold or decay envelope, so the bar can look jumpy/steppy at the poll rate.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1M 1L]",
      "errorsFound": "• [MEDIUM/inconsistency] Menu-bar mixer rows show no per-app level meter at all (only the main window does) (Sources/Waves/Features/Mixer/MixerRowView.swift:259-327) — The story's core promise is that 'each managed app's row shows a live cyan meter' and the AC says 'When a managed app is actively playing audio, its row shows a non-zero cyan meter.' The main-window MixerRowView renders the Capsule meter overlay reading store.liveLevels (MixerRowView.swift:122-134, 177-180), but the menu-bar surface uses CompactMixerRow (MenuBarMixerView.swift:191 -> MixerRowView.swift:259-327), which has NO meter overlay, no meterLevel, no showsLevelMeter, and no liveLevels read anywhere in MenuBarMixerView. A user living in the menu bar gets only a RoutingStateDot, never the moving cyan meter. Worse, MenuBarMixerView.onAppear calls beginLiveLevels() (MenuBarMixerView.swift:113), so when only the menu bar is open the 300ms poll runs and populates store.liveLevels with nothing on screen consuming it -- pure overhead that also undercuts LVL-02's near-zero-idle-CPU goal. This is a state desync between the menu-bar and main-window surfaces the first tester missed. | Fix: Render the same meter overlay (or an equivalent compact level indicator) in CompactMixerRow using store.liveLevels[app.logicalID], so the menu-bar surface honors the per-row meter feature; otherwise gate beginLiveLevels() to surfaces that actually draw meters so the menu-bar-only case does not poll needlessly.\n• [LOW/inconsistency] Worst-case meter latency (~550ms) exceeds the story's ~0.3-0.5s tracking window (Sources/Waves/Stores/AppStore.swift:621) — End-to-end refresh is the sum of two un-phase-aligned timers: the backend levelUpdateTask sleeps 250ms (WorkspaceAudioControlBackend.swift:1375) before copying controller levels into the snapshot, and the store poll sleeps 300ms (AppStore.swift:621) before reading audioLevels(). When a store poll fires just before a backend update lands, the displayed level is one full backend cycle stale, so worst-case visible lag is ~250+300 = ~550ms, beyond the ~0.3-0.5s acceptance bound. The bar also refreshes at only ~3fps (steps rather than glides) and there is no peak-hold/decay. Re-read of both timers confirms the math; the feature-status implNotes already acknowledge the ~550ms figure. | Fix: Drive the store poll from the backend's update cadence (e.g. an AsyncStream yielded on each updateAudioLevels), or shorten/align the two intervals (backend 100-150ms, store 100ms) so combined latency stays under 500ms. Add light peak-hold/decay so the bar reads smoothly between samples.",
      "fixStatus": "1 fixed · 1 n/a",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "inconsistency",
          "title": "Menu-bar mixer rows show no per-app level meter at all (only the main window does)",
          "detail": "The story's core promise is that 'each managed app's row shows a live cyan meter' and the AC says 'When a managed app is actively playing audio, its row shows a non-zero cyan meter.' The main-window MixerRowView renders the Capsule meter overlay reading store.liveLevels (MixerRowView.swift:122-134, 177-180), but the menu-bar surface uses CompactMixerRow (MenuBarMixerView.swift:191 -> MixerRowView.swift:259-327), which has NO meter overlay, no meterLevel, no showsLevelMeter, and no liveLevels read anywhere in MenuBarMixerView. A user living in the menu bar gets only a RoutingStateDot, never the moving cyan meter. Worse, MenuBarMixerView.onAppear calls beginLiveLevels() (MenuBarMixerView.swift:113), so when only the menu bar is open the 300ms poll runs and populates store.liveLevels with nothing on screen consuming it -- pure overhead that also undercuts LVL-02's near-zero-idle-CPU goal. This is a state desync between the menu-bar and main-window surfaces the first tester missed.",
          "codeRef": "Sources/Waves/Features/Mixer/MixerRowView.swift:259-327",
          "expectedVsActual": "Expected: a managed playing app's row shows a moving cyan meter in any mixer surface, and polling only runs when a meter is actually visible. Actual: the menu-bar row renders no meter, yet opening the menu bar still starts the level poll, spending CPU on data no menu-bar row displays.",
          "suggestedFix": "Render the same meter overlay (or an equivalent compact level indicator) in CompactMixerRow using store.liveLevels[app.logicalID], so the menu-bar surface honors the per-row meter feature; otherwise gate beginLiveLevels() to surfaces that actually draw meters so the menu-bar-only case does not poll needlessly.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "disposition": "iter3:implemented",
          "uid": "LVL-01#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "fixStatus": "applied",
          "fixedBy": "iter3-ui-parity",
          "fixTier": "iter3"
        },
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "Worst-case meter latency (~550ms) exceeds the story's ~0.3-0.5s tracking window",
          "detail": "End-to-end refresh is the sum of two un-phase-aligned timers: the backend levelUpdateTask sleeps 250ms (WorkspaceAudioControlBackend.swift:1375) before copying controller levels into the snapshot, and the store poll sleeps 300ms (AppStore.swift:621) before reading audioLevels(). When a store poll fires just before a backend update lands, the displayed level is one full backend cycle stale, so worst-case visible lag is ~250+300 = ~550ms, beyond the ~0.3-0.5s acceptance bound. The bar also refreshes at only ~3fps (steps rather than glides) and there is no peak-hold/decay. Re-read of both timers confirms the math; the feature-status implNotes already acknowledge the ~550ms figure.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:621",
          "expectedVsActual": "Expected: meter width tracks the audio level within ~0.3-0.5s. Actual: worst-case ~0.55s with coarse ~3fps cadence because two independent, unaligned 250ms/300ms timers stack.",
          "suggestedFix": "Drive the store poll from the backend's update cadence (e.g. an AsyncStream yielded on each updateAudioLevels), or shorten/align the two intervals (backend 100-150ms, store 100ms) so combined latency stays under 500ms. Add light peak-hold/decay so the bar reads smoothly between samples.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "LVL-01#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "LVL-02",
      "area": "Live Level Meters",
      "feature": "Visibility-gated, reference-counted level polling",
      "priority": "P2",
      "userStory": "As a user who keeps Waves running in the menu bar all day, I want level polling to run only while a mixer surface is actually on screen, so that the app uses near-zero CPU when I'm not looking at it.",
      "expectedBehavior": [
        "beginLiveLevels() increments liveLevelsRefcount and starts a single poll Task only if none exists; multiple visible surfaces share one poller (AppStore.swift:616-630).",
        "The poll loops every 300ms, awaits backend.audioLevels(), and assigns store.liveLevels only when the value changed and the task isn't cancelled (AppStore.swift:619-628).",
        "endLiveLevels() decrements the refcount, and when it reaches 0 cancels the task, nils it, and clears liveLevels to [:] (AppStore.swift:632-637).",
        "Both MainWindowView and MenuBarMixerView call beginLiveLevels() onAppear and endLiveLevels() onDisappear (MainWindowView.swift:98-99, MenuBarMixerView.swift:113-114).",
        "Assignments are skipped when levels == self.liveLevels to avoid needless SwiftUI redraws (AppStore.swift:626)."
      ],
      "acceptanceCriteria": [
        "Opening either the main window or the menu-bar panel starts exactly one poll task; opening the second surface does not start a second task.",
        "Closing all mixer surfaces stops the poll task and resets store.liveLevels to empty, hiding all meters.",
        "While no surface is visible, no audioLevels() polling occurs.",
        "Re-opening a surface after full teardown restarts polling cleanly.",
        "An unchanged levels snapshot does not trigger a store mutation/redraw."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:56-57",
        "Sources/Waves/Stores/AppStore.swift:616-637",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:98-99",
        "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:113-114"
      ],
      "implStatus": "implemented",
      "implNotes": "Refcounting and teardown are correct and the dual-surface sharing works. Note the gate only stops the *store* poll; the backend's 250ms levelUpdateTask keeps running unconditionally whenever any controllers exist (started in init, WorkspaceAudioControlBackend.swift:33/1371), so the realtime metering work continues even with no UI visible — the CPU win is partial, not total. Also endLiveLevels clamps the refcount at 0, so an unbalanced extra endLiveLevels() call is harmless but a mismatched begin/end (e.g. onAppear without onDisappear) could leave the poller running.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/edge-case] Unbalanced onAppear/onDisappear can leave the poll task running with no visible surface (Sources/Waves/Stores/AppStore.swift:632-637) — beginLiveLevels()/endLiveLevels() balance only if every onAppear is paired with exactly one onDisappear (MainWindowView.swift:98-99, MenuBarMixerView.swift:113-114). SwiftUI does not guarantee this in all teardown paths (a MenuBarExtra(.window) panel dismissed in certain ways, or a view that fires onAppear twice). endLiveLevels clamps the refcount at 0 (AppStore.swift:633), so a surplus end is harmless, but a surplus begin (onAppear without a matching onDisappear) leaves liveLevelsRefcount > 0, so levelPollTask never cancels and audioLevels() keeps polling with nothing on screen -- defeating the near-zero-idle-CPU goal. Verified: the refcount/teardown logic itself is correct; the risk is purely SwiftUI lifecycle fragility, hence 'likely' not 'confirmed'. | Fix: Make teardown idempotent against a lost onDisappear: also begin/end on scenePhase/window-occlusion, or cap the poller with a 'no consumer for N seconds' self-stop, and consider keying refcounting to surface identity rather than raw increment/decrement.",
      "fixStatus": "1 n/a",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "edge-case",
          "title": "Unbalanced onAppear/onDisappear can leave the poll task running with no visible surface",
          "detail": "beginLiveLevels()/endLiveLevels() balance only if every onAppear is paired with exactly one onDisappear (MainWindowView.swift:98-99, MenuBarMixerView.swift:113-114). SwiftUI does not guarantee this in all teardown paths (a MenuBarExtra(.window) panel dismissed in certain ways, or a view that fires onAppear twice). endLiveLevels clamps the refcount at 0 (AppStore.swift:633), so a surplus end is harmless, but a surplus begin (onAppear without a matching onDisappear) leaves liveLevelsRefcount > 0, so levelPollTask never cancels and audioLevels() keeps polling with nothing on screen -- defeating the near-zero-idle-CPU goal. Verified: the refcount/teardown logic itself is correct; the risk is purely SwiftUI lifecycle fragility, hence 'likely' not 'confirmed'.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:632-637",
          "expectedVsActual": "Expected: closing all mixer surfaces stops the poll task and resets liveLevels; no polling while nothing is visible. Actual: relies on perfectly balanced SwiftUI onAppear/onDisappear; an unmatched begin strands the poller indefinitely.",
          "suggestedFix": "Make teardown idempotent against a lost onDisappear: also begin/end on scenePhase/window-occlusion, or cap the poller with a 'no consumer for N seconds' self-stop, and consider keying refcounting to surface identity rather than raw increment/decrement.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "LVL-02#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "LVL-03",
      "area": "Live Level Meters",
      "feature": "Meter suppression for muted, excluded, and unmanaged rows",
      "priority": "P1",
      "userStory": "As a user, I want the level meter to disappear when an app is muted, excluded, or not under Waves's control, so that the meter only ever signals genuinely live, managed audio and never gives a misleading reading.",
      "expectedBehavior": [
        "showsLevelMeter is true only when the app is not muted, not excluded, and routingState is .managed or .live (MixerRowView.swift:173-175); otherwise no meter overlay is rendered.",
        "On mute, setMuted zeroes the app's peakLevel/rmsLevel in the snapshot (WorkspaceAudioControlBackend.swift:95-96), and the render IO proc zero-fills output and returns early without writing new levels when isMuted (WorkspaceAudioControlBackend.swift:1695-1698).",
        "audioLevels() excludes apps that are not .managed/.live, so excluded/monitor-only/recent apps never receive a liveLevels entry (WorkspaceAudioControlBackend.swift:1098).",
        "The meter overlay is allowsHitTesting(false) and accessibilityHidden(true), and the whole row dims to 0.55 opacity when excluded (MixerRowView.swift:131-132, 109).",
        "On route error the levels are forced to 0 (WorkspaceAudioControlBackend.swift:104-105)."
      ],
      "acceptanceCriteria": [
        "Muting a playing app removes its meter (meter width collapses to 0) and the underlying levels report 0.",
        "Excluding an app hides its meter regardless of whether it is producing audio.",
        "An app in .monitorOnly/.recent/.error state never shows a meter.",
        "The meter never intercepts clicks and is invisible to VoiceOver.",
        "An app whose route entered .error shows no meter."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MixerRowView.swift:173-175",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:109",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:131-132",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:95-96",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1098",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1695-1698"
      ],
      "implStatus": "implemented",
      "implNotes": "Suppression is correct and defensive at multiple layers (UI gate, snapshot zeroing on mute, render-thread early return, audioLevels filtering). One subtlety: on mute the snapshot is zeroed once, but the backend levelUpdateTask only overwrites levels for active controllers via getCurrentLevels(); since the muted render path returns before writeLevels, the controller's last-written non-zero level can persist in the box until the next render — however updateAudioLevels still copies that stale value. In practice the explicit snapshot zero in setMuted plus the UI gate (showsLevelMeter requires !isMuted) mask this, so the user never sees a stale meter on a muted row.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1H]",
      "errorsFound": "• [HIGH/correctness] A managed, non-muted app at volume 0 shows a frozen non-zero meter (misleading reading) (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1702-1705) — Dragging an app's slider to 0 (not muted) keeps routingState == .managed and does NOT zero peakLevel/rmsLevel (AppStore.swift:356-382; contrast setMuted which zeroes them at WorkspaceAudioControlBackend.swift:95-96). In the IO render proc, volume == 0.0 takes the early return at WorkspaceAudioControlBackend.swift:1702-1705, which zero-fills output and returns BEFORE renderTappedAudio -- and writeLevels is called ONLY at the end of renderTappedAudio (verified: sole call site at line 1937). So the controller's TapRenderStateBox retains its last non-zero peak/rms. The controller stays isActive (volume 0 does not call setInactive), so updateAudioLevels() keeps copying that stale value into snapshot.apps[].peakLevel/rmsLevel (1396-1401), audioLevels() returns it (1099), the 300ms poll writes it into store.liveLevels, and showsLevelMeter is true (!isMuted && !isExcluded && .managed). The main-window row therefore displays a static non-zero cyan bar for an app emitting pure silence -- the 'misleading reading' the story says must never happen. The muted and isActive==0 branches similarly skip writeLevels, but mute is masked by the snapshot zero + !isMuted gate; only the volume-0 case leaks. | Fix: Before the volume==0 early return (and ideally the isMuted and isActive==0 branches) call stateBox.writeLevels(peakLevel: 0, rmsLevel: 0). Alternatively treat volume==0 as silence in updateAudioLevels, or have setDesiredVolume(0) zero peakLevel/rmsLevel in the snapshot the way setMuted does.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier1: FU-STALE-METERS-ON-SILENCE",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "correctness",
          "title": "A managed, non-muted app at volume 0 shows a frozen non-zero meter (misleading reading)",
          "detail": "Dragging an app's slider to 0 (not muted) keeps routingState == .managed and does NOT zero peakLevel/rmsLevel (AppStore.swift:356-382; contrast setMuted which zeroes them at WorkspaceAudioControlBackend.swift:95-96). In the IO render proc, volume == 0.0 takes the early return at WorkspaceAudioControlBackend.swift:1702-1705, which zero-fills output and returns BEFORE renderTappedAudio -- and writeLevels is called ONLY at the end of renderTappedAudio (verified: sole call site at line 1937). So the controller's TapRenderStateBox retains its last non-zero peak/rms. The controller stays isActive (volume 0 does not call setInactive), so updateAudioLevels() keeps copying that stale value into snapshot.apps[].peakLevel/rmsLevel (1396-1401), audioLevels() returns it (1099), the 300ms poll writes it into store.liveLevels, and showsLevelMeter is true (!isMuted && !isExcluded && .managed). The main-window row therefore displays a static non-zero cyan bar for an app emitting pure silence -- the 'misleading reading' the story says must never happen. The muted and isActive==0 branches similarly skip writeLevels, but mute is masked by the snapshot zero + !isMuted gate; only the volume-0 case leaks.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1702-1705",
          "expectedVsActual": "Expected: the meter only ever signals genuinely live, managed audio; a silent managed app shows a zero-width meter. Actual: a managed, unmuted, volume-0 app freezes its meter at the last non-zero level because the render path returns before writing 0 and nothing else clears the stale value.",
          "suggestedFix": "Before the volume==0 early return (and ideally the isMuted and isActive==0 branches) call stateBox.writeLevels(peakLevel: 0, rmsLevel: 0). Alternatively treat volume==0 as silence in updateAudioLevels, or have setDesiredVolume(0) zero peakLevel/rmsLevel in the snapshot the way setMuted does.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "tier1:applied:FU-STALE-METERS-ON-SILENCE",
          "uid": "LVL-03#1",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-STALE-METERS-ON-SILENCE",
          "fixTier": "tier1"
        }
      ]
    },
    {
      "id": "LVL-04",
      "area": "Live Level Meters",
      "feature": "Activity grouping and 'Playing audio' subtitle driven by levels",
      "priority": "P2",
      "userStory": "As a user scanning the mixer, I want apps that are currently producing sound to be labeled 'Playing audio' and grouped as live, so that I can find the noisy app without studying every row.",
      "expectedBehavior": [
        "liveApps includes apps in .live state, or .managed-and-unmuted apps whose max(peakLevel, rmsLevel) > 0.001 (AppStore.swift:117-122).",
        "The row subtitle shows 'Playing audio' for .live apps, and for .managed unmuted apps with max(peakLevel,rmsLevel) > 0.001 (MixerRowView.swift:185-191).",
        "The .live routing state renders a distinct waveform chip and cyan tint via RoutingStateIndicator/RoutingStateDot (MixerRowView.swift:396-403, 458-464, 504-509).",
        "An activity-based sort/categorization path uses the same level threshold to classify live vs recent apps (AppStore.swift:124-127, 1304-1316)."
      ],
      "acceptanceCriteria": [
        "A managed, unmuted app with measurable output (>0.001) is labeled 'Playing audio' and counted in liveApps.",
        "A .live app shows 'Playing audio' even before managed metering kicks in.",
        "A silent or muted managed app is not labeled 'Playing audio'.",
        "The .live state row shows the waveform indicator in cyan."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:117-122",
        "Sources/Waves/Stores/AppStore.swift:1304-1316",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:185-191",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:458-464"
      ],
      "implStatus": "partial",
      "implNotes": "The subtitle/grouping logic reads app.peakLevel/app.rmsLevel from the `session` snapshot, NOT from the visibility-gated store.liveLevels dict that drives the meter bar. The session's peak/rms are only refreshed by an explicit refresh() (manual or device-change), backend.start(), or specific state transitions — there is no periodic session refresh loop. So the 'Playing audio' subtitle and live-vs-recent grouping can be stale relative to the moving meter: a row's cyan meter (LVL-01) updates every ~300ms while its 'Playing audio' label may not update until the next refresh. This is a real consistency gap between two level-derived UI signals reading from two different data sources at very different cadences.",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [1H 2M]",
      "errorsFound": "• [HIGH/inconsistency] 'Playing audio' label and live grouping read stale session levels, not the live meter data (Sources/Waves/Features/Mixer/MixerRowView.swift:187) — The subtitle (MixerRowView.swift:185-191), liveApps (AppStore.swift:117-122), recentApps grouping (AppStore.swift:124-128), and activityRank (AppStore.swift:1303-1320) all gate on max(app.peakLevel, app.rmsLevel) > 0.001 read from store.session. Verified there is NO periodic loop that refreshes session levels: the only periodic loop in AppStore is the level poll at 619-628, which writes store.liveLevels and never touches session.apps[].peakLevel; session peak/rms is updated only by explicit refresh()/start()/currentSnapshot() on user actions, device changes, or termination (none timer-driven). Meanwhile the meter bar (LVL-01) reads store.liveLevels, refreshed every ~300ms. Result: when a managed app starts playing, its cyan meter moves but its subtitle stays NOT 'Playing audio' (and it is not counted in liveApps / can be mis-sorted) until the next manual/structural refresh -- violating the AC 'A managed, unmuted app with measurable output (>0.001) is labeled Playing audio.' | Fix: Feed store.liveLevels into the label/grouping: compute 'isPlaying' from store.liveLevels[app.logicalID] (falling back to session) in subtitle/liveApps/activityRank, or have the level poll also update session.apps[].peakLevel/rmsLevel (debounced) so both UI signals share one cadence.\n• [MEDIUM/correctness] App that went silent can stay labeled 'Playing audio' due to stale session levels (Sources/Waves/Stores/AppStore.swift:120) — Because session.peakLevel/rmsLevel is not periodically refreshed (verified above), a managed app that was playing and then went silent retains peakLevel/rmsLevel > 0.001 in session until the next event-driven refresh(). The subtitle (MixerRowView.swift:185-191) and liveApps (AppStore.swift:120) therefore keep reporting 'Playing audio' for an app that is now silent -- violating the AC 'A silent or muted managed app is not labeled Playing audio.' (Mute is handled because setMuted zeroes session levels and termination zeroes them at AppStore.swift:929-930; plain silence on a still-managed app is not.) | Fix: Derive the playing state from the live-polled store.liveLevels (which decays toward 0 from the realtime path) instead of the static session snapshot, or refresh session levels on the poll cadence.\n• [MEDIUM/inconsistency] Menu-bar icon's 3-wave 'live' glyph is stale for managed apps that start/stop playing (Sources/Waves/Stores/AppStore.swift:149-150) — menuBarIconName (AppStore.swift:143-153) returns 'speaker.wave.3.fill' only when !liveApps.isEmpty, and liveApps (AppStore.swift:117-122) classifies a managed app as live via max(session.peakLevel, session.rmsLevel) > 0.001. Since session levels are never periodically refreshed (same root cause as the two issues above), a managed app that begins playing does not flip the menu-bar icon to the 3-wave glyph until the next event-driven refresh, and one that goes silent keeps the icon in the live state. This violates the feature's AC 'The menu-bar icon reflects a live state with the 3-wave speaker glyph' and the Live-section AC for managed-audible apps. Purely .live (unmanaged) apps still flip the icon correctly; only the managed-audible path is stale. | Fix: Have liveApps/menuBarIconName consult store.liveLevels for managed-audible classification (the same cadence as the meter), or refresh session levels on the poll cadence so the icon, the Live section, and the meter share one source of truth.",
      "fixStatus": "3 deferred",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "inconsistency",
          "title": "'Playing audio' label and live grouping read stale session levels, not the live meter data",
          "detail": "The subtitle (MixerRowView.swift:185-191), liveApps (AppStore.swift:117-122), recentApps grouping (AppStore.swift:124-128), and activityRank (AppStore.swift:1303-1320) all gate on max(app.peakLevel, app.rmsLevel) > 0.001 read from store.session. Verified there is NO periodic loop that refreshes session levels: the only periodic loop in AppStore is the level poll at 619-628, which writes store.liveLevels and never touches session.apps[].peakLevel; session peak/rms is updated only by explicit refresh()/start()/currentSnapshot() on user actions, device changes, or termination (none timer-driven). Meanwhile the meter bar (LVL-01) reads store.liveLevels, refreshed every ~300ms. Result: when a managed app starts playing, its cyan meter moves but its subtitle stays NOT 'Playing audio' (and it is not counted in liveApps / can be mis-sorted) until the next manual/structural refresh -- violating the AC 'A managed, unmuted app with measurable output (>0.001) is labeled Playing audio.'",
          "codeRef": "Sources/Waves/Features/Mixer/MixerRowView.swift:187",
          "expectedVsActual": "Expected: a managed, unmuted app with measurable output is labeled 'Playing audio' and counted in liveApps as it plays. Actual: the label/grouping derive from a session snapshot that is not periodically refreshed, so they lag (or never update) relative to the live meter that reads store.liveLevels.",
          "suggestedFix": "Feed store.liveLevels into the label/grouping: compute 'isPlaying' from store.liveLevels[app.logicalID] (falling back to session) in subtitle/liveApps/activityRank, or have the level poll also update session.apps[].peakLevel/rmsLevel (debounced) so both UI signals share one cadence.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "disposition": "deferred:needs-user-decision:Cross-file grouping/sort change with reorder risk and a threshold/source choice needing regression-testing.",
          "uid": "LVL-04#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MixerRowView.swift"
        },
        {
          "severity": "medium",
          "type": "correctness",
          "title": "App that went silent can stay labeled 'Playing audio' due to stale session levels",
          "detail": "Because session.peakLevel/rmsLevel is not periodically refreshed (verified above), a managed app that was playing and then went silent retains peakLevel/rmsLevel > 0.001 in session until the next event-driven refresh(). The subtitle (MixerRowView.swift:185-191) and liveApps (AppStore.swift:120) therefore keep reporting 'Playing audio' for an app that is now silent -- violating the AC 'A silent or muted managed app is not labeled Playing audio.' (Mute is handled because setMuted zeroes session levels and termination zeroes them at AppStore.swift:929-930; plain silence on a still-managed app is not.)",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:120",
          "expectedVsActual": "Expected: a silent managed app is not labeled 'Playing audio' and drops out of liveApps. Actual: it can remain labeled 'Playing audio' and grouped as live until an unrelated refresh updates the stale session levels.",
          "suggestedFix": "Derive the playing state from the live-polled store.liveLevels (which decays toward 0 from the realtime path) instead of the static session snapshot, or refresh session levels on the poll cadence.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-user-decision:Requires a design decision on decay threshold/cadence and spans a second file; risks re-sort thrash.",
          "uid": "LVL-04#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "medium",
          "type": "inconsistency",
          "title": "Menu-bar icon's 3-wave 'live' glyph is stale for managed apps that start/stop playing",
          "detail": "menuBarIconName (AppStore.swift:143-153) returns 'speaker.wave.3.fill' only when !liveApps.isEmpty, and liveApps (AppStore.swift:117-122) classifies a managed app as live via max(session.peakLevel, session.rmsLevel) > 0.001. Since session levels are never periodically refreshed (same root cause as the two issues above), a managed app that begins playing does not flip the menu-bar icon to the 3-wave glyph until the next event-driven refresh, and one that goes silent keeps the icon in the live state. This violates the feature's AC 'The menu-bar icon reflects a live state with the 3-wave speaker glyph' and the Live-section AC for managed-audible apps. Purely .live (unmanaged) apps still flip the icon correctly; only the managed-audible path is stale.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:149-150",
          "expectedVsActual": "Expected: the menu-bar icon shows the 3-wave glyph promptly when any managed app produces audio and reverts when audio stops. Actual: for managed apps the glyph tracks stale session levels and updates only on an unrelated refresh, so it can lag or stick.",
          "suggestedFix": "Have liveApps/menuBarIconName consult store.liveLevels for managed-audible classification (the same cadence as the meter), or refresh session levels on the poll cadence so the icon, the Live section, and the meter share one source of truth.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-user-decision:Unifying live classification across icon/section/meter needs a cadence/threshold design decision and a second file.",
          "uid": "LVL-04#3",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "LVL-05",
      "area": "Live Level Meters",
      "feature": "Realtime-safe, lock-light level capture",
      "priority": "P2",
      "userStory": "As a user, I want metering to never cause audio glitches or stutters, so that watching the meters never costs me clean playback.",
      "expectedBehavior": [
        "writeLevels is called on the realtime IO render thread and uses stateLock.try(); if the lock is contended it returns immediately and skips the update rather than blocking (WorkspaceAudioControlBackend.swift:1578-1588).",
        "TapDSP is documented and implemented as allocation-free, lock-free, dispatch-free sample math safe to call on the realtime thread (TapDSP.swift:11-17, 56-103).",
        "getCurrentLevels()/readLevels() take the lock from the main-actor/background side to read a consistent (peak,rms) pair (WorkspaceAudioControlBackend.swift:1590-1595, 1672-1673).",
        "rms() guards against divide-by-zero, returning 0 (never NaN) when sampleCount is 0 (TapDSP.swift:101-103)."
      ],
      "acceptanceCriteria": [
        "The render-thread level write path performs no blocking lock acquisition and no allocation.",
        "A contended write is dropped and recovered on the next render cycle without affecting audio output.",
        "rms(sum:0, sampleCount:0) returns 0 and is not NaN (covered by TapDSPTests).",
        "Reading levels yields a coherent peak/rms pair, not a torn read."
      ],
      "codeRefs": [
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1578-1595",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1672-1673",
        "Sources/WavesAudioCore/Audio/TapDSP.swift:11-17",
        "Sources/WavesAudioCore/Audio/TapDSP.swift:101-103"
      ],
      "implStatus": "implemented",
      "implNotes": "Solid realtime hygiene: try-lock on the audio thread, blocking lock only on consumer side, NaN guard tested. Minor: writeLevels updates both state.pointee and a duplicate stateBox struct under the lock, and the try-lock 'skip on contention' means under sustained main-thread lock pressure the meter could briefly freeze (acceptable, since it's cosmetic). The duplicated state storage (pointer + struct mirror) is a maintenance smell but not a correctness bug here.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "MENU-01",
      "area": "Menu Bar Icon & Menu Bar Mixer",
      "feature": "Dynamic menu bar status icon",
      "priority": "P1",
      "userStory": "As a macOS user running several audio apps, I want the Waves menu bar icon to reflect what my audio is doing right now, so that I can tell at a glance whether something is muted or actively playing without opening the panel.",
      "expectedBehavior": [
        "The menu bar extra renders a SF Symbol whose name is supplied by store.menuBarIconName (WavesApp.swift:59).",
        "If ANY visible app is muted, the icon is 'speaker.slash.fill' (AppStore.swift:146-147); this takes precedence over the playing state.",
        "Else if there is at least one live app (liveApps non-empty), the icon is 'speaker.wave.3.fill' (AppStore.swift:149-150).",
        "Otherwise the icon is the neutral 'speaker.wave.2.fill' (AppStore.swift:152).",
        "Because AppStore is @Observable (AppStore.swift:23), the icon recomputes reactively as mute state / live-level membership changes.",
        "Live-app membership is driven by routingState == .live OR (managed && !muted && peak/rms > 0.001) (AppStore.swift:117-122), which depends on the 300ms live-level poll while a surface is visible."
      ],
      "acceptanceCriteria": [
        "Muting any visible app changes the menu bar icon to the slash variant within one observation cycle.",
        "With at least one app actively producing sound (peak/rms above threshold) and none muted, the icon shows the 3-wave variant.",
        "With apps present but none muted and none live, the icon shows the 2-wave variant.",
        "When a muted app and a live app coexist, the muted (slash) icon wins.",
        "Unmuting the last muted app while audio is playing flips the icon from slash to 3-wave."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:143-153",
        "Sources/Waves/Stores/AppStore.swift:103-122",
        "Sources/Waves/App/WavesApp.swift:57-67"
      ],
      "implStatus": "partial",
      "implNotes": "Logic is wired and reactive, but two correctness/UX concerns: (1) The 'live' detection depends on the live-level poll (beginLiveLevels), which only runs while a UI surface is visible (MenuBarMixerView.onAppear / MixerRowView). When the menu panel and main window are both closed, peak/rms levels are not refreshed, so the 3-wave 'playing' state can be stale and the icon may sit on 2-wave even though audio is playing. (2) 'mute' uses ANY visible app muted, so muting a single background app makes the global icon show slash even while other apps play loudly, which can read as misleading. The 'speaker.slash.fill' choice also collides visually with the per-row mute glyph.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1H 1M]",
      "errorsFound": "• [HIGH/correctness] Menu bar 'playing' (3-wave) icon is stale: nothing refreshes session peak/routingState reactively to playback (Sources/Waves/Stores/AppStore.swift:117-122) — menuBarIconName -> liveApps reads store.session.apps[].routingState and .peakLevel/.rmsLevel (AppStore.swift:117-122). The store's session is only reassigned on explicit user actions (start/refresh/volume-commit/mute/boost/preset/device-change/recover — AppStore.swift:205,211,242,453,596,799,1010,1022,1184,1195,1419,1433); there is NO periodic/timer session refresh. The visibility-gated poll beginLiveLevels (AppStore.swift:616-629) writes ONLY to store.liveLevels, never into session. Confirmed in the real backend: WorkspaceAudioControlBackend.updateAudioLevels (line 1386-1402, every 250ms) mutates only the backend's internal snapshot.apps[].peakLevel/rmsLevel for active controllers and does NOT change routingState; the store never re-reads that snapshot in the background. .live is only assigned during discoverRunningApps/buildSnapshot. Net: when an app starts or stops producing sound, the icon does not flip between speaker.wave.2.fill and speaker.wave.3.fill until the user triggers a session-mutating action — even while the menu panel is open. | Fix: Drive liveApps from the data that is actually polled (consult store.liveLevels for managed-app liveness) and/or add a lightweight periodic session refresh while a surface is visible so routingState/peak reflect current playback; then align the story's expectedBehavior with the chosen source of truth.\n• [MEDIUM/ux] Slash icon shown whenever ANY visible app is muted, overriding the playing state even for a silent background app (Sources/Waves/Stores/AppStore.swift:146-147) — menuBarIconName returns speaker.slash.fill whenever visibleApps.contains(where: \\.isMuted) (AppStore.swift:146-147) with unconditional precedence over !liveApps.isEmpty. Muting one idle background app makes the global menu-bar icon read 'everything muted' even while other apps play loudly. The story explicitly documents this precedence as intended (expectedBehavior: 'this takes precedence over the playing state'), so it is a deliberate-but-debatable UX choice rather than a logic defect; still a real at-a-glance-misread risk. The chosen glyph also matches the per-row mute toggle glyph (MixerRowView.swift:317), so it reads like a control, not a status. | Fix: Prefer the 3-wave playing variant when a live app is concurrently playing; reserve the slash variant for when the muted app is the would-be-live one or nothing is playing. Consider a status glyph distinct from the per-row mute control.",
      "fixStatus": "2 deferred",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "correctness",
          "title": "Menu bar 'playing' (3-wave) icon is stale: nothing refreshes session peak/routingState reactively to playback",
          "detail": "menuBarIconName -> liveApps reads store.session.apps[].routingState and .peakLevel/.rmsLevel (AppStore.swift:117-122). The store's session is only reassigned on explicit user actions (start/refresh/volume-commit/mute/boost/preset/device-change/recover — AppStore.swift:205,211,242,453,596,799,1010,1022,1184,1195,1419,1433); there is NO periodic/timer session refresh. The visibility-gated poll beginLiveLevels (AppStore.swift:616-629) writes ONLY to store.liveLevels, never into session. Confirmed in the real backend: WorkspaceAudioControlBackend.updateAudioLevels (line 1386-1402, every 250ms) mutates only the backend's internal snapshot.apps[].peakLevel/rmsLevel for active controllers and does NOT change routingState; the store never re-reads that snapshot in the background. .live is only assigned during discoverRunningApps/buildSnapshot. Net: when an app starts or stops producing sound, the icon does not flip between speaker.wave.2.fill and speaker.wave.3.fill until the user triggers a session-mutating action — even while the menu panel is open.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:117-122",
          "expectedVsActual": "Story acceptance: 'With at least one app actively producing sound and none muted, the icon shows the 3-wave variant' and 'Unmuting the last muted app while audio is playing flips the icon from slash to 3-wave'. Actual: liveApps depends on a session copy that is never refreshed in the background, so the 3-wave variant can fail to appear (icon stuck on 2-wave) while audio plays, and unmute can flip to 2-wave instead of 3-wave.",
          "suggestedFix": "Drive liveApps from the data that is actually polled (consult store.liveLevels for managed-app liveness) and/or add a lightweight periodic session refresh while a surface is visible so routingState/peak reflect current playback; then align the story's expectedBehavior with the chosen source of truth.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-user-decision:Requires the same liveness source-of-truth / periodic-refresh design decision as SORT-02; reconciling icon semantics with the chosen source is not behavior-pres",
          "uid": "MENU-01#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "medium",
          "type": "ux",
          "title": "Slash icon shown whenever ANY visible app is muted, overriding the playing state even for a silent background app",
          "detail": "menuBarIconName returns speaker.slash.fill whenever visibleApps.contains(where: \\.isMuted) (AppStore.swift:146-147) with unconditional precedence over !liveApps.isEmpty. Muting one idle background app makes the global menu-bar icon read 'everything muted' even while other apps play loudly. The story explicitly documents this precedence as intended (expectedBehavior: 'this takes precedence over the playing state'), so it is a deliberate-but-debatable UX choice rather than a logic defect; still a real at-a-glance-misread risk. The chosen glyph also matches the per-row mute toggle glyph (MixerRowView.swift:317), so it reads like a control, not a status.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:146-147",
          "expectedVsActual": "Story intent: the icon should reflect 'whether something is muted or actively playing'. Actual: a single muted background app suppresses any playing indication for the whole system.",
          "suggestedFix": "Prefer the 3-wave playing variant when a live app is concurrently playing; reserve the slash variant for when the muted app is the would-be-live one or nothing is playing. Consider a status glyph distinct from the per-row mute control.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-user-decision:Icon precedence is a product/UX decision with conflicting acceptance criteria.",
          "uid": "MENU-01#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "MENU-02",
      "area": "Menu Bar Icon & Menu Bar Mixer",
      "feature": "Menu bar mixer panel (compact per-app rows)",
      "priority": "P0",
      "userStory": "As a user reacting to a sudden audio situation, I want a compact mixer to drop down from the menu bar with my pinned, live, and recent apps, so that I can adjust volume, boost, or mute in seconds without opening the full window.",
      "expectedBehavior": [
        "Clicking the menu bar extra opens a 400pt-wide window-style panel hosting MenuBarMixerView (WavesApp.swift:62-66).",
        "The panel shows a Pinned section only when pinnedApps is non-empty (MenuBarMixerView.swift:42-44), a Live section always (line 46), and a Recent section (max 3 rows) only when preferences.showRecentApps is true (lines 48-50).",
        "Each row (CompactMixerRow) shows app icon, name, an 'Excluded' tag or routing-state dot, a 0...1 volume slider, a boost menu, and a mute toggle (MixerRowView.swift:266-325).",
        "Dragging the slider calls store.setDesiredVolume live and commits on release via commitDesiredVolume (MixerRowView.swift:283-296).",
        "The mute button toggles store.setMuted and bounces the symbol unless reduceMotion is set (MixerRowView.swift:313-319).",
        "Excluded apps render at 0.55 opacity with slider/boost/mute disabled (MixerRowView.swift:308-326).",
        "If a section has more apps than maxVisible (4 for Pinned/Live, 3 for Recent), an 'N more in Waves' button opens the main window (MenuBarMixerView.swift:194-205).",
        "Opening the panel calls store.start() and store.beginLiveLevels(); closing it calls store.endLiveLevels() (MenuBarMixerView.swift:110-114)."
      ],
      "acceptanceCriteria": [
        "Opening the menu shows pinned/live/recent sections per the visibility rules above.",
        "Moving a row slider changes that app's desired volume and persists on release.",
        "Tapping a row mute button mutes/unmutes that specific app and updates the glyph.",
        "An excluded app's controls are visibly dimmed and non-interactive.",
        "A section with more than its max-visible apps shows an 'N more in Waves' affordance that opens the main window.",
        "Closing the menu stops the live-level poll when no other surface holds a reference."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:10-115",
        "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:177-209",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:259-327",
        "Sources/Waves/App/WavesApp.swift:57-66"
      ],
      "implStatus": "partial",
      "implNotes": "Fully functional for volume/boost/mute. Gap: the menu calls beginLiveLevels() (MenuBarMixerView.swift:113) but CompactMixerRow does NOT render the live cyan meter — the meter overlay (MixerRowView.swift:122-129, meterLevel:177) lives only in the full MixerRowView. So the poll's only visible effect in the menu is icon/section membership, not a moving meter, which is wasted work and an inconsistency with the main window. Also the 'Live' section header is always rendered logic-wise but CompactSection hides itself when empty (MixerRowView is fine), so an all-empty state is handled separately (see MENU-03).",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [1H 1M 1L]",
      "errorsFound": "• [HIGH/logic] Pinned apps are duplicated into the Live and/or Recent section of the menu panel (Sources/Waves/Stores/AppStore.swift:124-128) — MenuBarMixerView renders Pinned (store.pinnedApps), Live (store.liveApps), and Recent (store.recentApps) at once (lines 42-50). recentApps subtracts only liveApps IDs, not pinned IDs (AppStore.swift:124-128); pinnedApps and liveApps are also not mutually exclusive (both filter visibleApps independently, AppStore.swift:109-122). So a pinned-not-live app appears in BOTH Pinned and Recent, and a pinned-and-live app appears in BOTH Pinned and Live. Each occurrence is a separate CompactMixerRow with its own controls. (Verified not a crash: separate ForEach scopes, so it is visual/UX duplication, not an id collision.) The main window uses mutually-exclusive tabs, so duplication is specific to the menu panel. | Fix: Exclude pinned (and live) IDs from recentApps, and exclude pinned IDs from the Live section so each app appears in exactly one section of the menu panel.\n• [MEDIUM/edge-case] Menu panel shows 'No audio apps detected' empty state when Show-recent-apps is off, even with running apps (Sources/Waves/Features/Mixer/MenuBarMixerView.swift:48-52) — recentApps returns [] whenever preferences.showRecentApps is false (AppStore.swift:125), and that toggle is user-facing in Settings (SettingsView.swift:58-66, default true). The menu's only app sections are Pinned, Live, and Recent (MenuBarMixerView.swift:42-50), and the empty-state condition is pinnedApps.isEmpty && liveApps.isEmpty && recentApps.isEmpty (line 52). With Show-recent-apps disabled and nothing pinned or currently live, every running-but-idle app is suppressed, so the panel renders 'No audio apps detected' with Refresh/Settings buttons despite many apps running. The main window still lists them, producing a menu-vs-window state desync. The first tester missed this because they treated recentApps as always populated. | Fix: When showRecentApps is off, still surface non-live/non-pinned apps under a neutral section (or gate the empty state on the true count of visibleApps), so the menu never claims zero apps while apps are running.\n• [LOW/inconsistency] Menu panel starts the live-level poll but compact rows render no meter (wasted wakeups) (Sources/Waves/Features/Mixer/MixerRowView.swift:259-327) — Opening the panel calls store.beginLiveLevels() (MenuBarMixerView.swift:113), starting the 300ms poll that populates store.liveLevels (AppStore.swift:616-629). CompactMixerRow (MixerRowView.swift:259-327) has no level-meter overlay — the cyan meter (overlay MixerRowView.swift:122-134, meterLevel:177-180) exists only in the full MixerRowView used by the main window. So in the menu the poll draws no meter; and per MENU-01 it does not drive the icon's live state either, making it largely wasted work while the panel is open. | Fix: Either add the meter overlay to CompactMixerRow (reuse meterLevel/showsLevelMeter), or drop beginLiveLevels/endLiveLevels from the menu and feed the icon from a cheaper source.",
      "fixStatus": "2 fixed · 1 deferred",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "logic",
          "title": "Pinned apps are duplicated into the Live and/or Recent section of the menu panel",
          "detail": "MenuBarMixerView renders Pinned (store.pinnedApps), Live (store.liveApps), and Recent (store.recentApps) at once (lines 42-50). recentApps subtracts only liveApps IDs, not pinned IDs (AppStore.swift:124-128); pinnedApps and liveApps are also not mutually exclusive (both filter visibleApps independently, AppStore.swift:109-122). So a pinned-not-live app appears in BOTH Pinned and Recent, and a pinned-and-live app appears in BOTH Pinned and Live. Each occurrence is a separate CompactMixerRow with its own controls. (Verified not a crash: separate ForEach scopes, so it is visual/UX duplication, not an id collision.) The main window uses mutually-exclusive tabs, so duplication is specific to the menu panel.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:124-128",
          "expectedVsActual": "Story expects three distinct sections so the user can act 'in seconds'. Actual: any pinned app renders twice in the compact panel with duplicate independent controls.",
          "suggestedFix": "Exclude pinned (and live) IDs from recentApps, and exclude pinned IDs from the Live section so each app appears in exactly one section of the menu panel.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier2:applied:verified-in-code",
          "uid": "MENU-02#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-DEVICECHANGE-GATING/FU-MENU-SECTION-DEDUP",
          "fixTier": "tier2"
        },
        {
          "severity": "medium",
          "type": "edge-case",
          "title": "Menu panel shows 'No audio apps detected' empty state when Show-recent-apps is off, even with running apps",
          "detail": "recentApps returns [] whenever preferences.showRecentApps is false (AppStore.swift:125), and that toggle is user-facing in Settings (SettingsView.swift:58-66, default true). The menu's only app sections are Pinned, Live, and Recent (MenuBarMixerView.swift:42-50), and the empty-state condition is pinnedApps.isEmpty && liveApps.isEmpty && recentApps.isEmpty (line 52). With Show-recent-apps disabled and nothing pinned or currently live, every running-but-idle app is suppressed, so the panel renders 'No audio apps detected' with Refresh/Settings buttons despite many apps running. The main window still lists them, producing a menu-vs-window state desync. The first tester missed this because they treated recentApps as always populated.",
          "codeRef": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:48-52",
          "expectedVsActual": "Expected: the menu should reflect running apps the user can adjust. Actual: turning off 'Show recent apps' makes the menu falsely claim no audio apps exist whenever nothing is pinned or live.",
          "suggestedFix": "When showRecentApps is off, still surface non-live/non-pinned apps under a neutral section (or gate the empty state on the true count of visibleApps), so the menu never claims zero apps while apps are running.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift",
          "disposition": "deferred:needs-user-decision:Requires a product decision on intended menu-bar behavior when recent apps are disabled (neutral section vs. predicate change); not an obvio",
          "uid": "MENU-02#2",
          "canonicalFile": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift"
        },
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "Menu panel starts the live-level poll but compact rows render no meter (wasted wakeups)",
          "detail": "Opening the panel calls store.beginLiveLevels() (MenuBarMixerView.swift:113), starting the 300ms poll that populates store.liveLevels (AppStore.swift:616-629). CompactMixerRow (MixerRowView.swift:259-327) has no level-meter overlay — the cyan meter (overlay MixerRowView.swift:122-134, meterLevel:177-180) exists only in the full MixerRowView used by the main window. So in the menu the poll draws no meter; and per MENU-01 it does not drive the icon's live state either, making it largely wasted work while the panel is open.",
          "codeRef": "Sources/Waves/Features/Mixer/MixerRowView.swift:259-327",
          "expectedVsActual": "Expected: polling while the menu is visible should produce a moving meter consistent with the main window, or not run. Actual: poll runs with no visible meter in compact rows.",
          "suggestedFix": "Either add the meter overlay to CompactMixerRow (reuse meterLevel/showsLevelMeter), or drop beginLiveLevels/endLiveLevels from the menu and feed the icon from a cheaper source.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "disposition": "iter3:implemented",
          "uid": "MENU-02#3",
          "canonicalFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "fixStatus": "applied",
          "fixedBy": "iter3-ui-parity",
          "fixTier": "iter3"
        }
      ]
    },
    {
      "id": "MENU-03",
      "area": "Menu Bar Icon & Menu Bar Mixer",
      "feature": "Empty and loading states in the menu panel",
      "priority": "P1",
      "userStory": "As a user who opens the menu when nothing is playing or while detection is still running, I want clear feedback instead of a blank panel, so that I know Waves is working and how to proceed.",
      "expectedBehavior": [
        "While store.isLoading is true, the panel shows a small ProgressView with 'Refreshing audio sessions' caption (MenuBarMixerView.swift:30-38).",
        "When pinnedApps, liveApps, and recentApps are all empty, an empty state appears: a 'speaker.slash' glyph, 'No audio apps detected' text, and Refresh + Settings buttons (MenuBarMixerView.swift:52-72).",
        "The Refresh button (header icon and empty-state) calls store.refresh() (MenuBarMixerView.swift:19, 61).",
        "isLoading is set true on start/refresh only when session.apps is empty, and cleared in a defer (AppStore.swift:194, 234-239)."
      ],
      "acceptanceCriteria": [
        "On first launch with no cached session, the panel shows the loading row until detection completes.",
        "With zero detected apps across all sections, the empty-state graphic and Refresh/Settings buttons render.",
        "Tapping Refresh in the empty state re-runs detection and replaces the empty state once apps appear.",
        "The loading row disappears once isLoading clears."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:30-72",
        "Sources/Waves/Stores/AppStore.swift:194,228-254"
      ],
      "implStatus": "implemented",
      "implNotes": "Solid. Minor UX note: isLoading only becomes true when session.apps is empty, so a refresh that already has a warm cached session shows no spinner (header refresh icon stays static) — acceptable but means a slow refresh over an existing list gives no in-panel progress signal beyond the toast.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1M]",
      "errorsFound": "• [MEDIUM/ux] Loading spinner and 'No audio apps detected' empty state render simultaneously on first launch (Sources/Waves/Features/Mixer/MenuBarMixerView.swift:52) — The empty-state condition (MenuBarMixerView.swift:52) is pinnedApps.isEmpty && liveApps.isEmpty && recentApps.isEmpty and does NOT exclude store.isLoading. On first launch with no cached session, start() sets isLoading=true (AppStore.swift:194) while all sections are empty, so the panel shows BOTH the 'Refreshing audio sessions' ProgressView row (lines 30-38) AND the 'No audio apps detected' graphic with Refresh/Settings buttons (lines 52-72) at once. Verified: start() sets isLoading = session.apps.isEmpty, and the empty session is the first-launch default. | Fix: Gate the empty state on !store.isLoading (e.g. if !store.isLoading && pinnedApps.isEmpty && liveApps.isEmpty && recentApps.isEmpty), showing only the loading row while isLoading is true.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier2: FU-MENU-LOADING-EMPTYSTATE-GATING",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "ux",
          "title": "Loading spinner and 'No audio apps detected' empty state render simultaneously on first launch",
          "detail": "The empty-state condition (MenuBarMixerView.swift:52) is pinnedApps.isEmpty && liveApps.isEmpty && recentApps.isEmpty and does NOT exclude store.isLoading. On first launch with no cached session, start() sets isLoading=true (AppStore.swift:194) while all sections are empty, so the panel shows BOTH the 'Refreshing audio sessions' ProgressView row (lines 30-38) AND the 'No audio apps detected' graphic with Refresh/Settings buttons (lines 52-72) at once. Verified: start() sets isLoading = session.apps.isEmpty, and the empty session is the first-launch default.",
          "codeRef": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:52",
          "expectedVsActual": "Story acceptance: 'On first launch with no cached session, the panel shows the loading row until detection completes'. Actual: during detection the user simultaneously sees a spinner AND a contradictory 'No audio apps detected' message.",
          "suggestedFix": "Gate the empty state on !store.isLoading (e.g. if !store.isLoading && pinnedApps.isEmpty && liveApps.isEmpty && recentApps.isEmpty), showing only the loading row while isLoading is true.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift",
          "disposition": "tier2:applied:FU-MENU-LOADING-EMPTYSTATE-GATING",
          "uid": "MENU-03#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-MENU-LOADING-EMPTYSTATE-GATING",
          "fixTier": "tier2"
        }
      ]
    },
    {
      "id": "MENU-04",
      "area": "Menu Bar Icon & Menu Bar Mixer",
      "feature": "Output device switcher in the menu panel",
      "priority": "P1",
      "userStory": "As a user juggling speakers, headphones, and AirPods, I want to switch the system output device straight from the menu bar panel, so that I can re-route audio without opening System Settings.",
      "expectedBehavior": [
        "An OutputDevicePicker menu shows the current device name with a speaker icon and up/down chevron (MenuBarMixerView.swift:120-157).",
        "The menu lists store.availableDevices; the active device (id == currentDeviceID) gets a checkmark Label (MenuBarMixerView.swift:125-135).",
        "Selecting a device calls store.selectOutputDevice, which sets the default output via the backend and shows an 'Output switched' toast, then refreshes the device list (AppStore.swift:759-772).",
        "Re-selecting the already-current device is a no-op (AppStore.swift:760).",
        "If availableDevices is empty, the menu shows 'No output devices found' (MenuBarMixerView.swift:136-138).",
        "currentDeviceName falls back to 'No output device' when session.currentDevice is nil (AppStore.swift:139-141).",
        "The picker refreshes its device list onAppear (MenuBarMixerView.swift:156)."
      ],
      "acceptanceCriteria": [
        "Opening the picker lists all available output devices with a checkmark on the active one.",
        "Choosing a different device switches system output and shows a success toast.",
        "Choosing the current device does nothing.",
        "With no devices available, the menu shows the 'No output devices found' placeholder and the label reads 'No output device'.",
        "A switch failure surfaces an error toast rather than silently failing (AppStore.swift:768-770)."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:118-158",
        "Sources/Waves/Stores/AppStore.swift:139-141,753-772"
      ],
      "implStatus": "implemented",
      "implNotes": "Wired end-to-end with success/error toasts and empty-device handling. The checkmark/current-device state relies on availableDevices being refreshed after the switch and on device-change events updating session — generally consistent, though the checkmark can momentarily lag a system-initiated device change until the next refreshOutputDevices/handleDeviceChange.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1M]",
      "errorsFound": "• [MEDIUM/edge-case] Output picker checkmark/label go stale after a switch when 'Auto-restore device' is disabled (Sources/Waves/Stores/AppStore.swift:792-793) — selectOutputDevice (AppStore.swift:759-772) sets the system default and refreshes availableDevices but does NOT update session.currentDevice; it relies on the CoreAudio listener -> deviceChangeEvents -> handleDeviceChange. handleDeviceChange returns immediately when preferences.autoRestoreDevice is false (AppStore.swift:792-793). autoRestoreDevice defaults to true (UserPreferences.swift:9) but is user-toggleable (SettingsView.swift:78-86, verified). With it disabled, session.currentDevice is never updated after a switch, so currentDeviceID (AppStore.swift:130-132) keeps returning the old device: the picker label (currentDeviceName) and checkmark (MenuBarMixerView.swift:129) stay on the previously-selected device, and the guard device.id != currentDeviceID (line 760) compares against the stale value. | Fix: Update session.currentDevice in selectOutputDevice's success path independent of autoRestoreDevice (e.g. session = await backend.currentSnapshot()), or move the session refresh in handleDeviceChange out from under the autoRestoreDevice guard so a device change always refreshes the displayed current device.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier1: FU-DEVICECHANGE-GATING",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "edge-case",
          "title": "Output picker checkmark/label go stale after a switch when 'Auto-restore device' is disabled",
          "detail": "selectOutputDevice (AppStore.swift:759-772) sets the system default and refreshes availableDevices but does NOT update session.currentDevice; it relies on the CoreAudio listener -> deviceChangeEvents -> handleDeviceChange. handleDeviceChange returns immediately when preferences.autoRestoreDevice is false (AppStore.swift:792-793). autoRestoreDevice defaults to true (UserPreferences.swift:9) but is user-toggleable (SettingsView.swift:78-86, verified). With it disabled, session.currentDevice is never updated after a switch, so currentDeviceID (AppStore.swift:130-132) keeps returning the old device: the picker label (currentDeviceName) and checkmark (MenuBarMixerView.swift:129) stay on the previously-selected device, and the guard device.id != currentDeviceID (line 760) compares against the stale value.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:792-793",
          "expectedVsActual": "Story acceptance: the picker lists devices 'with a checkmark on the active one' and a switch reflects the new device. Actual: with auto-restore off, the success toast shows the new device, but the picker label and checkmark keep showing the OLD device until some unrelated action refreshes the session.",
          "suggestedFix": "Update session.currentDevice in selectOutputDevice's success path independent of autoRestoreDevice (e.g. session = await backend.currentSnapshot()), or move the session refresh in handleDeviceChange out from under the autoRestoreDevice guard so a device change always refreshes the displayed current device.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:FU-DEVICECHANGE-GATING",
          "uid": "MENU-04#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-DEVICECHANGE-GATING",
          "fixTier": "tier1"
        }
      ]
    },
    {
      "id": "MENU-05",
      "area": "Menu Bar Icon & Menu Bar Mixer",
      "feature": "Preset quick-apply, Open Waves, Settings, and Launch-at-login from the panel",
      "priority": "P2",
      "userStory": "As a returning user, I want one-tap access to presets, the full window, Settings, and a launch-at-login toggle from the menu panel, so that the menu bar is a complete control surface for the mixer.",
      "expectedBehavior": [
        "A 'Presets' submenu (slider icon) lists store.presets; choosing one calls store.applyPreset, which applies it (stripping excluded apps) and shows a 'Preset applied' toast (MenuBarMixerView.swift:160-175; AppStore.swift:1001-1029).",
        "'Open Waves' opens the main window by id and activates the app (MenuBarMixerView.swift:77-80).",
        "'Settings' opens the Settings scene and activates the app (MenuBarMixerView.swift:83-87).",
        "A labels-hidden 'Launch at login' switch is bound to store.launchAtLoginEnabled (MenuBarMixerView.swift:90-100).",
        "Toggling launch-at-login calls loginItemService.setEnabled; if the OS reports the state didn't take (needs approval), a 'Login item needs approval' warning toast is shown, and failures show an error toast (AppStore.swift:161-186).",
        "A header refresh button and ⌘R shortcut both call store.refresh() (MenuBarMixerView.swift:18-25)."
      ],
      "acceptanceCriteria": [
        "The Presets submenu lists all presets (defaults plus saved) and applying one changes the live mix and toasts.",
        "'Open Waves' brings the main mixer window to the front.",
        "'Settings' opens the preferences window to the front.",
        "Flipping the Launch-at-login switch persists the login-item state and reflects the OS-reported result (including an approval-needed warning).",
        "⌘R or the refresh icon re-detects apps."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:18-25,76-101,160-175",
        "Sources/Waves/Stores/AppStore.swift:161-186,1001-1029"
      ],
      "implStatus": "implemented",
      "implNotes": "All actions are wired with real effects and feedback. UX note: the Presets submenu has no empty-state branch (MenuBarMixerView.swift:164-168), but presets are seeded with Preset.defaults (AppStore.swift:82) so it is effectively never empty; if a user deleted all presets the submenu would render empty with no label. The launch-at-login binding's setter reconciles to the OS-reported status, which is the correct, honest behavior.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/edge-case] Presets submenu renders empty with no placeholder after the user deletes all presets, and stays empty across launches (Sources/Waves/Features/Mixer/MenuBarMixerView.swift:160-175) — PresetQuickPicker (MenuBarMixerView.swift:160-175) has no empty-state branch — it just ForEach(store.presets). deletePresets (AppStore.swift:1066-1077) lets the user remove every preset, including seeded Preset.defaults, and persists the empty array. Verified PresetStore.load (PresetStore.swift:30-57) re-seeds defaults ONLY when the file is missing or unreadable/corrupt — a valid empty array decodes successfully and is returned as-is, so the empty state survives relaunch. The Presets menu then opens as an empty popover with no explanatory label. | Fix: Add an empty-state item to PresetQuickPicker (disabled 'No presets saved' / 'Save one in Waves' when store.presets.isEmpty), and/or prevent deleting the last preset or re-seed defaults when the persisted array is empty.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier3: FU-MENU-PRESET-PICKER-EMPTY",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "edge-case",
          "title": "Presets submenu renders empty with no placeholder after the user deletes all presets, and stays empty across launches",
          "detail": "PresetQuickPicker (MenuBarMixerView.swift:160-175) has no empty-state branch — it just ForEach(store.presets). deletePresets (AppStore.swift:1066-1077) lets the user remove every preset, including seeded Preset.defaults, and persists the empty array. Verified PresetStore.load (PresetStore.swift:30-57) re-seeds defaults ONLY when the file is missing or unreadable/corrupt — a valid empty array decodes successfully and is returned as-is, so the empty state survives relaunch. The Presets menu then opens as an empty popover with no explanatory label.",
          "codeRef": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:160-175",
          "expectedVsActual": "Story acceptance: 'The Presets submenu lists all presets (defaults plus saved)'. Actual: after the user deletes all presets, the submenu is empty with no 'No presets' affordance and remains empty on subsequent launches.",
          "suggestedFix": "Add an empty-state item to PresetQuickPicker (disabled 'No presets saved' / 'Save one in Waves' when store.presets.isEmpty), and/or prevent deleting the last preset or re-seed defaults when the persisted array is empty.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift",
          "disposition": "tier3:applied:FU-MENU-PRESET-PICKER-EMPTY",
          "uid": "MENU-05#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-MENU-PRESET-PICKER-EMPTY",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "MENU-06",
      "area": "Menu Bar Icon & Menu Bar Mixer",
      "feature": "Menu bar extra presence / visibility control",
      "priority": "P3",
      "userStory": "As a user who wants control over my menu bar, I want to be able to show or hide the Waves menu bar icon, so that I can keep my menu bar uncluttered if I prefer to drive Waves from the main window.",
      "expectedBehavior": [
        "The MenuBarExtra's isInserted is bound to @AppStorage('showMenuBarExtra'), defaulting to true (WavesApp.swift:11,57-60).",
        "When showMenuBarExtra is true the menu bar icon is present; when false it would be removed."
      ],
      "acceptanceCriteria": [
        "By default the Waves menu bar icon is visible.",
        "There exists a user-facing control to hide/show the menu bar icon that flips showMenuBarExtra.",
        "Setting the preference persists across launches via AppStorage."
      ],
      "codeRefs": [
        "Sources/Waves/App/WavesApp.swift:11,57-60"
      ],
      "implStatus": "partial",
      "implNotes": "The plumbing exists (AppStorage-backed isInserted) and persists, but there is NO UI anywhere to toggle showMenuBarExtra — a grep across Sources finds the key only in WavesApp.swift, and SettingsView has no 'Show menu bar icon' control. So in practice the icon is always shown and the user has no supported way to hide it. Either add a Settings toggle or treat the icon as always-on by design; as written it is a half-wired preference.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1M 1L]",
      "errorsFound": "• [MEDIUM/dead-code] No user-facing control exists to toggle showMenuBarExtra; the preference is unreachable (Sources/Waves/App/WavesApp.swift:11) — MenuBarExtra isInserted binds to @AppStorage(\"showMenuBarExtra\") defaulting to true (WavesApp.swift:11,57-60). Verified by grep: the key appears ONLY in WavesApp.swift across all of Sources — there is no toggle in SettingsView or anywhere else (SettingsView exposes Show-recent-apps, Show-system-processes, Auto-restore device, etc., but no 'Show menu bar icon'). So the icon is effectively always shown and the user has no supported way to hide it; the AppStorage plumbing is half-wired. | Fix: Add a 'Show Waves in the menu bar' toggle in SettingsView bound to @AppStorage(\"showMenuBarExtra\"), or document the icon as always-on and remove the unused isInserted binding.\n• [LOW/accessibility] Menu bar icon conveys no mute/playing status to VoiceOver (Sources/Waves/App/WavesApp.swift:57-60) — The MenuBarExtra is created with a fixed title 'Waves' and only the systemImage changes with state (WavesApp.swift:57-60). The whole point of MENU-01's story is at-a-glance status (muted vs playing vs idle), but a VoiceOver user hears only 'Waves' regardless of which of the three SF Symbols is showing, because the accessible label is the static title and SF Symbol name changes are not surfaced as state. This is a real gap for the at-a-glance-status story; severity is low because the full per-app state is reachable inside the panel. | Fix: Expose the status to assistive tech, e.g. give the MenuBarExtra label a state-dependent accessibility value/label (Muted / Playing / Idle) derived from menuBarIconName, so the at-a-glance status is also available non-visually.",
      "fixStatus": "2 fixed",
      "fixNotes": "tier3: FU-MENUBAR-A11Y-LABEL",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "dead-code",
          "title": "No user-facing control exists to toggle showMenuBarExtra; the preference is unreachable",
          "detail": "MenuBarExtra isInserted binds to @AppStorage(\"showMenuBarExtra\") defaulting to true (WavesApp.swift:11,57-60). Verified by grep: the key appears ONLY in WavesApp.swift across all of Sources — there is no toggle in SettingsView or anywhere else (SettingsView exposes Show-recent-apps, Show-system-processes, Auto-restore device, etc., but no 'Show menu bar icon'). So the icon is effectively always shown and the user has no supported way to hide it; the AppStorage plumbing is half-wired.",
          "codeRef": "Sources/Waves/App/WavesApp.swift:11",
          "expectedVsActual": "Story acceptance: 'There exists a user-facing control to hide/show the menu bar icon that flips showMenuBarExtra.' Actual: no such control exists, so this criterion is unmet (default-visible and persistence criteria do pass via AppStorage).",
          "suggestedFix": "Add a 'Show Waves in the menu bar' toggle in SettingsView bound to @AppStorage(\"showMenuBarExtra\"), or document the icon as always-on and remove the unused isInserted binding.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/App/WavesApp.swift",
          "disposition": "iter3:implemented",
          "uid": "MENU-06#1",
          "canonicalFile": "Sources/Waves/App/WavesApp.swift",
          "fixStatus": "applied",
          "fixedBy": "iter3-ui-parity",
          "fixTier": "iter3"
        },
        {
          "severity": "low",
          "type": "accessibility",
          "title": "Menu bar icon conveys no mute/playing status to VoiceOver",
          "detail": "The MenuBarExtra is created with a fixed title 'Waves' and only the systemImage changes with state (WavesApp.swift:57-60). The whole point of MENU-01's story is at-a-glance status (muted vs playing vs idle), but a VoiceOver user hears only 'Waves' regardless of which of the three SF Symbols is showing, because the accessible label is the static title and SF Symbol name changes are not surfaced as state. This is a real gap for the at-a-glance-status story; severity is low because the full per-app state is reachable inside the panel.",
          "codeRef": "Sources/Waves/App/WavesApp.swift:57-60",
          "expectedVsActual": "Expected (story intent): a user can tell 'whether something is muted or actively playing without opening the panel'. Actual: a VoiceOver user gets no spoken indication of mute/playing/idle from the menu bar item.",
          "suggestedFix": "Expose the status to assistive tech, e.g. give the MenuBarExtra label a state-dependent accessibility value/label (Muted / Playing / Idle) derived from menuBarIconName, so the at-a-glance status is also available non-visually.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/App/WavesApp.swift",
          "disposition": "tier3:applied:FU-MENUBAR-A11Y-LABEL",
          "uid": "MENU-06#2",
          "canonicalFile": "Sources/Waves/App/WavesApp.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-MENUBAR-A11Y-LABEL",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "WIN-01",
      "area": "Main Window Mixer UI & Empty State",
      "feature": "Mixer source list with sidebar scopes",
      "priority": "P0",
      "userStory": "As a macOS user managing app audio, I want a main window that lists my apps in a sidebar/detail layout scoped by Running / Pinned / Live / Recent, so that I can find and control the exact app I care about.",
      "expectedBehavior": [
        "Window uses NavigationSplitView with a Sources sidebar (Running, Pinned, Live, Recent) and a detail pane showing the matching app list (MainWindowView.swift:14-24, 245-299)",
        "Each sidebar row shows an icon, title, and a count subtitle like '3 running apps' / '1 pinned app' with correct singular/plural (MainWindowView.swift:185-199, 301-322)",
        "Selected scope maps to a distinct store collection: Running->visibleApps, Pinned->pinnedApps, Live->liveApps, Recent->recentApps (MainWindowView.swift:102-113; AppStore.swift:103-128)",
        "Selected scope is persisted across launches via @SceneStorage('waves.selectedScope') so the user returns to the same scope (MainWindowView.swift:8)",
        "Detail pane renders one MixerRowView per app inside an inset List, and a top OutputSummaryView header plus a bottom DiagnosticsPanel (MainWindowView.swift:324-405)",
        "A single unique main Window (not WindowGroup) prevents duplicate mixer windows; min size 980x620, default 1100x680 (WavesApp.swift:30-39, 33)"
      ],
      "acceptanceCriteria": [
        "Selecting each of the four sidebar entries swaps the detail list to the corresponding scope's apps",
        "Sidebar count text matches the number of rows shown for that scope and pluralizes correctly",
        "Closing and reopening the window restores the previously selected scope",
        "Only one Waves main window can exist at a time"
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MainWindowView.swift:14",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:102",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:245",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:324",
        "Sources/Waves/App/WavesApp.swift:30",
        "Sources/Waves/Stores/AppStore.swift:103"
      ],
      "implStatus": "implemented",
      "implNotes": "Fully wired. Note 'Recent' is gated by preferences.showRecentApps and returns [] when disabled (AppStore.swift:124-128), so the Recent scope can legitimately appear empty even when apps exist — handled by the empty state but may surprise users. The local private enum is named SourceFilter and used directly as @SceneStorage RawRepresentable; works because it is String-backed.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/edge-case] Recent scope silently empty when showRecentApps preference is off, with misleading empty copy and dead recovery actions (Sources/Waves/Stores/AppStore.swift:124) — Re-verified: recentApps returns [] whenever preferences.showRecentApps is false (AppStore.swift:124-125), independent of how many apps exist. Selecting the Recent scope then renders the generic empty state title 'No Recent Apps' with message 'Apps appear here when they're not actively playing audio.' (MainWindowView.swift:223, 239-240) even though apps DO exist; the message gives no hint that the cause is a disabled preference. The sidebar count row shows '0 recent apps' (filter.count(in:) -> recentApps.count == 0). Compounding it, the empty-state offers a prominent 'Refresh' button (MainWindowView.swift:344-350) that can never populate the list while the preference is off, so the primary recovery affordance is a dead end. The feature-status implNotes for WIN-01 explicitly acknowledges this surprise. | Fix: When selection == .recent and preferences.showRecentApps == false, render a tailored empty message (e.g. 'Recent apps are turned off. Enable them in Settings.') and surface the Settings button as primary, or hide the Recent sidebar entry entirely while the preference is disabled.",
      "fixStatus": "1 n/a",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "edge-case",
          "title": "Recent scope silently empty when showRecentApps preference is off, with misleading empty copy and dead recovery actions",
          "detail": "Re-verified: recentApps returns [] whenever preferences.showRecentApps is false (AppStore.swift:124-125), independent of how many apps exist. Selecting the Recent scope then renders the generic empty state title 'No Recent Apps' with message 'Apps appear here when they're not actively playing audio.' (MainWindowView.swift:223, 239-240) even though apps DO exist; the message gives no hint that the cause is a disabled preference. The sidebar count row shows '0 recent apps' (filter.count(in:) -> recentApps.count == 0). Compounding it, the empty-state offers a prominent 'Refresh' button (MainWindowView.swift:344-350) that can never populate the list while the preference is off, so the primary recovery affordance is a dead end. The feature-status implNotes for WIN-01 explicitly acknowledges this surprise.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:124",
          "expectedVsActual": "Expected: an empty scope should explain why it is empty so the user can recover. Actual: Recent can be empty purely because showRecentApps is off, but copy, count, and the offered Refresh action all imply 'no apps qualify' rather than 'the feature is off,' so the user cannot self-correct from the empty state.",
          "suggestedFix": "When selection == .recent and preferences.showRecentApps == false, render a tailored empty message (e.g. 'Recent apps are turned off. Enable them in Settings.') and surface the Settings button as primary, or hide the Recent sidebar entry entirely while the preference is disabled.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "WIN-01#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "WIN-02",
      "area": "Main Window Mixer UI & Empty State",
      "feature": "Filter/search across apps",
      "priority": "P1",
      "userStory": "As a user with many running apps, I want to type in the sidebar search field to filter the list by app name or category, so that I can jump to a specific app quickly.",
      "expectedBehavior": [
        "A .searchable field placed in the sidebar with prompt 'Filter apps' drives searchText (MainWindowView.swift:25)",
        "Non-empty query matches case-insensitively against app.displayName OR app.category.displayName (MainWindowView.swift:115-130)",
        "Search deliberately spans ALL visibleApps rather than just the selected scope, so a match in another section is not hidden (MainWindowView.swift:123-129)",
        "Query is trimmed of whitespace and bounded to 100 characters to avoid performance issues (MainWindowView.swift:116-121)",
        "When search yields no matches, the empty state message switches to 'Try a different search term.' (MainWindowView.swift:227-230)",
        "While a search is active, drag-to-reorder is suppressed because filtered indices would not map back (MainWindowView.swift:369-375)"
      ],
      "acceptanceCriteria": [
        "Typing part of an app name or category narrows the list to matching apps regardless of selected scope",
        "An empty/whitespace query shows the unfiltered scoped list",
        "A query longer than 100 chars is truncated and still filters",
        "A no-match query shows the 'Try a different search term.' description"
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MainWindowView.swift:25",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:115",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:227"
      ],
      "implStatus": "implemented",
      "implNotes": "Behaves as documented. UX wrinkle: because search ignores the selected scope and always searches visibleApps, results while on Pinned/Live/Recent can include apps outside that scope; the header count (filter.detail) then reflects the filtered count but the sidebar scope label still says e.g. 'Pinned', which can read as inconsistent.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1M 1L]",
      "errorsFound": "• [MEDIUM/inconsistency] Search ignores selected scope but header keeps scope-specific noun, producing contradictory counts (Sources/Waves/Features/Mixer/MainWindowView.swift:447) — Re-verified: filteredApps filters store.visibleApps (ALL apps) regardless of the selected scope (MainWindowView.swift:126-129). The detail header renders filter.detail(count: apps.count) where filter is still the selected scope (MainWindowView.swift:334, 447, 185-199). So on the Pinned scope, typing a query that matches a non-pinned app lists that app while the header reads e.g. '1 pinned app' and the sidebar still highlights 'Pinned'. The noun (pinned/live/recent) does not describe the rows shown. | Fix: When a search query is active, show a neutral count label such as '\\(apps.count) result(s)' instead of filter.detail(...), and/or visually neutralize the selected sidebar scope while searching.\n• [LOW/inconsistency] Sidebar scope count and detail header count diverge during an active search (Sources/Waves/Features/Mixer/MainWindowView.swift:255) — Added in completeness pass. The sidebar row count is filter.detail(count: filter.count(in: store)) (MainWindowView.swift:255, 171-183), which reads from the full scope collection and is NOT affected by searchText. The detail header is filter.detail(count: apps.count) where apps == filteredApps (MainWindowView.swift:334, 447). So with a search active on the Running scope, the sidebar can show '8 running apps' while the detail header and list show '2 running apps' for the same scope at the same time — two count displays in one window disagreeing. This is a state-desync the first tester did not separately flag. | Fix: Either keep both counts unfiltered (move the header to the scoped, unfiltered count and show a separate 'N results' label during search), or thread the active query into the sidebar count so both reflect the same set.",
      "fixStatus": "2 fixed",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "inconsistency",
          "title": "Search ignores selected scope but header keeps scope-specific noun, producing contradictory counts",
          "detail": "Re-verified: filteredApps filters store.visibleApps (ALL apps) regardless of the selected scope (MainWindowView.swift:126-129). The detail header renders filter.detail(count: apps.count) where filter is still the selected scope (MainWindowView.swift:334, 447, 185-199). So on the Pinned scope, typing a query that matches a non-pinned app lists that app while the header reads e.g. '1 pinned app' and the sidebar still highlights 'Pinned'. The noun (pinned/live/recent) does not describe the rows shown.",
          "codeRef": "Sources/Waves/Features/Mixer/MainWindowView.swift:447",
          "expectedVsActual": "Expected: the count/label for an active search should describe the rows actually displayed (cross-scope matches). Actual: it keeps the scope noun while listing apps from any scope, so the label contradicts the list.",
          "suggestedFix": "When a search query is active, show a neutral count label such as '\\(apps.count) result(s)' instead of filter.detail(...), and/or visually neutralize the selected sidebar scope while searching.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "disposition": "iter3:implemented",
          "uid": "WIN-02#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "fixStatus": "applied",
          "fixedBy": "iter3-ui-parity",
          "fixTier": "iter3"
        },
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "Sidebar scope count and detail header count diverge during an active search",
          "detail": "Added in completeness pass. The sidebar row count is filter.detail(count: filter.count(in: store)) (MainWindowView.swift:255, 171-183), which reads from the full scope collection and is NOT affected by searchText. The detail header is filter.detail(count: apps.count) where apps == filteredApps (MainWindowView.swift:334, 447). So with a search active on the Running scope, the sidebar can show '8 running apps' while the detail header and list show '2 running apps' for the same scope at the same time — two count displays in one window disagreeing. This is a state-desync the first tester did not separately flag.",
          "codeRef": "Sources/Waves/Features/Mixer/MainWindowView.swift:255",
          "expectedVsActual": "Expected: counts shown for the same scope in the same window should agree (or the sidebar count should reflect the active filter). Actual: sidebar count stays at the unfiltered scope total while the header/list show the filtered subset, so the two numbers contradict during search.",
          "suggestedFix": "Either keep both counts unfiltered (move the header to the scoped, unfiltered count and show a separate 'N results' label during search), or thread the active query into the sidebar count so both reflect the same set.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "disposition": "iter3:implemented",
          "uid": "WIN-02#2",
          "canonicalFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "fixStatus": "applied",
          "fixedBy": "iter3-ui-parity",
          "fixTier": "iter3"
        }
      ]
    },
    {
      "id": "WIN-03",
      "area": "Main Window Mixer UI & Empty State",
      "feature": "Empty-state guidance with recovery actions",
      "priority": "P0",
      "userStory": "As a new or stuck user who sees no apps in a scope, I want a clear empty state explaining why and offering Refresh and Settings, so that I can fix the situation without guessing.",
      "expectedBehavior": [
        "When the scoped/filtered list is empty, a ContentUnavailableView shows a scope-specific title (e.g. 'No Running Apps', 'No Pinned Apps', 'No Live Apps', 'No Recent Apps') with a 'speaker.slash' icon (MainWindowView.swift:336-342, 214-225)",
        "Each scope has tailored guidance copy: Running='Waves hasn\\'t found any running apps yet.', Pinned='Pin apps from the source list to keep them here.', Live='Start playback in an app, then refresh if it does not appear here.', Recent='Apps appear here when they\\'re not actively playing audio.' (MainWindowView.swift:227-242)",
        "Empty state offers a prominent 'Refresh' button (calls store.refresh()) and a bordered 'Settings' button (calls openSettings()) (MainWindowView.swift:344-358)",
        "If a search is active the description overrides to 'Try a different search term.' regardless of scope (MainWindowView.swift:227-230)"
      ],
      "acceptanceCriteria": [
        "Each of the four scopes shows its own title and guidance message when empty",
        "Empty-state Refresh button triggers a session refresh; Settings button opens the Settings scene",
        "A non-matching search shows the search-specific message instead of the scope message"
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MainWindowView.swift:336",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:214",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:227"
      ],
      "implStatus": "implemented",
      "implNotes": "Solid and matches README's 'Empty State UI' promise. Minor: there is no distinct empty state for a permission-denied / backend-failed startup here — that surfaces only via the RouteHealthBadge and a startup-failed toast (AppStore.swift:221-224), so a user with denied capture permission sees 'No Running Apps' plus a red badge rather than a permissions-specific empty message.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "WIN-04",
      "area": "Main Window Mixer UI & Empty State",
      "feature": "Toolbar actions: save preset, refresh, recover routes",
      "priority": "P0",
      "userStory": "As a user actively managing my mix, I want toolbar buttons (and shortcuts) to save a preset, refresh the app list, and recover managed routes, so that I can maintain control without digging through menus.",
      "expectedBehavior": [
        "Toolbar '+' opens a Save Preset sheet (⌘S) with a name field; Save is disabled while the trimmed name is empty and validates name <=100 chars before calling store.savePreset (MainWindowView.swift:28-36, 85-91, 137-147, 592-618)",
        "Toolbar refresh button (arrow.clockwise, ⌘R) calls store.refresh(), which rebuilds the session, updates diagnostics, and shows a 'Library refreshed' toast (MainWindowView.swift:38-46; AppStore.swift:228-253)",
        "Toolbar 'Recover managed routes' (waveform.path) calls store.recoverRoutes(), reattaching active per-app routes and showing a success or failure toast (MainWindowView.swift:48-56; AppStore.swift:1181-1202)",
        "A duplicate Refresh command is also registered in the app menu with ⌘R (WavesApp.swift:43-48)",
        "Saving a preset with an existing (case-insensitive) name overwrites the existing preset in place rather than duplicating; excluded apps are stripped from saved presets (AppStore.swift:1041-1052, 1037-1040)",
        "All toolbar buttons carry accessibilityLabel + accessibilityHint and help tooltips (MainWindowView.swift:33-56)"
      ],
      "acceptanceCriteria": [
        "⌘S or the + button opens the Save Preset sheet; saving with a valid name adds/updates a preset and dismisses the sheet",
        "⌘R or the refresh button refreshes the session and shows a confirmation toast",
        "Recover routes button reattaches routes and reports success/failure via toast",
        "Save button stays disabled for empty/whitespace names"
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MainWindowView.swift:26",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:592",
        "Sources/Waves/Stores/AppStore.swift:228",
        "Sources/Waves/Stores/AppStore.swift:1181",
        "Sources/Waves/Stores/AppStore.swift:1031"
      ],
      "implStatus": "implemented",
      "implNotes": "All three actions are fully wired to backend calls. Two small concerns: (1) ⌘R is bound both on the toolbar button (MainWindowView.swift:46) and as an app-menu command (WavesApp.swift:47), a harmless duplicate; (2) refresh() and recoverRoutes() have no in-flight UI affordance on the recover button (only refresh sets isLoading), so rapid clicks on Recover can stack tasks — refresh is guarded by isRefreshing but recoverRoutes is not.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1M 1L]",
      "errorsFound": "• [MEDIUM/ux] Save Preset button is enabled for names over 100 chars but Save then silently fails, leaving the sheet stuck (Sources/Waves/Features/Mixer/MainWindowView.swift:142) — Re-verified: SavePresetSheet disables Save only when the trimmed name is empty (MainWindowView.swift:612). The 100-char limit is enforced only inside savePreset() via 'guard trimmed.count <= maxLength else { return }' (MainWindowView.swift:142-143), which returns WITHOUT calling store.savePreset, WITHOUT dismissing the sheet, and WITHOUT any toast/feedback. A user who types a 101+ char name sees an enabled Save button (and the default-action Return key) that does nothing, with the sheet stuck open. | Fix: Mirror the length guard in the .disabled() modifier (disable when trimmed.count > 100) with inline helper text, or have savePreset() surface a warning toast and keep the field focused.\n• [LOW/concurrency] Recover-routes toolbar action is unguarded, so rapid clicks stack concurrent recoverRoutes tasks with no in-flight affordance (Sources/Waves/Stores/AppStore.swift:1181) — Re-verified: the toolbar 'Recover managed routes' button calls store.recoverRoutes() (MainWindowView.swift:49), which spawns a Task calling backend.recoverRoutes() with no in-flight guard (AppStore.swift:1181-1202). Unlike refresh(), gated by 'guard !isRefreshing' (AppStore.swift:229), recoverRoutes has no equivalent flag, so repeated clicks launch overlapping recovery tasks that each reassign session and re-query diagnostics, and the button shows no spinner/disabled state. The feature-status implNotes for WIN-04 explicitly calls this out. | Fix: Add an isRecovering flag (mirroring isRefreshing) with 'guard !isRecovering' at the top of recoverRoutes(), set it around the Task, and disable the toolbar button (or show the progress pill) while true.",
      "fixStatus": "2 fixed",
      "fixNotes": "tier2: FU-PRESET-NAME-LENGTH-DISABLE | tier3: FU-RECOVER-ROUTES-GUARD",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "ux",
          "title": "Save Preset button is enabled for names over 100 chars but Save then silently fails, leaving the sheet stuck",
          "detail": "Re-verified: SavePresetSheet disables Save only when the trimmed name is empty (MainWindowView.swift:612). The 100-char limit is enforced only inside savePreset() via 'guard trimmed.count <= maxLength else { return }' (MainWindowView.swift:142-143), which returns WITHOUT calling store.savePreset, WITHOUT dismissing the sheet, and WITHOUT any toast/feedback. A user who types a 101+ char name sees an enabled Save button (and the default-action Return key) that does nothing, with the sheet stuck open.",
          "codeRef": "Sources/Waves/Features/Mixer/MainWindowView.swift:142",
          "expectedVsActual": "Expected: the Save action either accepts the name or clearly says why it was rejected; the control should never be a dead no-op. Actual: for names >100 chars the Save button is enabled but silently no-ops with the sheet still open and no feedback.",
          "suggestedFix": "Mirror the length guard in the .disabled() modifier (disable when trimmed.count > 100) with inline helper text, or have savePreset() surface a warning toast and keep the field focused.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "disposition": "tier2:applied:FU-PRESET-NAME-LENGTH-DISABLE",
          "uid": "WIN-04#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-PRESET-NAME-LENGTH-DISABLE",
          "fixTier": "tier2"
        },
        {
          "severity": "low",
          "type": "concurrency",
          "title": "Recover-routes toolbar action is unguarded, so rapid clicks stack concurrent recoverRoutes tasks with no in-flight affordance",
          "detail": "Re-verified: the toolbar 'Recover managed routes' button calls store.recoverRoutes() (MainWindowView.swift:49), which spawns a Task calling backend.recoverRoutes() with no in-flight guard (AppStore.swift:1181-1202). Unlike refresh(), gated by 'guard !isRefreshing' (AppStore.swift:229), recoverRoutes has no equivalent flag, so repeated clicks launch overlapping recovery tasks that each reassign session and re-query diagnostics, and the button shows no spinner/disabled state. The feature-status implNotes for WIN-04 explicitly calls this out.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1181",
          "expectedVsActual": "Expected: a long-running route-recovery action should be debounced/guarded and give in-flight feedback like refresh does. Actual: recoverRoutes is unguarded and gives no in-flight affordance, allowing stacked concurrent recovery tasks.",
          "suggestedFix": "Add an isRecovering flag (mirroring isRefreshing) with 'guard !isRecovering' at the top of recoverRoutes(), set it around the Task, and disable the toolbar button (or show the progress pill) while true.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier3:applied:FU-RECOVER-ROUTES-GUARD",
          "uid": "WIN-04#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-RECOVER-ROUTES-GUARD",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "WIN-05",
      "area": "Main Window Mixer UI & Empty State",
      "feature": "Output summary header and route-health badge",
      "priority": "P1",
      "userStory": "As a user, I want a header showing the current output device, how many apps are in view, what is currently playing, and overall routing health, so that I can trust the mixer at a glance.",
      "expectedBehavior": [
        "Header shows the current output device name (store.currentDeviceName, falling back to 'No output device') with a speaker glyph (MainWindowView.swift:434-444; AppStore.swift:139-141)",
        "Header shows the scope count text and, when apps are live, a cyan 'X, Y, Z playing' summary capped at 3 names with '+N playing' overflow (MainWindowView.swift:446-476)",
        "A RouteHealthBadge reports 'Ready' (green check) when backendStatus.isRouteRecoveryHealthy, 'Needs attention' (red triangle) when lastError is set, else 'Limited' (secondary) (MainWindowView.swift:479-519)",
        "The badge exposes its status as help text and accessibilityLabel ('Routing status: ...') (MainWindowView.swift:489-490, 517-519)",
        "A small inline 'Refreshing' progress pill appears top-leading while store.isLoading is true (MainWindowView.swift:64-82)"
      ],
      "acceptanceCriteria": [
        "Header device name updates when the system output device changes",
        "Live summary lists up to 3 playing app names with correct overflow count, and disappears when nothing is playing",
        "Route badge color/label correctly reflects healthy / lastError / limited states",
        "The refreshing indicator shows during a session load and clears afterward"
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MainWindowView.swift:428",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:479",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:64",
        "Sources/Waves/Stores/AppStore.swift:139"
      ],
      "implStatus": "implemented",
      "implNotes": "Correct and accessible. Edge case: the 'Limited' state (no error but not healthy) renders with .secondary color and the same exclamation-triangle symbol as the error state (MainWindowView.swift:503-515), so 'Limited' and 'Needs attention' are distinguished only by color/text, not icon — acceptable but slightly redundant.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1M 1L]",
      "errorsFound": "• [MEDIUM/ux] Refreshing indicator never appears on a normal refresh when apps are already loaded, failing the WIN-05 acceptance criterion (Sources/Waves/Stores/AppStore.swift:234) — Re-verified: the 'Refreshing' progress pill is gated on store.isLoading (MainWindowView.swift:64). Both start() and refresh() set isLoading = session.apps.isEmpty (AppStore.swift:194, 234), so isLoading is true only when there are zero apps. On any refresh while apps are already present (the common case, including the toolbar refresh button and the empty-state Refresh after first load), isLoading stays false and no spinner appears; the user gets no in-flight feedback until the terminal toast fires. WIN-05 acceptance criterion 'The refreshing indicator shows during a session load and clears afterward' is therefore not met for the typical refresh. Raising severity from the first tester's 'low' to 'medium' because it directly fails a written acceptance criterion (not merely a polish gap). | Fix: Drive the pill from isRefreshing (or 'isLoading || isRefreshing') rather than isLoading alone, so a user-initiated refresh shows the spinner even when the list is non-empty.\n• [LOW/accessibility] Route-health 'Limited' and 'Needs attention' states are distinguished only by color, sharing the same warning icon (Sources/Waves/Features/Mixer/MainWindowView.swift:503) — Re-verified: RouteHealthBadge.systemImage returns the same 'exclamationmark.triangle.fill' glyph for every non-healthy state (MainWindowView.swift:503-505); only color (.red vs .secondary, lines 507-515) and the label text ('Needs attention' vs 'Limited', lines 497-500) differ. For users with color-vision deficiency or in grayscale/high-contrast modes the two routing states are hard to tell apart from the icon alone. The feature-status implNotes for WIN-05 acknowledges this redundancy. | Fix: Give 'Limited' a distinct SF Symbol (e.g. 'exclamationmark.circle' or 'minus.circle') versus the error 'exclamationmark.triangle.fill', so the states differ by shape as well as color/text.",
      "fixStatus": "1 fixed · 1 deferred",
      "fixNotes": "tier3: FU-ROUTEHEALTH-LIMITED-DISTINCT-ICON",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "ux",
          "title": "Refreshing indicator never appears on a normal refresh when apps are already loaded, failing the WIN-05 acceptance criterion",
          "detail": "Re-verified: the 'Refreshing' progress pill is gated on store.isLoading (MainWindowView.swift:64). Both start() and refresh() set isLoading = session.apps.isEmpty (AppStore.swift:194, 234), so isLoading is true only when there are zero apps. On any refresh while apps are already present (the common case, including the toolbar refresh button and the empty-state Refresh after first load), isLoading stays false and no spinner appears; the user gets no in-flight feedback until the terminal toast fires. WIN-05 acceptance criterion 'The refreshing indicator shows during a session load and clears afterward' is therefore not met for the typical refresh. Raising severity from the first tester's 'low' to 'medium' because it directly fails a written acceptance criterion (not merely a polish gap).",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:234",
          "expectedVsActual": "Expected (acceptance): the indicator shows while a refresh is running. Actual: it only appears on a cold/empty load; an explicit refresh with apps present shows no refreshing indicator at all.",
          "suggestedFix": "Drive the pill from isRefreshing (or 'isLoading || isRefreshing') rather than isLoading alone, so a user-initiated refresh shows the spinner even when the list is non-empty.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-feature:The actual fix lives in MainWindowView.swift, not AppStore.swift.",
          "uid": "WIN-05#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "low",
          "type": "accessibility",
          "title": "Route-health 'Limited' and 'Needs attention' states are distinguished only by color, sharing the same warning icon",
          "detail": "Re-verified: RouteHealthBadge.systemImage returns the same 'exclamationmark.triangle.fill' glyph for every non-healthy state (MainWindowView.swift:503-505); only color (.red vs .secondary, lines 507-515) and the label text ('Needs attention' vs 'Limited', lines 497-500) differ. For users with color-vision deficiency or in grayscale/high-contrast modes the two routing states are hard to tell apart from the icon alone. The feature-status implNotes for WIN-05 acknowledges this redundancy.",
          "codeRef": "Sources/Waves/Features/Mixer/MainWindowView.swift:503",
          "expectedVsActual": "Expected: the three routing-health states should be distinguishable beyond color (per the app's accessibility focus). Actual: 'Limited' and 'Needs attention' share one icon and differ only by color and label text.",
          "suggestedFix": "Give 'Limited' a distinct SF Symbol (e.g. 'exclamationmark.circle' or 'minus.circle') versus the error 'exclamationmark.triangle.fill', so the states differ by shape as well as color/text.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "disposition": "tier3:applied:FU-ROUTEHEALTH-LIMITED-DISTINCT-ICON",
          "uid": "WIN-05#2",
          "canonicalFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-ROUTEHEALTH-LIMITED-DISTINCT-ICON",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "WIN-06",
      "area": "Main Window Mixer UI & Empty State",
      "feature": "Transient toast notifications",
      "priority": "P1",
      "userStory": "As a user, I want brief, dismissible toast banners confirming actions and surfacing errors, so that I get feedback without modal interruptions.",
      "expectedBehavior": [
        "AppToastStack renders store.toasts top-trailing over the window with spring animation (MainWindowView.swift:59-62; AppToasts.swift:3-16)",
        "Each banner shows a kind-specific icon/color (success green check, warning orange triangle, error red octagon, info cyan) plus title and optional 2-line detail (AppToasts.swift:18-122)",
        "Each banner has a manual dismiss 'x' button calling store.dismissToast(id:) and an accessibility 'Dismiss' action (AppToasts.swift:48-58, 81-83)",
        "Toasts auto-dismiss after their duration (default 2.0s) via a per-toast cancellable Task (AppStore.swift:1442-1469)",
        "At most 3 toasts are kept; older ones are trimmed and their dismissal tasks cancelled (AppStore.swift:58, 1477-1483)",
        "VoiceOver announces each banner on appear since transient banners are not announced automatically (AppToasts.swift:84-87)"
      ],
      "acceptanceCriteria": [
        "Performing actions (refresh, save preset, recover, errors) shows a toast of the correct kind/color",
        "A toast auto-dismisses after its duration and can be dismissed early via the x button",
        "No more than 3 toasts are visible simultaneously; the oldest is removed when a 4th arrives",
        "Each toast is announced to VoiceOver and offers a Dismiss accessibility action"
      ],
      "codeRefs": [
        "Sources/Waves/Shared/UI/AppToasts.swift:3",
        "Sources/Waves/Shared/UI/AppToasts.swift:48",
        "Sources/Waves/Stores/AppStore.swift:1442",
        "Sources/Waves/Stores/AppStore.swift:1477"
      ],
      "implStatus": "implemented",
      "implNotes": "Robust: refcounted auto-dismiss, trimming, and a11y announcements all present. Minor layout note: AppToastStack applies horizontal padding both in AppToasts.swift:11 and again at the call site (MainWindowView.swift:60), so banners are inset slightly more than other top chrome; purely cosmetic.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/ux] Toast stack horizontal inset is applied twice (stack + call site), over-insetting banners relative to other top chrome (Sources/Waves/Shared/UI/AppToasts.swift:12) — Re-verified with a correction to the first tester's count. AppToastStack applies .padding(.horizontal, 14) at AppToasts.swift:12 and the call site applies another .padding(.horizontal, 14) at MainWindowView.swift:60; these two compound to ~28pt of OUTER inset on the toast container, more than the toolbar/refresh-pill chrome. The first tester also cited the banner-internal .padding(.horizontal, 14) at AppToasts.swift:60 as a third duplicate, but that one is the banner's own content inset (text away from the banner's rounded edge) and is correct — it is not part of the over-inset. So the real defect is the doubled outer padding (stack + call site), not a triple-application. Purely cosmetic. | Fix: Remove the redundant horizontal padding from either AppToastStack (AppToasts.swift:12) or the call site (MainWindowView.swift:60) so the container is inset once; leave the banner-internal padding intact.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier3: FU-TOAST-OUTER-PADDING",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "ux",
          "title": "Toast stack horizontal inset is applied twice (stack + call site), over-insetting banners relative to other top chrome",
          "detail": "Re-verified with a correction to the first tester's count. AppToastStack applies .padding(.horizontal, 14) at AppToasts.swift:12 and the call site applies another .padding(.horizontal, 14) at MainWindowView.swift:60; these two compound to ~28pt of OUTER inset on the toast container, more than the toolbar/refresh-pill chrome. The first tester also cited the banner-internal .padding(.horizontal, 14) at AppToasts.swift:60 as a third duplicate, but that one is the banner's own content inset (text away from the banner's rounded edge) and is correct — it is not part of the over-inset. So the real defect is the doubled outer padding (stack + call site), not a triple-application. Purely cosmetic.",
          "codeRef": "Sources/Waves/Shared/UI/AppToasts.swift:12",
          "expectedVsActual": "Expected: toast banners align with other top-trailing chrome with a single intended outer inset. Actual: the outer horizontal padding is applied twice (stack 14 + call site 14 = 28pt), over-insetting the stack.",
          "suggestedFix": "Remove the redundant horizontal padding from either AppToastStack (AppToasts.swift:12) or the call site (MainWindowView.swift:60) so the container is inset once; leave the banner-internal padding intact.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Shared/UI/AppToasts.swift",
          "disposition": "gapfill:fixed",
          "uid": "WIN-06#1",
          "canonicalFile": "Sources/Waves/Shared/UI/AppToasts.swift",
          "fixStatus": "applied",
          "fixedBy": "gapfill",
          "fixTier": "gapfill"
        }
      ]
    },
    {
      "id": "WIN-07",
      "area": "Main Window Mixer UI & Empty State",
      "feature": "Keyboard-driven control and drag-to-reorder in the list",
      "priority": "P1",
      "userStory": "As a keyboard/power user, I want to drive the selected mixer row entirely from the keyboard and reorder apps by dragging, so that I can operate the mixer without a mouse and arrange it to my workflow.",
      "expectedBehavior": [
        "The detail List supports selection; key handlers act on the selected row: Space/M toggle mute, '='/'-' nudge volume by 0.05 (clamped 0..1 and committed), 'b' cycles boost 1->4->1, 'p' toggles pin (MainWindowView.swift:362-386, 408-425)",
        "handleKey returns .ignored when no row is selected so the keystroke falls through (MainWindowView.swift:408-414)",
        "Drag-to-reorder (onMove) only takes effect for the Running scope with manual sort mode and empty search, then calls store.reorderApps which switches sortMode to manual and persists the custom order (MainWindowView.swift:369-375; AppStore.swift:1339-1354)",
        "Accessibility rotors expose 'Playing apps' and 'Needs attention' (error routing) entries for quick navigation (MainWindowView.swift:388-397)"
      ],
      "acceptanceCriteria": [
        "With a row selected, Space/M mute, +/- volume nudge, b boost-cycle, and p pin all act on that row only",
        "Pressing a control key with no selection does nothing (event ignored)",
        "Dragging a row in Running+manual+no-search reorders and persists the order; reorder is disabled in other scopes, non-manual sort, or during search",
        "VoiceOver rotors list currently-playing and error apps"
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MainWindowView.swift:362",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:408",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:369",
        "Sources/Waves/Stores/AppStore.swift:1339"
      ],
      "implStatus": "implemented",
      "implNotes": "Comprehensive keyboard support. Edge cases worth noting: (1) onMove is wired on the ForEach even when reorder is disallowed, but the guard means a drag in non-Running/sorted/searching states is silently a no-op (the row snaps back), which can feel unresponsive without explanation; (2) cycleBoost rounds and caps at 4 but relies on volumeBoost already being an integer-ish Float, fine given the 1..4 menu options.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1M]",
      "errorsFound": "• [MEDIUM/ux] Drag-to-reorder is always armed but silently no-ops outside Running+manual+no-search, so the row snaps back with no explanation (Sources/Waves/Features/Mixer/MainWindowView.swift:369) — Re-verified: the .onMove handler is attached unconditionally to the ForEach (MainWindowView.swift:369-375), so List renders drag affordances in every scope, sort mode, and during search. The reorder is committed only when 'filter == .running && store.preferences.sortMode == .manual && searchText.isEmpty' (MainWindowView.swift:372); otherwise the closure body does nothing and the dragged row animates back with no toast, alert, or disabled cursor. Users on Pinned/Live/Recent, on Running with name/activity/category sort, or while searching experience a drag that appears to work but is silently rejected. The feature-status implNotes for WIN-07 acknowledges this. Severity medium is defensible (UX confusion, no data loss), matching the first tester. | Fix: Apply .moveDisabled(true) to rows (or attach .onMove only) when filter != .running || sortMode != .manual || !searchText.isEmpty, or show a one-line toast explaining reordering requires the Running scope with Manual sort and no active search.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier2: FU-ONMOVE-AFFORDANCE-GATING",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "ux",
          "title": "Drag-to-reorder is always armed but silently no-ops outside Running+manual+no-search, so the row snaps back with no explanation",
          "detail": "Re-verified: the .onMove handler is attached unconditionally to the ForEach (MainWindowView.swift:369-375), so List renders drag affordances in every scope, sort mode, and during search. The reorder is committed only when 'filter == .running && store.preferences.sortMode == .manual && searchText.isEmpty' (MainWindowView.swift:372); otherwise the closure body does nothing and the dragged row animates back with no toast, alert, or disabled cursor. Users on Pinned/Live/Recent, on Running with name/activity/category sort, or while searching experience a drag that appears to work but is silently rejected. The feature-status implNotes for WIN-07 acknowledges this. Severity medium is defensible (UX confusion, no data loss), matching the first tester.",
          "codeRef": "Sources/Waves/Features/Mixer/MainWindowView.swift:369",
          "expectedVsActual": "Expected: reordering should be disabled (no drag affordance) outside the supported state, or the rejection explained. Actual: dragging is always offered but silently discarded outside Running+manual+no-search, with no feedback.",
          "suggestedFix": "Apply .moveDisabled(true) to rows (or attach .onMove only) when filter != .running || sortMode != .manual || !searchText.isEmpty, or show a one-line toast explaining reordering requires the Running scope with Manual sort and no active search.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "disposition": "tier2:applied:FU-ONMOVE-AFFORDANCE-GATING",
          "uid": "WIN-07#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-ONMOVE-AFFORDANCE-GATING",
          "fixTier": "tier2"
        }
      ]
    },
    {
      "id": "KEY-01",
      "area": "Global Keyboard Shortcuts",
      "feature": "Global volume-up hotkey (⌘⌥↑)",
      "priority": "P1",
      "userStory": "As a Waves user, I want to press ⌘⌥↑ from anywhere in macOS to raise the volume of the app I'm currently using, so that I can fine-tune per-app loudness without switching to the Waves window.",
      "expectedBehavior": [
        "A system-wide keyDown monitor (NSEvent.addGlobalMonitorForEvents) is installed when shortcuts are enabled and fires on every keyDown app-wide (WavesApp.swift:150-155)",
        "On keyCode 126 (Up arrow) with both Command and Option held, AppDelegate calls store.increaseVolumeForFrontmostApp() (WavesApp.swift:174-182)",
        "The target app is resolved as activeApps.first ?? visibleApps.first, i.e. the first sorted app that is audible or OS-frontmost (AppStore.swift:1212-1213)",
        "Volume step defaults to 0.1, is clamped to [0.01, 0.5], and the new volume is clamped to a max of 1.0 (AppStore.swift:1216-1217)",
        "setDesiredVolume + commitDesiredVolume are called so the change is applied to the backend and persisted (AppStore.swift:1218-1219)",
        "An info toast 'Volume increased — <App>: <NN>%' is shown for 1 second (AppStore.swift:1221-1226)",
        "Action is a no-op when preferences.enableKeyboardShortcuts is false (AppStore.swift:1211) or when there is no active/visible app (AppStore.swift:1213)",
        "Keystrokes are ignored while the key window's first responder is an NSTextView or NSTextField, so typing is not hijacked (WavesApp.swift:169-172)"
      ],
      "acceptanceCriteria": [
        "With shortcuts enabled and an audible app present, pressing ⌘⌥↑ raises that app's desiredVolume by ~10% and shows a 'Volume increased' toast",
        "Repeated presses never push volume above 100% (1.0)",
        "Pressing ⌘⌥↑ with shortcuts disabled produces no change and no toast",
        "Pressing ⌘⌥↑ while focus is in a text field does not change any app volume",
        "With no apps in the session, pressing ⌘⌥↑ is a silent no-op (no crash, no toast)"
      ],
      "codeRefs": [
        "Sources/Waves/App/WavesApp.swift:150-189",
        "Sources/Waves/Stores/AppStore.swift:1210-1227",
        "Sources/Waves/Settings/SettingsView.swift:127-160",
        "Sources/WavesAudioCore/Models/AudioApp.swift:13"
      ],
      "implStatus": "partial",
      "implNotes": "Wired end-to-end and commits/persists correctly, but 'frontmost app' is mis-resolved: the target is activeApps.first AFTER sorting (by name/activity/category, AppStore.swift:1278-1297), not the OS-focused app. activeApps is visibleApps.filter(\\.isActive) where isActive = isAudible || isFrontmost (WorkspaceAudioControlBackend.swift:1177-1189), so when several apps are audible the hotkey adjusts whichever happens to sort first, not the app the user is actually using. In Name sort, that's effectively alphabetical — a real correctness/UX bug versus the README's 'frontmost app' promise (README.md:122).",
      "source": "phase1-story",
      "testStatus": "4 issue(s) [2H 1M 1L]",
      "errorsFound": "• [HIGH/correctness] \"Frontmost app\" is resolved as the sort-first active app, not the OS-focused app (Sources/Waves/Stores/AppStore.swift:1212) — increaseVolumeForFrontmostApp resolves the target as activeApps.first ?? visibleApps.first (AppStore.swift:1212). activeApps is visibleApps.filter(\\.isActive) (AppStore.swift:113-115) and isActive is set to isAudible || isFrontmost in the backend (WorkspaceAudioControlBackend.swift:1176-1189). visibleApps is run through sortedApps (AppStore.swift:103-107): in .name sort the comparator is purely alphabetical (AppStore.swift:1323-1325); in .activity sort audible apps (activityRank 0/1) outrank a silent frontmost app (rank 2, AppStore.swift:1303-1318). So whenever an audible app exists alongside the focused-but-silent app, ⌘⌥↑ raises the sort-first/audible app, not the focused one. Re-read and reproduced: the ordering and isActive definitions are exactly as the first tester described. | Fix: Resolve the target from NSWorkspace.shared.frontmostApplication's pid/bundleID and match it against session.apps, falling back to the first audible app only when no managed app matches. Do not rely on sorted activeApps ordering for 'frontmost'.\n• [HIGH/error-handling] Hotkey re-taps and shows a false \"Volume increased\" toast for an EXCLUDED app (Sources/Waves/Stores/AppStore.swift:1218) — When activeApps.first resolves to an excluded app (excluded apps remain in session.apps/visibleApps), setDesiredVolume early-returns at AppStore.swift:357 so session state and pendingVolumeTargets are NOT updated. But increaseVolumeForFrontmostApp still calls commitDesiredVolume(for: app) (AppStore.swift:1219). In commitDesiredVolume (AppStore.swift:384-392) pendingVolumeTargets[appKey] is nil, so it sets it to app.desiredVolume and schedules an immediate apply, which calls backend.setDesiredVolume (AppStore.swift:427). The backend has no exclusion concept and applyRoute sets routingState=.managed (WorkspaceAudioControlBackend.swift:65-72), re-engaging a tap on an app the user explicitly excluded. On top of that, the unconditional 'Volume increased — <App>: <NN>%' toast (AppStore.swift:1221-1226) and the async 'Managed route active' toast (AppStore.swift:433-438) both fire. So this is worse than the first tester's 'false toast' framing: the exclusion guarantee is violated. Story EXCL criterion says the excluded app's audio must not change. | Fix: Guard the hotkey on !isExcluded(app) before computing/applying and toasting (skip or show an 'App excluded' info toast). Do not call commitDesiredVolume when setDesiredVolume was a no-op; have setDesiredVolume report success so the commit and toast only run on an actual change.\n• [MEDIUM/ux] Confirmation toast is invisible in the exact scenario the hotkey targets (background use) (Sources/Waves/Stores/AppStore.swift:1221) — increaseVolumeForFrontmostApp shows a 'Volume increased' toast (AppStore.swift:1221-1226), but toasts render only inside MainWindowView (MainWindowView.swift:59) and the MenuBarExtra popover (MenuBarMixerView.swift:105) via AppToastStack (AppToasts.swift:3-14). The global hotkey is designed to fire while the user is working in another app (Waves backgrounded, popover closed), so neither toast surface is on screen — the user gets no visible confirmation at the moment the hotkey acts. This is a missed defect: the feedback the code builds is never seen in the feature's primary use case, and combined with the silent-failure paths (excluded app, denied Accessibility) the user has no signal whether anything happened. | Fix: Show an OS-level HUD/notification (or a borderless floating overlay) for hotkey-driven changes so feedback is visible while Waves is in the background; the in-window toast is insufficient for global shortcuts.\n• [LOW/correctness] Text-field guard cannot protect text fields in other apps (global monitor only fires when Waves is not frontmost) (Sources/Waves/App/WavesApp.swift:169-172) — handleGlobalKeyEvent guards typing by checking NSApp.keyWindow?.firstResponder for NSTextView/NSTextField (WavesApp.swift:169-172). But the monitor is NSEvent.addGlobalMonitorForEvents (WavesApp.swift:152), which by design only delivers events while Waves is NOT the active app. While the user types in another app, Waves is backgrounded, so NSApp.keyWindow is Waves's own window or nil — never the other app's editor. The guard therefore inspects the wrong responder. Confirmed correct. Downgraded to low: the ⌘⌥+Arrow/M combos rarely collide with text editing, so real-world impact is small, but the acceptance criterion as written is unmet. | Fix: Detect the focused element in the frontmost app via the Accessibility API (AXUIElement focused-element role) rather than NSApp.keyWindow, or accept the limitation and update the spec/docs.",
      "fixStatus": "2 fixed · 2 deferred",
      "fixNotes": "tier1: FU-EXCLUDED-COMMIT-GUARD; FU-FRONTMOST-RESOLUTION",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "correctness",
          "title": "\"Frontmost app\" is resolved as the sort-first active app, not the OS-focused app",
          "detail": "increaseVolumeForFrontmostApp resolves the target as activeApps.first ?? visibleApps.first (AppStore.swift:1212). activeApps is visibleApps.filter(\\.isActive) (AppStore.swift:113-115) and isActive is set to isAudible || isFrontmost in the backend (WorkspaceAudioControlBackend.swift:1176-1189). visibleApps is run through sortedApps (AppStore.swift:103-107): in .name sort the comparator is purely alphabetical (AppStore.swift:1323-1325); in .activity sort audible apps (activityRank 0/1) outrank a silent frontmost app (rank 2, AppStore.swift:1303-1318). So whenever an audible app exists alongside the focused-but-silent app, ⌘⌥↑ raises the sort-first/audible app, not the focused one. Re-read and reproduced: the ordering and isActive definitions are exactly as the first tester described.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1212",
          "expectedVsActual": "README:122 and the story promise the hotkey raises the volume of the app the user is currently using (frontmost). Actual: it raises the alphabetically/rank-first active app, frequently not the focused app.",
          "suggestedFix": "Resolve the target from NSWorkspace.shared.frontmostApplication's pid/bundleID and match it against session.apps, falling back to the first audible app only when no managed app matches. Do not rely on sorted activeApps ordering for 'frontmost'.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:FU-FRONTMOST-RESOLUTION",
          "uid": "KEY-01#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-FRONTMOST-RESOLUTION",
          "fixTier": "tier1"
        },
        {
          "severity": "high",
          "type": "error-handling",
          "title": "Hotkey re-taps and shows a false \"Volume increased\" toast for an EXCLUDED app",
          "detail": "When activeApps.first resolves to an excluded app (excluded apps remain in session.apps/visibleApps), setDesiredVolume early-returns at AppStore.swift:357 so session state and pendingVolumeTargets are NOT updated. But increaseVolumeForFrontmostApp still calls commitDesiredVolume(for: app) (AppStore.swift:1219). In commitDesiredVolume (AppStore.swift:384-392) pendingVolumeTargets[appKey] is nil, so it sets it to app.desiredVolume and schedules an immediate apply, which calls backend.setDesiredVolume (AppStore.swift:427). The backend has no exclusion concept and applyRoute sets routingState=.managed (WorkspaceAudioControlBackend.swift:65-72), re-engaging a tap on an app the user explicitly excluded. On top of that, the unconditional 'Volume increased — <App>: <NN>%' toast (AppStore.swift:1221-1226) and the async 'Managed route active' toast (AppStore.swift:433-438) both fire. So this is worse than the first tester's 'false toast' framing: the exclusion guarantee is violated. Story EXCL criterion says the excluded app's audio must not change.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1218",
          "expectedVsActual": "Expected: an excluded app is left completely untouched and the user is not told a change happened. Actual: commitDesiredVolume re-taps it via the backend (routingState becomes .managed) and a positive 'Volume increased'/'Managed route active' toast is shown.",
          "suggestedFix": "Guard the hotkey on !isExcluded(app) before computing/applying and toasting (skip or show an 'App excluded' info toast). Do not call commitDesiredVolume when setDesiredVolume was a no-op; have setDesiredVolume report success so the commit and toast only run on an actual change.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:FU-EXCLUDED-COMMIT-GUARD",
          "uid": "KEY-01#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-EXCLUDED-COMMIT-GUARD",
          "fixTier": "tier1"
        },
        {
          "severity": "medium",
          "type": "ux",
          "title": "Confirmation toast is invisible in the exact scenario the hotkey targets (background use)",
          "detail": "increaseVolumeForFrontmostApp shows a 'Volume increased' toast (AppStore.swift:1221-1226), but toasts render only inside MainWindowView (MainWindowView.swift:59) and the MenuBarExtra popover (MenuBarMixerView.swift:105) via AppToastStack (AppToasts.swift:3-14). The global hotkey is designed to fire while the user is working in another app (Waves backgrounded, popover closed), so neither toast surface is on screen — the user gets no visible confirmation at the moment the hotkey acts. This is a missed defect: the feedback the code builds is never seen in the feature's primary use case, and combined with the silent-failure paths (excluded app, denied Accessibility) the user has no signal whether anything happened.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1221",
          "expectedVsActual": "Story: the user adjusts the app they're using without opening Waves and gets feedback. Actual: the only feedback (a toast) lives inside Waves's own hidden window/popover, so a background ⌘⌥↑ produces no on-screen confirmation.",
          "suggestedFix": "Show an OS-level HUD/notification (or a borderless floating overlay) for hotkey-driven changes so feedback is visible while Waves is in the background; the in-window toast is insufficient for global shortcuts.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-feature:A background-visible HUD/notification surface is a sizable new feature.",
          "uid": "KEY-01#3",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "low",
          "type": "correctness",
          "title": "Text-field guard cannot protect text fields in other apps (global monitor only fires when Waves is not frontmost)",
          "detail": "handleGlobalKeyEvent guards typing by checking NSApp.keyWindow?.firstResponder for NSTextView/NSTextField (WavesApp.swift:169-172). But the monitor is NSEvent.addGlobalMonitorForEvents (WavesApp.swift:152), which by design only delivers events while Waves is NOT the active app. While the user types in another app, Waves is backgrounded, so NSApp.keyWindow is Waves's own window or nil — never the other app's editor. The guard therefore inspects the wrong responder. Confirmed correct. Downgraded to low: the ⌘⌥+Arrow/M combos rarely collide with text editing, so real-world impact is small, but the acceptance criterion as written is unmet.",
          "codeRef": "Sources/Waves/App/WavesApp.swift:169-172",
          "expectedVsActual": "Story criterion: 'Pressing ⌘⌥↑ while focus is in a text field does not change any app volume.' Actual: the guard only catches focus inside Waves's own windows; for the realistic case (typing in another app) it is a no-op.",
          "suggestedFix": "Detect the focused element in the frontmost app via the Accessibility API (AXUIElement focused-element role) rather than NSApp.keyWindow, or accept the limitation and update the spec/docs.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/App/WavesApp.swift",
          "disposition": "deferred:needs-user-decision:Platform limitation: cannot intercept/inspect another app's text-field focus.",
          "uid": "KEY-01#4",
          "canonicalFile": "Sources/Waves/App/WavesApp.swift"
        }
      ]
    },
    {
      "id": "KEY-02",
      "area": "Global Keyboard Shortcuts",
      "feature": "Global volume-down hotkey (⌘⌥↓)",
      "priority": "P1",
      "userStory": "As a Waves user, I want to press ⌘⌥↓ from anywhere to lower the volume of the app I'm using, so that I can quickly duck a noisy app without leaving my current window.",
      "expectedBehavior": [
        "On keyCode 125 (Down arrow) with Command+Option, AppDelegate calls store.decreaseVolumeForFrontmostApp() (WavesApp.swift:183)",
        "Target app resolution is identical to volume-up: activeApps.first ?? visibleApps.first (AppStore.swift:1231-1232)",
        "Step defaults to 0.1, clamped to [0.01, 0.5]; new volume is clamped to a minimum of 0.0 (AppStore.swift:1235-1236)",
        "setDesiredVolume + commitDesiredVolume apply and persist the lower volume (AppStore.swift:1237-1238)",
        "An info toast 'Volume decreased — <App>: <NN>%' is shown for 1 second (AppStore.swift:1240-1245)",
        "No-op when shortcuts disabled or no app available (AppStore.swift:1230-1232); ignored while typing in a text field (WavesApp.swift:169-172)"
      ],
      "acceptanceCriteria": [
        "Pressing ⌘⌥↓ lowers the resolved app's volume by ~10% and shows a 'Volume decreased' toast",
        "Repeated presses never push volume below 0% (0.0)",
        "Disabled shortcuts or empty session yields no change and no toast",
        "Focus in a text field suppresses the shortcut"
      ],
      "codeRefs": [
        "Sources/Waves/App/WavesApp.swift:179-189",
        "Sources/Waves/Stores/AppStore.swift:1229-1246",
        "Sources/Waves/Settings/SettingsView.swift:141-148"
      ],
      "implStatus": "partial",
      "implNotes": "Same frontmost-app mis-resolution as KEY-01 (activeApps.first after sorting rather than the OS-focused app). Otherwise complete and symmetric with the increase path; lower bound correctly clamps at 0.0.",
      "source": "phase1-story",
      "testStatus": "4 issue(s) [2H 1M 1L]",
      "errorsFound": "• [HIGH/correctness] \"Frontmost app\" mis-resolution (same defect as KEY-01) on the volume-down path (Sources/Waves/Stores/AppStore.swift:1231) — decreaseVolumeForFrontmostApp uses the identical resolution activeApps.first ?? visibleApps.first (AppStore.swift:1231). Confirmed against the same sort/isActive code paths as KEY-01. When an audible app coexists with a silent focused app, ⌘⌥↓ ducks the sort-first/audible app, not the focused one. | Fix: Same fix as KEY-01: resolve the target from NSWorkspace.shared.frontmostApplication and match it to a managed app before falling back to the first audible app.\n• [HIGH/error-handling] Hotkey re-taps and shows a false \"Volume decreased\" toast for an EXCLUDED app (Sources/Waves/Stores/AppStore.swift:1237) — Mirror of KEY-01's excluded-app issue on the down path: setDesiredVolume early-returns for excluded apps (AppStore.swift:357), but decreaseVolumeForFrontmostApp still calls commitDesiredVolume (AppStore.swift:1238), which writes a pendingVolumeTarget and schedules backend.setDesiredVolume, re-engaging a tap (routingState=.managed, WorkspaceAudioControlBackend.swift:65-72) on the excluded app, plus shows 'Volume decreased' (AppStore.swift:1240-1245) and 'Managed route active' toasts. | Fix: Guard on !isExcluded(app) before applying/committing/toasting; do not commit when setDesiredVolume no-ops.\n• [MEDIUM/ux] Confirmation toast is invisible during background use (volume-down path) (Sources/Waves/Stores/AppStore.swift:1240) — Same as KEY-01: the 'Volume decreased' toast (AppStore.swift:1240-1245) renders only inside MainWindowView/MenuBarMixerView (AppToasts.swift), which are not on screen when the global hotkey fires from another app, so a background ⌘⌥↓ produces no visible feedback. | Fix: Use an OS-level HUD/notification or floating overlay for hotkey-driven changes.\n• [LOW/correctness] Text-field guard is ineffective for typing in other apps on the volume-down path (Sources/Waves/App/WavesApp.swift:169-172) — Same root cause as KEY-01: the global monitor only fires when Waves is backgrounded, so NSApp.keyWindow?.firstResponder (WavesApp.swift:169-172) never sees the foreground app's text field and ⌘⌥↓ is not suppressed while typing in another app. Confirmed; downgraded to low because the combo rarely collides with typing. | Fix: Detect the frontmost app's focused element via the Accessibility API instead of NSApp.keyWindow, or document the limitation.",
      "fixStatus": "2 fixed · 2 deferred",
      "fixNotes": "tier1: FU-EXCLUDED-COMMIT-GUARD; FU-FRONTMOST-RESOLUTION",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "correctness",
          "title": "\"Frontmost app\" mis-resolution (same defect as KEY-01) on the volume-down path",
          "detail": "decreaseVolumeForFrontmostApp uses the identical resolution activeApps.first ?? visibleApps.first (AppStore.swift:1231). Confirmed against the same sort/isActive code paths as KEY-01. When an audible app coexists with a silent focused app, ⌘⌥↓ ducks the sort-first/audible app, not the focused one.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1231",
          "expectedVsActual": "Story/README: lowers the volume of the app the user is using. Actual: lowers the sort-first active app, frequently not the focused one.",
          "suggestedFix": "Same fix as KEY-01: resolve the target from NSWorkspace.shared.frontmostApplication and match it to a managed app before falling back to the first audible app.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:FU-FRONTMOST-RESOLUTION",
          "uid": "KEY-02#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-FRONTMOST-RESOLUTION",
          "fixTier": "tier1"
        },
        {
          "severity": "high",
          "type": "error-handling",
          "title": "Hotkey re-taps and shows a false \"Volume decreased\" toast for an EXCLUDED app",
          "detail": "Mirror of KEY-01's excluded-app issue on the down path: setDesiredVolume early-returns for excluded apps (AppStore.swift:357), but decreaseVolumeForFrontmostApp still calls commitDesiredVolume (AppStore.swift:1238), which writes a pendingVolumeTarget and schedules backend.setDesiredVolume, re-engaging a tap (routingState=.managed, WorkspaceAudioControlBackend.swift:65-72) on the excluded app, plus shows 'Volume decreased' (AppStore.swift:1240-1245) and 'Managed route active' toasts.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1237",
          "expectedVsActual": "Expected: excluded app untouched, no misleading confirmation. Actual: excluded app is re-tapped and a false 'Volume decreased' toast is shown.",
          "suggestedFix": "Guard on !isExcluded(app) before applying/committing/toasting; do not commit when setDesiredVolume no-ops.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:FU-EXCLUDED-COMMIT-GUARD",
          "uid": "KEY-02#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-EXCLUDED-COMMIT-GUARD",
          "fixTier": "tier1"
        },
        {
          "severity": "medium",
          "type": "ux",
          "title": "Confirmation toast is invisible during background use (volume-down path)",
          "detail": "Same as KEY-01: the 'Volume decreased' toast (AppStore.swift:1240-1245) renders only inside MainWindowView/MenuBarMixerView (AppToasts.swift), which are not on screen when the global hotkey fires from another app, so a background ⌘⌥↓ produces no visible feedback.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1240",
          "expectedVsActual": "Story: feedback when ducking the focused app from another window. Actual: feedback lives in Waves's hidden window/popover and is unseen.",
          "suggestedFix": "Use an OS-level HUD/notification or floating overlay for hotkey-driven changes.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-feature:Background-visible hotkey feedback is a new UI feature.",
          "uid": "KEY-02#3",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "low",
          "type": "correctness",
          "title": "Text-field guard is ineffective for typing in other apps on the volume-down path",
          "detail": "Same root cause as KEY-01: the global monitor only fires when Waves is backgrounded, so NSApp.keyWindow?.firstResponder (WavesApp.swift:169-172) never sees the foreground app's text field and ⌘⌥↓ is not suppressed while typing in another app. Confirmed; downgraded to low because the combo rarely collides with typing.",
          "codeRef": "Sources/Waves/App/WavesApp.swift:169-172",
          "expectedVsActual": "Story criterion: 'Focus in a text field suppresses the shortcut.' Actual: only suppressed for focus inside Waves's own windows.",
          "suggestedFix": "Detect the frontmost app's focused element via the Accessibility API instead of NSApp.keyWindow, or document the limitation.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/App/WavesApp.swift",
          "disposition": "deferred:needs-user-decision:Platform limitation: cannot intercept/inspect another app's text-field focus.",
          "uid": "KEY-02#4",
          "canonicalFile": "Sources/Waves/App/WavesApp.swift"
        }
      ]
    },
    {
      "id": "KEY-03",
      "area": "Global Keyboard Shortcuts",
      "feature": "Global mute-toggle hotkey (⌘⌥M)",
      "priority": "P1",
      "userStory": "As a Waves user, I want to press ⌘⌥M from anywhere to instantly mute or unmute the app I'm using, so that I can silence an app without hunting for its slider.",
      "expectedBehavior": [
        "On keyCode 46 (M) with Command+Option, AppDelegate calls store.toggleMuteForFrontmostApp() (WavesApp.swift:184-185)",
        "Target app resolution is activeApps.first ?? visibleApps.first (AppStore.swift:1250-1251)",
        "The new mute state is the inverse of the resolved app's current isMuted (AppStore.swift:1253)",
        "setMuted(newMutedState, for: app) is invoked, which marks muteSource = .user, updates appliedVolume, calls backend.setMuted, and persists (AppStore.swift:1254, 491-541)",
        "An info toast 'Muted'/'Unmuted' with the app name is shown for 1 second (AppStore.swift:1256-1261); setMuted also raises its own 'App muted/unmuted' success toast on backend completion (AppStore.swift:528-533)",
        "No mute step gate beyond enableKeyboardShortcuts; excluded apps are protected because setMuted early-returns on isExcluded(app) (AppStore.swift:492)",
        "Ignored while typing in a text field (WavesApp.swift:169-172)"
      ],
      "acceptanceCriteria": [
        "Pressing ⌘⌥M on an unmuted resolved app mutes it and shows a 'Muted' toast; pressing again unmutes it",
        "A user-initiated mute via this shortcut sets muteSource to .user so auto-resume does not override it",
        "Pressing ⌘⌥M with shortcuts disabled does nothing",
        "If the resolved app is excluded, no mute change occurs"
      ],
      "codeRefs": [
        "Sources/Waves/App/WavesApp.swift:184-185",
        "Sources/Waves/Stores/AppStore.swift:1248-1262",
        "Sources/Waves/Stores/AppStore.swift:491-541",
        "Sources/Waves/Settings/SettingsView.swift:150-157"
      ],
      "implStatus": "partial",
      "implNotes": "Functionally complete and backend-applied. Two concerns: (1) same frontmost-app mis-resolution as KEY-01/02. (2) Double-toast — toggleMuteForFrontmostApp shows an immediate info toast (AppStore.swift:1256) and setMuted shows a second success toast asynchronously (AppStore.swift:528), so a single ⌘⌥M can surface two overlapping toasts. (3) Unlike volume, toggleMuteForFrontmostApp does not itself check enableKeyboardShortcuts redundantly at the top before reading state — it does (line 1249) — fine; but note the optimistic toast fires even if backend.setMuted later fails (which only then shows an error toast).",
      "source": "phase1-story",
      "testStatus": "5 issue(s) [1H 3M 1L]",
      "errorsFound": "• [HIGH/correctness] \"Frontmost app\" mis-resolution (same defect as KEY-01) on the mute-toggle path (Sources/Waves/Stores/AppStore.swift:1250) — toggleMuteForFrontmostApp resolves activeApps.first ?? visibleApps.first (AppStore.swift:1250). Confirmed against the same sort/isActive paths. When several apps are audible, ⌘⌥M mutes the sort-first/audible app rather than the focused app. | Fix: Resolve via NSWorkspace.shared.frontmostApplication matched to a managed app before falling back to the first audible app.\n• [MEDIUM/ux] Double toast on a single ⌘⌥M press (Sources/Waves/Stores/AppStore.swift:1256) — toggleMuteForFrontmostApp shows an immediate info toast 'Muted'/'Unmuted' (AppStore.swift:1256-1261). The setMuted it calls then shows a second, asynchronous success toast 'App muted'/'App unmuted' on backend completion (AppStore.swift:528-533). showToast appends to the toast stack (AppStore.swift:1455), so one keypress surfaces two toasts for the same action. Confirmed by reading both toast sites and showToast. | Fix: Drop the optimistic toast in toggleMuteForFrontmostApp and rely on setMuted's success/error toast, or add a parameter to setMuted to suppress its own toast when invoked from the hotkey.\n• [MEDIUM/error-handling] Optimistic 'Muted'/'Unmuted' toast fires even when the mute is rejected (excluded app) or the backend later fails (Sources/Waves/Stores/AppStore.swift:1254) — toggleMuteForFrontmostApp shows the toast unconditionally (AppStore.swift:1256). setMuted early-returns for excluded apps before the backend Task (AppStore.swift:492) — so unlike the volume path, the mute path does NOT re-tap an excluded app, but the optimistic 'Muted'/'Unmuted' toast still claims a change that did not happen. Separately, if backend.setMuted fails the user sees the optimistic 'Muted' toast plus a later 'Mute toggle failed' error toast (AppStore.swift:539) — contradictory feedback. Confirmed. | Fix: Check isExcluded(app) in toggleMuteForFrontmostApp and skip/replace the toast; defer the success toast to setMuted's confirmed completion path instead of showing it optimistically.\n• [MEDIUM/ux] Mute confirmation toast(s) invisible during background use (Sources/Waves/Stores/AppStore.swift:1256) — Both the optimistic 'Muted'/'Unmuted' toast (AppStore.swift:1256-1261) and setMuted's 'App muted'/'App unmuted' toast (AppStore.swift:528-533) render only inside MainWindowView/MenuBarMixerView (AppToasts.swift), which are not on screen when ⌘⌥M is pressed from another app. So the user muting the focused app from another window gets no visible confirmation. | Fix: Surface hotkey mute feedback via an OS-level HUD/notification or floating overlay rather than the in-window toast stack.\n• [LOW/correctness] Text-field guard ineffective for typing in other apps on the mute-toggle path (Sources/Waves/App/WavesApp.swift:169-172) — Same root cause as KEY-01: the global monitor only fires while Waves is backgrounded, so the NSApp.keyWindow?.firstResponder check (WavesApp.swift:169-172) cannot see the foreground app's text field and ⌘⌥M is not suppressed while typing elsewhere. Confirmed; downgraded to low given collision rarity. | Fix: Use the Accessibility API focused-element role of the frontmost app, or document the limitation.",
      "fixStatus": "3 fixed · 2 deferred",
      "fixNotes": "tier1: FU-FRONTMOST-RESOLUTION | tier2: FU-EXCLUDED-FRONTMOST-TOASTS",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "correctness",
          "title": "\"Frontmost app\" mis-resolution (same defect as KEY-01) on the mute-toggle path",
          "detail": "toggleMuteForFrontmostApp resolves activeApps.first ?? visibleApps.first (AppStore.swift:1250). Confirmed against the same sort/isActive paths. When several apps are audible, ⌘⌥M mutes the sort-first/audible app rather than the focused app.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1250",
          "expectedVsActual": "Story/README: toggles mute for the app the user is using. Actual: toggles mute for the sort-first active app.",
          "suggestedFix": "Resolve via NSWorkspace.shared.frontmostApplication matched to a managed app before falling back to the first audible app.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:FU-FRONTMOST-RESOLUTION",
          "uid": "KEY-03#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-FRONTMOST-RESOLUTION",
          "fixTier": "tier1"
        },
        {
          "severity": "medium",
          "type": "ux",
          "title": "Double toast on a single ⌘⌥M press",
          "detail": "toggleMuteForFrontmostApp shows an immediate info toast 'Muted'/'Unmuted' (AppStore.swift:1256-1261). The setMuted it calls then shows a second, asynchronous success toast 'App muted'/'App unmuted' on backend completion (AppStore.swift:528-533). showToast appends to the toast stack (AppStore.swift:1455), so one keypress surfaces two toasts for the same action. Confirmed by reading both toast sites and showToast.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1256",
          "expectedVsActual": "Story expects a single 'Muted'/'Unmuted' toast per press. Actual: two toasts (optimistic info + async success).",
          "suggestedFix": "Drop the optimistic toast in toggleMuteForFrontmostApp and rely on setMuted's success/error toast, or add a parameter to setMuted to suppress its own toast when invoked from the hotkey.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "gapfill:already-resolved",
          "uid": "KEY-03#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "earlier-tier"
        },
        {
          "severity": "medium",
          "type": "error-handling",
          "title": "Optimistic 'Muted'/'Unmuted' toast fires even when the mute is rejected (excluded app) or the backend later fails",
          "detail": "toggleMuteForFrontmostApp shows the toast unconditionally (AppStore.swift:1256). setMuted early-returns for excluded apps before the backend Task (AppStore.swift:492) — so unlike the volume path, the mute path does NOT re-tap an excluded app, but the optimistic 'Muted'/'Unmuted' toast still claims a change that did not happen. Separately, if backend.setMuted fails the user sees the optimistic 'Muted' toast plus a later 'Mute toggle failed' error toast (AppStore.swift:539) — contradictory feedback. Confirmed.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1254",
          "expectedVsActual": "Story criterion: 'If the resolved app is excluded, no mute change occurs.' The mute correctly does not occur, but the UI confirms 'Muted', misleading the user; failures produce contradictory toasts.",
          "suggestedFix": "Check isExcluded(app) in toggleMuteForFrontmostApp and skip/replace the toast; defer the success toast to setMuted's confirmed completion path instead of showing it optimistically.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier2:partial:FU-EXCLUDED-FRONTMOST-TOASTS",
          "uid": "KEY-03#3",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "partial",
          "fixedBy": "FU-EXCLUDED-FRONTMOST-TOASTS",
          "fixTier": "tier2"
        },
        {
          "severity": "medium",
          "type": "ux",
          "title": "Mute confirmation toast(s) invisible during background use",
          "detail": "Both the optimistic 'Muted'/'Unmuted' toast (AppStore.swift:1256-1261) and setMuted's 'App muted'/'App unmuted' toast (AppStore.swift:528-533) render only inside MainWindowView/MenuBarMixerView (AppToasts.swift), which are not on screen when ⌘⌥M is pressed from another app. So the user muting the focused app from another window gets no visible confirmation.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1256",
          "expectedVsActual": "Story: silence the app you're using and get clear feedback. Actual: the feedback toasts live in Waves's hidden window/popover and are unseen in the primary use case.",
          "suggestedFix": "Surface hotkey mute feedback via an OS-level HUD/notification or floating overlay rather than the in-window toast stack.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-feature:Requires building a new OS-level HUD/notification/overlay surface.",
          "uid": "KEY-03#4",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "low",
          "type": "correctness",
          "title": "Text-field guard ineffective for typing in other apps on the mute-toggle path",
          "detail": "Same root cause as KEY-01: the global monitor only fires while Waves is backgrounded, so the NSApp.keyWindow?.firstResponder check (WavesApp.swift:169-172) cannot see the foreground app's text field and ⌘⌥M is not suppressed while typing elsewhere. Confirmed; downgraded to low given collision rarity.",
          "codeRef": "Sources/Waves/App/WavesApp.swift:169-172",
          "expectedVsActual": "Story expects typing to suppress the shortcut. Actual: only suppressed inside Waves's own windows.",
          "suggestedFix": "Use the Accessibility API focused-element role of the frontmost app, or document the limitation.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/App/WavesApp.swift",
          "disposition": "deferred:needs-user-decision:Platform limitation: cannot intercept/inspect another app's text-field focus.",
          "uid": "KEY-03#5",
          "canonicalFile": "Sources/Waves/App/WavesApp.swift"
        }
      ]
    },
    {
      "id": "KEY-04",
      "area": "Global Keyboard Shortcuts",
      "feature": "Enable/disable global shortcuts toggle with lazy monitor install",
      "priority": "P2",
      "userStory": "As a privacy-conscious user, I want a single setting to turn global shortcuts on or off, so that Waves only observes my system-wide keystrokes when I actually want the hotkeys.",
      "expectedBehavior": [
        "Settings exposes an 'Enable keyboard shortcuts' toggle bound to setKeyboardShortcutsEnabled (SettingsView.swift:98-103)",
        "enableKeyboardShortcuts defaults to true and is persisted via PreferencesStore (UserPreferences.swift:11, 30, 56)",
        "setKeyboardShortcutsEnabled writes the preference, persists it, and posts .wavesKeyboardShortcutsPreferenceChanged (AppStore.swift:1272-1276)",
        "AppDelegate observes that notification and calls updateGlobalHotkeysState, installing the global monitor only when enabled and removing it when disabled (WavesApp.swift:91-112)",
        "setupGlobalHotkeys guards against duplicate monitors (eventMonitor == nil) and removeGlobalHotkeys tears down the monitor (WavesApp.swift:150-162)",
        "The monitor is also removed on app termination (WavesApp.swift:118-119)",
        "When the toggle is on, a read-only reference card listing ⌘⌥↑ / ⌘⌥↓ / ⌘⌥M appears in Settings (SettingsView.swift:127-160)"
      ],
      "acceptanceCriteria": [
        "Turning the toggle off removes the global event monitor so no system-wide keystrokes are observed",
        "Turning it back on re-installs exactly one monitor (no duplicate monitors after repeated toggling)",
        "The preference survives app relaunch (default true on first launch)",
        "The ⌘⌥ shortcut reference list is shown only while the toggle is enabled",
        "On quit, the monitor is removed"
      ],
      "codeRefs": [
        "Sources/Waves/Settings/SettingsView.swift:98-160",
        "Sources/Waves/Stores/AppStore.swift:1272-1276",
        "Sources/Waves/Stores/AppStore.swift:1512",
        "Sources/Waves/App/WavesApp.swift:90-112,118-162",
        "Sources/Waves/Stores/UserPreferences.swift:11,30,56"
      ],
      "implStatus": "implemented",
      "implNotes": "Clean lazy install/teardown keyed off a NotificationCenter event, with duplicate-monitor guard and termination cleanup. The Settings reference list is purely informational (the shortcuts are hard-coded keyCodes; no remapping UI). Note the default is ON (UserPreferences.swift:11), so the global monitor is installed by default at first launch — reasonable but worth noting for a privacy-default review.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "KEY-05",
      "area": "Global Keyboard Shortcuts",
      "feature": "Accessibility permission requirement for global hotkeys",
      "priority": "P1",
      "userStory": "As a new user, I want Waves to tell me that global shortcuts need Accessibility permission and point me to System Settings, so that I understand why ⌘⌥ hotkeys don't work until I grant it.",
      "expectedBehavior": [
        "Onboarding presents an optional Accessibility step stating per-app routing works without it but global shortcuts/app-control helpers need it (OnboardingView.swift:25)",
        "The onboarding action for that step opens the Accessibility pane in System Settings via x-apple.systempreferences URL (OnboardingView.swift:167-170)",
        "Permission state is read with AXIsProcessTrusted() and stored as onboarding.accessibilityPermissionGranted (AppStore.swift:1359, 1499)",
        "Diagnostics report an 'Accessibility permission' check, passed when AXIsProcessTrusted() is true and a warning otherwise, with guidance text about global shortcuts (WorkspaceAudioControlBackend.swift:258-262, 318-319)",
        "README documents that Accessibility permission is required only for global shortcuts and lists it under troubleshooting (README.md:65, 187-190)"
      ],
      "acceptanceCriteria": [
        "Without Accessibility permission granted, the onboarding step and diagnostics surface a warning explaining global shortcuts need it",
        "Tapping the onboarding action opens the macOS Accessibility settings pane",
        "Granting permission flips the diagnostics check to passed and accessibilityPermissionGranted to true after the next sync/refresh",
        "Per-app volume routing remains usable without this permission"
      ],
      "codeRefs": [
        "Sources/Waves/Features/Onboarding/OnboardingView.swift:25,144,154,167-170",
        "Sources/Waves/Stores/AppStore.swift:1359,1499",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:258-262,318-319",
        "README.md:65,187-190"
      ],
      "implStatus": "partial",
      "implNotes": "Permission is surfaced in onboarding and diagnostics, but there is a functional gap at the hotkey layer: setupGlobalHotkeys (WavesApp.swift:150-155) installs the global NSEvent monitor regardless of AXIsProcessTrusted(). macOS will NOT deliver global keyDown events to that monitor without Accessibility permission, so with the shortcuts toggle ON but permission denied, the hotkeys silently do nothing and no toast/warning fires at press time. Nothing re-checks permission or prompts when the user enables shortcuts, and there is no AXIsProcessTrustedWithOptions prompt-on-demand. Users must self-diagnose via the diagnostics report or README troubleshooting.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1H]",
      "errorsFound": "• [HIGH/error-handling] Global hotkeys silently do nothing when Accessibility is denied — no press-time feedback, prompt, or recheck (Sources/Waves/App/WavesApp.swift:150-155) — setupGlobalHotkeys installs the NSEvent.addGlobalMonitorForEvents keyDown monitor whenever shortcuts are enabled (WavesApp.swift:150-155), with no AXIsProcessTrusted() check. macOS does not deliver global keyDown events to that monitor without Accessibility/Input-Monitoring permission, so with the toggle ON but permission denied the monitor's callback never fires and handleGlobalKeyEvent never runs — no toast, no warning at press time. setKeyboardShortcutsEnabled (AppStore.swift:1272-1276) and updateGlobalHotkeysState (WavesApp.swift:105-112) neither recheck permission nor call AXIsProcessTrustedWithOptions to prompt when the user turns shortcuts on. AX is only read in onboarding sync (AppStore.swift:1359) and diagnostics (WorkspaceAudioControlBackend.swift:257-263) — verified there is no AX reference in WavesApp.swift. The user must self-diagnose. Confirmed. | Fix: When shortcuts are enabled (setKeyboardShortcutsEnabled / updateGlobalHotkeysState), check AXIsProcessTrusted(); if false, call AXIsProcessTrustedWithOptions([kAXTrustedCheckOptionPrompt: true]) to prompt, and/or show a warning toast linking to System Settings so the user is told the hotkeys won't fire until permission is granted.",
      "fixStatus": "1 deferred",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "error-handling",
          "title": "Global hotkeys silently do nothing when Accessibility is denied — no press-time feedback, prompt, or recheck",
          "detail": "setupGlobalHotkeys installs the NSEvent.addGlobalMonitorForEvents keyDown monitor whenever shortcuts are enabled (WavesApp.swift:150-155), with no AXIsProcessTrusted() check. macOS does not deliver global keyDown events to that monitor without Accessibility/Input-Monitoring permission, so with the toggle ON but permission denied the monitor's callback never fires and handleGlobalKeyEvent never runs — no toast, no warning at press time. setKeyboardShortcutsEnabled (AppStore.swift:1272-1276) and updateGlobalHotkeysState (WavesApp.swift:105-112) neither recheck permission nor call AXIsProcessTrustedWithOptions to prompt when the user turns shortcuts on. AX is only read in onboarding sync (AppStore.swift:1359) and diagnostics (WorkspaceAudioControlBackend.swift:257-263) — verified there is no AX reference in WavesApp.swift. The user must self-diagnose. Confirmed.",
          "codeRef": "Sources/Waves/App/WavesApp.swift:150-155",
          "expectedVsActual": "Story: the user should understand why ⌘⌥ hotkeys don't work until Accessibility is granted. Onboarding/diagnostics surface a warning, but at the moment of enabling/using shortcuts there is no prompt, recheck, or feedback — the hotkeys just fail silently.",
          "suggestedFix": "When shortcuts are enabled (setKeyboardShortcutsEnabled / updateGlobalHotkeysState), check AXIsProcessTrusted(); if false, call AXIsProcessTrustedWithOptions([kAXTrustedCheckOptionPrompt: true]) to prompt, and/or show a warning toast linking to System Settings so the user is told the hotkeys won't fire until permission is granted.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/App/WavesApp.swift",
          "disposition": "deferred:needs-user-decision:Requires a new feedback surface, not an existing view/preference reuse.",
          "uid": "KEY-05#1",
          "canonicalFile": "Sources/Waves/App/WavesApp.swift"
        }
      ]
    },
    {
      "id": "URL-01",
      "area": "URL Scheme Automation (opt-in)",
      "feature": "Opt-in toggle for URL scheme automation",
      "priority": "P2",
      "userStory": "As a privacy-conscious Waves user, I want waves:// automation to be off by default and only enabled when I explicitly turn it on, so that no other app or link can control my audio without my consent.",
      "expectedBehavior": [
        "UserPreferences exposes enableURLScheme (default false) and a separate urlSchemeAutomationAcknowledged flag (default false).",
        "Settings > General shows a 'URL scheme automation' Toggle bound to store.preferences.enableURLScheme.",
        "Flipping the toggle sets enableURLScheme to the new value, sets urlSchemeAutomationAcknowledged = true, and persists preferences immediately.",
        "The toggle has a help tooltip explaining it lets other apps and links control Waves through waves:// URLs (set volume, mute, apply presets) and that it is off by default.",
        "On AppStore init, if urlSchemeAutomationAcknowledged is false the store force-resets enableURLScheme to false and sets acknowledged = true, then persists — a one-time migration so any pre-existing 'enabled' value from before the opt-in gate cannot silently leave automation on.",
        "handleURLScheme bails out immediately (logging a warning) if preferences.enableURLScheme is false, so the entire command surface is inert until opt-in.",
        "The CFBundleURLTypes / CFBundleURLSchemes 'waves' registration is present in the shipped Info.plist so macOS routes waves:// URLs to the app even though handling is gated in code."
      ],
      "acceptanceCriteria": [
        "On a fresh install, preferences.enableURLScheme == false and the Settings toggle renders off.",
        "Invoking any waves:// URL while enableURLScheme == false produces no audio change and logs 'URL scheme invocation rejected because URL schemes are disabled'.",
        "Toggling the Settings switch on persists enableURLScheme == true and urlSchemeAutomationAcknowledged == true to disk.",
        "A preferences file with enableURLScheme == true but urlSchemeAutomationAcknowledged == false (or absent) loads with enableURLScheme forced back to false after init.",
        "Encoding/decoding round-trips enableURLScheme correctly (covered by PersistedSchemaTests / UserPreferencesTests)."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/UserPreferences.swift:13",
        "Sources/Waves/Stores/UserPreferences.swift:14",
        "Sources/Waves/Stores/UserPreferences.swift:58",
        "Sources/Waves/Stores/UserPreferences.swift:59",
        "Sources/Waves/Settings/SettingsView.swift:115",
        "Sources/Waves/Settings/SettingsView.swift:118",
        "Sources/Waves/Settings/SettingsView.swift:121",
        "Sources/Waves/Settings/SettingsView.swift:125",
        "Sources/Waves/Stores/AppStore.swift:91",
        "Sources/Waves/Stores/AppStore.swift:256",
        "dist/Waves.app/Contents/Info.plist:17"
      ],
      "implStatus": "implemented",
      "implNotes": "Solid opt-in gate. Default off, persisted, gated at both the AppDelegate routing layer and the AppStore handler. The acknowledged-migration is a thoughtful safety net. Minor UX gap: the toggle is a bare switch with only a tooltip — there is no inline subtitle or warning shown in the Settings UI itself (the security caveat lives only in README and the hover help), so a user enabling it sees no in-app explanation of the risk. Two persisted booleans (enableURLScheme + urlSchemeAutomationAcknowledged) is slightly redundant now that the migration has run, but harmless.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "URL-02",
      "area": "URL Scheme Automation (opt-in)",
      "feature": "set-volume URL command",
      "priority": "P2",
      "userStory": "As an automation user, I want to set a specific app's volume via a waves://set-volume URL, so that scripts, Shortcuts, or Stream Deck buttons can adjust per-app levels without me opening the mixer.",
      "expectedBehavior": [
        "waves://set-volume?app=APP_ID&volume=0.5 is routed to handleURLSetVolume when automation is enabled.",
        "Requires both an 'app' query item and a 'volume' query item; app value must be <= 256 chars and volume value <= 32 chars (length caps to bound parsing of hostile input).",
        "volume must parse as Float and lie in the closed range 0.0...1.0; otherwise the command is rejected with a 'URL command blocked — Set-volume command was invalid.' warning toast.",
        "The target app is resolved via first(matchingAppKey:), which matches either the process id or the logicalID (bundle-style key), so APP_ID can be either identifier.",
        "On a match, it calls setDesiredVolume(volume, for: app) then commitDesiredVolume(for: app) to apply and persist the level.",
        "If no app matches, it shows a 'URL command blocked — App not found: <appID prefix>' warning toast (appID truncated to 64 chars in the message)."
      ],
      "acceptanceCriteria": [
        "waves://set-volume?app=<validKey>&volume=0.3 with automation on sets that app's volume to 0.3 and commits it.",
        "volume=1.5, volume=-0.1, volume=abc, or a missing volume/app param each yield no volume change and a 'Set-volume command was invalid.' toast.",
        "An app/volume value exceeding the length caps is rejected as invalid.",
        "A well-formed command targeting an unknown app yields an 'App not found' toast and no change.",
        "Matching works when APP_ID is supplied as either the runtime id or the logicalID."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:278",
        "Sources/Waves/Stores/AppStore.swift:291",
        "Sources/Waves/Stores/AppStore.swift:303",
        "Sources/Waves/Stores/AppStore.swift:308",
        "Sources/Waves/Stores/AppStore.swift:1491",
        "README.md:130"
      ],
      "implStatus": "implemented",
      "implNotes": "Well-validated: range-checks volume, caps input lengths, gives distinct invalid vs not-found feedback. Edge case worth noting: if APP_ID resolves to an app the user has EXCLUDED, the app is found but setDesiredVolume/commitDesiredVolume silently return at the isExcluded guard (AppStore.swift:357) with NO toast — the command appears to succeed (no error) yet nothing happens, which is confusing for an automation caller. Also, volume here is the 0..1 desired level only; it does not touch the >1.0 volume boost, so 'volume' semantics differ from the boost slider in the UI.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1H 1L]",
      "errorsFound": "• [HIGH/logic] set-volume URL re-taps an EXCLUDED app and shows a success toast, defeating the exclusion escape-hatch (Sources/Waves/Stores/AppStore.swift:384) — CONFIRMED by tracing the code. handleURLSetVolume (AppStore.swift:303-309) resolves the app via first(matchingAppKey:) — excluded apps remain in session.apps — then calls setDesiredVolume(volume, for: app) followed by commitDesiredVolume(for: app). setDesiredVolume bails at `guard !isExcluded(app)` (line 357) WITHOUT setting pendingVolumeTargets. At exclusion time pendingVolumeTargets[appKey] was cleared (setExcluded -> line 710 removeValue), so it is nil. commitDesiredVolume (line 384) has NO isExcluded guard: because pendingVolumeTargets[appKey] == nil, line 386-388 sets pendingVolumeTargets[appKey] = app.desiredVolume (the STALE last-known desired level, not the requested volume), then scheduleVolumeApply(immediate:true) -> ensureBackendHasApp(appID) (which calls backend.refresh to re-add the app) -> backend.setDesiredVolume -> applyRoute, which re-establishes a managed route/tap (WorkspaceAudioControlBackend.swift:69-71 sets routingState=.managed) and emits a 'Managed route active' success toast (line 433-438). The author defended this exact invariant elsewhere (applyPreset strips excluded entries at line 1002-1007; savePreset at 1035-1039) but missed the commitDesiredVolume path. NOTE/correction to first tester: the claim that the URL command is the ONLY path reaching commitDesiredVolume for an excluded app is inaccurate — increaseVolumeForFrontmostApp/decreaseVolumeForFrontmostApp (lines 1218-1219, 1237-1238) also call setDesiredVolume+commitDesiredVolume and would re-tap a frontmost excluded app via keyboard shortcuts. This does not change the URL-area bug; it confirms the root cause is the missing guard in commitDesiredVolume. | Fix: Add `guard !isExcluded(app) else { return }` at the top of commitDesiredVolume (AppStore.swift:384) — this also fixes the keyboard-shortcut variant. Optionally, have handleURLSetVolume check store.isExcluded(app) before applying and show a 'URL command blocked — App is excluded from Waves' warning toast.\n• [LOW/ux] set-volume against an excluded app gives no 'excluded/blocked' caller feedback (Sources/Waves/Stores/AppStore.swift:303) — CONFIRMED. Independent of the re-tap defect, an automation caller targeting an excluded app receives no response distinguishing it from a real change. Today, due to the re-tap bug, it sees a misleading 'Managed route active' success; even after the re-tap is fixed (guard added to commitDesiredVolume) the command would become a silent no-op with no toast, unlike the 'invalid' and 'App not found' paths which both toast. This is a genuine but lower-priority consistency gap that the high-severity fix should address together. | Fix: When the resolved app isExcluded, showToast(title: 'URL command blocked', detail: 'App is excluded from Waves.', kind: .warning) and return before touching volume.",
      "fixStatus": "2 fixed",
      "fixNotes": "tier1: FU-EXCLUDED-COMMIT-GUARD | tier3: FU-URL-EXCLUDED-FEEDBACK",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "logic",
          "title": "set-volume URL re-taps an EXCLUDED app and shows a success toast, defeating the exclusion escape-hatch",
          "detail": "CONFIRMED by tracing the code. handleURLSetVolume (AppStore.swift:303-309) resolves the app via first(matchingAppKey:) — excluded apps remain in session.apps — then calls setDesiredVolume(volume, for: app) followed by commitDesiredVolume(for: app). setDesiredVolume bails at `guard !isExcluded(app)` (line 357) WITHOUT setting pendingVolumeTargets. At exclusion time pendingVolumeTargets[appKey] was cleared (setExcluded -> line 710 removeValue), so it is nil. commitDesiredVolume (line 384) has NO isExcluded guard: because pendingVolumeTargets[appKey] == nil, line 386-388 sets pendingVolumeTargets[appKey] = app.desiredVolume (the STALE last-known desired level, not the requested volume), then scheduleVolumeApply(immediate:true) -> ensureBackendHasApp(appID) (which calls backend.refresh to re-add the app) -> backend.setDesiredVolume -> applyRoute, which re-establishes a managed route/tap (WorkspaceAudioControlBackend.swift:69-71 sets routingState=.managed) and emits a 'Managed route active' success toast (line 433-438). The author defended this exact invariant elsewhere (applyPreset strips excluded entries at line 1002-1007; savePreset at 1035-1039) but missed the commitDesiredVolume path. NOTE/correction to first tester: the claim that the URL command is the ONLY path reaching commitDesiredVolume for an excluded app is inaccurate — increaseVolumeForFrontmostApp/decreaseVolumeForFrontmostApp (lines 1218-1219, 1237-1238) also call setDesiredVolume+commitDesiredVolume and would re-tap a frontmost excluded app via keyboard shortcuts. This does not change the URL-area bug; it confirms the root cause is the missing guard in commitDesiredVolume.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:384",
          "expectedVsActual": "Expected (exclusion invariant, setExcluded doc at AppStore.swift:682-685): an excluded app is 'never tapped, audio left completely untouched', so set-volume targeting it should be a true no-op. Actual: setDesiredVolume no-ops but the paired commitDesiredVolume re-taps the excluded app through the backend, applies a stale volume, and reports a false 'Managed route active' success.",
          "suggestedFix": "Add `guard !isExcluded(app) else { return }` at the top of commitDesiredVolume (AppStore.swift:384) — this also fixes the keyboard-shortcut variant. Optionally, have handleURLSetVolume check store.isExcluded(app) before applying and show a 'URL command blocked — App is excluded from Waves' warning toast.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:FU-EXCLUDED-COMMIT-GUARD",
          "uid": "URL-02#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-EXCLUDED-COMMIT-GUARD",
          "fixTier": "tier1"
        },
        {
          "severity": "low",
          "type": "ux",
          "title": "set-volume against an excluded app gives no 'excluded/blocked' caller feedback",
          "detail": "CONFIRMED. Independent of the re-tap defect, an automation caller targeting an excluded app receives no response distinguishing it from a real change. Today, due to the re-tap bug, it sees a misleading 'Managed route active' success; even after the re-tap is fixed (guard added to commitDesiredVolume) the command would become a silent no-op with no toast, unlike the 'invalid' and 'App not found' paths which both toast. This is a genuine but lower-priority consistency gap that the high-severity fix should address together.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:303",
          "expectedVsActual": "Expected: a clear, distinct response for excluded targets, matching the story's differentiated 'invalid' vs 'App not found' feedback. Actual: either a false success toast (current) or silence (post-guard).",
          "suggestedFix": "When the resolved app isExcluded, showToast(title: 'URL command blocked', detail: 'App is excluded from Waves.', kind: .warning) and return before touching volume.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier3:applied:FU-URL-EXCLUDED-FEEDBACK",
          "uid": "URL-02#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-URL-EXCLUDED-FEEDBACK",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "URL-03",
      "area": "URL Scheme Automation (opt-in)",
      "feature": "mute URL command",
      "priority": "P2",
      "userStory": "As an automation user, I want to mute or unmute a specific app via a waves://mute URL, so that I can silence or restore an app from a script or hotkey tool.",
      "expectedBehavior": [
        "waves://mute?app=APP_ID&muted=true is routed to handleURLMute when automation is enabled.",
        "Requires both an 'app' query item (<= 256 chars) and a 'muted' query item (<= 16 chars) that parses as a Swift Bool ('true'/'false'); otherwise rejected with 'URL command blocked — Mute command was invalid.' warning toast.",
        "Resolves the target app via first(matchingAppKey:) (id or logicalID).",
        "On a match, calls setMuted(shouldMute, for: app) to apply the mute state.",
        "If no app matches, shows 'URL command blocked — App not found: <appID prefix>' warning toast."
      ],
      "acceptanceCriteria": [
        "waves://mute?app=<validKey>&muted=true mutes that app; muted=false unmutes it.",
        "muted=1, muted=yes, muted=TRUE-with-extra, or missing muted/app param each yield no change and a 'Mute command was invalid.' toast (Bool() accepts only 'true'/'false').",
        "An over-length app or muted value is rejected as invalid.",
        "A valid command for an unknown app yields an 'App not found' toast and no change."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:280",
        "Sources/Waves/Stores/AppStore.swift:312",
        "Sources/Waves/Stores/AppStore.swift:322",
        "Sources/Waves/Stores/AppStore.swift:327",
        "Sources/Waves/Stores/AppStore.swift:491",
        "README.md:131"
      ],
      "implStatus": "implemented",
      "implNotes": "Mirrors set-volume validation cleanly. Same excluded-app blind spot: setMuted returns silently at isExcluded (AppStore.swift:492) for an excluded target, so a mute command against an excluded app reports nothing and does nothing. Bool() is strict ('true'/'false' only, case-insensitive), so common truthy spellings like 1/yes/on are treated as invalid — correct but worth documenting for automation authors.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/ux] mute URL command against an excluded app is a silent no-op with no caller feedback (Sources/Waves/Stores/AppStore.swift:327) — CONFIRMED. handleURLMute (AppStore.swift:322-327) resolves the still-present excluded app and calls setMuted(shouldMute, for: app); setMuted bails at `guard !isExcluded(app) else { return }` (line 492) before any state change or toast. Unlike set-volume this is genuinely inert (no re-tap, because setMuted's backend Task is inside the guarded function), but the caller gets zero feedback while every other rejection path (invalid / not-found) emits a warning toast. A valid, well-formed mute command against an excluded app is indistinguishable from success. | Fix: In handleURLMute, check store.isExcluded(app) after resolution and show a 'URL command blocked — App is excluded from Waves' warning toast instead of silently calling setMuted.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier3: FU-URL-EXCLUDED-FEEDBACK",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "ux",
          "title": "mute URL command against an excluded app is a silent no-op with no caller feedback",
          "detail": "CONFIRMED. handleURLMute (AppStore.swift:322-327) resolves the still-present excluded app and calls setMuted(shouldMute, for: app); setMuted bails at `guard !isExcluded(app) else { return }` (line 492) before any state change or toast. Unlike set-volume this is genuinely inert (no re-tap, because setMuted's backend Task is inside the guarded function), but the caller gets zero feedback while every other rejection path (invalid / not-found) emits a warning toast. A valid, well-formed mute command against an excluded app is indistinguishable from success.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:327",
          "expectedVsActual": "Expected: consistent feedback for a command that has no effect (the story provides distinct 'invalid' and 'App not found' toasts). Actual: no audio change AND no toast/log.",
          "suggestedFix": "In handleURLMute, check store.isExcluded(app) after resolution and show a 'URL command blocked — App is excluded from Waves' warning toast instead of silently calling setMuted.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier3:applied:FU-URL-EXCLUDED-FEEDBACK",
          "uid": "URL-03#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-URL-EXCLUDED-FEEDBACK",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "URL-04",
      "area": "URL Scheme Automation (opt-in)",
      "feature": "apply-preset and refresh URL commands",
      "priority": "P2",
      "userStory": "As an automation user, I want to apply a named preset or refresh the audio session via waves:// URLs, so that I can switch my whole mixer configuration or force a re-scan from an external trigger.",
      "expectedBehavior": [
        "waves://apply-preset?name=Focus is routed to handleURLApplyPreset; requires a 'name' query item <= 256 chars or it rejects with 'URL command blocked — Preset command was invalid.' warning toast.",
        "Preset lookup is case-insensitive (localizedCaseInsensitiveCompare) against the saved presets list.",
        "On a match it calls applyPreset(preset); if no preset matches it shows 'URL command blocked — Preset not found: <name prefix>' warning toast (name truncated to 64 chars).",
        "waves://refresh is routed directly to refresh() to re-scan the audio session, with no parameters required.",
        "Any waves:// host other than set-volume / mute / apply-preset / refresh logs 'URL scheme invoked with unknown host' and does nothing else."
      ],
      "acceptanceCriteria": [
        "waves://apply-preset?name=focus applies a preset literally named 'Focus' (case-insensitive match).",
        "A missing name param yields a 'Preset command was invalid.' toast; an unknown name yields a 'Preset not found' toast.",
        "waves://refresh triggers a session refresh equivalent to the Refresh command.",
        "An unrecognized host (e.g. waves://foo) produces no action and only a warning log.",
        "The successful host name is logged at info level (host is .public)."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:282",
        "Sources/Waves/Stores/AppStore.swift:284",
        "Sources/Waves/Stores/AppStore.swift:330",
        "Sources/Waves/Stores/AppStore.swift:337",
        "Sources/Waves/Stores/AppStore.swift:342",
        "Sources/Waves/Stores/AppStore.swift:286",
        "README.md:132"
      ],
      "implStatus": "implemented",
      "implNotes": "apply-preset and refresh are both fully wired. Note asymmetry: apply-preset and the not-found/invalid paths emit user-facing toasts, but refresh and the unknown-host path are silent (no toast), so an automation caller hitting a typo'd host gets zero on-screen feedback. applyPreset itself iterates apps and skips excluded ones, so a preset partially applies across non-excluded apps — reasonable but not surfaced to the caller.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "URL-05",
      "area": "URL Scheme Automation (opt-in)",
      "feature": "URL command rate limiting and routing/parse safety",
      "priority": "P2",
      "userStory": "As a user who has enabled automation, I want Waves to throttle and sanity-check incoming waves:// URLs, so that a flood of commands or a malformed/foreign URL cannot hammer my audio state or exhaust the command budget.",
      "expectedBehavior": [
        "AppDelegate registers a Get-URL Apple Event handler at launch (setupURLSchemeHandler) and also implements application(_:open:), both funneling into a private handleURLScheme that drops any URL whose scheme != 'waves' before forwarding to the store.",
        "The handler is removed on applicationWillTerminate (removeURLSchemeHandler).",
        "AppStore.handleURLScheme requires scheme == 'waves', a parseable URLComponents, and a non-nil host; malformed URLs are dropped WITHOUT charging the rate limit.",
        "A sliding-window rate limit allows at most maxURLSchemeRequests (10) well-formed commands per urlSchemeRequestWindow (60s); checkURLSchemeRateLimit prunes timestamps older than the window, rejects when count >= 10 (logging 'rate limit was exceeded'), else records the timestamp.",
        "The rate limit is charged only AFTER the URL is confirmed well-formed (has a valid host), so malformed-URL spam cannot exhaust the quota for legitimate commands.",
        "Input length caps (app <= 256, volume <= 32, muted <= 16, preset name <= 256) and value truncation in toasts (prefix 64) bound the impact of hostile payloads."
      ],
      "acceptanceCriteria": [
        "A non-waves:// URL delivered to the app is ignored at the AppDelegate layer and never reaches the store handler.",
        "After 10 valid waves:// commands within 60s, the 11th is rejected with a rate-limit warning log and no audio change; commands succeed again once older entries age out of the window.",
        "Sending many malformed waves:// URLs (no host, or unparseable) does not consume rate-limit slots, so a valid command sent afterward still executes.",
        "A waves:// URL with a missing host is dropped silently (no toast, no rate-limit charge).",
        "The Apple Event Get-URL handler is installed at finish-launching and removed at terminate."
      ],
      "codeRefs": [
        "Sources/Waves/App/WavesApp.swift:124",
        "Sources/Waves/App/WavesApp.swift:140",
        "Sources/Waves/App/WavesApp.swift:191",
        "Sources/Waves/App/WavesApp.swift:197",
        "Sources/Waves/Stores/AppStore.swift:262",
        "Sources/Waves/Stores/AppStore.swift:270",
        "Sources/Waves/Stores/AppStore.swift:345",
        "Sources/Waves/Stores/AppStore.swift:63"
      ],
      "implStatus": "implemented",
      "implNotes": "Thoughtful defense-in-depth: scheme check at two layers, well-formedness gate before the rate limiter, sliding-window throttle, and input length caps. The deliberate ordering (charge rate limit only for well-formed commands) is documented in a comment and is the correct choice. Gaps: (1) the rate limit is a single global counter, so legitimate bursty automation hitting >10 cmds/min is silently dropped with only a log and no toast — the caller has no feedback that throttling occurred. (2) No automated tests exercise handleURLScheme parsing/rate-limit logic (only the enableURLScheme persistence is tested), so this security-relevant path is unverified by the suite. (3) Commands execute on whatever AudioApp keys currently exist; there is no confirmation prompt, so once enabled any local process can drive volume/mute within the rate budget — acceptable for an opt-in local-automation feature but the trust boundary rests entirely on the opt-in toggle.",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [1M 2L]",
      "errorsFound": "• [MEDIUM/edge-case] Empty-host URL (waves://) passes the well-formedness guard and CHARGES the rate limit, contradicting the 'missing host dropped without charge' criterion (Sources/Waves/Stores/AppStore.swift:264) — CONFIRMED empirically and by code. The guard `let host = components.host` (AppStore.swift:264) only fails on nil. I ran the parse: URLComponents(url: URL(string:\"waves://\")!, resolvingAgainstBaseURL:false)?.host == \"\" (empty string, NOT nil), and 'waves:///set-volume' likewise yields host==\"\"; only 'waves:' (no //) yields host==nil. So 'waves://' binds host=\"\", passes the gate, reaches checkURLSchemeRateLimit() (consuming a sliding-window slot at lines 270/345-353), then falls into the `switch host` default branch (logged as unknown host, no action). Flooding 10x 'waves://' within 60s exhausts the quota so the 11th legitimate command is rejected. Requires automation already enabled (opt-in), bounding real-world impact, but it directly violates a written acceptance criterion. Medium is appropriate. | Fix: Reject empty hosts before charging the rate limit: `guard url.scheme == \"waves\", let components = URLComponents(url: url, resolvingAgainstBaseURL: false), let host = components.host, !host.isEmpty else { return }`.\n• [LOW/dead-code] application(_:open:) is likely shadowed by the manual kAEGetURL Apple Event handler (Sources/Waves/App/WavesApp.swift:191) — CONFIRMED as likely. AppDelegate implements both application(_:open:) (WavesApp.swift:191) and installs its own kAEGetURL handler via NSAppleEventManager.setEventHandler in setupURLSchemeHandler (WavesApp.swift:124), called from applicationDidFinishLaunching (line 89). Registering a custom kAEGetURL handler after AppKit finishes launching replaces NSApp's own handler that normally drives application(_:open:), so for waves:// delivery the manual handleGetURLEvent path (line 140) fires and application(_:open:) is effectively unreachable. Both funnel into the same private handleURLScheme (line 197) which forwards to store.handleURLScheme, so correctness is unaffected — application(_:open:) is redundant/unexercised for the registered scheme. Exact dispatch depends on AppKit registration timing, hence 'likely'. | Fix: Pick one mechanism: drop the manual setEventHandler/removeEventHandler and rely on application(_:open:), or keep the manual handler and remove application(_:open:). If both are intentionally retained, comment that the manual handler is authoritative and application(_:open:) is a fallback.\n• [LOW/error-handling] Rate-limited and malformed/empty-host commands give the automation caller no on-screen feedback (Sources/Waves/Stores/AppStore.swift:270) — ADDED (completeness). When checkURLSchemeRateLimit() returns false the command is dropped with only logger.warning (AppStore.swift:270-273) — no toast — so legitimate bursty automation exceeding 10 cmds/min is silently throttled with zero caller feedback. Likewise an empty-host or unknown-host URL only logs. The story itself flags 'no toast that throttling occurred' as a gap, and combined with the URL-05 empty-host rate-charge bug, a caller can have legitimate commands silently dropped with no way to tell why. Low severity (opt-in, log exists) but a real UX/observability gap for the automation surface. | Fix: Emit a throttled warning toast on rate-limit rejection (e.g. 'URL command throttled — too many commands, try again shortly'), debounced so the toast itself can't be spammed.",
      "fixStatus": "3 fixed",
      "fixNotes": "tier2: FU-URL-EMPTY-HOST | tier3: FU-URL-FEEDBACK-WORDING; FU-URL-REDUNDANT-OPEN",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "edge-case",
          "title": "Empty-host URL (waves://) passes the well-formedness guard and CHARGES the rate limit, contradicting the 'missing host dropped without charge' criterion",
          "detail": "CONFIRMED empirically and by code. The guard `let host = components.host` (AppStore.swift:264) only fails on nil. I ran the parse: URLComponents(url: URL(string:\"waves://\")!, resolvingAgainstBaseURL:false)?.host == \"\" (empty string, NOT nil), and 'waves:///set-volume' likewise yields host==\"\"; only 'waves:' (no //) yields host==nil. So 'waves://' binds host=\"\", passes the gate, reaches checkURLSchemeRateLimit() (consuming a sliding-window slot at lines 270/345-353), then falls into the `switch host` default branch (logged as unknown host, no action). Flooding 10x 'waves://' within 60s exhausts the quota so the 11th legitimate command is rejected. Requires automation already enabled (opt-in), bounding real-world impact, but it directly violates a written acceptance criterion. Medium is appropriate.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:264",
          "expectedVsActual": "Expected (URL-05 acceptance): 'A waves:// URL with a missing host is dropped silently (no toast, no rate-limit charge)' and malformed/no-host spam does not consume rate-limit slots. Actual: an empty host (waves://) passes the guard and DOES consume a rate-limit slot.",
          "suggestedFix": "Reject empty hosts before charging the rate limit: `guard url.scheme == \"waves\", let components = URLComponents(url: url, resolvingAgainstBaseURL: false), let host = components.host, !host.isEmpty else { return }`.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier2:applied:FU-URL-EMPTY-HOST",
          "uid": "URL-05#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-URL-EMPTY-HOST",
          "fixTier": "tier2"
        },
        {
          "severity": "low",
          "type": "dead-code",
          "title": "application(_:open:) is likely shadowed by the manual kAEGetURL Apple Event handler",
          "detail": "CONFIRMED as likely. AppDelegate implements both application(_:open:) (WavesApp.swift:191) and installs its own kAEGetURL handler via NSAppleEventManager.setEventHandler in setupURLSchemeHandler (WavesApp.swift:124), called from applicationDidFinishLaunching (line 89). Registering a custom kAEGetURL handler after AppKit finishes launching replaces NSApp's own handler that normally drives application(_:open:), so for waves:// delivery the manual handleGetURLEvent path (line 140) fires and application(_:open:) is effectively unreachable. Both funnel into the same private handleURLScheme (line 197) which forwards to store.handleURLScheme, so correctness is unaffected — application(_:open:) is redundant/unexercised for the registered scheme. Exact dispatch depends on AppKit registration timing, hence 'likely'.",
          "codeRef": "Sources/Waves/App/WavesApp.swift:191",
          "expectedVsActual": "Expected (per story): both AppDelegate entry points funnel into handleURLScheme as defense-in-depth. Actual: the manual Apple Event handler supersedes NSApp's, making application(_:open:) an unreachable redundant path for waves:// delivery.",
          "suggestedFix": "Pick one mechanism: drop the manual setEventHandler/removeEventHandler and rely on application(_:open:), or keep the manual handler and remove application(_:open:). If both are intentionally retained, comment that the manual handler is authoritative and application(_:open:) is a fallback.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/App/WavesApp.swift",
          "disposition": "tier3:applied:FU-URL-REDUNDANT-OPEN",
          "uid": "URL-05#2",
          "canonicalFile": "Sources/Waves/App/WavesApp.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-URL-REDUNDANT-OPEN",
          "fixTier": "tier3"
        },
        {
          "severity": "low",
          "type": "error-handling",
          "title": "Rate-limited and malformed/empty-host commands give the automation caller no on-screen feedback",
          "detail": "ADDED (completeness). When checkURLSchemeRateLimit() returns false the command is dropped with only logger.warning (AppStore.swift:270-273) — no toast — so legitimate bursty automation exceeding 10 cmds/min is silently throttled with zero caller feedback. Likewise an empty-host or unknown-host URL only logs. The story itself flags 'no toast that throttling occurred' as a gap, and combined with the URL-05 empty-host rate-charge bug, a caller can have legitimate commands silently dropped with no way to tell why. Low severity (opt-in, log exists) but a real UX/observability gap for the automation surface.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:270",
          "expectedVsActual": "Expected: an automation caller can tell when a command was throttled or dropped (the other rejection paths — invalid/not-found — all toast). Actual: rate-limit rejection and malformed/unknown-host drops are log-only and invisible in-app.",
          "suggestedFix": "Emit a throttled warning toast on rate-limit rejection (e.g. 'URL command throttled — too many commands, try again shortly'), debounced so the toast itself can't be spammed.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier3:applied:FU-URL-FEEDBACK-WORDING",
          "uid": "URL-05#3",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-URL-FEEDBACK-WORDING",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "PAUSE-01",
      "area": "Auto-Pause Music when conferencing active",
      "feature": "Auto-pause media apps when a conferencing app becomes frontmost",
      "priority": "P1",
      "userStory": "As someone who keeps music playing while I work, I want Waves to automatically mute my media apps the moment I jump into a video call, so that I don't blast Spotify into a meeting and have to scramble for the mute button.",
      "expectedBehavior": [
        "A frontmost-application-change observer (NSWorkspace.didActivateApplicationNotification) fires checkAutoPauseMusic() whenever the active app changes (AppStore.swift:886-897).",
        "checkAutoPauseMusic() reads the LIVE frontmost app (NSWorkspace.shared.frontmostApplication) rather than the periodically-refreshed session snapshot, then classifies it via AppDiscoveryPolicy.inferCategory (AppStore.swift:945-953).",
        "Conferencing is detected purely by keyword match on bundleID/displayName: 'zoom', 'meet', 'teams', 'webex', or 'facetime' (AppDiscoveryPolicy.swift:38-42).",
        "When a conferencing app is frontmost, all visible apps with category == .media that are currently unmuted and not excluded are muted via backend.setMuted(true,...) (AppStore.swift:957-965).",
        "Each auto-muted app is tagged muteSource = .autoConferencing and its logicalID added to the pausedMusicApps set (AppStore.swift:963-964).",
        "Affected apps' isMuted/muteSource/appliedVolume are updated in place (not by replacing the whole snapshot) and the session is persisted (AppStore.swift:986-997).",
        "An early guard returns if the frontmost bundle id equals previousFrontmostApp, so re-activating the same conferencing app does not re-run the pause loop (AppStore.swift:947-948).",
        "checkAutoPauseMusic also runs on startup, on refresh, and after applying a preset (AppStore.swift:219, 248, 1014)."
      ],
      "acceptanceCriteria": [
        "Given the toggle is on and Spotify (category .media) is playing unmuted, when Zoom becomes the frontmost app, Spotify is muted and its muteSource becomes .autoConferencing.",
        "Given an app is already muted by the user, when a conferencing app activates, that app's muteSource stays .user (it is excluded by the !isMuted filter) and is not re-touched.",
        "Given an app is in the exclusion list, when a conferencing app activates, that app is never muted (isExcluded filter).",
        "Given Zoom is already frontmost and re-activates, when the notification fires again with the same bundle id, checkAutoPauseMusic returns early without re-muting.",
        "A media app whose category is correctly .media is the only kind of app paused; conferencing/communication/browser apps are left untouched."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:940-999",
        "Sources/Waves/Stores/AppStore.swift:886-897",
        "Sources/Waves/Stores/AppStore.swift:196",
        "Sources/WavesAudioCore/Backend/AppDiscoveryPolicy.swift:27-61"
      ],
      "implStatus": "partial",
      "implNotes": "Core path is wired and works for native conferencing apps. Two real detection gaps: (1) browser-based calls (Google Meet, Teams-in-browser, Zoom web client) make the frontmost app Chrome/Safari, which infers .browser, so the call is never detected and music is never paused. (2) Detection is keyword-only and frontmost-only — it never inspects whether the mic is actually live, so simply clicking a Zoom window (no active call) pauses music, and a call running in a background window while another app is frontmost is not detected. There is no UI feedback (toast/badge) telling the user Waves auto-paused something, so a silenced media app can look like a bug.",
      "source": "phase1-story",
      "testStatus": "5 issue(s) [2H 2M 1L]",
      "errorsFound": "• [HIGH/logic] previousFrontmostApp guard makes the refresh/startup/preset re-check paths no-ops while a conferencing app stays frontmost (Sources/Waves/Stores/AppStore.swift:947) — checkAutoPauseMusic() returns early at the guard currentFrontmostApp != previousFrontmostApp (AppStore.swift:947) before evaluating any pause/resume work. The function is also invoked from startup (line 219), refresh (line 248) and applyPreset (line 1014), but those paths do not change the frontmost app, so once previousFrontmostApp has latched the live conferencing bundle id the guard short-circuits every such call. Concrete trigger: already on a Zoom call (Zoom frontmost, music paused), launch Spotify and start playing; app discovery fires refresh() -> checkAutoPauseMusic() -> guard hits (Zoom == Zoom) -> returns, so the newly playing Spotify is never auto-paused and music blasts into the meeting. Same for applyPreset() unmuting a media app mid-call. Verified the guard is keyed purely on frontmost-bundle equality with no state-delta fallback. | Fix: Restrict the previousFrontmostApp dedupe to the didActivateApplication path only. For refresh/startup/applyPreset, call a variant that skips the dedupe, or replace the guard with a desired-state diff so re-entrant calls with the same frontmost still pause newly-eligible media apps.\n• [HIGH/edge-case] Browser-based calls (Google Meet, Zoom web, Teams-in-browser) are never detected, so music is never paused (Sources/WavesAudioCore/Backend/AppDiscoveryPolicy.swift:30-42) — inferCategory classifies the frontmost app: the browser branch (AppDiscoveryPolicy.swift:30-36) matches safari/chrome/firefox/arc/browser and returns .browser BEFORE the conferencing branch (lines 38-42) is reached. A call running in Chrome/Safari/Arc makes the frontmost app the browser, classified .browser, so isConferencingAppActive stays false and music keeps playing during the meeting. Confirmed the branch ordering and that classification is on the browser app's own name/bundle, not the tab. This is a documented design limitation (frontmost+keyword model), but it silently defeats a very common class of calls. | Fix: Detect active microphone/CoreAudio input use (or a known meeting URL) rather than equating frontmost-app category with call state. At minimum document the browser-call limitation in the Settings copy.\n• [MEDIUM/ux] No feedback when Waves auto-pauses a media app — silenced app looks like a bug (Sources/Waves/Stores/AppStore.swift:957-998) — The pause branch only logs (logger.info at AppStore.swift:965) and writes mute state; it never calls showToast or sets any badge. Other mutations in this store surface a toast (mute toggle at 528-533, preset at 1015-1020). A user who hears Spotify go silent on entering a call gets no in-app indication Waves did it; the muted row is indistinguishable from an unexpected mute. Confirmed there is no .autoConferencing-specific UI affordance anywhere. | Fix: Show a transient toast ('Auto-paused Spotify for your call') with a 'Keep playing' / 'Disable' affordance, and/or render a distinct badge for muteSource == .autoConferencing rows.\n• [MEDIUM/edge-case] Pause triggers on mere window focus, not an actual live call (Sources/Waves/Stores/AppStore.swift:945-965) — isConferencingAppActive is exactly frontmostCategory == .conferencing (AppStore.swift:953); there is no check that a call/mic is active. Opening the Zoom/Teams app to read a chat or check the schedule with no meeting makes it frontmost -> all unmuted .media apps are muted even though nothing is being recorded. Confirmed against inferCategory and the absence of any input-activity gate. | Fix: Gate pausing on actual microphone/recording activity in addition to the frontmost-app heuristic.\n• [LOW/concurrency] Guard updated synchronously but pause/resume run in detached Tasks, allowing interleaved pause+resume on rapid app switches (Sources/Waves/Stores/AppStore.swift:948-998) — checkAutoPauseMusic sets previousFrontmostApp synchronously (AppStore.swift:948) then spawns a Task (line 955) whose body awaits backend.setMuted. Rapidly switching e.g. media->Zoom->Finder fires two notifications: the first spawns a pause Task, the second (guard passes, prev now Zoom != Finder) spawns a resume Task. Both bodies are MainActor-isolated but suspend at each await, so the relative ordering of pausedMusicApps.insert (line 963) versus pausedMusicApps.removeAll() (line 983) and of the two backend.setMuted calls is not deterministic, leaving pausedMusicApps potentially inconsistent with muteSource. Impact is bounded because resume reads muteSource, not the set, but the final audible mute state can reflect whichever task's setMuted completed last. | Fix: Serialize passes: retain the in-flight Task and cancel/await it before starting a new one, so each frontmost change deterministically supersedes the previous.",
      "fixStatus": "1 fixed · 3 deferred · 1 n/a",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "logic",
          "title": "previousFrontmostApp guard makes the refresh/startup/preset re-check paths no-ops while a conferencing app stays frontmost",
          "detail": "checkAutoPauseMusic() returns early at the guard currentFrontmostApp != previousFrontmostApp (AppStore.swift:947) before evaluating any pause/resume work. The function is also invoked from startup (line 219), refresh (line 248) and applyPreset (line 1014), but those paths do not change the frontmost app, so once previousFrontmostApp has latched the live conferencing bundle id the guard short-circuits every such call. Concrete trigger: already on a Zoom call (Zoom frontmost, music paused), launch Spotify and start playing; app discovery fires refresh() -> checkAutoPauseMusic() -> guard hits (Zoom == Zoom) -> returns, so the newly playing Spotify is never auto-paused and music blasts into the meeting. Same for applyPreset() unmuting a media app mid-call. Verified the guard is keyed purely on frontmost-bundle equality with no state-delta fallback.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:947",
          "expectedVsActual": "Expected: checkAutoPauseMusic invoked from refresh/startup/preset re-evaluates conferencing state and pauses newly-eligible media apps. Actual: the same-frontmost early-return makes those calls no-ops whenever the frontmost app has not changed (the normal case during a call).",
          "suggestedFix": "Restrict the previousFrontmostApp dedupe to the didActivateApplication path only. For refresh/startup/applyPreset, call a variant that skips the dedupe, or replace the guard with a desired-state diff so re-entrant calls with the same frontmost still pause newly-eligible media apps.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-feature:Robust mid-call detection independent of frontmost focus needs a new capability (mic/input-in-use probe) and a design decision on pause/resume triggers.",
          "uid": "PAUSE-01#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "high",
          "type": "edge-case",
          "title": "Browser-based calls (Google Meet, Zoom web, Teams-in-browser) are never detected, so music is never paused",
          "detail": "inferCategory classifies the frontmost app: the browser branch (AppDiscoveryPolicy.swift:30-36) matches safari/chrome/firefox/arc/browser and returns .browser BEFORE the conferencing branch (lines 38-42) is reached. A call running in Chrome/Safari/Arc makes the frontmost app the browser, classified .browser, so isConferencingAppActive stays false and music keeps playing during the meeting. Confirmed the branch ordering and that classification is on the browser app's own name/bundle, not the tab. This is a documented design limitation (frontmost+keyword model), but it silently defeats a very common class of calls.",
          "codeRef": "Sources/WavesAudioCore/Backend/AppDiscoveryPolicy.swift:30-42",
          "expectedVsActual": "Expected (story): when I jump into a video call, media is muted. Actual: browser-hosted Meet/Zoom/Teams are silently undetected; the feature only works for native conferencing apps.",
          "suggestedFix": "Detect active microphone/CoreAudio input use (or a known meeting URL) rather than equating frontmost-app category with call state. At minimum document the browser-call limitation in the Settings copy.",
          "confidence": "confirmed",
          "primaryFile": "Sources/WavesAudioCore/Backend/AppDiscoveryPolicy.swift",
          "disposition": "deferred:needs-feature:New permission-sensitive detection capability; not validatable by reading code.",
          "uid": "PAUSE-01#2",
          "canonicalFile": "Sources/WavesAudioCore/Backend/AppDiscoveryPolicy.swift"
        },
        {
          "severity": "medium",
          "type": "ux",
          "title": "No feedback when Waves auto-pauses a media app — silenced app looks like a bug",
          "detail": "The pause branch only logs (logger.info at AppStore.swift:965) and writes mute state; it never calls showToast or sets any badge. Other mutations in this store surface a toast (mute toggle at 528-533, preset at 1015-1020). A user who hears Spotify go silent on entering a call gets no in-app indication Waves did it; the muted row is indistinguishable from an unexpected mute. Confirmed there is no .autoConferencing-specific UI affordance anywhere.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:957-998",
          "expectedVsActual": "Expected (good UX for an automatic, surprising action): the user is told Waves auto-paused N apps and how to undo. Actual: silent mutation; the muted row carries no provenance hint.",
          "suggestedFix": "Show a transient toast ('Auto-paused Spotify for your call') with a 'Keep playing' / 'Disable' affordance, and/or render a distinct badge for muteSource == .autoConferencing rows.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "gapfill:fixed",
          "uid": "PAUSE-01#3",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "gapfill",
          "fixTier": "gapfill"
        },
        {
          "severity": "medium",
          "type": "edge-case",
          "title": "Pause triggers on mere window focus, not an actual live call",
          "detail": "isConferencingAppActive is exactly frontmostCategory == .conferencing (AppStore.swift:953); there is no check that a call/mic is active. Opening the Zoom/Teams app to read a chat or check the schedule with no meeting makes it frontmost -> all unmuted .media apps are muted even though nothing is being recorded. Confirmed against inferCategory and the absence of any input-activity gate.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:945-965",
          "expectedVsActual": "Expected (story intent): pause when I am in a video call. Actual: pauses whenever a conferencing app window is merely focused.",
          "suggestedFix": "Gate pausing on actual microphone/recording activity in addition to the frontmost-app heuristic.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-hardware:Mic-activity gating needs Core Audio detection and on-device validation.",
          "uid": "PAUSE-01#4",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "low",
          "type": "concurrency",
          "title": "Guard updated synchronously but pause/resume run in detached Tasks, allowing interleaved pause+resume on rapid app switches",
          "detail": "checkAutoPauseMusic sets previousFrontmostApp synchronously (AppStore.swift:948) then spawns a Task (line 955) whose body awaits backend.setMuted. Rapidly switching e.g. media->Zoom->Finder fires two notifications: the first spawns a pause Task, the second (guard passes, prev now Zoom != Finder) spawns a resume Task. Both bodies are MainActor-isolated but suspend at each await, so the relative ordering of pausedMusicApps.insert (line 963) versus pausedMusicApps.removeAll() (line 983) and of the two backend.setMuted calls is not deterministic, leaving pausedMusicApps potentially inconsistent with muteSource. Impact is bounded because resume reads muteSource, not the set, but the final audible mute state can reflect whichever task's setMuted completed last.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:948-998",
          "expectedVsActual": "Expected: a later frontmost change cleanly supersedes an in-flight pass. Actual: overlapping detached tasks can produce out-of-order setMuted calls and a pausedMusicApps set that disagrees with muteSource.",
          "suggestedFix": "Serialize passes: retain the in-flight Task and cancel/await it before starting a new one, so each frontmost change deterministically supersedes the previous.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "PAUSE-01#5",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "PAUSE-02",
      "area": "Auto-Pause Music when conferencing active",
      "feature": "Auto-resume the apps Waves paused once the call ends",
      "priority": "P1",
      "userStory": "As a user whose music was auto-paused for a call, I want Waves to un-mute exactly the apps it paused once I leave the conferencing app, so that my music comes back on its own without restoring tracks I had intentionally muted.",
      "expectedBehavior": [
        "When the frontmost app is NOT conferencing, checkAutoPauseMusic resumes apps that are muted AND have muteSource == .autoConferencing AND are not excluded, via backend.setMuted(false,...) (AppStore.swift:970-982).",
        "Resume is intentionally scoped to .autoConferencing-tagged apps so a user's own mutes are never overridden (comment + filter at AppStore.swift:971-973).",
        "After resuming, the app's muteSource is reset to .user and pausedMusicApps is cleared entirely (AppStore.swift:977, 983).",
        "appliedVolume is restored to desiredVolume on unmute via the in-place muteChanges application (AppStore.swift:993).",
        "The same previousFrontmostApp early-return guard prevents repeated resume passes when the frontmost app hasn't changed (AppStore.swift:947-948)."
      ],
      "acceptanceCriteria": [
        "Given Waves auto-paused Spotify for a Zoom call, when the user switches away from Zoom to any non-conferencing app, Spotify is unmuted and its appliedVolume returns to desiredVolume.",
        "Given the user manually muted Apple Music before the call, when the call ends, Apple Music stays muted (muteSource .user is never resumed).",
        "After a resume pass, pausedMusicApps is empty.",
        "An excluded app is never unmuted by the resume pass even if it somehow carries an .autoConferencing tag."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:970-998",
        "Sources/WavesAudioCore/Models/AudioApp.swift:193-198"
      ],
      "implStatus": "partial",
      "implNotes": "Resume logic is correct for the simple case but has edge-case gaps. (1) On resume the app is retagged muteSource = .user (line 977) — semantically odd: an app Waves muted and Waves unmuted is now indistinguishable from a user action, which is only safe because pausedMusicApps is also cleared. (2) 'Not conferencing frontmost' is the resume trigger, so switching from Zoom to Finder mid-call (call still running) resumes music while you are still talking — the feature equates 'conferencing app not frontmost' with 'call over', which is frequently false. (3) Because detection keys off the frontmost-change notification, if you quit the conferencing app rather than switching away (didTerminate, not didActivate), resume depends on the next frontmost activation firing; there is no resume hook in handleAppTermination.",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [1H 1M 1L]",
      "errorsFound": "• [HIGH/logic] Resume equates 'conferencing app not frontmost' with 'call over' — music returns mid-call (Sources/Waves/Stores/AppStore.swift:970-982) — The resume branch runs whenever the live frontmost app is not conferencing (AppStore.swift:970-984). During an active call users routinely switch to notes/browser/Finder while still talking. The moment focus leaves the Zoom window, isConferencingAppActive becomes false and Waves unmutes every .autoConferencing app, so music resumes while the call is still live — re-creating the problem the feature exists to prevent. Confirmed the resume trigger is purely frontmost-category, with no call/mic-state check. | Fix: Decouple resume from frontmost focus: resume only when the call truly ends (conferencing app terminated, or mic/input no longer in use).\n• [MEDIUM/error-handling] No resume hook when the conferencing app is quit rather than switched away from (Sources/Waves/Stores/AppStore.swift:915-938) — Resume is driven solely by the didActivateApplication frontmost-change notification (observeFrontmostAppChanges, line 886-895). handleAppTermination (AppStore.swift:915-938) releases controllers and prunes pausedMusicApps but never calls checkAutoPauseMusic(). If the conferencing app quits/crashes and macOS does not promptly activate another app, no resume pass fires and music stays muted until the next manual app activation. In practice macOS usually activates another app on quit (which would fire didActivate and self-heal), so this is an edge case — hence 'likely' rather than always-reproducible. | Fix: In handleAppTermination, if the terminated app was conferencing or any .autoConferencing-muted apps remain, invoke a resume pass directly instead of waiting for the next activation.\n• [LOW/inconsistency] Resume retags muteSource to .user, erasing provenance of a Waves-applied auto-pause (Sources/Waves/Stores/AppStore.swift:977) — On resume, an app Waves muted and Waves unmuted is tagged muteSource = .user (AppStore.swift:977, applied at :992). This is only safe because pausedMusicApps is cleared in the same pass (line 983); the two sources of truth must stay in lockstep. The codebase itself notes this is harmless for an app being unmuted (its provenance reverts to the default .user). A code smell rather than a functional bug, but if the two representations ever diverge (e.g. via the PAUSE-01 interleaving) an app could appear user-controlled when it was machine-controlled. | Fix: Leave muteSource at a neutral value on auto-resume (or drop the duplicate pausedMusicApps set and derive paused state from muteSource alone so the two can never disagree).",
      "fixStatus": "1 fixed · 1 deferred · 1 n/a",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "logic",
          "title": "Resume equates 'conferencing app not frontmost' with 'call over' — music returns mid-call",
          "detail": "The resume branch runs whenever the live frontmost app is not conferencing (AppStore.swift:970-984). During an active call users routinely switch to notes/browser/Finder while still talking. The moment focus leaves the Zoom window, isConferencingAppActive becomes false and Waves unmutes every .autoConferencing app, so music resumes while the call is still live — re-creating the problem the feature exists to prevent. Confirmed the resume trigger is purely frontmost-category, with no call/mic-state check.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:970-982",
          "expectedVsActual": "Expected (story): un-mute once I leave the conferencing app / the call ends. Actual: un-mutes as soon as the conferencing app loses frontmost focus, frequently false for 'call over'.",
          "suggestedFix": "Decouple resume from frontmost focus: resume only when the call truly ends (conferencing app terminated, or mic/input no longer in use).",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-feature:True call-end detection needs a new mic/input-in-use probe and a UX decision; changing the resume trigger risks broad conferencing regressions.",
          "uid": "PAUSE-02#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "medium",
          "type": "error-handling",
          "title": "No resume hook when the conferencing app is quit rather than switched away from",
          "detail": "Resume is driven solely by the didActivateApplication frontmost-change notification (observeFrontmostAppChanges, line 886-895). handleAppTermination (AppStore.swift:915-938) releases controllers and prunes pausedMusicApps but never calls checkAutoPauseMusic(). If the conferencing app quits/crashes and macOS does not promptly activate another app, no resume pass fires and music stays muted until the next manual app activation. In practice macOS usually activates another app on quit (which would fire didActivate and self-heal), so this is an edge case — hence 'likely' rather than always-reproducible.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:915-938",
          "expectedVsActual": "Expected: quitting the call app brings music back. Actual: resume depends on a subsequent didActivateApplication; if none fires, auto-paused apps remain silent.",
          "suggestedFix": "In handleAppTermination, if the terminated app was conferencing or any .autoConferencing-muted apps remain, invoke a resume pass directly instead of waiting for the next activation.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "gapfill:fixed",
          "uid": "PAUSE-02#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "gapfill",
          "fixTier": "gapfill"
        },
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "Resume retags muteSource to .user, erasing provenance of a Waves-applied auto-pause",
          "detail": "On resume, an app Waves muted and Waves unmuted is tagged muteSource = .user (AppStore.swift:977, applied at :992). This is only safe because pausedMusicApps is cleared in the same pass (line 983); the two sources of truth must stay in lockstep. The codebase itself notes this is harmless for an app being unmuted (its provenance reverts to the default .user). A code smell rather than a functional bug, but if the two representations ever diverge (e.g. via the PAUSE-01 interleaving) an app could appear user-controlled when it was machine-controlled.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:977",
          "expectedVsActual": "Expected: resume returns the app to a neutral/unmuted state without falsely attributing the change to the user. Actual: it stamps .user, conflating automatic and manual provenance.",
          "suggestedFix": "Leave muteSource at a neutral value on auto-resume (or drop the duplicate pausedMusicApps set and derive paused state from muteSource alone so the two can never disagree).",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "PAUSE-02#3",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "PAUSE-03",
      "area": "Auto-Pause Music when conferencing active",
      "feature": "Enable/disable the auto-pause behavior from Settings",
      "priority": "P2",
      "userStory": "As a user who sometimes wants music to keep playing during calls, I want a single settings toggle to turn auto-pause on or off, so that I stay in control of whether Waves ever silences my media apps.",
      "expectedBehavior": [
        "Settings exposes a 'Auto-pause music during calls' toggle bound to preferences.autoPauseMusicForConferencing (SettingsView.swift:88-96).",
        "Changing the toggle immediately persists preferences via store.persistPreferences() (SettingsView.swift:93-94).",
        "The preference defaults to true (UserPreferences.swift:10) and is decoded defensively so older preference files missing the key still load (UserPreferences.swift:46-47, 55).",
        "checkAutoPauseMusic guards on the preference and returns immediately when it is false, so no muting/resuming occurs while disabled (AppStore.swift:941)."
      ],
      "acceptanceCriteria": [
        "Given the toggle is off, when a conferencing app becomes frontmost, no media app is muted (checkAutoPauseMusic returns at the guard).",
        "When the toggle is changed, the new value is written to the preferences store immediately (no app restart required).",
        "A preferences file written before this key existed loads with autoPauseMusicForConferencing == true (default).",
        "The toggle reflects the currently persisted value when Settings is opened."
      ],
      "codeRefs": [
        "Sources/Waves/Settings/SettingsView.swift:88-96",
        "Sources/Waves/Stores/UserPreferences.swift:10",
        "Sources/Waves/Stores/UserPreferences.swift:29",
        "Sources/Waves/Stores/UserPreferences.swift:55",
        "Sources/Waves/Stores/AppStore.swift:941"
      ],
      "implStatus": "partial",
      "implNotes": "Toggle is fully wired and persisted. The gap is the disable-mid-call case: turning the toggle OFF while music is currently auto-paused does NOT immediately resume anything — the setter only persists the value and never calls a resume pass, so any already-.autoConferencing-muted apps stay silent until the next frontmost change moves off the conferencing app. A user toggling it off to 'bring my music back now' will see nothing happen. The settings copy ('during calls') also overstates capability given detection is frontmost+keyword only, not actual call state.",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [2M 1L]",
      "errorsFound": "• [MEDIUM/ux] Turning the toggle OFF mid-call leaves auto-paused apps muted indefinitely with no resume path (Sources/Waves/Settings/SettingsView.swift:92-95) — The Settings toggle setter only writes preferences.autoPauseMusicForConferencing and calls persistPreferences() (SettingsView.swift:92-95); it never triggers a resume pass. Worse than 'lingers until you leave the call app': once the preference is off, checkAutoPauseMusic returns at its first guard (AppStore.swift:941) on EVERY subsequent frontmost change, so the resume branch (970-984) is never reached. The .autoConferencing-muted apps therefore stay silent indefinitely — the user must manually unmute each one. A user toggling the option off to 'bring my music back now' sees nothing happen and has no automatic way to recover. Verified the early guard precedes the resume branch. | Fix: In the toggle's set closure, when turning the preference off, run a one-shot resume of all muteSource == .autoConferencing apps (the resume-branch logic), clear pausedMusicApps, then persist.\n• [MEDIUM/edge-case] Auto-paused mute persists across relaunch and can re-mute (and stick) after the call is long over (Sources/Waves/Stores/AppStore.swift:820-844) — The session snapshot persists muteSource == .autoConferencing with isMuted == true (persistSessionSnapshot writes the full session). On relaunch, reapplyRestoredAudioState (AppStore.swift:824-844) re-applies isMuted=true for those apps, audibly re-muting them. The only resume path on launch is checkAutoPauseMusic() at line 219, which (a) requires the toggle on — if it is off, line 941 bails and the app stays muted forever; and (b) requires a non-conferencing app to be frontmost at that instant. If Waves restarts (update/crash/logout) hours after a call, a previously auto-paused media app comes back muted and, with the toggle off, never auto-resumes. This persistence/restore desync was missed by the first tester. | Fix: Do not persist .autoConferencing mutes as durable state (treat them as session-only and clear them on shutdown), or on launch always resume .autoConferencing-tagged apps that no conferencing app currently justifies, independent of the toggle state.\n• [LOW/inconsistency] Toggle copy 'Auto-pause music during calls' overstates capability (Sources/Waves/Settings/SettingsView.swift:88-89) — The label promises behavior keyed to actual calls (SettingsView.swift:88-89), but detection is frontmost-app + keyword only with no notion of an active call (see PAUSE-01). It misses browser calls and fires on mere window focus, so 'during calls' is misleading. Verified label text and detection model. | Fix: Reword to reflect the heuristic, e.g. 'Pause media when a video-call app is in front', with a help note that browser-based calls are unsupported.",
      "fixStatus": "2 fixed · 1 deferred",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "ux",
          "title": "Turning the toggle OFF mid-call leaves auto-paused apps muted indefinitely with no resume path",
          "detail": "The Settings toggle setter only writes preferences.autoPauseMusicForConferencing and calls persistPreferences() (SettingsView.swift:92-95); it never triggers a resume pass. Worse than 'lingers until you leave the call app': once the preference is off, checkAutoPauseMusic returns at its first guard (AppStore.swift:941) on EVERY subsequent frontmost change, so the resume branch (970-984) is never reached. The .autoConferencing-muted apps therefore stay silent indefinitely — the user must manually unmute each one. A user toggling the option off to 'bring my music back now' sees nothing happen and has no automatic way to recover. Verified the early guard precedes the resume branch.",
          "codeRef": "Sources/Waves/Settings/SettingsView.swift:92-95",
          "expectedVsActual": "Expected: disabling auto-pause stops future silencing AND un-does any active auto-pause so audio returns. Actual: the toggle only affects future passes; in-flight auto-mutes are stranded with no toggle-driven or focus-driven way to clear them.",
          "suggestedFix": "In the toggle's set closure, when turning the preference off, run a one-shot resume of all muteSource == .autoConferencing apps (the resume-branch logic), clear pausedMusicApps, then persist.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Settings/SettingsView.swift",
          "disposition": "deferred:needs-hardware:Correct fix needs a new async AppStore resume method awaiting backend.setMuted per stranded app plus a behavioral judgement about force-resuming apps; cross-fil",
          "uid": "PAUSE-03#1",
          "canonicalFile": "Sources/Waves/Settings/SettingsView.swift"
        },
        {
          "severity": "medium",
          "type": "edge-case",
          "title": "Auto-paused mute persists across relaunch and can re-mute (and stick) after the call is long over",
          "detail": "The session snapshot persists muteSource == .autoConferencing with isMuted == true (persistSessionSnapshot writes the full session). On relaunch, reapplyRestoredAudioState (AppStore.swift:824-844) re-applies isMuted=true for those apps, audibly re-muting them. The only resume path on launch is checkAutoPauseMusic() at line 219, which (a) requires the toggle on — if it is off, line 941 bails and the app stays muted forever; and (b) requires a non-conferencing app to be frontmost at that instant. If Waves restarts (update/crash/logout) hours after a call, a previously auto-paused media app comes back muted and, with the toggle off, never auto-resumes. This persistence/restore desync was missed by the first tester.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:820-844",
          "expectedVsActual": "Expected: a transient call-time auto-pause should not survive as a persistent mute the user can only clear by hand. Actual: .autoConferencing mute is persisted and re-applied on relaunch, and is unrecoverable via auto-resume when the toggle is off.",
          "suggestedFix": "Do not persist .autoConferencing mutes as durable state (treat them as session-only and clear them on shutdown), or on launch always resume .autoConferencing-tagged apps that no conferencing app currently justifies, independent of the toggle state.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "gapfill:fixed",
          "uid": "PAUSE-03#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "gapfill",
          "fixTier": "gapfill"
        },
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "Toggle copy 'Auto-pause music during calls' overstates capability",
          "detail": "The label promises behavior keyed to actual calls (SettingsView.swift:88-89), but detection is frontmost-app + keyword only with no notion of an active call (see PAUSE-01). It misses browser calls and fires on mere window focus, so 'during calls' is misleading. Verified label text and detection model.",
          "codeRef": "Sources/Waves/Settings/SettingsView.swift:88-89",
          "expectedVsActual": "Expected: copy matches behavior. Actual: copy implies call-state awareness the implementation lacks.",
          "suggestedFix": "Reword to reflect the heuristic, e.g. 'Pause media when a video-call app is in front', with a help note that browser-based calls are unsupported.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Settings/SettingsView.swift",
          "disposition": "iter3:implemented",
          "uid": "PAUSE-03#3",
          "canonicalFile": "Sources/Waves/Settings/SettingsView.swift",
          "fixStatus": "applied",
          "fixedBy": "iter3-ui-parity",
          "fixTier": "iter3"
        }
      ]
    },
    {
      "id": "PAUSE-04",
      "area": "Auto-Pause Music when conferencing active",
      "feature": "Respect user intent and app lifecycle during auto-pause",
      "priority": "P2",
      "userStory": "As a user, I want auto-pause to never fight my manual choices or get confused when apps quit, so that I can override Waves at any time and trust that stale paused-state never lingers.",
      "expectedBehavior": [
        "A direct user mute/unmute always sets muteSource = .user so the auto-resume pass will never override it (AppStore.swift:501).",
        "If the user manually unmutes an app Waves auto-paused, it is removed from pausedMusicApps so resume does not double-act (AppStore.swift:504-506).",
        "Excluding an app removes it from pausedMusicApps and resets its muteSource to .user, so an excluded app is dropped from auto-pause bookkeeping (AppStore.swift:707, 711).",
        "On refresh, pausedMusicApps is filtered to only ids still present in the session, and again filtered after a session merge, so quit apps don't leak into the set (AppStore.swift:478-479, 934-936).",
        "muteSource is persisted in the session snapshot (SessionStore.swift:78) and re-merged on launch (AppStore.swift:1403), with a legacy-decode default of .user (AudioApp.swift:139)."
      ],
      "acceptanceCriteria": [
        "Given Waves auto-paused an app, when the user manually unmutes it, it is removed from pausedMusicApps and a later resume pass does not re-toggle it.",
        "Given a user manually mutes an app during a call, that mute survives the call ending (muteSource .user not resumed).",
        "When an auto-paused app quits, its id is purged from pausedMusicApps on the next refresh/merge.",
        "An app moved to the exclusion list while auto-paused is removed from pausedMusicApps and no longer participates in resume.",
        "A session file written before muteSource existed decodes with muteSource == .user."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:491-506",
        "Sources/Waves/Stores/AppStore.swift:686-712",
        "Sources/Waves/Stores/AppStore.swift:478-479",
        "Sources/Waves/Stores/AppStore.swift:934-936",
        "Sources/WavesAudioCore/Models/AudioApp.swift:139",
        "Tests/WavesTests/AppDiscoveryPolicyTests.swift:62-72"
      ],
      "implStatus": "implemented",
      "implNotes": "This is the most carefully handled part of the feature: user-intent precedence, exclusion cleanup, and stale-id pruning are all consistent, and muteSource persistence has round-trip + legacy-default test coverage. Minor redundancy: pausedMusicApps duplicates information already carried by muteSource == .autoConferencing — resume reads muteSource, not the set, so the set is mostly bookkeeping and a potential source of drift if the two ever disagree. No correctness bug observed, but it is dead-weight state that could be derived from muteSource alone. There is no unit test exercising checkAutoPauseMusic itself (only the muteSource codec is tested).",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [2L]",
      "errorsFound": "• [LOW/dead-code] pausedMusicApps is redundant bookkeeping that duplicates muteSource == .autoConferencing and can drift (Sources/Waves/Stores/AppStore.swift:961-984) — Resume selects apps via the muteSource filter (AppStore.swift:973), not via pausedMusicApps. The set is only inserted to (963), pruned (479, 711, 934-936) and cleared (983); it carries no information not derivable from muteSource == .autoConferencing on session apps. Verified resume never reads the set. Its only risk is silently diverging from muteSource (e.g. under the PAUSE-01 interleaving) without affecting resume — a debugging hazard rather than a functional input. | Fix: Remove pausedMusicApps; derive the paused set on demand from session.apps.filter { $0.muteSource == .autoConferencing } and delete the prune/clear sites.\n• [LOW/edge-case] No unit test exercises checkAutoPauseMusic; only the muteSource codec is covered (Tests/WavesTests/AppDiscoveryPolicyTests.swift:62-72) — AppDiscoveryPolicyTests.swift covers only muteSource round-trip and legacy-decode default (the two @Test functions around lines 62-72). There is no test for pause/resume selection, user-intent precedence, exclusion cleanup, the previousFrontmostApp guard, or stale-id pruning — none of the acceptance criteria for this stateful behavior are guarded, so the PAUSE-01/02/03 regressions can land undetected. Confirmed via grep: the only test file referencing this feature is AppDiscoveryPolicyTests, and it touches only the codec. | Fix: Extract a pure helper from checkAutoPauseMusic and test: user-muted app skipped on pause, excluded app skipped, resume restricted to .autoConferencing, manual unmute removing from the paused set, and stale-id pruning.",
      "fixStatus": "1 deferred · 1 n/a",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "dead-code",
          "title": "pausedMusicApps is redundant bookkeeping that duplicates muteSource == .autoConferencing and can drift",
          "detail": "Resume selects apps via the muteSource filter (AppStore.swift:973), not via pausedMusicApps. The set is only inserted to (963), pruned (479, 711, 934-936) and cleared (983); it carries no information not derivable from muteSource == .autoConferencing on session apps. Verified resume never reads the set. Its only risk is silently diverging from muteSource (e.g. under the PAUSE-01 interleaving) without affecting resume — a debugging hazard rather than a functional input.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:961-984",
          "expectedVsActual": "Expected: a single source of truth for which apps Waves paused. Actual: two parallel representations where only muteSource is consulted for resume.",
          "suggestedFix": "Remove pausedMusicApps; derive the paused set on demand from session.apps.filter { $0.muteSource == .autoConferencing } and delete the prune/clear sites.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "PAUSE-04#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "low",
          "type": "edge-case",
          "title": "No unit test exercises checkAutoPauseMusic; only the muteSource codec is covered",
          "detail": "AppDiscoveryPolicyTests.swift covers only muteSource round-trip and legacy-decode default (the two @Test functions around lines 62-72). There is no test for pause/resume selection, user-intent precedence, exclusion cleanup, the previousFrontmostApp guard, or stale-id pruning — none of the acceptance criteria for this stateful behavior are guarded, so the PAUSE-01/02/03 regressions can land undetected. Confirmed via grep: the only test file referencing this feature is AppDiscoveryPolicyTests, and it touches only the codec.",
          "codeRef": "Tests/WavesTests/AppDiscoveryPolicyTests.swift:62-72",
          "expectedVsActual": "Expected: the intent/lifecycle logic is regression-tested. Actual: only muteSource serialization is tested; the decision logic is untested.",
          "suggestedFix": "Extract a pure helper from checkAutoPauseMusic and test: user-muted app skipped on pause, excluded app skipped, resume restricted to .autoConferencing, manual unmute removing from the paused set, and stale-id pruning.",
          "confidence": "confirmed",
          "primaryFile": "Tests/WavesTests/AppDiscoveryPolicyTests.swift",
          "disposition": "deferred:needs-feature:Test needs a production refactor in AppStore (not allowed) that could change behavior.",
          "uid": "PAUSE-04#2",
          "canonicalFile": "Tests/WavesTests/AppDiscoveryPolicyTests.swift"
        }
      ]
    },
    {
      "id": "EXCL-01",
      "area": "Exclude/Ignore Apps (escape hatch)",
      "feature": "Exclude an app from Waves management",
      "priority": "P1",
      "userStory": "As a user running a DAW, VoIP/echo-cancellation app, or other tap-sensitive audio tool, I want to exclude that app from Waves in one click, so that Waves never taps or alters its audio and the app keeps working normally.",
      "expectedBehavior": [
        "Each mixer row's context menu shows 'Exclude from Waves' (or 'Manage with Waves' when already excluded) as the last item below a divider (MixerRowView.swift:158-161).",
        "Selecting it calls store.setExcluded(!isExcluded, for: app), which inserts/removes the app's logicalID in preferences.excludedAppIDs (a sorted, de-duplicated set) and persists preferences immediately (AppStore.swift:686-694).",
        "On exclude, Waves tears down any active route: it calls backend.releaseControllers(forBundleID:pid:) and locally resets the row to routingState=.monitorOnly, appliedVolume=nil, isMuted=false, muteSource=.user (AppStore.swift:696-708).",
        "On exclude, any pending volume-apply task is cancelled, the pending volume target is cleared, and the app is removed from the auto-paused-music set (AppStore.swift:709-711).",
        "A confirmation toast ('Excluded from Waves' / 'Managed by Waves' with the app name) appears for 1.4s (AppStore.swift:714-719).",
        "The same toggle is exposed as a VoiceOver custom action named 'Exclude from Waves' / 'Manage with Waves' (MixerRowView.swift:166-168)."
      ],
      "acceptanceCriteria": [
        "After choosing 'Exclude from Waves', the app's logicalID is present in preferences.excludedAppIDs and the preferences file on disk reflects it.",
        "After exclusion, backend.releaseControllers is invoked and the row's routingState becomes .monitorOnly with appliedVolume nil and isMuted false.",
        "Choosing 'Manage with Waves' on an excluded app removes its logicalID from excludedAppIDs.",
        "excludedAppIDs round-trips through encode/decode preserving entries (covered by UserPreferencesTests).",
        "A toast is shown reflecting the new state."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:686-720",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:158-168",
        "Sources/Waves/Stores/UserPreferences.swift:15-18",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1104-1124"
      ],
      "implStatus": "implemented",
      "implNotes": "Core flow is fully wired and persisted. Edge case: setExcluded passes pid = app.pid ?? -1 to releaseControllers, and the backend matches by (bundleID == bundleID) OR (pid == pid). If a row has no bundleID and no pid, the -1 sentinel matches nothing, so teardown is a no-op (the app would simply re-show monitorOnly state, which is acceptable). Secondary concern: companion-audio processes get a pid-suffixed logicalID (bundleID::name::pid-N) from AppDiscoveryPolicy.logicalAppID, so excluding such a process stores a pid-bound key that will NOT match after a relaunch — exclusion of helper/renderer-type processes does not persist across restarts. Normal apps (logicalID == bundleID) persist correctly.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "EXCL-02",
      "area": "Exclude/Ignore Apps (escape hatch)",
      "feature": "Excluded apps are never tapped or altered by any control path",
      "priority": "P0",
      "userStory": "As a user who excluded a sensitive app, I want every Waves control and automation to leave that app completely untouched, so that I can trust the exclusion as a real escape hatch rather than a cosmetic flag.",
      "expectedBehavior": [
        "setDesiredVolume, setMuted, setVolumeBoost, and setOutputDevice all early-return via 'guard !isExcluded(app) else { return }' before touching the backend (AppStore.swift:357, 492, 545, 650).",
        "The launch-time reapplyRestoredAudioState loop skips excluded apps with 'guard !isExcluded(app) else { continue }', so saved volume/mute/boost/device is never re-applied to an excluded app (AppStore.swift:824-826).",
        "Auto-pause-music for conferencing filters out excluded apps when selecting music apps to mute (AppStore.swift:959).",
        "Auto-resume after conferencing filters out excluded apps when selecting resumable apps (AppStore.swift:973).",
        "isExcluded(app) is a simple membership check against preferences.excludedAppIDs keyed by logicalID (AppStore.swift:678-680)."
      ],
      "acceptanceCriteria": [
        "Calling setDesiredVolume/setMuted/setVolumeBoost/setOutputDevice on an excluded app produces no backend call and no state change.",
        "On relaunch, an excluded app that previously had customized volume/mute/boost/device is not re-tapped (no setVolumeBoost/setMuted/setDesiredVolume backend calls for it).",
        "When a conferencing app becomes frontmost, an excluded media app is not auto-muted.",
        "When conferencing ends, an excluded app is not auto-resumed."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:357",
        "Sources/Waves/Stores/AppStore.swift:492",
        "Sources/Waves/Stores/AppStore.swift:545",
        "Sources/Waves/Stores/AppStore.swift:650",
        "Sources/Waves/Stores/AppStore.swift:824-826",
        "Sources/Waves/Stores/AppStore.swift:959",
        "Sources/Waves/Stores/AppStore.swift:973"
      ],
      "implStatus": "implemented",
      "implNotes": "Exclusion guards are consistently placed at every per-app mutation entry point and both automation paths, so the escape-hatch promise ('never tapped, audio left completely untouched') holds. No automated tests cover these guards — only preference persistence is tested — so regressions (e.g. a future new control path that forgets the guard) would not be caught. The global keyboard-shortcut handlers (increaseVolumeForFrontmostApp/decreaseVolumeForFrontmostApp/toggleMuteForFrontmostApp) do not check isExcluded themselves; they rely on the downstream setDesiredVolume/setMuted guards (see EXCL-04 for the resulting misleading-toast gap).",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1C 1M]",
      "errorsFound": "• [CRITICAL/correctness] Per-device volume-preset restore re-taps excluded apps on output-device change (Sources/Waves/Stores/AppStore.swift:851) — CONFIRMED. restoreDeviceVolumePresets iterates every app in session.apps and, for any app with a saved per-device preset, calls backend.setDesiredVolume / backend.setMuted / backend.setVolumeBoost directly with NO isExcluded guard (AppStore.swift:848-867). It is invoked from handleDeviceChange when the output device changes (AppStore.swift:802-803). setExcluded does not clear an app's saved deviceVolumePresets (AppStore.swift:686-720), and setDesiredVolume/setMuted/setVolumeBoost write presets before exclusion (AppStore.swift:380, 514, 561). So an app customized on a device and later excluded gets its saved settings re-applied straight to the backend on the next switch to that device — re-tapping the excluded app. This bypasses the early-return guards because it hits the backend directly. Contrast reapplyRestoredAudioState (AppStore.swift:826) which correctly guards with 'guard !isExcluded(app) else { continue }'. | Fix: Add 'guard !isExcluded(app) else { continue }' at the top of the loop in restoreDeviceVolumePresets (after reading session.apps[index]), mirroring reapplyRestoredAudioState. Optionally also drop the app's deviceVolumePresets entries in setExcluded.\n• [MEDIUM/correctness] No automated coverage for the exclusion guards; an unguarded control path bypasses the escape hatch undetected (Sources/Waves/Stores/AppStore.swift:357) — CONFIRMED. The only test touching exclusion is UserPreferencesTests (codec round-trip of excludedAppIDs at UserPreferencesTests.swift:35-49). None of the AppStore guards (setDesiredVolume:357, setMuted:492, setVolumeBoost:545, setOutputDevice:650, reapplyRestoredAudioState:826, checkAutoPauseMusic:959/973) are tested, and the now-confirmed restoreDeviceVolumePresets (EXCL-02-1) and commitDesiredVolume (EXCL-04-1) gaps prove the risk is real, not hypothetical — at least two backend-facing paths already omit the guard and would not be caught. | Fix: Add unit tests against a spy backend asserting zero backend calls for an excluded app across setDesiredVolume/commitDesiredVolume/setMuted/setVolumeBoost/setOutputDevice, the launch restore, the device-preset restore, and the auto-pause/resume loops.",
      "fixStatus": "1 fixed · 1 deferred",
      "fixNotes": "tier1: FU-EXCLUDED-DEVICEPRESET-GUARD",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "critical",
          "type": "correctness",
          "title": "Per-device volume-preset restore re-taps excluded apps on output-device change",
          "detail": "CONFIRMED. restoreDeviceVolumePresets iterates every app in session.apps and, for any app with a saved per-device preset, calls backend.setDesiredVolume / backend.setMuted / backend.setVolumeBoost directly with NO isExcluded guard (AppStore.swift:848-867). It is invoked from handleDeviceChange when the output device changes (AppStore.swift:802-803). setExcluded does not clear an app's saved deviceVolumePresets (AppStore.swift:686-720), and setDesiredVolume/setMuted/setVolumeBoost write presets before exclusion (AppStore.swift:380, 514, 561). So an app customized on a device and later excluded gets its saved settings re-applied straight to the backend on the next switch to that device — re-tapping the excluded app. This bypasses the early-return guards because it hits the backend directly. Contrast reapplyRestoredAudioState (AppStore.swift:826) which correctly guards with 'guard !isExcluded(app) else { continue }'.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:851",
          "expectedVsActual": "Story (P0): every Waves control and automation must leave an excluded app completely untouched. Actual: the per-device-preset restore path re-taps excluded apps on device change.",
          "suggestedFix": "Add 'guard !isExcluded(app) else { continue }' at the top of the loop in restoreDeviceVolumePresets (after reading session.apps[index]), mirroring reapplyRestoredAudioState. Optionally also drop the app's deviceVolumePresets entries in setExcluded.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:FU-EXCLUDED-DEVICEPRESET-GUARD",
          "uid": "EXCL-02#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-EXCLUDED-DEVICEPRESET-GUARD",
          "fixTier": "tier1"
        },
        {
          "severity": "medium",
          "type": "correctness",
          "title": "No automated coverage for the exclusion guards; an unguarded control path bypasses the escape hatch undetected",
          "detail": "CONFIRMED. The only test touching exclusion is UserPreferencesTests (codec round-trip of excludedAppIDs at UserPreferencesTests.swift:35-49). None of the AppStore guards (setDesiredVolume:357, setMuted:492, setVolumeBoost:545, setOutputDevice:650, reapplyRestoredAudioState:826, checkAutoPauseMusic:959/973) are tested, and the now-confirmed restoreDeviceVolumePresets (EXCL-02-1) and commitDesiredVolume (EXCL-04-1) gaps prove the risk is real, not hypothetical — at least two backend-facing paths already omit the guard and would not be caught.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:357",
          "expectedVsActual": "Story acceptance: any mutation on an excluded app yields zero backend calls and no state change. Actual: guards exist at the enumerated sites but are untested, and confirmed unguarded paths exist (device-preset restore, commit-volume).",
          "suggestedFix": "Add unit tests against a spy backend asserting zero backend calls for an excluded app across setDesiredVolume/commitDesiredVolume/setMuted/setVolumeBoost/setOutputDevice, the launch restore, the device-preset restore, and the auto-pause/resume loops.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-feature:Tests must be added in a test-target file, outside the allowed file.",
          "uid": "EXCL-02#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "EXCL-03",
      "area": "Exclude/Ignore Apps (escape hatch)",
      "feature": "Excluded apps presented clearly and read-only in the mixer",
      "priority": "P2",
      "userStory": "As a user, I want excluded apps to stay visible in the mixer but clearly marked and with their controls disabled, so that I can see what I excluded and re-enable it without losing track of the app.",
      "expectedBehavior": [
        "Excluded rows remain in the list (visibleApps does not filter them out; it only filters system processes), so the app is still shown rather than hidden (AppStore.swift:103-107).",
        "Full row shows an 'Excluded' capsule badge next to the app name with accessibilityLabel 'Excluded from Waves' (MixerRowView.swift:29-37).",
        "For excluded rows the routing-state chip/dot is hidden because Waves isn't controlling them (MixerRowView.swift:46-50; compact: 273-279).",
        "Slider, boost menu, and mute button are .disabled(isExcluded) in both full and compact rows (MixerRowView.swift:81,94,107,308,311,324).",
        "Excluded rows are dimmed to 0.55 opacity (MixerRowView.swift:109,326).",
        "The 'Output Device' submenu is omitted from the context menu for excluded apps (MixerRowView.swift:139-157).",
        "No live level meter is drawn for excluded rows (showsLevelMeter requires !isExcluded) (MixerRowView.swift:173-175)."
      ],
      "acceptanceCriteria": [
        "An excluded app still appears in the mixer list (not removed).",
        "The 'Excluded' badge/text and dimmed appearance render for excluded rows in both full and compact layouts.",
        "Volume slider, boost menu, and mute button are non-interactive for excluded rows.",
        "No routing-state indicator and no level meter is shown for excluded rows.",
        "The context menu hides the Output Device submenu for excluded apps."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MixerRowView.swift:29-50",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:81-109",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:139-157",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:173-175",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:273-326",
        "Sources/Waves/Stores/AppStore.swift:103-107"
      ],
      "implStatus": "implemented",
      "implNotes": "Presentation is thorough and consistent across full and compact rows. UX gap: the menu-bar surface uses CompactMixerRow, which has NO context menu and no exclude/re-include affordance at all (MenuBarMixerView.swift:191; the .contextMenu only exists on MixerRowView, the main-window full row at MixerRowView.swift:135). A user who lives in the menu bar can see an app is 'Excluded' but cannot toggle exclusion from there — they must open the main window. The exclude action is also discoverable only via right-click/VoiceOver action, with no visible button or hint.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [2L]",
      "errorsFound": "• [LOW/ux] Menu-bar mixer offers no way to exclude or re-include an app (Sources/Waves/Features/Mixer/MenuBarMixerView.swift:191) — CONFIRMED (severity lowered to low). CompactMixerRow (MixerRowView.swift:259-327, rendered by MenuBarMixerView via CompactSection at MenuBarMixerView.swift:191) has no .contextMenu and no exclude/re-include control. The 'Exclude from Waves' / 'Manage with Waves' toggle and its accessibility action exist only on the main-window MixerRowView (MixerRowView.swift:159-168). A menu-bar-centric user sees the 'Excluded' label but cannot toggle exclusion without opening the main window. Lowered from medium to low because the escape hatch remains fully reachable from the main window and feature-status documents the toggle as a main-window affordance (the re-include capability still exists; only the menu-bar surface lacks the control). | Fix: Add a .contextMenu (or small toggle) with 'Exclude from Waves' / 'Manage with Waves' to CompactMixerRow mirroring MixerRowView, or document that exclusion is managed from the main window only.\n• [LOW/accessibility] Compact 'Excluded' badge lacks the descriptive accessibility label used in the full row (Sources/Waves/Features/Mixer/MixerRowView.swift:274) — CONFIRMED. The full row's Excluded badge sets .accessibilityLabel('Excluded from Waves') (MixerRowView.swift:36). The compact row's Excluded Text (MixerRowView.swift:273-276) has no accessibilityLabel, so VoiceOver in the menu-bar layout reads the bare word 'Excluded' instead of 'Excluded from Waves'. An inconsistency in the 'clearly marked' presentation between the two surfaces. | Fix: Add .accessibilityLabel('Excluded from Waves') to the compact Excluded Text to match the full row.",
      "fixStatus": "1 fixed · 1 deferred",
      "fixNotes": "tier3: FU-EXCL-A11Y-LABEL",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "ux",
          "title": "Menu-bar mixer offers no way to exclude or re-include an app",
          "detail": "CONFIRMED (severity lowered to low). CompactMixerRow (MixerRowView.swift:259-327, rendered by MenuBarMixerView via CompactSection at MenuBarMixerView.swift:191) has no .contextMenu and no exclude/re-include control. The 'Exclude from Waves' / 'Manage with Waves' toggle and its accessibility action exist only on the main-window MixerRowView (MixerRowView.swift:159-168). A menu-bar-centric user sees the 'Excluded' label but cannot toggle exclusion without opening the main window. Lowered from medium to low because the escape hatch remains fully reachable from the main window and feature-status documents the toggle as a main-window affordance (the re-include capability still exists; only the menu-bar surface lacks the control).",
          "codeRef": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:191",
          "expectedVsActual": "Story: user can re-enable an excluded app without losing track. Actual: in the menu-bar layout the toggle is absent; state is visible but not actionable there.",
          "suggestedFix": "Add a .contextMenu (or small toggle) with 'Exclude from Waves' / 'Manage with Waves' to CompactMixerRow mirroring MixerRowView, or document that exclusion is managed from the main window only.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift",
          "disposition": "deferred:needs-user-decision:CompactMixerRow has no such affordance; adding one is new view/store wiring and a product call, since the toggle is fully reachable from the",
          "uid": "EXCL-03#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift"
        },
        {
          "severity": "low",
          "type": "accessibility",
          "title": "Compact 'Excluded' badge lacks the descriptive accessibility label used in the full row",
          "detail": "CONFIRMED. The full row's Excluded badge sets .accessibilityLabel('Excluded from Waves') (MixerRowView.swift:36). The compact row's Excluded Text (MixerRowView.swift:273-276) has no accessibilityLabel, so VoiceOver in the menu-bar layout reads the bare word 'Excluded' instead of 'Excluded from Waves'. An inconsistency in the 'clearly marked' presentation between the two surfaces.",
          "codeRef": "Sources/Waves/Features/Mixer/MixerRowView.swift:274",
          "expectedVsActual": "Story: excluded apps are clearly marked. Actual: the compact layout's marker is less descriptive to VoiceOver than the full layout's.",
          "suggestedFix": "Add .accessibilityLabel('Excluded from Waves') to the compact Excluded Text to match the full row.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "disposition": "tier3:applied:FU-EXCL-A11Y-LABEL",
          "uid": "EXCL-03#2",
          "canonicalFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-EXCL-A11Y-LABEL",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "EXCL-04",
      "area": "Exclude/Ignore Apps (escape hatch)",
      "feature": "Presets and keyboard shortcuts respect exclusions",
      "priority": "P1",
      "userStory": "As a user, I want saving and applying presets (and using global volume shortcuts) to never silently re-tap an app I excluded, so that automation can't defeat my escape hatch.",
      "expectedBehavior": [
        "applyPreset strips entries whose appID is in excludedAppIDs before sending the preset to the backend, so an excluded app is never re-tapped by a preset (AppStore.swift:1001-1007).",
        "savePreset filters out excluded entries from the saved preset so they aren't baked in for later application (AppStore.swift:1035-1040).",
        "Global shortcut handlers (increase/decrease/toggle for frontmost app) route through setDesiredVolume/setMuted, which are guarded by isExcluded, so an excluded frontmost app's audio is not changed (AppStore.swift:1210-1262 → 357/492)."
      ],
      "acceptanceCriteria": [
        "Applying a preset that contains an excluded app's appID does not tap or alter that app.",
        "A saved preset does not contain entries for currently-excluded apps.",
        "Pressing the global volume-up/down or mute shortcut while an excluded app is frontmost does not change that app's audio."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:1001-1007",
        "Sources/Waves/Stores/AppStore.swift:1031-1040",
        "Sources/Waves/Stores/AppStore.swift:1210-1262"
      ],
      "implStatus": "partial",
      "implNotes": "Preset apply/save correctly strip excluded entries, and shortcut mutations are blocked by the downstream guards, so no audio is altered. However the keyboard-shortcut handlers compute newVolume / newMutedState and unconditionally show a success toast ('Volume increased', 'Volume decreased', 'Muted'/'Unmuted') BEFORE/around the guarded call (AppStore.swift:1221-1226, 1240-1245, 1256-1261). When the frontmost app is excluded, setDesiredVolume/setMuted no-op but the toast still claims the change happened — a misleading-feedback bug. Preset stripping only filters by exact appID equality against excludedAppIDs, so a pid-suffixed companion logicalID (see EXCL-01) would not be matched after relaunch.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1C 1M]",
      "errorsFound": "• [CRITICAL/correctness] Unguarded commitDesiredVolume taps an excluded app — reachable from the global volume shortcut, the main-window =/- keys, and the set-volume URL scheme (Sources/Waves/Stores/AppStore.swift:386) — CONFIRMED, and broader than originally filed. increaseVolumeForFrontmostApp / decreaseVolumeForFrontmostApp call setDesiredVolume (guarded → no-op, never sets pendingVolumeTargets) then commitDesiredVolume (AppStore.swift:1218-1219, 1237-1238). commitDesiredVolume has NO isExcluded guard: with pendingVolumeTargets[appKey] nil it sets it to app.desiredVolume (AppStore.swift:386-388) and calls scheduleVolumeApply(immediate: true), whose task runs ensureBackendHasApp then backend.setDesiredVolume directly (AppStore.swift:426-427). An excluded app stays in visibleApps→activeApps (visibleApps filters only on system category, AppStore.swift:103-107), so it is the shortcut's frontmost target and gets tapped, showing a 'Managed route active' toast. COMPLETENESS: the same unguarded commitDesiredVolume is also reached by (a) the main-window '='/'-' keyboard shortcut via nudgeVolume (MainWindowView.swift:383-384, 416-420) on a selected excluded row, and (b) the waves://set-volume URL scheme via handleURLSetVolume (AppStore.swift:308-309) — the latter is exactly the kind of programmatic automation the escape hatch must block. | Fix: Add 'guard !isExcluded(app) else { return }' to commitDesiredVolume itself (single fix covering all three callers), and early-return in increaseVolumeForFrontmostApp/decreaseVolumeForFrontmostApp before showing any toast.\n• [MEDIUM/ux] Volume/mute shortcuts show a success toast even when the excluded app should be untouched (Sources/Waves/Stores/AppStore.swift:1256) — CONFIRMED. increaseVolumeForFrontmostApp/decreaseVolumeForFrontmostApp/toggleMuteForFrontmostApp unconditionally show 'Volume increased'/'Volume decreased'/'Muted'/'Unmuted' toasts (AppStore.swift:1221-1226, 1240-1245, 1256-1261). For toggleMuteForFrontmostApp, setMuted genuinely no-ops on an excluded app (guard at AppStore.swift:492) yet the toast still claims the mute changed — a direct lie. For the volume shortcuts the toast accompanies the (wrong) real tap from EXCL-04-1, so fixing that path also fixes the toast. | Fix: Guard the shortcut handlers on isExcluded and return before showing any toast (or show an 'App excluded from Waves' info toast instead).",
      "fixStatus": "2 fixed",
      "fixNotes": "tier1: FU-EXCLUDED-COMMIT-GUARD | tier2: FU-EXCLUDED-FRONTMOST-TOASTS",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "critical",
          "type": "correctness",
          "title": "Unguarded commitDesiredVolume taps an excluded app — reachable from the global volume shortcut, the main-window =/- keys, and the set-volume URL scheme",
          "detail": "CONFIRMED, and broader than originally filed. increaseVolumeForFrontmostApp / decreaseVolumeForFrontmostApp call setDesiredVolume (guarded → no-op, never sets pendingVolumeTargets) then commitDesiredVolume (AppStore.swift:1218-1219, 1237-1238). commitDesiredVolume has NO isExcluded guard: with pendingVolumeTargets[appKey] nil it sets it to app.desiredVolume (AppStore.swift:386-388) and calls scheduleVolumeApply(immediate: true), whose task runs ensureBackendHasApp then backend.setDesiredVolume directly (AppStore.swift:426-427). An excluded app stays in visibleApps→activeApps (visibleApps filters only on system category, AppStore.swift:103-107), so it is the shortcut's frontmost target and gets tapped, showing a 'Managed route active' toast. COMPLETENESS: the same unguarded commitDesiredVolume is also reached by (a) the main-window '='/'-' keyboard shortcut via nudgeVolume (MainWindowView.swift:383-384, 416-420) on a selected excluded row, and (b) the waves://set-volume URL scheme via handleURLSetVolume (AppStore.swift:308-309) — the latter is exactly the kind of programmatic automation the escape hatch must block.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:386",
          "expectedVsActual": "Story acceptance: no Waves control or automation changes an excluded app's audio. Actual: commitDesiredVolume reaches the backend and taps the excluded app via three separate callers (global shortcut, main-window keys, set-volume URL).",
          "suggestedFix": "Add 'guard !isExcluded(app) else { return }' to commitDesiredVolume itself (single fix covering all three callers), and early-return in increaseVolumeForFrontmostApp/decreaseVolumeForFrontmostApp before showing any toast.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:FU-EXCLUDED-COMMIT-GUARD",
          "uid": "EXCL-04#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-EXCLUDED-COMMIT-GUARD",
          "fixTier": "tier1"
        },
        {
          "severity": "medium",
          "type": "ux",
          "title": "Volume/mute shortcuts show a success toast even when the excluded app should be untouched",
          "detail": "CONFIRMED. increaseVolumeForFrontmostApp/decreaseVolumeForFrontmostApp/toggleMuteForFrontmostApp unconditionally show 'Volume increased'/'Volume decreased'/'Muted'/'Unmuted' toasts (AppStore.swift:1221-1226, 1240-1245, 1256-1261). For toggleMuteForFrontmostApp, setMuted genuinely no-ops on an excluded app (guard at AppStore.swift:492) yet the toast still claims the mute changed — a direct lie. For the volume shortcuts the toast accompanies the (wrong) real tap from EXCL-04-1, so fixing that path also fixes the toast.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1256",
          "expectedVsActual": "Story: automation can't defeat the escape hatch and shouldn't claim it did. Actual: the toast asserts the change happened even when the excluded app was (for mute) correctly left unchanged.",
          "suggestedFix": "Guard the shortcut handlers on isExcluded and return before showing any toast (or show an 'App excluded from Waves' info toast instead).",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier2:partial:FU-EXCLUDED-FRONTMOST-TOASTS",
          "uid": "EXCL-04#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "partial",
          "fixedBy": "FU-EXCLUDED-FRONTMOST-TOASTS",
          "fixTier": "tier2"
        }
      ]
    },
    {
      "id": "SET-01",
      "area": "Settings Window",
      "feature": "Settings window structure & tabbed navigation",
      "priority": "P1",
      "userStory": "As a Waves user, I want a standard macOS Settings window with clearly separated tabs, so that I can find and adjust preferences, setup status, audio routing, presets, diagnostics, and help in one familiar place.",
      "expectedBehavior": [
        "Opening Settings (⌘, via the standard Settings scene) presents a TabView with six tabs: General, Setup, Audio, Presets, Advanced, Help, each with an SF Symbol label (SettingsView.swift:8-37)",
        "The window is provided by the SwiftUI `Settings { SettingsView() }` scene wired in WavesApp and constrained to a minimum size of 720x500 (WavesApp.swift:51-55)",
        "The whole window is padded 20pt and painted with the app's dark window gradient background (SettingsView.swift:38-39)",
        "The entire app (including Settings) is pinned to darkAqua appearance so the dark gradient never sits under light-mode label colors that would make text invisible (WavesApp.swift:88)",
        "When the Settings window disappears, store.persistPreferences() is called to flush any pending preference changes to disk (SettingsView.swift:40-42)",
        "All tab subviews read shared state from the same AppStore via @Environment(AppStore.self) injected at the scene (SettingsView.swift:5,47; WavesApp.swift:53)"
      ],
      "acceptanceCriteria": [
        "Pressing ⌘, opens a single Settings window showing exactly six tabs in the order General, Setup, Audio, Presets, Advanced, Help",
        "Each tab shows its labeled SF Symbol icon and switches content when clicked",
        "The window cannot be resized below 720x500",
        "Text in every tab is legible (light-on-dark) regardless of the system's Light/Dark appearance setting",
        "Closing the Settings window triggers a preferences save"
      ],
      "codeRefs": [
        "Sources/Waves/Settings/SettingsView.swift:4-44",
        "Sources/Waves/App/WavesApp.swift:51-55",
        "Sources/Waves/App/WavesApp.swift:88"
      ],
      "implStatus": "implemented",
      "implNotes": "Cleanly wired. Note the Setup tab embeds OnboardingView and the Help tab embeds HelpView — those are full features owned by other audit areas (Onboarding, Help) and are only surfaced here as tabs; I scope them out of dedicated stories below. Minor: persistPreferences() on onDisappear is redundant because every General toggle already persists inline on change, but it is harmless.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "SET-02",
      "area": "Settings Window",
      "feature": "General preferences toggles & sort mode",
      "priority": "P0",
      "userStory": "As a Waves user, I want a General settings panel with toggles for app-list visibility, automation, and an app sort order, so that I can tailor which apps appear and how Waves behaves to my workflow.",
      "expectedBehavior": [
        "GeneralSettingsView renders a grouped Form with toggles: Launch at login, Show recent apps, Show system processes, Auto-restore device, Auto-pause music during calls, Enable keyboard shortcuts, Per-device volume presets, URL scheme automation, plus a 'Sort apps by' picker (SettingsView.swift:50-176)",
        "'Show system processes' off filters out apps whose category is .system from visibleApps; on includes them (AppStore.swift:103-107)",
        "'Show recent apps' off makes recentApps return an empty list; on returns visible apps that are not currently live (AppStore.swift:124-128)",
        "'Auto-restore device' gates handleDeviceChange so routes are only re-read on device change when enabled (AppStore.swift:792-793)",
        "'Auto-pause music during calls' gates checkAutoPauseMusic so media apps are only auto-muted when a conferencing app is frontmost while enabled (AppStore.swift:940-941, 955-984)",
        "'Per-device volume presets' gates restoring saved per-device volumes on device switch (AppStore.swift:802-803)",
        "'Sort apps by' offers Activity/Name/Category/Manual (SortMode.allCases) and drives sortedApps ordering; manual uses the saved customAppOrder (SettingsView.swift:162-175; UserPreferences.swift:116-136; AppStore.swift:1278-1337)",
        "Each of these toggles/picker (except Launch-at-login and keyboard shortcuts) sets the preference and immediately calls store.persistPreferences() in its setter (SettingsView.swift:62-176)",
        "Defaults: showRecentApps=true, showSystemProcesses=false, autoRestoreDevice=true, autoPauseMusicForConferencing=true, enableKeyboardShortcuts=true, enablePerDeviceVolumePresets=true, sortMode=.name (UserPreferences.swift:4-12)",
        "Preferences decode field-by-field with per-key fallbacks so a file written by an older build (missing keys) loads cleanly instead of wiping all settings (UserPreferences.swift:40-61)"
      ],
      "acceptanceCriteria": [
        "Toggling 'Show system processes' on/off adds/removes system-category apps from the mixer list",
        "Toggling 'Show recent apps' off hides the recent-apps section (recentApps is empty)",
        "Changing 'Sort apps by' reorders the app list per the selected mode immediately",
        "Selecting Manual sort honors a previously saved drag order; choosing it with no saved order still produces a stable name-based order",
        "Every General toggle persists across an app relaunch (written to Application Support/Waves/preferences.json)",
        "Loading a preferences file missing newer keys keeps existing values and fills new keys with defaults rather than resetting everything"
      ],
      "codeRefs": [
        "Sources/Waves/Settings/SettingsView.swift:46-178",
        "Sources/Waves/Stores/UserPreferences.swift:3-62",
        "Sources/Waves/Stores/UserPreferences.swift:116-136",
        "Sources/Waves/Stores/AppStore.swift:103-128",
        "Sources/Waves/Stores/AppStore.swift:1278-1301"
      ],
      "implStatus": "implemented",
      "implNotes": "All toggles are genuinely wired to behavior (verified each gating call site), not placeholders. One UX gap: the 'Sort apps by' picker lets the user choose Manual even when no custom order exists; on next launch the init() silently downgrades manual→name if customAppOrder is empty (AppStore.swift:88-90), so a user who selects Manual but never drags will find it reset to Name after relaunch — mildly surprising. Bindings persist inline AND onDisappear, so writes are duplicated but consistent.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [2L]",
      "errorsFound": "• [LOW/ux] Selecting Manual sort with no drag order silently reverts to Name after relaunch (Sources/Waves/Stores/AppStore.swift:88-90) — Verified in code: the 'Sort apps by' picker persists Manual immediately (SettingsView.swift:162-175), but AppStore.init downgrades manual->name when customAppOrder is empty (AppStore.swift:88-90). A user who selects Manual but never drags any row finds the setting reset to Name after relaunch with no explanation. In-session the manualOrderComparator (AppStore.swift:1327-1337) falls back to displayName, so the live order is stable; only the persisted choice is silently discarded. | Fix: Keep sortMode == .manual across relaunch even with an empty customAppOrder (the comparator already falls back to name), or disable/annotate the Manual option until the user performs at least one drag.\n• [LOW/edge-case] Manual sort order silently loses hidden / not-running apps on every reorder (Sources/Waves/Stores/AppStore.swift:1345-1351) — reorderApps rebuilds the persisted order from the *filtered* set: preferences.customAppOrder = visibleApps.map { $0.logicalID } (AppStore.swift:1345,1351). visibleApps excludes system-category apps when 'Show system processes' is off, and excludes any app not currently running. So any app that was previously placed in the manual order but is hidden or quit at the moment of the next drag is permanently dropped from customAppOrder. When it reappears it falls to Int.max in manualOrderComparator (AppStore.swift:1330-1331), i.e. sorted to the end by name, losing its prior manual position. The first tester missed this; it is a genuine persistence/desync edge for the 'honors a previously saved drag order' criterion when the visible set changes between reorders. | Fix: Merge the reordered visible IDs back into the existing customAppOrder (preserve entries for apps not currently visible) instead of replacing the whole array with visibleApps only.",
      "fixStatus": "2 deferred",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "ux",
          "title": "Selecting Manual sort with no drag order silently reverts to Name after relaunch",
          "detail": "Verified in code: the 'Sort apps by' picker persists Manual immediately (SettingsView.swift:162-175), but AppStore.init downgrades manual->name when customAppOrder is empty (AppStore.swift:88-90). A user who selects Manual but never drags any row finds the setting reset to Name after relaunch with no explanation. In-session the manualOrderComparator (AppStore.swift:1327-1337) falls back to displayName, so the live order is stable; only the persisted choice is silently discarded.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:88-90",
          "expectedVsActual": "Expected (acceptance): selecting Manual honors a saved order, and with no saved order produces a stable name-based order. Actual: it produces a stable order in-session, but the Manual selection is silently downgraded to Name on next launch.",
          "suggestedFix": "Keep sortMode == .manual across relaunch even with an empty customAppOrder (the comparator already falls back to name), or disable/annotate the Manual option until the user performs at least one drag.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-user-decision:Same intentional init downgrade (AppStore.swift:107-109): Manual selected with no saved drag order reverts to Name on relaunch. Persisting Manual vs. annotating",
          "uid": "SET-02#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "low",
          "type": "edge-case",
          "title": "Manual sort order silently loses hidden / not-running apps on every reorder",
          "detail": "reorderApps rebuilds the persisted order from the *filtered* set: preferences.customAppOrder = visibleApps.map { $0.logicalID } (AppStore.swift:1345,1351). visibleApps excludes system-category apps when 'Show system processes' is off, and excludes any app not currently running. So any app that was previously placed in the manual order but is hidden or quit at the moment of the next drag is permanently dropped from customAppOrder. When it reappears it falls to Int.max in manualOrderComparator (AppStore.swift:1330-1331), i.e. sorted to the end by name, losing its prior manual position. The first tester missed this; it is a genuine persistence/desync edge for the 'honors a previously saved drag order' criterion when the visible set changes between reorders.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1345-1351",
          "expectedVsActual": "Expected: a saved manual order is preserved for all ordered apps across sessions. Actual: each reorder overwrites the saved order with only the currently-visible apps, so hidden/quit apps lose their saved manual position.",
          "suggestedFix": "Merge the reordered visible IDs back into the existing customAppOrder (preserve entries for apps not currently visible) instead of replacing the whole array with visibleApps only.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:Switching from full-replace to merge changes the persisted ordering semantics and the relaunched-app placement behavior, which interacts with manualOrderComparator and the visible/hidden filtering. It needs a deliberate decision on how absent IDs are positioned (and whether to prune), so it is more than a behavior-preserving localized fix. ORDER-04#4's pruning is an explicit policy choice. Low user impact (manual sort is opt-in and apps still appear, just at the end).",
          "uid": "SET-02#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "SET-03",
      "area": "Settings Window",
      "feature": "Launch at login (login item) with approval handling",
      "priority": "P1",
      "userStory": "As a Waves user, I want a 'Launch at login' toggle that registers Waves as a macOS login item, so that the mixer starts automatically when I log in.",
      "expectedBehavior": [
        "The 'Launch at login' toggle binds to store.launchAtLoginEnabled, whose setter calls LoginItemService.setEnabled and registers/unregisters via SMAppService.mainApp (SettingsView.swift:51-56; AppStore.swift:161-186; LoginItemService.swift:29-35)",
        "After attempting to enable, the store reads back the real SMAppService status and stores preferences.launchAtLoginEnabled from that actual status (not the requested value) (AppStore.swift:166-169)",
        "If macOS requires user approval (status .requiresApproval) so the effective state differs from requested, a warning toast 'Login item needs approval' is shown with the status description (AppStore.swift:170-177; LoginItemService.swift:18-19)",
        "If register/unregister throws, the store falls back to the real status, persists it, and shows an error toast 'Login item update failed' (AppStore.swift:178-184)",
        "On launch and on session sync, the persisted launchAtLoginEnabled and onboarding state are reconciled with the live login-item status so the toggle reflects reality (AppStore.swift:95-99, 1356-1365)"
      ],
      "acceptanceCriteria": [
        "Enabling the toggle registers Waves as a login item via SMAppService",
        "If the OS requires approval, the toggle reflects the real (still-disabled) state and a warning toast appears telling the user approval is needed",
        "A failure to register/unregister surfaces an error toast and does not leave the toggle showing a false 'on' state",
        "Reopening Settings shows the toggle matching the actual current login-item registration status"
      ],
      "codeRefs": [
        "Sources/Waves/Settings/SettingsView.swift:51-56",
        "Sources/Waves/Stores/AppStore.swift:161-186",
        "Sources/Waves/Services/LoginItem/LoginItemService.swift:1-36",
        "Sources/Waves/Stores/AppStore.swift:1356-1365"
      ],
      "implStatus": "implemented",
      "implNotes": "Robust: it reflects the OS-reported status rather than the requested value and gives toast feedback for the approval and error cases — good handling of the .requiresApproval edge case. No way to deep-link the user to System Settings > Login Items from the warning toast, which would smooth the approval flow, but that is a minor enhancement.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/edge-case] Login-at-login toggle can show stale state if changed externally while Settings stays open (Sources/Waves/Stores/AppStore.swift:1356-1365) — Verified: store.launchAtLoginEnabled getter reads preferences.launchAtLoginEnabled (AppStore.swift:161-162), which is reconciled with the real SMAppService status only at init (AppStore.swift:95) and inside syncOnboarding (AppStore.swift:1356-1365), called on start/refresh/device-change/recover. start() is guarded by isBootstrapped so it runs once. GeneralSettingsView and SettingsView have no .onAppear that re-reads login status (only the Advanced tab has an onAppear, SettingsView.swift:310). If the user toggles the login item externally (System Settings > Login Items) or grants pending approval there, then reopens Waves Settings without triggering a refresh/device-change, the toggle shows the stale persisted value. | Fix: Re-read loginItemService.status and update preferences.launchAtLoginEnabled in GeneralSettingsView.onAppear (or SettingsView.onAppear) so the toggle reflects the real registration status each time Settings opens.",
      "fixStatus": "1 deferred",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "edge-case",
          "title": "Login-at-login toggle can show stale state if changed externally while Settings stays open",
          "detail": "Verified: store.launchAtLoginEnabled getter reads preferences.launchAtLoginEnabled (AppStore.swift:161-162), which is reconciled with the real SMAppService status only at init (AppStore.swift:95) and inside syncOnboarding (AppStore.swift:1356-1365), called on start/refresh/device-change/recover. start() is guarded by isBootstrapped so it runs once. GeneralSettingsView and SettingsView have no .onAppear that re-reads login status (only the Advanced tab has an onAppear, SettingsView.swift:310). If the user toggles the login item externally (System Settings > Login Items) or grants pending approval there, then reopens Waves Settings without triggering a refresh/device-change, the toggle shows the stale persisted value.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1356-1365",
          "expectedVsActual": "Expected (acceptance 4): 'Reopening Settings shows the toggle matching the actual current login-item registration status.' Actual: the toggle matches the last-reconciled status, not necessarily the current one, because reconciliation is not tied to the Settings window appearing.",
          "suggestedFix": "Re-read loginItemService.status and update preferences.launchAtLoginEnabled in GeneralSettingsView.onAppear (or SettingsView.onAppear) so the toggle reflects the real registration status each time Settings opens.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-feature:Login toggle can show stale state if changed externally while Settings stays open. Fixing requires a new lifecycle observer (scenePhase/didBecomeActive) or view",
          "uid": "SET-03#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "SET-04",
      "area": "Settings Window",
      "feature": "Keyboard shortcuts toggle, global hotkey lifecycle & cheat-sheet",
      "priority": "P1",
      "userStory": "As a Waves user, I want to enable or disable global volume/mute hotkeys and see what they are, so that I can control the frontmost app's audio without leaving my current app while keeping Waves from monitoring keystrokes when I don't want it to.",
      "expectedBehavior": [
        "The 'Enable keyboard shortcuts' toggle routes through store.setKeyboardShortcutsEnabled, which sets the preference, persists it, and posts .wavesKeyboardShortcutsPreferenceChanged (SettingsView.swift:98-103; AppStore.swift:1272-1276)",
        "AppDelegate observes that notification and installs the global key monitor only while shortcuts are enabled, removing it otherwise so Waves never observes global keystrokes when disabled (WavesApp.swift:91-112, 150-162)",
        "While enabled, the General tab reveals a 'Keyboard shortcuts' cheat-sheet listing Increase volume ⌘⌥↑, Decrease volume ⌘⌥↓, Toggle mute ⌘⌥M (SettingsView.swift:127-160)",
        "The global monitor maps keyCode 126→increaseVolumeForFrontmostApp, 125→decrease, 46→toggleMute, requiring Command+Option, and ignores events while a text field/text view is first responder (WavesApp.swift:164-189)",
        "Each shortcut handler re-checks preferences.enableKeyboardShortcuts before acting and targets the frontmost active app (falling back to first visible app), showing a confirmation toast (AppStore.swift:1210-1262)"
      ],
      "acceptanceCriteria": [
        "Disabling the toggle removes the global event monitor (Waves stops receiving global keystrokes)",
        "Enabling the toggle installs the monitor and shows the shortcut cheat-sheet in the General tab",
        "⌘⌥↑/↓ adjust, and ⌘⌥M toggles mute on, the frontmost app, with a toast",
        "Hotkeys are ignored while the user is typing in a text field",
        "The shortcut list is informational only and matches the documented keys"
      ],
      "codeRefs": [
        "Sources/Waves/Settings/SettingsView.swift:98-160",
        "Sources/Waves/Stores/AppStore.swift:1272-1276",
        "Sources/Waves/Stores/AppStore.swift:1210-1262",
        "Sources/Waves/App/WavesApp.swift:91-112",
        "Sources/Waves/App/WavesApp.swift:150-189"
      ],
      "implStatus": "implemented",
      "implNotes": "The enable/disable lifecycle for the global monitor is correctly gated through a notification, which is good for privacy. The shortcuts are hard-coded and non-rebindable — the cheat-sheet is display-only, so a user cannot customize keys. Also the listed shortcuts are static text and would silently drift if the keyCode mapping in WavesApp ever changed; no single source of truth ties the cheat-sheet to the handler.",
      "source": "phase1-story",
      "testStatus": "4 issue(s) [1H 1M 2L]",
      "errorsFound": "• [HIGH/correctness] Shortcuts target first-sorted active (audible-or-frontmost) app, not the true frontmost app (Sources/Waves/Stores/AppStore.swift:1212) — Verified: increaseVolumeForFrontmostApp / decreaseVolumeForFrontmostApp / toggleMuteForFrontmostApp all resolve the target as `activeApps.first ?? visibleApps.first` (AppStore.swift:1212,1231,1250). activeApps = visibleApps.filter(\\.isActive) (AppStore.swift:113-115), and the backend sets isActive = isAudible || isFrontmost (WorkspaceAudioControlBackend.swift:1189). So `activeApps.first` is the first app *in current sort order* that is audible OR frontmost, not the OS frontmost app. Concrete trigger: background music (audible->active) sorts before the foreground app under Name/Category sort, so pressing the hotkey changes the music app's volume/mute instead of the foreground app. isActive is also snapshot-derived and only refreshed periodically (checkAutoPauseMusic at AppStore.swift:940-943 deliberately uses NSWorkspace.shared.frontmostApplication *instead of* the snapshot for exactly this reason), so the shortcut handlers ignore the more-correct pattern already present in the codebase. When nothing is active, the visibleApps.first fallback mutes/adjusts an arbitrary alphabetically-first app. | Fix: Resolve the frontmost app from NSWorkspace.shared.frontmostApplication (match bundleIdentifier/pid against session.apps as checkAutoPauseMusic already does) and act on that app; fall back to activeApps.first only when no managed match exists for the OS-frontmost app.\n• [MEDIUM/logic] Text-field guard is ineffective: a global monitor never sees the foreign app's first responder (Sources/Waves/App/WavesApp.swift:169-172) — Verified: handleGlobalKeyEvent guards via NSApp.keyWindow?.firstResponder being an NSTextView/NSTextField (WavesApp.swift:169-172). But the monitor is installed only via NSEvent.addGlobalMonitorForEvents (WavesApp.swift:152) with no addLocalMonitorForEvents companion (grep confirms only one monitor). A global monitor delivers key events destined for OTHER apps; when another app is frontmost (the only time these events fire) NSApp.keyWindow is generally nil, so the guard reads nil and never blocks. Result: pressing the hotkeys (incl. Cmd+Option+M) while typing in another app's text field still fires. Practical severity is tempered because the shortcuts use Command+Option, which rarely collides with text entry, but the acceptance criterion is not met. | Fix: Detect typing in the frontmost foreign app via the Accessibility API (AXUIElement focused-element role) rather than NSApp.keyWindow, or explicitly document the guard as only covering Waves' own windows.\n• [LOW/ux] Each volume/mute hotkey emits two stacked toasts (Sources/Waves/Stores/AppStore.swift:1221-1226) — Verified: increaseVolumeForFrontmostApp shows a 'Volume increased' info toast (AppStore.swift:1221-1226) and then calls commitDesiredVolume, whose apply path shows a second 'Managed route active - <App> set to N%' success toast (AppStore.swift:433-438). Likewise toggleMuteForFrontmostApp shows 'Muted'/'Unmuted' (AppStore.swift:1256-1261) while the setMuted it calls shows 'App muted'/'App unmuted' (AppStore.swift:528-533). With maxToasts = 3, a single keypress consumes two of the three toast slots with duplicated information. The first tester did not flag this. | Fix: Suppress the per-action confirmation toast in setMuted/commit when invoked from a shortcut handler (e.g. a silent variant), or drop the handler-level toast and rely on the apply-path toast.\n• [LOW/inconsistency] Cheat-sheet shortcut text is decoupled from the keyCode handler (silent drift risk) (Sources/Waves/Settings/SettingsView.swift:132-157) — Verified: the cheat-sheet strings '⌘⌥↑','⌘⌥↓','⌘⌥M' in GeneralSettingsView (SettingsView.swift:136,145,154) are hard-coded literals with no shared source of truth with the keyCode switch (126/125/46 + Command+Option) in WavesApp.handleGlobalKeyEvent (WavesApp.swift:179-188). Today the values are consistent (keyCode 46 = ANSI M, 126 = up, 125 = down) so there is no user-facing error, but if the handler mapping changes the displayed cheat-sheet would silently misstate the real keys. | Fix: Define the shortcut set (keyCode, modifiers, label) once in a shared enum/struct and drive both the global-monitor switch and the cheat-sheet rows from it.",
      "fixStatus": "2 fixed · 2 deferred",
      "fixNotes": "tier1: FU-FRONTMOST-RESOLUTION | tier3: FU-HOTKEY-DOUBLE-TOAST",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "correctness",
          "title": "Shortcuts target first-sorted active (audible-or-frontmost) app, not the true frontmost app",
          "detail": "Verified: increaseVolumeForFrontmostApp / decreaseVolumeForFrontmostApp / toggleMuteForFrontmostApp all resolve the target as `activeApps.first ?? visibleApps.first` (AppStore.swift:1212,1231,1250). activeApps = visibleApps.filter(\\.isActive) (AppStore.swift:113-115), and the backend sets isActive = isAudible || isFrontmost (WorkspaceAudioControlBackend.swift:1189). So `activeApps.first` is the first app *in current sort order* that is audible OR frontmost, not the OS frontmost app. Concrete trigger: background music (audible->active) sorts before the foreground app under Name/Category sort, so pressing the hotkey changes the music app's volume/mute instead of the foreground app. isActive is also snapshot-derived and only refreshed periodically (checkAutoPauseMusic at AppStore.swift:940-943 deliberately uses NSWorkspace.shared.frontmostApplication *instead of* the snapshot for exactly this reason), so the shortcut handlers ignore the more-correct pattern already present in the codebase. When nothing is active, the visibleApps.first fallback mutes/adjusts an arbitrary alphabetically-first app.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1212",
          "expectedVsActual": "Expected (story + acceptance): the hotkeys adjust/mute 'the frontmost app's audio.' Actual: they act on whichever audible-or-frontmost app sorts first, frequently a background audible app, or an arbitrary first app when none is active.",
          "suggestedFix": "Resolve the frontmost app from NSWorkspace.shared.frontmostApplication (match bundleIdentifier/pid against session.apps as checkAutoPauseMusic already does) and act on that app; fall back to activeApps.first only when no managed match exists for the OS-frontmost app.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:FU-FRONTMOST-RESOLUTION",
          "uid": "SET-04#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-FRONTMOST-RESOLUTION",
          "fixTier": "tier1"
        },
        {
          "severity": "medium",
          "type": "logic",
          "title": "Text-field guard is ineffective: a global monitor never sees the foreign app's first responder",
          "detail": "Verified: handleGlobalKeyEvent guards via NSApp.keyWindow?.firstResponder being an NSTextView/NSTextField (WavesApp.swift:169-172). But the monitor is installed only via NSEvent.addGlobalMonitorForEvents (WavesApp.swift:152) with no addLocalMonitorForEvents companion (grep confirms only one monitor). A global monitor delivers key events destined for OTHER apps; when another app is frontmost (the only time these events fire) NSApp.keyWindow is generally nil, so the guard reads nil and never blocks. Result: pressing the hotkeys (incl. Cmd+Option+M) while typing in another app's text field still fires. Practical severity is tempered because the shortcuts use Command+Option, which rarely collides with text entry, but the acceptance criterion is not met.",
          "codeRef": "Sources/Waves/App/WavesApp.swift:169-172",
          "expectedVsActual": "Expected (acceptance): 'Hotkeys are ignored while the user is typing in a text field.' Actual: the only first responder checked belongs to Waves' own key window, which a global monitor never observes for foreign apps, so the guard cannot satisfy the criterion for the text fields the user actually types in.",
          "suggestedFix": "Detect typing in the frontmost foreign app via the Accessibility API (AXUIElement focused-element role) rather than NSApp.keyWindow, or explicitly document the guard as only covering Waves' own windows.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/App/WavesApp.swift",
          "disposition": "deferred:needs-user-decision:Platform limitation: an app cannot inspect another app's text-field focus.",
          "uid": "SET-04#2",
          "canonicalFile": "Sources/Waves/App/WavesApp.swift"
        },
        {
          "severity": "low",
          "type": "ux",
          "title": "Each volume/mute hotkey emits two stacked toasts",
          "detail": "Verified: increaseVolumeForFrontmostApp shows a 'Volume increased' info toast (AppStore.swift:1221-1226) and then calls commitDesiredVolume, whose apply path shows a second 'Managed route active - <App> set to N%' success toast (AppStore.swift:433-438). Likewise toggleMuteForFrontmostApp shows 'Muted'/'Unmuted' (AppStore.swift:1256-1261) while the setMuted it calls shows 'App muted'/'App unmuted' (AppStore.swift:528-533). With maxToasts = 3, a single keypress consumes two of the three toast slots with duplicated information. The first tester did not flag this.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1221-1226",
          "expectedVsActual": "Expected: one confirmation toast per hotkey action. Actual: two toasts per keypress (the handler's own toast plus the underlying setMuted/volume-apply toast).",
          "suggestedFix": "Suppress the per-action confirmation toast in setMuted/commit when invoked from a shortcut handler (e.g. a silent variant), or drop the handler-level toast and rely on the apply-path toast.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier3:applied:FU-HOTKEY-DOUBLE-TOAST",
          "uid": "SET-04#3",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-HOTKEY-DOUBLE-TOAST",
          "fixTier": "tier3"
        },
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "Cheat-sheet shortcut text is decoupled from the keyCode handler (silent drift risk)",
          "detail": "Verified: the cheat-sheet strings '⌘⌥↑','⌘⌥↓','⌘⌥M' in GeneralSettingsView (SettingsView.swift:136,145,154) are hard-coded literals with no shared source of truth with the keyCode switch (126/125/46 + Command+Option) in WavesApp.handleGlobalKeyEvent (WavesApp.swift:179-188). Today the values are consistent (keyCode 46 = ANSI M, 126 = up, 125 = down) so there is no user-facing error, but if the handler mapping changes the displayed cheat-sheet would silently misstate the real keys.",
          "codeRef": "Sources/Waves/Settings/SettingsView.swift:132-157",
          "expectedVsActual": "Expected: 'The shortcut list is informational only and matches the documented keys.' Actual: it matches today but is duplicated literal text decoupled from the handler, so it can drift undetected.",
          "suggestedFix": "Define the shortcut set (keyCode, modifiers, label) once in a shared enum/struct and drive both the global-monitor switch and the cheat-sheet rows from it.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Settings/SettingsView.swift",
          "disposition": "deferred:needs-feature:No current user-facing defect; the anti-drift fix introduces a new shared shortcut abstraction that re-routes the live key handler in WavesApp.swift (out of sco",
          "uid": "SET-04#4",
          "canonicalFile": "Sources/Waves/Settings/SettingsView.swift"
        }
      ]
    },
    {
      "id": "SET-05",
      "area": "Settings Window",
      "feature": "URL scheme automation opt-in (security default)",
      "priority": "P2",
      "userStory": "As a privacy-conscious user, I want URL scheme automation to be off by default and only enabled by an explicit choice, so that other apps cannot drive Waves via waves:// links unless I deliberately allow it.",
      "expectedBehavior": [
        "The 'URL scheme automation' toggle defaults off (enableURLScheme=false) and carries a help tooltip explaining it lets other apps/links control Waves via waves:// URLs (UserPreferences.swift:13; SettingsView.swift:115-125)",
        "Turning the toggle on sets enableURLScheme and also marks urlSchemeAutomationAcknowledged=true, then persists (SettingsView.swift:117-123)",
        "On startup, if the user has never acknowledged the automation choice, the store force-disables URL schemes and marks them acknowledged — a fail-safe so automation is never silently on after an upgrade (AppStore.swift:91-94)",
        "handleURLScheme rejects (and logs a warning for) any incoming waves:// invocation while enableURLScheme is disabled (AppStore.swift:256-258; WavesApp.swift:197-200)",
        "The AppDelegate registers/unregisters the Apple Event GetURL handler around app lifecycle so the scheme is only handled while the app runs (WavesApp.swift:124-148, 191-200)"
      ],
      "acceptanceCriteria": [
        "On a fresh install the URL scheme toggle is off and waves:// commands are ignored",
        "Enabling the toggle persists both enableURLScheme=true and the acknowledgment flag",
        "While the toggle is off, sending a waves:// URL produces no audio change and is logged as rejected",
        "After an app update where the user had never made a choice, automation remains disabled until explicitly enabled"
      ],
      "codeRefs": [
        "Sources/Waves/Settings/SettingsView.swift:115-125",
        "Sources/Waves/Stores/UserPreferences.swift:13-14",
        "Sources/Waves/Stores/AppStore.swift:91-94",
        "Sources/Waves/Stores/AppStore.swift:256-258",
        "Sources/Waves/App/WavesApp.swift:124-148"
      ],
      "implStatus": "implemented",
      "implNotes": "Sensible security posture: opt-in, acknowledgment-gated, and force-disabled when never acknowledged. One subtlety: the acknowledgment flag is set true the first time the toggle is touched (even when turning it back off) and the startup fail-safe at AppStore.swift:91-94 only fires when not yet acknowledged — so the 'reset to off on upgrade' protection only applies to users who never interacted with the toggle, which is the intended scope but easy to misread.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/dead-code] application(_:open:) URL path is effectively unreachable because a custom GetURL AppleEvent handler is installed (Sources/Waves/App/WavesApp.swift:191-195) — Verified: applicationDidFinishLaunching installs a custom kAEGetURL AppleEvent handler via setupURLSchemeHandler (WavesApp.swift:89,124-131), which overrides NSApplication's default GetURL handler that would otherwise dispatch to application(_:open:). With the override in place, incoming waves:// URLs are delivered to handleGetURLEvent (WavesApp.swift:140-148) and AppDelegate.application(_:open:) (WavesApp.swift:191-195) is effectively dead. Both ultimately call handleURLScheme, so behavior is correct, but the open(urls:) implementation is redundant/unreachable and could mislead a maintainer into thinking there are two live entry points (or, if both ever fired, double-dispatch the command). Info.plist does register the waves scheme, so the launch path matters; the AppleEvent handler simply wins. | Fix: Remove the redundant application(_:open:) (or remove the manual setEventHandler and rely on application(_:open:)) so there is one well-defined URL entry path; if both must coexist, guard against double handling.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier3: FU-URL-REDUNDANT-OPEN",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "dead-code",
          "title": "application(_:open:) URL path is effectively unreachable because a custom GetURL AppleEvent handler is installed",
          "detail": "Verified: applicationDidFinishLaunching installs a custom kAEGetURL AppleEvent handler via setupURLSchemeHandler (WavesApp.swift:89,124-131), which overrides NSApplication's default GetURL handler that would otherwise dispatch to application(_:open:). With the override in place, incoming waves:// URLs are delivered to handleGetURLEvent (WavesApp.swift:140-148) and AppDelegate.application(_:open:) (WavesApp.swift:191-195) is effectively dead. Both ultimately call handleURLScheme, so behavior is correct, but the open(urls:) implementation is redundant/unreachable and could mislead a maintainer into thinking there are two live entry points (or, if both ever fired, double-dispatch the command). Info.plist does register the waves scheme, so the launch path matters; the AppleEvent handler simply wins.",
          "codeRef": "Sources/Waves/App/WavesApp.swift:191-195",
          "expectedVsActual": "Expected: a single, clear URL entry point. Actual: two entry points exist but only the AppleEvent handler runs; application(_:open:) is effectively dead code.",
          "suggestedFix": "Remove the redundant application(_:open:) (or remove the manual setEventHandler and rely on application(_:open:)) so there is one well-defined URL entry path; if both must coexist, guard against double handling.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/App/WavesApp.swift",
          "disposition": "tier3:applied:FU-URL-REDUNDANT-OPEN",
          "uid": "SET-05#1",
          "canonicalFile": "Sources/Waves/App/WavesApp.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-URL-REDUNDANT-OPEN",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "SET-06",
      "area": "Settings Window",
      "feature": "Audio tab: current output device & route recovery",
      "priority": "P1",
      "userStory": "As a Waves user, I want the Audio settings tab to show my current output device, explain how managed routing works, and let me re-establish broken routes, so that I can understand and recover my audio routing without leaving Settings.",
      "expectedBehavior": [
        "AudioSettingsView shows 'Current output device' with store.currentDeviceName, which resolves to the session's current device name or 'No output device' when none is present (SettingsView.swift:186-189; AppStore.swift:139-141)",
        "A 'Managed routing' explainer describes that Waves uses Core Audio process taps to capture, process (volume/mute/boost) and replay app audio locally to the current output device (SettingsView.swift:191-196)",
        "A 'Recover Routes' button calls store.recoverRoutes(), which asks the backend to reattach managed routing paths, refreshes diagnostics and the session, and shows a 'Routes recovered' success toast — or a 'Recovery failed' error toast with fallback to the current snapshot (SettingsView.swift:198-200; AppStore.swift:1181-1202)",
        "A caption guides boost usage (1x transparent, 2x–4x for quiet apps to avoid clipping) (SettingsView.swift:202-204)"
      ],
      "acceptanceCriteria": [
        "The Audio tab displays the actual current output device name, or 'No output device' when none is connected",
        "Tapping 'Recover Routes' reattaches managed routes and shows a success toast on success",
        "If recovery fails, an error toast is shown and the view falls back to the current backend snapshot rather than leaving a broken state",
        "The managed-routing and boost explainer text is present and accurate"
      ],
      "codeRefs": [
        "Sources/Waves/Settings/SettingsView.swift:181-208",
        "Sources/Waves/Stores/AppStore.swift:139-141",
        "Sources/Waves/Stores/AppStore.swift:1181-1202"
      ],
      "implStatus": "implemented",
      "implNotes": "Functional but read-only: the Audio tab shows the current device name and a Recover Routes action but provides no output-device picker here (device selection lives elsewhere in the app, e.g. selectOutputDevice at AppStore.swift:759). That is a deliberate split, but a user expecting to change the output device from the 'Audio' settings tab will not find a control. The 'No output device' empty state is handled.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "SET-07",
      "area": "Settings Window",
      "feature": "Presets management (import / export / delete / empty state)",
      "priority": "P1",
      "userStory": "As a Waves user, I want to manage my saved mix presets from Settings — importing, exporting, and deleting them — so that I can back up, share, and curate reusable per-app volume mixes.",
      "expectedBehavior": [
        "PresetSettingsView lists store.presets, each row showing the preset name and an entry count with correct singular/plural ('1 app' vs 'N apps') (SettingsView.swift:234-242)",
        "When no presets exist, an empty-state message 'No presets yet. Save a mix from the main window, or import one.' is shown (SettingsView.swift:227-232)",
        "An 'Import' button triggers store.importPreset(), which opens an NSOpenPanel for a JSON file, validates size (≤10MB), non-empty name, and ≤1000 entries, then either replaces a same-named preset (preserving id/createdAt) or appends with a fresh UUID, saves, and shows a success/failure toast (SettingsView.swift:221-224; AppStore.swift:1112-1179)",
        "Each row's 'Export' button calls store.exportPreset, which encodes pretty-printed JSON and presents an NSSavePanel defaulting to '<name>.json', writing atomically and toasting success/failure (SettingsView.swift:246-249; AppStore.swift:1079-1110)",
        "Each row's 'Delete' button (and swipe-to-delete via onDelete) calls store.deletePresets(at:), which removes by offset, persists via presetStore.save, and shows a 'Preset removed' toast; the delete button carries an accessibility label 'Delete preset <name>' (SettingsView.swift:252-262; AppStore.swift:1066-1077)"
      ],
      "acceptanceCriteria": [
        "With zero presets, the empty-state copy is shown and the list is empty",
        "Importing a valid preset JSON adds it (or updates a same-named one) and shows a success toast",
        "Importing an oversized (>10MB), empty-named, or >1000-entry file is rejected with a specific error toast",
        "Exporting a preset writes a pretty-printed JSON file at the chosen location and confirms via toast",
        "Deleting a preset (button or swipe) removes it from the list, persists the change, and confirms via toast",
        "Each delete control exposes an accessible label naming the preset"
      ],
      "codeRefs": [
        "Sources/Waves/Settings/SettingsView.swift:210-267",
        "Sources/Waves/Stores/AppStore.swift:1066-1179"
      ],
      "implStatus": "implemented",
      "implNotes": "Thorough import validation (size/name/entry-count caps) and identity handling (fresh UUID on append, id/createdAt preserved on same-name overwrite) are solid and guard against list-identity bugs. Both export and import depend on NSApp.mainWindow being present to anchor the panel; if the main mixer window is closed while only Settings is open, exportPreset/importPreset bail with a 'No main window available' error toast (AppStore.swift:1091-1093, 1120-1122) — a real edge case where the feature is unusable from a standalone Settings window.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1H]",
      "errorsFound": "• [HIGH/edge-case] Import/Export unusable from a standalone Settings window (no main window to anchor the panel) (Sources/Waves/Stores/AppStore.swift:1091-1093) — Verified: exportPreset and importPreset both require NSApp.mainWindow to anchor the NSSavePanel/NSOpenPanel sheet and bail with a 'No main window available' error toast if it is nil (AppStore.swift:1091-1093 and 1120-1123). Waves is a menu-bar utility whose mixer is a single closable Window; Settings is opened independently (Cmd-, or the menu bar). With the main mixer window closed (a normal state), NSApp.mainWindow is nil, so neither Import nor any row's Export works - the core function of the Presets tab is dead in that state. | Fix: Fall back to NSApp.keyWindow (the Settings window) when NSApp.mainWindow is nil, or present the panel modeless via begin {}; apply the same fix to importPreset at AppStore.swift:1120-1123.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier1: FU-PANEL-WINDOW-ANCHOR",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "edge-case",
          "title": "Import/Export unusable from a standalone Settings window (no main window to anchor the panel)",
          "detail": "Verified: exportPreset and importPreset both require NSApp.mainWindow to anchor the NSSavePanel/NSOpenPanel sheet and bail with a 'No main window available' error toast if it is nil (AppStore.swift:1091-1093 and 1120-1123). Waves is a menu-bar utility whose mixer is a single closable Window; Settings is opened independently (Cmd-, or the menu bar). With the main mixer window closed (a normal state), NSApp.mainWindow is nil, so neither Import nor any row's Export works - the core function of the Presets tab is dead in that state.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1091-1093",
          "expectedVsActual": "Expected (acceptance): importing a valid preset adds/updates it and exporting writes a file, both from the Presets settings tab. Actual: with the main mixer window closed, both actions immediately fail with 'No main window available' and the feature is unusable from Settings.",
          "suggestedFix": "Fall back to NSApp.keyWindow (the Settings window) when NSApp.mainWindow is nil, or present the panel modeless via begin {}; apply the same fix to importPreset at AppStore.swift:1120-1123.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:FU-PANEL-WINDOW-ANCHOR",
          "uid": "SET-07#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-PANEL-WINDOW-ANCHOR",
          "fixTier": "tier1"
        }
      ]
    },
    {
      "id": "SET-08",
      "area": "Settings Window",
      "feature": "Advanced diagnostics report, copy-to-clipboard & unavailable state",
      "priority": "P2",
      "userStory": "As a user troubleshooting audio routing, I want an Advanced tab showing route-health diagnostics and a one-click way to copy a plain-text report, so that I can self-diagnose or file a useful bug report.",
      "expectedBehavior": [
        "DiagnosticsSettingsView shows a 'Running apps' header with store.sourceInventorySummary (e.g. 'N running apps', singular/plural aware) and refreshes diagnostics on appear (SettingsView.swift:275-289, 310-312; AppStore.swift:155-159)",
        "When diagnostics are available, each check is rendered as a card with its title and detail over an ultraThinMaterial rounded background (SettingsView.swift:291-303)",
        "A 'Copy Diagnostics' button calls store.copyDiagnosticsToPasteboard(), which builds a plain-text report (macOS version, output device, audio component installed, capture permission granted/not, route-recovery health, last error, per-app routing/volume/mute/boost lines, and each check) and copies it, toasting 'Diagnostics copied' (SettingsView.swift:283-289; AppStore.swift:725-751, 774-779)",
        "When diagnostics are nil, DiagnosticsUnavailableView shows 'Diagnostics are not loaded' plus the backend's lastError (or guidance text), with 'Refresh Diagnostics' and 'Recover Routes' buttons (SettingsView.swift:304-347; AppStore.swift:324)",
        "Diagnostics are produced by backend.diagnosticsReport() and refreshed on many state changes (start, refresh, device change, recover) (AppStore.swift:215, 246, 807, 1186, 1204-1207)"
      ],
      "acceptanceCriteria": [
        "Opening the Advanced tab triggers a diagnostics refresh and renders one card per check when a report exists",
        "The 'Running apps' summary count matches the number of visible apps and uses correct singular/plural",
        "'Copy Diagnostics' places a complete plain-text report on the clipboard including OS, device, permission status, route health, per-app state, and checks, and shows a confirmation toast",
        "When no diagnostics are loaded, the unavailable view shows the last backend error (when present) and offers Refresh and Recover actions",
        "Tapping Refresh or Recover in the unavailable state reloads/repairs and updates the panel"
      ],
      "codeRefs": [
        "Sources/Waves/Settings/SettingsView.swift:269-347",
        "Sources/Waves/Stores/AppStore.swift:155-159",
        "Sources/Waves/Stores/AppStore.swift:725-779",
        "Sources/Waves/Stores/AppStore.swift:1204-1207"
      ],
      "implStatus": "implemented",
      "implNotes": "Genuinely useful: the copied report is comprehensive and the nil/unavailable state is handled with actionable buttons and surfaces the backend's lastError. The diagnostics cards are read-only text with no severity color/icon per check status (DiagnosticsCheck.status is only emitted in the copied text via [rawValue], not visually in the cards at SettingsView.swift:291-303), so a failing check is not visually distinguished from a passing one in the UI — a minor scannability gap relative to the plain-text export.",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [1M 2L]",
      "errorsFound": "• [MEDIUM/inconsistency] Diagnostics 'unavailable' state with lastError is practically unreachable (Sources/Waves/Settings/SettingsView.swift:304-306) — Verified: DiagnosticsSettingsView shows DiagnosticsUnavailableView only when store.diagnostics is nil (SettingsView.swift:291,304-306). backend.diagnosticsReport() is non-throwing and always returns a fully-populated DiagnosticsReport with 5 checks even in degraded states (WorkspaceAudioControlBackend.swift:241-278), onAppear immediately calls refreshDiagnostics (SettingsView.swift:310-312), and grep confirms diagnostics is never set back to nil anywhere in AppStore. So the unavailable branch - the only place session.backendStatus.lastError is surfaced in this tab (SettingsView.swift:324) - is shown only during the brief async gap before the first report resolves (or if start() failed entirely and Advanced is opened first). A genuinely degraded backend still yields a report with warning/failed checks, so the user lands on the cards view, not the lastError view. | Fix: Surface session.backendStatus.lastError within the cards/available view (e.g. a prominent error banner when lastError is non-nil) rather than gating it solely on diagnostics == nil.\n• [LOW/accessibility] Diagnostics check cards do not convey status (passed/warning/failed) visually or to VoiceOver (Sources/Waves/Settings/SettingsView.swift:291-303) — Verified: each DiagnosticsCheck carries a status (passed/warning/failed/informational) and diagnosticsReport assigns meaningful statuses (WorkspaceAudioControlBackend.swift:245-275). The copied plain-text report includes it as [rawValue] (AppStore.swift:747). But the in-app cards render only check.title and check.detail over a uniform ultraThinMaterial background with no color, icon, or accessibilityLabel per status (SettingsView.swift:291-303). A failing check is visually indistinguishable from a passing one, and VoiceOver users get no status cue. | Fix: Add a status SF Symbol + accent color per check (checkmark.circle/exclamationmark.triangle/xmark.octagon) and an accessibilityLabel including the status text, mirroring the plain-text export.\n• [LOW/inconsistency] 'Running apps' count counts visible (filtered) apps, not actually running apps (Sources/Waves/Stores/AppStore.swift:155-159) — Verified: sourceInventorySummary labels the count 'running app(s)' but computes it from visibleApps.count (AppStore.swift:155-159), which excludes system-category apps when 'Show system processes' is off. The acceptance criterion explicitly ties the number to visible apps (which the code satisfies), so this is a label-wording mismatch rather than a functional defect: the header under-reports true running audio apps when system processes are hidden. | Fix: Relabel to 'Apps shown' / 'Visible apps' to match what is counted, or compute from the unfiltered running set if 'Running apps' wording is intended.",
      "fixStatus": "1 fixed · 1 deferred · 1 n/a",
      "fixNotes": "tier2: FU-DIAG-CARD-STATUS",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "inconsistency",
          "title": "Diagnostics 'unavailable' state with lastError is practically unreachable",
          "detail": "Verified: DiagnosticsSettingsView shows DiagnosticsUnavailableView only when store.diagnostics is nil (SettingsView.swift:291,304-306). backend.diagnosticsReport() is non-throwing and always returns a fully-populated DiagnosticsReport with 5 checks even in degraded states (WorkspaceAudioControlBackend.swift:241-278), onAppear immediately calls refreshDiagnostics (SettingsView.swift:310-312), and grep confirms diagnostics is never set back to nil anywhere in AppStore. So the unavailable branch - the only place session.backendStatus.lastError is surfaced in this tab (SettingsView.swift:324) - is shown only during the brief async gap before the first report resolves (or if start() failed entirely and Advanced is opened first). A genuinely degraded backend still yields a report with warning/failed checks, so the user lands on the cards view, not the lastError view.",
          "codeRef": "Sources/Waves/Settings/SettingsView.swift:304-306",
          "expectedVsActual": "Expected (acceptance): 'When no diagnostics are loaded, the unavailable view shows the last backend error (when present) and offers Refresh and Recover actions.' Actual: the backend always yields a report, so the unavailable view (and its lastError surfacing) is essentially unreachable in normal operation.",
          "suggestedFix": "Surface session.backendStatus.lastError within the cards/available view (e.g. a prominent error banner when lastError is non-nil) rather than gating it solely on diagnostics == nil.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Settings/SettingsView.swift",
          "disposition": "deferred:needs-user-decision:Real state-reachability/design fork per instructions.",
          "uid": "SET-08#1",
          "canonicalFile": "Sources/Waves/Settings/SettingsView.swift"
        },
        {
          "severity": "low",
          "type": "accessibility",
          "title": "Diagnostics check cards do not convey status (passed/warning/failed) visually or to VoiceOver",
          "detail": "Verified: each DiagnosticsCheck carries a status (passed/warning/failed/informational) and diagnosticsReport assigns meaningful statuses (WorkspaceAudioControlBackend.swift:245-275). The copied plain-text report includes it as [rawValue] (AppStore.swift:747). But the in-app cards render only check.title and check.detail over a uniform ultraThinMaterial background with no color, icon, or accessibilityLabel per status (SettingsView.swift:291-303). A failing check is visually indistinguishable from a passing one, and VoiceOver users get no status cue.",
          "codeRef": "Sources/Waves/Settings/SettingsView.swift:291-303",
          "expectedVsActual": "Expected: a scannable route-health panel where a failing check is distinguishable from a passing one (the copied report distinguishes them). Actual: all cards look identical regardless of status and expose no programmatic status cue.",
          "suggestedFix": "Add a status SF Symbol + accent color per check (checkmark.circle/exclamationmark.triangle/xmark.octagon) and an accessibilityLabel including the status text, mirroring the plain-text export.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Settings/SettingsView.swift",
          "disposition": "tier2:applied:FU-DIAG-CARD-STATUS",
          "uid": "SET-08#2",
          "canonicalFile": "Sources/Waves/Settings/SettingsView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-DIAG-CARD-STATUS",
          "fixTier": "tier2"
        },
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "'Running apps' count counts visible (filtered) apps, not actually running apps",
          "detail": "Verified: sourceInventorySummary labels the count 'running app(s)' but computes it from visibleApps.count (AppStore.swift:155-159), which excludes system-category apps when 'Show system processes' is off. The acceptance criterion explicitly ties the number to visible apps (which the code satisfies), so this is a label-wording mismatch rather than a functional defect: the header under-reports true running audio apps when system processes are hidden.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:155-159",
          "expectedVsActual": "Expected: a header that accurately describes its number. Actual: the number is the visible-apps count (correct per criterion) but labeled 'Running apps', which is misleading when system processes are filtered out.",
          "suggestedFix": "Relabel to 'Apps shown' / 'Visible apps' to match what is counted, or compute from the unfiltered running set if 'Running apps' wording is intended.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "SET-08#3",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "ONB-01",
      "area": "Onboarding Flow",
      "feature": "Setup checklist with live completion progress",
      "priority": "P1",
      "userStory": "As a new Waves user, I want a guided setup checklist that shows which prerequisites for per-app audio are met, so that I know whether Waves is ready to manage my audio and what is still missing.",
      "expectedBehavior": [
        "The Setup view shows a 'Welcome to Waves' header and renders four ordered steps: confirm managed audio support, validate output device visibility, test device switching, and enable keyboard-shortcut (Accessibility) permission (OnboardingView.swift:7-28,45-72).",
        "A ProgressView reflects completion as completedSteps/3 over the first three steps only (audioComponentInstalled, outputDeviceVisible, routeHealthReady); the Accessibility step is intentionally excluded from progress and from isFullyComplete (OnboardingView.swift:30-43).",
        "Each step row shows one of three states via icon/color: complete (green checkmark), needs-action (warning triangle + action button), or pending (gray circle), with matching accessibility labels 'Completed'/'Needs action'/'Pending' (OnboardingView.swift:185-218).",
        "When all three core steps are complete the header switches to 'You're all set!', the progress tint turns green, and a green 'Ready to use' summary card listing post-setup capabilities is shown (OnboardingView.swift:51-58,74-117).",
        "Step completion is driven by syncOnboarding mapping backend status onto OnboardingState: audioComponentInstalled, hasRequiredPermissions, AXIsProcessTrusted(), currentDevice != nil, and isRouteRecoveryHealthy (AppStore.swift:1356-1364).",
        "The checklist updates only when a refresh/recover/device-change re-runs syncOnboarding; there is no continuous polling of permission/device state (AppStore.swift:228-254,1181-1202,1356-1364)."
      ],
      "acceptanceCriteria": [
        "Given a fresh launch with no managed support, the progress bar reads 0% and steps 0-2 show the warning state.",
        "Given audioComponentInstalled, outputDeviceVisible, and routeHealthReady are all true, the header reads 'You're all set!', the bar is full and green, and the 'Ready to use' card is visible.",
        "Granting only Accessibility (step 3) does not change the progress bar or trigger the 'You're all set!' state.",
        "Each step icon and its accessibility label correctly reflect complete/needs-action/pending for the underlying flag."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Onboarding/OnboardingView.swift:7-72",
        "Sources/Waves/Features/Onboarding/OnboardingView.swift:30-43",
        "Sources/Waves/Features/Onboarding/OnboardingView.swift:74-117",
        "Sources/Waves/Features/Onboarding/OnboardingView.swift:139-219",
        "Sources/Waves/Stores/AppStore.swift:1356-1364"
      ],
      "implStatus": "implemented",
      "implNotes": "Wired and functional as a status checklist. Concern: progress is hard-coded to /3 and excludes the Accessibility step, so a user who completes that optional step sees no progress credit, and the meaning of '100%' silently ignores one of the four listed steps. State is snapshot-driven, not live, so the checklist can show stale completion until the user triggers a refresh.",
      "source": "phase1-story",
      "testStatus": "6 issue(s) [1H 2M 3L]",
      "errorsFound": "• [HIGH/correctness] Checklist shows a false \"You're all set!\" from stale persisted backendStatus on launch (Sources/Waves/Stores/AppStore.swift:100) — Verified: backendStatus (including isRouteRecoveryHealthy/isAudioComponentInstalled/hasRequiredPermissions) is fully Codable (AudioSessionSnapshot.swift:45-64) and is persisted to disk by SessionStore.save (it copies snapshot.backendStatus verbatim into the payload). At init the persisted session is loaded (AppStore.swift:83) and syncOnboarding(using: session) runs synchronously at AppStore.swift:100, seeding routeHealthReady/audioComponentInstalled/outputDeviceVisible from the stale snapshot. In start() the warm path also re-syncs from the same warmSnapshot (AppStore.swift:206) before backend.start(). Only the post-start sync at AppStore.swift:218 reflects live state. So if the previous run ended healthy but capture permission was since revoked (or the device unplugged), the Setup tab shows green checks, a full green bar, and the 'You're all set!' card for the whole async start() window (seconds). | Fix: Do not seed onboarding route/permission flags from the persisted session. Reset routeHealthReady/audioComponentInstalled/permissionsGranted to false until the first live syncOnboarding after backend.start(), or neutralize backendStatus on load (set isRouteRecoveryHealthy/isAudioComponentInstalled/hasRequiredPermissions to false) so the checklist never shows green from cached state.\n• [MEDIUM/inconsistency] Acceptance [0] cannot hold: step 1 completion is independent of managed-audio support (Sources/Waves/Stores/AppStore.swift:1360) — Confirmed. Step 1 (outputDeviceVisible) is set purely from snapshot.currentDevice != nil (AppStore.swift:1360), which is unrelated to supportsPerAppRouting. Moreover buildSnapshot always fabricates a fallback 'System Output' device when none is readable (WorkspaceAudioControlBackend.swift:1062-1070), so currentDevice is effectively never nil after a live build — meaning step 1 is essentially always green and completionProgress is at least 1/3 (~33%) even on an unsupported OS, contradicting the acceptance's '0%'. | Fix: Either correct the acceptance criterion (step 1 is intentionally orthogonal to OS support), or gate the device step's completion/credit behind audioComponentInstalled so the progress narrative matches 'no managed support => 0%'.\n• [MEDIUM/ux] The 'guided setup checklist' is never surfaced to new users (Sources/Waves/Settings/SettingsView.swift:13) — Confirmed. WavesApp launches into MainWindowView, the Settings scene, and a MenuBarExtra (WavesApp.swift:30-67); there is no first-run flag and no auto-presentation of onboarding. OnboardingView is reachable only as the 'Setup' tab in SettingsView (SettingsView.swift:13). The openSettings() calls in MainWindowView/MenuBarMixerView open Settings to its default (General) tab, not Setup. Grep confirms no hasSeenOnboarding/firstLaunch logic exists. A genuinely new user is never shown the prerequisites checklist unless they happen to open Settings and click the Setup tab. | Fix: Auto-present the Setup tab (or a dedicated onboarding window/sheet) on first launch using a persisted 'hasSeenOnboarding' flag, and/or add a visible 'Setup' affordance in the main window/menu bar when isFullyComplete is false.\n• [LOW/ux] Optional Accessibility step gives no progress credit, so completing it appears to do nothing (Sources/Waves/Features/Onboarding/OnboardingView.swift:36) — Confirmed. completionProgress and isFullyComplete count only the first three steps (OnboardingView.swift:30-43). Step 3 renders identically to the others — a warning triangle and a 'Needs action' label when ungranted (EnhancedSetupStepRow, OnboardingView.swift:188-191) — yet granting it never moves the bar and the row is not visually marked 'optional' beyond its body text. | Fix: Visually badge step 3 as 'Optional' (a chip/label distinct from the warning triangle) and/or show a separate 'optional extras' indicator, so users understand it is intentionally excluded from core progress.\n• [LOW/dead-code] Unused @State selectedStep in OnboardingView (Sources/Waves/Features/Onboarding/OnboardingView.swift:5) — Confirmed. OnboardingView declares @State private var selectedStep = 0 (OnboardingView.swift:5) and it is never read or written anywhere. Dead state. | Fix: Delete the selectedStep property.\n• [LOW/accessibility] Setup ProgressView has no accessibility label/value describing setup completion (Sources/Waves/Features/Onboarding/OnboardingView.swift:57) — Confirmed. The ProgressView (OnboardingView.swift:57-58) carries no .accessibilityLabel or .accessibilityValue, so VoiceOver announces only a generic progress indicator with no indication that this is setup progress or 'X of 3 steps complete'. (Note: the individual step icons do carry accessibilityLabel at OnboardingView.swift:191, so only the overall bar is unlabeled.) | Fix: Add .accessibilityLabel(\"Setup progress\") and .accessibilityValue(\"\\(completedSteps) of 3 steps complete\") to the ProgressView.",
      "fixStatus": "3 fixed · 3 deferred",
      "fixNotes": "tier3: FU-ONB-DEAD-SELECTEDSTEP; FU-ONB-PROGRESS-A11Y",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "correctness",
          "title": "Checklist shows a false \"You're all set!\" from stale persisted backendStatus on launch",
          "detail": "Verified: backendStatus (including isRouteRecoveryHealthy/isAudioComponentInstalled/hasRequiredPermissions) is fully Codable (AudioSessionSnapshot.swift:45-64) and is persisted to disk by SessionStore.save (it copies snapshot.backendStatus verbatim into the payload). At init the persisted session is loaded (AppStore.swift:83) and syncOnboarding(using: session) runs synchronously at AppStore.swift:100, seeding routeHealthReady/audioComponentInstalled/outputDeviceVisible from the stale snapshot. In start() the warm path also re-syncs from the same warmSnapshot (AppStore.swift:206) before backend.start(). Only the post-start sync at AppStore.swift:218 reflects live state. So if the previous run ended healthy but capture permission was since revoked (or the device unplugged), the Setup tab shows green checks, a full green bar, and the 'You're all set!' card for the whole async start() window (seconds).",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:100",
          "expectedVsActual": "Expected: the checklist reflects current readiness. Actual: it can display a fully-complete, 'You're all set!' state derived from a stale persisted backendStatus until the live backend finishes starting.",
          "suggestedFix": "Do not seed onboarding route/permission flags from the persisted session. Reset routeHealthReady/audioComponentInstalled/permissionsGranted to false until the first live syncOnboarding after backend.start(), or neutralize backendStatus on load (set isRouteRecoveryHealthy/isAudioComponentInstalled/hasRequiredPermissions to false) so the checklist never shows green from cached state.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-user-decision:Redefining checklist 'ready'/seeding semantics and backendStatus persistence is a UX decision tied to ONB-02's step restructuring; not a safe isolated edit.",
          "uid": "ONB-01#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "medium",
          "type": "inconsistency",
          "title": "Acceptance [0] cannot hold: step 1 completion is independent of managed-audio support",
          "detail": "Confirmed. Step 1 (outputDeviceVisible) is set purely from snapshot.currentDevice != nil (AppStore.swift:1360), which is unrelated to supportsPerAppRouting. Moreover buildSnapshot always fabricates a fallback 'System Output' device when none is readable (WorkspaceAudioControlBackend.swift:1062-1070), so currentDevice is effectively never nil after a live build — meaning step 1 is essentially always green and completionProgress is at least 1/3 (~33%) even on an unsupported OS, contradicting the acceptance's '0%'.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1360",
          "expectedVsActual": "Expected (per acceptance [0]): 0% and steps 0-2 all in warning state when there is no managed support. Actual: with any output device present (and one is always synthesized), step 1 is complete and the bar reads ~33% regardless of managed-audio support.",
          "suggestedFix": "Either correct the acceptance criterion (step 1 is intentionally orthogonal to OS support), or gate the device step's completion/credit behind audioComponentInstalled so the progress narrative matches 'no managed support => 0%'.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-user-decision:Resolving the orthogonality requires a product decision or a doc change outside the file.",
          "uid": "ONB-01#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "medium",
          "type": "ux",
          "title": "The 'guided setup checklist' is never surfaced to new users",
          "detail": "Confirmed. WavesApp launches into MainWindowView, the Settings scene, and a MenuBarExtra (WavesApp.swift:30-67); there is no first-run flag and no auto-presentation of onboarding. OnboardingView is reachable only as the 'Setup' tab in SettingsView (SettingsView.swift:13). The openSettings() calls in MainWindowView/MenuBarMixerView open Settings to its default (General) tab, not Setup. Grep confirms no hasSeenOnboarding/firstLaunch logic exists. A genuinely new user is never shown the prerequisites checklist unless they happen to open Settings and click the Setup tab.",
          "codeRef": "Sources/Waves/Settings/SettingsView.swift:13",
          "expectedVsActual": "Expected (per user story): a guided setup checklist presented to new users so they know whether Waves is ready. Actual: the checklist is buried as a Settings tab with no first-run prompt or entry point.",
          "suggestedFix": "Auto-present the Setup tab (or a dedicated onboarding window/sheet) on first launch using a persisted 'hasSeenOnboarding' flag, and/or add a visible 'Setup' affordance in the main window/menu bar when isFullyComplete is false.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Settings/SettingsView.swift",
          "disposition": "deferred:needs-feature:New feature requiring new store state and cross-file wiring.",
          "uid": "ONB-01#3",
          "canonicalFile": "Sources/Waves/Settings/SettingsView.swift"
        },
        {
          "severity": "low",
          "type": "ux",
          "title": "Optional Accessibility step gives no progress credit, so completing it appears to do nothing",
          "detail": "Confirmed. completionProgress and isFullyComplete count only the first three steps (OnboardingView.swift:30-43). Step 3 renders identically to the others — a warning triangle and a 'Needs action' label when ungranted (EnhancedSetupStepRow, OnboardingView.swift:188-191) — yet granting it never moves the bar and the row is not visually marked 'optional' beyond its body text.",
          "codeRef": "Sources/Waves/Features/Onboarding/OnboardingView.swift:36",
          "expectedVsActual": "Expected: completing a presented step gives visible feedback. Actual: completing the optional Accessibility step changes nothing in the progress UI and the row is not visually flagged as optional.",
          "suggestedFix": "Visually badge step 3 as 'Optional' (a chip/label distinct from the warning triangle) and/or show a separate 'optional extras' indicator, so users understand it is intentionally excluded from core progress.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Onboarding/OnboardingView.swift",
          "disposition": "gapfill:fixed",
          "uid": "ONB-01#4",
          "canonicalFile": "Sources/Waves/Features/Onboarding/OnboardingView.swift",
          "fixStatus": "applied",
          "fixedBy": "gapfill",
          "fixTier": "gapfill"
        },
        {
          "severity": "low",
          "type": "dead-code",
          "title": "Unused @State selectedStep in OnboardingView",
          "detail": "Confirmed. OnboardingView declares @State private var selectedStep = 0 (OnboardingView.swift:5) and it is never read or written anywhere. Dead state.",
          "codeRef": "Sources/Waves/Features/Onboarding/OnboardingView.swift:5",
          "expectedVsActual": "Expected: declared state is used. Actual: selectedStep is never referenced.",
          "suggestedFix": "Delete the selectedStep property.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Onboarding/OnboardingView.swift",
          "disposition": "tier3:applied:FU-ONB-DEAD-SELECTEDSTEP",
          "uid": "ONB-01#5",
          "canonicalFile": "Sources/Waves/Features/Onboarding/OnboardingView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-ONB-DEAD-SELECTEDSTEP",
          "fixTier": "tier3"
        },
        {
          "severity": "low",
          "type": "accessibility",
          "title": "Setup ProgressView has no accessibility label/value describing setup completion",
          "detail": "Confirmed. The ProgressView (OnboardingView.swift:57-58) carries no .accessibilityLabel or .accessibilityValue, so VoiceOver announces only a generic progress indicator with no indication that this is setup progress or 'X of 3 steps complete'. (Note: the individual step icons do carry accessibilityLabel at OnboardingView.swift:191, so only the overall bar is unlabeled.)",
          "codeRef": "Sources/Waves/Features/Onboarding/OnboardingView.swift:57",
          "expectedVsActual": "Expected: the overall progress is announced meaningfully to VoiceOver. Actual: the ProgressView is unlabeled and its meaning is opaque to assistive tech.",
          "suggestedFix": "Add .accessibilityLabel(\"Setup progress\") and .accessibilityValue(\"\\(completedSteps) of 3 steps complete\") to the ProgressView.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Onboarding/OnboardingView.swift",
          "disposition": "tier3:applied:FU-ONB-PROGRESS-A11Y",
          "uid": "ONB-01#6",
          "canonicalFile": "Sources/Waves/Features/Onboarding/OnboardingView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-ONB-PROGRESS-A11Y",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "ONB-02",
      "area": "Onboarding Flow",
      "feature": "Confirm managed audio support / capture readiness",
      "priority": "P0",
      "userStory": "As a user on a supported Mac, I want Waves to confirm that local Core Audio process-tap routing is available, so that I trust audio stays on my Mac and per-app routing will work.",
      "expectedBehavior": [
        "Step 0 explains Waves uses local Core Audio process taps on macOS 14.2+ and that audio stays on-device, with a 'Refresh Status' action (OnboardingView.swift:8-12).",
        "Step 0 is marked complete when onboarding.audioComponentInstalled is true, which is backendStatus.isAudioComponentInstalled == supportsPerAppRouting (macOS 14.2+ capability gate) (OnboardingView.swift:141, AppStore.swift:1357, WorkspaceAudioControlBackend.swift:1087).",
        "The action calls store.refresh(), which re-runs the backend refresh, recomputes backend status, and re-syncs onboarding, then shows a 'Library refreshed' toast (OnboardingView.swift:159-163, AppStore.swift:228-254).",
        "Actual audio-capture authorization is probed separately by creating/destroying a private process tap and is mapped to onboarding.permissionsGranted (hasRequiredPermissions), surfaced in diagnostics but not as its own onboarding step (AppStore.swift:1358, WorkspaceAudioControlBackend.swift:327-358,1088)."
      ],
      "acceptanceCriteria": [
        "On macOS 14.2+ where per-app routing is supported, tapping 'Refresh Status' marks step 0 complete.",
        "On an unsupported OS, step 0 never completes and the action only re-refreshes without offering an alternative.",
        "The capture-permission (hasRequiredPermissions) result is reflected somewhere visible to the user after a refresh."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Onboarding/OnboardingView.swift:8-12",
        "Sources/Waves/Features/Onboarding/OnboardingView.swift:141,151,159-163",
        "Sources/Waves/Stores/AppStore.swift:1357-1358",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1087-1089",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:327-358"
      ],
      "implStatus": "partial",
      "implNotes": "Step 0 only verifies OS-version capability (supportsPerAppRouting), not whether TCC audio-capture permission is granted. permissionsGranted/hasRequiredPermissions is computed and probed but is NOT its own onboarding step and has no dedicated 'grant permission' action — the title 'Confirm managed audio support' conflates capability with permission. A user who is on a supported OS but has denied capture permission can see step 0 'complete' yet still have non-functional routing (caught only later via the route-health step). On an unsupported OS the only offered action is a refresh that can never succeed, with no guidance.",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [1H 1M 1L]",
      "errorsFound": "• [HIGH/correctness] Step 0 'Confirm managed audio support' marks complete on OS-version capability alone, ignoring denied capture permission (Sources/Waves/Stores/AppStore.swift:1357) — Confirmed. Step 0 completion = audioComponentInstalled = backendStatus.isAudioComponentInstalled, which buildSnapshot sets to supportsPerAppRouting (WorkspaceAudioControlBackend.swift:1087), i.e. merely 'macOS 14.2+'. The real TCC capture grant is a separate probe mapped to hasRequiredPermissions/permissionsGranted (WorkspaceAudioControlBackend.swift:1088, refreshCaptureAuthorization at 332-357) but has no onboarding step and no 'grant permission' action. A user on a supported OS who has denied audio capture sees step 0 green while per-app routing silently does nothing. | Fix: Add a dedicated capture-permission step driven by onboarding.permissionsGranted with an 'Allow audio recording in System Settings' action, or fold permissionsGranted into step 0 so 'Confirm managed audio support' requires both supportsPerAppRouting AND captureAuthorization == .authorized.\n• [MEDIUM/edge-case] On an unsupported OS, step 0's only action is a Refresh that can never succeed and offers no guidance (Sources/Waves/Features/Onboarding/OnboardingView.swift:161) — Confirmed. On macOS < 14.2 supportsPerAppRouting is false (WorkspaceAudioControlBackend.swift:310-315), so audioComponentInstalled is permanently false and canPerformAction(0) stays true, exposing the 'Refresh Status' button (OnboardingView.swift:151). handleAction case 0 calls store.refresh() (OnboardingView.swift:161-162) which can never flip the flag. No copy explains the OS requirement. (Diagnostics text 'needs macOS 14.2 or newer' exists only in the separate Advanced tab.) | Fix: When audioComponentInstalled is false due to OS version, replace the Refresh action with an informational message ('Requires macOS 14.2 or newer') and hide the dead button.\n• [LOW/dead-code] Real capture-permission state (permissionsGranted / isReadyForEverydayUse) is computed but never consumed by the onboarding UI (Sources/Waves/Stores/AppStore.swift:1504) — New finding (missed by first tester). syncOnboarding faithfully computes onboarding.permissionsGranted from the live TCC probe (AppStore.swift:1358), and OnboardingState exposes isReadyForEverydayUse = permissionsGranted && outputDeviceVisible && routeHealthReady (AppStore.swift:1504-1506). Grep confirms permissionsGranted is read ONLY by isReadyForEverydayUse, and isReadyForEverydayUse is referenced nowhere in the codebase. The OnboardingView instead bases step 0/'fully complete' on audioComponentInstalled (OS support), so the one flag that actually tracks capture permission is dead. This is the root cause of the ONB-02 'capability vs permission' conflation: the correct signal exists but is wired to nothing. | Fix: Drive the checklist off isReadyForEverydayUse (or surface permissionsGranted as its own step), or delete the unused members. Reusing isReadyForEverydayUse would also fix the step-0 conflation in this story.",
      "fixStatus": "1 fixed · 2 deferred",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "correctness",
          "title": "Step 0 'Confirm managed audio support' marks complete on OS-version capability alone, ignoring denied capture permission",
          "detail": "Confirmed. Step 0 completion = audioComponentInstalled = backendStatus.isAudioComponentInstalled, which buildSnapshot sets to supportsPerAppRouting (WorkspaceAudioControlBackend.swift:1087), i.e. merely 'macOS 14.2+'. The real TCC capture grant is a separate probe mapped to hasRequiredPermissions/permissionsGranted (WorkspaceAudioControlBackend.swift:1088, refreshCaptureAuthorization at 332-357) but has no onboarding step and no 'grant permission' action. A user on a supported OS who has denied audio capture sees step 0 green while per-app routing silently does nothing.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1357",
          "expectedVsActual": "Expected (per user story): the user trusts per-app routing 'will work' once this step is confirmed. Actual: the step can be green while capture permission is denied; the real grant is only surfaced indirectly via the Advanced tab diagnostics.",
          "suggestedFix": "Add a dedicated capture-permission step driven by onboarding.permissionsGranted with an 'Allow audio recording in System Settings' action, or fold permissionsGranted into step 0 so 'Confirm managed audio support' requires both supportsPerAppRouting AND captureAuthorization == .authorized.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-feature:Adding a capture-permission step/action or redefining step-0 completion is a new onboarding feature and UX decision, not a behavior-preserving edit.",
          "uid": "ONB-02#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "medium",
          "type": "edge-case",
          "title": "On an unsupported OS, step 0's only action is a Refresh that can never succeed and offers no guidance",
          "detail": "Confirmed. On macOS < 14.2 supportsPerAppRouting is false (WorkspaceAudioControlBackend.swift:310-315), so audioComponentInstalled is permanently false and canPerformAction(0) stays true, exposing the 'Refresh Status' button (OnboardingView.swift:151). handleAction case 0 calls store.refresh() (OnboardingView.swift:161-162) which can never flip the flag. No copy explains the OS requirement. (Diagnostics text 'needs macOS 14.2 or newer' exists only in the separate Advanced tab.)",
          "codeRef": "Sources/Waves/Features/Onboarding/OnboardingView.swift:161",
          "expectedVsActual": "Expected: on an unsupported OS the user is told why this can't complete. Actual: only a futile 'Refresh Status' action is offered, with no copy indicating the OS requirement is unmet.",
          "suggestedFix": "When audioComponentInstalled is false due to OS version, replace the Refresh action with an informational message ('Requires macOS 14.2 or newer') and hide the dead button.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Onboarding/OnboardingView.swift",
          "disposition": "gapfill:fixed",
          "uid": "ONB-02#2",
          "canonicalFile": "Sources/Waves/Features/Onboarding/OnboardingView.swift",
          "fixStatus": "applied",
          "fixedBy": "gapfill",
          "fixTier": "gapfill"
        },
        {
          "severity": "low",
          "type": "dead-code",
          "title": "Real capture-permission state (permissionsGranted / isReadyForEverydayUse) is computed but never consumed by the onboarding UI",
          "detail": "New finding (missed by first tester). syncOnboarding faithfully computes onboarding.permissionsGranted from the live TCC probe (AppStore.swift:1358), and OnboardingState exposes isReadyForEverydayUse = permissionsGranted && outputDeviceVisible && routeHealthReady (AppStore.swift:1504-1506). Grep confirms permissionsGranted is read ONLY by isReadyForEverydayUse, and isReadyForEverydayUse is referenced nowhere in the codebase. The OnboardingView instead bases step 0/'fully complete' on audioComponentInstalled (OS support), so the one flag that actually tracks capture permission is dead. This is the root cause of the ONB-02 'capability vs permission' conflation: the correct signal exists but is wired to nothing.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1504",
          "expectedVsActual": "Expected: the onboarding readiness logic uses the real capture-permission flag it already computes. Actual: permissionsGranted/isReadyForEverydayUse are dead code; the UI uses OS-support capability instead, so a permission denial is invisible in the checklist.",
          "suggestedFix": "Drive the checklist off isReadyForEverydayUse (or surface permissionsGranted as its own step), or delete the unused members. Reusing isReadyForEverydayUse would also fix the step-0 conflation in this story.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:This is a product/design decision (delete vs. surface the permission step). Option (b) materially changes onboarding 'complete' semantics (capability-vs-permission conflation, ONB-02's core), which is exactly the kind of behavior change that needs an owner decision, not a drive-by fix. Even the delete-only path should be confirmed against the intended onboarding model rather than removed unilaterally.",
          "uid": "ONB-02#3",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "ONB-03",
      "area": "Onboarding Flow",
      "feature": "Validate output device visibility",
      "priority": "P1",
      "userStory": "As a user, I want Waves to confirm it can see an audio output device, so that I know managed routes can be created for my speakers or headphones.",
      "expectedBehavior": [
        "Step 1 instructs the user to ensure speakers/headphones are connected and recognized, with a 'Refresh Devices' action (OnboardingView.swift:14-17).",
        "Step 1 completes when onboarding.outputDeviceVisible is true, set from snapshot.currentDevice != nil (OnboardingView.swift:142, AppStore.swift:1360).",
        "The action calls store.refresh(), re-acquiring the current device and re-syncing onboarding (OnboardingView.swift:164, AppStore.swift:228-254).",
        "OnboardingState.outputDeviceVisible defaults to true before any sync, so the initial pre-refresh state optimistically assumes a device is present (AppStore.swift:1500)."
      ],
      "acceptanceCriteria": [
        "With a recognized output device, step 1 shows complete after the initial sync/refresh.",
        "With no current output device, step 1 shows the needs-action state and 'Refresh Devices' re-checks.",
        "Disconnecting all output devices and refreshing flips step 1 back to needs-action."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Onboarding/OnboardingView.swift:14-17",
        "Sources/Waves/Features/Onboarding/OnboardingView.swift:142,152,164",
        "Sources/Waves/Stores/AppStore.swift:1360",
        "Sources/Waves/Stores/AppStore.swift:1500"
      ],
      "implStatus": "implemented",
      "implNotes": "Straightforward and correct. Minor UX concern: outputDeviceVisible defaults to true (AppStore.swift:1500), so on the very first render before syncOnboarding completes the step can briefly appear satisfied; in practice the initializer calls syncOnboarding immediately (AppStore.swift:100) so the window is tiny.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/edge-case] outputDeviceVisible defaults to true, so step 1 can momentarily show 'complete' before the first sync (Sources/Waves/Stores/AppStore.swift:1500) — Confirmed but narrow. OnboardingState.outputDeviceVisible defaults to true (AppStore.swift:1500). In the init path, OnboardingState is constructed at AppStore.swift:96-98 and then syncOnboarding(using:) runs synchronously at AppStore.swift:100, so the optimistic default is overwritten before any view renders — the real exposure window is essentially zero in the normal flow. The semantic concern (asserting a device exists before anything checked) stands, but practically the default is masked immediately. Low severity is correct. | Fix: Default outputDeviceVisible to false to match the other flags' semantics, so the step starts in needs-action until syncOnboarding confirms a device.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier3: FU-ONBOARDING-DEVICE-DEFAULT",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "edge-case",
          "title": "outputDeviceVisible defaults to true, so step 1 can momentarily show 'complete' before the first sync",
          "detail": "Confirmed but narrow. OnboardingState.outputDeviceVisible defaults to true (AppStore.swift:1500). In the init path, OnboardingState is constructed at AppStore.swift:96-98 and then syncOnboarding(using:) runs synchronously at AppStore.swift:100, so the optimistic default is overwritten before any view renders — the real exposure window is essentially zero in the normal flow. The semantic concern (asserting a device exists before anything checked) stands, but practically the default is masked immediately. Low severity is correct.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1500",
          "expectedVsActual": "Expected: device visibility is unknown/false until verified. Actual: it defaults to true, but is overwritten synchronously at init, so the unverified 'device present' state is not actually observable in the normal launch path.",
          "suggestedFix": "Default outputDeviceVisible to false to match the other flags' semantics, so the step starts in needs-action until syncOnboarding confirms a device.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier3:applied:FU-ONBOARDING-DEVICE-DEFAULT",
          "uid": "ONB-03#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-ONBOARDING-DEVICE-DEFAULT",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "ONB-04",
      "area": "Onboarding Flow",
      "feature": "Test device switching / route recovery",
      "priority": "P0",
      "userStory": "As a user who switches between speakers and headphones, I want to verify Waves automatically re-establishes managed routes after a device change, so that I trust audio keeps working without restarting the app.",
      "expectedBehavior": [
        "Step 2 explains routes are re-established automatically on device switches and offers a 'Test Recovery' action (OnboardingView.swift:18-22).",
        "Step 2 completes when onboarding.routeHealthReady is true, from backendStatus.isRouteRecoveryHealthy (supportsPerAppRouting && capture authorized && no route errors) (OnboardingView.swift:143, AppStore.swift:1361, WorkspaceAudioControlBackend.swift:1089).",
        "The action is only enabled when a device is visible but route health is not yet ready (canPerformAction case 2), preventing a recovery attempt with no device (OnboardingView.swift:153).",
        "'Test Recovery' calls store.recoverRoutes(), which reattaches managed routing paths, re-syncs onboarding, and shows a 'Routes recovered' success toast; on failure it reloads the current snapshot and shows a 'Recovery failed' error toast (OnboardingView.swift:165-166, AppStore.swift:1181-1202)."
      ],
      "acceptanceCriteria": [
        "Step 2's action button is disabled/hidden until step 1 (device visible) is satisfied.",
        "Tapping 'Test Recovery' on a healthy system marks step 2 complete and shows a success toast.",
        "If recovery throws, step 2 stays incomplete and an error toast with the failure detail is shown.",
        "After a real output-device change, route health re-syncs and step 2 reflects the new state."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Onboarding/OnboardingView.swift:18-22",
        "Sources/Waves/Features/Onboarding/OnboardingView.swift:143,153,165-166",
        "Sources/Waves/Stores/AppStore.swift:1181-1202",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1089"
      ],
      "implStatus": "implemented",
      "implNotes": "Well-gated (action only enabled once a device is visible) and has explicit success/failure toasts. Because routeHealthReady also requires capture authorization, a permission-denied user will see this step stuck on 'needs action' even though the failure root-cause (no capture permission) is never named in the onboarding UI — the user is told to 'Test Recovery' rather than to grant permission.",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [1H 2M]",
      "errorsFound": "• [HIGH/error-handling] 'Test Recovery' shows a green 'Routes recovered' success toast even when route health is still not ready (e.g. capture permission denied) (Sources/Waves/Stores/AppStore.swift:1189) — Confirmed. backend.recoverRoutes() optimistically sets isRouteRecoveryHealthy = true (WorkspaceAudioControlBackend.swift:212) but then rebuilds the snapshot via buildSnapshot (line 214), which recomputes isRouteRecoveryHealthy = supportsPerAppRouting && captureAuthorization == .authorized && !hasRouteErrors (line 1089). If capture is not authorized the returned snapshot has routeHealthReady = false, and recoverRoutes does NOT throw. Back in AppStore.recoverRoutes the no-throw path runs syncOnboarding (which sets routeHealthReady=false, AppStore.swift:1188) and then unconditionally shows the 'Routes recovered' success toast (AppStore.swift:1189-1193). Net: green success toast while step 2 stays on 'needs action' with the button still present — contradicts acceptance [1] and defeats acceptance [2] (the common failure never throws). | Fix: After recoverRoutes() returns, branch on the resulting onboarding.routeHealthReady (or returned backendStatus): show success only if route health is now ready; otherwise show a warning/error toast naming the unmet prerequisite (capture permission denied / no device) and pointing the user to grant capture permission.\n• [MEDIUM/edge-case] Device-change route-health re-sync is skipped entirely when 'auto-restore device' is disabled (Sources/Waves/Stores/AppStore.swift:793) — Confirmed. handleDeviceChange() is the sole device-change handler (observeDeviceChanges wires only it, AppStore.swift:781-789) and it early-returns at AppStore.swift:793 when preferences.autoRestoreDevice is false, before reaching syncOnboarding at AppStore.swift:809. autoRestoreDevice is a user toggle (default true, toggled at SettingsView.swift:78-86). With it off, a real output-device change does not re-sync onboarding, so step 2/step 1 go stale until a manual Refresh — contradicting acceptance [3] and the step's own 'no restart needed' copy. | Fix: Always re-read the snapshot and re-sync onboarding/diagnostics on a device-change event; gate only the route re-establishment behavior on autoRestoreDevice.\n• [MEDIUM/ux] Permission-denied user is told to 'Test Recovery' instead of to grant capture permission (Sources/Waves/Features/Onboarding/OnboardingView.swift:166) — Confirmed. routeHealthReady requires captureAuthorization == .authorized (WorkspaceAudioControlBackend.swift:1089). When capture is denied, step 2 sits on 'needs action' offering 'Test Recovery' (canPerformAction case 2 / handleAction case 2 -> recoverRoutes, OnboardingView.swift:153,165-166), but recovery cannot create a TCC grant, so the user loops on a button that addresses the wrong root cause. The onboarding UI never names 'audio capture permission' (only the Advanced/diagnostics tab does). | Fix: When routeHealthReady is false specifically because captureAuthorization != .authorized, change the step's action/detail to 'Allow audio recording in System Settings' (open the Microphone privacy pane) instead of 'Test Recovery'.",
      "fixStatus": "3 fixed",
      "fixNotes": "tier1: FU-DEVICECHANGE-GATING",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "error-handling",
          "title": "'Test Recovery' shows a green 'Routes recovered' success toast even when route health is still not ready (e.g. capture permission denied)",
          "detail": "Confirmed. backend.recoverRoutes() optimistically sets isRouteRecoveryHealthy = true (WorkspaceAudioControlBackend.swift:212) but then rebuilds the snapshot via buildSnapshot (line 214), which recomputes isRouteRecoveryHealthy = supportsPerAppRouting && captureAuthorization == .authorized && !hasRouteErrors (line 1089). If capture is not authorized the returned snapshot has routeHealthReady = false, and recoverRoutes does NOT throw. Back in AppStore.recoverRoutes the no-throw path runs syncOnboarding (which sets routeHealthReady=false, AppStore.swift:1188) and then unconditionally shows the 'Routes recovered' success toast (AppStore.swift:1189-1193). Net: green success toast while step 2 stays on 'needs action' with the button still present — contradicts acceptance [1] and defeats acceptance [2] (the common failure never throws).",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1189",
          "expectedVsActual": "Expected: success toast only when recovery actually makes the route healthy; otherwise an error/warning toast naming the cause. Actual: 'Routes recovered' is shown unconditionally on no-throw, even when routeHealthReady is still false.",
          "suggestedFix": "After recoverRoutes() returns, branch on the resulting onboarding.routeHealthReady (or returned backendStatus): show success only if route health is now ready; otherwise show a warning/error toast naming the unmet prerequisite (capture permission denied / no device) and pointing the user to grant capture permission.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "iter2:fixed",
          "uid": "ONB-04#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "iter2-deferred-reduction",
          "fixTier": "iter2"
        },
        {
          "severity": "medium",
          "type": "edge-case",
          "title": "Device-change route-health re-sync is skipped entirely when 'auto-restore device' is disabled",
          "detail": "Confirmed. handleDeviceChange() is the sole device-change handler (observeDeviceChanges wires only it, AppStore.swift:781-789) and it early-returns at AppStore.swift:793 when preferences.autoRestoreDevice is false, before reaching syncOnboarding at AppStore.swift:809. autoRestoreDevice is a user toggle (default true, toggled at SettingsView.swift:78-86). With it off, a real output-device change does not re-sync onboarding, so step 2/step 1 go stale until a manual Refresh — contradicting acceptance [3] and the step's own 'no restart needed' copy.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:793",
          "expectedVsActual": "Expected: route health (and the checklist) re-syncs after every device change. Actual: when auto-restore is disabled, the handler returns before syncOnboarding, so the checklist goes stale until a manual refresh.",
          "suggestedFix": "Always re-read the snapshot and re-sync onboarding/diagnostics on a device-change event; gate only the route re-establishment behavior on autoRestoreDevice.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:FU-DEVICECHANGE-GATING",
          "uid": "ONB-04#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-DEVICECHANGE-GATING",
          "fixTier": "tier1"
        },
        {
          "severity": "medium",
          "type": "ux",
          "title": "Permission-denied user is told to 'Test Recovery' instead of to grant capture permission",
          "detail": "Confirmed. routeHealthReady requires captureAuthorization == .authorized (WorkspaceAudioControlBackend.swift:1089). When capture is denied, step 2 sits on 'needs action' offering 'Test Recovery' (canPerformAction case 2 / handleAction case 2 -> recoverRoutes, OnboardingView.swift:153,165-166), but recovery cannot create a TCC grant, so the user loops on a button that addresses the wrong root cause. The onboarding UI never names 'audio capture permission' (only the Advanced/diagnostics tab does).",
          "codeRef": "Sources/Waves/Features/Onboarding/OnboardingView.swift:166",
          "expectedVsActual": "Expected: when the blocker is a missing capture permission, the step directs the user to grant it. Actual: it offers 'Test Recovery', which cannot resolve a permission denial.",
          "suggestedFix": "When routeHealthReady is false specifically because captureAuthorization != .authorized, change the step's action/detail to 'Allow audio recording in System Settings' (open the Microphone privacy pane) instead of 'Test Recovery'.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Onboarding/OnboardingView.swift",
          "disposition": "gapfill:fixed",
          "uid": "ONB-04#3",
          "canonicalFile": "Sources/Waves/Features/Onboarding/OnboardingView.swift",
          "fixStatus": "applied",
          "fixedBy": "gapfill",
          "fixTier": "gapfill"
        }
      ]
    },
    {
      "id": "ONB-05",
      "area": "Onboarding Flow",
      "feature": "Enable Accessibility permission for global shortcuts",
      "priority": "P2",
      "userStory": "As a user who wants global keyboard shortcuts, I want a step that helps me grant Accessibility permission, so that Waves can control volume system-wide; and I want it clearly marked optional since routing works without it.",
      "expectedBehavior": [
        "Step 3 states Accessibility is optional and that per-app routing works without it, with an 'Open System Settings' action (OnboardingView.swift:24-27).",
        "Step 3 completion reflects onboarding.accessibilityPermissionGranted, set from AXIsProcessTrusted() during syncOnboarding (OnboardingView.swift:144, AppStore.swift:1359).",
        "The action opens the Accessibility pane via the x-apple.systempreferences Privacy_Accessibility URL using NSWorkspace (OnboardingView.swift:167-170).",
        "Step 3 is deliberately excluded from completionProgress and isFullyComplete, so it does not block the 'You're all set!' state (OnboardingView.swift:30-43)."
      ],
      "acceptanceCriteria": [
        "Tapping 'Open System Settings' opens the macOS Accessibility privacy pane.",
        "After granting Accessibility and triggering a refresh, step 3 shows complete via AXIsProcessTrusted().",
        "Leaving Accessibility ungranted does not prevent the checklist from reaching the fully-complete state."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Onboarding/OnboardingView.swift:24-27",
        "Sources/Waves/Features/Onboarding/OnboardingView.swift:144,154,167-170",
        "Sources/Waves/Stores/AppStore.swift:1359"
      ],
      "implStatus": "partial",
      "implNotes": "Opens the right settings pane but does no live re-check: AXIsProcessTrusted() is only re-evaluated on the next syncOnboarding (refresh/recover/device-change), so after the user grants permission in System Settings the step keeps showing 'needs action' until they manually hit a Refresh button — there is no AXIsProcessTrustedWithOptions prompt or polling. The step's value also depends on enableKeyboardShortcuts being on (default true), which is configured elsewhere in General settings, not surfaced here.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [2L]",
      "errorsFound": "• [LOW/ux] Step 3 does not live re-check Accessibility; it stays 'needs action' until a manual refresh (Sources/Waves/Features/Onboarding/OnboardingView.swift:167) — Confirmed. handleAction case 3 only opens the Accessibility pane via NSWorkspace (OnboardingView.swift:167-170) with no prompt or polling. accessibilityPermissionGranted is re-evaluated via AXIsProcessTrusted() only inside syncOnboarding (AppStore.swift:1359), which runs on refresh/recover/device-change. After granting Accessibility and returning to Waves, the step keeps showing 'needs action' until a manual Refresh. Acceptance [1] only requires correctness after a refresh, so this is a UX shortfall rather than an acceptance failure. | Fix: Re-run syncOnboarding when the app/window becomes active (didBecomeActiveNotification / scenePhase active), or use AXIsProcessTrustedWithOptions(prompt:true) so the grant is reflected promptly.\n• [LOW/inconsistency] Step 3 ignores the enableKeyboardShortcuts preference it implicitly depends on (Sources/Waves/Features/Onboarding/OnboardingView.swift:144) — Confirmed. The step's value is enabling global shortcuts, which also require preferences.enableKeyboardShortcuts (default true; gated in increase/decrease/toggle handlers, e.g. AppStore.swift:1211,1230,1249). The step checks only AXIsProcessTrusted() (isStepComplete case 3 -> accessibilityPermissionGranted, OnboardingView.swift:144) and neither checks nor surfaces the toggle. A user who disabled keyboard shortcuts can grant Accessibility, see the step 'complete', yet shortcuts still won't fire, with no hint that the General-settings toggle governs the feature. | Fix: Mention (or surface a toggle for) enableKeyboardShortcuts within this step, or note that shortcuts must also be enabled in General settings for the grant to take effect.",
      "fixStatus": "2 fixed",
      "fixNotes": "",
      "retestStatus": "PASS (remediated & verified)",
      "issues": [
        {
          "severity": "low",
          "type": "ux",
          "title": "Step 3 does not live re-check Accessibility; it stays 'needs action' until a manual refresh",
          "detail": "Confirmed. handleAction case 3 only opens the Accessibility pane via NSWorkspace (OnboardingView.swift:167-170) with no prompt or polling. accessibilityPermissionGranted is re-evaluated via AXIsProcessTrusted() only inside syncOnboarding (AppStore.swift:1359), which runs on refresh/recover/device-change. After granting Accessibility and returning to Waves, the step keeps showing 'needs action' until a manual Refresh. Acceptance [1] only requires correctness after a refresh, so this is a UX shortfall rather than an acceptance failure.",
          "codeRef": "Sources/Waves/Features/Onboarding/OnboardingView.swift:167",
          "expectedVsActual": "Expected: after granting Accessibility, the step updates without an explicit manual refresh. Actual: it requires the user to press Refresh to re-evaluate AXIsProcessTrusted().",
          "suggestedFix": "Re-run syncOnboarding when the app/window becomes active (didBecomeActiveNotification / scenePhase active), or use AXIsProcessTrustedWithOptions(prompt:true) so the grant is reflected promptly.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Onboarding/OnboardingView.swift",
          "disposition": "gapfill:fixed",
          "uid": "ONB-05#1",
          "canonicalFile": "Sources/Waves/Features/Onboarding/OnboardingView.swift",
          "fixStatus": "applied",
          "fixedBy": "gapfill",
          "fixTier": "gapfill"
        },
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "Step 3 ignores the enableKeyboardShortcuts preference it implicitly depends on",
          "detail": "Confirmed. The step's value is enabling global shortcuts, which also require preferences.enableKeyboardShortcuts (default true; gated in increase/decrease/toggle handlers, e.g. AppStore.swift:1211,1230,1249). The step checks only AXIsProcessTrusted() (isStepComplete case 3 -> accessibilityPermissionGranted, OnboardingView.swift:144) and neither checks nor surfaces the toggle. A user who disabled keyboard shortcuts can grant Accessibility, see the step 'complete', yet shortcuts still won't fire, with no hint that the General-settings toggle governs the feature.",
          "codeRef": "Sources/Waves/Features/Onboarding/OnboardingView.swift:144",
          "expectedVsActual": "Expected: the Accessibility step reflects whether global shortcuts will actually work. Actual: it reflects only AXIsProcessTrusted(), independent of the enableKeyboardShortcuts preference it depends on.",
          "suggestedFix": "Mention (or surface a toggle for) enableKeyboardShortcuts within this step, or note that shortcuts must also be enabled in General settings for the grant to take effect.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Onboarding/OnboardingView.swift",
          "disposition": "gapfill:fixed",
          "uid": "ONB-05#2",
          "canonicalFile": "Sources/Waves/Features/Onboarding/OnboardingView.swift",
          "fixStatus": "applied",
          "fixedBy": "gapfill",
          "fixTier": "gapfill"
        }
      ]
    },
    {
      "id": "ONB-06",
      "area": "Onboarding Flow",
      "feature": "Quick actions: global refresh and enable launch-at-login from setup",
      "priority": "P2",
      "userStory": "As a user finishing setup, I want quick buttons to re-check status and to enable launch-at-login, so that I can finalize my configuration without leaving the setup screen.",
      "expectedBehavior": [
        "A prominent 'Refresh Status' button calls store.refresh() and store.refreshDiagnostics(), updating both the session-derived onboarding flags and the diagnostics report (OnboardingView.swift:121-126, AppStore.swift:228-254,1204-1208).",
        "An 'Enable Launch at Login' button is shown only while onboarding.launchAtLoginEnabled is false; tapping it sets store.launchAtLoginEnabled = true (OnboardingView.swift:128-133).",
        "Setting launchAtLoginEnabled registers the login item via LoginItemService, persists preferences, and re-syncs onboarding.launchAtLoginEnabled; if the OS reports the item still disabled (needs approval) a 'Login item needs approval' warning toast is shown, and registration errors show a failure toast (AppStore.swift:161-186).",
        "launchAtLoginEnabled state is also re-synced from loginItemService.status on every syncOnboarding so the button visibility tracks the real system state (AppStore.swift:1362-1364)."
      ],
      "acceptanceCriteria": [
        "'Refresh Status' re-runs backend refresh and diagnostics and updates all step states.",
        "The 'Enable Launch at Login' button disappears once launch-at-login is actually enabled.",
        "If enabling launch-at-login requires user approval, a warning toast appears and the button remains until approval is granted.",
        "A registration error surfaces an error toast and does not crash."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Onboarding/OnboardingView.swift:121-134",
        "Sources/Waves/Stores/AppStore.swift:161-186",
        "Sources/Waves/Stores/AppStore.swift:228-254",
        "Sources/Waves/Stores/AppStore.swift:1362-1364"
      ],
      "implStatus": "implemented",
      "implNotes": "Both quick actions are fully wired with proper toasts and state re-sync. Note the top 'Refresh Status' button duplicates step 0's 'Refresh Status' action label, which may confuse users about scope. There is no explicit empty/error state in onboarding for a backend that fails to start — failures surface only as transient toasts, and the checklist simply shows steps as incomplete.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1M 1L]",
      "errorsFound": "• [MEDIUM/error-handling] No onboarding state for a backend that fails to start; failures appear only as a transient toast (Sources/Waves/Stores/AppStore.swift:221) — Confirmed, and raised from 'likely' to confirmed / low->medium. If backend.start() throws, the catch resets isBootstrapped=false and shows only a transient 'Startup failed' toast (AppStore.swift:221-224); syncOnboarding is never reached on that path, so onboarding flags keep whatever stale/seeded values they had and the Setup tab simply renders steps incomplete with no failure context. Worse, because isBootstrapped was reset to false, the Setup tab's bottom 'Refresh Status' -> store.refresh() is gated by isRefreshing/started-backend assumptions and there is no visible retry of start(); the persistent lastError is only shown in the separate Advanced/diagnostics tab (SettingsView.swift:324). The user has no in-Setup indication that the backend never started, nor a clear recovery path. | Fix: Surface session.backendStatus.lastError (or a dedicated 'backend failed to start' banner) within OnboardingView when start() failed, with an explicit retry affordance that re-invokes start().\n• [LOW/ux] Two different 'Refresh Status' buttons with the same label but different scope (Sources/Waves/Features/Onboarding/OnboardingView.swift:122) — Confirmed. The bottom quick-action 'Refresh Status' calls both store.refresh() and store.refreshDiagnostics() (OnboardingView.swift:122-125), while step 0's needs-action button is also labeled 'Refresh Status' (steps array, OnboardingView.swift:11) but handleAction case 0 calls only store.refresh() (OnboardingView.swift:161-162). Identical labels for actions of different scope can confuse users. | Fix: Rename the bottom quick action (e.g. 'Re-check Everything' / 'Refresh All'), or have step 0 also call refreshDiagnostics() so the two truly match.",
      "fixStatus": "1 fixed · 1 deferred",
      "fixNotes": "tier3: FU-ONB-REFRESH-LABEL-DUP",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "error-handling",
          "title": "No onboarding state for a backend that fails to start; failures appear only as a transient toast",
          "detail": "Confirmed, and raised from 'likely' to confirmed / low->medium. If backend.start() throws, the catch resets isBootstrapped=false and shows only a transient 'Startup failed' toast (AppStore.swift:221-224); syncOnboarding is never reached on that path, so onboarding flags keep whatever stale/seeded values they had and the Setup tab simply renders steps incomplete with no failure context. Worse, because isBootstrapped was reset to false, the Setup tab's bottom 'Refresh Status' -> store.refresh() is gated by isRefreshing/started-backend assumptions and there is no visible retry of start(); the persistent lastError is only shown in the separate Advanced/diagnostics tab (SettingsView.swift:324). The user has no in-Setup indication that the backend never started, nor a clear recovery path.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:221",
          "expectedVsActual": "Expected: a backend-start failure is reflected persistently in the Setup UI with guidance/retry. Actual: it surfaces only as a brief toast; the checklist just shows steps incomplete with no error context, and recovery from the Setup tab is unclear.",
          "suggestedFix": "Surface session.backendStatus.lastError (or a dedicated 'backend failed to start' banner) within OnboardingView when start() failed, with an explicit retry affordance that re-invokes start().",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-feature:The retry banner is a new UI surface in OnboardingView.swift, outside the allowed file.",
          "uid": "ONB-06#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "low",
          "type": "ux",
          "title": "Two different 'Refresh Status' buttons with the same label but different scope",
          "detail": "Confirmed. The bottom quick-action 'Refresh Status' calls both store.refresh() and store.refreshDiagnostics() (OnboardingView.swift:122-125), while step 0's needs-action button is also labeled 'Refresh Status' (steps array, OnboardingView.swift:11) but handleAction case 0 calls only store.refresh() (OnboardingView.swift:161-162). Identical labels for actions of different scope can confuse users.",
          "codeRef": "Sources/Waves/Features/Onboarding/OnboardingView.swift:122",
          "expectedVsActual": "Expected: distinct controls have distinct, scope-clear labels. Actual: two buttons share the label 'Refresh Status' but do different amounts of work.",
          "suggestedFix": "Rename the bottom quick action (e.g. 'Re-check Everything' / 'Refresh All'), or have step 0 also call refreshDiagnostics() so the two truly match.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Onboarding/OnboardingView.swift",
          "disposition": "tier3:applied:FU-ONB-REFRESH-LABEL-DUP",
          "uid": "ONB-06#2",
          "canonicalFile": "Sources/Waves/Features/Onboarding/OnboardingView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-ONB-REFRESH-LABEL-DUP",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "HELP-01",
      "area": "In-App Help",
      "feature": "Help reference accessible from Settings",
      "priority": "P3",
      "userStory": "As a new Waves user, I want an in-app help reference reachable from the app's settings, so that I can learn how to use the mixer without leaving the app or finding external docs.",
      "expectedBehavior": [
        "A 'Help' tab appears in the Settings window TabView with a question-mark-circle icon (SettingsView.swift:33-36).",
        "Selecting the tab renders HelpView, a vertically scrolling document with six sections: header, Quick Start, Keyboard Shortcuts, URL Scheme Automation, Presets, Troubleshooting (HelpView.swift:8-18).",
        "Content is laid out in a ScrollView so long help text remains reachable on small windows (HelpView.swift:8, 19).",
        "The header shows the title 'Waves Help' and a subtitle 'Your comprehensive guide to Waves audio control' (HelpView.swift:24-28).",
        "Help is only reachable through the Settings scene; there is no standalone Help window, sheet, or macOS Help-menu entry wired (only reference is SettingsView.swift:33)."
      ],
      "acceptanceCriteria": [
        "Opening Settings (Cmd+,) and clicking the Help tab shows the help document.",
        "All six sections render and the view scrolls when content exceeds the window height.",
        "No crash or blank tab when the Help tab is selected with no audio apps running (content is fully static).",
        "There is no other entry point to Help besides the Settings tab."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Help/HelpView.swift:4-20",
        "Sources/Waves/Features/Help/HelpView.swift:22-30",
        "Sources/Waves/Settings/SettingsView.swift:33-36"
      ],
      "implStatus": "implemented",
      "implNotes": "Wired and renders correctly as a Settings tab. Two concerns: (1) HelpView declares @Environment(\\.dismiss) at HelpView.swift:5 but never uses it — dead code, and there is no dismiss/close affordance because the view lives inside a tab. (2) Help is buried as the last Settings tab with no Help-menu item or toolbar shortcut, so discoverability is low for a 'comprehensive guide'. README.md:264 directs users to 'the in-app diagnostics panel or check the troubleshooting section', implying Help is meant to be a primary support surface, which the current placement undercuts.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [2L]",
      "errorsFound": "• [LOW/dead-code] Unused @Environment(\\.dismiss) in HelpView (dead code, no close affordance) (Sources/Waves/Features/Help/HelpView.swift:5) — HelpView declares `@Environment(\\.dismiss) private var dismiss` at line 5 but never references `dismiss` anywhere in the file (verified by reading all 250 lines). HelpView is embedded as a Settings TabView tab (SettingsView.swift:33-36); there is no dismiss/close button, so the environment value is meaningless here and the declaration is dead. | Fix: Delete line 5 (`@Environment(\\.dismiss) private var dismiss`). The Help tab is dismissed by closing the Settings window, so no dismiss handling is needed.\n• [LOW/ux] Help is buried as the last Settings tab with no Help-menu / toolbar entry point (Sources/Waves/App/WavesApp.swift:43-48) — The only path to Help is Settings (⌘,) → last of six tabs (SettingsView.swift:33-36). WavesApp.swift only registers a CommandGroup(after: .toolbar) for 'Refresh' (lines 43-48); the default .help command group is never replaced, so macOS shows only an auto-generated empty Help menu (search field) with no 'Waves Help' entry. There is no toolbar '?' button and no standalone Help window/sheet. For a surface titled 'Your comprehensive guide to Waves audio control', discoverability is low. (Note: README.md:264 points users to the in-app diagnostics panel / its own troubleshooting section, not explicitly to in-app Help, so the README dependency the first tester implied is weaker than stated — but the discoverability gap itself is real.) | Fix: Add a standard Help menu command (CommandGroup(replacing: .help)) that calls openSettings() and selects the Help tab, or add a '?' toolbar item in MainWindowView that opens Settings to Help.",
      "fixStatus": "2 fixed",
      "fixNotes": "tier3: FU-HELP-DEAD-DISMISS",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "dead-code",
          "title": "Unused @Environment(\\.dismiss) in HelpView (dead code, no close affordance)",
          "detail": "HelpView declares `@Environment(\\.dismiss) private var dismiss` at line 5 but never references `dismiss` anywhere in the file (verified by reading all 250 lines). HelpView is embedded as a Settings TabView tab (SettingsView.swift:33-36); there is no dismiss/close button, so the environment value is meaningless here and the declaration is dead.",
          "codeRef": "Sources/Waves/Features/Help/HelpView.swift:5",
          "expectedVsActual": "Expected: every declared property is used. Actual: `dismiss` is declared and never used; it implies a closeable surface that does not exist for a Settings tab.",
          "suggestedFix": "Delete line 5 (`@Environment(\\.dismiss) private var dismiss`). The Help tab is dismissed by closing the Settings window, so no dismiss handling is needed.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Help/HelpView.swift",
          "disposition": "tier3:applied:FU-HELP-DEAD-DISMISS",
          "uid": "HELP-01#1",
          "canonicalFile": "Sources/Waves/Features/Help/HelpView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-HELP-DEAD-DISMISS",
          "fixTier": "tier3"
        },
        {
          "severity": "low",
          "type": "ux",
          "title": "Help is buried as the last Settings tab with no Help-menu / toolbar entry point",
          "detail": "The only path to Help is Settings (⌘,) → last of six tabs (SettingsView.swift:33-36). WavesApp.swift only registers a CommandGroup(after: .toolbar) for 'Refresh' (lines 43-48); the default .help command group is never replaced, so macOS shows only an auto-generated empty Help menu (search field) with no 'Waves Help' entry. There is no toolbar '?' button and no standalone Help window/sheet. For a surface titled 'Your comprehensive guide to Waves audio control', discoverability is low. (Note: README.md:264 points users to the in-app diagnostics panel / its own troubleshooting section, not explicitly to in-app Help, so the README dependency the first tester implied is weaker than stated — but the discoverability gap itself is real.)",
          "codeRef": "Sources/Waves/App/WavesApp.swift:43-48",
          "expectedVsActual": "The story explicitly accepts Settings-only reachability (acceptance: 'There is no other entry point to Help besides the Settings tab'), so this is a discoverability UX observation rather than a contract violation. Actual: a single buried entry for a comprehensive guide.",
          "suggestedFix": "Add a standard Help menu command (CommandGroup(replacing: .help)) that calls openSettings() and selects the Help tab, or add a '?' toolbar item in MainWindowView that opens Settings to Help.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/App/WavesApp.swift",
          "disposition": "iter3:implemented",
          "uid": "HELP-01#2",
          "canonicalFile": "Sources/Waves/App/WavesApp.swift",
          "fixStatus": "applied",
          "fixedBy": "iter3-ui-parity",
          "fixTier": "iter3"
        }
      ]
    },
    {
      "id": "HELP-02",
      "area": "In-App Help",
      "feature": "Quick Start onboarding steps",
      "priority": "P2",
      "userStory": "As a first-time user, I want a short numbered quick-start walkthrough, so that I immediately understand the core actions (detect apps, adjust volume, mute, pin).",
      "expectedBehavior": [
        "Quick Start renders four numbered steps in a material-backed card (HelpView.swift:32-62).",
        "Step 1 'Launch Waves' states apps are auto-detected; Step 2 'Adjust Volumes' describes per-app sliders; Step 3 'Mute Apps' describes the speaker icon; Step 4 'Pin Apps' says to right-click apps to pin (HelpView.swift:38-57).",
        "Each step shows an accent-colored number badge in a circle next to a bold title and secondary description (helpStep, HelpView.swift:191-207)."
      ],
      "acceptanceCriteria": [
        "Four steps appear in order with badges 1–4.",
        "Step 4's right-click-to-pin instruction matches a real context-menu Pin action (MixerRowView.swift:135-137).",
        "Step 3's claim that the speaker icon mutes/unmutes matches the mute control in the mixer row.",
        "Descriptions reflect actual app behavior, not aspirational features."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Help/HelpView.swift:32-62",
        "Sources/Waves/Features/Help/HelpView.swift:191-207",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:135-137"
      ],
      "implStatus": "implemented",
      "implNotes": "Accurate. All four steps map to real features (auto-detection, sliders, mute toggle, right-click Pin via contextMenu at MixerRowView.swift:135-137 / togglePinned at AppStore.swift:590). Content is fully static text — correct for a help doc, no functional risk. Minor: it omits that adjusting volume/mute on an unmanaged app first enrolls it in managed routing (see MixerRowView.swift:211-221 tooltips), which can surprise users; Quick Start presents control as instantaneous.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "HELP-03",
      "area": "In-App Help",
      "feature": "Keyboard shortcuts reference",
      "priority": "P2",
      "userStory": "As a power user, I want a list of Waves keyboard shortcuts, so that I can control volume, mute, refresh, and save presets without the mouse.",
      "expectedBehavior": [
        "Lists six shortcuts in a monospaced card: Increase volume Cmd+Opt+Up, Decrease volume Cmd+Opt+Down, Toggle mute Cmd+Opt+M, Refresh app list Cmd+R, Save preset Cmd+S, Open Settings Cmd+, (HelpView.swift:64-95).",
        "Shows a caption: 'Enable keyboard shortcuts in General Settings to use these globally.' (HelpView.swift:97-99).",
        "Each row renders the action label (secondary) and the key glyphs (medium weight) via shortcutRow (HelpView.swift:209-217)."
      ],
      "acceptanceCriteria": [
        "The three global volume/mute shortcuts (Cmd+Opt+Up/Down/M) match the real global hotkey handler keyCodes 126/125/46 (WavesApp.swift:179-188).",
        "Cmd+R maps to a real Refresh command (WavesApp.swift:44-47, MainWindowView.swift:46) and Cmd+S to the real Save-preset toolbar button (MainWindowView.swift:36).",
        "Cmd+, opens Settings via the standard Settings scene.",
        "The 'enable globally' caption is true only for the volume/mute shortcuts, not for the always-on app shortcuts."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Help/HelpView.swift:64-103",
        "Sources/Waves/Features/Help/HelpView.swift:209-217",
        "Sources/Waves/App/WavesApp.swift:179-188",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:36-46"
      ],
      "implStatus": "partial",
      "implNotes": "Shortcuts are real but the section mixes two categories and the caption is misleading. Cmd+Opt+Up/Down/M are global hotkeys that only fire when preferences.enableKeyboardShortcuts is on (WavesApp.swift:105-112, 164-188). Cmd+R, Cmd+S, and Cmd+, are standard SwiftUI command/toolbar/Settings shortcuts that work regardless of that toggle (WavesApp.swift:44-47; MainWindowView.swift:36,46). The single caption 'Enable keyboard shortcuts in General Settings to use these globally' implies the whole list is gated, which is incorrect for ⌘R/⌘S/⌘,. Also, the listed step is 'increase/decrease volume' but the global handler uses a 0.1 step (WavesApp.swift:181-183 -> increaseVolumeForFrontmostApp default 0.1 at AppStore.swift:1210) while in-row accessibility uses 0.05 (MixerRowView.swift:224) — not documented, minor.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1M 1L]",
      "errorsFound": "• [MEDIUM/correctness] Misleading caption: 'Enable keyboard shortcuts to use these globally' applies to only 3 of the 6 listed shortcuts (Sources/Waves/Features/Help/HelpView.swift:97-99) — The Keyboard Shortcuts card lists six shortcuts (⌘⌥↑, ⌘⌥↓, ⌘⌥M, ⌘R, ⌘S, ⌘,) then shows one blanket caption 'Enable keyboard shortcuts in General Settings to use these globally.' (HelpView.swift:97). Verified in code: only the three ⌘⌥ shortcuts are global hotkeys gated by preferences.enableKeyboardShortcuts (AppStore.swift:1211/1230/1249 each `guard preferences.enableKeyboardShortcuts else { return }`; installed via NSEvent.addGlobalMonitorForEvents in WavesApp.swift:105-112,150-189). ⌘R is a SwiftUI command (WavesApp.swift:43-48), ⌘S is the save-preset toolbar shortcut (MainWindowView.swift:36), and ⌘, is the standard Settings shortcut — all three work regardless of the toggle and are app-local (only while a Waves window is key), not system-wide. So the caption is doubly wrong for ⌘R/⌘S/⌘,: it implies they are gated (they are not) and global (they are not). | Fix: Split into two groups — 'Global shortcuts (require Enable keyboard shortcuts)': ⌘⌥↑/↓/M; and 'App shortcuts (always available in Waves)': ⌘R, ⌘S, ⌘, — and scope the 'enable globally' caption to the first group only.\n• [LOW/inconsistency] Volume step differs between global shortcut (0.1) and in-row/keyboard nudge (0.05), undocumented (Sources/Waves/Stores/AppStore.swift:1210) — The global ⌘⌥↑/↓ shortcuts step volume by a default 0.1 (AppStore.swift:1210 increaseVolumeForFrontmostApp(step: Float = 0.1); 1229 decrease), while the in-row accessibility adjustable action steps by 0.05 (MixerRowView.swift:224) and the list key handlers '='/'-' nudge by 0.05 (MainWindowView.swift:383-384). Help documents only 'Increase/Decrease volume' with no step, so this is not a stated falsehood, but the two volume-adjust paths use inconsistent increments and Help never notes it. Tangential to the Help area (it is really an app-level inconsistency), kept as low. | Fix: Pick one canonical step (e.g. 0.05) for both the global default and in-row nudge, or note the 10%/5% difference in Help. Low priority.",
      "fixStatus": "1 fixed · 1 n/a",
      "fixNotes": "tier2: FU-HELP-CAPTION-CORRECTNESS",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "correctness",
          "title": "Misleading caption: 'Enable keyboard shortcuts to use these globally' applies to only 3 of the 6 listed shortcuts",
          "detail": "The Keyboard Shortcuts card lists six shortcuts (⌘⌥↑, ⌘⌥↓, ⌘⌥M, ⌘R, ⌘S, ⌘,) then shows one blanket caption 'Enable keyboard shortcuts in General Settings to use these globally.' (HelpView.swift:97). Verified in code: only the three ⌘⌥ shortcuts are global hotkeys gated by preferences.enableKeyboardShortcuts (AppStore.swift:1211/1230/1249 each `guard preferences.enableKeyboardShortcuts else { return }`; installed via NSEvent.addGlobalMonitorForEvents in WavesApp.swift:105-112,150-189). ⌘R is a SwiftUI command (WavesApp.swift:43-48), ⌘S is the save-preset toolbar shortcut (MainWindowView.swift:36), and ⌘, is the standard Settings shortcut — all three work regardless of the toggle and are app-local (only while a Waves window is key), not system-wide. So the caption is doubly wrong for ⌘R/⌘S/⌘,: it implies they are gated (they are not) and global (they are not).",
          "codeRef": "Sources/Waves/Features/Help/HelpView.swift:97-99",
          "expectedVsActual": "Story acceptance criterion: 'The enable-globally caption is true only for the volume/mute shortcuts, not for the always-on app shortcuts.' Actual: a single blanket caption sits under all six rows, false for ⌘R/⌘S/⌘,. Severity reduced from the first tester's 'high' to 'medium' because this is a static P2 help caption, not a runtime control defect.",
          "suggestedFix": "Split into two groups — 'Global shortcuts (require Enable keyboard shortcuts)': ⌘⌥↑/↓/M; and 'App shortcuts (always available in Waves)': ⌘R, ⌘S, ⌘, — and scope the 'enable globally' caption to the first group only.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Help/HelpView.swift",
          "disposition": "tier2:applied:FU-HELP-CAPTION-CORRECTNESS",
          "uid": "HELP-03#1",
          "canonicalFile": "Sources/Waves/Features/Help/HelpView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-HELP-CAPTION-CORRECTNESS",
          "fixTier": "tier2"
        },
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "Volume step differs between global shortcut (0.1) and in-row/keyboard nudge (0.05), undocumented",
          "detail": "The global ⌘⌥↑/↓ shortcuts step volume by a default 0.1 (AppStore.swift:1210 increaseVolumeForFrontmostApp(step: Float = 0.1); 1229 decrease), while the in-row accessibility adjustable action steps by 0.05 (MixerRowView.swift:224) and the list key handlers '='/'-' nudge by 0.05 (MainWindowView.swift:383-384). Help documents only 'Increase/Decrease volume' with no step, so this is not a stated falsehood, but the two volume-adjust paths use inconsistent increments and Help never notes it. Tangential to the Help area (it is really an app-level inconsistency), kept as low.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1210",
          "expectedVsActual": "Shortcuts match by key, but the magnitude is inconsistent across the global vs. in-row paths and undocumented.",
          "suggestedFix": "Pick one canonical step (e.g. 0.05) for both the global default and in-row nudge, or note the 10%/5% difference in Help. Low priority.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "HELP-03#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "HELP-04",
      "area": "In-App Help",
      "feature": "URL scheme automation reference",
      "priority": "P3",
      "userStory": "As an automation user, I want documentation of the waves:// URL scheme, so that I can script volume, mute, preset, and refresh commands from Shortcuts or scripts.",
      "expectedBehavior": [
        "Documents four commands: waves://set-volume?app=APP_ID&volume=0.5 (0.0-1.0), waves://mute?app=APP_ID&muted=true, waves://apply-preset?name=Focus, waves://refresh (HelpView.swift:105-131).",
        "Each row shows the scheme (accent monospaced), optional params, and a plain-language description via urlSchemeRow (HelpView.swift:219-237).",
        "Shows a caption telling the user to 'Enable URL scheme automation in General Settings before using these commands.' (HelpView.swift:133-135)."
      ],
      "acceptanceCriteria": [
        "Each documented host matches a real branch in the URL handler: set-volume, mute, apply-preset, refresh (AppStore.swift:277-288).",
        "Documented params match what the handlers parse: app+volume (AppStore.swift:291-310), app+muted (AppStore.swift:312-328), name (AppStore.swift:330-343).",
        "The volume range 0.0-1.0 documented matches the handler's validation volume>=0 && volume<=1 (AppStore.swift:296-298).",
        "The 'enable in General Settings' caption matches the real gate preferences.enableURLScheme (AppStore.swift:257-260)."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Help/HelpView.swift:105-139",
        "Sources/Waves/Features/Help/HelpView.swift:219-237",
        "Sources/Waves/Stores/AppStore.swift:256-343"
      ],
      "implStatus": "implemented",
      "implNotes": "Documentation is accurate and complete: all four hosts, their query params, the 0.0-1.0 range, and the enable-in-settings prerequisite all match the implementation (AppStore.swift:256-343). Not documented (acceptable for brevity but worth noting): commands are rate-limited (checkURLSchemeRateLimit, AppStore.swift:345-354) and silently/with-toast rejected when the app/preset isn't found, and 'APP_ID' format (logicalID/appKey matching, AppStore.swift:303) is not explained, so users may guess the wrong identifier.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "HELP-05",
      "area": "In-App Help",
      "feature": "Presets usage reference",
      "priority": "P2",
      "userStory": "As a user managing multiple audio setups, I want help on saving, applying, exporting, and importing presets, so that I can reuse volume/mute configurations.",
      "expectedBehavior": [
        "Bulleted list explains: click the + toolbar button to save, name it to capture current config, click presets in the sidebar to apply, export to share, import from JSON (HelpView.swift:141-152).",
        "Caption clarifies presets remember volume and mute states for all running apps (HelpView.swift:155-157)."
      ],
      "acceptanceCriteria": [
        "The '+' toolbar save flow matches the real Save-preset toolbar button (MainWindowView.swift:28-36).",
        "Export and Import bullets match real store actions exportPreset/importPreset (SettingsView.swift:221-247; AppStore.swift:1079, 1112).",
        "The import description ('from JSON files') matches the importer behavior.",
        "The caption's claim about captured state matches what a preset actually stores."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Help/HelpView.swift:141-161",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:28-36",
        "Sources/Waves/Settings/SettingsView.swift:221-247",
        "Sources/Waves/Stores/AppStore.swift:1079-1175"
      ],
      "implStatus": "implemented",
      "implNotes": "Claims map to real features: save via + toolbar button, export/import via the Presets settings tab (SettingsView.swift:221-247 -> AppStore.swift:1079/1112). One UX mismatch: the bullet says 'Click on presets in the sidebar to apply them', but preset apply/export controls in code live in the Presets settings tab list (SettingsView.swift:221-247), not necessarily the main-window sidebar — the wording may point users to the wrong place. Import also enforces limits not mentioned here (10MB, max 1000 entries, non-empty name; AppStore.swift:1131-1146), so a rejected import could confuse a user following only this help.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "HELP-06",
      "area": "In-App Help",
      "feature": "Troubleshooting guidance",
      "priority": "P2",
      "userStory": "As a user hitting problems, I want troubleshooting tips for common failures, so that I can self-resolve detection, routing, shortcut, and device issues without contacting support.",
      "expectedBehavior": [
        "Lists four issue/solution pairs: no apps detected (play sound, enable 'Show system processes', refresh ⌘R), volume not applying (check Diagnostics in Advanced, 'Recover routes now'), shortcuts not working (enable in Settings, check accessibility permissions, conflicts), device switching (enable 'Auto-restore device', recover routes, check connection) (HelpView.swift:163-189).",
        "Each item renders a bold issue title and a secondary solution line via troubleshootingItem (HelpView.swift:239-248)."
      ],
      "acceptanceCriteria": [
        "'Show system processes' refers to a real toggle (SettingsView.swift:69).",
        "'Auto-restore device' refers to a real toggle (SettingsView.swift:79).",
        "The recover-routes remedy maps to a real action store.recoverRoutes (SettingsView.swift:199, 336).",
        "'Diagnostics in Advanced settings' matches the real Advanced/Diagnostics tab (SettingsView.swift:28-31, 269)."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Help/HelpView.swift:163-189",
        "Sources/Waves/Features/Help/HelpView.swift:239-248",
        "Sources/Waves/Settings/SettingsView.swift:69,79,199,269-341"
      ],
      "implStatus": "partial",
      "implNotes": "Guidance is mostly accurate and points at real toggles/actions, but two label mismatches reduce trust: the help says 'Recover routes now' while the actual buttons are labeled 'Recover Routes' (SettingsView.swift:198, 338) and the toolbar tooltip is 'Recover managed routes' (MainWindowView.swift:53) — no control is literally named 'Recover routes now'. The shortcuts remedy says 'check accessibility permissions', but the global hotkey monitor uses NSEvent.addGlobalMonitorForEvents (WavesApp.swift:152) which requires Input Monitoring/Accessibility approval that the app never prompts for or surfaces — so this tip may not be actionable for the user. Otherwise 'Show system processes' and 'Auto-restore device' toggles and the Advanced/Diagnostics tab all exist.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1M 1L]",
      "errorsFound": "• [MEDIUM/correctness] Troubleshooting points to 'Recover routes now' on the Advanced/Diagnostics tab, where that button is normally absent (Sources/Waves/Features/Help/HelpView.swift:175) — Help (HelpView.swift:175) tells users with volume issues to 'Check Diagnostics in Advanced settings, try \\'Recover routes now\\''. The Advanced tab is DiagnosticsSettingsView (SettingsView.swift:28-31,269). Its .onAppear calls store.refreshDiagnostics() (SettingsView.swift:310-312) which populates store.diagnostics; when diagnostics is non-nil the tab renders only the checks plus a 'Copy Diagnostics' button (SettingsView.swift:283-303) and NO recover button. The 'Recover Routes' button on that tab lives exclusively inside DiagnosticsUnavailableView (SettingsView.swift:335-340), shown only in the `else` branch when store.diagnostics == nil. So once diagnostics load (the normal steady state) a user following this tip finds no recover control on the Advanced tab. The reliably-present 'Recover Routes' button is on the Audio tab (SettingsView.swift:198-200) and the main-window toolbar (MainWindowView.swift:48-55), neither of which the help mentions. | Fix: Either add a persistent 'Recover Routes' button to DiagnosticsSettingsView (not just the unavailable state), or change the help text to point at the Audio tab / main-window toolbar Recover control.\n• [LOW/inconsistency] Help label 'Recover routes now' matches no real control name (Sources/Waves/Features/Help/HelpView.swift:175) — Help says 'Recover routes now' (HelpView.swift:175) but the actual controls are labeled 'Recover Routes' (SettingsView.swift:198, 338) and the toolbar tooltip/accessibility label is 'Recover managed routes' (MainWindowView.swift:53-54). No control is literally named 'Recover routes now', so a user scanning the UI for that exact label will not find it. | Fix: Quote the real label: change 'Recover routes now' to 'Recover Routes' to match SettingsView.swift:198/338.",
      "fixStatus": "2 fixed",
      "fixNotes": "tier2: FU-HELP-CAPTION-CORRECTNESS",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "correctness",
          "title": "Troubleshooting points to 'Recover routes now' on the Advanced/Diagnostics tab, where that button is normally absent",
          "detail": "Help (HelpView.swift:175) tells users with volume issues to 'Check Diagnostics in Advanced settings, try \\'Recover routes now\\''. The Advanced tab is DiagnosticsSettingsView (SettingsView.swift:28-31,269). Its .onAppear calls store.refreshDiagnostics() (SettingsView.swift:310-312) which populates store.diagnostics; when diagnostics is non-nil the tab renders only the checks plus a 'Copy Diagnostics' button (SettingsView.swift:283-303) and NO recover button. The 'Recover Routes' button on that tab lives exclusively inside DiagnosticsUnavailableView (SettingsView.swift:335-340), shown only in the `else` branch when store.diagnostics == nil. So once diagnostics load (the normal steady state) a user following this tip finds no recover control on the Advanced tab. The reliably-present 'Recover Routes' button is on the Audio tab (SettingsView.swift:198-200) and the main-window toolbar (MainWindowView.swift:48-55), neither of which the help mentions.",
          "codeRef": "Sources/Waves/Features/Help/HelpView.swift:175",
          "expectedVsActual": "Story acceptance: the recover-routes remedy maps to a real action reachable where the help sends the user (Advanced/Diagnostics, SettingsView.swift:199,336). Actual: on the Advanced tab the recover button only appears in the diagnostics-unavailable state; with diagnostics present there is no recover button there, so the documented remedy is unreachable from the place the help points.",
          "suggestedFix": "Either add a persistent 'Recover Routes' button to DiagnosticsSettingsView (not just the unavailable state), or change the help text to point at the Audio tab / main-window toolbar Recover control.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Help/HelpView.swift",
          "disposition": "tier2:applied:FU-HELP-CAPTION-CORRECTNESS",
          "uid": "HELP-06#1",
          "canonicalFile": "Sources/Waves/Features/Help/HelpView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-HELP-CAPTION-CORRECTNESS",
          "fixTier": "tier2"
        },
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "Help label 'Recover routes now' matches no real control name",
          "detail": "Help says 'Recover routes now' (HelpView.swift:175) but the actual controls are labeled 'Recover Routes' (SettingsView.swift:198, 338) and the toolbar tooltip/accessibility label is 'Recover managed routes' (MainWindowView.swift:53-54). No control is literally named 'Recover routes now', so a user scanning the UI for that exact label will not find it.",
          "codeRef": "Sources/Waves/Features/Help/HelpView.swift:175",
          "expectedVsActual": "Expected: help references the real button label. Actual: it invents 'Recover routes now', which exists nowhere in the UI.",
          "suggestedFix": "Quote the real label: change 'Recover routes now' to 'Recover Routes' to match SettingsView.swift:198/338.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Help/HelpView.swift",
          "disposition": "tier2:applied:FU-HELP-CAPTION-CORRECTNESS",
          "uid": "HELP-06#2",
          "canonicalFile": "Sources/Waves/Features/Help/HelpView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-HELP-CAPTION-CORRECTNESS",
          "fixTier": "tier2"
        }
      ]
    },
    {
      "id": "DIAG-01",
      "area": "Diagnostics Export & Route Health Report",
      "feature": "Route health report (diagnostics checks)",
      "priority": "P1",
      "userStory": "As a Waves user whose per-app volume controls aren't behaving, I want a structured route-health report listing each system prerequisite and its pass/warn/fail status, so that I can see exactly which capability (OS support, capture permission, accessibility, route recovery, app coverage) is blocking control.",
      "expectedBehavior": [
        "Backend's diagnosticsReport() builds a DiagnosticsReport with a summary plus five checks: Audio component, Audio capture permission, Accessibility permission, Route recovery, and Support matrix (WorkspaceAudioControlBackend.swift:241-278)",
        "'Audio component' is .passed when isAudioComponentInstalled, else .warning with detail 'Per-app routing needs macOS 14.2 or newer.' (WorkspaceAudioControlBackend.swift:245-251)",
        "'Audio capture permission' maps captureAuthorization: .authorized->passed, .notGranted->failed, .undetermined/.unsupported->warning, each with a tailored remediation detail pointing to System Settings (WorkspaceAudioControlBackend.swift:252-256, 280-299)",
        "'Accessibility permission' is .passed when AXIsProcessTrusted(), else .warning noting global shortcuts need it but routing still works (WorkspaceAudioControlBackend.swift:257-263, 318-320)",
        "'Route recovery' is .passed when isRouteRecoveryHealthy else .warning advising 'Recover routes and retry.' (WorkspaceAudioControlBackend.swift:264-270)",
        "'Support matrix' is always .informational showing coverageSummary 'N/M validated' (WorkspaceAudioControlBackend.swift:271-275, SupportMatrix.swift:10-13)",
        "summary (recoverabilitySummary) reports count of currently managed routes, or that routing is unavailable on the OS version (WorkspaceAudioControlBackend.swift:301-308)",
        "Each check renders in Settings as title + detail cards (SettingsView.swift:291-303) and in the mixer DiagnosticsPanel with a colored status dot and VoiceOver status label (MainWindowView.swift:531-589)"
      ],
      "acceptanceCriteria": [
        "When capture permission is denied, the 'Audio capture permission' check shows status failed and a red dot, with detail directing the user to System Settings > Privacy & Security > Microphone",
        "On macOS earlier than 14.2 the 'Audio component' check shows a warning referencing the 14.2 requirement",
        "The Support matrix check always shows as informational (gray dot), never pass/fail, and its detail equals 'supportedCount/totalCount validated'",
        "The mixer DiagnosticsPanel summary string reflects the number of currently-managed apps",
        "Each check's status color in the panel maps passed=green, warning=orange, failed=red, informational=secondary"
      ],
      "codeRefs": [
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:241-308",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:280-320",
        "Sources/WavesAudioCore/Backend/AudioControlBackend.swift:54-90",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:522-590",
        "Sources/Waves/Settings/SettingsView.swift:291-303",
        "Sources/WavesAudioCore/Models/SupportMatrix.swift:10-13"
      ],
      "implStatus": "implemented",
      "implNotes": "Fully wired and rendered in two places. Concern: the DiagnosticsPanel in the mixer shows check titles and details with status dots but never surfaces the status label visually for sighted users beyond the dot color (status word is VoiceOver-only), so a color-blind sighted user can't distinguish warning vs failed without the announced label. Minor: 'Route recovery' uses .warning (orange) even when routing is fully broken, never .failed, so a genuinely failed route looks the same severity as a non-blocking accessibility warning.",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [3M]",
      "errorsFound": "• [MEDIUM/inconsistency] Route recovery check can never report .failed, so a fully broken route looks like a benign warning (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:264-270) — Re-read WorkspaceAudioControlBackend.swift:264-270: the 'Route recovery' check status is a binary ternary (snapshot.backendStatus.isRouteRecoveryHealthy ? .passed : .warning). isRouteRecoveryHealthy is false whenever per-app routing is genuinely broken — capture not granted, OS unsupported, or any app in .error routingState (hasRouteErrors at :1061 / isRouteRecoveryHealthy at :1089). In all of those cases the check is rendered as orange .warning, the same severity as the explicitly non-blocking Accessibility warning. No code path yields .failed for route recovery even though DiagnosticsStatus.failed exists (AudioControlBackend.swift:88) and both panels map it to red. Confirmed. | Fix: Distinguish hard failures from soft ones: when supportsPerAppRouting && captureAuthorization == .authorized but hasRouteErrors, set Route recovery to .failed (red); reserve .warning for transient/recoverable states. Thread hasRouteErrors / captureAuthorization into the check status.\n• [MEDIUM/accessibility] Mixer DiagnosticsPanel encodes check status only as a colored dot for sighted users (Sources/Waves/Features/Mixer/MainWindowView.swift:537-554) — Re-read MainWindowView.swift:537-554: each check renders check.title and check.detail visually; status is conveyed solely by the fill color of a 7pt Circle (color(for:) at 565-576). That circle is accessibilityHidden(true) (line 543) and the status word ('Passed'/'Warning'/'Failed'/'Info') is placed only in the VoiceOver accessibilityLabel (line 547). A sighted user with a color-vision deficiency cannot distinguish passed (green) from warning (orange) from failed (red) — a 7pt dot's hue is the only cue and no text/shape status is shown. WCAG 'use of color' gap. Confirmed. | Fix: Render the statusLabel text or a shape-differentiated SF Symbol (checkmark.circle / exclamationmark.triangle / xmark.octagon / info.circle) next to the dot so status is distinguishable without relying on hue alone.\n• [MEDIUM/accessibility] Settings Diagnostics check cards show NO status indicator at all — not even a colored dot or VoiceOver label (Sources/Waves/Settings/SettingsView.swift:291-303) — Missed by the first tester. Re-read SettingsView.swift:291-303: in DiagnosticsSettingsView each check renders only Text(check.title) and Text(check.detail) inside a material card. Unlike the mixer DiagnosticsPanel, there is no colored dot, no status text, and no accessibility status label whatsoever. check.status is completely dropped on this surface. So in the primary Settings › Diagnostics tab a passing check is visually and to VoiceOver indistinguishable from a failed one — a worse accessibility/scannability gap than the mixer panel, and the surface where users actually go to read diagnostics. State-desync corollary: the two diagnostics surfaces (mixer panel vs Settings tab) present the same checks with different fidelity. | Fix: Mirror the mixer panel's status dot + statusLabel accessibility label (ideally with a shape-differentiated SF Symbol) in the Settings check cards, or factor a shared CheckRow view used by both surfaces.",
      "fixStatus": "3 fixed",
      "fixNotes": "tier2: FU-DIAG-CARD-STATUS; FU-DIAG-CHECK-STATUS-NONCOLOR-CUE; FU-DIAGNOSTICS-ROUTE-NEVER-FAILED",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "inconsistency",
          "title": "Route recovery check can never report .failed, so a fully broken route looks like a benign warning",
          "detail": "Re-read WorkspaceAudioControlBackend.swift:264-270: the 'Route recovery' check status is a binary ternary (snapshot.backendStatus.isRouteRecoveryHealthy ? .passed : .warning). isRouteRecoveryHealthy is false whenever per-app routing is genuinely broken — capture not granted, OS unsupported, or any app in .error routingState (hasRouteErrors at :1061 / isRouteRecoveryHealthy at :1089). In all of those cases the check is rendered as orange .warning, the same severity as the explicitly non-blocking Accessibility warning. No code path yields .failed for route recovery even though DiagnosticsStatus.failed exists (AudioControlBackend.swift:88) and both panels map it to red. Confirmed.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:264-270",
          "expectedVsActual": "Expected: a genuinely failed/blocked route surfaces at higher severity than an optional accessibility warning. Actual: both render as orange .warning; route recovery never reaches red .failed even when per-app control is completely non-functional (e.g. capture denied on a 14.2+ Mac).",
          "suggestedFix": "Distinguish hard failures from soft ones: when supportsPerAppRouting && captureAuthorization == .authorized but hasRouteErrors, set Route recovery to .failed (red); reserve .warning for transient/recoverable states. Thread hasRouteErrors / captureAuthorization into the check status.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "tier2:applied:FU-DIAGNOSTICS-ROUTE-NEVER-FAILED",
          "uid": "DIAG-01#1",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-DIAGNOSTICS-ROUTE-NEVER-FAILED",
          "fixTier": "tier2"
        },
        {
          "severity": "medium",
          "type": "accessibility",
          "title": "Mixer DiagnosticsPanel encodes check status only as a colored dot for sighted users",
          "detail": "Re-read MainWindowView.swift:537-554: each check renders check.title and check.detail visually; status is conveyed solely by the fill color of a 7pt Circle (color(for:) at 565-576). That circle is accessibilityHidden(true) (line 543) and the status word ('Passed'/'Warning'/'Failed'/'Info') is placed only in the VoiceOver accessibilityLabel (line 547). A sighted user with a color-vision deficiency cannot distinguish passed (green) from warning (orange) from failed (red) — a 7pt dot's hue is the only cue and no text/shape status is shown. WCAG 'use of color' gap. Confirmed.",
          "codeRef": "Sources/Waves/Features/Mixer/MainWindowView.swift:537-554",
          "expectedVsActual": "Story: each check renders with a colored status dot plus a VoiceOver status label. Actual matches the letter of the story, but the result is a color-only status indicator for sighted users with no redundant text/shape cue.",
          "suggestedFix": "Render the statusLabel text or a shape-differentiated SF Symbol (checkmark.circle / exclamationmark.triangle / xmark.octagon / info.circle) next to the dot so status is distinguishable without relying on hue alone.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "disposition": "tier2:applied:FU-DIAG-CHECK-STATUS-NONCOLOR-CUE",
          "uid": "DIAG-01#2",
          "canonicalFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-DIAG-CHECK-STATUS-NONCOLOR-CUE",
          "fixTier": "tier2"
        },
        {
          "severity": "medium",
          "type": "accessibility",
          "title": "Settings Diagnostics check cards show NO status indicator at all — not even a colored dot or VoiceOver label",
          "detail": "Missed by the first tester. Re-read SettingsView.swift:291-303: in DiagnosticsSettingsView each check renders only Text(check.title) and Text(check.detail) inside a material card. Unlike the mixer DiagnosticsPanel, there is no colored dot, no status text, and no accessibility status label whatsoever. check.status is completely dropped on this surface. So in the primary Settings › Diagnostics tab a passing check is visually and to VoiceOver indistinguishable from a failed one — a worse accessibility/scannability gap than the mixer panel, and the surface where users actually go to read diagnostics. State-desync corollary: the two diagnostics surfaces (mixer panel vs Settings tab) present the same checks with different fidelity.",
          "codeRef": "Sources/Waves/Settings/SettingsView.swift:291-303",
          "expectedVsActual": "Expected (and matched by the mixer panel): each check shows its pass/warn/fail status via color plus a non-color cue. Actual: the Settings Diagnostics cards omit status entirely; a failed check looks identical to a passed one for all users, sighted or VoiceOver.",
          "suggestedFix": "Mirror the mixer panel's status dot + statusLabel accessibility label (ideally with a shape-differentiated SF Symbol) in the Settings check cards, or factor a shared CheckRow view used by both surfaces.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Settings/SettingsView.swift",
          "disposition": "tier2:applied:FU-DIAG-CARD-STATUS",
          "uid": "DIAG-01#3",
          "canonicalFile": "Sources/Waves/Settings/SettingsView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-DIAG-CARD-STATUS",
          "fixTier": "tier2"
        }
      ]
    },
    {
      "id": "DIAG-02",
      "area": "Diagnostics Export & Route Health Report",
      "feature": "Copy plain-text diagnostics to clipboard for bug reports",
      "priority": "P2",
      "userStory": "As a user filing a bug report, I want to copy a plain-text route-health snapshot to my clipboard with one click, so that I can paste system, device, permission, per-app, and check state into an issue without manually transcribing it.",
      "expectedBehavior": [
        "copyDiagnosticsToPasteboard() clears NSPasteboard.general and writes diagnosticsExportText as a string, then shows a success toast 'Diagnostics copied / Paste into a bug report.' for 1.4s (AppStore.swift:774-779)",
        "diagnosticsExportText composes: header 'Waves diagnostics', macOS version string, output device name, audio component installed bool, capture permission granted/not granted, route recovery healthy bool, and lastError if present (AppStore.swift:725-736)",
        "It then lists every visible app as 'name — routingState, NN%' with optional ', muted' and ', boost Nx' suffixes (AppStore.swift:737-742)",
        "If a diagnostics report is loaded, it appends a 'Checks:' section with '[status] title: detail' per check (AppStore.swift:743-749)",
        "The Settings Diagnostics tab exposes a 'Copy Diagnostics' button with a clipboard icon and help text 'Copy a plain-text route-health report to the clipboard.' (SettingsView.swift:283-288)",
        "The doc comment asserts the text contains no audio and no sensitive content beyond already-visible app names (AppStore.swift:722-724)"
      ],
      "acceptanceCriteria": [
        "Clicking 'Copy Diagnostics' places a multi-line plain-text report on the system clipboard and shows the success toast",
        "The report's first line is 'Waves diagnostics' and includes lines for macOS, Output device, Audio component installed, Audio capture permission, and Route recovery healthy",
        "A muted app appears in the Apps list with ', muted'; an app with boost > 1 appears with ', boost Nx'",
        "When status.lastError is nil, no 'Last error:' line is emitted; when present it is included",
        "If store.diagnostics is nil, the 'Checks:' section is omitted entirely but the rest of the report still copies"
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:722-751",
        "Sources/Waves/Stores/AppStore.swift:774-779",
        "Sources/Waves/Settings/SettingsView.swift:283-288"
      ],
      "implStatus": "implemented",
      "implNotes": "Works and is privacy-conscious (clipboard-only, no file write, no PIDs/audio). Note this is 'export' only in the copy-to-clipboard sense — despite README phrasing 'diagnostics export', there is no save-to-file path for diagnostics (the NSSavePanel/.json code at AppStore.swift:1086+ is for preset export, not diagnostics). UX gap: the 'Copy Diagnostics' button is always enabled even before a report loads, so an early click copies a report with no 'Checks:' section. Boost is printed as Int(volumeBoost) which truncates fractional boosts (e.g. 2.5x shows as '2x'). No automated test covers diagnosticsExportText formatting.",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [3L]",
      "errorsFound": "• [LOW/correctness] Boost in diagnostics export is truncated by Int(), dropping fractional boosts (Sources/Waves/Stores/AppStore.swift:740) — Re-read AppStore.swift:740: ', boost \\(Int(app.volumeBoost))x'. volumeBoost is a Float clamped to [0.0, 10.0] in AudioApp.init (AudioApp.swift:88) and decoded from persistence (AudioApp.swift:138) / imported presets (Preset.swift:41 clamps to [1.0,4.0] but allows fractions). The mixer UI only offers integers and setVolumeBoost clamps to [1.0,4.0] (AppStore.swift:548), but a persisted or imported preset can carry a fractional value (e.g. 2.5). Int(2.5) prints '2x', misreporting the actual boost in a bug-report snapshot. Confirmed; correctly low severity (narrow input path). | Fix: Use String(format: \"%g\", app.volumeBoost) so 2.0 shows '2x' and 2.5 shows '2.5x'.\n• [LOW/ux] Copy Diagnostics button is always enabled, so an early click copies a report with no Checks section (Sources/Waves/Settings/SettingsView.swift:283-288) — Re-read SettingsView.swift:283-289 (the Button has no .disabled) and AppStore.swift:743-749 (the 'Checks:' section is only appended when store.diagnostics != nil). DiagnosticsSettingsView triggers refreshDiagnostics() in onAppear (SettingsView.swift:310-312) but that is async; a click in the brief window before the first report resolves yields a report missing the Checks section, while copyDiagnosticsToPasteboard still shows the 'Diagnostics copied' success toast (AppStore.swift:774-779). Silent partial export. Confirmed. | Fix: Disable Copy Diagnostics while store.diagnostics == nil, or have copyDiagnosticsToPasteboard await a fresh diagnosticsReport() before composing the text so Checks is always present.\n• [LOW/inconsistency] Export header capture-permission line can contradict the appended Checks section (Sources/Waves/Stores/AppStore.swift:733) — Re-read AppStore.swift:733: header line 'Audio capture permission: granted/not granted' derives from status.hasRequiredPermissions, which is captureAuthorization == .authorized (WorkspaceAudioControlBackend.swift:1088). On .undetermined or .unsupported this collapses to 'not granted'. The same export's Checks section prints the 'Audio capture permission' check as [warning] with detail 'status is not yet known' (.undetermined) or 'needs macOS 14.2 or newer' (.unsupported) (WorkspaceAudioControlBackend.swift:284-298). So the header says flatly 'not granted' while the check says 'not yet known' / unsupported — confusing in a bug report. Confirmed. | Fix: Report capture permission in the header using the same vocabulary as the check (granted / not granted / not yet determined / unsupported) rather than a boolean.",
      "fixStatus": "3 fixed",
      "fixNotes": "tier3: FU-DIAGNOSTICS-EXPORT",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "correctness",
          "title": "Boost in diagnostics export is truncated by Int(), dropping fractional boosts",
          "detail": "Re-read AppStore.swift:740: ', boost \\(Int(app.volumeBoost))x'. volumeBoost is a Float clamped to [0.0, 10.0] in AudioApp.init (AudioApp.swift:88) and decoded from persistence (AudioApp.swift:138) / imported presets (Preset.swift:41 clamps to [1.0,4.0] but allows fractions). The mixer UI only offers integers and setVolumeBoost clamps to [1.0,4.0] (AppStore.swift:548), but a persisted or imported preset can carry a fractional value (e.g. 2.5). Int(2.5) prints '2x', misreporting the actual boost in a bug-report snapshot. Confirmed; correctly low severity (narrow input path).",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:740",
          "expectedVsActual": "Expected: the diagnostics report faithfully reflects the app's actual boost. Actual: fractional boosts truncate to the next lower integer, understating the real value.",
          "suggestedFix": "Use String(format: \"%g\", app.volumeBoost) so 2.0 shows '2x' and 2.5 shows '2.5x'.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier3:applied:FU-DIAGNOSTICS-EXPORT",
          "uid": "DIAG-02#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-DIAGNOSTICS-EXPORT",
          "fixTier": "tier3"
        },
        {
          "severity": "low",
          "type": "ux",
          "title": "Copy Diagnostics button is always enabled, so an early click copies a report with no Checks section",
          "detail": "Re-read SettingsView.swift:283-289 (the Button has no .disabled) and AppStore.swift:743-749 (the 'Checks:' section is only appended when store.diagnostics != nil). DiagnosticsSettingsView triggers refreshDiagnostics() in onAppear (SettingsView.swift:310-312) but that is async; a click in the brief window before the first report resolves yields a report missing the Checks section, while copyDiagnosticsToPasteboard still shows the 'Diagnostics copied' success toast (AppStore.swift:774-779). Silent partial export. Confirmed.",
          "codeRef": "Sources/Waves/Settings/SettingsView.swift:283-288",
          "expectedVsActual": "Expected: copying always yields the full report, or the user is told it is partial. Actual: an early copy silently omits the Checks section while still showing a success toast.",
          "suggestedFix": "Disable Copy Diagnostics while store.diagnostics == nil, or have copyDiagnosticsToPasteboard await a fresh diagnosticsReport() before composing the text so Checks is always present.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Settings/SettingsView.swift",
          "disposition": "iter3:implemented",
          "uid": "DIAG-02#2",
          "canonicalFile": "Sources/Waves/Settings/SettingsView.swift",
          "fixStatus": "applied",
          "fixedBy": "iter3-ui-parity",
          "fixTier": "iter3"
        },
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "Export header capture-permission line can contradict the appended Checks section",
          "detail": "Re-read AppStore.swift:733: header line 'Audio capture permission: granted/not granted' derives from status.hasRequiredPermissions, which is captureAuthorization == .authorized (WorkspaceAudioControlBackend.swift:1088). On .undetermined or .unsupported this collapses to 'not granted'. The same export's Checks section prints the 'Audio capture permission' check as [warning] with detail 'status is not yet known' (.undetermined) or 'needs macOS 14.2 or newer' (.unsupported) (WorkspaceAudioControlBackend.swift:284-298). So the header says flatly 'not granted' while the check says 'not yet known' / unsupported — confusing in a bug report. Confirmed.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:733",
          "expectedVsActual": "Expected: header permission summary and the Checks section agree. Actual: undetermined/unsupported renders as a flat 'not granted' header contradicting the more nuanced warning check.",
          "suggestedFix": "Report capture permission in the header using the same vocabulary as the check (granted / not granted / not yet determined / unsupported) rather than a boolean.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier3:applied:FU-DIAGNOSTICS-EXPORT",
          "uid": "DIAG-02#3",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-DIAGNOSTICS-EXPORT",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "DIAG-03",
      "area": "Diagnostics Export & Route Health Report",
      "feature": "Diagnostics empty/unavailable state with refresh and recover actions",
      "priority": "P2",
      "userStory": "As a user who opens the Diagnostics settings before a report has been generated (or after a backend error), I want a clear unavailable state that explains what's wrong and lets me retry, so that I'm not stuck with a blank panel.",
      "expectedBehavior": [
        "DiagnosticsSettingsView calls store.refreshDiagnostics() in .onAppear so the report loads when the tab is shown (SettingsView.swift:310-312, AppStore.swift:1204-1208)",
        "When store.diagnostics is nil, DiagnosticsUnavailableView renders instead of the checks list (SettingsView.swift:304-306)",
        "The unavailable view shows a 'Diagnostics are not loaded' label and surfaces session.backendStatus.lastError, or a fallback prompt to refresh and inspect permissions/route recovery/support coverage (SettingsView.swift:316-325)",
        "It offers a prominent 'Refresh Diagnostics' button (calls refreshDiagnostics) and a bordered 'Recover Routes' button (calls recoverRoutes) (SettingsView.swift:327-341)",
        "recoverRoutes() reattaches managed routes, then re-fetches diagnostics on both success and failure paths and toasts the result (AppStore.swift:1181-1202)"
      ],
      "acceptanceCriteria": [
        "Opening the Diagnostics settings tab triggers a diagnostics refresh automatically",
        "While diagnostics is nil, the panel shows 'Diagnostics are not loaded' with Refresh and Recover Routes buttons",
        "If backendStatus.lastError is set, the unavailable view shows that error text instead of the generic prompt",
        "Clicking 'Recover Routes' from the unavailable view attempts recovery and refreshes diagnostics regardless of outcome",
        "After a successful refresh, the unavailable view is replaced by the list of checks"
      ],
      "codeRefs": [
        "Sources/Waves/Settings/SettingsView.swift:269-347",
        "Sources/Waves/Stores/AppStore.swift:1181-1208"
      ],
      "implStatus": "partial",
      "implNotes": "Because backend.diagnosticsReport() always returns a non-nil report and refreshDiagnostics runs on .onAppear, the nil/unavailable branch is effectively only visible for the brief window before the first async refresh completes — and the mixer's DiagnosticsPanel (MainWindowView.swift:531) silently renders nothing while nil rather than showing an empty state. So the carefully-built DiagnosticsUnavailableView (with lastError surfacing and recover action) is nearly dead UI in practice. The lastError shown here is whatever is on the current session snapshot, which may be stale relative to the actual refresh that just succeeded.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1M 1L]",
      "errorsFound": "• [MEDIUM/correctness] Unavailable view shows a stale lastError because refreshDiagnostics never updates the session snapshot (Sources/Waves/Stores/AppStore.swift:1204-1208) — Re-read AppStore.swift:1204-1208: refreshDiagnostics() only assigns diagnostics = await backend.diagnosticsReport(); it never re-reads currentSnapshot(), so session.backendStatus.lastError is left at whatever the last full session sync produced. DiagnosticsUnavailableView surfaces store.session.backendStatus.lastError (SettingsView.swift:324). Thus the error text in the unavailable state can be stale relative to the report that the same refresh just produced. (Note: recoverRoutes at :1184-1188 does resync session, so this is specific to the standalone refreshDiagnostics path wired to onAppear and the Refresh button.) Confirmed. | Fix: Have refreshDiagnostics() also refresh the session snapshot (session = await backend.currentSnapshot()) when fetching the report, or have DiagnosticsReport carry a lastError field the view reads so the displayed error matches report generation.\n• [LOW/dead-code] DiagnosticsUnavailableView is near-unreachable and the mixer panel shows no empty state when diagnostics is nil (Sources/Waves/Settings/SettingsView.swift:304-306) — Re-read: backend.diagnosticsReport() returns a non-optional DiagnosticsReport (AudioControlBackend.swift:54) so it is never nil, store.diagnostics starts nil (AppStore.swift:30) and DiagnosticsSettingsView refreshes onAppear, so store.diagnostics is nil only for the brief async window before the first report resolves. The DiagnosticsUnavailableView (SettingsView.swift:304-306, 316-346) with its lastError surfacing + Refresh + Recover Routes is therefore effectively dead UI. Worse, the mixer DiagnosticsPanel renders an empty DisclosureGroup body when diagnostics is nil (MainWindowView.swift:530-557: 'if let diagnostics' with no else), so during that window an expanded panel shows nothing. Confirmed. | Fix: Give the mixer DiagnosticsPanel an explicit empty/loading state mirroring DiagnosticsUnavailableView, or let diagnosticsReport()/refreshDiagnostics return nil on real backend failure so the unavailable state is genuinely reachable.",
      "fixStatus": "1 fixed · 1 deferred",
      "fixNotes": "tier2: FU-DIAG-SYNC-DESYNC",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "correctness",
          "title": "Unavailable view shows a stale lastError because refreshDiagnostics never updates the session snapshot",
          "detail": "Re-read AppStore.swift:1204-1208: refreshDiagnostics() only assigns diagnostics = await backend.diagnosticsReport(); it never re-reads currentSnapshot(), so session.backendStatus.lastError is left at whatever the last full session sync produced. DiagnosticsUnavailableView surfaces store.session.backendStatus.lastError (SettingsView.swift:324). Thus the error text in the unavailable state can be stale relative to the report that the same refresh just produced. (Note: recoverRoutes at :1184-1188 does resync session, so this is specific to the standalone refreshDiagnostics path wired to onAppear and the Refresh button.) Confirmed.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1204-1208",
          "expectedVsActual": "Story AC: 'If backendStatus.lastError is set, the unavailable view shows that error text.' Actual: the lastError shown is from the last full session sync, not refreshed by refreshDiagnostics, so it can be stale/out of sync with the current report.",
          "suggestedFix": "Have refreshDiagnostics() also refresh the session snapshot (session = await backend.currentSnapshot()) when fetching the report, or have DiagnosticsReport carry a lastError field the view reads so the displayed error matches report generation.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier2:applied:FU-DIAG-SYNC-DESYNC",
          "uid": "DIAG-03#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-DIAG-SYNC-DESYNC",
          "fixTier": "tier2"
        },
        {
          "severity": "low",
          "type": "dead-code",
          "title": "DiagnosticsUnavailableView is near-unreachable and the mixer panel shows no empty state when diagnostics is nil",
          "detail": "Re-read: backend.diagnosticsReport() returns a non-optional DiagnosticsReport (AudioControlBackend.swift:54) so it is never nil, store.diagnostics starts nil (AppStore.swift:30) and DiagnosticsSettingsView refreshes onAppear, so store.diagnostics is nil only for the brief async window before the first report resolves. The DiagnosticsUnavailableView (SettingsView.swift:304-306, 316-346) with its lastError surfacing + Refresh + Recover Routes is therefore effectively dead UI. Worse, the mixer DiagnosticsPanel renders an empty DisclosureGroup body when diagnostics is nil (MainWindowView.swift:530-557: 'if let diagnostics' with no else), so during that window an expanded panel shows nothing. Confirmed.",
          "codeRef": "Sources/Waves/Settings/SettingsView.swift:304-306",
          "expectedVsActual": "Story: a clear unavailable state with retry/recover the user can act on. Actual: the unavailable view almost never appears because the report is always non-nil; the mixer panel has no empty state at all for the nil case.",
          "suggestedFix": "Give the mixer DiagnosticsPanel an explicit empty/loading state mirroring DiagnosticsUnavailableView, or let diagnosticsReport()/refreshDiagnostics return nil on real backend failure so the unavailable state is genuinely reachable.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Settings/SettingsView.swift",
          "disposition": "deferred:needs-user-decision:Not truly dead code; removal needs a new empty/loading state design decision, and fix spans MainWindowView.",
          "uid": "DIAG-03#2",
          "canonicalFile": "Sources/Waves/Settings/SettingsView.swift"
        }
      ]
    },
    {
      "id": "DIAG-04",
      "area": "Diagnostics Export & Route Health Report",
      "feature": "Route health badge in the mixer header",
      "priority": "P1",
      "userStory": "As a user glancing at the mixer, I want an at-a-glance routing-status badge in the output header, so that I instantly know whether per-app control is Ready, Limited, or Needs attention without opening diagnostics.",
      "expectedBehavior": [
        "RouteHealthBadge shows a capsule label with icon, driven by backendStatus (MainWindowView.swift:479-520)",
        "When isRouteRecoveryHealthy it reads 'Ready' with a green checkmark.circle.fill (MainWindowView.swift:493-509)",
        "When not healthy but lastError != nil it reads 'Needs attention' in red with exclamationmark.triangle.fill (MainWindowView.swift:497-512)",
        "When not healthy and no lastError it reads 'Limited' in secondary color (MainWindowView.swift:500-514)",
        "The badge exposes a VoiceOver/help string 'Routing status: <title>' (MainWindowView.swift:489-490, 517-519)",
        "It is placed in the OutputSummaryView header next to the device name and live summary (MainWindowView.swift:461)"
      ],
      "acceptanceCriteria": [
        "When route recovery is healthy the badge shows 'Ready' with a green checkmark",
        "When there is a lastError and routing is unhealthy the badge shows 'Needs attention' in red with a warning triangle",
        "When routing is unhealthy with no recorded error the badge shows 'Limited' in a muted color",
        "The badge's accessibility label reads 'Routing status: ' followed by the current title",
        "The badge updates reactively when session.backendStatus changes"
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MainWindowView.swift:461",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:479-520"
      ],
      "implStatus": "implemented",
      "implNotes": "Clean, reactive, and accessible. The 'Limited' vs 'Needs attention' distinction hinges entirely on whether lastError happens to be populated, which is a fragile proxy: an unhealthy route with a transient/cleared error will read as the softer 'Limited' even when control is fully broken. The warning-triangle icon is shared by both unhealthy states, so only the title/color differentiate them.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/ux] RouteHealthBadge 'Limited' state shows a warning-triangle icon and is decided by a fragile lastError proxy (Sources/Waves/Features/Mixer/MainWindowView.swift:503-505) — Re-read MainWindowView.swift:503-505: systemImage returns 'exclamationmark.triangle.fill' for every non-healthy state, so the muted 'Limited' state pairs a secondary-gray label with a warning triangle — visually contradictory. And per title (493-501) / color (507-515), the only thing separating 'Limited' from 'Needs attention' is whether session.backendStatus.lastError is non-nil. An unhealthy route whose error was transient or cleared reads as the softer 'Limited' even though per-app control is fully broken, understating the problem. Confirmed. | Fix: Use a distinct lower-alarm SF Symbol for 'Limited' (e.g. minus.circle or exclamationmark.circle) and base the Limited/Needs-attention distinction on a concrete capability signal (captureAuthorization / hasRouteErrors) rather than solely lastError != nil.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier3: FU-ROUTEHEALTH-LIMITED-DISTINCT-ICON",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "ux",
          "title": "RouteHealthBadge 'Limited' state shows a warning-triangle icon and is decided by a fragile lastError proxy",
          "detail": "Re-read MainWindowView.swift:503-505: systemImage returns 'exclamationmark.triangle.fill' for every non-healthy state, so the muted 'Limited' state pairs a secondary-gray label with a warning triangle — visually contradictory. And per title (493-501) / color (507-515), the only thing separating 'Limited' from 'Needs attention' is whether session.backendStatus.lastError is non-nil. An unhealthy route whose error was transient or cleared reads as the softer 'Limited' even though per-app control is fully broken, understating the problem. Confirmed.",
          "codeRef": "Sources/Waves/Features/Mixer/MainWindowView.swift:503-505",
          "expectedVsActual": "Story AC: healthy=Ready/green check, unhealthy+error=Needs attention/red triangle, unhealthy+no error=Limited/muted. Actual: 'Limited' is rendered with the same alarming triangle as 'Needs attention', and the Limited-vs-Needs-attention split relies on a possibly-cleared lastError rather than actual capability.",
          "suggestedFix": "Use a distinct lower-alarm SF Symbol for 'Limited' (e.g. minus.circle or exclamationmark.circle) and base the Limited/Needs-attention distinction on a concrete capability signal (captureAuthorization / hasRouteErrors) rather than solely lastError != nil.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "disposition": "tier3:applied:FU-ROUTEHEALTH-LIMITED-DISTINCT-ICON",
          "uid": "DIAG-04#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-ROUTEHEALTH-LIMITED-DISTINCT-ICON",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "DIAG-05",
      "area": "Diagnostics Export & Route Health Report",
      "feature": "Route health gates onboarding readiness",
      "priority": "P2",
      "userStory": "As a first-run user, I want the onboarding flow to confirm route health is ready before declaring setup complete, so that I don't finish setup while per-app routing is still broken.",
      "expectedBehavior": [
        "syncOnboarding maps onboarding.routeHealthReady = snapshot.backendStatus.isRouteRecoveryHealthy (AppStore.swift:1361)",
        "OnboardingState.isComplete requires permissionsGranted && outputDeviceVisible && routeHealthReady (AppStore.swift:1501-1505)",
        "The onboarding route-health step calls store.refreshDiagnostics() and gates step completion/advancement on routeHealthReady (OnboardingView.swift:34-42, 124, 143, 153)",
        "Diagnostics are refreshed across virtually every state-changing path (start, refresh, preset apply, device change, recover, exclude) keeping routeHealthReady current (AppStore.swift:215, 246, 430, 455, 526, 537, 572, 583, 807, 1012, 1024, 1186, 1197)"
      ],
      "acceptanceCriteria": [
        "Onboarding cannot report complete unless isRouteRecoveryHealthy is true at the time of the last sync",
        "The onboarding route-health step is marked ready only when route recovery is healthy",
        "Triggering 'Recover Routes' during onboarding refreshes diagnostics and can flip the route-health step to ready",
        "routeHealthReady reflects the most recent snapshot after any session-changing action"
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:1358-1361",
        "Sources/Waves/Stores/AppStore.swift:1501-1505",
        "Sources/Waves/Features/Onboarding/OnboardingView.swift:34-42",
        "Sources/Waves/Features/Onboarding/OnboardingView.swift:124-153"
      ],
      "implStatus": "implemented",
      "implNotes": "Tightly integrated: route-health state is a first-class onboarding gate and is kept fresh by refreshing diagnostics on nearly every mutation. This is more a consumer of route health than a distinct diagnostics-export feature, but it is the most user-visible consequence of the route-health signal. Concern: isRouteRecoveryHealthy can be true even when capture permission is undetermined in some snapshot paths (it is computed from supportsPerAppRouting && captureAuthorization==.authorized && !hasRouteErrors at WorkspaceAudioControlBackend.swift:1089), so the gate is sound at the source but depends on captureAuthorization having been probed first.",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [2M 1L]",
      "errorsFound": "• [MEDIUM/logic] routeHealthReady is not updated on volume/mute/boost/preset paths despite diagnostics being refreshed there (Sources/Waves/Stores/AppStore.swift:525-537) — Re-read all syncOnboarding / diagnostics call sites. routeHealthReady is only assigned inside syncOnboarding(using:) (AppStore.swift:1361), called at 100, 206, 218, 247, 809, 1188 — start, refresh, device-change restore, exclude, recoverRoutes. The volume-apply (429-430/453-455), mute (525-526/536-537), boost (571-572/581-583), and preset-apply (1012, 1024) paths refresh store.diagnostics and call mergeAppState (which DOES update session.backendStatus at :1429, so the RouteHealthBadge re-derives correctly) but never call syncOnboarding. So after a mute/boost/volume/preset action, onboarding.routeHealthReady goes stale relative to the now-fresh session.backendStatus.isRouteRecoveryHealthy — a real state-desync: the mixer route badge can flip while the onboarding route-health step (and isFullyComplete) lag until the next full sync. Confirmed. | Fix: Call syncOnboarding(using: session) after mergeAppState on the mute/boost/volume/preset-apply paths (or factor a helper that always pairs a session refresh with syncOnboarding) so routeHealthReady tracks every session-changing action.\n• [MEDIUM/dead-code] OnboardingState.isReadyForEverydayUse (the cited completion gate) is unused; live UI uses a divergent completeness check (Sources/Waves/Stores/AppStore.swift:1504-1506) — Re-read AppStore.swift:1504-1506 and grep'd the whole tree: OnboardingState.isReadyForEverydayUse (permissionsGranted && outputDeviceVisible && routeHealthReady) is never referenced anywhere except its own definition. The completeness shown to the user is OnboardingView.isFullyComplete (OnboardingView.swift:39-43), which checks audioComponentInstalled && outputDeviceVisible && routeHealthReady — it gates on audioComponentInstalled, NOT permissionsGranted. So the only completeness logic that drives UI omits the capture-permission gate (a 14.2 system with capture denied can still read 'You're all set!'), while the permission-aware gate is dead code. Confirmed. | Fix: Delete isReadyForEverydayUse, or have OnboardingView.isFullyComplete delegate to store.onboarding.isReadyForEverydayUse so there is a single source of truth for completeness.\n• [LOW/inconsistency] Story claims onboarding refreshes diagnostics in .onAppear, but OnboardingView has no .onAppear (Sources/Waves/Features/Onboarding/OnboardingView.swift:45-137) — Re-read OnboardingView.swift in full: there is no .onAppear modifier anywhere. Lines 30-43 are completionProgress/isFullyComplete computed properties; 122-125 is the global 'Refresh Status' button (store.refresh + store.refreshDiagnostics); 139-157 are isStepComplete/canPerformAction; the route-health step (index 2) action is store.recoverRoutes() (line 166), which does refresh diagnostics and syncOnboarding internally. So the gate can still flip via explicit action, but there is no automatic onAppear refresh — on first showing, the route-health step reflects whatever the last sync produced rather than a fresh probe. Confirmed (low: requires only one extra user click). | Fix: Add .onAppear { store.refresh(); store.refreshDiagnostics() } to OnboardingView (or the route-health step) so route-health state is probed when the flow is shown, matching the documented behavior.",
      "fixStatus": "3 fixed",
      "fixNotes": "tier2: FU-DIAG-SYNC-DESYNC",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "logic",
          "title": "routeHealthReady is not updated on volume/mute/boost/preset paths despite diagnostics being refreshed there",
          "detail": "Re-read all syncOnboarding / diagnostics call sites. routeHealthReady is only assigned inside syncOnboarding(using:) (AppStore.swift:1361), called at 100, 206, 218, 247, 809, 1188 — start, refresh, device-change restore, exclude, recoverRoutes. The volume-apply (429-430/453-455), mute (525-526/536-537), boost (571-572/581-583), and preset-apply (1012, 1024) paths refresh store.diagnostics and call mergeAppState (which DOES update session.backendStatus at :1429, so the RouteHealthBadge re-derives correctly) but never call syncOnboarding. So after a mute/boost/volume/preset action, onboarding.routeHealthReady goes stale relative to the now-fresh session.backendStatus.isRouteRecoveryHealthy — a real state-desync: the mixer route badge can flip while the onboarding route-health step (and isFullyComplete) lag until the next full sync. Confirmed.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:525-537",
          "expectedVsActual": "Story AC: 'routeHealthReady reflects the most recent snapshot after any session-changing action.' Actual: after mute/boost/volume/preset-apply, diagnostics and session.backendStatus are current but onboarding.routeHealthReady is not re-derived, so the onboarding gate and the route badge can disagree.",
          "suggestedFix": "Call syncOnboarding(using: session) after mergeAppState on the mute/boost/volume/preset-apply paths (or factor a helper that always pairs a session refresh with syncOnboarding) so routeHealthReady tracks every session-changing action.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier2:applied:FU-DIAG-SYNC-DESYNC",
          "uid": "DIAG-05#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-DIAG-SYNC-DESYNC",
          "fixTier": "tier2"
        },
        {
          "severity": "medium",
          "type": "dead-code",
          "title": "OnboardingState.isReadyForEverydayUse (the cited completion gate) is unused; live UI uses a divergent completeness check",
          "detail": "Re-read AppStore.swift:1504-1506 and grep'd the whole tree: OnboardingState.isReadyForEverydayUse (permissionsGranted && outputDeviceVisible && routeHealthReady) is never referenced anywhere except its own definition. The completeness shown to the user is OnboardingView.isFullyComplete (OnboardingView.swift:39-43), which checks audioComponentInstalled && outputDeviceVisible && routeHealthReady — it gates on audioComponentInstalled, NOT permissionsGranted. So the only completeness logic that drives UI omits the capture-permission gate (a 14.2 system with capture denied can still read 'You're all set!'), while the permission-aware gate is dead code. Confirmed.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1504-1506",
          "expectedVsActual": "Story AC: onboarding cannot report complete unless route recovery is healthy via a gate combining permissionsGranted + outputDeviceVisible + routeHealthReady. Actual: that gate (isReadyForEverydayUse) is dead code; the live UI uses isFullyComplete which substitutes audioComponentInstalled for permissionsGranted, so the two definitions diverge. (routeHealthReady itself already folds in capture authorization, so the user-facing gate is still sound on permission — but the permissionsGranted-based property is dead and misleading.)",
          "suggestedFix": "Delete isReadyForEverydayUse, or have OnboardingView.isFullyComplete delegate to store.onboarding.isReadyForEverydayUse so there is a single source of truth for completeness.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "iter2:fixed",
          "uid": "DIAG-05#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "iter2-deferred-reduction",
          "fixTier": "iter2"
        },
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "Story claims onboarding refreshes diagnostics in .onAppear, but OnboardingView has no .onAppear",
          "detail": "Re-read OnboardingView.swift in full: there is no .onAppear modifier anywhere. Lines 30-43 are completionProgress/isFullyComplete computed properties; 122-125 is the global 'Refresh Status' button (store.refresh + store.refreshDiagnostics); 139-157 are isStepComplete/canPerformAction; the route-health step (index 2) action is store.recoverRoutes() (line 166), which does refresh diagnostics and syncOnboarding internally. So the gate can still flip via explicit action, but there is no automatic onAppear refresh — on first showing, the route-health step reflects whatever the last sync produced rather than a fresh probe. Confirmed (low: requires only one extra user click).",
          "codeRef": "Sources/Waves/Features/Onboarding/OnboardingView.swift:45-137",
          "expectedVsActual": "Story: onboarding auto-refreshes diagnostics on appear to keep the route-health step current. Actual: no onAppear refresh exists; the step is refreshed only by explicit user action (Refresh Status button or Test Recovery / recoverRoutes).",
          "suggestedFix": "Add .onAppear { store.refresh(); store.refreshDiagnostics() } to OnboardingView (or the route-health step) so route-health state is probed when the flow is shown, matching the documented behavior.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Onboarding/OnboardingView.swift",
          "disposition": "gapfill:fixed",
          "uid": "DIAG-05#3",
          "canonicalFile": "Sources/Waves/Features/Onboarding/OnboardingView.swift",
          "fixStatus": "applied",
          "fixedBy": "gapfill",
          "fixTier": "gapfill"
        }
      ]
    },
    {
      "id": "SETUP-01",
      "area": "Setup Checklist & Permissions",
      "feature": "Guided setup checklist with progress",
      "priority": "P1",
      "userStory": "As a new Waves user, I want a step-by-step setup checklist that shows what is done and what still needs attention, so that I can get per-app audio working without guessing.",
      "expectedBehavior": [
        "The Settings window exposes a \"Setup\" tab (checklist icon) that renders OnboardingView (SettingsView.swift:13-16).",
        "The checklist shows four ordered steps: confirm managed audio support, validate output-device visibility, test device switching/recovery, and enable accessibility for keyboard shortcuts (OnboardingView.swift:7-28).",
        "A ProgressView reflects completion as completedSteps/3, counting only audioComponentInstalled, outputDeviceVisible, and routeHealthReady — the accessibility step is intentionally excluded from progress (OnboardingView.swift:30-37).",
        "Each step row shows a green check when complete, an amber warning triangle with an action button when actionable, or a grey pending circle otherwise, with accessibility labels Completed/Needs action/Pending (OnboardingView.swift:185-218).",
        "Header copy switches to \"You're all set!\" and a green \"Ready to use\" capabilities panel appears once audioComponentInstalled && outputDeviceVisible && routeHealthReady are all true (OnboardingView.swift:39-43, 51-53, 74-117).",
        "A bottom \"Refresh Status\" button calls store.refresh() + store.refreshDiagnostics(); an \"Enable Launch at Login\" button appears only while launchAtLoginEnabled is false (OnboardingView.swift:121-134)."
      ],
      "acceptanceCriteria": [
        "Opening Settings › Setup shows exactly four steps in the listed order.",
        "With all three gating flags true the progress bar reads 100%, tints green, and the \"Ready to use\" panel is visible.",
        "With any gating flag false the header reads the \"Let's get Waves set up\" copy and the panel is hidden.",
        "Granting/denying accessibility changes step 4's icon but does NOT move the progress bar.",
        "\"Enable Launch at Login\" button is hidden once launch-at-login is enabled."
      ],
      "codeRefs": [
        "Sources/Waves/Settings/SettingsView.swift:13-16",
        "Sources/Waves/Features/Onboarding/OnboardingView.swift:7-37",
        "Sources/Waves/Features/Onboarding/OnboardingView.swift:39-137",
        "Sources/Waves/Features/Onboarding/OnboardingView.swift:177-218"
      ],
      "implStatus": "implemented",
      "implNotes": "Wired end to end. UX gap: the most decisive permission for routing to actually work — audio-capture (microphone/TCC) — is NOT one of the four checklist steps. A user can reach a 100% green \"You're all set\" checklist while capture is denied, in which case per-app volume/mute silently does nothing; capture status only surfaces in the separate Advanced diagnostics tab. The unused shared component SetupStepRow (Sources/Waves/Shared/UI/SetupStepRow.swift) is dead code — OnboardingView uses its own private EnhancedSetupStepRow.",
      "source": "phase1-story",
      "testStatus": "5 issue(s) [1M 4L]",
      "errorsFound": "• [MEDIUM/ux] Decisive audio-capture (Microphone/TCC) permission is never surfaced on the setup checklist (Sources/Waves/Features/Onboarding/OnboardingView.swift:7-28) — Verified in code. The four checklist steps (OnboardingView.swift:7-28) cover OS support, output-device visibility, route recovery, and accessibility. The decisive gate for per-app volume/mute/boost — Core Audio capture authorization (captureAuthorization == .authorized) — is only folded into routeHealthReady (WorkspaceAudioControlBackend.swift:1089). When capture is denied, routeHealthReady is false so step 3 stays incomplete, the user is stranded at 2/3 (66%) with only a 'Test Recovery' button (which silently no-ops, see SETUP-04), and no copy anywhere on the checklist names Microphone permission as the blocker. Capture status only appears in the separate Advanced tab. Note: the first tester's framing that the user can reach 100% 'all set' while capture is denied is slightly imprecise (routeHealthReady gates isFullyComplete, so 100% is NOT reachable with capture denied); the real, confirmed defect is the unexplained stuck step with no guided fix path. | Fix: Add a fifth checklist step (or split step 3) for 'Allow audio capture' bound to backend captureAuthorization, with an 'Open System Settings' action deep-linking to Privacy_Microphone and copy explaining routing silently no-ops without it.\n• [LOW/dead-code] Shared SetupStepRow component is dead code (Sources/Waves/Shared/UI/SetupStepRow.swift:3) — Confirmed via grep: 'SetupStepRow(' is never constructed anywhere; only the struct declaration at SetupStepRow.swift:3 exists. OnboardingView builds its own private EnhancedSetupStepRow (OnboardingView.swift:62,177). The shared component is also strictly less capable (no action button, no accessibility labels). | Fix: Delete Sources/Waves/Shared/UI/SetupStepRow.swift, or consolidate OnboardingView onto it.\n• [LOW/dead-code] OnboardingState.isReadyForEverydayUse and permissionsGranted are unused (Sources/Waves/Stores/AppStore.swift:1504) — Confirmed via grep. syncOnboarding writes onboarding.permissionsGranted (AppStore.swift:1358) but the only reader is OnboardingState.isReadyForEverydayUse (AppStore.swift:1504-1506), which itself has zero references anywhere. OnboardingView computes its own isFullyComplete (OnboardingView.swift:39-43) instead. This is a parallel readiness computation that can silently diverge from the live one. | Fix: Remove isReadyForEverydayUse and permissionsGranted, or have OnboardingView consume a single store-level readiness computation.\n• [LOW/accessibility] Progress bar exposes no value/label to VoiceOver (Sources/Waves/Features/Onboarding/OnboardingView.swift:57) — Confirmed. ProgressView(value: completionProgress) at OnboardingView.swift:57 has no accessibilityLabel/accessibilityValue, so VoiceOver gets no 'X of 3 steps complete' context. (Note: completionProgress = completedSteps/3, so the suggested completionProgress*3 yields the integer step count — math is correct.) | Fix: Add .accessibilityLabel(\"Setup progress\") and .accessibilityValue(\"\\(Int(completionProgress*3)) of 3 steps complete\") to the ProgressView.\n• [LOW/inconsistency] Two simultaneously visible buttons labeled 'Refresh Status' (Sources/Waves/Features/Onboarding/OnboardingView.swift:122) — Confirmed. steps[0].action is 'Refresh Status' (OnboardingView.swift:11), rendered when canPerformAction(0) = !audioComponentInstalled (line 151,204). The bottom global button is also 'Refresh Status' (line 122) and additionally calls refreshDiagnostics(). When step 1 is incomplete both render at once with identical labels but slightly different behavior — ambiguous for keyboard/VoiceOver users. | Fix: Rename the step-0 action (e.g. 'Re-check support') or give the buttons distinct accessibility labels.",
      "fixStatus": "4 fixed · 1 deferred",
      "fixNotes": "tier3: FU-ONB-PROGRESS-A11Y; FU-ONB-REFRESH-LABEL-DUP; FU-SETUPSTEPROW-DEAD-FILE",
      "retestStatus": "PASS (remediated & verified)",
      "issues": [
        {
          "severity": "medium",
          "type": "ux",
          "title": "Decisive audio-capture (Microphone/TCC) permission is never surfaced on the setup checklist",
          "detail": "Verified in code. The four checklist steps (OnboardingView.swift:7-28) cover OS support, output-device visibility, route recovery, and accessibility. The decisive gate for per-app volume/mute/boost — Core Audio capture authorization (captureAuthorization == .authorized) — is only folded into routeHealthReady (WorkspaceAudioControlBackend.swift:1089). When capture is denied, routeHealthReady is false so step 3 stays incomplete, the user is stranded at 2/3 (66%) with only a 'Test Recovery' button (which silently no-ops, see SETUP-04), and no copy anywhere on the checklist names Microphone permission as the blocker. Capture status only appears in the separate Advanced tab. Note: the first tester's framing that the user can reach 100% 'all set' while capture is denied is slightly imprecise (routeHealthReady gates isFullyComplete, so 100% is NOT reachable with capture denied); the real, confirmed defect is the unexplained stuck step with no guided fix path.",
          "codeRef": "Sources/Waves/Features/Onboarding/OnboardingView.swift:7-28",
          "expectedVsActual": "Story (SETUP-01): a guided checklist so a new user can 'get per-app audio working without guessing.' Actual: the single most decisive permission for routing is absent from the checklist; a capture-denied user sees an unexplained stuck step 3 and no path to discover/grant Microphone permission from the checklist.",
          "suggestedFix": "Add a fifth checklist step (or split step 3) for 'Allow audio capture' bound to backend captureAuthorization, with an 'Open System Settings' action deep-linking to Privacy_Microphone and copy explaining routing silently no-ops without it.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Onboarding/OnboardingView.swift",
          "disposition": "gapfill:fixed",
          "uid": "SETUP-01#1",
          "canonicalFile": "Sources/Waves/Features/Onboarding/OnboardingView.swift",
          "fixStatus": "applied",
          "fixedBy": "gapfill",
          "fixTier": "gapfill"
        },
        {
          "severity": "low",
          "type": "dead-code",
          "title": "Shared SetupStepRow component is dead code",
          "detail": "Confirmed via grep: 'SetupStepRow(' is never constructed anywhere; only the struct declaration at SetupStepRow.swift:3 exists. OnboardingView builds its own private EnhancedSetupStepRow (OnboardingView.swift:62,177). The shared component is also strictly less capable (no action button, no accessibility labels).",
          "codeRef": "Sources/Waves/Shared/UI/SetupStepRow.swift:3",
          "expectedVsActual": "Expected: one reusable step-row component. Actual: SetupStepRow.swift is unreferenced dead code while a near-duplicate private view is used instead.",
          "suggestedFix": "Delete Sources/Waves/Shared/UI/SetupStepRow.swift, or consolidate OnboardingView onto it.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Shared/UI/SetupStepRow.swift",
          "disposition": "tier3:applied:FU-SETUPSTEPROW-DEAD-FILE",
          "uid": "SETUP-01#2",
          "canonicalFile": "Sources/Waves/Shared/UI/SetupStepRow.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-SETUPSTEPROW-DEAD-FILE",
          "fixTier": "tier3"
        },
        {
          "severity": "low",
          "type": "dead-code",
          "title": "OnboardingState.isReadyForEverydayUse and permissionsGranted are unused",
          "detail": "Confirmed via grep. syncOnboarding writes onboarding.permissionsGranted (AppStore.swift:1358) but the only reader is OnboardingState.isReadyForEverydayUse (AppStore.swift:1504-1506), which itself has zero references anywhere. OnboardingView computes its own isFullyComplete (OnboardingView.swift:39-43) instead. This is a parallel readiness computation that can silently diverge from the live one.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1504",
          "expectedVsActual": "Expected: completion/readiness logic in one place. Actual: a dead parallel computation (permissionsGranted + isReadyForEverydayUse) exists alongside the live isFullyComplete in the view.",
          "suggestedFix": "Remove isReadyForEverydayUse and permissionsGranted, or have OnboardingView consume a single store-level readiness computation.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:This is a product/design decision (delete vs. surface the permission step). Option (b) materially changes onboarding 'complete' semantics (capability-vs-permission conflation, ONB-02's core), which is exactly the kind of behavior change that needs an owner decision, not a drive-by fix. Even the delete-only path should be confirmed against the intended onboarding model rather than removed unilaterally.",
          "uid": "SETUP-01#3",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "low",
          "type": "accessibility",
          "title": "Progress bar exposes no value/label to VoiceOver",
          "detail": "Confirmed. ProgressView(value: completionProgress) at OnboardingView.swift:57 has no accessibilityLabel/accessibilityValue, so VoiceOver gets no 'X of 3 steps complete' context. (Note: completionProgress = completedSteps/3, so the suggested completionProgress*3 yields the integer step count — math is correct.)",
          "codeRef": "Sources/Waves/Features/Onboarding/OnboardingView.swift:57",
          "expectedVsActual": "Story: a checklist that 'shows what is done and what still needs attention.' Actual: completion is conveyed visually only; assistive tech gets no spoken progress value.",
          "suggestedFix": "Add .accessibilityLabel(\"Setup progress\") and .accessibilityValue(\"\\(Int(completionProgress*3)) of 3 steps complete\") to the ProgressView.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Onboarding/OnboardingView.swift",
          "disposition": "tier3:applied:FU-ONB-PROGRESS-A11Y",
          "uid": "SETUP-01#4",
          "canonicalFile": "Sources/Waves/Features/Onboarding/OnboardingView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-ONB-PROGRESS-A11Y",
          "fixTier": "tier3"
        },
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "Two simultaneously visible buttons labeled 'Refresh Status'",
          "detail": "Confirmed. steps[0].action is 'Refresh Status' (OnboardingView.swift:11), rendered when canPerformAction(0) = !audioComponentInstalled (line 151,204). The bottom global button is also 'Refresh Status' (line 122) and additionally calls refreshDiagnostics(). When step 1 is incomplete both render at once with identical labels but slightly different behavior — ambiguous for keyboard/VoiceOver users.",
          "codeRef": "Sources/Waves/Features/Onboarding/OnboardingView.swift:122",
          "expectedVsActual": "Expected: each control uniquely identifiable. Actual: duplicate 'Refresh Status' labels can coexist with different side effects.",
          "suggestedFix": "Rename the step-0 action (e.g. 'Re-check support') or give the buttons distinct accessibility labels.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Onboarding/OnboardingView.swift",
          "disposition": "tier3:applied:FU-ONB-REFRESH-LABEL-DUP",
          "uid": "SETUP-01#5",
          "canonicalFile": "Sources/Waves/Features/Onboarding/OnboardingView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-ONB-REFRESH-LABEL-DUP",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "SETUP-02",
      "area": "Setup Checklist & Permissions",
      "feature": "Managed-audio support check (macOS 14.2+ / process taps)",
      "priority": "P0",
      "userStory": "As a user on an older or unsupported macOS, I want Waves to tell me clearly that per-app routing requires macOS 14.2+, so that I understand why the feature is unavailable instead of seeing silent failures.",
      "expectedBehavior": [
        "Step 1 (\"Confirm managed audio support\") is complete iff onboarding.audioComponentInstalled, which mirrors snapshot.backendStatus.isAudioComponentInstalled (OnboardingView.swift:141, AppStore.swift:1357).",
        "isAudioComponentInstalled is set to supportsPerAppRouting, true only under #available(macOS 14.2, *) (WorkspaceAudioControlBackend.swift:310-316, 1087).",
        "When incomplete, the row's \"Refresh Status\" action re-runs store.refresh() to re-probe support (OnboardingView.swift:151, 161-162).",
        "The Advanced diagnostics tab shows an \"Audio component\" check: passed with \"Process tap routing is supported\" or warning \"Per-app routing needs macOS 14.2 or newer.\" (WorkspaceAudioControlBackend.swift:245-251)."
      ],
      "acceptanceCriteria": [
        "On macOS 14.2+ step 1 reads complete and the Audio component diagnostic passes.",
        "On macOS < 14.2 step 1 stays incomplete and shows the 14.2 warning copy in both the checklist detail and the diagnostics check.",
        "Tapping \"Refresh Status\" on this step calls refresh() and updates audioComponentInstalled from a fresh snapshot."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Onboarding/OnboardingView.swift:139-162",
        "Sources/Waves/Stores/AppStore.swift:1356-1361",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:310-316",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1086-1093"
      ],
      "implStatus": "implemented",
      "implNotes": "Correctly gated on the OS version that actually enables Core Audio process taps. This step measures OS capability only, not whether capture is granted — accurate to its label.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "SETUP-03",
      "area": "Setup Checklist & Permissions",
      "feature": "Output-device visibility check",
      "priority": "P1",
      "userStory": "As a user, I want Waves to confirm it can see my output device before it tries to route audio, so that I know speakers/headphones are recognized by macOS.",
      "expectedBehavior": [
        "Step 2 (\"Validate output device visibility\") is complete iff onboarding.outputDeviceVisible, set from snapshot.currentDevice != nil (OnboardingView.swift:142, AppStore.swift:1360).",
        "When incomplete (no current device), the row exposes a \"Refresh Devices\" action that calls store.refresh() (OnboardingView.swift:152, 163-164).",
        "Step 3's recovery action is gated on step 2 being complete: canPerformAction(2) requires outputDeviceVisible && !routeHealthReady (OnboardingView.swift:153).",
        "Current device name is shown in the Audio settings tab and the diagnostics export (\"Output device: …\" / \"No output device\") (AppStore.swift:139-141, 731)."
      ],
      "acceptanceCriteria": [
        "With at least one output device present, step 2 reads complete.",
        "With no current device, step 2 shows the warning and a \"Refresh Devices\" button that triggers refresh().",
        "Step 3's \"Test Recovery\" button is disabled/hidden until step 2 is complete."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Onboarding/OnboardingView.swift:142,152,153,163-164",
        "Sources/Waves/Stores/AppStore.swift:1360",
        "Sources/Waves/Stores/AppStore.swift:139-141",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1062-1070"
      ],
      "implStatus": "partial",
      "implNotes": "Two correctness/UX concerns. (1) OnboardingState.outputDeviceVisible defaults to true (AppStore.swift:1500), so before the first snapshot loads the step optimistically reads complete. (2) buildSnapshot almost always synthesizes a fallback currentDevice (\"System Output\") when currentOutputDevice() fails (WorkspaceAudioControlBackend.swift:1062-1070), so snapshot.currentDevice is rarely nil and this step will read complete even when no real hardware device is selectable — making the check close to a no-op in practice.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1M 1L]",
      "errorsFound": "• [MEDIUM/correctness] Output-device check is a near no-op because buildSnapshot synthesizes a fallback 'System Output' device (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1062-1070) — Confirmed in code. outputDeviceVisible is set from snapshot.currentDevice != nil (AppStore.swift:1360). But buildSnapshot computes currentDevice = (try? currentOutputDevice()) ?? previousSnapshot?.currentDevice ?? AudioDevice(id: \"system-output\", name: \"System Output\", ...) (WorkspaceAudioControlBackend.swift:1062-1070), so currentDevice is effectively never nil even when currentDefaultOutputDeviceID() throws managedRouteUnavailable (lines 654-656). Step 2 therefore reads complete even when no real hardware output is selectable — the AC state 'With no current device, step 2 shows the warning and a Refresh Devices button' is practically unreachable. The synthetic fallback even sets isManagedRouteAvailable: supportsPerAppRouting, masking the failure. | Fix: Drive outputDeviceVisible from the presence of a real device (e.g. !availableOutputDevices().isEmpty, or a synthesized-vs-real flag on AudioDevice) and do not let the synthetic 'System Output' fallback satisfy the visibility check.\n• [LOW/edge-case] outputDeviceVisible defaults to true and can show complete from a stale cached session before the live probe (Sources/Waves/Stores/AppStore.swift:1500) — Confirmed. OnboardingState.outputDeviceVisible defaults to true (AppStore.swift:1500). init() runs syncOnboarding(using: session) where session = sessionStore.load() ?? emptySession (AppStore.swift:83,100), and start() also calls syncOnboarding from the warm/cached snapshot before backend.start() (AppStore.swift:203-207) and again after the live snapshot (line 218). For emptySession it correctly flips to false (currentDevice nil), but a cached session with a now-stale currentDevice makes step 2 read complete from cache, momentarily reflecting a device that may no longer be present until the live buildSnapshot lands. | Fix: Default outputDeviceVisible to false and only set true from a freshly built live snapshot, or mark cached-derived status as provisional until backend.start() completes.",
      "fixStatus": "1 fixed · 1 deferred",
      "fixNotes": "tier3: FU-ONBOARDING-DEVICE-DEFAULT",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "correctness",
          "title": "Output-device check is a near no-op because buildSnapshot synthesizes a fallback 'System Output' device",
          "detail": "Confirmed in code. outputDeviceVisible is set from snapshot.currentDevice != nil (AppStore.swift:1360). But buildSnapshot computes currentDevice = (try? currentOutputDevice()) ?? previousSnapshot?.currentDevice ?? AudioDevice(id: \"system-output\", name: \"System Output\", ...) (WorkspaceAudioControlBackend.swift:1062-1070), so currentDevice is effectively never nil even when currentDefaultOutputDeviceID() throws managedRouteUnavailable (lines 654-656). Step 2 therefore reads complete even when no real hardware output is selectable — the AC state 'With no current device, step 2 shows the warning and a Refresh Devices button' is practically unreachable. The synthetic fallback even sets isManagedRouteAvailable: supportsPerAppRouting, masking the failure.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1062-1070",
          "expectedVsActual": "Story AC (SETUP-03): 'With no current device, step 2 shows the warning and a Refresh Devices button.' Actual: there is essentially no path to a nil currentDevice, so the warning/Refresh-Devices state is practically unreachable and the step always passes.",
          "suggestedFix": "Drive outputDeviceVisible from the presence of a real device (e.g. !availableOutputDevices().isEmpty, or a synthesized-vs-real flag on AudioDevice) and do not let the synthetic 'System Output' fallback satisfy the visibility check.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "deferred:needs-user-decision:The synthetic fallback is deliberate so downstream consumers always have a non-nil currentDevice; changing the no-device signal needs a model/flag decision and ",
          "uid": "SETUP-03#1",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift"
        },
        {
          "severity": "low",
          "type": "edge-case",
          "title": "outputDeviceVisible defaults to true and can show complete from a stale cached session before the live probe",
          "detail": "Confirmed. OnboardingState.outputDeviceVisible defaults to true (AppStore.swift:1500). init() runs syncOnboarding(using: session) where session = sessionStore.load() ?? emptySession (AppStore.swift:83,100), and start() also calls syncOnboarding from the warm/cached snapshot before backend.start() (AppStore.swift:203-207) and again after the live snapshot (line 218). For emptySession it correctly flips to false (currentDevice nil), but a cached session with a now-stale currentDevice makes step 2 read complete from cache, momentarily reflecting a device that may no longer be present until the live buildSnapshot lands.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1500",
          "expectedVsActual": "Expected: step reflects live device state. Actual: prior to the first live buildSnapshot the step can reflect a cached/optimistic value.",
          "suggestedFix": "Default outputDeviceVisible to false and only set true from a freshly built live snapshot, or mark cached-derived status as provisional until backend.start() completes.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier3:applied:FU-ONBOARDING-DEVICE-DEFAULT",
          "uid": "SETUP-03#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-ONBOARDING-DEVICE-DEFAULT",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "SETUP-04",
      "area": "Setup Checklist & Permissions",
      "feature": "Route-health / recovery test",
      "priority": "P0",
      "userStory": "As a user, I want to verify that Waves can establish and reattach managed routes (e.g. after a device switch), so that I trust per-app control will survive device changes.",
      "expectedBehavior": [
        "Step 3 (\"Test device switching\") is complete iff onboarding.routeHealthReady, mirroring snapshot.backendStatus.isRouteRecoveryHealthy (OnboardingView.swift:143, AppStore.swift:1361).",
        "isRouteRecoveryHealthy is true only when supportsPerAppRouting && captureAuthorization == .authorized && no app is in routingState .error (WorkspaceAudioControlBackend.swift:1089).",
        "The step's \"Test Recovery\" action calls store.recoverRoutes(), which disposes controllers, rebuilds the snapshot, reattaches previously-managed routes, refreshes diagnostics, re-syncs onboarding, and toasts success or failure (OnboardingView.swift:166, AppStore.swift:1181-1202; backend recoverRoutes WorkspaceAudioControlBackend.swift:204-221).",
        "On recovery failure the store falls back to currentSnapshot(), still refreshes diagnostics, and shows an error toast (AppStore.swift:1194-1200).",
        "The Advanced diagnostics tab shows a \"Route recovery\" check: passed \"Per-app routing is active and can be reapplied\" or warning about active setup/control errors (WorkspaceAudioControlBackend.swift:264-270)."
      ],
      "acceptanceCriteria": [
        "With routing supported, capture authorized, and no route errors, step 3 reads complete and the Route recovery diagnostic passes.",
        "Tapping \"Test Recovery\" invokes recoverRoutes(); a success toast \"Routes recovered\" appears on success and an error toast on failure.",
        "If capture is not authorized, step 3 cannot become complete regardless of the recovery action."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Onboarding/OnboardingView.swift:143,153,166",
        "Sources/Waves/Stores/AppStore.swift:1181-1208",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:204-221",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:1088-1090",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:264-270"
      ],
      "implStatus": "implemented",
      "implNotes": "Logic is sound and route health correctly folds in capture authorization. Subtle UX trap: because routeHealthReady requires capture authorization, a user with capture denied sees step 3 stuck incomplete with only a \"Test Recovery\" button — but the actual blocker (microphone permission) is never surfaced on the checklist, only in the Advanced tab. Recovery here can therefore appear to \"not work\" with no on-checklist explanation.",
      "source": "phase1-story",
      "testStatus": "4 issue(s) [1H 3L]",
      "errorsFound": "• [HIGH/error-handling] 'Test Recovery' always shows a success toast even when recovery leaves routes unhealthy (capture denied) (Sources/Waves/Stores/AppStore.swift:1189-1193) — Confirmed. AppStore.recoverRoutes() shows the green 'Routes recovered' toast on any non-throwing return (AppStore.swift:1189-1193). Verified that backend.recoverRoutes() (WorkspaceAudioControlBackend.swift:204-221) has no throwing calls (disposeControllers, buildSnapshot, reattachRoutes are all non-throwing), so it cannot throw in production. buildSnapshot recomputes isRouteRecoveryHealthy = supportsPerAppRouting && captureAuthorization == .authorized && !hasRouteErrors (line 1089). So with capture denied: recoverRoutes() does not throw, the success toast fires, yet syncOnboarding sets routeHealthReady = false (AppStore.swift:1361) and step 3 visibly stays in the warning state. The user gets a green 'Routes recovered' confirmation contradicting the just-displayed incomplete step. (The success detail text 'Managed routing paths were reattached' is also misleading when nothing was managed.) | Fix: After recoverRoutes, branch on session.backendStatus.isRouteRecoveryHealthy: show success only when true; otherwise show a warning toast naming the real blocker (e.g. 'Audio capture not granted — open Microphone settings').\n• [LOW/dead-code] Recovery failure toast path is effectively unreachable with the production backend (Sources/Waves/Stores/AppStore.swift:1194-1200) — Confirmed. AppStore.recoverRoutes()'s catch branch (AppStore.swift:1194-1200) shows 'Recovery failed' and falls back to currentSnapshot(). The protocol declares recoverRoutes() async throws, but WorkspaceAudioControlBackend.recoverRoutes() (lines 204-221) contains no throwing calls, so it never throws in production — the error toast and currentSnapshot() fallback are dead in practice, and real failure conditions surface as a success toast (prior issue). | Fix: Have the backend surface real failures (throw when reattach fails or routes remain in .error), or drive success/failure messaging from the resulting isRouteRecoveryHealthy rather than from try/catch.\n• [LOW/dead-code] Backend sets isRouteRecoveryHealthy = true then immediately overwrites it (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:212) — Confirmed. recoverRoutes() assigns snapshot.backendStatus.isRouteRecoveryHealthy = true and lastError = nil (WorkspaceAudioControlBackend.swift:212-213), but the next statement snapshot = await buildSnapshot(merging: snapshot) (line 214) replaces the entire backendStatus with a freshly computed one (lines 1086-1091). The line-212/213 writes have no observable effect. | Fix: Remove the line-212/213 assignments and let buildSnapshot be the single source of route-health truth.\n• [LOW/ux] Once routes are healthy the 'Test Recovery' button disappears, so the check cannot be re-run from the checklist (Sources/Waves/Features/Onboarding/OnboardingView.swift:153) — Confirmed. canPerformAction(2) = outputDeviceVisible && !routeHealthReady (OnboardingView.swift:153) and the action button only renders when canPerformAction is true (OnboardingView.swift:204). Once routeHealthReady is true the button is hidden, yet the step copy says 'Run a quick check here' (OnboardingView.swift:20). A user wanting to re-verify recovery has no control on the checklist (only the Audio/Advanced tab's 'Recover Routes'). | Fix: Keep a secondary 'Re-test' button on the completed step, or move the test action out of the canPerformAction gate.",
      "fixStatus": "3 fixed · 1 n/a",
      "fixNotes": "tier3: FU-DEAD-RECOVER-HEALTH-WRITES",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "error-handling",
          "title": "'Test Recovery' always shows a success toast even when recovery leaves routes unhealthy (capture denied)",
          "detail": "Confirmed. AppStore.recoverRoutes() shows the green 'Routes recovered' toast on any non-throwing return (AppStore.swift:1189-1193). Verified that backend.recoverRoutes() (WorkspaceAudioControlBackend.swift:204-221) has no throwing calls (disposeControllers, buildSnapshot, reattachRoutes are all non-throwing), so it cannot throw in production. buildSnapshot recomputes isRouteRecoveryHealthy = supportsPerAppRouting && captureAuthorization == .authorized && !hasRouteErrors (line 1089). So with capture denied: recoverRoutes() does not throw, the success toast fires, yet syncOnboarding sets routeHealthReady = false (AppStore.swift:1361) and step 3 visibly stays in the warning state. The user gets a green 'Routes recovered' confirmation contradicting the just-displayed incomplete step. (The success detail text 'Managed routing paths were reattached' is also misleading when nothing was managed.)",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1189-1193",
          "expectedVsActual": "Story AC (SETUP-04): 'a success toast Routes recovered appears on success and an error toast on failure'; 'If capture is not authorized, step 3 cannot become complete.' Actual: a no-throw recovery that ends unhealthy still reports success while step 3 stays failed — misleading, contradictory feedback.",
          "suggestedFix": "After recoverRoutes, branch on session.backendStatus.isRouteRecoveryHealthy: show success only when true; otherwise show a warning toast naming the real blocker (e.g. 'Audio capture not granted — open Microphone settings').",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "iter2:fixed",
          "uid": "SETUP-04#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "iter2-deferred-reduction",
          "fixTier": "iter2"
        },
        {
          "severity": "low",
          "type": "dead-code",
          "title": "Recovery failure toast path is effectively unreachable with the production backend",
          "detail": "Confirmed. AppStore.recoverRoutes()'s catch branch (AppStore.swift:1194-1200) shows 'Recovery failed' and falls back to currentSnapshot(). The protocol declares recoverRoutes() async throws, but WorkspaceAudioControlBackend.recoverRoutes() (lines 204-221) contains no throwing calls, so it never throws in production — the error toast and currentSnapshot() fallback are dead in practice, and real failure conditions surface as a success toast (prior issue).",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1194-1200",
          "expectedVsActual": "Story AC: 'an error toast on failure.' Actual: production recovery cannot throw, so the failure UX is never exercised.",
          "suggestedFix": "Have the backend surface real failures (throw when reattach fails or routes remain in .error), or drive success/failure messaging from the resulting isRouteRecoveryHealthy rather than from try/catch.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "SETUP-04#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "low",
          "type": "dead-code",
          "title": "Backend sets isRouteRecoveryHealthy = true then immediately overwrites it",
          "detail": "Confirmed. recoverRoutes() assigns snapshot.backendStatus.isRouteRecoveryHealthy = true and lastError = nil (WorkspaceAudioControlBackend.swift:212-213), but the next statement snapshot = await buildSnapshot(merging: snapshot) (line 214) replaces the entire backendStatus with a freshly computed one (lines 1086-1091). The line-212/213 writes have no observable effect.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:212",
          "expectedVsActual": "Expected: meaningful state transitions. Actual: dead writes unconditionally overwritten by buildSnapshot.",
          "suggestedFix": "Remove the line-212/213 assignments and let buildSnapshot be the single source of route-health truth.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "tier3:applied:FU-DEAD-RECOVER-HEALTH-WRITES",
          "uid": "SETUP-04#3",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-DEAD-RECOVER-HEALTH-WRITES",
          "fixTier": "tier3"
        },
        {
          "severity": "low",
          "type": "ux",
          "title": "Once routes are healthy the 'Test Recovery' button disappears, so the check cannot be re-run from the checklist",
          "detail": "Confirmed. canPerformAction(2) = outputDeviceVisible && !routeHealthReady (OnboardingView.swift:153) and the action button only renders when canPerformAction is true (OnboardingView.swift:204). Once routeHealthReady is true the button is hidden, yet the step copy says 'Run a quick check here' (OnboardingView.swift:20). A user wanting to re-verify recovery has no control on the checklist (only the Audio/Advanced tab's 'Recover Routes').",
          "codeRef": "Sources/Waves/Features/Onboarding/OnboardingView.swift:153",
          "expectedVsActual": "Step copy implies an on-demand recovery check. Actual: the check is only runnable while it is failing; once passing the control vanishes.",
          "suggestedFix": "Keep a secondary 'Re-test' button on the completed step, or move the test action out of the canPerformAction gate.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Onboarding/OnboardingView.swift",
          "disposition": "gapfill:fixed",
          "uid": "SETUP-04#4",
          "canonicalFile": "Sources/Waves/Features/Onboarding/OnboardingView.swift",
          "fixStatus": "applied",
          "fixedBy": "gapfill",
          "fixTier": "gapfill"
        }
      ]
    },
    {
      "id": "SETUP-05",
      "area": "Setup Checklist & Permissions",
      "feature": "Accessibility permission step (optional, opens System Settings)",
      "priority": "P2",
      "userStory": "As a user who wants global keyboard shortcuts, I want a one-tap way to grant Accessibility, so that I can enable shortcuts while understanding per-app routing works without it.",
      "expectedBehavior": [
        "Step 4 (\"Enable keyboard shortcut permission\") is complete iff onboarding.accessibilityPermissionGranted, set from AXIsProcessTrusted() during syncOnboarding (OnboardingView.swift:144, AppStore.swift:1359).",
        "The detail copy explicitly states the permission is optional and that per-app volume routing works without it (OnboardingView.swift:24-27).",
        "When not granted, the action button \"Open System Settings\" deep-links to x-apple.systempreferences:com.apple.preference.security?Privacy_Accessibility (OnboardingView.swift:167-170).",
        "This step is excluded from completionProgress and from isFullyComplete, so the checklist can reach 100%/\"all set\" without it (OnboardingView.swift:30-43).",
        "The Advanced diagnostics tab shows an \"Accessibility permission\" check reflecting AXIsProcessTrusted() with guidance copy (WorkspaceAudioControlBackend.swift:257-263, 318-320)."
      ],
      "acceptanceCriteria": [
        "When Accessibility is not trusted, step 4 shows the warning state and an \"Open System Settings\" button that opens the Privacy_Accessibility pane.",
        "Granting Accessibility (then refreshing) flips step 4 to complete without changing the progress bar or \"all set\" state.",
        "The Accessibility diagnostic check matches the step's complete/incomplete state."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Onboarding/OnboardingView.swift:24-27,144,154,167-170",
        "Sources/Waves/Stores/AppStore.swift:1359",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:257-263",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:318-320"
      ],
      "implStatus": "implemented",
      "implNotes": "Correctly treated as optional and clearly communicated. accessibilityPermissionGranted only refreshes on syncOnboarding (refresh/recover/device-change), so after the user grants it in System Settings the checklist won't update until they hit \"Refresh Status\" — acceptable but worth a note. The deep-link URL is the standard pane and matches README troubleshooting guidance.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1M 1L]",
      "errorsFound": "• [MEDIUM/inconsistency] Accessibility diagnostic and checklist step can disagree after granting permission (Sources/Waves/Stores/AppStore.swift:1204-1208) — Confirmed. Step 4 completion uses onboarding.accessibilityPermissionGranted, refreshed only in syncOnboarding (AppStore.swift:1359), which is called by refresh/recoverRoutes/handleDeviceChange/start. The Advanced tab's Accessibility diagnostic reads hasAccessibilityPermission = AXIsProcessTrusted() live on every diagnosticsReport() (WorkspaceAudioControlBackend.swift:259,318-320). DiagnosticsSettingsView.onAppear calls store.refreshDiagnostics() (SettingsView.swift:310-312), which only rebuilds the report (AppStore.swift:1204-1208) and does NOT call syncOnboarding. So after granting Accessibility and opening Advanced, the diagnostic flips to 'passed' while step 4 still reads incomplete until 'Refresh Status' is pressed. | Fix: Have refreshDiagnostics() also re-read AXIsProcessTrusted() into onboarding.accessibilityPermissionGranted (or run a lightweight permission re-sync), so both surfaces share one source.\n• [LOW/ux] Granting Accessibility does not auto-update the checklist; requires manual Refresh (Sources/Waves/Stores/AppStore.swift:1359) — Confirmed. After 'Open System Settings' (OnboardingView.swift:167-170) and granting Accessibility, returning to Waves does not flip step 4 because accessibilityPermissionGranted is only re-read on the next syncOnboarding (refresh/recover/device-change/start). There is no window-activation or polling refresh, so step 4 stays stale with no hint that a refresh is needed. | Fix: Re-probe AXIsProcessTrusted() when the Settings window/app reactivates (e.g. NSApplication.didBecomeActive or scenePhase .active) or when the Setup tab reappears.",
      "fixStatus": "1 fixed · 1 n/a",
      "fixNotes": "tier2: FU-DIAG-SYNC-DESYNC",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "inconsistency",
          "title": "Accessibility diagnostic and checklist step can disagree after granting permission",
          "detail": "Confirmed. Step 4 completion uses onboarding.accessibilityPermissionGranted, refreshed only in syncOnboarding (AppStore.swift:1359), which is called by refresh/recoverRoutes/handleDeviceChange/start. The Advanced tab's Accessibility diagnostic reads hasAccessibilityPermission = AXIsProcessTrusted() live on every diagnosticsReport() (WorkspaceAudioControlBackend.swift:259,318-320). DiagnosticsSettingsView.onAppear calls store.refreshDiagnostics() (SettingsView.swift:310-312), which only rebuilds the report (AppStore.swift:1204-1208) and does NOT call syncOnboarding. So after granting Accessibility and opening Advanced, the diagnostic flips to 'passed' while step 4 still reads incomplete until 'Refresh Status' is pressed.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1204-1208",
          "expectedVsActual": "Story AC (SETUP-05): 'The Accessibility diagnostic check matches the step's complete/incomplete state.' Actual: the diagnostic uses a live AX probe while the checklist step uses a value updated only on a fuller refresh, so they can diverge.",
          "suggestedFix": "Have refreshDiagnostics() also re-read AXIsProcessTrusted() into onboarding.accessibilityPermissionGranted (or run a lightweight permission re-sync), so both surfaces share one source.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier2:applied:FU-DIAG-SYNC-DESYNC",
          "uid": "SETUP-05#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-DIAG-SYNC-DESYNC",
          "fixTier": "tier2"
        },
        {
          "severity": "low",
          "type": "ux",
          "title": "Granting Accessibility does not auto-update the checklist; requires manual Refresh",
          "detail": "Confirmed. After 'Open System Settings' (OnboardingView.swift:167-170) and granting Accessibility, returning to Waves does not flip step 4 because accessibilityPermissionGranted is only re-read on the next syncOnboarding (refresh/recover/device-change/start). There is no window-activation or polling refresh, so step 4 stays stale with no hint that a refresh is needed.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1359",
          "expectedVsActual": "Story: 'a one-tap way to grant Accessibility.' Actual: granting is one tap but the checklist will not reflect it without an additional manual Refresh.",
          "suggestedFix": "Re-probe AXIsProcessTrusted() when the Settings window/app reactivates (e.g. NSApplication.didBecomeActive or scenePhase .active) or when the Setup tab reappears.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "SETUP-05#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "SETUP-06",
      "area": "Setup Checklist & Permissions",
      "feature": "Advanced diagnostics: permission/route report and copy-to-clipboard",
      "priority": "P1",
      "userStory": "As a user troubleshooting why audio control isn't working, I want a detailed diagnostics panel I can copy into a bug report, so that I (or support) can see permission, device, and route-health status at a glance.",
      "expectedBehavior": [
        "The Settings \"Advanced\" tab renders a running-apps summary plus a list of DiagnosticsReport checks, refreshing diagnostics on appear (SettingsView.swift:28-31, 269-313).",
        "Diagnostics include five checks: Audio component, Audio capture permission, Accessibility permission, Route recovery, and an informational Support matrix coverage summary (WorkspaceAudioControlBackend.swift:241-277).",
        "Audio-capture status comes from a real TCC probe: refreshCaptureAuthorization() creates and immediately destroys a private global process tap; success => authorized, failure => notGranted, run on every buildSnapshot (WorkspaceAudioControlBackend.swift:325-358, 988-991).",
        "Capture-not-granted detail directs the user to System Settings › Privacy & Security › Microphone and to refresh (WorkspaceAudioControlBackend.swift:288-298).",
        "\"Copy Diagnostics\" writes a plain-text report (macOS version, output device, component/permission/route flags, last error, per-app routing/volume/mute/boost, and check results) to the pasteboard and toasts confirmation (SettingsView.swift:283-289, AppStore.swift:725-779).",
        "When no diagnostics are loaded, a fallback view shows the last backend error and \"Refresh Diagnostics\" / \"Recover Routes\" buttons (SettingsView.swift:304-347)."
      ],
      "acceptanceCriteria": [
        "Opening Advanced populates the five diagnostic checks; the capture check reflects an actual tap-create probe rather than a static flag.",
        "With capture denied, the Audio capture permission check reads failed with the Microphone-settings guidance.",
        "\"Copy Diagnostics\" places a multi-line report on the clipboard containing macOS version, output device, all three status booleans, and per-app rows, and shows a \"Diagnostics copied\" toast.",
        "With diagnostics nil, the unavailable view shows the last error (or default copy) and both recovery buttons."
      ],
      "codeRefs": [
        "Sources/Waves/Settings/SettingsView.swift:269-347",
        "Sources/Waves/Stores/AppStore.swift:725-779",
        "Sources/Waves/Stores/AppStore.swift:1204-1208",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:241-299",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:325-358"
      ],
      "implStatus": "implemented",
      "implNotes": "Strong, honest implementation — the capture probe genuinely tests TCC rather than trusting OS version, and the report is comprehensive. Concern: this Advanced tab is the ONLY place the decisive audio-capture permission is shown; it is absent from the Setup checklist (see SETUP-01), so a user who never opens Advanced has no guided path to discover/grant Microphone permission. The probe's create/destroy tap on every snapshot is cheap but does run frequently.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1M]",
      "errorsFound": "• [MEDIUM/correctness] Opening Advanced does not re-run the capture probe; the capture check can show a stale TCC result (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:241-256) — Confirmed. The real tap-create probe refreshCaptureAuthorization() (WorkspaceAudioControlBackend.swift:332-358) runs only inside buildSnapshot (line 991), i.e. on start/refresh/recoverRoutes/device-change. DiagnosticsSettingsView.onAppear calls store.refreshDiagnostics() (SettingsView.swift:310-312) -> backend.diagnosticsReport(), which reads the cached captureAuthorization property via captureAuthorizationStatus/captureAuthorizationDetail (WorkspaceAudioControlBackend.swift:254-255,280-299) and does NOT re-probe. So if the user changes Microphone permission and merely opens/reopens Advanced, the 'Audio capture permission' check reflects the last probe, not current TCC state, until a full refresh. | Fix: Call refreshCaptureAuthorization() (or backend.refresh()) before building the report on Advanced-tab appear, or have diagnosticsReport() re-probe capture authorization itself.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier2: FU-DIAGNOSTICS-CAPTURE-NOT-REPROBED",
      "retestStatus": "PASS (remediated & verified)",
      "issues": [
        {
          "severity": "medium",
          "type": "correctness",
          "title": "Opening Advanced does not re-run the capture probe; the capture check can show a stale TCC result",
          "detail": "Confirmed. The real tap-create probe refreshCaptureAuthorization() (WorkspaceAudioControlBackend.swift:332-358) runs only inside buildSnapshot (line 991), i.e. on start/refresh/recoverRoutes/device-change. DiagnosticsSettingsView.onAppear calls store.refreshDiagnostics() (SettingsView.swift:310-312) -> backend.diagnosticsReport(), which reads the cached captureAuthorization property via captureAuthorizationStatus/captureAuthorizationDetail (WorkspaceAudioControlBackend.swift:254-255,280-299) and does NOT re-probe. So if the user changes Microphone permission and merely opens/reopens Advanced, the 'Audio capture permission' check reflects the last probe, not current TCC state, until a full refresh.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:241-256",
          "expectedVsActual": "Story AC (SETUP-06): 'Opening Advanced populates the five diagnostic checks; the capture check reflects an actual tap-create probe rather than a static flag.' Actual: opening Advanced reflects a previously-cached probe result, not a fresh probe, so a just-changed permission is not reflected until a full refresh.",
          "suggestedFix": "Call refreshCaptureAuthorization() (or backend.refresh()) before building the report on Advanced-tab appear, or have diagnosticsReport() re-probe capture authorization itself.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "tier2:applied:FU-DIAGNOSTICS-CAPTURE-NOT-REPROBED",
          "uid": "SETUP-06#1",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-DIAGNOSTICS-CAPTURE-NOT-REPROBED",
          "fixTier": "tier2"
        }
      ]
    },
    {
      "id": "LOGIN-01",
      "area": "Launch at Login (Login Item)",
      "feature": "Toggle Launch at Login from Settings",
      "priority": "P2",
      "userStory": "As a Waves user, I want a \"Launch at login\" toggle in Settings so that Waves starts automatically every time I log into my Mac without my having to open it manually.",
      "expectedBehavior": [
        "GeneralSettingsView renders a SwiftUI Toggle labeled \"Launch at login\" bound to store.launchAtLoginEnabled (SettingsView.swift:51-56).",
        "Turning the toggle on calls AppStore.launchAtLoginEnabled setter, which calls loginItemService.setEnabled(true) -> SMAppService.mainApp.register() (AppStore.swift:165, LoginItemService.swift:29-31).",
        "Turning the toggle off calls setEnabled(false) -> SMAppService.mainApp.unregister() (LoginItemService.swift:32-34).",
        "After registering/unregistering, the setter re-reads the live SMAppService status and writes status.isEnabled back into preferences and onboarding, then persists (AppStore.swift:166-169).",
        "The toggle's displayed value comes from preferences.launchAtLoginEnabled, which is reconciled to the actual OS login-item state on every relevant refresh (AppStore.swift:161-162, 1362-1364)."
      ],
      "acceptanceCriteria": [
        "Enabling the toggle registers Waves as an SMAppService login item and, after a real login, Waves launches automatically.",
        "Disabling the toggle unregisters the login item and Waves no longer auto-launches.",
        "The toggle visually reflects the actual OS state, not just the last requested value, because the setter reconciles from loginItemService.status after each change.",
        "The preference change is persisted (preferencesStore.save) so it survives an app restart."
      ],
      "codeRefs": [
        "Sources/Waves/Settings/SettingsView.swift:51",
        "Sources/Waves/Settings/SettingsView.swift:54",
        "Sources/Waves/Stores/AppStore.swift:161",
        "Sources/Waves/Stores/AppStore.swift:165",
        "Sources/Waves/Stores/AppStore.swift:166",
        "Sources/Waves/Services/LoginItem/LoginItemService.swift:29"
      ],
      "implStatus": "implemented",
      "implNotes": "Cleanly wired end-to-end using the modern ServiceManagement SMAppService.mainApp API (correct for sandboxed/notarized apps, no separate helper bundle needed). The setter is robust: it reconciles the toggle to the real OS status after every call rather than trusting the requested value, so the UI cannot drift out of sync. Persistence and onboarding mirror are both updated. No automated tests cover this path.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "LOGIN-02",
      "area": "Launch at Login (Login Item)",
      "feature": "Toggle Launch at Login from the menu bar mixer",
      "priority": "P2",
      "userStory": "As a menu-bar-first user, I want to flip \"Launch at login\" directly from the menu bar mixer popover so that I can change startup behavior without opening the full Settings window.",
      "expectedBehavior": [
        "MenuBarMixerView shows a switch-style Toggle labeled \"Launch at login\" (labelsHidden, accessibilityLabel set) bound to the same store.launchAtLoginEnabled property (MenuBarMixerView.swift:90-100).",
        "Flipping it runs the identical AppStore.launchAtLoginEnabled setter path as the Settings toggle, so register/unregister, reconciliation, persistence, and toast behavior are shared (AppStore.swift:161-186).",
        "Because both surfaces read the same computed property, changing it in one place is reflected in the other."
      ],
      "acceptanceCriteria": [
        "The menu bar switch enables/disables the login item identically to the Settings toggle.",
        "Toggling in the menu bar updates the Settings toggle state (shared source of truth) and vice versa.",
        "The switch exposes an accessibility label \"Launch at login\" for VoiceOver."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:90",
        "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:92",
        "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:100",
        "Sources/Waves/Stores/AppStore.swift:161"
      ],
      "implStatus": "implemented",
      "implNotes": "Duplicate entry point onto the same setter, so it inherits all the correctness of LOGIN-01. Good that it reuses the binding rather than reimplementing. Minor UX note: with labelsHidden the bare switch relies entirely on adjacent context/accessibility label for meaning; sighted users see only an unlabeled switch in the popover footer.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/ux] Menu-bar Launch-at-login switch has no visible label for sighted users (Sources/Waves/Features/Mixer/MenuBarMixerView.swift:99) — Confirmed by re-reading MenuBarMixerView.swift:90-101. The Toggle wraps Text(\"Launch at login\") but applies .labelsHidden(), and the footer HStack contains only the \"Open Waves\" and \"Settings\" buttons, a Spacer, and then the bare switch. There is no adjacent visible caption. Sighted users see an unlabeled switch in the popover's bottom-right corner; only VoiceOver users get meaning via accessibilityLabel(\"Launch at login\"). Acceptance criteria (shared binding + VO label) are technically met, but the on-screen affordance is ambiguous. | Fix: Remove .labelsHidden() so the inline \"Launch at login\" label renders, or add a small visible caption (e.g. a leading Text or a .help tooltip) next to the switch, keeping the accessibilityLabel for VoiceOver.",
      "fixStatus": "1 fixed",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "ux",
          "title": "Menu-bar Launch-at-login switch has no visible label for sighted users",
          "detail": "Confirmed by re-reading MenuBarMixerView.swift:90-101. The Toggle wraps Text(\"Launch at login\") but applies .labelsHidden(), and the footer HStack contains only the \"Open Waves\" and \"Settings\" buttons, a Spacer, and then the bare switch. There is no adjacent visible caption. Sighted users see an unlabeled switch in the popover's bottom-right corner; only VoiceOver users get meaning via accessibilityLabel(\"Launch at login\"). Acceptance criteria (shared binding + VO label) are technically met, but the on-screen affordance is ambiguous.",
          "codeRef": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:99",
          "expectedVsActual": "Expected: the menu-bar control is understandable as a Launch-at-login switch for all users. Actual: .labelsHidden() removes the only on-screen label, leaving an unlabeled switch with no adjacent caption.",
          "suggestedFix": "Remove .labelsHidden() so the inline \"Launch at login\" label renders, or add a small visible caption (e.g. a leading Text or a .help tooltip) next to the switch, keeping the accessibilityLabel for VoiceOver.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift",
          "disposition": "iter3:implemented",
          "uid": "LOGIN-02#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift",
          "fixStatus": "applied",
          "fixedBy": "iter3-ui-parity",
          "fixTier": "iter3"
        }
      ]
    },
    {
      "id": "LOGIN-03",
      "area": "Launch at Login (Login Item)",
      "feature": "Enable Launch at Login during onboarding",
      "priority": "P3",
      "userStory": "As a first-time user, I want an \"Enable Launch at Login\" button in onboarding so that I can opt into auto-start as part of initial setup.",
      "expectedBehavior": [
        "OnboardingView shows an \"Enable Launch at Login\" bordered button only when store.onboarding.launchAtLoginEnabled is false (OnboardingView.swift:128-133).",
        "Tapping it sets store.launchAtLoginEnabled = true, running the standard register + reconcile + persist path (OnboardingView.swift:130, AppStore.swift:163-177).",
        "Once enabled (onboarding.launchAtLoginEnabled becomes true via reconciliation), the button disappears.",
        "onboarding.launchAtLoginEnabled is seeded from the live SMAppService status at store init and re-synced on every syncOnboarding call (AppStore.swift:95-98, 1362-1363)."
      ],
      "acceptanceCriteria": [
        "The button is visible only while the login item is not enabled.",
        "Pressing it registers the login item and then hides the button after status reconciles to enabled.",
        "The onboarding state reflects the real OS login-item status, not a stale value, because it is sourced from loginItemService.status."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Onboarding/OnboardingView.swift:128",
        "Sources/Waves/Features/Onboarding/OnboardingView.swift:130",
        "Sources/Waves/Stores/AppStore.swift:1362",
        "Sources/Waves/Stores/AppStore.swift:96"
      ],
      "implStatus": "implemented",
      "implNotes": "Onboarding offers a one-way enable (no disable from here), which is reasonable for a setup flow. Note the button visibility depends on onboarding.launchAtLoginEnabled being refreshed; it is updated in syncOnboarding, but syncOnboarding is only invoked on refresh/snapshot events, so immediately after pressing the button the disappearance relies on the @Observable store mutation in the setter (which does update onboarding.launchAtLoginEnabled at AppStore.swift:168) rather than on a snapshot refresh — that path is covered, so it works.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "LOGIN-04",
      "area": "Launch at Login (Login Item)",
      "feature": "Reconcile and surface approval-required / failure states",
      "priority": "P2",
      "userStory": "As a user whose system requires approval (or where registration fails), I want clear feedback when enabling Launch at Login does not fully take effect so that I understand why Waves may not auto-start.",
      "expectedBehavior": [
        "LoginItemService.status maps SMAppService.status to a LoginItemStatus: .enabled->\"Enabled\", .requiresApproval->isEnabled=false/\"Requires approval\", .notRegistered->\"Disabled\", .notFound->\"Not found\", @unknown->\"Unknown\" (LoginItemService.swift:13-27).",
        "When the requested value differs from the reconciled status.isEnabled (e.g. macOS returns .requiresApproval), the setter shows a warning toast titled \"Login item needs approval\" with status.statusDescription as detail (AppStore.swift:170-177).",
        "If setEnabled throws, the catch block re-reads status, resyncs preferences + onboarding, persists, and shows an error toast \"Login item update failed\" with the localized error (AppStore.swift:178-184).",
        "In all branches the persisted/displayed preference is forced back to the true OS status so the toggle reflects reality even after an approval or failure (AppStore.swift:167-168, 180-181)."
      ],
      "acceptanceCriteria": [
        "Toggling on while the OS reports .requiresApproval leaves the toggle off and shows the \"Login item needs approval\" warning toast.",
        "A thrown error from register/unregister shows the \"Login item update failed\" error toast and leaves the toggle matching the real OS state.",
        "After any approval-required or error outcome, the toggle does not falsely show \"on\".",
        "The status description string corresponds to the SMAppService.Status case."
      ],
      "codeRefs": [
        "Sources/Waves/Services/LoginItem/LoginItemService.swift:13",
        "Sources/Waves/Stores/AppStore.swift:170",
        "Sources/Waves/Stores/AppStore.swift:178",
        "Sources/Waves/Stores/AppStore.swift:183"
      ],
      "implStatus": "partial",
      "implNotes": "State reconciliation and user feedback are genuinely handled (warning vs error toasts, toggle never lies), which is better than most implementations. The real gap is the .requiresApproval recovery UX: macOS routes login-item approval to System Settings > General > Login Items, but the code never deep-links there (the only x-apple.systempreferences link in the app points to Privacy_Accessibility, OnboardingView.swift:168). The user is told \"Requires approval\" via a 2.4s toast but is given no button/affordance to open the Login Items pane to grant it, so they may be stuck. Also the toast auto-dismisses quickly and the status string is not surfaced persistently anywhere in Settings. No automated tests exist for any branch (status mapping, approval, or error).",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [2M 1L]",
      "errorsFound": "• [MEDIUM/inconsistency] Warning toast is always titled \"Login item needs approval\" even for non-approval mismatches (Sources/Waves/Stores/AppStore.swift:172) — Confirmed at AppStore.swift:170-177. The setter shows the warning toast whenever status.isEnabled != newValue. When enabling (newValue == true) this fires for ANY status whose isEnabled is false per LoginItemService.swift:18-25: .requiresApproval (\"Requires approval\"), .notRegistered (\"Disabled\"), .notFound (\"Not found\"), and @unknown (\"Unknown\"). In every case the title is hardcoded to \"Login item needs approval\" while only the detail reflects the real status. So if register() returns without throwing but the item lands on .notRegistered/.notFound/.unknown, the user is told it \"needs approval\" with a contradicting detail (e.g. \"Not found\"), pointing them at a System-Settings approval recovery path that does not apply. | Fix: Branch the toast on the real status: title \"Login item needs approval\" only when the underlying SMAppService.status is .requiresApproval; otherwise use a generic title such as \"Couldn't enable Launch at login\" with status.statusDescription as detail. Expose the SMAppService.Status case (or a richer enum) from LoginItemService so the store can distinguish them.\n• [MEDIUM/ux] No affordance to open the Login Items pane when approval is required (Sources/Waves/Stores/AppStore.swift:171) — Confirmed. An SMAppService.mainApp registration returning .requiresApproval must be approved in System Settings > General > Login Items & Extensions. The code surfaces only a 2.4s warning toast (AppStore.swift:170-177) with no button or deep link to that pane. Grep confirms the only x-apple.systempreferences URL in the app targets Privacy_Accessibility (OnboardingView.swift:168), not Login Items, and there is no SMAppService.openSystemSettingsLoginItems() call anywhere. The toast auto-dismisses and the approval status is never surfaced persistently in GeneralSettingsView, so a user whose enable requires approval is told \"Requires approval\" but has no way to act on it, leaving auto-start silently non-functional. The feature-status implNotes for LOGIN-04 itself flag this gap. | Fix: When the status indicates .requiresApproval, add a persistent status row/banner in GeneralSettingsView showing status.statusDescription plus a button that opens the Login Items pane via SMAppService.openSystemSettingsLoginItems() (or the x-apple.systempreferences Login Items URL). Consider a non-auto-dismissing message for this case.\n• [LOW/ux] Pending-approval login item is silently shown as OFF on launch/refresh with no indication (Sources/Waves/Services/LoginItem/LoginItemService.swift:19) — Missed by the first tester. LoginItemService.status maps .requiresApproval to isEnabled=false (LoginItemService.swift:18-19). The init reconcile (AppStore.swift:95) and every syncOnboarding (AppStore.swift:1362-1364) overwrite preferences.launchAtLoginEnabled with status.isEnabled. So a user who enabled Launch-at-login but whose item is registered-pending-approval will, on the next app launch or refresh, see both the Settings and menu-bar toggles show OFF with no toast and no hint that the item is actually registered and only awaiting approval. This is a silent state-surfacing gap distinct from the missing deep link: even before reaching System Settings the user gets no signal that approval is the blocker. Low severity because re-toggling re-triggers the approval toast, but the resting state is misleading. | Fix: Surface the SMAppService.Status (not just a Bool) in the UI: show a persistent \"Requires approval\" caption/row in GeneralSettingsView when status is .requiresApproval, so the resting toggle state is not silently misleading after a reconcile.",
      "fixStatus": "1 fixed · 2 deferred",
      "fixNotes": "tier2: FU-LOGIN-ITEM-TITLE",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "inconsistency",
          "title": "Warning toast is always titled \"Login item needs approval\" even for non-approval mismatches",
          "detail": "Confirmed at AppStore.swift:170-177. The setter shows the warning toast whenever status.isEnabled != newValue. When enabling (newValue == true) this fires for ANY status whose isEnabled is false per LoginItemService.swift:18-25: .requiresApproval (\"Requires approval\"), .notRegistered (\"Disabled\"), .notFound (\"Not found\"), and @unknown (\"Unknown\"). In every case the title is hardcoded to \"Login item needs approval\" while only the detail reflects the real status. So if register() returns without throwing but the item lands on .notRegistered/.notFound/.unknown, the user is told it \"needs approval\" with a contradicting detail (e.g. \"Not found\"), pointing them at a System-Settings approval recovery path that does not apply.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:172",
          "expectedVsActual": "Expected: the title corresponds to the actual SMAppService status (approval vs. generic failure to enable). Actual: title is fixed to \"Login item needs approval\" for every enable-mismatch, contradicting the detail string for .notRegistered/.notFound/.unknown.",
          "suggestedFix": "Branch the toast on the real status: title \"Login item needs approval\" only when the underlying SMAppService.status is .requiresApproval; otherwise use a generic title such as \"Couldn't enable Launch at login\" with status.statusDescription as detail. Expose the SMAppService.Status case (or a richer enum) from LoginItemService so the store can distinguish them.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier2:partial:FU-LOGIN-ITEM-TITLE",
          "uid": "LOGIN-04#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "partial",
          "fixedBy": "FU-LOGIN-ITEM-TITLE",
          "fixTier": "tier2"
        },
        {
          "severity": "medium",
          "type": "ux",
          "title": "No affordance to open the Login Items pane when approval is required",
          "detail": "Confirmed. An SMAppService.mainApp registration returning .requiresApproval must be approved in System Settings > General > Login Items & Extensions. The code surfaces only a 2.4s warning toast (AppStore.swift:170-177) with no button or deep link to that pane. Grep confirms the only x-apple.systempreferences URL in the app targets Privacy_Accessibility (OnboardingView.swift:168), not Login Items, and there is no SMAppService.openSystemSettingsLoginItems() call anywhere. The toast auto-dismisses and the approval status is never surfaced persistently in GeneralSettingsView, so a user whose enable requires approval is told \"Requires approval\" but has no way to act on it, leaving auto-start silently non-functional. The feature-status implNotes for LOGIN-04 itself flag this gap.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:171",
          "expectedVsActual": "Expected (per the story: understand why Waves may not auto-start and be able to fix it): a clear, actionable path to grant approval. Actual: a transient toast with no link/affordance to open Login Items, and no persistent status shown in Settings.",
          "suggestedFix": "When the status indicates .requiresApproval, add a persistent status row/banner in GeneralSettingsView showing status.statusDescription plus a button that opens the Login Items pane via SMAppService.openSystemSettingsLoginItems() (or the x-apple.systempreferences Login Items URL). Consider a non-auto-dismissing message for this case.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-feature:New banner + deep link belongs in the Settings view, outside AppStore.swift, and needs a design decision.",
          "uid": "LOGIN-04#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "low",
          "type": "ux",
          "title": "Pending-approval login item is silently shown as OFF on launch/refresh with no indication",
          "detail": "Missed by the first tester. LoginItemService.status maps .requiresApproval to isEnabled=false (LoginItemService.swift:18-19). The init reconcile (AppStore.swift:95) and every syncOnboarding (AppStore.swift:1362-1364) overwrite preferences.launchAtLoginEnabled with status.isEnabled. So a user who enabled Launch-at-login but whose item is registered-pending-approval will, on the next app launch or refresh, see both the Settings and menu-bar toggles show OFF with no toast and no hint that the item is actually registered and only awaiting approval. This is a silent state-surfacing gap distinct from the missing deep link: even before reaching System Settings the user gets no signal that approval is the blocker. Low severity because re-toggling re-triggers the approval toast, but the resting state is misleading.",
          "codeRef": "Sources/Waves/Services/LoginItem/LoginItemService.swift:19",
          "expectedVsActual": "Expected: a pending-approval login item is distinguishable from a genuinely-disabled one (e.g. a status caption \"Requires approval\"). Actual: .requiresApproval collapses to isEnabled=false, so the toggle silently reads OFF identically to the disabled case with no surfaced reason.",
          "suggestedFix": "Surface the SMAppService.Status (not just a Bool) in the UI: show a persistent \"Requires approval\" caption/row in GeneralSettingsView when status is .requiresApproval, so the resting toggle state is not silently misleading after a reconcile.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Services/LoginItem/LoginItemService.swift",
          "disposition": "deferred:needs-user-decision:State-surfacing UX spanning service + reconcile + Settings copy.",
          "uid": "LOGIN-04#3",
          "canonicalFile": "Sources/Waves/Services/LoginItem/LoginItemService.swift"
        }
      ]
    },
    {
      "id": "LOGIN-05",
      "area": "Launch at Login (Login Item)",
      "feature": "Persist preference but treat OS as source of truth across launches",
      "priority": "P2",
      "userStory": "As a returning user, I want Waves to show the correct Launch-at-login state on every launch so that the toggle always matches what the system will actually do, even if the login item was changed outside the app.",
      "expectedBehavior": [
        "launchAtLoginEnabled defaults to false in UserPreferences and is decoded defensively so an older prefs file missing the key does not wipe settings (UserPreferences.swift:4, 40-49).",
        "On store initialization the persisted preference is overwritten with loginItemService.status.isEnabled before first use, and re-persisted (AppStore.swift:95-99).",
        "syncOnboarding re-reads the live login-item status on each refresh and writes it back into both onboarding and preferences (AppStore.swift:1362-1364).",
        "persistPreferences saves the preferences via preferencesStore (AppStore.swift:1264-1267)."
      ],
      "acceptanceCriteria": [
        "On launch the toggle reflects the actual SMAppService registration state, even if the login item was enabled/disabled via System Settings while the app was closed.",
        "A preferences file from an older build that lacks the launchAtLoginEnabled key loads without error and other settings are preserved.",
        "The reconciled state is written back to disk so it is consistent on subsequent reads."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/UserPreferences.swift:4",
        "Sources/Waves/Stores/UserPreferences.swift:49",
        "Sources/Waves/Stores/AppStore.swift:95",
        "Sources/Waves/Stores/AppStore.swift:1362"
      ],
      "implStatus": "implemented",
      "implNotes": "Strong design choice: the OS login-item status is the source of truth and the stored preference is just a cache that gets reconciled on init and on each syncOnboarding. This prevents the classic drift where the app shows 'on' but the OS disabled it. Defensive Codable decoding avoids the saved preference being a brittle field. One caveat: reconciliation happens on init and on snapshot/refresh events, not via a scenePhase/didBecomeActive observer, so if the user changes the login item in System Settings while the Waves window is already open, the toggle may show a stale value until the next refresh.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [2L]",
      "errorsFound": "• [LOW/correctness] syncOnboarding reconciles preferences.launchAtLoginEnabled in memory but never persists it (Sources/Waves/Stores/AppStore.swift:1364) — Confirmed. syncOnboarding writes both onboarding.launchAtLoginEnabled and preferences.launchAtLoginEnabled from the live login-item status (AppStore.swift:1362-1364) but does not call persistPreferences(). Verified its callers do not persist preferences either: start() (line 218) and refresh() (line 247) call only persistSessionSnapshot() (which saves session, not preferences — AppStore.swift:1438-1440); the device-change handler (line 809) calls only persistSessionSnapshot() (line 806). Only the init path persists the reconciled value (AppStore.swift:99). persistPreferences() itself just does preferencesStore.save (AppStore.swift:1264-1267). So a mid-session reconcile reaches disk only if SettingsView happens to be open at quit (onDisappear -> persistPreferences, SettingsView.swift:40-42). Impact is limited because init always re-overwrites from OS status, so a stale on-disk value is harmless on next launch — hence low — but the code does not match the story's stated persist-on-reconcile behavior (acceptance criterion 3). | Fix: Call persistPreferences() at the end of syncOnboarding after writing preferences.launchAtLoginEnabled (guarded by a change check to avoid redundant writes on every refresh), or have the callers persist preferences when the value changed.\n• [LOW/edge-case] Toggle shows stale state if the login item is changed externally while a Waves window is open (Sources/Waves/Stores/AppStore.swift:1362) — Confirmed. Reconciliation of preferences.launchAtLoginEnabled from loginItemService.status happens only at store init (AppStore.swift:95) and inside syncOnboarding, which is invoked solely on start/refresh/device-change/snapshot events (callers at AppStore.swift:100,206,218,247,809,1188). Grep confirms no scenePhase observer and no NSApplication.didBecomeActiveNotification observer anywhere in Sources/Waves. If the user enables/disables the Waves login item in System Settings while a Waves window or popover is already open, both the Settings and menu-bar toggles keep showing the old value until the next manual refresh or device change. This satisfies the literal 'on launch' criterion but undermines the story's framing of the OS as the live source of truth. | Fix: Re-read loginItemService.status and update preferences/onboarding when the app/window becomes active — observe NSApplication.didBecomeActiveNotification or use SwiftUI .onChange(of: scenePhase) to run a lightweight reconcile of launchAtLoginEnabled.",
      "fixStatus": "1 fixed · 1 deferred",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "correctness",
          "title": "syncOnboarding reconciles preferences.launchAtLoginEnabled in memory but never persists it",
          "detail": "Confirmed. syncOnboarding writes both onboarding.launchAtLoginEnabled and preferences.launchAtLoginEnabled from the live login-item status (AppStore.swift:1362-1364) but does not call persistPreferences(). Verified its callers do not persist preferences either: start() (line 218) and refresh() (line 247) call only persistSessionSnapshot() (which saves session, not preferences — AppStore.swift:1438-1440); the device-change handler (line 809) calls only persistSessionSnapshot() (line 806). Only the init path persists the reconciled value (AppStore.swift:99). persistPreferences() itself just does preferencesStore.save (AppStore.swift:1264-1267). So a mid-session reconcile reaches disk only if SettingsView happens to be open at quit (onDisappear -> persistPreferences, SettingsView.swift:40-42). Impact is limited because init always re-overwrites from OS status, so a stale on-disk value is harmless on next launch — hence low — but the code does not match the story's stated persist-on-reconcile behavior (acceptance criterion 3).",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1364",
          "expectedVsActual": "Expected: every reconcile (init and each syncOnboarding) writes the OS-derived launch-at-login state to disk. Actual: only the init reconcile persists; refresh/start/device-change reconciles update memory only.",
          "suggestedFix": "Call persistPreferences() at the end of syncOnboarding after writing preferences.launchAtLoginEnabled (guarded by a change check to avoid redundant writes on every refresh), or have the callers persist preferences when the value changed.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "iter2:fixed",
          "uid": "LOGIN-05#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "iter2-deferred-reduction",
          "fixTier": "iter2"
        },
        {
          "severity": "low",
          "type": "edge-case",
          "title": "Toggle shows stale state if the login item is changed externally while a Waves window is open",
          "detail": "Confirmed. Reconciliation of preferences.launchAtLoginEnabled from loginItemService.status happens only at store init (AppStore.swift:95) and inside syncOnboarding, which is invoked solely on start/refresh/device-change/snapshot events (callers at AppStore.swift:100,206,218,247,809,1188). Grep confirms no scenePhase observer and no NSApplication.didBecomeActiveNotification observer anywhere in Sources/Waves. If the user enables/disables the Waves login item in System Settings while a Waves window or popover is already open, both the Settings and menu-bar toggles keep showing the old value until the next manual refresh or device change. This satisfies the literal 'on launch' criterion but undermines the story's framing of the OS as the live source of truth.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1362",
          "expectedVsActual": "Expected: the toggle reflects the real OS login-item state whenever the window is visible. Actual: external mid-session changes are not detected until the next refresh-style event, so the toggle can show a stale value.",
          "suggestedFix": "Re-read loginItemService.status and update preferences/onboarding when the app/window becomes active — observe NSApplication.didBecomeActiveNotification or use SwiftUI .onChange(of: scenePhase) to run a lightweight reconcile of launchAtLoginEnabled.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-feature:Toggle shows stale state on external mid-session login-item changes. Requires wiring a new app/window-active observer (NSApplication.didBecomeActive or scenePha",
          "uid": "LOGIN-05#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "PERSIST-01",
      "area": "Persistence (Session / Preferences / Presets / Device volume presets)",
      "feature": "Session state caching and cross-launch restore",
      "priority": "P0",
      "userStory": "As a Waves user, I want my per-app volumes, mutes, boosts, pins and pinned output routes to persist and be re-applied when I relaunch the app, so that my customized mix survives quits, crashes and restarts without me redoing everything.",
      "expectedBehavior": [
        "On every meaningful change (volume commit, mute, boost, pin, device change, preset apply, route recovery) the current AudioSessionSnapshot is written to ~/Library/Application Support/Waves/session.json via SessionStore.save (AppStore persistSessionSnapshot at AppStore.swift:1438-1440).",
        "Saves run asynchronously on a dedicated userInitiated DispatchQueue and write atomically (SessionStore.swift:50-96).",
        "To save space, iconTIFFData is explicitly excluded from the persisted snapshot via a manual field-by-field AudioApp remap (SessionStore.swift:53-87).",
        "On init, AppStore loads the cached session (sessionStore.load() ?? emptySession) before the backend starts so the UI shows the last-known mix immediately (AppStore.swift:83, 203-207).",
        "On startup the cached snapshot is merged with the freshly built live snapshot by logicalID, copying desiredVolume/appliedVolume/isMuted/isPinned/compatibility/volumeBoost/muteSource/targetDeviceUID from cache onto live apps (mergedSession, AppStore.swift:1383-1415).",
        "reapplyRestoredAudioState() re-applies saved boost/mute/volume and pinned-device routes to the backend for any app whose state differs from defaults, so audible output matches the restored UI; apps not running this session are skipped on error (AppStore.swift:820-846).",
        "load() rejects a session.json larger than the 10MB maxFileSize and returns nil (SessionStore.swift:33-38)."
      ],
      "acceptanceCriteria": [
        "Setting an app to 30% then quitting and relaunching shows that app at 30% in the UI and re-applies 30% to the backend once the app is running again.",
        "session.json exists under Application Support/Waves after the first volume/mute change and contains no iconTIFFData field.",
        "A session.json over 10MB causes load() to return nil and the app to start from an empty session without crashing.",
        "Apps present in the cache but no longer running are merged in without throwing, and re-apply errors for them are logged and swallowed (no crash, no toast spam).",
        "Saving never blocks the main thread (writes occur on the store's background queue)."
      ],
      "codeRefs": [
        "Sources/Waves/Services/Persistence/SessionStore.swift:30-96",
        "Sources/Waves/Stores/AppStore.swift:83",
        "Sources/Waves/Stores/AppStore.swift:203-218",
        "Sources/Waves/Stores/AppStore.swift:820-846",
        "Sources/Waves/Stores/AppStore.swift:1383-1415",
        "Sources/Waves/Stores/AppStore.swift:1438-1440"
      ],
      "implStatus": "implemented",
      "implNotes": "Wired end-to-end and reasonably robust. Two correctness concerns: (1) SessionStore is the ONLY store that does NOT back up a corrupt/undecodable session.json — load() just returns nil (SessionStore.swift:43-46), so the next save silently overwrites the unreadable file; the recovery affordance the other three stores have is missing here. (2) The save path manually re-constructs AudioApp field-by-field to drop iconTIFFData (SessionStore.swift:56-87); if AudioApp gains a new persisted field and a developer forgets to add it here it will be silently dropped from the session cache — the comment flags this but nothing enforces it. mergedSession trusts cached compatibility values, which could be stale if an app's tap compatibility changed between launches.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1H 1L]",
      "errorsFound": "• [HIGH/correctness] Pinned per-app output device route is NOT re-applied on relaunch (Sources/Waves/Stores/AppStore.swift:820-846) — reapplyRestoredAudioState() (AppStore.swift:820-846) treats an app with a non-nil targetDeviceUID as 'customized' (line 830) and enters the re-apply loop, but the loop body (lines 833-835) only calls backend.setVolumeBoost, setMuted, and setDesiredVolume. It never calls backend.setOutputDevice(uid:forAppID:) (which exists at WorkspaceAudioControlBackend.swift:710 and is used by the live setOutputDevice path at AppStore.swift:658). On a fresh start the backend's internal apps all have targetDeviceUID=nil, so createController routes to the system default (WorkspaceAudioControlBackend.swift:436-444 only honors targetDeviceUID when the backend's own app carries it). The pinned route is merged into the AppStore UI (mergedSession line 1404) but never re-established in the audio engine, so an app pinned to e.g. headphones plays to the system default device after relaunch. | Fix: Inside the loop, when app.targetDeviceUID != nil, also call `try await backend.setOutputDevice(uid: app.targetDeviceUID, forAppID: app.logicalID)` within the existing do/catch so apps not running this session are skipped on error.\n• [LOW/error-handling] Corrupt or newer-version session.json is silently discarded and overwritten (no recovery copy) (Sources/Waves/Services/Persistence/SessionStore.swift:43-46) — SessionStore.load() (SessionStore.swift:43-46) returns nil on any decode error, including the dataCorrupted thrown by PersistedSchema for a session.json written by a newer build (schemaVersion > current). Unlike PreferencesStore, PresetStore, and DeviceVolumePresetsStore — all of which call backupCorruptFile() before falling back — SessionStore has no backup step; on the next persistSessionSnapshot() the unreadable file is overwritten via the atomic write at SessionStore.swift:91. A user who downgrades, or whose session.json is partially corrupted, loses the saved mix with no recovery artifact. | Fix: Add a backupCorruptFile() (move session.json to session.json.corrupt) in the catch before returning nil, matching the other three stores, so a newer-build or corrupted session is preserved rather than overwritten.",
      "fixStatus": "2 fixed",
      "fixNotes": "tier1: FU-RELAUNCH-PINNED-ROUTE | tier3: FU-SESSIONSTORE-CORRUPT-BACKUP",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "high",
          "type": "correctness",
          "title": "Pinned per-app output device route is NOT re-applied on relaunch",
          "detail": "reapplyRestoredAudioState() (AppStore.swift:820-846) treats an app with a non-nil targetDeviceUID as 'customized' (line 830) and enters the re-apply loop, but the loop body (lines 833-835) only calls backend.setVolumeBoost, setMuted, and setDesiredVolume. It never calls backend.setOutputDevice(uid:forAppID:) (which exists at WorkspaceAudioControlBackend.swift:710 and is used by the live setOutputDevice path at AppStore.swift:658). On a fresh start the backend's internal apps all have targetDeviceUID=nil, so createController routes to the system default (WorkspaceAudioControlBackend.swift:436-444 only honors targetDeviceUID when the backend's own app carries it). The pinned route is merged into the AppStore UI (mergedSession line 1404) but never re-established in the audio engine, so an app pinned to e.g. headphones plays to the system default device after relaunch.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:820-846",
          "expectedVsActual": "Expected (PERSIST-01): 'reapplyRestoredAudioState() re-applies saved boost/mute/volume and pinned-device routes to the backend.' Actual: pinned-device routes are used as a customization trigger but never re-applied; only volume/mute/boost are restored.",
          "suggestedFix": "Inside the loop, when app.targetDeviceUID != nil, also call `try await backend.setOutputDevice(uid: app.targetDeviceUID, forAppID: app.logicalID)` within the existing do/catch so apps not running this session are skipped on error.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier1:applied:FU-RELAUNCH-PINNED-ROUTE",
          "uid": "PERSIST-01#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-RELAUNCH-PINNED-ROUTE",
          "fixTier": "tier1"
        },
        {
          "severity": "low",
          "type": "error-handling",
          "title": "Corrupt or newer-version session.json is silently discarded and overwritten (no recovery copy)",
          "detail": "SessionStore.load() (SessionStore.swift:43-46) returns nil on any decode error, including the dataCorrupted thrown by PersistedSchema for a session.json written by a newer build (schemaVersion > current). Unlike PreferencesStore, PresetStore, and DeviceVolumePresetsStore — all of which call backupCorruptFile() before falling back — SessionStore has no backup step; on the next persistSessionSnapshot() the unreadable file is overwritten via the atomic write at SessionStore.swift:91. A user who downgrades, or whose session.json is partially corrupted, loses the saved mix with no recovery artifact.",
          "codeRef": "Sources/Waves/Services/Persistence/SessionStore.swift:43-46",
          "expectedVsActual": "Expected: parity with the recovery affordance the other three stores provide. Actual: silent data loss with no .corrupt copy, inconsistent with the other stores.",
          "suggestedFix": "Add a backupCorruptFile() (move session.json to session.json.corrupt) in the catch before returning nil, matching the other three stores, so a newer-build or corrupted session is preserved rather than overwritten.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Persistence/SessionStore.swift",
          "disposition": "tier3:applied:FU-SESSIONSTORE-CORRUPT-BACKUP",
          "uid": "PERSIST-01#2",
          "canonicalFile": "Sources/Waves/Services/Persistence/SessionStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-SESSIONSTORE-CORRUPT-BACKUP",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "PERSIST-02",
      "area": "Persistence (Session / Preferences / Presets / Device volume presets)",
      "feature": "User preferences persistence with forward/backward-compatible decoding",
      "priority": "P0",
      "userStory": "As a Waves user, I want my settings (sort mode, recent/system app visibility, auto-restore device, auto-pause music, keyboard shortcuts, per-device presets, URL scheme, excluded apps, manual order) to be remembered across launches and to survive app updates, so that I configure Waves once and it stays the way I left it.",
      "expectedBehavior": [
        "All settings toggles in SettingsView write through to preferences and call store.persistPreferences(), which saves preferences.json atomically on a background queue (SettingsView.swift:41-168, AppStore.swift:1264-1267, PreferencesStore.swift:69-79).",
        "First launch with no preferences.json returns default UserPreferences() without writing a file (PreferencesStore.swift:33-35).",
        "UserPreferences decodes each field independently with per-field fallbacks, so a file written by an older build that is missing newly-added keys still loads cleanly instead of resetting every setting (UserPreferences.swift:40-61).",
        "On init, a manual sortMode with an empty customAppOrder is downgraded to .name, and an unacknowledged URL scheme is force-disabled then marked acknowledged, and launchAtLoginEnabled is reconciled with the actual login-item status (AppStore.swift:88-99).",
        "excludedAppIDs are persisted and used to stop Waves tapping DAW/VoIP apps; excluding an app sorts and saves the list (AppStore.swift:679-694).",
        "Manual reordering of apps writes customAppOrder and flips sortMode to .manual, persisting both (AppStore.swift:1339-1354).",
        "A preferences file exceeding 10MB, or one that fails to decode, is backed up to preferences.json.corrupt and defaults are used instead of overwriting (PreferencesStore.swift:38-67)."
      ],
      "acceptanceCriteria": [
        "Changing any settings toggle and relaunching restores that toggle's value.",
        "Deleting preferences.json and launching yields all default settings and no error.",
        "A preferences.json missing a newly-added key (e.g. enablePerDeviceVolumePresets) loads with that key defaulted and all other saved keys intact.",
        "A corrupt preferences.json is renamed to preferences.json.corrupt on next load and the app starts with defaults.",
        "Reordering apps in the list persists the order and a later relaunch shows the same manual order.",
        "Launching with manual sort but no saved order falls back to Name sort."
      ],
      "codeRefs": [
        "Sources/Waves/Services/Persistence/PreferencesStore.swift:29-79",
        "Sources/Waves/Stores/UserPreferences.swift:3-62",
        "Sources/Waves/Stores/AppStore.swift:88-99",
        "Sources/Waves/Stores/AppStore.swift:679-694",
        "Sources/Waves/Stores/AppStore.swift:1264-1267",
        "Sources/Waves/Stores/AppStore.swift:1339-1354",
        "Sources/Waves/Settings/SettingsView.swift:41-168"
      ],
      "implStatus": "implemented",
      "implNotes": "Strong implementation: per-field tolerant decoding plus corrupt-file backup makes preferences resilient to schema drift and corruption. Minor concern: launchAtLoginEnabled is persisted into preferences.json but is also overwritten from loginItemService.status on every init and in syncOnboarding (AppStore.swift:95, 1364), so the persisted value is effectively advisory and the OS login-item state is the source of truth — harmless but the stored field is redundant.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/edge-case] Manual reorder drops saved order for apps not currently visible/running (Sources/Waves/Stores/AppStore.swift:1339-1354) — reorderApps() (AppStore.swift:1339-1354) rebuilds customAppOrder from `visibleApps.map { $0.logicalID }` (line 1345). visibleApps is built from session.apps filtered by showSystemProcesses (lines 103-107) and only contains apps present in the current session. Any logicalID previously stored in customAppOrder for an app not running this session — or for a hidden system process when showSystemProcesses is off — is discarded on the next manual reorder. When that app later relaunches, manualOrderComparator's `firstIndex(of:) ?? Int.max` (line 1330) sorts it to the end instead of its saved slot. | Fix: Merge the new visible ordering into the existing customAppOrder instead of replacing it: keep entries for currently-absent IDs and splice the reordered visible IDs around them.",
      "fixStatus": "1 deferred",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "edge-case",
          "title": "Manual reorder drops saved order for apps not currently visible/running",
          "detail": "reorderApps() (AppStore.swift:1339-1354) rebuilds customAppOrder from `visibleApps.map { $0.logicalID }` (line 1345). visibleApps is built from session.apps filtered by showSystemProcesses (lines 103-107) and only contains apps present in the current session. Any logicalID previously stored in customAppOrder for an app not running this session — or for a hidden system process when showSystemProcesses is off — is discarded on the next manual reorder. When that app later relaunches, manualOrderComparator's `firstIndex(of:) ?? Int.max` (line 1330) sorts it to the end instead of its saved slot.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1339-1354",
          "expectedVsActual": "Expected (PERSIST-02): manual order persists across relaunches. Actual: a manual reorder while some previously-ordered apps are offline (or system-hidden) silently prunes them from customAppOrder, losing their saved position.",
          "suggestedFix": "Merge the new visible ordering into the existing customAppOrder instead of replacing it: keep entries for currently-absent IDs and splice the reordered visible IDs around them.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:Switching from full-replace to merge changes the persisted ordering semantics and the relaunched-app placement behavior, which interacts with manualOrderComparator and the visible/hidden filtering. It needs a deliberate decision on how absent IDs are positioned (and whether to prune), so it is more than a behavior-preserving localized fix. ORDER-04#4's pruning is an explicit policy choice. Low user impact (manual sort is opt-in and apps still appear, just at the end).",
          "uid": "PERSIST-02#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "PERSIST-03",
      "area": "Persistence (Session / Preferences / Presets / Device volume presets)",
      "feature": "Volume preset library (create/apply/delete, seeded defaults)",
      "priority": "P1",
      "userStory": "As a Waves user, I want to save the current per-app mix as a named preset and re-apply it later, with sensible built-in Focus/Meeting presets out of the box, so that I can switch between known audio setups (e.g. focus vs. meeting) in one click.",
      "expectedBehavior": [
        "On first launch presets.json is missing, so PresetStore seeds the built-in defaults (Focus, Meeting) to disk and returns them (PresetStore.swift:30-36; Preset.defaults at AudioSessionSnapshot+Preview.swift:121-144).",
        "savePreset(named:) captures the current backend state as a Preset, strips any excludedAppIDs entries, then either replaces an existing preset matched case-insensitively by name (preserving its id/createdAt) or appends a new one, and persists the array (AppStore.swift:1031-1064).",
        "applyPreset filters out excluded apps, applies the preset through the backend, persists the resulting session, and re-checks auto-pause; on failure it falls back to the current backend snapshot and still persists (AppStore.swift:1001-1029).",
        "deletePresets(at:) removes presets at the given offsets and saves the updated array (AppStore.swift:1066-1077).",
        "PresetEntry clamps appID length to 256 chars, desiredVolume to [0,1], and volumeBoost to [1,4] on decode/init, defending against malformed persisted data (Preset.swift:31-58).",
        "A presets file over 10MB or that fails to decode is backed up to presets.json.corrupt and the built-in defaults are returned rather than losing the user's library silently (PresetStore.swift:38-68)."
      ],
      "acceptanceCriteria": [
        "A clean install shows Focus and Meeting presets and writes presets.json.",
        "Saving a preset with the name of an existing one (any case) updates it in place rather than creating a duplicate.",
        "Saving a new-named preset appends it and it survives relaunch.",
        "Applying a preset changes app volumes/mutes accordingly and persists the new session.",
        "Deleting a preset removes it from disk and it does not reappear on relaunch.",
        "An excluded app never appears in a newly saved preset and is never re-tapped by applying a preset.",
        "A corrupt presets.json is renamed to presets.json.corrupt and the defaults are restored."
      ],
      "codeRefs": [
        "Sources/Waves/Services/Persistence/PresetStore.swift:30-81",
        "Sources/Waves/Stores/AppStore.swift:1001-1077",
        "Sources/WavesAudioCore/Models/Preset.swift:25-67",
        "Sources/WavesAudioCore/Backend/AudioSessionSnapshot+Preview.swift:121-144"
      ],
      "implStatus": "implemented",
      "implNotes": "Complete and defensive (clamping, exclusion filtering, corrupt-file backup, default seeding). UX note: there is no explicit rename-preset function and no way to restore the built-in defaults once a user has deleted them (defaults are only seeded when presets.json is absent, PresetStore.swift:33-36) — deleting Focus/Meeting and relaunching will NOT bring them back because the file now exists. Replacing a same-named preset silently overwrites without confirmation.",
      "source": "phase1-story",
      "testStatus": "3 issue(s) [3L]",
      "errorsFound": "• [LOW/ux] Deleted built-in Focus/Meeting presets can never be restored (Sources/Waves/Services/Persistence/PresetStore.swift:30-36) — PresetStore.load(defaults:) (PresetStore.swift:30-36) seeds Preset.defaults only when presets.json is absent. Once any presets.json exists, deleting the built-in Focus/Meeting presets and relaunching will not bring them back because the file is present and loaded as-is (lines 46-48). There is no 'restore defaults' affordance anywhere in the UI. | Fix: Add a 'Restore built-in presets' action in SettingsView's preset section that re-adds any missing default-named presets, or re-seed missing defaults on load.\n• [LOW/ux] Saving a preset with an existing name overwrites it with no confirmation (Sources/Waves/Stores/AppStore.swift:1041-1052) — savePreset() (AppStore.swift:1041-1052) matches an existing preset case-insensitively by trimmed name and replaces it in place without any confirmation. The success toast says 'Preset saved' (line 1055) with no indication that an existing preset's contents were destroyed. A user typing the name of an existing preset silently overwrites it. | Fix: When a same-name match is found, surface a confirmation or at least a distinct 'Preset updated' toast so the overwrite is intentional and visible.\n• [LOW/ux] Over-length preset name silently fails to save with no feedback (Sources/Waves/Features/Mixer/MainWindowView.swift:137-147) — In the save sheet handler MainWindowView.savePreset() (MainWindowView.swift:137-147), a trimmed name longer than maxLength=100 hits `guard trimmed.count <= maxLength else { return }` (line 143) and returns without calling store.savePreset and without dismissing the sheet or showing any error. The Save button is only disabled when the name is empty (MainWindowView.swift:612), so for an over-length name the user clicks Save and nothing happens — no toast, no message, sheet stays open. The import path has no such limit at all (see PERSIST-04), so the 100-char rule is both inconsistent and silent. | Fix: Either clamp/truncate the name to 100 chars before saving, or surface an inline validation message / disable Save when the trimmed name exceeds the limit.",
      "fixStatus": "1 fixed · 1 deferred · 1 n/a",
      "fixNotes": "tier2: FU-PRESET-NAME-LENGTH-DISABLE",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "ux",
          "title": "Deleted built-in Focus/Meeting presets can never be restored",
          "detail": "PresetStore.load(defaults:) (PresetStore.swift:30-36) seeds Preset.defaults only when presets.json is absent. Once any presets.json exists, deleting the built-in Focus/Meeting presets and relaunching will not bring them back because the file is present and loaded as-is (lines 46-48). There is no 'restore defaults' affordance anywhere in the UI.",
          "codeRef": "Sources/Waves/Services/Persistence/PresetStore.swift:30-36",
          "expectedVsActual": "Expected (PERSIST-03): 'sensible built-in Focus/Meeting presets out of the box.' Actual: defaults are one-time seeded; after deletion they are gone permanently with no way to recover.",
          "suggestedFix": "Add a 'Restore built-in presets' action in SettingsView's preset section that re-adds any missing default-named presets, or re-seed missing defaults on load.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Persistence/PresetStore.swift",
          "disposition": "deferred:needs-user-decision:Either-or fix; both options need a product decision on intended semantics.",
          "uid": "PERSIST-03#1",
          "canonicalFile": "Sources/Waves/Services/Persistence/PresetStore.swift"
        },
        {
          "severity": "low",
          "type": "ux",
          "title": "Saving a preset with an existing name overwrites it with no confirmation",
          "detail": "savePreset() (AppStore.swift:1041-1052) matches an existing preset case-insensitively by trimmed name and replaces it in place without any confirmation. The success toast says 'Preset saved' (line 1055) with no indication that an existing preset's contents were destroyed. A user typing the name of an existing preset silently overwrites it.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1041-1052",
          "expectedVsActual": "Expected: updating a same-named preset in place is intended, but the overwrite should be visible/confirmed. Actual: silent destructive overwrite with a generic 'saved' toast.",
          "suggestedFix": "When a same-name match is found, surface a confirmation or at least a distinct 'Preset updated' toast so the overwrite is intentional and visible.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "PERSIST-03#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "low",
          "type": "ux",
          "title": "Over-length preset name silently fails to save with no feedback",
          "detail": "In the save sheet handler MainWindowView.savePreset() (MainWindowView.swift:137-147), a trimmed name longer than maxLength=100 hits `guard trimmed.count <= maxLength else { return }` (line 143) and returns without calling store.savePreset and without dismissing the sheet or showing any error. The Save button is only disabled when the name is empty (MainWindowView.swift:612), so for an over-length name the user clicks Save and nothing happens — no toast, no message, sheet stays open. The import path has no such limit at all (see PERSIST-04), so the 100-char rule is both inconsistent and silent.",
          "codeRef": "Sources/Waves/Features/Mixer/MainWindowView.swift:137-147",
          "expectedVsActual": "Expected: a save action either succeeds or tells the user why it failed. Actual: an over-100-char preset name is silently dropped with no feedback.",
          "suggestedFix": "Either clamp/truncate the name to 100 chars before saving, or surface an inline validation message / disable Save when the trimmed name exceeds the limit.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "disposition": "tier2:applied:FU-PRESET-NAME-LENGTH-DISABLE",
          "uid": "PERSIST-03#3",
          "canonicalFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-PRESET-NAME-LENGTH-DISABLE",
          "fixTier": "tier2"
        }
      ]
    },
    {
      "id": "PERSIST-04",
      "area": "Persistence (Session / Preferences / Presets / Device volume presets)",
      "feature": "Preset export/import as JSON files",
      "priority": "P2",
      "userStory": "As a Waves user, I want to export a preset to a JSON file and import presets shared by others, so that I can back up and share my audio configurations across machines or with teammates.",
      "expectedBehavior": [
        "exportPreset encodes a single Preset as pretty-printed JSON and writes it via an NSSavePanel, defaulting the filename to <preset name>.json (AppStore.swift:1079-1110).",
        "importPreset opens an NSOpenPanel limited to .json, single selection, files only (AppStore.swift:1112-1126).",
        "Import validates the file is <=10MB, the decoded preset name is non-empty, and entries count <=1000 before accepting it; each failure shows an error toast and aborts (AppStore.swift:1128-1148).",
        "On import, a preset whose name case-insensitively matches an existing one replaces it (keeping the existing id/createdAt); otherwise the imported preset is given a fresh UUID and timestamps to avoid SwiftUI identity collisions, then appended (AppStore.swift:1150-1165).",
        "After a successful import the presets array is persisted via presetStore.save (AppStore.swift:1167).",
        "If no main window is available, export/import abort with an error toast (AppStore.swift:1091-1094, 1120-1123)."
      ],
      "acceptanceCriteria": [
        "Exporting a preset produces a readable, pretty-printed JSON file named after the preset.",
        "Importing that JSON file recreates the preset and persists it.",
        "Importing a file over 10MB, with an empty name, or with more than 1000 entries is rejected with an error toast and no change to the library.",
        "Importing a preset whose name matches an existing one updates the existing entry instead of creating a duplicate.",
        "Importing a uniquely-named preset assigns it a new UUID so list identity does not collide with an existing preset."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:1079-1110",
        "Sources/Waves/Stores/AppStore.swift:1112-1179"
      ],
      "implStatus": "implemented",
      "implNotes": "Solid with good input validation (size, name, entry-count, fresh UUID on append). Minor gap: imported PresetEntry values are clamped by PresetEntry.init on decode, but there is no validation/sanitization of the preset's own name length on import, and a malformed JSON that decodes to a Preset with a 256-char-trimmed appID is accepted silently. Export/import operate on one preset at a time only (no bulk export of the whole library).",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/edge-case] Imported preset name length is not validated or clamped (Sources/Waves/Stores/AppStore.swift:1128-1165) — importPreset() (AppStore.swift:1128-1165) validates file size (<=10MB, line 1130), non-empty name (line 1140), and entry count (<=1000, line 1145), but never bounds the name length. PresetEntry.init clamps appID to 256 chars and volume/boost ranges, but Preset.name has no clamp on decode or import. A crafted JSON with a multi-megabyte name (under the 10MB file cap) is accepted, persisted, rendered in the preset list, and used as the default export filename. This is also inconsistent with the save sheet, which enforces a 100-char limit (MainWindowView.swift:142-143). | Fix: After decode, truncate (or reject) names over a sane limit (e.g. 100 chars to match the save sheet) before matching/appending and persisting.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier3: FU-IMPORT-PRESET-VALIDATION",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "edge-case",
          "title": "Imported preset name length is not validated or clamped",
          "detail": "importPreset() (AppStore.swift:1128-1165) validates file size (<=10MB, line 1130), non-empty name (line 1140), and entry count (<=1000, line 1145), but never bounds the name length. PresetEntry.init clamps appID to 256 chars and volume/boost ranges, but Preset.name has no clamp on decode or import. A crafted JSON with a multi-megabyte name (under the 10MB file cap) is accepted, persisted, rendered in the preset list, and used as the default export filename. This is also inconsistent with the save sheet, which enforces a 100-char limit (MainWindowView.swift:142-143).",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1128-1165",
          "expectedVsActual": "Expected: import validates the file before accepting (story emphasizes size/name/entry-count validation). Actual: name length is unbounded — the one structural field left unsanitized.",
          "suggestedFix": "After decode, truncate (or reject) names over a sane limit (e.g. 100 chars to match the save sheet) before matching/appending and persisting.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier3:applied:FU-IMPORT-PRESET-VALIDATION",
          "uid": "PERSIST-04#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-IMPORT-PRESET-VALIDATION",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "PERSIST-05",
      "area": "Persistence (Session / Preferences / Presets / Device volume presets)",
      "feature": "Per-device volume presets (remember per-app volumes per output device)",
      "priority": "P1",
      "userStory": "As a Waves user with multiple output devices, I want each app's volume/mute/boost to be remembered separately per output device and re-applied automatically when I switch devices, so that, e.g., music can be quiet on speakers but loud on headphones without me re-adjusting on every switch.",
      "expectedBehavior": [
        "When enablePerDeviceVolumePresets is on, every volume/mute/boost change records an AppVolumeSettings under the current device's id in DeviceVolumePresets and saves to deviceVolumePresets.json (AppStore.swift:371-381, 508-519, 555-565; DeviceVolumePresetsStore.save writes pretty-printed atomic JSON, DeviceVolumePresetsStore.swift:69-80).",
        "Volume changes update the in-memory preset on each tick but defer the disk write to commit so a slider drag does not rewrite the file per frame (AppStore.swift:378-381, 389-392).",
        "On a device change, if the new device differs from the previous one and the feature is enabled, restoreDeviceVolumePresets re-applies each saved app's desiredVolume/isMuted/volumeBoost to both the session and the backend, then persists the session (AppStore.swift:802-804, 848-867).",
        "Settings exposes a toggle for the feature; turning it off stops recording and restoring per-device presets (SettingsView.swift:105-114).",
        "First launch with no file returns an empty DeviceVolumePresets; a file >10MB or failing to decode is backed up to .corrupt and empty presets are used (DeviceVolumePresetsStore.swift:29-67).",
        "AppVolumeSettings clamps volumeBoost to [1,4] and decodes missing fields with defaults (UserPreferences.swift:64-95)."
      ],
      "acceptanceCriteria": [
        "With the feature on, setting an app to 20% on Speakers and 80% on Headphones, then switching devices, re-applies 20%/80% respectively to that app.",
        "Per-device settings persist across relaunch (deviceVolumePresets.json present and reloaded).",
        "Disabling the toggle prevents new per-device recording and prevents restore on device switch.",
        "A slider drag results in a single file write at commit, not one per intermediate value.",
        "A corrupt deviceVolumePresets.json is renamed to .corrupt and the feature starts from empty without crashing."
      ],
      "codeRefs": [
        "Sources/Waves/Services/Persistence/DeviceVolumePresetsStore.swift:29-80",
        "Sources/Waves/Stores/UserPreferences.swift:64-114",
        "Sources/Waves/Stores/AppStore.swift:371-392",
        "Sources/Waves/Stores/AppStore.swift:508-565",
        "Sources/Waves/Stores/AppStore.swift:802-867",
        "Sources/Waves/Settings/SettingsView.swift:105-114"
      ],
      "implStatus": "partial",
      "implNotes": "Functionally wired and persisted, but has real gaps. (1) Unbounded growth / no cleanup: DeviceVolumePresets.removeDevice exists (UserPreferences.swift:111-113) but is NEVER called anywhere in production — entries for unplugged/forgotten devices accumulate forever, and every app the user ever touches on every device is retained indefinitely with no pruning against currently-known apps/devices. (2) Inconsistent save scheduling: setMuted/setVolumeBoost fire deviceVolumePresetsStore.save inside an unstructured Task (AppStore.swift:516-518, 562-564) while commitDesiredVolume saves synchronously through the store's own queue; both are safe (the store serializes on its queue) but the extra Task is redundant. (3) restoreDeviceVolumePresets only restores apps already present in the current session; saved settings for apps not yet running this device-switch are not pre-applied (acceptable but worth noting). (4) Device identity is keyed by device id string, so a device that returns under a different id (some aggregate/Bluetooth cases) will not match its saved settings.",
      "source": "phase1-story",
      "testStatus": "6 issue(s) [1M 5L]",
      "errorsFound": "• [MEDIUM/correctness] Device-switch restore re-taps EXCLUDED apps (Sources/Waves/Stores/AppStore.swift:848-863) — restoreDeviceVolumePresets() (AppStore.swift:848-867) iterates all session apps and applies any saved per-device settings to the backend (setDesiredVolume/setMuted/setVolumeBoost, lines 856-859) with NO isExcluded guard — unlike reapplyRestoredAudioState which guards at line 826. While new recordings are blocked for excluded apps (the setDesiredVolume/setMuted/setVolumeBoost entry points all guard on !isExcluded), settings recorded BEFORE an app was excluded remain in deviceVolumePresets: setExcluded() (AppStore.swift:686-713) does NOT prune that app's per-device entries. On a later output-device switch, those stale settings cause Waves to re-tap an app the user explicitly excluded — exactly the DAW/VoIP escape-hatch that must never be touched. | Fix: Add `guard !isExcluded(app) else { continue }` at the top of the loop body in restoreDeviceVolumePresets, and prune the app's per-device entries in setExcluded().\n• [LOW/dead-code] DeviceVolumePresets.removeDevice is dead code; presets grow unbounded with no pruning (Sources/Waves/Stores/UserPreferences.swift:111-113) — DeviceVolumePresets.removeDevice(_:) (UserPreferences.swift:111-113) is never called anywhere in production (grep confirms the only 'removeDevice…' hit in source is the unrelated removeDeviceChangeListener). Every device the user has ever used and every app touched on it is retained forever in deviceVolumes; entries for permanently-unplugged devices and uninstalled apps accumulate with no cleanup. Growth is bounded only by the 10MB load cap, beyond which the whole file is backed up to .corrupt and the feature resets to empty. | Fix: Either remove removeDevice as dead code, or wire periodic pruning (drop device entries not seen in availableDevices and app entries whose logicalID no longer appears) and call it on startup/device-change.\n• [LOW/correctness] Device-switch restore leaves appliedVolume stale and persists it (Sources/Waves/Stores/AppStore.swift:852-866) — restoreDeviceVolumePresets() updates session.apps[index].desiredVolume/isMuted/volumeBoost (lines 852-854) but never recomputes appliedVolume, and never re-merges the backend snapshot afterward (unlike reapplyRestoredAudioState which sets appliedVolume = isMuted ? 0 : desiredVolume at line 837). It then persistSessionSnapshot() at line 866, writing the stale appliedVolume to disk. After a device switch the row's effective/applied level can be inconsistent with the restored desiredVolume/mute until the next backend refresh. | Fix: After updating the three fields, set `session.apps[index].appliedVolume = settings.isMuted ? 0 : settings.desiredVolume` (and routingState as appropriate), or re-merge from the backend snapshot before persisting.\n• [LOW/correctness] Applying a preset does not update per-device presets, so a later device switch reverts the preset (Sources/Waves/Stores/AppStore.swift:1001-1029) — applyPreset() (AppStore.swift:1001-1029) changes session volumes/mutes via the backend and persists the session, but never records the resulting per-app values into deviceVolumePresets for the current device. The per-device recording only happens in setDesiredVolume/setMuted/setVolumeBoost (AppStore.swift:371-381, 508-519, 555-565). With enablePerDeviceVolumePresets on, after applying a preset the deviceVolumePresets for the current device still hold the OLD values, so the next time the user switches output devices and back, restoreDeviceVolumePresets (lines 848-867) re-applies the stale pre-preset values, silently undoing the applied preset. | Fix: After a successful applyPreset, when enablePerDeviceVolumePresets is on, write each affected app's resulting desiredVolume/isMuted/volumeBoost into deviceVolumePresets for currentDeviceID and save.\n• [LOW/concurrency] Each device-change event spawns an independent Task; rapid switches can interleave restore (Sources/Waves/Stores/AppStore.swift:792-818) — observeDeviceChanges()'s `for await` loop (AppStore.swift:784-789) calls handleDeviceChange() synchronously, which itself launches an unstructured Task (lines 795-817) and returns immediately, so the loop proceeds to the next event without awaiting completion. There is no stored handle, cancellation, or serialization. previousDeviceID is captured from `session` at each Task's start (line 798); under rapid device changes two Tasks can run concurrently, each awaiting backend snapshots and per-device restore, leading to interleaved writes to session.apps and a possible restore against the wrong/previous device or a double-restore. | Fix: Serialize device-change handling (store the Task and cancel/await the prior one, or confine to a single queue) so only one restore runs at a time and stale ones are cancelled.\n• [LOW/correctness] AppVolumeSettings does not clamp desiredVolume to [0,1] on decode (Sources/Waves/Stores/UserPreferences.swift:69-73) — AppVolumeSettings.init clamps volumeBoost to [1,4] (UserPreferences.swift:72) but applies no clamp to desiredVolume (lines 69-73), and the decode init passes desiredVolume straight through (lines 83-87). A malformed/edited deviceVolumePresets.json with desiredVolume outside [0,1] is loaded as-is and, on the next device switch, restoreDeviceVolumePresets passes it straight to backend.setDesiredVolume and into session.apps[index].desiredVolume. PresetEntry and AudioApp both clamp volume to [0,1], making this an inconsistent defensive gap. | Fix: Clamp desiredVolume to [0,1] in AppVolumeSettings.init alongside the existing volumeBoost clamp.",
      "fixStatus": "3 fixed · 3 n/a",
      "fixNotes": "tier2: FU-EXCLUSION-PRESET-RESTORE | tier3: FU-APPVOLSETTINGS-VOLUME-CLAMP",
      "retestStatus": "PASS (remediated & verified)",
      "issues": [
        {
          "severity": "medium",
          "type": "correctness",
          "title": "Device-switch restore re-taps EXCLUDED apps",
          "detail": "restoreDeviceVolumePresets() (AppStore.swift:848-867) iterates all session apps and applies any saved per-device settings to the backend (setDesiredVolume/setMuted/setVolumeBoost, lines 856-859) with NO isExcluded guard — unlike reapplyRestoredAudioState which guards at line 826. While new recordings are blocked for excluded apps (the setDesiredVolume/setMuted/setVolumeBoost entry points all guard on !isExcluded), settings recorded BEFORE an app was excluded remain in deviceVolumePresets: setExcluded() (AppStore.swift:686-713) does NOT prune that app's per-device entries. On a later output-device switch, those stale settings cause Waves to re-tap an app the user explicitly excluded — exactly the DAW/VoIP escape-hatch that must never be touched.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:848-863",
          "expectedVsActual": "Expected: excluded apps are never tapped or altered by Waves (exclusion contract; honored by reapplyRestoredAudioState at line 826). Actual: restoreDeviceVolumePresets has no isExcluded guard and re-applies saved volume/mute/boost to excluded apps on every qualifying device change.",
          "suggestedFix": "Add `guard !isExcluded(app) else { continue }` at the top of the loop body in restoreDeviceVolumePresets, and prune the app's per-device entries in setExcluded().",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier2:partial:FU-EXCLUSION-PRESET-RESTORE",
          "uid": "PERSIST-05#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "partial",
          "fixedBy": "FU-EXCLUSION-PRESET-RESTORE",
          "fixTier": "tier2"
        },
        {
          "severity": "low",
          "type": "dead-code",
          "title": "DeviceVolumePresets.removeDevice is dead code; presets grow unbounded with no pruning",
          "detail": "DeviceVolumePresets.removeDevice(_:) (UserPreferences.swift:111-113) is never called anywhere in production (grep confirms the only 'removeDevice…' hit in source is the unrelated removeDeviceChangeListener). Every device the user has ever used and every app touched on it is retained forever in deviceVolumes; entries for permanently-unplugged devices and uninstalled apps accumulate with no cleanup. Growth is bounded only by the 10MB load cap, beyond which the whole file is backed up to .corrupt and the feature resets to empty.",
          "codeRef": "Sources/Waves/Stores/UserPreferences.swift:111-113",
          "expectedVsActual": "Expected: per-device presets are remembered. Actual: they are never pruned; the only cleanup helper is unreferenced dead code, risking eventual file bloat and a hard reset at the 10MB cap.",
          "suggestedFix": "Either remove removeDevice as dead code, or wire periodic pruning (drop device entries not seen in availableDevices and app entries whose logicalID no longer appears) and call it on startup/device-change.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/UserPreferences.swift",
          "disposition": "iter2:fixed",
          "uid": "PERSIST-05#2",
          "canonicalFile": "Sources/Waves/Stores/UserPreferences.swift",
          "fixStatus": "applied",
          "fixedBy": "iter2-deferred-reduction",
          "fixTier": "iter2"
        },
        {
          "severity": "low",
          "type": "correctness",
          "title": "Device-switch restore leaves appliedVolume stale and persists it",
          "detail": "restoreDeviceVolumePresets() updates session.apps[index].desiredVolume/isMuted/volumeBoost (lines 852-854) but never recomputes appliedVolume, and never re-merges the backend snapshot afterward (unlike reapplyRestoredAudioState which sets appliedVolume = isMuted ? 0 : desiredVolume at line 837). It then persistSessionSnapshot() at line 866, writing the stale appliedVolume to disk. After a device switch the row's effective/applied level can be inconsistent with the restored desiredVolume/mute until the next backend refresh.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:852-866",
          "expectedVsActual": "Expected: restored per-device settings are reflected consistently in the session/UI. Actual: appliedVolume is not recomputed on restore, leaving a stale value that is also persisted.",
          "suggestedFix": "After updating the three fields, set `session.apps[index].appliedVolume = settings.isMuted ? 0 : settings.desiredVolume` (and routingState as appropriate), or re-merge from the backend snapshot before persisting.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "PERSIST-05#3",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "low",
          "type": "correctness",
          "title": "Applying a preset does not update per-device presets, so a later device switch reverts the preset",
          "detail": "applyPreset() (AppStore.swift:1001-1029) changes session volumes/mutes via the backend and persists the session, but never records the resulting per-app values into deviceVolumePresets for the current device. The per-device recording only happens in setDesiredVolume/setMuted/setVolumeBoost (AppStore.swift:371-381, 508-519, 555-565). With enablePerDeviceVolumePresets on, after applying a preset the deviceVolumePresets for the current device still hold the OLD values, so the next time the user switches output devices and back, restoreDeviceVolumePresets (lines 848-867) re-applies the stale pre-preset values, silently undoing the applied preset.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1001-1029",
          "expectedVsActual": "Expected: an applied mix is what gets remembered for the current device. Actual: preset application bypasses per-device recording, so a device round-trip reverts the preset.",
          "suggestedFix": "After a successful applyPreset, when enablePerDeviceVolumePresets is on, write each affected app's resulting desiredVolume/isMuted/volumeBoost into deviceVolumePresets for currentDeviceID and save.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "PERSIST-05#4",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "low",
          "type": "concurrency",
          "title": "Each device-change event spawns an independent Task; rapid switches can interleave restore",
          "detail": "observeDeviceChanges()'s `for await` loop (AppStore.swift:784-789) calls handleDeviceChange() synchronously, which itself launches an unstructured Task (lines 795-817) and returns immediately, so the loop proceeds to the next event without awaiting completion. There is no stored handle, cancellation, or serialization. previousDeviceID is captured from `session` at each Task's start (line 798); under rapid device changes two Tasks can run concurrently, each awaiting backend snapshots and per-device restore, leading to interleaved writes to session.apps and a possible restore against the wrong/previous device or a double-restore.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:792-818",
          "expectedVsActual": "Expected: a device switch restores that device's presets once, coherently. Actual: no guard against overlapping device-change handlers, so concurrent runs can race on the shared session.",
          "suggestedFix": "Serialize device-change handling (store the Task and cancel/await the prior one, or confine to a single queue) so only one restore runs at a time and stale ones are cancelled.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "invalid:not-a-defect",
          "uid": "PERSIST-05#5",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        },
        {
          "severity": "low",
          "type": "correctness",
          "title": "AppVolumeSettings does not clamp desiredVolume to [0,1] on decode",
          "detail": "AppVolumeSettings.init clamps volumeBoost to [1,4] (UserPreferences.swift:72) but applies no clamp to desiredVolume (lines 69-73), and the decode init passes desiredVolume straight through (lines 83-87). A malformed/edited deviceVolumePresets.json with desiredVolume outside [0,1] is loaded as-is and, on the next device switch, restoreDeviceVolumePresets passes it straight to backend.setDesiredVolume and into session.apps[index].desiredVolume. PresetEntry and AudioApp both clamp volume to [0,1], making this an inconsistent defensive gap.",
          "codeRef": "Sources/Waves/Stores/UserPreferences.swift:69-73",
          "expectedVsActual": "Expected: defensive clamping consistent with PresetEntry/AudioApp. Actual: desiredVolume is not clamped in AppVolumeSettings, so an out-of-range persisted value flows through unbounded.",
          "suggestedFix": "Clamp desiredVolume to [0,1] in AppVolumeSettings.init alongside the existing volumeBoost clamp.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/UserPreferences.swift",
          "disposition": "tier3:applied:FU-APPVOLSETTINGS-VOLUME-CLAMP",
          "uid": "PERSIST-05#6",
          "canonicalFile": "Sources/Waves/Stores/UserPreferences.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-APPVOLSETTINGS-VOLUME-CLAMP",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "PERSIST-06",
      "area": "Persistence (Session / Preferences / Presets / Device volume presets)",
      "feature": "Versioned on-disk schema with legacy and future-version handling",
      "priority": "P1",
      "userStory": "As a Waves user, I want my saved data to keep loading correctly after app updates and to never be wiped just because the file format evolved, so that updates and downgrades do not silently destroy my presets, settings, or saved mix.",
      "expectedBehavior": [
        "All four stores encode their payload inside a VersionedPayload envelope carrying schemaVersion = PersistedSchema.current (1) via PersistedSchema.encode (PersistedSchema.swift:16-19; used in every store's save).",
        "decode accepts both the new versioned envelope and a legacy bare/unversioned payload written before envelopes existed (treated as schema 0), so old files still load (PersistedSchema.swift:23-44).",
        "A file whose schemaVersion is greater than the build's current is rejected with a dataCorrupted error, so a newer build's file is not loaded by an older build (PersistedSchema.swift:32-39); the calling store then backs the file up (except SessionStore) and uses defaults.",
        "migrate is a hook keyed on version for future non-additive format changes; v1 needs no migration and returns the payload unchanged (PersistedSchema.swift:46-50).",
        "All stores resolve their file under Application Support/Waves, falling back to ~/.Waves if the support directory is unavailable, creating intermediate directories (e.g. SessionStore.swift:13-28)."
      ],
      "acceptanceCriteria": [
        "A freshly written file contains top-level keys schemaVersion and payload (verified by PersistedSchemaTests.encodedPayloadCarriesSchemaVersion).",
        "A legacy unversioned object or array still decodes (verified by decodeAcceptsLegacyUnversionedFile / decodeAcceptsLegacyUnversionedArray).",
        "A file with schemaVersion 2 fails to decode under a build whose current is 1, triggering the store's corrupt-backup-and-default path (preferences/presets/device presets) rather than a load of unknown data.",
        "If Application Support is unavailable, stores create and use ~/.Waves without crashing.",
        "Round-tripping preferences through encode/decode preserves field values (verified by versionedEnvelopeRoundTrips)."
      ],
      "codeRefs": [
        "Sources/Waves/Services/Persistence/PersistedSchema.swift:1-51",
        "Sources/Waves/Services/Persistence/SessionStore.swift:13-28",
        "Sources/Waves/Services/Persistence/PreferencesStore.swift:38-67",
        "Tests/WavesTests/PersistedSchemaTests.swift:7-42"
      ],
      "implStatus": "implemented",
      "implNotes": "Well-designed and unit-tested envelope with deliberate future-version rejection and legacy acceptance. Concern: the future-version rejection only protects the user when the calling store backs up the rejected file; SessionStore does not back up on decode failure (SessionStore.swift:43-46), so a session.json written by a newer build is silently discarded and overwritten by the older build on next save. The migrate() function is currently an identity no-op (correct for v1) but is untested for any actual migration path since none exist yet.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/error-handling] Future-version/corrupt session.json bypasses the documented corrupt-backup path and is silently overwritten (Sources/Waves/Services/Persistence/SessionStore.swift:43-46) — PERSIST-06's design rejects any file whose schemaVersion exceeds the build's current with a dataCorrupted error (PersistedSchema), and the story's acceptance criterion states the calling store then takes 'the store's corrupt-backup-and-default path'. This holds for PreferencesStore, PresetStore, and DeviceVolumePresetsStore (each calls backupCorruptFile in its catch), but NOT for SessionStore: load() (SessionStore.swift:43-46) just logs and returns nil with no backup, so a session.json written by a newer build (downgrade) — or any corrupt session — is silently discarded and overwritten by the next save (SessionStore.swift:91). The versioned-schema safety net the story promises is absent for the session store specifically. (Same root cause as the PERSIST-01 backup finding, recorded here because PERSIST-06 owns the future-version/downgrade contract for all four stores.) | Fix: Add backupCorruptFile() to SessionStore.load()'s catch (and the size-limit branch) so the session store matches the other three stores' future-version/corruption handling.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier3: FU-SESSIONSTORE-CORRUPT-BACKUP",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "error-handling",
          "title": "Future-version/corrupt session.json bypasses the documented corrupt-backup path and is silently overwritten",
          "detail": "PERSIST-06's design rejects any file whose schemaVersion exceeds the build's current with a dataCorrupted error (PersistedSchema), and the story's acceptance criterion states the calling store then takes 'the store's corrupt-backup-and-default path'. This holds for PreferencesStore, PresetStore, and DeviceVolumePresetsStore (each calls backupCorruptFile in its catch), but NOT for SessionStore: load() (SessionStore.swift:43-46) just logs and returns nil with no backup, so a session.json written by a newer build (downgrade) — or any corrupt session — is silently discarded and overwritten by the next save (SessionStore.swift:91). The versioned-schema safety net the story promises is absent for the session store specifically. (Same root cause as the PERSIST-01 backup finding, recorded here because PERSIST-06 owns the future-version/downgrade contract for all four stores.)",
          "codeRef": "Sources/Waves/Services/Persistence/SessionStore.swift:43-46",
          "expectedVsActual": "Expected (PERSIST-06): a schemaVersion-2 / corrupt file triggers the store's corrupt-backup-and-default path rather than loading or destroying unknown data. Actual: SessionStore has no backup branch, so a newer-build/corrupt session is silently dropped and overwritten.",
          "suggestedFix": "Add backupCorruptFile() to SessionStore.load()'s catch (and the size-limit branch) so the session store matches the other three stores' future-version/corruption handling.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Persistence/SessionStore.swift",
          "disposition": "tier3:applied:FU-SESSIONSTORE-CORRUPT-BACKUP",
          "uid": "PERSIST-06#1",
          "canonicalFile": "Sources/Waves/Services/Persistence/SessionStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-SESSIONSTORE-CORRUPT-BACKUP",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "TOAST-01",
      "area": "Toasts & In-App Notifications",
      "feature": "Transient toast banners for action feedback",
      "priority": "P1",
      "userStory": "As a Waves user, I want a brief on-screen banner whenever I change something in the mixer, so that I get immediate confirmation that my action took effect (or didn't) without leaving the window.",
      "expectedBehavior": [
        "showToast(title:detail:kind:duration:) constructs an AppToast and appends it to store.toasts, which the AppToastStack renders (AppStore.swift:1442-1456, AppToasts.swift:6-15).",
        "Nearly every store action emits a success/info/error toast: volume commit (\"Managed route active\", AppStore.swift:433), mute/unmute (\"App muted\"/\"App unmuted\", 528-533), boost (\"Boost updated\", 574-579), pin/unpin (\"Pinned\"/\"Unpinned\", 599-604), output routing (\"Output set\", 662-667), preset apply/save/delete/import/export (1015-1110), device switch (\"Output switched\", 767), diagnostics copy (\"Diagnostics copied\", 778), route recovery (1189-1199), and startup/refresh (220, 249).",
        "Each banner shows the toast.title (bold, 1 line) and optional toast.detail (secondary, up to 2 lines) (AppToasts.swift:32-44).",
        "Banners use a thick Material background, rounded 14pt corners, hairline border, drop shadow, and slide-in-from-top + fade transition (AppToasts.swift:63-77)."
      ],
      "acceptanceCriteria": [
        "Performing any mixer action that calls showToast adds exactly one banner to the stack with the supplied title/detail.",
        "A toast with a nil or empty detail renders only the title row (no empty secondary line).",
        "Detail text longer than two lines is truncated to two lines; title longer than one line is truncated to one.",
        "The banner animates in from the top edge with a spring transition."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:1442-1469",
        "Sources/Waves/Shared/UI/AppToasts.swift:3-95",
        "Sources/Waves/Stores/AppStore.swift:433-438",
        "Sources/Waves/Stores/AppStore.swift:528-533"
      ],
      "implStatus": "implemented",
      "implNotes": "Core feedback path is complete and wired across essentially all store mutations. One UX concern: the success toast for every committed volume change (\"Managed route active\") fires per app on each commit, so dragging multiple sliders quickly produces a rapid burst of low-value success toasts that can crowd out more important messages (see TOAST-04).",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1M 1L]",
      "errorsFound": "• [MEDIUM/correctness] Keyboard volume/mute nudge on an EXCLUDED frontmost app shows a false 'Volume increased/Muted' toast though nothing changed (Sources/Waves/Stores/AppStore.swift:1218-1226) — increaseVolumeForFrontmostApp/decreaseVolumeForFrontmostApp/toggleMuteForFrontmostApp (AppStore.swift:1210-1262) call setDesiredVolume/setMuted and then emit the confirmation toast UNCONDITIONALLY. But setDesiredVolume (AppStore.swift:357) and setMuted (AppStore.swift:492) early-return for excluded apps. There is no isExcluded() guard in the keyboard handlers (verified: no guard between 1212-1226 / 1232-1245 / 1250-1261). So pressing the global volume hotkey while an excluded app is frontmost shows e.g. 'Volume increased — App: 60%' (a value computed locally at 1217/1223 from app.desiredVolume, which never updated) while the backend was never touched. This is the same false-success class as TOAST-07 but via the keyboard path, and it directly violates the TOAST-01 user goal ('confirmation that my action took effect'). Note checkAutoPauseMusic correctly skips excluded apps and emits no toast, so only the manual keyboard path is affected. | Fix: Add `guard !isExcluded(app) else { return }` (or surface a warning toast) at the top of increaseVolumeForFrontmostApp/decreaseVolumeForFrontmostApp/toggleMuteForFrontmostApp before showing the toast, mirroring the guards already in setDesiredVolume/setMuted.\n• [LOW/dead-code] Per-banner .animation(value: toast.id) never fires; insertion is driven only by the stack (Sources/Waves/Shared/UI/AppToasts.swift:77) — AppToastBanner applies .animation(.spring(response: 0.24, dampingFraction: 0.8), value: toast.id) (AppToasts.swift:77). toast.id is a `let id = UUID()` set once at construction (AppStore.swift:16) and never mutates for a live banner, so a value-keyed animation modifier on the same view instance can never trigger. The visible slide-in comes from the .transition (AppToasts.swift:73-76) plus the parent AppToastStack's .animation(value: store.toasts) (AppToasts.swift:14). Re-read confirms the per-banner modifier is unreachable/no-op. Acceptance criterion 'animates in from the top edge with a spring transition' is satisfied by the stack-level animation, so behavior is correct; the per-banner modifier is dead. | Fix: Remove the .animation(..., value: toast.id) from AppToastBanner (line 77) and rely on the stack-level animation, or key on a value that actually changes if a per-banner spring is desired.",
      "fixStatus": "2 fixed",
      "fixNotes": "tier2: FU-EXCLUDED-FRONTMOST-TOASTS | tier3: FU-TOAST-ANIMATION-REDUCEMOTION",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "correctness",
          "title": "Keyboard volume/mute nudge on an EXCLUDED frontmost app shows a false 'Volume increased/Muted' toast though nothing changed",
          "detail": "increaseVolumeForFrontmostApp/decreaseVolumeForFrontmostApp/toggleMuteForFrontmostApp (AppStore.swift:1210-1262) call setDesiredVolume/setMuted and then emit the confirmation toast UNCONDITIONALLY. But setDesiredVolume (AppStore.swift:357) and setMuted (AppStore.swift:492) early-return for excluded apps. There is no isExcluded() guard in the keyboard handlers (verified: no guard between 1212-1226 / 1232-1245 / 1250-1261). So pressing the global volume hotkey while an excluded app is frontmost shows e.g. 'Volume increased — App: 60%' (a value computed locally at 1217/1223 from app.desiredVolume, which never updated) while the backend was never touched. This is the same false-success class as TOAST-07 but via the keyboard path, and it directly violates the TOAST-01 user goal ('confirmation that my action took effect'). Note checkAutoPauseMusic correctly skips excluded apps and emits no toast, so only the manual keyboard path is affected.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1218-1226",
          "expectedVsActual": "Expected: a keyboard nudge on an excluded app is a no-op and either shows no toast or a 'blocked'/'excluded' warning. Actual: setDesiredVolume/setMuted no-op silently but the success/info toast still fires with a fabricated value, falsely confirming an action on an app on the don't-tap escape hatch.",
          "suggestedFix": "Add `guard !isExcluded(app) else { return }` (or surface a warning toast) at the top of increaseVolumeForFrontmostApp/decreaseVolumeForFrontmostApp/toggleMuteForFrontmostApp before showing the toast, mirroring the guards already in setDesiredVolume/setMuted.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier2:partial:FU-EXCLUDED-FRONTMOST-TOASTS",
          "uid": "TOAST-01#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "partial",
          "fixedBy": "FU-EXCLUDED-FRONTMOST-TOASTS",
          "fixTier": "tier2"
        },
        {
          "severity": "low",
          "type": "dead-code",
          "title": "Per-banner .animation(value: toast.id) never fires; insertion is driven only by the stack",
          "detail": "AppToastBanner applies .animation(.spring(response: 0.24, dampingFraction: 0.8), value: toast.id) (AppToasts.swift:77). toast.id is a `let id = UUID()` set once at construction (AppStore.swift:16) and never mutates for a live banner, so a value-keyed animation modifier on the same view instance can never trigger. The visible slide-in comes from the .transition (AppToasts.swift:73-76) plus the parent AppToastStack's .animation(value: store.toasts) (AppToasts.swift:14). Re-read confirms the per-banner modifier is unreachable/no-op. Acceptance criterion 'animates in from the top edge with a spring transition' is satisfied by the stack-level animation, so behavior is correct; the per-banner modifier is dead.",
          "codeRef": "Sources/Waves/Shared/UI/AppToasts.swift:77",
          "expectedVsActual": "Expected: the spring transition animates the banner in. Actual: that is achieved by the stack-level animation; the per-banner .animation(..., value: toast.id) is dead code that implies an animation source that never contributes.",
          "suggestedFix": "Remove the .animation(..., value: toast.id) from AppToastBanner (line 77) and rely on the stack-level animation, or key on a value that actually changes if a per-banner spring is desired.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Shared/UI/AppToasts.swift",
          "disposition": "tier3:applied:FU-TOAST-ANIMATION-REDUCEMOTION",
          "uid": "TOAST-01#2",
          "canonicalFile": "Sources/Waves/Shared/UI/AppToasts.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-TOAST-ANIMATION-REDUCEMOTION",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "TOAST-02",
      "area": "Toasts & In-App Notifications",
      "feature": "Kind-based styling (success / warning / error / info)",
      "priority": "P2",
      "userStory": "As a Waves user, I want toasts to look different depending on whether they report success, a warning, an error, or neutral info, so that I can tell at a glance how urgent a message is.",
      "expectedBehavior": [
        "AppToast.Kind has four cases: success, warning, error, info (AppStore.swift:8-14).",
        "Each kind maps to an SF Symbol: success=checkmark.circle.fill, warning=exclamationmark.triangle.fill, error=xmark.octagon.fill, info=info.circle.fill (AppToasts.swift:97-108).",
        "Each kind maps to an icon color: success=green, warning=orange, error=red, info=WavesDesign.accent (AppToasts.swift:110-121).",
        "The icon sits in a tinted circle (iconColor at 16% opacity) at the leading edge (AppToasts.swift:25-30)."
      ],
      "acceptanceCriteria": [
        "A .error toast renders a red xmark.octagon.fill icon; a .success toast renders a green checkmark.circle.fill icon.",
        "The icon background circle tint matches the icon color at reduced opacity for all four kinds.",
        "Changing the kind passed to showToast changes both the symbol and the color accordingly."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:8-21",
        "Sources/Waves/Shared/UI/AppToasts.swift:97-121",
        "Sources/Waves/Shared/UI/AppToasts.swift:24-30"
      ],
      "implStatus": "implemented",
      "implNotes": "Styling is fully wired and distinct per kind. Color is the only differentiator between success/warning/error beyond the symbol; users with color-vision deficiency still get distinct symbol shapes, which is good. No text-level severity prefix in the accessibility label, so screen-reader users don't hear the kind (e.g. an error vs. success is announced with identical phrasing) — minor a11y gap.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/accessibility] Kind (success/warning/error/info) is conveyed by color + symbol only; never exposed to VoiceOver (Sources/Waves/Shared/UI/AppToasts.swift:90-95) — Re-read confirms toast.kind drives only the SF Symbol (AppToasts.swift:97-108) and the icon color (AppToasts.swift:110-121). accessibilityMessage (AppToasts.swift:90-95) is title + optional detail with no severity word, and .accessibilityElement(children: .combine) (AppToasts.swift:78) absorbs the icon so its symbol carries no accessibility text. A VoiceOver user hears an .error and a .success toast with identical phrasing, defeating the story's 'tell at a glance how urgent a message is'. This matches the doc's own implNotes for TOAST-02 ('No text-level severity prefix in the accessibility label'). | Fix: Prefix accessibilityMessage / the announcement with a kind word for .error/.warning (e.g. 'Error. ' / 'Warning. ') so spoken output encodes severity.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier3: FU-TOAST-SEVERITY-LABEL",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "accessibility",
          "title": "Kind (success/warning/error/info) is conveyed by color + symbol only; never exposed to VoiceOver",
          "detail": "Re-read confirms toast.kind drives only the SF Symbol (AppToasts.swift:97-108) and the icon color (AppToasts.swift:110-121). accessibilityMessage (AppToasts.swift:90-95) is title + optional detail with no severity word, and .accessibilityElement(children: .combine) (AppToasts.swift:78) absorbs the icon so its symbol carries no accessibility text. A VoiceOver user hears an .error and a .success toast with identical phrasing, defeating the story's 'tell at a glance how urgent a message is'. This matches the doc's own implNotes for TOAST-02 ('No text-level severity prefix in the accessibility label').",
          "codeRef": "Sources/Waves/Shared/UI/AppToasts.swift:90-95",
          "expectedVsActual": "Expected (story intent): the user can tell how urgent a message is. Actual: urgency is communicated purely visually; screen-reader users get no severity cue and .error vs .success are announced indistinguishably.",
          "suggestedFix": "Prefix accessibilityMessage / the announcement with a kind word for .error/.warning (e.g. 'Error. ' / 'Warning. ') so spoken output encodes severity.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Shared/UI/AppToasts.swift",
          "disposition": "tier3:applied:FU-TOAST-SEVERITY-LABEL",
          "uid": "TOAST-02#1",
          "canonicalFile": "Sources/Waves/Shared/UI/AppToasts.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-TOAST-SEVERITY-LABEL",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "TOAST-03",
      "area": "Toasts & In-App Notifications",
      "feature": "Auto-dismiss after a per-toast duration",
      "priority": "P1",
      "userStory": "As a Waves user, I want toasts to disappear on their own after a short time, so that confirmation messages don't pile up or block the mixer.",
      "expectedBehavior": [
        "Each toast carries a Duration; showToast defaults to defaultToastDuration = 2.0s when no duration is passed (AppStore.swift:59, 1452).",
        "On creation a per-toast Task sleeps for toast.duration then calls dismissToast(id:) (AppStore.swift:1458-1468).",
        "Callers override the duration for specific messages, e.g. volume commit 1.2s (AppStore.swift:437), mute 1.1s (532), keyboard-shortcut nudges 1.0s (1226), startup failure 3.2s (223), preset export/import 2.0s (1103/1172).",
        "dismissToast cancels the pending dismissal Task and removes the toast from the array (AppStore.swift:1471-1475)."
      ],
      "acceptanceCriteria": [
        "A toast created with the default duration disappears ~2.0s after appearing.",
        "A toast created with an explicit duration disappears after that duration, not the default.",
        "Manually dismissing a toast before its timer fires cancels the timer (no double-removal / no crash).",
        "If the same toast id is somehow shown again, the prior dismissal task is cancelled first (toastDismissals[id]?.cancel() at 1458)."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:1452-1475",
        "Sources/Waves/Stores/AppStore.swift:59",
        "Sources/Waves/Stores/AppStore.swift:223",
        "Sources/Waves/Stores/AppStore.swift:437"
      ],
      "implStatus": "implemented",
      "implNotes": "Timer lifecycle is correct and cancellation-safe. UX concern: error toasts surfacing error.localizedDescription mostly use the 2.0s default (e.g. mute/boost/route/preset failures), which is short for reading a full error string; only startup failure gets 3.2s. Errors are also not visually persistent — they auto-dismiss like any success, so a user who looks away can miss a failure entirely.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [1M 1L]",
      "errorsFound": "• [MEDIUM/ux] Error toasts auto-dismiss on the same short ~2.0s timer as routine successes; failures can vanish before being read (Sources/Waves/Stores/AppStore.swift:1452) — Re-read confirms only 'Startup failed' passes an explicit longer duration (3.2s, AppStore.swift:223). Every other error path falls back to defaultToastDuration = 2.0s (AppStore.swift:59, 1452): 'Refresh failed' (251), 'Volume change failed' (446-450, default), 'Mute toggle failed' (539), 'Boost update failed' (585), \"Couldn't route\" (671), \"Couldn't switch output\" (769), 'Preset apply failed' (1026), 'Pinning failed' (606), 'Login item update failed' (183), and the import/export failures. Many of these surface error.localizedDescription. 2.0s is too short to read a full error string, and error toasts are not visually persistent, so a user who glances away misses the failure entirely. This undermines the TOAST-01/03 'or didn't' feedback guarantee and matches the doc's TOAST-03 implNotes. | Fix: In showToast, branch on kind: give .error (and ideally .warning) a longer default (4-5s) or make them sticky-until-dismissed.\n• [LOW/dead-code] toastDismissals[toast.id]?.cancel() before scheduling is unreachable (ids are always unique) (Sources/Waves/Stores/AppStore.swift:1458) — Re-read confirms showToast constructs a brand-new AppToast each call (AppStore.swift:1448-1453) whose id is a fresh UUID (AppStore.swift:16), then calls toastDismissals[toast.id]?.cancel() at 1458 before inserting the dismissal task. Because the id is guaranteed unique and not yet a key in toastDismissals, the cancel is always a no-op. Acceptance criterion #4 ('if the same toast id is somehow shown again, the prior dismissal task is cancelled first') describes a path that cannot occur given the construction. Harmless defensive code, but dead. | Fix: Drop line 1458 as unreachable, or make the toast id caller-supplied/stable so the guard becomes meaningful, or document it as an intentional defensive no-op.",
      "fixStatus": "2 fixed",
      "fixNotes": "tier3: FU-TOAST-DEAD-CANCEL",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "ux",
          "title": "Error toasts auto-dismiss on the same short ~2.0s timer as routine successes; failures can vanish before being read",
          "detail": "Re-read confirms only 'Startup failed' passes an explicit longer duration (3.2s, AppStore.swift:223). Every other error path falls back to defaultToastDuration = 2.0s (AppStore.swift:59, 1452): 'Refresh failed' (251), 'Volume change failed' (446-450, default), 'Mute toggle failed' (539), 'Boost update failed' (585), \"Couldn't route\" (671), \"Couldn't switch output\" (769), 'Preset apply failed' (1026), 'Pinning failed' (606), 'Login item update failed' (183), and the import/export failures. Many of these surface error.localizedDescription. 2.0s is too short to read a full error string, and error toasts are not visually persistent, so a user who glances away misses the failure entirely. This undermines the TOAST-01/03 'or didn't' feedback guarantee and matches the doc's TOAST-03 implNotes.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1452",
          "expectedVsActual": "Expected: a user reliably learns when an action failed. Actual: error toasts disappear after ~2.0s like routine successes (only startup failure gets extra time), so failures can be missed.",
          "suggestedFix": "In showToast, branch on kind: give .error (and ideally .warning) a longer default (4-5s) or make them sticky-until-dismissed.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "gapfill:fixed",
          "uid": "TOAST-03#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "gapfill",
          "fixTier": "gapfill"
        },
        {
          "severity": "low",
          "type": "dead-code",
          "title": "toastDismissals[toast.id]?.cancel() before scheduling is unreachable (ids are always unique)",
          "detail": "Re-read confirms showToast constructs a brand-new AppToast each call (AppStore.swift:1448-1453) whose id is a fresh UUID (AppStore.swift:16), then calls toastDismissals[toast.id]?.cancel() at 1458 before inserting the dismissal task. Because the id is guaranteed unique and not yet a key in toastDismissals, the cancel is always a no-op. Acceptance criterion #4 ('if the same toast id is somehow shown again, the prior dismissal task is cancelled first') describes a path that cannot occur given the construction. Harmless defensive code, but dead.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1458",
          "expectedVsActual": "Expected: criterion #4 implies a real reuse-protection path. Actual: ids are unconditionally unique, so the guard never has an effect.",
          "suggestedFix": "Drop line 1458 as unreachable, or make the toast id caller-supplied/stable so the guard becomes meaningful, or document it as an intentional defensive no-op.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier3:applied:FU-TOAST-DEAD-CANCEL",
          "uid": "TOAST-03#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-TOAST-DEAD-CANCEL",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "TOAST-04",
      "area": "Toasts & In-App Notifications",
      "feature": "Stack cap of 3 with oldest-first eviction",
      "priority": "P2",
      "userStory": "As a Waves user, I want only a few toasts visible at once so a burst of changes doesn't bury the window, so that the mixer stays usable during rapid edits.",
      "expectedBehavior": [
        "maxToasts = 3; after each append, trimToasts() removes from the front while toasts.count > 3 (AppStore.swift:58, 1456, 1477-1483).",
        "Evicted toasts have their pending dismissal Task cancelled and removed from toastDismissals (AppStore.swift:1479-1481).",
        "The stack renders toasts top-to-bottom in a VStack with 10pt spacing and a spring layout animation keyed on store.toasts (AppToasts.swift:7-14)."
      ],
      "acceptanceCriteria": [
        "Emitting 5 toasts in quick succession leaves exactly 3 banners visible.",
        "The 3 visible banners are the 3 most recent; the 2 oldest are gone.",
        "Evicting a toast does not leave an orphaned dismissal task (no later removal of an id that is already gone).",
        "The stack width is capped (maxWidth 420 in stack / 360-ish at mount sites) and aligned to the top."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:1477-1483",
        "Sources/Waves/Stores/AppStore.swift:58",
        "Sources/Waves/Shared/UI/AppToasts.swift:6-15",
        "Sources/Waves/Stores/AppStore.swift:1455-1456"
      ],
      "implStatus": "partial",
      "implNotes": "Cap and timer cleanup are correct, but eviction is purely chronological (removeFirst) with no priority for kind. A burst of success toasts (e.g. committing volume across several apps, or auto-pause/auto-resume cycles) can silently evict a pending .error toast before the user reads it. Since errors and successes share the same queue and the same short auto-dismiss, important failures can be pushed out by routine confirmations. Worth considering: never evict an error in favor of a success, or give errors a longer/sticky lifetime.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1M]",
      "errorsFound": "• [MEDIUM/logic] Oldest-first eviction is kind-blind: a pending .error can be evicted by routine .success/.info toasts (Sources/Waves/Stores/AppStore.swift:1478-1479) — Re-read confirms trimToasts removes strictly from the front while count > maxToasts(3), ignoring kind (AppStore.swift:1477-1483). Real burst sources verified: per-app volume commits each emit 'Managed route active' (433-438) on every commitDesiredVolume, and rapid keyboard nudges emit 'Volume increased/decreased' (1221/1240) — three of these in quick succession push an earlier .error (e.g. 'Volume change failed', 446) out of the 3-slot stack before its ~2s timer fires and before the user reads it. (Note: applyPreset emits only ONE 'Preset applied' toast at 1016, not a burst — so the first tester's mention of preset apply as a burst source is inaccurate, but the volume-commit and keyboard-nudge sources are correct and sufficient.) Errors and successes share one queue with the same short lifetime, so important failures are silently displaced. Matches the doc's TOAST-04 implNotes (implStatus 'partial'). | Fix: Make trimToasts prefer evicting non-error toasts: when over cap, remove the oldest toast whose kind != .error first; evict an error only if no lower-priority toast remains. Optionally give errors a longer/sticky lifetime.",
      "fixStatus": "1 fixed",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "logic",
          "title": "Oldest-first eviction is kind-blind: a pending .error can be evicted by routine .success/.info toasts",
          "detail": "Re-read confirms trimToasts removes strictly from the front while count > maxToasts(3), ignoring kind (AppStore.swift:1477-1483). Real burst sources verified: per-app volume commits each emit 'Managed route active' (433-438) on every commitDesiredVolume, and rapid keyboard nudges emit 'Volume increased/decreased' (1221/1240) — three of these in quick succession push an earlier .error (e.g. 'Volume change failed', 446) out of the 3-slot stack before its ~2s timer fires and before the user reads it. (Note: applyPreset emits only ONE 'Preset applied' toast at 1016, not a burst — so the first tester's mention of preset apply as a burst source is inaccurate, but the volume-commit and keyboard-nudge sources are correct and sufficient.) Errors and successes share one queue with the same short lifetime, so important failures are silently displaced. Matches the doc's TOAST-04 implNotes (implStatus 'partial').",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:1478-1479",
          "expectedVsActual": "Expected: the mixer stays usable under bursts AND failures are not lost. Actual: the cap is enforced correctly, but eviction never prioritizes errors, so a routine success/info can evict an unread error.",
          "suggestedFix": "Make trimToasts prefer evicting non-error toasts: when over cap, remove the oldest toast whose kind != .error first; evict an error only if no lower-priority toast remains. Optionally give errors a longer/sticky lifetime.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "gapfill:fixed",
          "uid": "TOAST-04#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "gapfill",
          "fixTier": "gapfill"
        }
      ]
    },
    {
      "id": "TOAST-05",
      "area": "Toasts & In-App Notifications",
      "feature": "Manual dismissal (close button + accessibility action)",
      "priority": "P2",
      "userStory": "As a Waves user, I want to dismiss a toast immediately, so that I can clear a message I've already read without waiting for it to time out.",
      "expectedBehavior": [
        "Each banner has an xmark button that calls store.dismissToast(id: toast.id) (AppToasts.swift:48-58).",
        "The button has help text \"Dismiss\" and accessibilityLabel \"Dismiss notification\" (AppToasts.swift:57-58).",
        "Because the banner combines its children for accessibility, dismissal is also exposed as a named VoiceOver custom action \"Dismiss\" (AppToasts.swift:81-83)."
      ],
      "acceptanceCriteria": [
        "Clicking the xmark removes that specific toast and cancels its auto-dismiss timer.",
        "The VoiceOver \"Dismiss\" custom action removes the focused toast.",
        "Dismissing one toast leaves the others in the stack untouched and correctly re-laid-out.",
        "Removal uses an opacity transition (asymmetric removal) rather than the slide-in used for insertion."
      ],
      "codeRefs": [
        "Sources/Waves/Shared/UI/AppToasts.swift:48-58",
        "Sources/Waves/Shared/UI/AppToasts.swift:78-83",
        "Sources/Waves/Stores/AppStore.swift:1471-1475"
      ],
      "implStatus": "implemented",
      "implNotes": "Close button and VoiceOver action both route to dismissToast and work. Minor UX gap: contentShape(Rectangle()) is set on the whole banner (AppToasts.swift:72) implying tap-to-dismiss was intended, but no tap gesture is attached to the body — only the small xmark is interactive, which is a tiny hit target. Tapping the banner body does nothing.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/ux] contentShape(Rectangle()) on the banner implies tap-to-dismiss but no tap gesture is attached (Sources/Waves/Shared/UI/AppToasts.swift:72) — Re-read confirms the banner sets .contentShape(Rectangle()) (AppToasts.swift:72) but no .onTapGesture / gesture is attached to the banner body (checked the full AppToastBanner and AppToastStack). Only the xmark Button (AppToasts.swift:48-58, padding 4) is interactive. So tapping the banner body does nothing and the contentShape is a no-op for dismissal; the only manual-dismiss hit target is the tiny xmark glyph, small for a transient element. Matches the doc's TOAST-05 implNotes. | Fix: Either remove the orphan .contentShape(Rectangle()), or add .onTapGesture { store.dismissToast(id: toast.id) } to the banner body to make tap-anywhere-to-dismiss real and enlarge the hit target.",
      "fixStatus": "1 deferred",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "ux",
          "title": "contentShape(Rectangle()) on the banner implies tap-to-dismiss but no tap gesture is attached",
          "detail": "Re-read confirms the banner sets .contentShape(Rectangle()) (AppToasts.swift:72) but no .onTapGesture / gesture is attached to the banner body (checked the full AppToastBanner and AppToastStack). Only the xmark Button (AppToasts.swift:48-58, padding 4) is interactive. So tapping the banner body does nothing and the contentShape is a no-op for dismissal; the only manual-dismiss hit target is the tiny xmark glyph, small for a transient element. Matches the doc's TOAST-05 implNotes.",
          "codeRef": "Sources/Waves/Shared/UI/AppToasts.swift:72",
          "expectedVsActual": "Expected: contentShape(Rectangle()) paired with a body gesture making the whole banner interactive. Actual: no body gesture exists, so the modifier is dead and dismissal is only via the small xmark.",
          "suggestedFix": "Either remove the orphan .contentShape(Rectangle()), or add .onTapGesture { store.dismissToast(id: toast.id) } to the banner body to make tap-anywhere-to-dismiss real and enlarge the hit target.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Shared/UI/AppToasts.swift",
          "disposition": "deferred:needs-user-decision:Genuine UX fork (remove contentShape vs. add tap-anywhere-to-dismiss); contentShape also interacts with the combined a11y element, so remova",
          "uid": "TOAST-05#1",
          "canonicalFile": "Sources/Waves/Shared/UI/AppToasts.swift"
        }
      ]
    },
    {
      "id": "TOAST-06",
      "area": "Toasts & In-App Notifications",
      "feature": "VoiceOver announcement of transient toasts",
      "priority": "P2",
      "userStory": "As a VoiceOver user, I want toasts to be spoken when they appear, so that I get the same audio/visual feedback sighted users get even though banners are transient.",
      "expectedBehavior": [
        "On banner onAppear, an AccessibilityNotification.Announcement is posted with the combined message (AppToasts.swift:84-87).",
        "accessibilityMessage joins title and detail as \"<title>. <detail>\" when detail is non-empty, otherwise just the title (AppToasts.swift:90-95).",
        "The banner is an accessibility element with children combined and the same message as its label (AppToasts.swift:78-79)."
      ],
      "acceptanceCriteria": [
        "When a toast appears, VoiceOver announces its title (and detail, if present).",
        "A toast with no detail announces only the title.",
        "The combined banner exposes a single accessibility element labeled with the toast message, not its individual subviews."
      ],
      "codeRefs": [
        "Sources/Waves/Shared/UI/AppToasts.swift:84-95",
        "Sources/Waves/Shared/UI/AppToasts.swift:78-79"
      ],
      "implStatus": "partial",
      "implNotes": "Announcement is correctly posted because VoiceOver does not auto-announce transient banners (comment at AppToasts.swift:85). Gaps: (1) the announcement has no priority/severity, so an error and a success are announced identically; (2) up to 3 toasts can appear near-simultaneously (e.g. auto-pause muting several music apps, or a preset apply), and each onAppear posts its own Announcement — later announcements can interrupt earlier ones, so a VoiceOver user may not hear all of them; (3) eviction (TOAST-04) can remove a toast before its announcement is fully spoken.",
      "source": "phase1-story",
      "testStatus": "2 issue(s) [2M]",
      "errorsFound": "• [MEDIUM/accessibility] Simultaneous toast announcements have no priority and can interrupt/replace each other (Sources/Waves/Shared/UI/AppToasts.swift:86) — Re-read confirms each banner onAppear fires AccessibilityNotification.Announcement(accessibilityMessage).post() (AppToasts.swift:84-87) with no priority attribute. When multiple toasts insert near-simultaneously, each posts its own unprioritized announcement and VoiceOver coalesces/interrupts them, so earlier ones (which may be an error) can be cut off. Combined with TOAST-04 eviction, a toast can be removed before its announcement completes. CORRECTION to the first tester: the cited 'auto-pause muting several music apps' is NOT a real burst source — checkAutoPauseMusic (AppStore.swift:940-998) mutes/unmutes silently and emits NO toast. Likewise applyPreset emits only one toast. The genuine near-simultaneous sources are per-app volume commits ('Managed route active', 433) and rapid keyboard nudges (1221/1240). The criterion 'each appearing toast is announced' is not reliably met under those bursts. | Fix: Attach an announcement priority for higher-severity kinds (AttributedString with .accessibilitySpeechAnnouncementPriority(.high) for .error/.warning) and/or serialize announcements so they queue rather than interrupt; ensure a toast isn't evicted before its announcement is delivered.\n• [MEDIUM/accessibility] Each toast is announced (and rendered) twice when the main window and menu-bar panel are both open (Sources/Waves/Features/Mixer/MenuBarMixerView.swift:105) — AppToastStack is mounted in BOTH MainWindowView (line 59) and MenuBarMixerView (line 105), and both read the same shared store.toasts via @Environment(AppStore.self). When both surfaces are on screen at once (the menu-bar popover can be open while the main window is visible), a single showToast appends one AppToast that renders in TWO stacks. Each stack's AppToastBanner.onAppear independently posts AccessibilityNotification.Announcement (AppToasts.swift:84-87), so a VoiceOver user hears the same message announced twice; sighted users see the banner duplicated in both surfaces. This is a menu-bar/main-window state-desync the first tester missed. (Auto-dismiss/eviction stay consistent because there is a single source-of-truth array, so it is purely a duplicate-presentation/double-speak issue, not a crash.) | Fix: Post the VoiceOver announcement from a single owner (e.g. in showToast itself, once, rather than per-banner onAppear), and/or render only one AppToastStack for the key window / gate the menu-bar stack so toasts aren't duplicated across simultaneously-visible surfaces.",
      "fixStatus": "1 fixed · 1 deferred",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "accessibility",
          "title": "Simultaneous toast announcements have no priority and can interrupt/replace each other",
          "detail": "Re-read confirms each banner onAppear fires AccessibilityNotification.Announcement(accessibilityMessage).post() (AppToasts.swift:84-87) with no priority attribute. When multiple toasts insert near-simultaneously, each posts its own unprioritized announcement and VoiceOver coalesces/interrupts them, so earlier ones (which may be an error) can be cut off. Combined with TOAST-04 eviction, a toast can be removed before its announcement completes. CORRECTION to the first tester: the cited 'auto-pause muting several music apps' is NOT a real burst source — checkAutoPauseMusic (AppStore.swift:940-998) mutes/unmutes silently and emits NO toast. Likewise applyPreset emits only one toast. The genuine near-simultaneous sources are per-app volume commits ('Managed route active', 433) and rapid keyboard nudges (1221/1240). The criterion 'each appearing toast is announced' is not reliably met under those bursts.",
          "codeRef": "Sources/Waves/Shared/UI/AppToasts.swift:86",
          "expectedVsActual": "Expected: each appearing toast is announced. Actual: with several toasts appearing together (rapid volume commits / keyboard nudges), later unprioritized announcements interrupt earlier ones, so a VoiceOver user may miss some (notably errors).",
          "suggestedFix": "Attach an announcement priority for higher-severity kinds (AttributedString with .accessibilitySpeechAnnouncementPriority(.high) for .error/.warning) and/or serialize announcements so they queue rather than interrupt; ensure a toast isn't evicted before its announcement is delivered.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Shared/UI/AppToasts.swift",
          "disposition": "iter3:implemented",
          "uid": "TOAST-06#1",
          "canonicalFile": "Sources/Waves/Shared/UI/AppToasts.swift",
          "fixStatus": "applied",
          "fixedBy": "iter3-ui-parity",
          "fixTier": "iter3"
        },
        {
          "severity": "medium",
          "type": "accessibility",
          "title": "Each toast is announced (and rendered) twice when the main window and menu-bar panel are both open",
          "detail": "AppToastStack is mounted in BOTH MainWindowView (line 59) and MenuBarMixerView (line 105), and both read the same shared store.toasts via @Environment(AppStore.self). When both surfaces are on screen at once (the menu-bar popover can be open while the main window is visible), a single showToast appends one AppToast that renders in TWO stacks. Each stack's AppToastBanner.onAppear independently posts AccessibilityNotification.Announcement (AppToasts.swift:84-87), so a VoiceOver user hears the same message announced twice; sighted users see the banner duplicated in both surfaces. This is a menu-bar/main-window state-desync the first tester missed. (Auto-dismiss/eviction stay consistent because there is a single source-of-truth array, so it is purely a duplicate-presentation/double-speak issue, not a crash.)",
          "codeRef": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:105",
          "expectedVsActual": "Expected: a toast appears (and is announced) once. Actual: with both surfaces visible the toast renders in both stacks and onAppear posts the announcement twice, producing duplicate VoiceOver speech and duplicate visible banners.",
          "suggestedFix": "Post the VoiceOver announcement from a single owner (e.g. in showToast itself, once, rather than per-banner onAppear), and/or render only one AppToastStack for the key window / gate the menu-bar stack so toasts aren't duplicated across simultaneously-visible surfaces.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift",
          "disposition": "deferred:needs-user-decision:Fix spans AppStore + AppToasts (out of allowed file) and requires a toast-ownership design decision; not a localized MenuBarMixerView edit.",
          "uid": "TOAST-06#2",
          "canonicalFile": "Sources/Waves/Features/Mixer/MenuBarMixerView.swift"
        }
      ]
    },
    {
      "id": "TOAST-07",
      "area": "Toasts & In-App Notifications",
      "feature": "Toasts as the result channel for URL-scheme automation commands",
      "priority": "P3",
      "userStory": "As a power user driving Waves via waves:// URLs, I want a toast when a command is rejected or malformed, so that I can tell whether my automation actually did anything.",
      "expectedBehavior": [
        "Invalid set-volume, mute, and apply-preset commands surface a .warning toast titled \"URL command blocked\" with a reason (AppStore.swift:299,304,318,323,333,338).",
        "Unknown app/preset names produce \"URL command blocked\" with the (truncated) identifier (AppStore.swift:304,338).",
        "Valid commands fall through to the normal action handlers, which then emit their own success/error toasts (e.g. set-volume -> commit -> \"Managed route active\").",
        "Note: when URL schemes are disabled or the rate limit is exceeded, the request is only logged (logger.warning) and produces NO toast (AppStore.swift:256-260, 270-273)."
      ],
      "acceptanceCriteria": [
        "A waves://set-volume with a missing/out-of-range volume shows a \"URL command blocked\" warning toast and changes nothing.",
        "A waves://mute targeting a non-running app shows \"App not found: <id>\" as a warning toast.",
        "A valid waves://apply-preset?name=... shows the normal \"Preset applied\" success toast.",
        "A command received while URL schemes are disabled or rate-limited produces no toast (silent, log-only)."
      ],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:291-343",
        "Sources/Waves/Stores/AppStore.swift:256-289",
        "Sources/Waves/Stores/AppStore.swift:345-354"
      ],
      "implStatus": "partial",
      "implNotes": "Malformed/unknown-target commands give clear warning toasts, which is good. The gap is the silent paths: a command dropped because URL schemes are disabled or because the per-minute rate limit (10/60s) is exceeded gives the user zero in-app feedback — only an os_log entry the user won't see. From the automation author's perspective these failures are indistinguishable from success. A muted/info toast on rate-limit or disabled-scheme rejection would close the loop.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1M]",
      "errorsFound": "• [MEDIUM/correctness] URL set-volume on an excluded app emits a misleading 'Managed route active' success toast (Sources/Waves/Stores/AppStore.swift:308-309) — Re-read confirms handleURLSetVolume (AppStore.swift:291-310) calls setDesiredVolume(volume, for: app) then commitDesiredVolume(for: app) with no isExcluded check. setDesiredVolume guards on exclusion and returns early (357), BUT commitDesiredVolume (384-393) and scheduleVolumeApply (395-462) have NO exclusion guard (verified: the only isExcluded guards are at 357/492/545/650/826). So for an excluded app, commitDesiredVolume still seeds pendingVolumeTargets[appKey] = app.desiredVolume (the unchanged value, since setDesiredVolume no-op'd), calls backend.setDesiredVolume on success, and emits the 'Managed route active' success toast (433-438). The automation author gets a false success on an app that is on the don't-tap escape hatch, and the backend is asked to act on it. | Fix: Guard handleURLSetVolume on isExcluded(app) (emit a warning toast and return), or add an isExcluded early-return to commitDesiredVolume/scheduleVolumeApply so excluded apps never commit, never call the backend, and never toast.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier2: FU-URL-EXCLUDED-COMMIT",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "correctness",
          "title": "URL set-volume on an excluded app emits a misleading 'Managed route active' success toast",
          "detail": "Re-read confirms handleURLSetVolume (AppStore.swift:291-310) calls setDesiredVolume(volume, for: app) then commitDesiredVolume(for: app) with no isExcluded check. setDesiredVolume guards on exclusion and returns early (357), BUT commitDesiredVolume (384-393) and scheduleVolumeApply (395-462) have NO exclusion guard (verified: the only isExcluded guards are at 357/492/545/650/826). So for an excluded app, commitDesiredVolume still seeds pendingVolumeTargets[appKey] = app.desiredVolume (the unchanged value, since setDesiredVolume no-op'd), calls backend.setDesiredVolume on success, and emits the 'Managed route active' success toast (433-438). The automation author gets a false success on an app that is on the don't-tap escape hatch, and the backend is asked to act on it.",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:308-309",
          "expectedVsActual": "Expected: a URL set-volume targeting an excluded app is a no-op (ideally a 'URL command blocked'/excluded warning toast), never a success. Actual: setDesiredVolume no-ops but commitDesiredVolume runs and surfaces 'Managed route active', falsely confirming an action on an excluded app.",
          "suggestedFix": "Guard handleURLSetVolume on isExcluded(app) (emit a warning toast and return), or add an isExcluded early-return to commitDesiredVolume/scheduleVolumeApply so excluded apps never commit, never call the backend, and never toast.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "tier2:applied:FU-URL-EXCLUDED-COMMIT",
          "uid": "TOAST-07#1",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-URL-EXCLUDED-COMMIT",
          "fixTier": "tier2"
        }
      ]
    },
    {
      "id": "A11Y-01",
      "area": "Accessibility (VoiceOver, contrast, reduced motion, label scaling)",
      "feature": "VoiceOver labels, values, and hints on controls",
      "priority": "P0",
      "userStory": "As a VoiceOver user, I want every mixer control and icon-only button to announce a clear label, current value, and hint, so that I can operate the per-app mixer without seeing the screen.",
      "expectedBehavior": [
        "Each per-app volume slider announces 'Volume for <app>', a value of '<n>%', and the hint 'Adjusts the per-app volume target' (full and compact rows).",
        "The mute button announces 'Mute <app>' or 'Unmute <app>' depending on state; compact row adds a hint ('Silences this app' / 'Restores audio for this app').",
        "The boost control announces 'Boost for <app>' with value '<n>x'.",
        "Toolbar icon buttons (Save preset / Refresh app list / Recover managed routes) each expose an accessibilityLabel plus a descriptive hint.",
        "Menu-bar icon buttons announce labels: 'Refresh app list', 'Open Waves main window', 'Open Settings', 'Launch at login', and the output-device menu announces label 'Output device' with the current device name as its value.",
        "Decorative elements (app icon, % readout text, level-meter capsule, status dot circle) are marked accessibilityHidden so VoiceOver does not read redundant or non-actionable content.",
        "Pinned pin glyph and 'Excluded' chip carry labels ('Pinned', 'Excluded from Waves').",
        "Route-health badge and diagnostics check rows expose combined labels including their status word (e.g. 'Passed: <title>')."
      ],
      "acceptanceCriteria": [
        "With VoiceOver on, focusing any volume slider speaks an app-specific label and a percentage value.",
        "Focusing the mute button speaks Mute/Unmute matching the app's current muted state.",
        "Every icon-only button in the toolbar and menu bar is reachable and named (no 'button' with empty name).",
        "The app icon, numeric % label, and meter are skipped by VoiceOver navigation.",
        "The output-device picker speaks 'Output device' and the active device name."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MixerRowView.swift:75",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:105",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:302",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:322",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:385",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:91",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:552",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:34",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:490",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:546",
        "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:153",
        "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:24"
      ],
      "implStatus": "implemented",
      "implNotes": "Coverage is thorough and consistent: icon-only controls all have labels, and decorative content is correctly hidden. Minor gap: the full MixerRowView mute button has no hint (the compact row does), and the PresetQuickPicker menu-bar label ('Presets') has no custom a11y label but is text-based so still readable. Boost is a Menu so its accessibilityValue may not be announced as an adjustable value — it reads as a popup, which is acceptable.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/correctness] Volume percentage truncates instead of rounds, so VoiceOver value and the visible % readout can read one point low (Sources/Waves/Features/Mixer/MixerRowView.swift:76) — Both the full and compact slider expose accessibilityValue as \"\\(Int(app.desiredVolume * 100))%\" (lines 76 and 303), and the full row also renders a visible Text(\"\\(Int(app.desiredVolume * 100))%\") at line 83. desiredVolume is a Float that accumulates rounding error from 0.05 steps, so values like 0.39999998 or 0.79999995 stringify to '39%' / '79%' instead of '40%' / '80%'. I reproduced this in Swift: after stepping, Int(0.39999998*100)=39 while .rounded() gives 40. The defect is genuine but intermittent (only non-exact accumulated values are affected; clean values like 0.95, 0.90 read correctly), so it is low severity, not medium. The visible label is affected the same way the VoiceOver value is. | Fix: Use Int((app.desiredVolume * 100).rounded()) for the accessibilityValue at lines 76 and 303 and the visible label at line 83.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier3: FU-PCT-ROUNDING",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "correctness",
          "title": "Volume percentage truncates instead of rounds, so VoiceOver value and the visible % readout can read one point low",
          "detail": "Both the full and compact slider expose accessibilityValue as \"\\(Int(app.desiredVolume * 100))%\" (lines 76 and 303), and the full row also renders a visible Text(\"\\(Int(app.desiredVolume * 100))%\") at line 83. desiredVolume is a Float that accumulates rounding error from 0.05 steps, so values like 0.39999998 or 0.79999995 stringify to '39%' / '79%' instead of '40%' / '80%'. I reproduced this in Swift: after stepping, Int(0.39999998*100)=39 while .rounded() gives 40. The defect is genuine but intermittent (only non-exact accumulated values are affected; clean values like 0.95, 0.90 read correctly), so it is low severity, not medium. The visible label is affected the same way the VoiceOver value is.",
          "codeRef": "Sources/Waves/Features/Mixer/MixerRowView.swift:76",
          "expectedVsActual": "Expected: VoiceOver speaks (and the label shows) the slider's actual percentage, e.g. 40%. Actual: truncation of an accumulated 0.39999998 yields 39%, one point low, for many post-step values.",
          "suggestedFix": "Use Int((app.desiredVolume * 100).rounded()) for the accessibilityValue at lines 76 and 303 and the visible label at line 83.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "disposition": "tier3:applied:FU-PCT-ROUNDING",
          "uid": "A11Y-01#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-PCT-ROUNDING",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "A11Y-02",
      "area": "Accessibility (VoiceOver, contrast, reduced motion, label scaling)",
      "feature": "Adjustable sliders via VoiceOver increment/decrement",
      "priority": "P0",
      "userStory": "As a VoiceOver user, I want to change an app's volume with the up/down rotor gestures, so that I can set levels precisely without dragging a slider I cannot see.",
      "expectedBehavior": [
        "Each volume slider declares an accessibilityAdjustableAction.",
        "Increment raises desiredVolume by 0.05 clamped to a max of 1.0; decrement lowers it by 0.05 clamped to a min of 0.0.",
        "Each adjustment calls store.setDesiredVolume and immediately store.commitDesiredVolume so the change is applied and persisted, not left pending.",
        "Unknown adjustment directions are ignored (no crash, no change).",
        "Both the full MixerRowView and the CompactMixerRow implement identical 5% step behavior."
      ],
      "acceptanceCriteria": [
        "Swiping up on a focused slider increases the announced percentage by 5 points until 100%.",
        "Swiping down decreases it by 5 points until 0%.",
        "After an adjust gesture the value is committed (survives a refresh / is reflected in the backend), not just visually nudged.",
        "Adjusting an excluded app's slider has no effect because the slider is disabled."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MixerRowView.swift:78",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:223",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:305",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:343"
      ],
      "implStatus": "implemented",
      "implNotes": "Clean and correct: 5% steps, clamped, committed on each step. The disabled() on excluded rows correctly blocks the adjustable action too. Step size (5%) matches the keyboard nudge in MainWindowView, so behavior is consistent across input methods.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/logic] VoiceOver decrement announces occasional off-by-one percentages because the percent is truncated, not rounded (Sources/Waves/Features/Mixer/MixerRowView.swift:303) — adjustVolume() steps desiredVolume by exactly 0.05 and clamps correctly (the step logic is right). The defect is in displaying the value: Int(desiredVolume*100) truncates. I verified the actual Float32 sequences in Swift and the first tester's reproduction is partly INACCURATE. Decrement from 0.5 announces 50, 45, 39, 34, 29, 24 (NOT '50 -> 44 -> 39' — the first step is a clean 45, then 39 appears where 40 is expected). Decrement from 1.0 announces 100, 95, 90, 85, 79 (NOT '100 -> 94 -> 89 -> 84' — the first four are clean, the error first shows at 79 instead of 80). So the bug is real but it is an occasional 1-point-low announcement, not a systematic 6-point-then-1-low pattern, and not every step is wrong. Same root cause as A11Y-01; low severity. | Fix: Round in the integer conversion: Int((app.desiredVolume * 100).rounded()) at lines 76, 83, and 303. Optionally quantize desiredVolume in adjustVolume() (e.g. ((value*20).rounded())/20) to keep the stored value on exact 5% marks.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier3: FU-PCT-ROUNDING",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "logic",
          "title": "VoiceOver decrement announces occasional off-by-one percentages because the percent is truncated, not rounded",
          "detail": "adjustVolume() steps desiredVolume by exactly 0.05 and clamps correctly (the step logic is right). The defect is in displaying the value: Int(desiredVolume*100) truncates. I verified the actual Float32 sequences in Swift and the first tester's reproduction is partly INACCURATE. Decrement from 0.5 announces 50, 45, 39, 34, 29, 24 (NOT '50 -> 44 -> 39' — the first step is a clean 45, then 39 appears where 40 is expected). Decrement from 1.0 announces 100, 95, 90, 85, 79 (NOT '100 -> 94 -> 89 -> 84' — the first four are clean, the error first shows at 79 instead of 80). So the bug is real but it is an occasional 1-point-low announcement, not a systematic 6-point-then-1-low pattern, and not every step is wrong. Same root cause as A11Y-01; low severity.",
          "codeRef": "Sources/Waves/Features/Mixer/MixerRowView.swift:303",
          "expectedVsActual": "Expected: each down-swipe announces a clean 5-point decrement (50,45,40,35... / 100,95,90,85,80). Actual: most steps are clean but truncation makes some announce one point low (40->39, 80->79).",
          "suggestedFix": "Round in the integer conversion: Int((app.desiredVolume * 100).rounded()) at lines 76, 83, and 303. Optionally quantize desiredVolume in adjustVolume() (e.g. ((value*20).rounded())/20) to keep the stored value on exact 5% marks.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "disposition": "tier3:applied:FU-PCT-ROUNDING",
          "uid": "A11Y-02#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-PCT-ROUNDING",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "A11Y-03",
      "area": "Accessibility (VoiceOver, contrast, reduced motion, label scaling)",
      "feature": "Custom VoiceOver actions for combined/hidden controls",
      "priority": "P1",
      "userStory": "As a VoiceOver user, I want pin, exclude, and toast-dismiss available as VoiceOver actions, so that I can reach controls that live in context menus or are absorbed by element grouping.",
      "expectedBehavior": [
        "Each mixer row exposes accessibilityAction 'Pin'/'Unpin' (state-dependent) that toggles pinned state via store.togglePinned.",
        "Each mixer row exposes accessibilityAction 'Exclude from Waves'/'Manage with Waves' (state-dependent) that calls store.setExcluded.",
        "Toast banners combine children into one element and re-expose the dismiss control as an accessibilityAction named 'Dismiss' (because .combine absorbs the dismiss button), wired to store.dismissToast.",
        "These actions mirror the same operations available via the row context menu, so mouse and VoiceOver users have parity."
      ],
      "acceptanceCriteria": [
        "With VoiceOver on a mixer row, the actions rotor lists Pin/Unpin and Exclude/Manage, and invoking them changes the app's state.",
        "On a toast, the actions rotor lists 'Dismiss' and invoking it removes the toast.",
        "Action names update to reflect current state (e.g. shows 'Unpin' once an app is pinned)."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MixerRowView.swift:163",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:166",
        "Sources/Waves/Shared/UI/AppToasts.swift:78",
        "Sources/Waves/Shared/UI/AppToasts.swift:81"
      ],
      "implStatus": "implemented",
      "implNotes": "Good pattern — the comment in AppToasts explicitly notes that .combine absorbs the dismiss button, so the action is the correct workaround. The row's Output Device submenu from the context menu is NOT re-exposed as a VoiceOver action, so device routing per app is unreachable via VoiceOver custom actions (only pin/exclude are). That is a real parity gap worth noting.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/inconsistency] Per-app Output Device submenu has no VoiceOver custom-action parity (Sources/Waves/Features/Mixer/MixerRowView.swift:140) — Verified: the row context menu (lines 135-162) offers Pin/Unpin, an Output Device submenu, and Exclude/Manage, but only Pin/Unpin and Exclude/Manage are re-exposed via .accessibilityAction (lines 163-168). The Output Device submenu is not exposed as a VoiceOver action, so a VoiceOver user relying on the actions rotor cannot reach per-app output routing — a flagship feature that mouse users get from the same menu. The story's enumerated acceptance criteria only list pin/exclude/dismiss (all satisfied), so this is a parity gap against the story's stated intent rather than a hard criterion failure; the story's own implNotes already calls it out. Low severity. | Fix: Add accessibilityAction(s) for output routing, e.g. a 'Use System Default Output' action plus per-device actions, mirroring the context-menu submenu.",
      "fixStatus": "1 deferred",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "inconsistency",
          "title": "Per-app Output Device submenu has no VoiceOver custom-action parity",
          "detail": "Verified: the row context menu (lines 135-162) offers Pin/Unpin, an Output Device submenu, and Exclude/Manage, but only Pin/Unpin and Exclude/Manage are re-exposed via .accessibilityAction (lines 163-168). The Output Device submenu is not exposed as a VoiceOver action, so a VoiceOver user relying on the actions rotor cannot reach per-app output routing — a flagship feature that mouse users get from the same menu. The story's enumerated acceptance criteria only list pin/exclude/dismiss (all satisfied), so this is a parity gap against the story's stated intent rather than a hard criterion failure; the story's own implNotes already calls it out. Low severity.",
          "codeRef": "Sources/Waves/Features/Mixer/MixerRowView.swift:140",
          "expectedVsActual": "Expected (per story intent of mouse/VoiceOver parity): context-menu controls reachable as VoiceOver actions. Actual: device-routing submenu is mouse-only; only pin/exclude are exposed as actions.",
          "suggestedFix": "Add accessibilityAction(s) for output routing, e.g. a 'Use System Default Output' action plus per-device actions, mirroring the context-menu submenu.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "disposition": "deferred:needs-user-decision:Flat-action enumeration of a dynamic device list needs a deliberate UX design decision, not an obvious one-liner.",
          "uid": "A11Y-03#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MixerRowView.swift"
        }
      ]
    },
    {
      "id": "A11Y-04",
      "area": "Accessibility (VoiceOver, contrast, reduced motion, label scaling)",
      "feature": "VoiceOver rotors for playing apps and apps needing attention",
      "priority": "P2",
      "userStory": "As a VoiceOver user, I want rotors that jump straight to apps currently playing audio or apps in an error state, so that I can navigate a long app list efficiently.",
      "expectedBehavior": [
        "The main window source list declares an accessibilityRotor 'Playing apps' populated from the apps that are also in store.liveApps.",
        "It declares a second accessibilityRotor 'Needs attention' populated from apps whose routingState == .error.",
        "Each rotor entry uses the app's displayName and the app id, so selecting an entry moves focus to that row.",
        "Rotors are scoped to the currently filtered/visible apps list."
      ],
      "acceptanceCriteria": [
        "With VoiceOver, the rotor control lists a 'Playing apps' category containing only live apps.",
        "A 'Needs attention' category lists only apps with a failed route.",
        "Selecting a rotor entry moves VoiceOver focus to the corresponding mixer row.",
        "When no apps match (none playing / none errored) the rotor category is empty rather than incorrect."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MainWindowView.swift:388",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:393"
      ],
      "implStatus": "implemented",
      "implNotes": "Wired and scoped correctly to the visible list. Rotors are only present in the main window's non-empty list branch (not in the menu-bar panel), which is reasonable. Empty rotors are valid SwiftUI. No correctness concern; matches the README claim of 'VoiceOver rotors'.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "A11Y-05",
      "area": "Accessibility (VoiceOver, contrast, reduced motion, label scaling)",
      "feature": "VoiceOver announcements for transient toast banners",
      "priority": "P1",
      "userStory": "As a VoiceOver user, I want status toasts to be spoken when they appear, so that I learn about route changes, errors, and confirmations that I would otherwise miss because banners are transient.",
      "expectedBehavior": [
        "When a toast banner appears, it posts an AccessibilityNotification.Announcement with the toast's accessible message.",
        "The announced message is the title, or 'title. detail' when a non-empty detail is present.",
        "The same combined message is used as the banner's accessibilityLabel for users who navigate to it manually."
      ],
      "acceptanceCriteria": [
        "With VoiceOver on, triggering an action that raises a toast causes the toast text to be spoken without the user navigating to it.",
        "A toast with detail speaks both title and detail; a title-only toast speaks just the title.",
        "Navigating to the banner element reads the same combined message."
      ],
      "codeRefs": [
        "Sources/Waves/Shared/UI/AppToasts.swift:84",
        "Sources/Waves/Shared/UI/AppToasts.swift:86",
        "Sources/Waves/Shared/UI/AppToasts.swift:90"
      ],
      "implStatus": "implemented",
      "implNotes": "Correctly addresses the known issue that VoiceOver does not auto-announce transient banners (comment says so). Edge case: the announcement fires in .onAppear; if multiple toasts insert at once each posts its own announcement which may queue/overlap, and there is no .screenChanged vs .announcement priority tuning, but for occasional status messages this is acceptable.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/edge-case] A toast is announced twice when both the main window and menu-bar panel are open (Sources/Waves/Shared/UI/AppToasts.swift:86) — Confirmed: AppToastStack is mounted in exactly two places — MainWindowView.swift:59 and MenuBarMixerView.swift:105 — and both bind to the same store.toasts. Each AppToastBanner posts AccessibilityNotification.Announcement in its own .onAppear (AppToasts.swift:86). When both surfaces are visible at once (menu-bar panel open while the main window is also open), a newly appended toast instantiates a banner in both stacks, so the announcement fires twice and VoiceOver speaks (or queues) the message twice. The single-surface case is correct. Low severity (only manifests with both surfaces simultaneously open). | Fix: Drive the announcement once from the store when a toast is appended (post in showToast on the main actor) instead of from each banner's onAppear; or guard so only the key window's stack owns announcements.",
      "fixStatus": "1 deferred",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "edge-case",
          "title": "A toast is announced twice when both the main window and menu-bar panel are open",
          "detail": "Confirmed: AppToastStack is mounted in exactly two places — MainWindowView.swift:59 and MenuBarMixerView.swift:105 — and both bind to the same store.toasts. Each AppToastBanner posts AccessibilityNotification.Announcement in its own .onAppear (AppToasts.swift:86). When both surfaces are visible at once (menu-bar panel open while the main window is also open), a newly appended toast instantiates a banner in both stacks, so the announcement fires twice and VoiceOver speaks (or queues) the message twice. The single-surface case is correct. Low severity (only manifests with both surfaces simultaneously open).",
          "codeRef": "Sources/Waves/Shared/UI/AppToasts.swift:86",
          "expectedVsActual": "Expected: a toast is announced once when it appears. Actual: with two AppToastStacks mounted, the announcement fires once per stack, duplicating the spoken message.",
          "suggestedFix": "Drive the announcement once from the store when a toast is appended (post in showToast on the main actor) instead of from each banner's onAppear; or guard so only the key window's stack owns announcements.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Shared/UI/AppToasts.swift",
          "disposition": "deferred:needs-user-decision:Fix requires either a store-owned announcement (showToast change, outside the allowed file) or a key-window guard (no existing API in AppToa",
          "uid": "A11Y-05#1",
          "canonicalFile": "Sources/Waves/Shared/UI/AppToasts.swift"
        }
      ]
    },
    {
      "id": "A11Y-06",
      "area": "Accessibility (VoiceOver, contrast, reduced motion, label scaling)",
      "feature": "Reduced-motion honoring for animations",
      "priority": "P2",
      "userStory": "As a user with the Reduce Motion accessibility setting enabled, I want Waves to suppress spring and bounce animations, so that level meters and value changes do not cause motion discomfort.",
      "expectedBehavior": [
        "Both MixerRowView and CompactMixerRow read the accessibilityReduceMotion environment value.",
        "The animated numeric % readout uses a spring animation only when reduceMotion is false, otherwise nil (no animation).",
        "The live level-meter capsule uses a linear 0.18s animation only when reduceMotion is false.",
        "The mute-button bounce symbol effect (animateMuteChange toggle) is only triggered when reduceMotion is false, so toggling mute does not bounce under Reduce Motion."
      ],
      "acceptanceCriteria": [
        "With Reduce Motion on, changing a volume value updates the % label without an animated transition.",
        "With Reduce Motion on, the level meter resizes without an animated tween.",
        "With Reduce Motion on, toggling mute does not play the bounce symbol effect.",
        "With Reduce Motion off, all three animations play."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MixerRowView.swift:7",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:90",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:98",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:129",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:261",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:315"
      ],
      "implStatus": "partial",
      "implNotes": "Honored where it matters most (mixer rows). However reduceMotion is NOT consulted elsewhere: the toast stack spring animations (AppToasts.swift:14, 77), the toast insertion .move transition, and the main-window 'Refreshing' indicator are unconditional. The numeric % still uses .contentTransition(.numericText()) even under reduce motion (only the .animation is gated), so a subtle text morph may persist. So motion reduction is real but incomplete across the app.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/accessibility] Toast spring animation and slide-in transition are not gated by Reduce Motion (Sources/Waves/Shared/UI/AppToasts.swift:77) — Confirmed: the three mixer-row animations named in the acceptance criteria ARE correctly gated on reduceMotion (numeric % spring line 90, level-meter linear line 129, mute bounce via animateMuteChange at lines 98/315) and pass. But I grepped the whole Sources tree and reduceMotion / accessibilityReduceMotion is read ONLY in MixerRowView. The toast stack spring (AppToasts.swift:14), the per-banner spring (AppToasts.swift:77), and the .move(edge:.top) insertion transition (AppToasts.swift:74) never consult accessibilityReduceMotion, so a Reduce-Motion user still sees springy banners sliding in from the top. This is a real but narrow gap against the story's stated goal of suppressing spring/move motion app-wide. Low severity. | Fix: Read @Environment(\\.accessibilityReduceMotion) in AppToastStack/AppToastBanner; use reduceMotion ? nil : .spring(...) for the value animations (lines 14, 77) and swap the .move insertion for a plain .opacity transition when reduceMotion is true.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier3: FU-TOAST-ANIMATION-REDUCEMOTION",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "accessibility",
          "title": "Toast spring animation and slide-in transition are not gated by Reduce Motion",
          "detail": "Confirmed: the three mixer-row animations named in the acceptance criteria ARE correctly gated on reduceMotion (numeric % spring line 90, level-meter linear line 129, mute bounce via animateMuteChange at lines 98/315) and pass. But I grepped the whole Sources tree and reduceMotion / accessibilityReduceMotion is read ONLY in MixerRowView. The toast stack spring (AppToasts.swift:14), the per-banner spring (AppToasts.swift:77), and the .move(edge:.top) insertion transition (AppToasts.swift:74) never consult accessibilityReduceMotion, so a Reduce-Motion user still sees springy banners sliding in from the top. This is a real but narrow gap against the story's stated goal of suppressing spring/move motion app-wide. Low severity.",
          "codeRef": "Sources/Waves/Shared/UI/AppToasts.swift:77",
          "expectedVsActual": "Expected (per story intent): spring/move animations suppressed under Reduce Motion. Actual: only mixer-row animations are gated; the toast spring + slide-in transition remain unconditional.",
          "suggestedFix": "Read @Environment(\\.accessibilityReduceMotion) in AppToastStack/AppToastBanner; use reduceMotion ? nil : .spring(...) for the value animations (lines 14, 77) and swap the .move insertion for a plain .opacity transition when reduceMotion is true.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Shared/UI/AppToasts.swift",
          "disposition": "tier3:applied:FU-TOAST-ANIMATION-REDUCEMOTION",
          "uid": "A11Y-06#1",
          "canonicalFile": "Sources/Waves/Shared/UI/AppToasts.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-TOAST-ANIMATION-REDUCEMOTION",
          "fixTier": "tier3"
        }
      ]
    },
    {
      "id": "A11Y-07",
      "area": "Accessibility (VoiceOver, contrast, reduced motion, label scaling)",
      "feature": "Increase Contrast honoring for low-contrast surfaces",
      "priority": "P1",
      "userStory": "As a user with macOS Increase Contrast enabled, I want faint chips and hairline borders to become clearly visible, so that route-state indicators and toast edges are not invisible to me.",
      "expectedBehavior": [
        "RoutingStateIndicator reads colorSchemeContrast; when increased, it adds a solid Capsule stroke outline in the state color and raises the chip background opacity to 0.28 (vs 0.08/0.12 normally).",
        "Toast banners read colorSchemeContrast and switch their border from the 9%-white stroke to WavesDesign.hairline, which returns 45%-white under increased contrast.",
        "Route-state chips always pair an SF Symbol and a text label (and an accessibilityLabel) with the color, so meaning is conveyed without relying on color alone (managed/live/recent/error each have distinct symbols and words)."
      ],
      "acceptanceCriteria": [
        "With Increase Contrast on, each route-state chip shows a visible outline and a stronger fill.",
        "With Increase Contrast on, toast banners show a distinctly brighter border than the default hairline.",
        "Route state is distinguishable by symbol + text even for a user who cannot perceive the color difference.",
        "With Increase Contrast off, the original subtle styling is preserved."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MixerRowView.swift:408",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:430",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:442",
        "Sources/Waves/Shared/UI/DesignSystem.swift:46",
        "Sources/Waves/Shared/UI/AppToasts.swift:69"
      ],
      "implStatus": "partial",
      "implNotes": "The chip and toast treatments are correct and the symbol+text pairing satisfies 'understandable without color alone' (PRODUCT.md). But coverage is narrow: many other faint surfaces (WavesDesign.rail/stroke panels, the 'Refreshing' overlay border at MainWindowView.swift:77, OutputSummary divider, RouteHealthBadge 0.12 fill) do NOT consult contrast and stay low-contrast. Larger concern: the app is hard-pinned to dark mode via NSApp.appearance = .darkAqua (WavesApp.swift:88), so an Increase-Contrast user who also runs Light Appearance never gets a light high-contrast surface — contrast adaptation is dark-only. PRODUCT.md targets WCAG AA broadly, which this only partially meets.",
      "source": "phase1-story",
      "testStatus": "1 issue(s) [1L]",
      "errorsFound": "• [LOW/accessibility] Compact menu-bar route indicator conveys state with icon + color only, no visible text label (Sources/Waves/Features/Mixer/MixerRowView.swift:486) — Verified: the full row's RoutingStateIndicator pairs an SF Symbol with a visible Text(state.displayName) (lines 415-423), satisfying 'distinguishable without color'. The compact menu-bar row uses RoutingStateDot (lines 483-493), which renders only a state-colored SF Symbol with no adjacent visible text; the text label exists only as an accessibilityLabel for VoiceOver. Mitigating factor (which keeps this low / partial): the dot's glyphs DO differ per state (checkmark.circle.fill / waveform / checkmark.circle / clock.fill / exclamationmark.triangle.fill), so color is supplementary, not the sole channel, for a user who can perceive shape. It is a partial gap in the compact surface, not a full color-only failure. | Fix: Optionally add a short visible label/abbreviation beside the compact dot, or document that the distinct glyph shapes carry the meaning and color is supplementary.",
      "fixStatus": "1 deferred",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "low",
          "type": "accessibility",
          "title": "Compact menu-bar route indicator conveys state with icon + color only, no visible text label",
          "detail": "Verified: the full row's RoutingStateIndicator pairs an SF Symbol with a visible Text(state.displayName) (lines 415-423), satisfying 'distinguishable without color'. The compact menu-bar row uses RoutingStateDot (lines 483-493), which renders only a state-colored SF Symbol with no adjacent visible text; the text label exists only as an accessibilityLabel for VoiceOver. Mitigating factor (which keeps this low / partial): the dot's glyphs DO differ per state (checkmark.circle.fill / waveform / checkmark.circle / clock.fill / exclamationmark.triangle.fill), so color is supplementary, not the sole channel, for a user who can perceive shape. It is a partial gap in the compact surface, not a full color-only failure.",
          "codeRef": "Sources/Waves/Features/Mixer/MixerRowView.swift:486",
          "expectedVsActual": "Expected: route state distinguishable by symbol + text for users who cannot perceive color. Actual: the compact dot shows symbol + color but no visible word; the text label is VoiceOver-only.",
          "suggestedFix": "Optionally add a short visible label/abbreviation beside the compact dot, or document that the distinct glyph shapes carry the meaning and color is supplementary.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MixerRowView.swift",
          "disposition": "deferred:needs-user-decision:Adding a visible text label is a space-constrained design call (two reasonable answers); the JSON itself notes glyph shapes already carry th",
          "uid": "A11Y-07#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MixerRowView.swift"
        }
      ]
    },
    {
      "id": "A11Y-08",
      "area": "Accessibility (VoiceOver, contrast, reduced motion, label scaling)",
      "feature": "Numeric label scaling to avoid clipping at large text sizes",
      "priority": "P2",
      "userStory": "As a user who increases system text size, I want the fixed-width percentage and boost labels to shrink-to-fit rather than truncate, so that I can still read the exact volume and boost values.",
      "expectedBehavior": [
        "The volume % readout uses lineLimit(1) plus minimumScaleFactor(0.7) inside its fixed 40pt-wide frame, so larger glyphs scale down instead of clipping.",
        "The boost label uses lineLimit(1) plus minimumScaleFactor(0.7) inside its fixed 34/38pt frame.",
        "The same minimumScaleFactor(0.7) protects the boost menu label text in both compact and full layouts."
      ],
      "acceptanceCriteria": [
        "At large accessibility text sizes the '100%' label remains fully readable within its frame (not cut off).",
        "The boost 'x' label (e.g. '4x') remains readable at large text sizes.",
        "At default text size the labels render at full size."
      ],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MixerRowView.swift:87",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:88",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:380",
        "Sources/Waves/Features/Mixer/MixerRowView.swift:381"
      ],
      "implStatus": "partial",
      "implNotes": "This is a clipping mitigation, not true large-text support. The labels are still in fixed-width frames (40/38/34pt) with a 0.7 floor, so beyond ~1.4x they will scale-floor and could still clip; and the frames themselves do not grow. Critically, macOS does not surface iOS-style Dynamic Type, and no view here reads dynamicTypeSize/sizeCategory or uses @ScaledMetric, so the whole UI does not actually reflow for the system 'larger text' accessibility setting — only these two labels shrink to fit. The commit message calls this 'scale numeric labels at large Dynamic Type sizes,' which overstates the actual effect on macOS.",
      "source": "phase1-story",
      "testStatus": "PASS",
      "errorsFound": "",
      "fixStatus": "",
      "fixNotes": "",
      "retestStatus": "PASS",
      "issues": []
    },
    {
      "id": "AUDIO-01",
      "area": "Output Device Management & Auto-Restore",
      "feature": "Per-device volume-control mode (hardware / software / automatic)",
      "priority": "",
      "userStory": "",
      "expectedBehavior": [],
      "acceptanceCriteria": [],
      "codeRefs": [
        "Sources/WavesAudioCore/Backend/AudioControlBackend.swift:11 (func setVolumeControlMode(_:forDeviceID:))",
        "Sources/WavesAudioCore/Models/AudioDevice.swift:55-83 (enum VolumeControlMode hardware/software/automatic with displayName + description)",
        "Sources/WavesAudioCore/Models/AudioDevice.swift:9 (AudioDevice.volumeControlMode)",
        "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:142 (setVolumeControlMode implementation)",
        "README.md:16 ('Smart Volume Backend: Intelligently switches between hardware and software volume control')",
        "README.md:163 (Audio Settings: 'Choose the preferred route-control mode for Waves-managed sessions')"
      ],
      "implStatus": "stubbed",
      "implNotes": "[critic gap] It is a backend/model capability with no remaining UI surface, so a UI-walk of menus/buttons/toggles never reaches it, and the 171 stories were derived from the visible control set. The picker that exposed it was deliberately removed (CHANGELOG: 'Removed — Decorative volume-control-mode picker (it was a no-op)'), but the protocol method, enum, model field, and the README text that promises it were all left behind, so it looks like a feature when scanning code/docs but isn't wired to anything users can do.",
      "source": "phase1-critic-gap",
      "testStatus": "2 issue(s) [2M]",
      "errorsFound": "• [MEDIUM/dead-code] Per-device volume-control mode is a no-op: written but never read by any routing/volume path (Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:142) — Verified by reading WorkspaceAudioControlBackend.setVolumeControlMode (line 142-146): it only mutates snapshot.currentDevice?.volumeControlMode when deviceID == current device, performs no Core Audio call, and changes no behavior. A full grep of Sources confirms AudioDevice.volumeControlMode is only ever WRITTEN (the two backend setters at WorkspaceAudioControlBackend.swift:144 and PreviewAudioControlBackend.swift:80, plus the preview initializer in AudioSessionSnapshot+Preview.swift:94 and the AudioDevice init) and is NEVER READ by any routing/volume code. The deciding UI picker was removed as a no-op (CHANGELOG), so no user can invoke setVolumeControlMode at all. The protocol method (AudioControlBackend.swift:11), VolumeControlMode enum with displayName/description (AudioDevice.swift:55-83), and AudioDevice.volumeControlMode field are stranded surface area. Minor inaccuracy in the original report: the field IS technically serialized as part of the Codable AudioDevice in the session snapshot, but the write is transient (the device-change handler reloads currentSnapshot, resetting it to .software) and nothing reads it, so the dead-code conclusion stands. | Fix: Either fully remove the dead surface (delete setVolumeControlMode from AudioControlBackend and both backends, remove AudioDevice.volumeControlMode and the VolumeControlMode enum, drop the README promises), or re-add a UI control, persist the chosen mode, and make the routing layer actually read volumeControlMode.\n• [MEDIUM/inconsistency] README advertises a 'Smart Volume Backend' / route-control mode that does not exist for users (README.md:16) — Verified README.md:16 states 'Smart Volume Backend: Intelligently switches between hardware and software volume control' and README.md:163 (Audio Settings section) states 'Choose the preferred route-control mode for Waves-managed sessions'. Both describe the removed/no-op volume-control-mode capability. Confirmed by reading AudioSettingsView (SettingsView.swift:181-208): it contains only the current-output-device label, a managed-routing description, a Recover Routes button, and a boost tip — there is NO route-control-mode control, so README:163 references absent UI. | Fix: Remove the 'Smart Volume Backend' bullet (README:16) and the 'Choose the preferred route-control mode' line (README:163), or re-add them only once the feature is wired to a working UI and routing path.",
      "fixStatus": "1 fixed · 1 deferred",
      "fixNotes": "tier2: FU-README-PHANTOM-FEATURES",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "dead-code",
          "title": "Per-device volume-control mode is a no-op: written but never read by any routing/volume path",
          "detail": "Verified by reading WorkspaceAudioControlBackend.setVolumeControlMode (line 142-146): it only mutates snapshot.currentDevice?.volumeControlMode when deviceID == current device, performs no Core Audio call, and changes no behavior. A full grep of Sources confirms AudioDevice.volumeControlMode is only ever WRITTEN (the two backend setters at WorkspaceAudioControlBackend.swift:144 and PreviewAudioControlBackend.swift:80, plus the preview initializer in AudioSessionSnapshot+Preview.swift:94 and the AudioDevice init) and is NEVER READ by any routing/volume code. The deciding UI picker was removed as a no-op (CHANGELOG), so no user can invoke setVolumeControlMode at all. The protocol method (AudioControlBackend.swift:11), VolumeControlMode enum with displayName/description (AudioDevice.swift:55-83), and AudioDevice.volumeControlMode field are stranded surface area. Minor inaccuracy in the original report: the field IS technically serialized as part of the Codable AudioDevice in the session snapshot, but the write is transient (the device-change handler reloads currentSnapshot, resetting it to .software) and nothing reads it, so the dead-code conclusion stands.",
          "codeRef": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift:142",
          "expectedVsActual": "Expected (per README:16 'Smart Volume Backend: Intelligently switches between hardware and software volume control' and README:163 'Choose the preferred route-control mode'): a selectable hardware/software/automatic mode that affects how Waves controls volume. Actual: no UI exposes it, the setter is a no-op mutating an unread field, and no routing code reads volumeControlMode.",
          "suggestedFix": "Either fully remove the dead surface (delete setVolumeControlMode from AudioControlBackend and both backends, remove AudioDevice.volumeControlMode and the VolumeControlMode enum, drop the README promises), or re-add a UI control, persist the chosen mode, and make the routing layer actually read volumeControlMode.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift",
          "disposition": "deferred:needs-feature:setVolumeControlMode is a no-op writing an unread field, but resolving it means either deleting surface across AudioControlBackend.swift, PreviewAudioControlBac",
          "uid": "AUDIO-01#1",
          "canonicalFile": "Sources/Waves/Services/Audio/WorkspaceAudioControlBackend.swift"
        },
        {
          "severity": "medium",
          "type": "inconsistency",
          "title": "README advertises a 'Smart Volume Backend' / route-control mode that does not exist for users",
          "detail": "Verified README.md:16 states 'Smart Volume Backend: Intelligently switches between hardware and software volume control' and README.md:163 (Audio Settings section) states 'Choose the preferred route-control mode for Waves-managed sessions'. Both describe the removed/no-op volume-control-mode capability. Confirmed by reading AudioSettingsView (SettingsView.swift:181-208): it contains only the current-output-device label, a managed-routing description, a Recover Routes button, and a boost tip — there is NO route-control-mode control, so README:163 references absent UI.",
          "codeRef": "README.md:16",
          "expectedVsActual": "Expected: docs reflect shipped, working behavior. Actual: README promises an intelligent hardware/software switching backend and an Audio-settings route-control-mode chooser, neither of which exists or has any effect.",
          "suggestedFix": "Remove the 'Smart Volume Backend' bullet (README:16) and the 'Choose the preferred route-control mode' line (README:163), or re-add them only once the feature is wired to a working UI and routing path.",
          "confidence": "confirmed",
          "primaryFile": "README.md",
          "disposition": "tier2:applied:FU-README-PHANTOM-FEATURES",
          "uid": "AUDIO-01#2",
          "canonicalFile": "README.md",
          "fixStatus": "applied",
          "fixedBy": "FU-README-PHANTOM-FEATURES",
          "fixTier": "tier2"
        }
      ]
    },
    {
      "id": "DISC-07",
      "area": "App Discovery & List Population",
      "feature": "Show / hide system audio processes in the app list",
      "priority": "",
      "userStory": "",
      "expectedBehavior": [],
      "acceptanceCriteria": [],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:103-107 (visibleApps filters out .system unless showSystemProcesses)",
        "Sources/Waves/Stores/UserPreferences.swift:6 (showSystemProcesses default false)",
        "Sources/Waves/Settings/SettingsView.swift:68-77 (Toggle 'Show system processes')",
        "Sources/WavesAudioCore/Models/AudioApp.swift:228 (.system category)"
      ],
      "implStatus": "implemented",
      "implNotes": "[critic gap] The captured discovery stories cover helper filtering (DISC-03), category inference (DISC-04), and filtered-list states (DISC-06), but none captures the user-controllable inclusion/exclusion of the entire `.system` category from the visible list. It is a distinct list-population behavior driven by a dedicated preference and toggle, only loosely subsumed under the generic 'General preferences toggles' (SET-02).",
      "source": "phase1-critic-gap",
      "testStatus": "1 issue(s) [1M]",
      "errorsFound": "• [MEDIUM/ux] Empty-state copy is wrong when the only audio apps are hidden system processes (Sources/Waves/Features/Mixer/MainWindowView.swift:234) — Verified: visibleApps filters out .system unless showSystemProcesses (AppStore.swift:104-106), which defaults to false (UserPreferences.swift:6). AppDiscoveryPolicy.inferCategory returns .system for any unmatched com.apple.* bundle ID (AppDiscoveryPolicy.swift:56-57). So if every detected source is .system, the Running scope's scopedApps is empty and SourceListView renders ContentUnavailableView with emptyMessage 'Waves hasn't found any running apps yet.' (MainWindowView.swift:234). That copy is wrong: apps WERE found but filtered out, and nothing tells the user a filter is hiding them. The empty state does include a Settings button (MainWindowView.swift:352-357), which mitigates discoverability slightly, but the message still misattributes the cause. | Fix: When scopedApps is empty but session.apps contains .system entries and showSystemProcesses is false, show a distinct message such as 'System processes are hidden — enable Show system processes in Settings to see them.' Optionally surface a hidden-count badge.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier2: FU-EMPTY-STATE-SYSTEM-PROCESSES",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "ux",
          "title": "Empty-state copy is wrong when the only audio apps are hidden system processes",
          "detail": "Verified: visibleApps filters out .system unless showSystemProcesses (AppStore.swift:104-106), which defaults to false (UserPreferences.swift:6). AppDiscoveryPolicy.inferCategory returns .system for any unmatched com.apple.* bundle ID (AppDiscoveryPolicy.swift:56-57). So if every detected source is .system, the Running scope's scopedApps is empty and SourceListView renders ContentUnavailableView with emptyMessage 'Waves hasn't found any running apps yet.' (MainWindowView.swift:234). That copy is wrong: apps WERE found but filtered out, and nothing tells the user a filter is hiding them. The empty state does include a Settings button (MainWindowView.swift:352-357), which mitigates discoverability slightly, but the message still misattributes the cause.",
          "codeRef": "Sources/Waves/Features/Mixer/MainWindowView.swift:234",
          "expectedVsActual": "Expected: when the list is empty only because the system-processes filter hid everything, the UI should indicate a filter is active and how to reveal the apps. Actual: it claims no apps were found, hiding the toggle that caused the empty list.",
          "suggestedFix": "When scopedApps is empty but session.apps contains .system entries and showSystemProcesses is false, show a distinct message such as 'System processes are hidden — enable Show system processes in Settings to see them.' Optionally surface a hidden-count badge.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "disposition": "tier2:applied:FU-EMPTY-STATE-SYSTEM-PROCESSES",
          "uid": "DISC-07#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-EMPTY-STATE-SYSTEM-PROCESSES",
          "fixTier": "tier2"
        }
      ]
    },
    {
      "id": "DISC-08",
      "area": "App Discovery & List Population",
      "feature": "Recent apps grouping and the 'Show recent apps' toggle",
      "priority": "",
      "userStory": "",
      "expectedBehavior": [],
      "acceptanceCriteria": [],
      "codeRefs": [
        "Sources/Waves/Stores/AppStore.swift:124-128 (recentApps computed, gated by showRecentApps)",
        "Sources/Waves/Stores/UserPreferences.swift:5 (showRecentApps default true)",
        "Sources/Waves/Settings/SettingsView.swift:58-66 (Toggle 'Show recent apps')",
        "Sources/Waves/Features/Mixer/MenuBarMixerView.swift:48-50 (Recent CompactSection)",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:110-112,234-241 (Recent sidebar scope + empty message)"
      ],
      "implStatus": "implemented",
      "implNotes": "[critic gap] The inventory references 'recent' only obliquely (LoginItem/persistence and menu-bar sections), but there is no story for the Recent grouping itself — the computed set of non-live visible apps surfaced as a dedicated sidebar scope and a menu-bar section, gated by a user preference. It is a separate organizational/list-population behavior from live/pinned grouping (ROUTE-04, PIN-04).",
      "source": "phase1-critic-gap",
      "testStatus": "2 issue(s) [2M]",
      "errorsFound": "• [MEDIUM/inconsistency] Main-window 'Recent' sidebar scope stays visible (and shows a misleading empty state) when 'Show recent apps' is OFF (Sources/Waves/Features/Mixer/MainWindowView.swift:252) — Verified: the menu bar hides the Recent section when showRecentApps is false (MenuBarMixerView.swift:48 guards 'if store.preferences.showRecentApps'). The main window does NOT: SidebarView renders ForEach(SourceFilter.allCases) which always includes .recent (MainWindowView.swift:252), and recentApps returns [] when showRecentApps is false (AppStore.swift:124-128). With the toggle off, the sidebar still shows a 'Recent' row reading '0 recent apps', and selecting it shows 'No Recent Apps' with 'Apps appear here when they're not actively playing audio.' (MainWindowView.swift:240) — attributing emptiness to no recent apps rather than the disabled preference. Inconsistent with the menu bar and misleading. | Fix: Filter SourceFilter.allCases to drop .recent when !store.preferences.showRecentApps (and reset selection to .running if it was .recent), or special-case the Recent empty message to say recent apps are turned off and link to Settings.\n• [MEDIUM/ux] 'Recent' scope is a misnomer — it is 'all non-live apps' with no recency ordering (Sources/Waves/Stores/AppStore.swift:124) — Verified: recentApps is visibleApps minus the live set (AppStore.swift:124-128), returned in the user's sortMode order via visibleApps (sortedApps, AppStore.swift:1278-1301). There is no last-active timestamp or recency sort anywhere — confirmed AudioApp/sort code has no recency concept. Yet the scope is labelled 'Recent' with a clock.fill icon (MainWindowView.swift:167,210) and the menu-bar section title is 'Recent' (MenuBarMixerView.swift:49). The label/icon imply time-ordered recently-used apps; the implementation is every visible app not currently playing, including pinned and never-played apps. The feature-status implNotes itself describes it as 'the computed set of non-live visible apps.' | Fix: Either implement real recency (track a last-active timestamp per app and sort/limit by it), or rename the scope to 'Inactive' / 'Not Playing', change the clock icon, and adjust the empty/section copy to match.",
      "fixStatus": "1 fixed · 1 deferred",
      "fixNotes": "tier2: FU-RECENT-SCOPE-GATING",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "inconsistency",
          "title": "Main-window 'Recent' sidebar scope stays visible (and shows a misleading empty state) when 'Show recent apps' is OFF",
          "detail": "Verified: the menu bar hides the Recent section when showRecentApps is false (MenuBarMixerView.swift:48 guards 'if store.preferences.showRecentApps'). The main window does NOT: SidebarView renders ForEach(SourceFilter.allCases) which always includes .recent (MainWindowView.swift:252), and recentApps returns [] when showRecentApps is false (AppStore.swift:124-128). With the toggle off, the sidebar still shows a 'Recent' row reading '0 recent apps', and selecting it shows 'No Recent Apps' with 'Apps appear here when they're not actively playing audio.' (MainWindowView.swift:240) — attributing emptiness to no recent apps rather than the disabled preference. Inconsistent with the menu bar and misleading.",
          "codeRef": "Sources/Waves/Features/Mixer/MainWindowView.swift:252",
          "expectedVsActual": "Expected: turning off 'Show recent apps' removes/disables the Recent grouping everywhere, matching the menu bar. Actual: the main-window Recent scope remains, always shows 0, and its empty-state copy blames a lack of recent apps rather than the disabled preference.",
          "suggestedFix": "Filter SourceFilter.allCases to drop .recent when !store.preferences.showRecentApps (and reset selection to .running if it was .recent), or special-case the Recent empty message to say recent apps are turned off and link to Settings.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "disposition": "tier2:applied:FU-RECENT-SCOPE-GATING",
          "uid": "DISC-08#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-RECENT-SCOPE-GATING",
          "fixTier": "tier2"
        },
        {
          "severity": "medium",
          "type": "ux",
          "title": "'Recent' scope is a misnomer — it is 'all non-live apps' with no recency ordering",
          "detail": "Verified: recentApps is visibleApps minus the live set (AppStore.swift:124-128), returned in the user's sortMode order via visibleApps (sortedApps, AppStore.swift:1278-1301). There is no last-active timestamp or recency sort anywhere — confirmed AudioApp/sort code has no recency concept. Yet the scope is labelled 'Recent' with a clock.fill icon (MainWindowView.swift:167,210) and the menu-bar section title is 'Recent' (MenuBarMixerView.swift:49). The label/icon imply time-ordered recently-used apps; the implementation is every visible app not currently playing, including pinned and never-played apps. The feature-status implNotes itself describes it as 'the computed set of non-live visible apps.'",
          "codeRef": "Sources/Waves/Stores/AppStore.swift:124",
          "expectedVsActual": "Expected (per the 'Recent' label and clock icon): recently-active apps, ideally ordered by recency. Actual: all visible apps not live right now, in the normal sort order, with no recency concept.",
          "suggestedFix": "Either implement real recency (track a last-active timestamp per app and sort/limit by it), or rename the scope to 'Inactive' / 'Not Playing', change the clock icon, and adjust the empty/section copy to match.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Stores/AppStore.swift",
          "disposition": "deferred:needs-feature:Neither option is achievable in AppStore.swift alone; requires model/backend changes or a UX rename across files.",
          "uid": "DISC-08#2",
          "canonicalFile": "Sources/Waves/Stores/AppStore.swift"
        }
      ]
    },
    {
      "id": "WIN-08",
      "area": "Main Window Mixer UI & Empty State",
      "feature": "Last-used sidebar source scope restored across launches",
      "priority": "",
      "userStory": "",
      "expectedBehavior": [],
      "acceptanceCriteria": [],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MainWindowView.swift:8 (@SceneStorage(\"waves.selectedScope\") var selection: SourceFilter = .running)",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:14-16 (SidebarView selection binding)"
      ],
      "implStatus": "implemented",
      "implNotes": "[critic gap] The main-window stories cover sidebar scopes (WIN-01) and search (WIN-02) but not the persistence of the selected scope. The selected source filter (Running / Pinned / Live / Recent) is saved via @SceneStorage and restored on relaunch so the user returns to the scope they left — a distinct UX-continuity behavior separate from preference/session persistence (PERSIST-01/02).",
      "source": "phase1-critic-gap",
      "testStatus": "1 issue(s) [1M]",
      "errorsFound": "• [MEDIUM/edge-case] Persisted sidebar scope can be restored into a permanently-empty, misleading state (Recent scope with 'Show recent apps' off) (Sources/Waves/Features/Mixer/MainWindowView.swift:8) — Completeness-pass finding the first tester missed (they marked WIN-08 pass). selection is @SceneStorage(\"waves.selectedScope\") and any SourceFilter — including .recent and .pinned — is persisted and restored across launches (MainWindowView.swift:8). There is no validation that the restored scope is still populatable. If the user leaves the window on the Recent scope and then disables 'Show recent apps', recentApps becomes [] forever (AppStore.swift:124-128) and scopedApps for .recent is always empty (MainWindowView.swift:110-112), so on this and every subsequent launch the window opens to a Recent scope that can never show anything, with the misleading 'No Recent Apps' copy (MainWindowView.swift:240). Unlike the menu bar, nothing drops/hides the .recent scope or resets selection to .running, so the persisted scope is a stuck dead-end. This is the WIN-08 persistence/desync angle distinct from DISC-08's general inconsistency. | Fix: On window appear, validate the restored selection against currently-available scopes and fall back to .running when the scope is disabled (e.g. .recent while !showRecentApps); and drop .recent from SourceFilter.allCases when showRecentApps is off so it can't be selected or restored.",
      "fixStatus": "1 fixed",
      "fixNotes": "tier2: FU-RECENT-SCOPE-GATING",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "edge-case",
          "title": "Persisted sidebar scope can be restored into a permanently-empty, misleading state (Recent scope with 'Show recent apps' off)",
          "detail": "Completeness-pass finding the first tester missed (they marked WIN-08 pass). selection is @SceneStorage(\"waves.selectedScope\") and any SourceFilter — including .recent and .pinned — is persisted and restored across launches (MainWindowView.swift:8). There is no validation that the restored scope is still populatable. If the user leaves the window on the Recent scope and then disables 'Show recent apps', recentApps becomes [] forever (AppStore.swift:124-128) and scopedApps for .recent is always empty (MainWindowView.swift:110-112), so on this and every subsequent launch the window opens to a Recent scope that can never show anything, with the misleading 'No Recent Apps' copy (MainWindowView.swift:240). Unlike the menu bar, nothing drops/hides the .recent scope or resets selection to .running, so the persisted scope is a stuck dead-end. This is the WIN-08 persistence/desync angle distinct from DISC-08's general inconsistency.",
          "codeRef": "Sources/Waves/Features/Mixer/MainWindowView.swift:8",
          "expectedVsActual": "Expected: a restored sidebar scope should always be a usable, populatable scope; disabling the preference that backs the restored scope should not leave the window stuck on an unfillable view. Actual: a persisted .recent (or filtered-out) scope is restored even after its preference is disabled, yielding a permanently empty, misleadingly-worded view with no auto-reset.",
          "suggestedFix": "On window appear, validate the restored selection against currently-available scopes and fall back to .running when the scope is disabled (e.g. .recent while !showRecentApps); and drop .recent from SourceFilter.allCases when showRecentApps is off so it can't be selected or restored.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "disposition": "tier2:applied:FU-RECENT-SCOPE-GATING",
          "uid": "WIN-08#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-RECENT-SCOPE-GATING",
          "fixTier": "tier2"
        }
      ]
    },
    {
      "id": "WIN-09",
      "area": "Main Window Mixer UI & Empty State",
      "feature": "Keyboard 'b' to cycle boost and 'p' to pin on the selected mixer row",
      "priority": "",
      "userStory": "",
      "expectedBehavior": [],
      "acceptanceCriteria": [],
      "codeRefs": [
        "Sources/Waves/Features/Mixer/MainWindowView.swift:381-386 (onKeyPress space/m/=/-/b/p)",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:422-425 (cycleBoost wraps 4x back to 1x)",
        "Sources/Waves/Features/Mixer/MainWindowView.swift:386 (onKeyPress(\"p\") togglePinned)"
      ],
      "implStatus": "implemented",
      "implNotes": "[critic gap] WIN-07 captures 'keyboard-driven control and drag-to-reorder' generically and KEY-01..03 cover the global frontmost-app hotkeys, but the in-list row-level key handlers go beyond volume/mute: 'b' cycles boost 1x->4x->1x and 'p' toggles pin on the currently selected row. Pin-via-keyboard and the in-window (non-global) boost-cycle key are not separately captured, and a UI-walk focused on buttons/shortcut-modifiers can miss bare-character onKeyPress handlers.",
      "source": "phase1-critic-gap",
      "testStatus": "3 issue(s) [2M 1L]",
      "errorsFound": "• [MEDIUM/ux] In-list 'b' (cycle boost) and 'p' (pin) shortcuts are completely undocumented (Sources/Waves/Features/Mixer/MainWindowView.swift:385) — Verified MainWindowView.swift:381-386 attaches bare-character onKeyPress handlers to the source list: space/m (mute), =/- (nudge volume by 0.05), b (cycleBoost, MainWindowView.swift:422-425), p (togglePinned). Confirmed none appear in the Settings 'Keyboard shortcuts' section (SettingsView.swift:127-160, lists only ⌘⌥↑/↓/M) nor in HelpView's Keyboard Shortcuts list (HelpView.swift:64-103, lists only ⌘⌥↑/↓/M, ⌘R, ⌘S, ⌘,). No accessibility hint advertises them either. A user cannot discover that 'b' cycles boost or 'p' pins the selected row. | Fix: List the in-row keys (space/m mute, =/- volume, b boost, p pin) in HelpView and/or the Settings keyboard-shortcuts section, or surface them via per-row accessibility hints / a help overlay.\n• [MEDIUM/inconsistency] In-list keys ignore the 'Enable keyboard shortcuts' preference that gates the global hotkeys (Sources/Waves/Features/Mixer/MainWindowView.swift:408) — Verified the global hotkey handlers all guard on preferences.enableKeyboardShortcuts (increaseVolumeForFrontmostApp AppStore.swift:1211, decreaseVolumeForFrontmostApp AppStore.swift:1230, toggleMuteForFrontmostApp AppStore.swift:1249). The in-list handleKey helper (MainWindowView.swift:408-414) and its onKeyPress callers (MainWindowView.swift:381-386) call store.setMuted / nudgeVolume / cycleBoost / store.togglePinned with NO enableKeyboardShortcuts guard. So disabling 'Enable keyboard shortcuts' in Settings does not disable space/m/=/-/b/p on the selected row, contradicting the toggle's implied meaning. | Fix: Gate handleKey on store.preferences.enableKeyboardShortcuts (return .ignored when disabled), or clarify in copy that the toggle only governs the global ⌘⌥ hotkeys while in-list keys are always on.\n• [LOW/ux] onKeyPress intercepts b/p/m/-/=, defeating List type-select for the selected-row case (Sources/Waves/Features/Mixer/MainWindowView.swift:386) — Verified MainWindowView.swift:381-386 consume 'm', 'b', 'p', '-', '=' as command keys, returning .handled whenever a row is selected (handleKey, MainWindowView.swift:408-414). AppKit/SwiftUI lists support type-select (typing a letter jumps to the next matching row). Because these letters are swallowed when a row is selected, a user cannot type m/b/p to jump to apps starting with M/B/P; the keystroke mutes/cycles-boosts/pins the selected row instead. Note handleKey returns .ignored when no row is selected, so type-select still works before any selection — limiting the impact, hence low severity and 'likely'. | Fix: Require a modifier for the action keys, or scope these handlers so they don't shadow list type-select, or document the tradeoff clearly.",
      "fixStatus": "2 fixed · 1 deferred",
      "fixNotes": "tier2: FU-INLIST-KEYS-RESPECT-PREFERENCE",
      "retestStatus": "PASS",
      "issues": [
        {
          "severity": "medium",
          "type": "ux",
          "title": "In-list 'b' (cycle boost) and 'p' (pin) shortcuts are completely undocumented",
          "detail": "Verified MainWindowView.swift:381-386 attaches bare-character onKeyPress handlers to the source list: space/m (mute), =/- (nudge volume by 0.05), b (cycleBoost, MainWindowView.swift:422-425), p (togglePinned). Confirmed none appear in the Settings 'Keyboard shortcuts' section (SettingsView.swift:127-160, lists only ⌘⌥↑/↓/M) nor in HelpView's Keyboard Shortcuts list (HelpView.swift:64-103, lists only ⌘⌥↑/↓/M, ⌘R, ⌘S, ⌘,). No accessibility hint advertises them either. A user cannot discover that 'b' cycles boost or 'p' pins the selected row.",
          "codeRef": "Sources/Waves/Features/Mixer/MainWindowView.swift:385",
          "expectedVsActual": "Expected: a discoverable shortcut (documented in Help/Settings or hinted in the UI). Actual: 'b' and 'p' (and space/m/=/-) are hidden, undocumented behaviors only findable by reading source.",
          "suggestedFix": "List the in-row keys (space/m mute, =/- volume, b boost, p pin) in HelpView and/or the Settings keyboard-shortcuts section, or surface them via per-row accessibility hints / a help overlay.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "disposition": "iter3:implemented",
          "uid": "WIN-09#1",
          "canonicalFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "fixStatus": "applied",
          "fixedBy": "iter3-ui-parity",
          "fixTier": "iter3"
        },
        {
          "severity": "medium",
          "type": "inconsistency",
          "title": "In-list keys ignore the 'Enable keyboard shortcuts' preference that gates the global hotkeys",
          "detail": "Verified the global hotkey handlers all guard on preferences.enableKeyboardShortcuts (increaseVolumeForFrontmostApp AppStore.swift:1211, decreaseVolumeForFrontmostApp AppStore.swift:1230, toggleMuteForFrontmostApp AppStore.swift:1249). The in-list handleKey helper (MainWindowView.swift:408-414) and its onKeyPress callers (MainWindowView.swift:381-386) call store.setMuted / nudgeVolume / cycleBoost / store.togglePinned with NO enableKeyboardShortcuts guard. So disabling 'Enable keyboard shortcuts' in Settings does not disable space/m/=/-/b/p on the selected row, contradicting the toggle's implied meaning.",
          "codeRef": "Sources/Waves/Features/Mixer/MainWindowView.swift:408",
          "expectedVsActual": "Expected: the 'Enable keyboard shortcuts' toggle controls all keyboard-driven mixer actions. Actual: it only controls the global ⌘⌥ hotkeys; the in-list character keys remain active regardless.",
          "suggestedFix": "Gate handleKey on store.preferences.enableKeyboardShortcuts (return .ignored when disabled), or clarify in copy that the toggle only governs the global ⌘⌥ hotkeys while in-list keys are always on.",
          "confidence": "confirmed",
          "primaryFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "disposition": "tier2:applied:FU-INLIST-KEYS-RESPECT-PREFERENCE",
          "uid": "WIN-09#2",
          "canonicalFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "fixStatus": "applied",
          "fixedBy": "FU-INLIST-KEYS-RESPECT-PREFERENCE",
          "fixTier": "tier2"
        },
        {
          "severity": "low",
          "type": "ux",
          "title": "onKeyPress intercepts b/p/m/-/=, defeating List type-select for the selected-row case",
          "detail": "Verified MainWindowView.swift:381-386 consume 'm', 'b', 'p', '-', '=' as command keys, returning .handled whenever a row is selected (handleKey, MainWindowView.swift:408-414). AppKit/SwiftUI lists support type-select (typing a letter jumps to the next matching row). Because these letters are swallowed when a row is selected, a user cannot type m/b/p to jump to apps starting with M/B/P; the keystroke mutes/cycles-boosts/pins the selected row instead. Note handleKey returns .ignored when no row is selected, so type-select still works before any selection — limiting the impact, hence low severity and 'likely'.",
          "codeRef": "Sources/Waves/Features/Mixer/MainWindowView.swift:386",
          "expectedVsActual": "Expected: typing a letter navigates the list by name. Actual: once a row is selected, m/b/p (and -/=) trigger mixer actions and are swallowed before type-select can use them.",
          "suggestedFix": "Require a modifier for the action keys, or scope these handlers so they don't shadow list type-select, or document the tradeoff clearly.",
          "confidence": "likely",
          "primaryFile": "Sources/Waves/Features/Mixer/MainWindowView.swift",
          "disposition": "deferred:needs-user-decision:Behavioral product fork (single-key vs type-select tradeoff), not a behavior-preserving safe tweak; explicitly flagged to defer.",
          "uid": "WIN-09#3",
          "canonicalFile": "Sources/Waves/Features/Mixer/MainWindowView.swift"
        }
      ]
    }
  ]
}